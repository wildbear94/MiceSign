---
phase: 27-v1.1-verification-hygiene
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - .planning/phases/21-schemafieldeditor/21-HUMAN-UAT.md
  - .planning/phases/23-table-column-editor/23-HUMAN-UAT.md
autonomous: false
requirements: [CND-01, CND-02]
must_haves:
  truths:
    - "Phase 21 에 21-HUMAN-UAT.md 가 존재하고 수동 검증 항목이 pass 로 기록되어 있다"
    - "Phase 23 에 23-HUMAN-UAT.md 가 존재하고 수동 검증 항목이 pass 로 기록되어 있다"
  artifacts:
    - path: ".planning/phases/21-schemafieldeditor/21-HUMAN-UAT.md"
      provides: "Phase 21 수동 검증 기록 (status: complete)"
      contains: "status: complete"
    - path: ".planning/phases/23-table-column-editor/23-HUMAN-UAT.md"
      provides: "Phase 23 수동 검증 기록 (status: complete)"
      contains: "status: complete"
  key_links:
    - from: "21-VERIFICATION.md / 23-VERIFICATION.md"
      to: "해당 phase 의 HUMAN-UAT.md"
      via: "수동 검증 체크리스트 전이"
      pattern: "status: complete"
---

<objective>
Phase 21, 23 에 누락된 HUMAN-UAT.md 파일을 생성하고, 해당 phase 의 수동 검증 항목을 사용자와 함께 브라우저에서 확인하여 결과를 기록한다.

Purpose: v1.1-MILESTONE-AUDIT tech debt 중 HUMAN-UAT 파일 공백을 채운다. 실제 인간 확인이 필요한 항목이 있으므로 `autonomous: false`.
Output: 21-HUMAN-UAT.md, 23-HUMAN-UAT.md (status: complete).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-schemafieldeditor/21-VERIFICATION.md
@.planning/phases/21-schemafieldeditor/21-01-SUMMARY.md
@.planning/phases/23-table-column-editor/23-VERIFICATION.md
@.planning/phases/23-table-column-editor/23-01-SUMMARY.md
@.planning/phases/23-table-column-editor/23-02-SUMMARY.md
@.planning/phases/22-split-layout-live-preview/22-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 21-HUMAN-UAT.md 스켈레톤 생성 (status: partial)</name>
  <files>.planning/phases/21-schemafieldeditor/21-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/21-schemafieldeditor/21-VERIFICATION.md (수동 검증 항목 목록)
    - .planning/phases/21-schemafieldeditor/21-01-SUMMARY.md (리팩토링 범위)
    - .planning/phases/22-split-layout-live-preview/22-HUMAN-UAT.md (frontmatter 포맷 레퍼런스)
  </read_first>
  <action>
22-HUMAN-UAT.md 와 동일한 frontmatter/섹션 형식으로 새 파일을 생성한다. frontmatter:
(YAML frontmatter keys) status: partial / phase: 21-schemafieldeditor / source: [21-VERIFICATION.md] / started: 2026-04-14T00:00:00+09:00 / updated: 2026-04-14T00:00:00+09:00

Phase 21 Success Criteria(리팩토링 전후 기능 동등성) 기반으로 아래 4개 테스트 항목을 나열하고 result 는 모두 `[pending]` 로 기록:

### 1. 필드 추가/삭제 회귀
expected: SchemaFieldEditor 에서 필드 추가/삭제가 리팩토링 전과 동일하게 작동
result: [pending]

### 2. 필드 순서 변경 회귀
expected: @dnd-kit 드래그로 필드 순서를 변경하면 저장 후에도 순서 유지
result: [pending]

### 3. FieldConfigEditor 타입별 설정 회귀
expected: text/number/date/select/table 등 각 타입별 config 패널이 기존과 동일하게 표시·편집됨
result: [pending]

### 4. 양식 생성/편집 저장 회귀
expected: 리팩토링 후에도 새 양식 생성 및 기존 양식 편집 저장이 정상 완료됨
result: [pending]

Summary 블록은 22-HUMAN-UAT.md 와 동일한 키(total/passed/issues/pending/skipped/blocked)로 초기값 `total: 4, passed: 0, pending: 4, 나머지 0` 기록. 마지막에 빈 `## Gaps` 섹션 포함.
  </action>
  <verify>
    <automated>test -f .planning/phases/21-schemafieldeditor/21-HUMAN-UAT.md && grep -q "status: partial" .planning/phases/21-schemafieldeditor/21-HUMAN-UAT.md && grep -c "### " .planning/phases/21-schemafieldeditor/21-HUMAN-UAT.md 결과 4 이상</automated>
  </verify>
  <acceptance_criteria>
    - 파일 존재, frontmatter `status: partial`, `phase: 21-schemafieldeditor`
    - `### N.` 로 시작하는 테스트 항목 4개
    - `result: [pending]` 가 4회 등장
    - `## Summary` 블록 존재
  </acceptance_criteria>
  <done>21-HUMAN-UAT.md 생성, 4개 항목 pending</done>
</task>

<task type="auto">
  <name>Task 2: 23-HUMAN-UAT.md 스켈레톤 생성 (status: partial)</name>
  <files>.planning/phases/23-table-column-editor/23-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/23-table-column-editor/23-VERIFICATION.md
    - .planning/phases/23-table-column-editor/23-01-SUMMARY.md
    - .planning/phases/23-table-column-editor/23-02-SUMMARY.md
  </read_first>
  <action>
21-HUMAN-UAT.md 와 동일한 구조로 23-HUMAN-UAT.md 생성. frontmatter `status: partial`, `phase: 23-table-column-editor`, `source: [23-VERIFICATION.md]`, started/updated 는 2026-04-14.

Phase 23 Success Criteria (TBL-01, TBL-02) 기반 4개 항목:

### 1. 테이블 컬럼 추가/삭제
expected: table 필드 선택 시 TableColumnEditor 에서 컬럼 추가/삭제 버튼이 정상 동작

### 2. @dnd-kit 컬럼 드래그 순서 변경
expected: 컬럼을 드래그해서 순서를 변경하면 즉시 반영되고 저장 후에도 유지

### 3. 컬럼별 타입/라벨/필수 설정
expected: ColumnConfigPanel 에서 text/number/date/select 타입, 라벨, 필수여부를 설정할 수 있음

### 4. 미리보기 테이블 실시간 반영
expected: 컬럼 변경사항이 FormPreview 의 테이블에 실시간 반영됨

각 항목 result 는 `[pending]`. Summary 블록 `total: 4, pending: 4, 나머지 0`. 빈 `## Gaps` 섹션.
  </action>
  <verify>
    <automated>test -f .planning/phases/23-table-column-editor/23-HUMAN-UAT.md && grep -q "phase: 23-table-column-editor" .planning/phases/23-table-column-editor/23-HUMAN-UAT.md && grep -c "result: \[pending\]" .planning/phases/23-table-column-editor/23-HUMAN-UAT.md 결과 4</automated>
  </verify>
  <acceptance_criteria>
    - 파일 존재, frontmatter 올바름
    - 테스트 항목 4개, 모두 pending
    - Summary 블록 존재
  </acceptance_criteria>
  <done>23-HUMAN-UAT.md 생성, 4개 항목 pending</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 21/23 수동 브라우저 UAT 실행 및 결과 기록</name>
  <what-built>
21-HUMAN-UAT.md (4개 항목) + 23-HUMAN-UAT.md (4개 항목) 체크리스트가 pending 상태로 준비되어 있음. Phase 21 은 SchemaFieldEditor 리팩토링 후 기능 동등성 확인, Phase 23 은 table 컬럼 편집기 신규 기능 동작 확인.
  </what-built>
  <how-to-verify>
1. `cd frontend && npm run dev` 로 개발 서버 기동
2. 관리자 계정으로 로그인 → 양식 관리 → 새 양식 생성 또는 기존 CUSTOM 양식 편집 진입
3. **Phase 21 항목 (21-HUMAN-UAT.md)**:
   - (1) 필드 추가/삭제: 텍스트/숫자/날짜/선택 필드를 추가하고 삭제 버튼으로 제거
   - (2) 드래그 순서변경: 필드 카드를 드래그해 순서 바꾸고 저장 → 다시 편집 시 순서 유지 확인
   - (3) 각 타입별 FieldConfigEditor 패널 열어서 라벨/필수/옵션 등 설정이 정상 표시되는지 확인
   - (4) 저장 → 목록에서 해당 양식 재진입 시 설정이 보존되는지 확인
4. **Phase 23 항목 (23-HUMAN-UAT.md)**:
   - (1) table 필드 추가 → 컬럼 추가/삭제 버튼 동작 확인
   - (2) 컬럼을 드래그해서 순서 변경 후 저장, 재진입 시 순서 유지
   - (3) 각 컬럼의 타입(text/number/date/select)/라벨/required 토글 설정
   - (4) 우측 FormPreview 패널의 테이블에 컬럼 변경이 실시간 반영되는지 확인
5. 사용자는 각 항목을 통과(pass)/실패(issue)/차단(blocked) 중 하나로 보고
6. 보고 결과를 받은 후 Claude 가 두 파일을 업데이트:
   - 각 항목 `result: [pending]` → `result: pass` 또는 `result: issue — {설명}` 로 교체
   - frontmatter `status: partial` → `status: complete`, `updated` 필드 갱신
   - Summary 블록의 passed/pending 숫자 반영
   - 만약 issue 가 있으면 `## Gaps` 섹션에 항목 기록 (후속 phase 에서 처리)
  </how-to-verify>
  <resume-signal>
사용자가 각 8개 항목에 대해 "pass" 또는 "issue: {내용}" 을 알려주면 Claude 가 두 파일을 업데이트하고 status 를 complete 로 전환한다.
  </resume-signal>
  <acceptance_criteria>
    - 21-HUMAN-UAT.md frontmatter `status: complete`
    - 21-HUMAN-UAT.md 에 `result: [pending]` 가 0회 등장
    - 23-HUMAN-UAT.md frontmatter `status: complete`
    - 23-HUMAN-UAT.md 에 `result: [pending]` 가 0회 등장
    - Summary 블록의 passed + issues + blocked + skipped = total, pending = 0
  </acceptance_criteria>
  <done>양 파일 status: complete, 8개 항목 전원 pending 해제</done>
</task>

</tasks>

<verification>
- 두 파일 frontmatter `status: complete`
- `result: [pending]` 문자열이 두 파일 통틀어 0회 등장
- Summary 블록 숫자 일관성 (passed + issues + skipped + blocked == total)
</verification>

<success_criteria>
Phase 21 과 Phase 23 에 HUMAN-UAT.md 가 존재하고 모든 수동 검증 항목이 인간 확인을 거쳐 pass 또는 issue 로 기록되어 status 가 complete 이다.
</success_criteria>

<output>
`.planning/phases/27-v1.1-verification-hygiene/27-02-SUMMARY.md` 생성.
</output>
</content>
</invoke>