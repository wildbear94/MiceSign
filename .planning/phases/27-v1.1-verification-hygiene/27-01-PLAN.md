---
phase: 27-v1.1-verification-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/admin/components/TemplateFormModal.tsx
autonomous: true
requirements: [CND-01, CND-02]
must_haves:
  truths:
    - "편집 모드에서 detailQuery.data 는 있으나 schemaDefinition 이 없는 분기가 conditionalRules 상태를 빈 배열로 리셋한다"
    - "TypeScript 빌드가 통과한다"
  artifacts:
    - path: "frontend/src/features/admin/components/TemplateFormModal.tsx"
      provides: "설치형 모달 편집 모드 빈 스키마 분기 리셋 대칭"
      contains: "setConditionalRules([])"
  key_links:
    - from: "TemplateFormModal.tsx L132-135 else-if 분기"
      to: "conditionalRules state"
      via: "setConditionalRules setter 호출"
      pattern: "setConditionalRules\\(\\[\\]\\)"
---

<objective>
v1.1-MILESTONE-AUDIT 의 통합 FLAG 1건을 해소한다. 편집 모드에서 `detailQuery.data` 는 있으나 `schemaDefinition` 이 없는 분기에 `setConditionalRules([])` 한 줄을 추가하여 `setSchemaFields`/`setCalculationRules` 와 대칭을 맞춘다.

Purpose: 모달 재사용 시 이전 편집 세션의 conditionalRules 누수 방지 (CND-01/CND-02 regression protection).
Output: TemplateFormModal.tsx 1 line diff + tsc 빌드 통과.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/v1.1-MILESTONE-AUDIT.md
@frontend/src/features/admin/components/TemplateFormModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: TemplateFormModal 빈 스키마 분기 conditionalRules 리셋 추가</name>
  <files>frontend/src/features/admin/components/TemplateFormModal.tsx</files>
  <read_first>
    - frontend/src/features/admin/components/TemplateFormModal.tsx (L118-137 useEffect 블록 전체)
    - .planning/v1.1-MILESTONE-AUDIT.md (섹션 3 FLAG 상세)
  </read_first>
  <action>
TemplateFormModal.tsx 의 편집 모드 schema 로드 useEffect(L119-137) 내 else-if 분기를 다음과 같이 수정한다:

BEFORE (현재 상태 — 이미 working tree 에 적용되어 있을 수 있음, 반드시 재확인):
```tsx
} else if (detailQuery.data && !detailQuery.data.schemaDefinition) {
  setSchemaFields([]);
  setCalculationRules([]);
}
```

AFTER (정확히 아래 형태로 존재해야 함):
```tsx
} else if (detailQuery.data && !detailQuery.data.schemaDefinition) {
  setSchemaFields([]);
  setConditionalRules([]);
  setCalculationRules([]);
}
```

순서가 중요: `setSchemaFields` → `setConditionalRules` → `setCalculationRules` (L122-126 try 블록과 L127-130 catch 블록과 대칭). `git diff` 결과 이미 이 형태로 존재하면 추가 편집 없이 그대로 두고 검증만 수행한다 (working tree 에 unstaged change 로 이미 적용됨 — 2026-04-14 기준).
  </action>
  <verify>
    <automated>grep -n "setConditionalRules(\[\])" frontend/src/features/admin/components/TemplateFormModal.tsx | wc -l 결과가 2 이상 (catch 블록 1건 + else-if 분기 1건); cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "setConditionalRules(\[\])" frontend/src/features/admin/components/TemplateFormModal.tsx` 가 2 이상
    - else-if 분기(`!detailQuery.data.schemaDefinition`) 내부에 `setConditionalRules([])` 가 존재
    - `setSchemaFields` / `setConditionalRules` / `setCalculationRules` 3줄이 연속으로 나타남
    - `cd frontend && npx tsc --noEmit` 종료코드 0
    - 해당 파일 외 다른 파일 수정 없음
  </acceptance_criteria>
  <done>else-if 분기에 setConditionalRules([]) 포함, tsc 통과</done>
</task>

</tasks>

<verification>
- grep 로 setConditionalRules 호출이 최소 3곳(initialValues 리셋, catch, else-if)에 존재하는지 확인
- frontend tsc --noEmit 통과
- 기존 관리자 UAT 회귀 수동 확인은 Phase 27 plan 02/03 에서 수행
</verification>

<success_criteria>
TemplateFormModal.tsx L132-135 블록에 setConditionalRules([]) 가 추가되어 있고 TypeScript 빌드가 통과한다.
</success_criteria>

<output>
`.planning/phases/27-v1.1-verification-hygiene/27-01-SUMMARY.md` 생성.
</output>
</content>
</invoke>