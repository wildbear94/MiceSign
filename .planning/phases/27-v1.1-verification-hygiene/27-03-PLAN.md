---
phase: 27-v1.1-verification-hygiene
plan: 03
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - .planning/phases/22-split-layout-live-preview/22-HUMAN-UAT.md
  - .planning/phases/24-ui/24-HUMAN-UAT.md
autonomous: false
requirements: [CND-01, CND-02]
must_haves:
  truths:
    - "22-HUMAN-UAT.md 의 모든 체크리스트 항목이 pending 이 아닌 상태(pass/issue)로 기록되어 있다"
    - "24-HUMAN-UAT.md 의 모든 체크리스트 항목이 pending 이 아닌 상태(pass/issue)로 기록되어 있다"
    - "두 파일 frontmatter status 가 partial → complete 로 전환되었다"
  artifacts:
    - path: ".planning/phases/22-split-layout-live-preview/22-HUMAN-UAT.md"
      provides: "Phase 22 수동 검증 완료 기록"
      contains: "status: complete"
    - path: ".planning/phases/24-ui/24-HUMAN-UAT.md"
      provides: "Phase 24 수동 검증 완료 기록 (CND-01/02 regression)"
      contains: "status: complete"
  key_links:
    - from: "22/24 HUMAN-UAT 체크리스트 항목"
      to: "v1.1 milestone tech_debt 해소"
      via: "status: partial → complete 전환"
      pattern: "status: complete"
---

<objective>
Phase 22 와 Phase 24 의 기존 HUMAN-UAT.md 파일이 `status: partial` 이고 각 항목이 `result: [pending]` 로 남아있는 상태를 해소한다. 사용자와 함께 브라우저 UAT 를 실행하고 결과를 기록하여 status 를 complete 로 전환한다.

Purpose: v1.1-MILESTONE-AUDIT tech debt 중 Phase 22/24 HUMAN-UAT partial 상태 청산. Phase 24 는 CND-01/CND-02 의 regression protection 과 직결.
Output: 22-HUMAN-UAT.md, 24-HUMAN-UAT.md (status: complete).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-split-layout-live-preview/22-HUMAN-UAT.md
@.planning/phases/22-split-layout-live-preview/22-VERIFICATION.md
@.planning/phases/24-ui/24-HUMAN-UAT.md
@.planning/phases/24-ui/24-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Phase 22 분할 레이아웃 + 라이브 미리보기 수동 UAT 실행</name>
  <what-built>
22-HUMAN-UAT.md 에 5개 항목(분할 레이아웃/실시간 미리보기/전체화면 포탈+ESC/미리보기 토글/기존 기능 회귀)이 pending 상태로 등록되어 있음.
  </what-built>
  <how-to-verify>
1. `cd frontend && npm run dev` 실행 후 관리자로 로그인, 양식 관리 → 새 양식 생성 클릭
2. **항목 1 (분할 레이아웃)**: 모달이 95vw/95vh 크기로 열리고 좌측(편집)/우측(미리보기) 50:50 분할로 표시되는지 확인
3. **항목 2 (실시간 미리보기)**: 좌측에서 텍스트/숫자/선택 필드를 추가·수정·삭제하며 우측 미리보기가 즉시 업데이트 되는지 확인
4. **항목 3 (전체화면 포탈 + ESC)**: 미리보기 패널의 Maximize2 버튼 클릭 → 전체화면 포탈(z-[60]) 표시 → ESC 누르면 전체화면만 닫히고 편집 모달은 유지되는지 확인
5. **항목 4 (미리보기 토글)**: EyeOff 버튼으로 미리보기 숨김 → 편집 영역 전체 너비 확장, Eye 로 복원 시 50:50 재분할
6. **항목 5 (기존 기능 회귀)**: 필드 몇 개 입력 후 저장 → 목록 노출 → 재편집 시 동일 내용 로드
7. 각 항목에 대해 사용자가 pass / issue: {설명} / blocked 보고
8. Claude 가 22-HUMAN-UAT.md 업데이트:
   - `result: [pending]` → 해당 결과로 교체
   - frontmatter `updated` 필드 현재 시각으로 갱신
   - 모든 항목 처리 후 `status: partial` → `status: complete`
   - Summary 블록 숫자 재계산 (passed/issues/pending/blocked)
   - issue 가 있으면 `## Gaps` 섹션에 기록
  </how-to-verify>
  <resume-signal>
사용자가 5개 항목 각각에 대해 pass/issue/blocked 결과를 보고하면 파일 업데이트.
  </resume-signal>
  <acceptance_criteria>
    - 22-HUMAN-UAT.md `status: complete`
    - `result: [pending]` 문자열 0회
    - Summary 블록 `pending: 0`, 합계 일치
  </acceptance_criteria>
  <done>22-HUMAN-UAT.md status: complete, 5개 항목 전원 pending 해제</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 24 조건부 표시 규칙 UI 수동 UAT 실행</name>
  <what-built>
24-HUMAN-UAT.md 에 4개 항목(조건 규칙 설정 UI / 미리보기 인터랙티브 / 필드 삭제 시 규칙 정리 토스트 / 저장·로드 라운드트립)이 pending 상태로 등록되어 있음. 이 plan 은 CND-01/CND-02 regression protection 을 담당. Plan 27-01 의 conditionalRules 리셋 수정도 여기서 간접 확인됨.
  </what-built>
  <how-to-verify>
1. 개발 서버(`npm run dev`) 기동, 관리자 양식 편집 화면 진입
2. **항목 1 (조건 규칙 UI)**:
   - 필드 카드의 "조건" 탭 오픈 → IF 필드 드롭다운이 현재 필드를 제외하고 필터링되는지 확인
   - IF-THEN 규칙 추가 → Zap 배지가 필드 카드에 표시되는지 확인
   - 체크박스 목록으로 표시/숨김/필수/선택 대상 필드 토글
3. **항목 2 (미리보기 인터랙티브)**:
   - 미리보기 패널에서 IF 조건 필드에 값을 입력 → 즉시 THEN 대상 필드의 표시/숨김이 전환되는지 확인
   - 미리보기 초기화 버튼 동작 확인
4. **항목 3 (필드 삭제 시 규칙 정리 토스트)**:
   - 조건 규칙이 참조하는 필드를 삭제 시도 → window.confirm 표시 → 확인 → 토스트 `조건 규칙 N개가 자동 제거되었습니다` 표시 확인
5. **항목 4 (저장·로드 라운드트립)**:
   - 조건 규칙 설정 후 저장 → 양식 목록 재진입 → 규칙 유지되는지 확인
   - **추가 regression check (27-01 FLAG)**: schemaDefinition 이 비어있는 기존 템플릿(있다면)을 편집해서 열었다가 다른 탭 전환, 모달 재오픈 시 이전 세션의 conditionalRules 가 누수되지 않는지 확인
6. 사용자가 4개 항목 + regression check 결과 보고
7. Claude 가 24-HUMAN-UAT.md 업데이트:
   - result 채우기, status partial → complete, updated 갱신
   - Summary 숫자 재계산
   - regression check 결과를 항목 4 result 에 병기 (예: `pass — 조건 규칙 round-trip 정상, 27-01 FLAG regression 도 확인됨`)
  </how-to-verify>
  <resume-signal>
사용자가 4개 항목 + 27-01 regression 결과를 보고하면 파일 업데이트.
  </resume-signal>
  <acceptance_criteria>
    - 24-HUMAN-UAT.md `status: complete`
    - `result: [pending]` 문자열 0회
    - Summary 블록 `pending: 0`, 합계 일치
    - 27-01 FLAG regression 확인 노트가 해당 항목 result 에 포함
  </acceptance_criteria>
  <done>24-HUMAN-UAT.md status: complete, 4개 항목 전원 pending 해제, CND-01/02 regression 재확인</done>
</task>

<task type="auto">
  <name>Task 3: TypeScript 빌드 + 기존 UAT 회귀 재확인</name>
  <files>(no file modifications)</files>
  <read_first>
    - frontend/package.json (빌드 스크립트 확인용)
  </read_first>
  <action>
`cd frontend && npx tsc --noEmit` 실행하여 전체 TypeScript 빌드가 통과하는지 확인. 실패 시 오류를 사용자에게 보고하고 중단. 성공 시 27-03 SUMMARY 에 빌드 통과 기록.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` 종료 코드 0
    - 신규 TypeScript 오류 0건
  </acceptance_criteria>
  <done>TypeScript 빌드 통과 확인</done>
</task>

</tasks>

<verification>
- 22/24-HUMAN-UAT.md 두 파일 모두 status: complete, pending 항목 0
- frontend tsc --noEmit 통과
- v1.1-MILESTONE-AUDIT 의 HUMAN-UAT tech debt 4건(21/22/23/24) 모두 해소됨 (Plan 27-02 + 27-03 합산)
</verification>

<success_criteria>
Phase 22, 24 의 HUMAN-UAT 체크리스트가 채워지고 status 가 partial → complete 로 전환되었으며, TypeScript 빌드와 기존 UAT 회귀가 그대로 통과한다.
</success_criteria>

<output>
`.planning/phases/27-v1.1-verification-hygiene/27-03-SUMMARY.md` 생성.
</output>
</content>
</invoke>