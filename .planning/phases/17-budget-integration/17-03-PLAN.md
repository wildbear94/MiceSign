---
phase: 17-budget-integration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/micesign/service/DocumentService.java
  - backend/src/main/java/com/micesign/budget/RealBudgetApiClient.java
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [BDGT-01, BDGT-02]

must_haves:
  truths:
    - "재무 문서(지출 결의, 구매 요청) 제출 시 BudgetIntegrationEvent가 발행되어 BudgetIntegrationService가 호출된다"
    - "문서 회수(withdraw) 시 BudgetCancellationEvent가 발행되어 취소 플로우가 작동한다"
    - "RealBudgetApiClient의 @Retryable/@Recover 매칭이 label 속성으로 명시적이다"
  artifacts:
    - path: "backend/src/main/java/com/micesign/service/DocumentService.java"
      provides: "BudgetIntegrationEvent 및 BudgetCancellationEvent 발행"
      contains: "BudgetIntegrationEvent"
    - path: "backend/src/main/java/com/micesign/budget/RealBudgetApiClient.java"
      provides: "명시적 @Retryable label 바인딩"
      contains: "label ="
    - path: ".planning/REQUIREMENTS.md"
      provides: "BDGT-01, BDGT-02 요구사항 추적"
      contains: "BDGT-01"
  key_links:
    - from: "DocumentService.submitDocument()"
      to: "BudgetIntegrationService.handleBudgetEvent()"
      via: "applicationEventPublisher.publishEvent(new BudgetIntegrationEvent(...))"
      pattern: "publishEvent.*BudgetIntegrationEvent"
    - from: "DocumentService.withdrawDocument()"
      to: "BudgetIntegrationService.handleCancellationEvent()"
      via: "applicationEventPublisher.publishEvent(new BudgetCancellationEvent(...))"
      pattern: "publishEvent.*BudgetCancellationEvent"
---

<objective>
Phase 17 검증에서 발견된 핵심 gap을 수정한다: BudgetIntegrationEvent/BudgetCancellationEvent가 어디서도 발행되지 않아 예산 시스템 연동이 실제로 작동하지 않는 문제.

Purpose: BudgetIntegrationService 핸들러는 완전히 구현되어 있으나, DocumentService가 이벤트를 발행하지 않아 연결 고리가 끊어져 있다. 이 plan이 이벤트 발행을 추가하여 전체 파이프라인을 완성한다.
Output: 문서 제출/회수 시 예산 이벤트가 발행되고, @Retryable label이 수정된 코드
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-budget-integration/17-01-SUMMARY.md
@.planning/phases/17-budget-integration/17-02-SUMMARY.md
@.planning/phases/17-budget-integration/17-VERIFICATION.md

<interfaces>
<!-- BudgetIntegrationEvent 생성자 — DocumentService에서 사용해야 함 -->
From backend/src/main/java/com/micesign/event/BudgetIntegrationEvent.java:
```java
public BudgetIntegrationEvent(Long documentId, String templateCode, String docNumber, Long drafterId)
```

From backend/src/main/java/com/micesign/event/BudgetCancellationEvent.java:
```java
public BudgetCancellationEvent(Long documentId, String templateCode, String docNumber, Long actorUserId, String reason)
// reason: "REJECTED" or "WITHDRAWN"
```

From backend/src/main/java/com/micesign/service/DocumentService.java (현재 상태):
- Line 15-16: `import com.micesign.event.ApprovalNotificationEvent;` + `import com.micesign.domain.enums.NotificationEventType;`
- Line 31: `import org.springframework.context.ApplicationEventPublisher;` (이미 inject됨)
- Line 219: `applicationEventPublisher.publishEvent(new ApprovalNotificationEvent(...))` (submitDocument 내)
- Line 310: `applicationEventPublisher.publishEvent(new ApprovalNotificationEvent(...))` (withdrawDocument 내)

From backend/src/main/java/com/micesign/budget/RealBudgetApiClient.java (현재 상태):
- Line 32-35: `@Retryable(retryFor = {RestClientException.class}, maxAttempts = 3, ...)` on sendExpenseData (label 없음)
- Line 55-58: `@Retryable(retryFor = {RestClientException.class}, maxAttempts = 3, ...)` on sendCancellation (label 없음)
- Line 47: `@Recover` recoverSendExpenseData
- Line 70: `@Recover` recoverSendCancellation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DocumentService에 BudgetIntegrationEvent/BudgetCancellationEvent 발행 추가</name>
  <files>backend/src/main/java/com/micesign/service/DocumentService.java</files>
  <read_first>
    - backend/src/main/java/com/micesign/service/DocumentService.java
    - backend/src/main/java/com/micesign/event/BudgetIntegrationEvent.java
    - backend/src/main/java/com/micesign/event/BudgetCancellationEvent.java
    - backend/src/main/java/com/micesign/budget/BudgetIntegrationService.java
  </read_first>
  <action>
    DocumentService.java에 두 가지 이벤트 발행을 추가한다.

    1. **import 추가** (파일 상단, 기존 import 블록에):
       ```java
       import com.micesign.event.BudgetIntegrationEvent;
       import com.micesign.event.BudgetCancellationEvent;
       ```

    2. **submitDocument() 메서드** — 기존 ApprovalNotificationEvent 발행 직후 (line 219 이후)에 추가:
       ```java
       // Budget integration event (async, non-blocking)
       applicationEventPublisher.publishEvent(
               new BudgetIntegrationEvent(document.getId(), document.getTemplateCode(),
                       document.getDocNumber(), userId));
       ```
       이 코드는 `applicationEventPublisher.publishEvent(new ApprovalNotificationEvent(...))` 바로 다음에 위치해야 한다.
       BudgetIntegrationService.handleBudgetEvent()가 @TransactionalEventListener(AFTER_COMMIT) + @Async로 처리하므로, submitDocument 트랜잭션을 차단하지 않는다. budget_enabled 여부 확인은 BudgetIntegrationService 내부에서 수행하므로 여기서는 모든 문서에 대해 이벤트를 발행한다.

    3. **withdrawDocument() 메서드** — 기존 ApprovalNotificationEvent 발행 직후 (line 310 이후)에 추가:
       ```java
       // Budget cancellation event (async, non-blocking)
       applicationEventPublisher.publishEvent(
               new BudgetCancellationEvent(document.getId(), document.getTemplateCode(),
                       document.getDocNumber(), userId, "WITHDRAWN"));
       ```

    applicationEventPublisher는 이미 DocumentService에 inject되어 있으므로 생성자 수정은 불필요하다.
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign && grep -n "BudgetIntegrationEvent" backend/src/main/java/com/micesign/service/DocumentService.java && grep -n "BudgetCancellationEvent" backend/src/main/java/com/micesign/service/DocumentService.java</automated>
  </verify>
  <acceptance_criteria>
    - DocumentService.java에 `import com.micesign.event.BudgetIntegrationEvent;` 존재
    - DocumentService.java에 `import com.micesign.event.BudgetCancellationEvent;` 존재
    - submitDocument() 메서드 내에 `publishEvent(new BudgetIntegrationEvent(` 존재
    - withdrawDocument() 메서드 내에 `publishEvent(new BudgetCancellationEvent(` 존재
    - BudgetCancellationEvent 생성자의 마지막 인자가 `"WITHDRAWN"` 문자열
    - `./gradlew compileJava` 컴파일 성공
  </acceptance_criteria>
  <done>DocumentService.submitDocument()가 BudgetIntegrationEvent를, withdrawDocument()가 BudgetCancellationEvent를 발행하여 BudgetIntegrationService 핸들러가 실제로 호출된다</done>
</task>

<task type="auto">
  <name>Task 2: RealBudgetApiClient @Retryable label 수정 및 REQUIREMENTS.md 업데이트</name>
  <files>backend/src/main/java/com/micesign/budget/RealBudgetApiClient.java, .planning/REQUIREMENTS.md</files>
  <read_first>
    - backend/src/main/java/com/micesign/budget/RealBudgetApiClient.java
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    **1. RealBudgetApiClient.java — @Retryable label 추가:**

    sendExpenseData 메서드의 @Retryable 어노테이션을 다음으로 변경:
    ```java
    @Retryable(
        label = "sendExpenseData",
        retryFor = {RestClientException.class},
        maxAttempts = 3,
        backoff = @Backoff(delay = 2000, multiplier = 1.5)
    )
    ```

    sendCancellation 메서드의 @Retryable 어노테이션을 다음으로 변경:
    ```java
    @Retryable(
        label = "sendCancellation",
        retryFor = {RestClientException.class},
        maxAttempts = 3,
        backoff = @Backoff(delay = 2000, multiplier = 1.5)
    )
    ```

    이렇게 하면 Spring Retry가 2개의 @Recover 메서드(recoverSendExpenseData, recoverSendCancellation) 중 올바른 메서드를 매칭할 때 ambiguity가 해소된다.

    **2. REQUIREMENTS.md 업데이트:**

    `## v2 Requirements` 섹션 앞에 (또는 `### Additional Templates` 다음에) 새 섹션 추가:
    ```markdown
    ### Budget Integration (v1.2)

    - [x] **BDGT-01**: 재무 문서(지출 결의, 구매 요청) 제출 시 외부 예산 시스템에 REST API로 지출 데이터를 자동 전송
    - [x] **BDGT-02**: API 호출 실패 시 설정된 횟수만큼 재시도하고 모든 시도를 로깅하며, 문서 제출 워크플로를 차단하지 않음
    ```

    Traceability 표에도 추가 (기존 행 끝에):
    ```
    | BDGT-01 | Phase 17: Budget Integration | Complete |
    | BDGT-02 | Phase 17: Budget Integration | Complete |
    ```

    Coverage 숫자도 업데이트: `v1 requirements: 38 total` (36+2), `Mapped to phases: 38`.
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign && grep -n 'label = "sendExpenseData"' backend/src/main/java/com/micesign/budget/RealBudgetApiClient.java && grep -n 'label = "sendCancellation"' backend/src/main/java/com/micesign/budget/RealBudgetApiClient.java && grep "BDGT-01" .planning/REQUIREMENTS.md && grep "BDGT-02" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - RealBudgetApiClient.java의 sendExpenseData @Retryable에 `label = "sendExpenseData"` 존재
    - RealBudgetApiClient.java의 sendCancellation @Retryable에 `label = "sendCancellation"` 존재
    - REQUIREMENTS.md에 `BDGT-01` 행 존재
    - REQUIREMENTS.md에 `BDGT-02` 행 존재
    - REQUIREMENTS.md Traceability 표에 `BDGT-01 | Phase 17` 존재
    - REQUIREMENTS.md Traceability 표에 `BDGT-02 | Phase 17` 존재
    - `./gradlew compileJava` 컴파일 성공
  </acceptance_criteria>
  <done>@Retryable/@Recover 매칭이 명시적이고, BDGT-01/BDGT-02가 REQUIREMENTS.md에서 추적 가능하다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DocumentService → BudgetIntegrationService | 이벤트 기반 비동기 호출, 제출 트랜잭션에 영향 없음 |
| BudgetApiClient → 외부 예산 API | 외부 시스템으로의 REST 호출 (기존 구현) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | D (Denial of Service) | Budget event 대량 발행 | accept | 이벤트는 문서당 1회만 발행, @Async로 처리되어 제출 플로우 미차단 |
| T-17-02 | I (Information Disclosure) | Budget API 로그 | accept | 기존 로깅 인프라 사용, 민감 정보는 BudgetIntegrationLog에 저장 (기존 구현) |
</threat_model>

<verification>
1. `grep -c "BudgetIntegrationEvent" backend/src/main/java/com/micesign/service/DocumentService.java` → 2 이상 (import + publishEvent)
2. `grep -c "BudgetCancellationEvent" backend/src/main/java/com/micesign/service/DocumentService.java` → 2 이상 (import + publishEvent)
3. `grep 'label = "send' backend/src/main/java/com/micesign/budget/RealBudgetApiClient.java` → 2 매치
4. `grep "BDGT-0" .planning/REQUIREMENTS.md` → BDGT-01, BDGT-02 존재
5. `./gradlew compileJava` → BUILD SUCCESSFUL
</verification>

<success_criteria>
- DocumentService.submitDocument()가 BudgetIntegrationEvent를 발행하여 BudgetIntegrationService.handleBudgetEvent()가 트리거됨
- DocumentService.withdrawDocument()가 BudgetCancellationEvent를 발행하여 BudgetIntegrationService.handleCancellationEvent()가 트리거됨
- RealBudgetApiClient의 @Retryable에 label이 있어 @Recover 메서드 매칭이 명시적
- BDGT-01, BDGT-02가 REQUIREMENTS.md에서 추적 가능
</success_criteria>

<output>
After completion, create `.planning/phases/17-budget-integration/17-03-SUMMARY.md`
</output>
