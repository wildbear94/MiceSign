---
phase: 16-template-migration
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - frontend/src/features/document/types/document.ts
  - frontend/src/features/document/pages/DocumentEditorPage.tsx
  - frontend/src/features/document/pages/DocumentDetailPage.tsx
autonomous: false
requirements: [MIGR-02, MIGR-03]

must_haves:
  truths:
    - "신규 문서를 마이그레이션된 템플릿(GENERAL 등)으로 생성하면 동적 렌더러(DynamicForm)가 사용된다"
    - "기존 DRAFT 문서를 편집하면 하드코딩 컴포넌트가 계속 사용된다"
    - "동적 템플릿으로 생성된 문서의 상세 페이지에서 DynamicReadOnly가 정상 렌더링���다"
    - "기존 SUBMITTED 문서의 상세 페이지가 변경 없이 정상 표시된다"
  artifacts:
    - path: "frontend/src/features/document/types/document.ts"
      provides: "schemaDefinitionSnapshot 필드가 DocumentDetailResponse에 추가"
      contains: "schemaDefinitionSnapshot"
    - path: "frontend/src/features/document/pages/DocumentEditorPage.tsx"
      provides: "스키마 존재 시 동적 렌더러 우선 분기 로직"
      contains: "schemaDefinition"
    - path: "frontend/src/features/document/pages/DocumentDetailPage.tsx"
      provides: "DynamicReadOnly 폴백 통합"
      contains: "DynamicReadOnly"
  key_links:
    - from: "DocumentEditorPage.tsx"
      to: "DynamicForm"
      via: "template schemaDefinition 존재 여부로 분기"
      pattern: "isDynamicTemplate"
    - from: "DocumentDetailPage.tsx"
      to: "DynamicReadOnly"
      via: "schemaDefinitionSnapshot 존재 여부로 폴백"
      pattern: "schemaDefinitionSnapshot"
---

<objective>
프론트엔드 듀얼 렌더링을 완성하여 신규 문서는 동적 렌더러를, 기존 문서는 하드코딩 컴��넌트를 사용하도록 한다.

Purpose: MIGR-02(듀얼 렌더링) 프론트엔드 완성, MIGR-03(기존 문서 정상 표시) 최종 검증.
Output: DocumentDetailResponse 타입에 schemaDefinitionSnapshot 추가, DocumentEditorPage 분기 로직 수정, DocumentDetailPage에 DynamicReadOnly 통합, 수동 검증 체크포인트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-template-migration/16-CONTEXT.md
@.planning/phases/16-template-migration/16-RESEARCH.md
@.planning/phases/16-template-migration/16-UI-SPEC.md
@.planning/phases/16-template-migration/16-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성/수정된 백엔드 계약 -->

DocumentDetailResponse (Plan 01에서 수정됨):
```java
public record DocumentDetailResponse(
    Long id, String docNumber, String templateCode, String templateName,
    String title, String status, DrafterInfo drafter,
    String bodyHtml, String formData,
    String schemaDefinitionSnapshot,  // NEW — Plan 01에서 추가
    LocalDateTime submittedAt, LocalDateTime completedAt,
    LocalDateTime createdAt, LocalDateTime updatedAt
)
```

From frontend/src/features/document/types/document.ts (현재):
```typescript
export interface DocumentDetailResponse extends DocumentResponse {
  drafter: DrafterInfo;
  bodyHtml: string | null;
  formData: string | null;
  submittedAt: string | null;
  completedAt: string | null;
}
// 누락: schemaDefinitionSnapshot
```

From frontend/src/features/document/components/templates/DynamicReadOnly.tsx:
```typescript
interface DynamicReadOnlyProps extends TemplateReadOnlyProps {
  schemaDefinitionSnapshot?: string | null;
}
export default function DynamicReadOnly({ formData, schemaDefinitionSnapshot }: DynamicReadOnlyProps)
```

From frontend/src/features/document/components/templates/DynamicForm.tsx:
```typescript
// templateCode를 받아 API에서 schema를 로드하여 렌더링
interface DynamicFormProps {
  templateCode: string;
  documentId: number | null;
  initialData: { title: string; formData: string };
  onSave: (data: { title: string; bodyHtml?: string; formData?: string }) => Promise<void>;
}
```

From frontend/src/features/document/components/templates/templateRegistry.ts:
```typescript
export const TEMPLATE_REGISTRY: Record<string, TemplateEntry> = {
  GENERAL: { ... }, EXPENSE: { ... }, LEAVE: { ... }
};
```

From frontend/src/features/document/pages/DocumentEditorPage.tsx (현재 분기):
```typescript
const templateEntry = TEMPLATE_REGISTRY[resolvedTemplateCode];
const isDynamicTemplate = !templateEntry;
// templateEntry 존재 -> EditComponent (하드코딩)
// templateEntry 없음 -> DynamicForm (동적)
```

From frontend/src/features/document/pages/DocumentDetailPage.tsx (현재):
```typescript
const templateEntry = TEMPLATE_REGISTRY[doc.templateCode];
const ReadOnlyComponent = templateEntry?.readOnlyComponent;
// ReadOnlyComponent 존재 -> 하드코딩 읽기
// 없으�� -> "알 수 없는 양식입니다." (갭)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 프론트엔드 타입 + 편집/상세 페이지 듀얼 렌더링 완성</name>
  <files>
    frontend/src/features/document/types/document.ts,
    frontend/src/features/document/pages/DocumentEditorPage.tsx,
    frontend/src/features/document/pages/DocumentDetailPage.tsx
  </files>
  <read_first>
    - frontend/src/features/document/types/document.ts
    - frontend/src/features/document/pages/DocumentEditorPage.tsx
    - frontend/src/features/document/pages/DocumentDetailPage.tsx
    - frontend/src/features/document/components/templates/templateRegistry.ts
    - frontend/src/features/document/components/templates/DynamicForm.tsx
    - frontend/src/features/document/components/templates/DynamicReadOnly.tsx
    - frontend/src/features/document/hooks/useDocuments.ts
    - frontend/src/features/document/api/documentApi.ts
    - .planning/phases/16-template-migration/16-UI-SPEC.md
    - .planning/phases/16-template-migration/16-01-SUMMARY.md
  </read_first>
  <action>
3개 파일을 수정하여 프론트엔드 듀얼 렌더링을 완성한다 (per D-01).

**(A) document.ts — schemaDefinitionSnapshot 필드 추가:**

DocumentDetailResponse 인터페이스에 필드 추가:
```typescript
export interface DocumentDetailResponse extends DocumentResponse {
  drafter: DrafterInfo;
  bodyHtml: string | null;
  formData: string | null;
  schemaDefinitionSnapshot: string | null;  // NEW
  submittedAt: string | null;
  completedAt: string | null;
}
```

**(B) DocumentEditorPage.tsx — 분기 로직 수정:**

핵심 원�� (per D-01, RESEARCH.md Pitfall 5):
- 기존 DRAFT 문서(마이그레이션 전 생성): 하드코딩 컴포넌트 유지
- 신규 문서: 템플릿에 schema_definition이 있으면 DynamicForm 사용

분기 로직을 다음과 같이 변경:
```typescript
const templateEntry = TEMPLATE_REGISTRY[resolvedTemplateCode];

// 기존 문서 편집 시: schemaDefinitionSnapshot이 있으면 동적, 없���면 하드코딩
// 신규 문서 생성 시: 항상 DynamicForm (마이그레이션 후 모든 템플릿에 schema_definition 존재)
const useHardcodedEditor = isEditMode
  ? (templateEntry && !existingDoc?.schemaDefinitionSnapshot)
  : false;

const EditComponent = useHardcodedEditor ? templateEntry?.editComponent : undefined;
const isDynamicTemplate = !useHardcodedEditor;
```

렌더링 부분은 기존 구조를 유지하되 조건만 변경:
```tsx
{EditComponent ? (
  <EditComponent ... />
) : isDynamicTemplate ? (
  <DynamicForm
    templateCode={resolvedTemplateCode}
    documentId={savedDocId}
    initialData={...}
    onSave={handleSave}
  />
) : null}
```

**(C) DocumentDetailPage.tsx — DynamicReadOnly 폴백 통합:**

import 추가:
```typescript
import DynamicReadOnly from '../components/templates/DynamicReadOnly';
```

읽�� 전용 렌더링 분기를 UI-SPEC에 따라 수정:
```tsx
{/* Content */}
<div className="bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-700 rounded-xl p-6 mb-6">
  {ReadOnlyComponent && !doc.schemaDefinitionSnapshot ? (
    <ReadOnlyComponent
      title={doc.title}
      bodyHtml={doc.bodyHtml}
      formData={doc.formData}
    />
  ) : doc.schemaDefinitionSnapshot ? (
    <DynamicReadOnly
      formData={doc.formData}
      schemaDefinitionSnapshot={doc.schemaDefinitionSnapshot}
    />
  ) : (
    <p className="text-sm text-gray-400">알 ��� 없는 양식입니다.</p>
  )}
</div>
```

이 분기 로직은:
1. 하드코딩 컴포넌트 존재 + 스냅샷 없음 -> 레거시 문서 -> 하드코딩 렌더러 (per D-01, MIGR-03)
2. 스냅샷 존��� -> 동적 문서 -> DynamicReadOnly (per MIGR-02)
3. 둘 다 없음 -> "알 수 없는 양식" 텍스트
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - document.ts의 DocumentDetailResponse에 `schemaDefinitionSnapshot: string | null` 필드가 존재한다
    - DocumentEditorPage.tsx��서 신규 문서 생성 시 DynamicForm이 사용되는 분기 로직이 존재한다
    - DocumentEditorPage.tsx에서 기존 DRAFT 편집 시 schemaDefinitionSnapshot 유무로 하드코딩/동적 분기한다
    - DocumentDetailPage.tsx에 `import DynamicReadOnly` 가 존재한다
    - DocumentDetailPage.tsx의 읽�� 전용 렌더링에서 `doc.schemaDefinitionSnapshot` 조건 분기�� 존재한��
    - DocumentDetailPage.tsx에서 schemaDefinitionSnapshot이 있을 때 `<DynamicReadOnly` 컴포넌트가 렌더링된다
    - `npx tsc --noEmit`이 에러 없이 통과한다
  </acceptance_criteria>
  <done>프론트엔드 듀얼 렌더링이 완성��어 기존 문서는 하드코딩, 신규 문서는 동적 렌더러를 사용하며, 타입 에러가 없���.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 듀얼 렌더링 및 기존 문서 호환성 수동 검증</name>
  <files>none</files>
  <action>
사용자가 브라우저에서 듀얼 ���더링과 기존 문서 호환성을 수동 검증한다.

사전 조건: 백엔드를 재시작���여 V12 마이그레이션이 적용되도록 한다.

테스트 1 — 기존 문서 정상 표시 (MIGR-03):
1. 기존 GENERAL DRAFT 문서를 열어 편집 -> 하드코딩 Tiptap 에디터가 표시되는지 확인
2. 기존 SUBMITTED 문서(GENERAL/EXPENSE/LEAVE)의 상세 페이지 -> 하드코딩 ReadOnly 컴포넌트가 표시되는지 확인
3. 기존 문서의 내용이 이전과 동일한지 확인 (데이터 변경 없음)

테스트 2 — 신규 문서 동적 렌더링 (MIGR-02):
1. 새 문서 만들기 -> GENERAL 템플릿 선택 -> DynamicForm이 렌더링되는지 확인 (textarea, Tiptap 아님)
2. 새 문서 만들기 -> EXPENSE 템플릿 선택 -> 테이블 필드와 금액 필드가 표시되는지 확인
3. 새 문서 만들기 -> PURCHASE 템플릿 선택 -> 동적 폼이 렌더링되는지 확인

테스트 3 — 신규 동적 문서 저장 및 읽기 (MIGR-02):
1. PURCHASE로 신�� 문서 작성 -> 저장
2. 해당 문서의 상세 페이지에서 DynamicReadOnly로 정상 표시되는지 확인
  </action>
  <verify>
    <automated>echo "Manual verification checkpoint -- no automated test"</automated>
  </verify>
  <acceptance_criteria>
    - 기존 DRAFT 문서 편집 시 하드코딩 Tiptap 에디터가 표시된다
    - 기존 SUBMITTED 문서 상세에서 하드코딩 ReadOnly가 표시된다
    - 신규 GENERAL 문서 생성 시 DynamicForm(textarea)이 표시된다
    - 신규 PURCHASE 문서 생성/저장 후 상세 페이지에서 DynamicReadOnly가 표시된다
    - 사용자가 "approved" 신호를 보낸다
  </acceptance_criteria>
  <done>사용자가 듀얼 렌더링과 기존 문서 호환성을 확인하고 "approved"를 입력한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend API -> Frontend | schemaDefinitionSnapshot이 API 응답으로 전달됨 |
| Frontend rendering path | 분기 로직이 올바른 렌더러를 선택해야 함 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | T (Tampering) | DocumentEditorPage 분기 ���직 | mitigate | schemaDefinitionSnapshot은 서버에서만 설정��. 클라이언트가 조작해도 렌더링에만 영향, 데이터 무결성은 백엔드 밸리데이터가 보장 |
| T-16-05 | R (Repudiation) | 기존 문서 렌더링 경로 변경 | accept | D-01 결정에 따라 하드코딩 컴포넌트를 유지하므로 기존 문서 렌더링은 변경 없음 |
</threat_model>

<verification>
1. `npx tsc --noEmit` — TypeScript 컴파��� 에러 없음
2. 브라우저에서 기존 문서 상세/편집 정상 동작 확인
3. 브라우저에서 ��규 동적 문서 ��성/저장/읽기 정상 동작 확인
</verification>

<success_criteria>
- DocumentDetailResponse 타입�� schemaDefinitionSnapshot 필드 존재
- 신규 문서 생성 시 DynamicForm 사용 (GENERAL/EXPENSE/LEAVE 포함)
- 기존 DRAFT 편집 시 하드코딩 컴포넌트 유지
- DocumentDetailPage에서 schemaDefinitionSnapshot 있으면 DynamicReadOnly 렌더링
- 기존 SUBMITTED 문서 상세에서 하드코딩 ReadOnly 컴포넌��� 유지
- TypeScript 컴파일 에러 없음
- 수동 검증 체크포인트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/16-template-migration/16-02-SUMMARY.md`
</output>
