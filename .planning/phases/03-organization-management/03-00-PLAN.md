---
phase: 03-organization-management
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/src/test/java/com/micesign/admin/DepartmentControllerTest.java
  - backend/src/test/java/com/micesign/admin/DepartmentServiceTest.java
  - backend/src/test/java/com/micesign/admin/PositionControllerTest.java
  - backend/src/test/java/com/micesign/admin/UserManagementControllerTest.java
  - backend/src/test/java/com/micesign/admin/UserManagementServiceTest.java
  - backend/src/test/java/com/micesign/admin/RbacEnforcementTest.java
  - backend/src/test/resources/db/testmigration/V4__add_unique_department_name.sql
autonomous: true
requirements: [ORG-01, ORG-02, ORG-03, ORG-04]

must_haves:
  truths:
    - "All 7 test files compile but every test method fails (RED state)"
    - "Test stubs cover department CRUD, position CRUD, user management, and RBAC enforcement"
    - "V4 test migration file exists for H2 compatibility"
  artifacts:
    - path: "backend/src/test/java/com/micesign/admin/DepartmentControllerTest.java"
      provides: "Integration test stubs for department CRUD API"
      contains: "@SpringBootTest"
    - path: "backend/src/test/java/com/micesign/admin/DepartmentServiceTest.java"
      provides: "Unit test stubs for department business rules"
      contains: "@ExtendWith"
    - path: "backend/src/test/java/com/micesign/admin/PositionControllerTest.java"
      provides: "Integration test stubs for position CRUD API"
      contains: "@SpringBootTest"
    - path: "backend/src/test/java/com/micesign/admin/UserManagementControllerTest.java"
      provides: "Integration test stubs for user management API"
      contains: "@SpringBootTest"
    - path: "backend/src/test/java/com/micesign/admin/UserManagementServiceTest.java"
      provides: "Unit test stubs for last SUPER_ADMIN protection"
      contains: "@ExtendWith"
    - path: "backend/src/test/java/com/micesign/admin/RbacEnforcementTest.java"
      provides: "Integration test stubs for RBAC role enforcement"
      contains: "@SpringBootTest"
  key_links: []
---

<objective>
Create Wave 0 test stubs for all Phase 3 backend tests. These stubs compile but intentionally fail, establishing the RED phase of TDD before Plan 03-01 implements production code.

Purpose: Satisfies Nyquist compliance — every automated verify command in Plan 03-01 has a pre-existing test file to fail against. VALIDATION.md Wave 0 Requirements are fulfilled.
Output: 7 test stub files that compile (via `compileTestJava`) but fail when run (asserting against non-existent endpoints/services).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-organization-management/03-CONTEXT.md
@.planning/phases/03-organization-management/03-VALIDATION.md

@backend/src/test/java/com/micesign/auth/AuthControllerTest.java
@backend/src/main/java/com/micesign/common/dto/ApiResponse.java
@backend/src/main/java/com/micesign/domain/enums/UserRole.java
@backend/src/main/java/com/micesign/domain/enums/UserStatus.java
@backend/src/main/java/com/micesign/config/SecurityConfig.java

<interfaces>
<!-- Follow existing AuthControllerTest pattern -->

From backend/src/test/java/com/micesign/auth/AuthControllerTest.java:
```java
@SpringBootTest
@AutoConfigureMockMvc
class AuthControllerTest {
    @Autowired MockMvc mockMvc;
    @Autowired JdbcTemplate jdbcTemplate;
    // Uses @BeforeEach for cleanup
    // Uses helper method to generate JWT tokens
}
```

From backend/src/main/java/com/micesign/common/dto/ApiResponse.java:
```java
public record ApiResponse<T>(boolean success, T data, ErrorDetail error) {
    public static <T> ApiResponse<T> ok(T data) { ... }
    public static <T> ApiResponse<T> error(String code, String message) { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stub files — compiling but failing stubs for all 6 test classes + V4 test migration</name>
  <files>
    backend/src/test/java/com/micesign/admin/DepartmentControllerTest.java,
    backend/src/test/java/com/micesign/admin/DepartmentServiceTest.java,
    backend/src/test/java/com/micesign/admin/PositionControllerTest.java,
    backend/src/test/java/com/micesign/admin/UserManagementControllerTest.java,
    backend/src/test/java/com/micesign/admin/UserManagementServiceTest.java,
    backend/src/test/java/com/micesign/admin/RbacEnforcementTest.java,
    backend/src/test/resources/db/testmigration/V4__add_unique_department_name.sql
  </files>
  <read_first>
    backend/src/test/java/com/micesign/auth/AuthControllerTest.java,
    backend/src/main/java/com/micesign/config/SecurityConfig.java,
    backend/src/main/java/com/micesign/common/dto/ApiResponse.java,
    backend/src/test/resources/db/testmigration/V1__create_schema.sql,
    backend/build.gradle.kts
  </read_first>
  <action>
Create 6 test stub files in `backend/src/test/java/com/micesign/admin/` package and 1 V4 test migration. Each test class must:
- Compile successfully (imports reference only existing classes or standard JUnit/Spring)
- Contain `@Disabled("Wave 0 stub — waiting for Plan 03-01 production code")` on every test method so `compileTestJava` passes but running tests shows them as skipped (not failing the build before production code exists)
- Define the test method signatures and Javadoc describing expected behavior, so Plan 03-01 Task 3 removes `@Disabled` and implements assertions

**DepartmentControllerTest.java** (`@SpringBootTest @AutoConfigureMockMvc`):
```java
package com.micesign.admin;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.web.servlet.MockMvc;

@SpringBootTest
@AutoConfigureMockMvc
class DepartmentControllerTest {

    @Autowired
    MockMvc mockMvc;

    @Test @Disabled("Wave 0 stub")
    void createDepartment_returns201() { /* POST /api/v1/admin/departments with valid body -> 201 */ }

    @Test @Disabled("Wave 0 stub")
    void createDepartment_duplicateName_returns400() { /* ORG_DUPLICATE_NAME */ }

    @Test @Disabled("Wave 0 stub")
    void createDepartment_depthExceeded_returns400() { /* ORG_DEPTH_EXCEEDED at depth > 3 */ }

    @Test @Disabled("Wave 0 stub")
    void getDepartmentTree_returnsNestedStructure() { /* GET / returns tree with children */ }

    @Test @Disabled("Wave 0 stub")
    void getDepartmentMembers_returnsUserList() { /* GET /{id}/members returns department members */ }

    @Test @Disabled("Wave 0 stub")
    void deactivateDepartment_withActiveChildren_returns400() { /* ORG_HAS_ACTIVE_CHILDREN */ }

    @Test @Disabled("Wave 0 stub")
    void updateDepartment_circularReference_returns400() { /* ORG_CIRCULAR_REF when setting parent to descendant */ }

    @Test @Disabled("Wave 0 stub")
    void getUserCount_returnsCorrectActiveCount() { /* GET /{id}/user-count returns count of active users — needed by D-06 deactivation dialog and D-16 parent-move confirmation */ }
}
```

**DepartmentServiceTest.java** (`@ExtendWith(MockitoExtension.class)` unit test):
```java
package com.micesign.admin;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.junit.jupiter.MockitoExtension;

@ExtendWith(MockitoExtension.class)
class DepartmentServiceTest {

    @Test @Disabled("Wave 0 stub")
    void buildTree_convertsListToNestedStructure() { /* Flat list -> recursive tree */ }

    @Test @Disabled("Wave 0 stub")
    void validateDepth_rejectsDepthBeyondThree() { /* Max 3 levels enforced */ }

    @Test @Disabled("Wave 0 stub")
    void validateNoCircularRef_detectsCycle() { /* Parent set to own descendant -> exception */ }

    @Test @Disabled("Wave 0 stub")
    void deactivate_blockedByActiveChildren() { /* ORG_HAS_ACTIVE_CHILDREN thrown */ }
}
```

**PositionControllerTest.java** (`@SpringBootTest @AutoConfigureMockMvc`):
```java
package com.micesign.admin;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.web.servlet.MockMvc;

@SpringBootTest
@AutoConfigureMockMvc
class PositionControllerTest {

    @Autowired
    MockMvc mockMvc;

    @Test @Disabled("Wave 0 stub")
    void createPosition_returns201() { /* POST /api/v1/admin/positions -> 201 */ }

    @Test @Disabled("Wave 0 stub")
    void createPosition_duplicateName_returns400() { /* ORG_DUPLICATE_NAME */ }

    @Test @Disabled("Wave 0 stub")
    void reorderPositions_updatesSortOrder() { /* PUT /reorder -> 200 */ }

    @Test @Disabled("Wave 0 stub")
    void deactivatePosition_withActiveUsers_returns400() { /* ORG_HAS_ACTIVE_USERS */ }

    @Test @Disabled("Wave 0 stub")
    void deactivatePosition_noUsers_returns200() { /* Success deactivation */ }
}
```

**UserManagementControllerTest.java** (`@SpringBootTest @AutoConfigureMockMvc`):
```java
package com.micesign.admin;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.web.servlet.MockMvc;

@SpringBootTest
@AutoConfigureMockMvc
class UserManagementControllerTest {

    @Autowired
    MockMvc mockMvc;

    @Test @Disabled("Wave 0 stub")
    void createUser_returns201_withMustChangePassword() { /* mustChangePassword = true */ }

    @Test @Disabled("Wave 0 stub")
    void getUserList_paginatedWithFilters() { /* GET / with keyword, departmentId, role, status, page, size */ }

    @Test @Disabled("Wave 0 stub")
    void getUserDetail_returnsFullProfile() { /* GET /{id} -> UserDetailResponse */ }

    @Test @Disabled("Wave 0 stub")
    void updateUser_savesChanges() { /* PUT /{id} -> updated UserDetailResponse */ }

    @Test @Disabled("Wave 0 stub")
    void deactivateUser_setsInactive() { /* PATCH /{id}/deactivate -> status INACTIVE */ }
}
```

**UserManagementServiceTest.java** (`@ExtendWith(MockitoExtension.class)` unit test):
```java
package com.micesign.admin;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.junit.jupiter.MockitoExtension;

@ExtendWith(MockitoExtension.class)
class UserManagementServiceTest {

    @Test @Disabled("Wave 0 stub")
    void lastSuperAdmin_cannotBeDemoted() { /* ORG_LAST_SUPER_ADMIN when demoting sole SUPER_ADMIN */ }

    @Test @Disabled("Wave 0 stub")
    void lastSuperAdmin_cannotBeDeactivated() { /* ORG_LAST_SUPER_ADMIN when deactivating sole SUPER_ADMIN */ }

    @Test @Disabled("Wave 0 stub")
    void createUser_setsInitialPasswordAndForceChange() { /* password encoded, mustChangePassword=true */ }
}
```

**RbacEnforcementTest.java** (`@SpringBootTest @AutoConfigureMockMvc`):
```java
package com.micesign.admin;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.web.servlet.MockMvc;

@SpringBootTest
@AutoConfigureMockMvc
class RbacEnforcementTest {

    @Autowired
    MockMvc mockMvc;

    @Test @Disabled("Wave 0 stub")
    void adminCannotCreateAdminRoleUser() { /* ADMIN creates user with role=ADMIN -> 400 AUTH_FORBIDDEN */ }

    @Test @Disabled("Wave 0 stub")
    void adminCannotPromoteToAdmin() { /* ADMIN updates user role to ADMIN -> 400 AUTH_FORBIDDEN */ }

    @Test @Disabled("Wave 0 stub")
    void userRoleBlocked_returns403() { /* USER role accessing /api/v1/admin/* -> 403 */ }

    @Test @Disabled("Wave 0 stub")
    void selfDeactivation_blocked() { /* Admin deactivating self -> 400 ORG_SELF_DEACTIVATION */ }

    @Test @Disabled("Wave 0 stub")
    void superAdminCanCreateAdminRoleUser() { /* SUPER_ADMIN creates ADMIN user -> 201 success */ }
}
```

**V4 test migration** (`backend/src/test/resources/db/testmigration/V4__add_unique_department_name.sql`):
```sql
ALTER TABLE department ADD CONSTRAINT uk_department_name UNIQUE (name);
ALTER TABLE "position" ADD CONSTRAINT uk_position_name UNIQUE (name);
```
(H2 uses double-quote escaping for reserved word `position`.)
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/backend && JAVA_HOME=$(/usr/libexec/java_home -v 17) ./gradlew compileTestJava 2>&1 | tail -5</automated>
  </verify>
  <done>All 6 test stub files compile successfully. Each test method is @Disabled, documenting expected behavior. V4 test migration exists. Plan 03-01 will remove @Disabled and implement assertions as production code becomes available.</done>
</task>

</tasks>

<verification>
1. `compileTestJava` succeeds — all stubs compile against existing Spring Boot test infrastructure
2. `ls backend/src/test/java/com/micesign/admin/` shows all 6 .java files
3. `ls backend/src/test/resources/db/testmigration/V4*` shows V4 migration exists
</verification>

<success_criteria>
- All 6 test stub files exist under `com.micesign.admin` package
- V4 test migration file exists at `backend/src/test/resources/db/testmigration/V4__add_unique_department_name.sql`
- `compileTestJava` passes with no errors
- Every test method has `@Disabled` annotation
- DepartmentControllerTest includes `getUserCount_returnsCorrectActiveCount` test case (per Warning #4)
- VALIDATION.md Wave 0 Requirements are fulfilled
</success_criteria>

<output>
After completion, create `.planning/phases/03-organization-management/03-00-SUMMARY.md`
</output>
