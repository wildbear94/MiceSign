---
phase: 09-integration-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/micesign/service/ApprovalService.java
  - backend/src/main/java/com/micesign/service/DocumentService.java
  - backend/src/main/java/com/micesign/common/AuditAction.java
  - backend/src/main/java/com/micesign/controller/AuditLogController.java
  - backend/src/main/java/com/micesign/repository/DocumentRepository.java
autonomous: true
gap_closure: true
requirements: [AUD-01]

must_haves:
  truths:
    - "getCompletedApprovals() returns documents the user approved/rejected, not documents the user drafted"
    - "rewriteDocument() creates an audit log entry with DOC_REWRITE action"
    - "AuditLogController delegates to AuditLogService.search() instead of direct repository + N+1 user lookups"
  artifacts:
    - path: "backend/src/main/java/com/micesign/service/ApprovalService.java"
      provides: "Fixed getCompletedApprovals using actedLines document IDs"
      contains: "documentRepository.findByIdIn"
    - path: "backend/src/main/java/com/micesign/service/DocumentService.java"
      provides: "Audit log call in rewriteDocument"
      contains: "auditLogService.log"
    - path: "backend/src/main/java/com/micesign/common/AuditAction.java"
      provides: "DOC_REWRITE constant"
      contains: "DOC_REWRITE"
    - path: "backend/src/main/java/com/micesign/controller/AuditLogController.java"
      provides: "Delegated search via AuditLogService"
      contains: "auditLogService.search"
  key_links:
    - from: "ApprovalService.getCompletedApprovals"
      to: "DocumentRepository.findByIdIn"
      via: "actedLines document IDs extracted and queried"
      pattern: "findByIdIn"
    - from: "DocumentService.rewriteDocument"
      to: "AuditLogService.log"
      via: "DOC_REWRITE audit action"
      pattern: "DOC_REWRITE"
    - from: "AuditLogController.searchAuditLogs"
      to: "AuditLogService.search"
      via: "service delegation"
      pattern: "auditLogService\\.search"
---

<objective>
Fix three backend wiring bugs discovered during v1.0 milestone audit: (1) getCompletedApprovals() returns wrong dataset (drafter's docs instead of approver's docs), (2) rewriteDocument() missing audit trail entry, (3) AuditLogController bypasses AuditLogService causing N+1 queries.

Purpose: Restore correct "completed approvals" data, complete audit trail coverage for rewrite operations, and eliminate N+1 query pattern in audit log controller.
Output: Three backend service/controller fixes with correct behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@backend/src/main/java/com/micesign/service/ApprovalService.java
@backend/src/main/java/com/micesign/service/DocumentService.java
@backend/src/main/java/com/micesign/controller/AuditLogController.java
@backend/src/main/java/com/micesign/service/AuditLogService.java
@backend/src/main/java/com/micesign/common/AuditAction.java
@backend/src/main/java/com/micesign/repository/DocumentRepository.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix getCompletedApprovals() to return approver's acted documents</name>
  <read_first>
    - backend/src/main/java/com/micesign/service/ApprovalService.java (lines 196-213 — current broken implementation)
    - backend/src/main/java/com/micesign/repository/DocumentRepository.java (current query methods)
    - backend/src/main/java/com/micesign/repository/ApprovalLineRepository.java (findByApproverIdAndStatus)
  </read_first>
  <files>
    backend/src/main/java/com/micesign/service/ApprovalService.java
    backend/src/main/java/com/micesign/repository/DocumentRepository.java
  </files>
  <action>
1. In `DocumentRepository.java`, add a new method:
   ```java
   Page<Document> findByIdInAndStatusIn(List<Long> ids, List<DocumentStatus> statuses, Pageable pageable);
   ```

2. In `ApprovalService.java`, method `getCompletedApprovals()` (lines 197-213):
   - Keep the existing `actedLines` collection logic (lines 200-203) which correctly finds lines where the user APPROVED or REJECTED
   - Extract document IDs from actedLines: `List<Long> docIds = actedLines.stream().map(line -> line.getDocument().getId()).distinct().toList();`
   - Replace the incorrect `documentRepository.findByDrafterIdAndStatusIn(userId, ...)` call (line 207-208) with: `documentRepository.findByIdInAndStatusIn(docIds, List.of(DocumentStatus.APPROVED, DocumentStatus.REJECTED), pageable)`
   - Handle empty docIds case: if `docIds.isEmpty()`, return `Page.empty(pageable)` to avoid an empty IN clause

Remove the comment on lines 206-207 that says "For now, delegate to a paginated query on drafter's completed documents".
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign && JAVA_HOME=$(/usr/libexec/java_home -v 17) ./backend/gradlew -p backend compileJava 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ApprovalService.getCompletedApprovals()` no longer calls `findByDrafterIdAndStatusIn`
    - `ApprovalService.getCompletedApprovals()` calls `findByIdInAndStatusIn` with IDs from `actedLines`
    - `DocumentRepository` contains method `findByIdInAndStatusIn(List<Long>, List<DocumentStatus>, Pageable)`
    - Empty actedLines case returns `Page.empty(pageable)`
    - Backend compiles without errors
  </acceptance_criteria>
  <done>getCompletedApprovals() queries documents by approval line document IDs (where user acted as approver), not by drafter ID. Compilation succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Add audit log to rewriteDocument() and wire AuditLogController to service</name>
  <read_first>
    - backend/src/main/java/com/micesign/service/DocumentService.java (lines 396-445 — rewriteDocument method)
    - backend/src/main/java/com/micesign/common/AuditAction.java (existing constants)
    - backend/src/main/java/com/micesign/controller/AuditLogController.java (current N+1 implementation)
    - backend/src/main/java/com/micesign/service/AuditLogService.java (search method at line 100-120)
  </read_first>
  <files>
    backend/src/main/java/com/micesign/common/AuditAction.java
    backend/src/main/java/com/micesign/service/DocumentService.java
    backend/src/main/java/com/micesign/controller/AuditLogController.java
  </files>
  <action>
1. **AuditAction.java** — Add a new constant after DOC_WITHDRAW:
   ```java
   public static final String DOC_REWRITE = "DOC_REWRITE";
   ```

2. **DocumentService.java** — In `rewriteDocument()`, after `newDoc = documentRepository.save(newDoc);` (line 420) and before the content copy block, add:
   ```java
   auditLogService.log(userId, AuditAction.DOC_REWRITE, "DOCUMENT", newDoc.getId(),
           Map.of("sourceDocId", docId, "sourceDocNumber", sourceDoc.getDocNumber() != null ? sourceDoc.getDocNumber() : ""));
   ```
   Add `import java.util.Map;` if not already present (check existing imports first).

3. **AuditLogController.java** — Replace entire controller to delegate to `AuditLogService.search()`:
   - Change constructor to inject `AuditLogService` instead of `AuditLogRepository` and `UserRepository`
   - Replace the `searchAuditLogs` method body:
     ```java
     Page<AuditLogResponse> response = auditLogService.search(userId, action, targetType, dateFrom, dateTo, pageable);
     return ResponseEntity.ok(ApiResponse.ok(response));
     ```
   - Remove the private `toResponse` helper method entirely
   - Remove imports for `AuditLog`, `AuditLogRepository`, `UserRepository`, `AuditLogSpecification`
   - Add import for `AuditLogService` and `AuditLogResponse` (keep existing imports for `ApiResponse`, `Page`, `Pageable`, etc.)
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign && JAVA_HOME=$(/usr/libexec/java_home -v 17) ./backend/gradlew -p backend compileJava 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `AuditAction.java` contains `public static final String DOC_REWRITE = "DOC_REWRITE";`
    - `DocumentService.rewriteDocument()` contains `auditLogService.log(userId, AuditAction.DOC_REWRITE`
    - `AuditLogController` constructor injects `AuditLogService` (not `AuditLogRepository` + `UserRepository`)
    - `AuditLogController.searchAuditLogs()` calls `auditLogService.search()`
    - `AuditLogController` has no `toResponse` private method
    - Backend compiles without errors
  </acceptance_criteria>
  <done>rewriteDocument() creates DOC_REWRITE audit log entry with source document ID. AuditLogController delegates to AuditLogService.search() eliminating N+1 user lookups.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller -> Service | All data flows through service layer, no direct repo access from controller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | getCompletedApprovals | mitigate | Query scoped to documents where user has acted approval lines only |
| T-09-02 | Repudiation | rewriteDocument | mitigate | Added DOC_REWRITE audit log entry with source doc reference |
</threat_model>

<verification>
1. `compileJava` passes with no errors
2. grep confirms `findByIdInAndStatusIn` in DocumentRepository
3. grep confirms `DOC_REWRITE` in AuditAction and DocumentService
4. grep confirms `auditLogService.search` in AuditLogController
5. grep confirms no `userRepository` in AuditLogController
</verification>

<success_criteria>
- getCompletedApprovals returns documents where user acted as approver (not drafter's docs)
- rewriteDocument produces DOC_REWRITE audit entry
- AuditLogController uses AuditLogService.search() with no N+1 pattern
- Backend compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/09-integration-gap-closure/09-01-SUMMARY.md`
</output>
