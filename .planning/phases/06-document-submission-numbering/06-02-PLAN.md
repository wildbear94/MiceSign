---
phase: 06-document-submission-numbering
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - frontend/src/features/document/api/documentApi.ts
  - frontend/src/features/document/hooks/useDocuments.ts
  - frontend/src/features/document/pages/DocumentEditorPage.tsx
  - frontend/src/features/document/pages/DocumentDetailPage.tsx
  - frontend/src/features/document/components/DocumentListTable.tsx
  - frontend/src/features/document/components/attachment/FileAttachmentArea.tsx
  - frontend/public/locales/ko/document.json
  - frontend/public/locales/en/document.json
autonomous: false
requirements:
  - DOC-03
  - DOC-04
  - DOC-07

must_haves:
  truths:
    - "사용자가 에디터 상단에서 제출 버튼을 클릭할 수 있다"
    - "제출 확인 다이얼로그가 표시되고 확인/취소가 가능하다"
    - "제출 성공 시 문서 상세 페이지로 리다이렉트되며 성공 메시지가 표시된다"
    - "문서 상세 페이지에서 문서번호가 표시된다"
    - "문서 목록 테이블에 문서번호 칼럼이 존재한다"
    - "DRAFT가 아닌 문서에서는 제출 버튼이 숨겨진다"
    - "제출 처리 중 로딩 상태가 표시되고 버튼이 비활성화된다"
  artifacts:
    - path: "frontend/src/features/document/api/documentApi.ts"
      provides: "submit API 함수"
      contains: "submit"
    - path: "frontend/src/features/document/hooks/useDocuments.ts"
      provides: "useSubmitDocument mutation hook"
      contains: "useSubmitDocument"
    - path: "frontend/src/features/document/pages/DocumentEditorPage.tsx"
      provides: "제출 버튼, 확인 다이얼로그, 제출 flow"
      contains: "handleSubmit"
    - path: "frontend/src/features/document/pages/DocumentDetailPage.tsx"
      provides: "성공 메시지 배너"
      contains: "submitSuccess"
    - path: "frontend/src/features/document/components/DocumentListTable.tsx"
      provides: "문서번호 칼럼"
      contains: "docNumber"
  key_links:
    - from: "DocumentEditorPage.tsx"
      to: "documentApi.submit"
      via: "useSubmitDocument mutation"
      pattern: "useSubmitDocument"
    - from: "DocumentEditorPage.tsx"
      to: "DocumentDetailPage.tsx"
      via: "navigate with state"
      pattern: "submitSuccess"
---

<objective>
프론트엔드에 문서 제출 기능 전체를 구현한다: 제출 API/훅, 에디터 제출 버튼 + 확인 다이얼로그, 성공 메시지, 문서번호 표시.

Purpose: 사용자가 작성한 문서를 제출하여 결재 프로세스를 시작할 수 있게 한다. 제출 후에는 읽기 전용으로 전환되어 문서 무결성을 보장한다.
Output: 제출 가능한 에디터 UI, 문서번호가 표시되는 상세/목록 페이지
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-document-submission-numbering/06-CONTEXT.md
@.planning/phases/06-document-submission-numbering/06-RESEARCH.md
@.planning/phases/06-document-submission-numbering/06-UI-SPEC.md
@.planning/phases/06-document-submission-numbering/06-01-SUMMARY.md

@frontend/src/features/document/api/documentApi.ts
@frontend/src/features/document/hooks/useDocuments.ts
@frontend/src/features/document/hooks/useAutoSave.ts
@frontend/src/features/document/pages/DocumentEditorPage.tsx
@frontend/src/features/document/pages/DocumentDetailPage.tsx
@frontend/src/features/document/components/DocumentListTable.tsx
@frontend/src/features/document/components/DocumentStatusBadge.tsx
@frontend/src/features/document/components/attachment/FileAttachmentArea.tsx
@frontend/src/features/document/components/attachment/useFileUpload.ts
@frontend/src/features/document/types/document.ts
@frontend/src/features/admin/components/ConfirmDialog.tsx
@frontend/public/locales/ko/document.json

<interfaces>
<!-- Key types and contracts the executor needs -->

From document.ts:
```typescript
export interface DocumentDetailResponse extends DocumentResponse {
  drafter: DrafterInfo;
  bodyHtml: string | null;
  formData: string | null;
  submittedAt: string | null;
  completedAt: string | null;
}
// docNumber is already in DocumentResponse as: docNumber: string | null;
// DocumentStatus = 'DRAFT' | 'SUBMITTED' | 'APPROVED' | 'REJECTED' | 'WITHDRAWN';
```

From documentApi.ts (current):
```typescript
export const documentApi = {
  getMyDocuments, getById, create, update, delete
  // submit needs to be added
};
```

From useDocuments.ts (current hooks):
```typescript
export function useMyDocuments(params)
export function useDocumentDetail(id)
export function useCreateDocument()
export function useUpdateDocument()
export function useDeleteDocument()
// useSubmitDocument needs to be added
```

From useAutoSave.ts:
```typescript
export function useAutoSave(saveFn, dependencies, delayMs = 30000) {
  return { status, saveNow, triggerSave };
  // saveNow(): cancel timer + immediate save
}
```

From ConfirmDialog.tsx:
```typescript
interface ConfirmDialogProps {
  open: boolean; onClose: () => void; onConfirm: () => void;
  title: string; message: string; confirmLabel: string;
  confirmVariant: 'danger' | 'primary'; isLoading?: boolean; showCancel?: boolean;
}
// Cancel button text is hardcoded as "닫기" — for submit dialog, per D-02, it should say "취소"
// Note: ConfirmDialog currently hardcodes "닫기". May need to add cancelLabel prop or accept existing.
```

From FileAttachmentArea.tsx:
```typescript
interface FileAttachmentAreaProps {
  documentId: number;
  documentStatus: DocumentStatus;
  readOnly: boolean;
  // No upload state callback currently — needs onUploadStateChange prop for D-13/D-14
}
```

From useFileUpload.ts:
```typescript
return { uploadItems, addFiles, cancelUpload, validationError, clearError };
// uploadItems: FileUploadItem[] with status: 'pending' | 'uploading' | 'complete' | 'error'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Submit API, Hook, FileAttachmentArea 업로드 상태 콜백, i18n 키 추가</name>
  <files>
    frontend/src/features/document/api/documentApi.ts,
    frontend/src/features/document/hooks/useDocuments.ts,
    frontend/src/features/document/components/attachment/FileAttachmentArea.tsx,
    frontend/public/locales/ko/document.json,
    frontend/public/locales/en/document.json
  </files>
  <read_first>
    frontend/src/features/document/api/documentApi.ts,
    frontend/src/features/document/hooks/useDocuments.ts,
    frontend/src/features/document/components/attachment/FileAttachmentArea.tsx,
    frontend/src/features/document/components/attachment/useFileUpload.ts,
    frontend/src/features/document/types/document.ts,
    frontend/public/locales/ko/document.json,
    frontend/public/locales/en/document.json
  </read_first>
  <action>
**1) documentApi.ts에 submit 함수 추가:**

기존 documentApi 객체에 submit 메서드 추가:

```typescript
submit: (id: number) =>
  apiClient.post<ApiResponse<DocumentDetailResponse>>(`${BASE}/${id}/submit`),
```

DocumentDetailResponse import가 이미 존재하므로 추가 import 불필요.

**2) useDocuments.ts에 useSubmitDocument hook 추가:**

파일 하단에 추가:

```typescript
export function useSubmitDocument() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (id: number) =>
      documentApi.submit(id).then((res) => res.data.data!),
    onSuccess: (_data, id) => {
      queryClient.invalidateQueries({ queryKey: ['documents'] });
      queryClient.invalidateQueries({ queryKey: ['documents', id] });
    },
  });
}
```

**3) FileAttachmentArea에 onUploadStateChange 콜백 추가 (D-13, D-14):**

per D-13 (업로드 중 제출 대기)와 D-14 (실패 파일 있으면 제출 차단), 에디터 페이지에서 업로드 상태를 알아야 한다.

FileAttachmentAreaProps 인터페이스에 추가:
```typescript
interface FileAttachmentAreaProps {
  documentId: number;
  documentStatus: DocumentStatus;
  readOnly: boolean;
  onUploadStateChange?: (state: { isUploading: boolean; hasError: boolean }) => void;
}
```

FileAttachmentArea 컴포넌트 내부에서, useFileUpload의 uploadItems가 변경될 때마다 콜백 호출:

```typescript
const { uploadItems, addFiles, cancelUpload, validationError, clearError } = useFileUpload(documentId, ...);

// Report upload state to parent
useEffect(() => {
  if (onUploadStateChange) {
    const isUploading = uploadItems.some(item => item.status === 'uploading' || item.status === 'pending');
    const hasError = uploadItems.some(item => item.status === 'error');
    onUploadStateChange({ isUploading, hasError });
  }
}, [uploadItems, onUploadStateChange]);
```

기존 readOnly=true로 호출하는 곳(DocumentDetailPage)에서는 onUploadStateChange를 전달하지 않으므로 영향 없음.

**4) i18n 키 추가:**

ko/document.json에 다음 키 추가:

```json
{
  "submit": "제출",
  "submitConfirm": {
    "title": "문서 제출",
    "message": "제출 후에는 수정할 수 없습니다. 제출하시겠습니까?",
    "confirm": "제출",
    "cancel": "취소"
  },
  "submitSuccess": "문서가 제출되었습니다 ({{docNumber}})",
  "columns": {
    "docNumber": "문서번호"
  },
  "error": {
    "submitFailed": "문서 제출에 실패했습니다. 잠시 후 다시 시도해주세요.",
    "alreadySubmitted": "이미 제출된 문서입니다",
    "uploadInProgress": "파일 업로드가 진행 중입니다. 완료 후 다시 시도해주세요.",
    "uploadFailed": "업로드 실패한 파일이 있습니다. 삭제 후 다시 시도해주세요."
  }
}
```

주의: 기존 `columns` 키에 `docNumber` 항목을 추가하는 것이지, `columns` 전체를 교체하면 안 된다. 기존 키(title, template, status, createdAt)를 유지한 채 `docNumber`만 추가.

마찬가지로 기존 `error` 키에 새 항목만 추가.

en/document.json에도 대응하는 영문 키 추가:
```json
{
  "submit": "Submit",
  "submitConfirm": {
    "title": "Submit Document",
    "message": "You cannot modify the document after submission. Are you sure?",
    "confirm": "Submit",
    "cancel": "Cancel"
  },
  "submitSuccess": "Document submitted successfully ({{docNumber}})",
  "columns": {
    "docNumber": "Doc Number"
  },
  "error": {
    "submitFailed": "Failed to submit document. Please try again later.",
    "alreadySubmitted": "This document has already been submitted",
    "uploadInProgress": "File upload is in progress. Please wait and try again.",
    "uploadFailed": "There are failed uploads. Please remove them and try again."
  }
}
```
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - documentApi.ts에 `submit: (id: number) =>` 함수가 존재
    - useDocuments.ts에 `export function useSubmitDocument()` 함수가 존재
    - useDocuments.ts의 useSubmitDocument에서 `documentApi.submit(id)` 호출
    - FileAttachmentArea.tsx의 props 인터페이스에 `onUploadStateChange?:` 존재
    - FileAttachmentArea.tsx에 `uploadItems.some(item => item.status === 'uploading'` 패턴 존재
    - ko/document.json에 `"submit": "제출"` 키 존재
    - ko/document.json에 `"submitConfirm"` 객체 존재
    - ko/document.json에 `"submitSuccess"` 키 존재
    - ko/document.json에 `"docNumber": "문서번호"` 키 존재 (columns 내)
    - ko/document.json에 기존 키(save, delete, backToList 등)가 여전히 존재
    - TypeScript 컴파일 에러 없음 (npx tsc --noEmit 성공)
  </acceptance_criteria>
  <done>submit API 함수, useSubmitDocument hook, FileAttachmentArea 업로드 상태 콜백, 제출 관련 i18n 키가 모두 추가되어 Task 2에서 사용할 준비 완료</done>
</task>

<task type="auto">
  <name>Task 2: DocumentEditorPage 제출 버튼 + 확인 다이얼로그 + 제출 flow 구현</name>
  <files>
    frontend/src/features/document/pages/DocumentEditorPage.tsx,
    frontend/src/features/document/pages/DocumentDetailPage.tsx,
    frontend/src/features/document/components/DocumentListTable.tsx
  </files>
  <read_first>
    frontend/src/features/document/pages/DocumentEditorPage.tsx,
    frontend/src/features/document/pages/DocumentDetailPage.tsx,
    frontend/src/features/document/components/DocumentListTable.tsx,
    frontend/src/features/document/hooks/useDocuments.ts,
    frontend/src/features/document/hooks/useAutoSave.ts,
    frontend/src/features/admin/components/ConfirmDialog.tsx,
    frontend/src/features/document/types/document.ts,
    .planning/phases/06-document-submission-numbering/06-UI-SPEC.md
  </read_first>
  <action>
**1) DocumentEditorPage.tsx 수정:**

per D-01, D-02, D-03, D-04, D-05, D-06, D-08, D-09, D-12, D-13, D-14, D-17, D-18

**(a) 새로운 import 추가:**
```typescript
import { Loader2 } from 'lucide-react';
import { useSubmitDocument } from '../hooks/useDocuments';
```

**(b) 새로운 state 추가 (기존 state 아래):**
```typescript
const [showSubmitConfirm, setShowSubmitConfirm] = useState(false);
const [uploadState, setUploadState] = useState<{ isUploading: boolean; hasError: boolean }>({ isUploading: false, hasError: false });
const submitMutation = useSubmitDocument();
```

**(c) handleSubmit 함수 추가 (handleDelete 아래):**

```typescript
const handleSubmitClick = useCallback(() => {
  setErrorMessage(null);

  // D-14: 업로드 실패 파일 있으면 차단
  if (uploadState.hasError) {
    setErrorMessage(t('error.uploadFailed'));
    return;
  }

  // D-13: 업로드 중이면 차단
  if (uploadState.isUploading) {
    setErrorMessage(t('error.uploadInProgress'));
    return;
  }

  // D-06: 프론트엔드 폼 검증 — form의 requestSubmit으로 Zod 검증 트리거
  // 제출 전 form 검증을 위해 hidden submit 트리거
  const form = document.getElementById('document-form') as HTMLFormElement;
  if (form && !form.checkValidity()) {
    form.requestSubmit(); // triggers Zod validation + inline errors
    return;
  }

  // D-09: 검증 통과 시 확인 다이얼로그
  setShowSubmitConfirm(true);
}, [uploadState, t]);

const handleSubmitConfirm = useCallback(async () => {
  if (!savedDocId) return;

  try {
    // D-12: 미저장 변경사항 자동 저장
    await saveNow();

    // D-05: 제출 API 호출 (mutation pending 상태에서 버튼 비활성화됨)
    const result = await submitMutation.mutateAsync(savedDocId);

    // D-17: 문서 상세 페이지로 리다이렉트
    // D-18: 성공 메시지 전달 (React Router state)
    navigate(`/documents/${savedDocId}`, {
      state: {
        submitSuccess: true,
        docNumber: result.docNumber,
      },
    });
  } catch (err: unknown) {
    setShowSubmitConfirm(false);
    // D-11, D-15: 에러 메시지 표시
    if (err && typeof err === 'object' && 'response' in err) {
      const axiosErr = err as { response?: { status?: number } };
      if (axiosErr.response?.status === 409) {
        setErrorMessage(t('error.alreadySubmitted'));
        return;
      }
    }
    setErrorMessage(t('error.submitFailed'));
  }
}, [savedDocId, saveNow, submitMutation, navigate, t]);
```

**(d) 버튼 영역 재배치 (per D-01, D-03):**

기존 action bar의 버튼 배치를 변경한다. 현재: [저장(blue)] [삭제(red)]. 변경 후: [삭제(gray outline)] [저장(gray outline)] [제출(blue primary)].

```tsx
<div className="flex items-center gap-2">
  {savedDocId && (
    <button
      type="button"
      onClick={() => setShowDeleteConfirm(true)}
      disabled={submitMutation.isPending}
      className="inline-flex items-center gap-2 h-11 px-4 border border-gray-300 text-gray-700 dark:border-gray-600 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-800 text-sm font-medium rounded-lg disabled:opacity-50"
    >
      <Trash2 className="h-4 w-4" />
      {t('delete')}
    </button>
  )}
  <button
    type="button"
    onClick={handleManualSave}
    disabled={submitMutation.isPending}
    className="inline-flex items-center gap-2 h-11 px-4 border border-gray-300 text-gray-700 dark:border-gray-600 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-800 text-sm font-medium rounded-lg disabled:opacity-50"
  >
    {t('save')}
  </button>
  {savedDocId && (
    <button
      type="button"
      onClick={handleSubmitClick}
      disabled={submitMutation.isPending}
      className="inline-flex items-center gap-2 h-11 px-4 bg-blue-600 hover:bg-blue-700 text-white text-sm font-medium rounded-lg disabled:opacity-50 focus:ring-2 focus:ring-blue-500 focus:ring-offset-2"
      aria-label={submitMutation.isPending ? t('submitConfirm.title') + '...' : undefined}
    >
      {submitMutation.isPending ? (
        <Loader2 className="h-4 w-4 animate-spin" />
      ) : null}
      {t('submit')}
    </button>
  )}
</div>
```

**(e) 폼 컨테이너에 제출 중 오버레이 (per D-05):**

기존 form container `<div>` 에 조건부 클래스 추가:

```tsx
<div className={`bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-700 rounded-xl p-6 ${submitMutation.isPending ? 'opacity-50 pointer-events-none' : ''}`}
     aria-busy={submitMutation.isPending}>
```

**(f) 제출 확인 다이얼로그 추가 (기존 삭제 확인 다이얼로그 아래):**

```tsx
<ConfirmDialog
  open={showSubmitConfirm}
  onClose={() => setShowSubmitConfirm(false)}
  onConfirm={handleSubmitConfirm}
  title={t('submitConfirm.title')}
  message={t('submitConfirm.message')}
  confirmLabel={t('submitConfirm.confirm')}
  confirmVariant="primary"
  isLoading={submitMutation.isPending}
/>
```

**(g) FileAttachmentArea에 onUploadStateChange 전달:**

에디터 내 FileAttachmentArea가 있다면 (existingDoc 조건 하에), onUploadStateChange prop 추가:
```tsx
<FileAttachmentArea
  documentId={savedDocId}
  documentStatus="DRAFT"
  readOnly={false}
  onUploadStateChange={setUploadState}
/>
```

만약 현재 에디터에 FileAttachmentArea가 렌더링되고 있다면 해당 JSX를 찾아서 prop을 추가한다. 없다면 (Phase 5에서 이미 통합됨) 해당 위치를 확인한다.

**2) DocumentDetailPage.tsx 수정:**

per D-17, D-18, D-19, D-20

**(a) 새로운 import 추가:**
```typescript
import { useState, useEffect } from 'react';
import { useLocation } from 'react-router';
import { CheckCircle, X } from 'lucide-react';
```

**(b) 성공 메시지 state + 자동 사라짐:**

```typescript
const location = useLocation();
const [successMessage, setSuccessMessage] = useState<string | null>(null);

// D-18, D-19: 성공 메시지 표시 + 5초 자동 사라짐
useEffect(() => {
  const state = location.state as { submitSuccess?: boolean; docNumber?: string } | null;
  if (state?.submitSuccess && state?.docNumber) {
    setSuccessMessage(t('submitSuccess', { docNumber: state.docNumber }));
    // Clear location state to prevent re-showing on refresh
    window.history.replaceState({}, document.title);
  }
}, [location.state, t]);

useEffect(() => {
  if (!successMessage) return;
  const timer = setTimeout(() => setSuccessMessage(null), 5000);
  return () => clearTimeout(timer);
}, [successMessage]);
```

**(c) 성공 메시지 배너 (non-DRAFT 뷰의 back link 아래, meta section 위에):**

```tsx
{successMessage && (
  <div role="alert" className="mb-4 p-3 bg-green-50 dark:bg-green-900/20 border border-green-200 dark:border-green-800 rounded-lg flex items-center justify-between">
    <div className="flex items-center gap-2">
      <CheckCircle className="h-4 w-4 text-green-600 dark:text-green-400 flex-shrink-0" />
      <span className="text-sm text-green-700 dark:text-green-400">{successMessage}</span>
    </div>
    <button
      type="button"
      onClick={() => setSuccessMessage(null)}
      className="h-6 w-6 flex items-center justify-center text-green-400 hover:text-green-600"
    >
      <X className="h-4 w-4" />
    </button>
  </div>
)}
```

**(d) D-20: 문서번호를 제목 옆에 표시**

현재 meta info 섹션에 이미 문서번호가 별도 grid 셀로 표시되고 있다 (`doc.docNumber ?? '-'`). D-20 요구사항은 "제목 옆에" 표시인데, 기존 구현도 같은 영역에 있으므로 meta info 섹션 유지. 다만, 제목 행에 문서번호를 인라인으로 추가:

현재 `<h1>` 태그를 수정:
```tsx
<div className="flex items-center justify-between mb-3">
  <h1 className="text-xl font-semibold text-gray-900 dark:text-gray-50">
    {doc.title}
  </h1>
  {doc.docNumber && (
    <span className="text-sm font-medium text-gray-500 dark:text-gray-400 font-mono">
      {doc.docNumber}
    </span>
  )}
</div>
```

기존 grid 내 문서번호 셀은 그대로 유지해도 되고, 중복이면 제거한다 (D-20은 제목 옆 표시를 명시하므로, grid 셀의 "문서번호" 라벨+값은 유지하여 정보 완전성 확보).

**3) DocumentListTable.tsx 수정:**

per D-21

문서번호 칼럼을 제목 칼럼 다음에 추가:

```tsx
// thead에 추가 (title 칼럼 다음):
<th className="text-left text-sm font-semibold text-gray-600 dark:text-gray-400 px-4 py-3 w-40">
  {t('columns.docNumber')}
</th>

// tbody 각 행에 추가 (title 셀 다음):
<td className="px-4 py-3 text-sm text-gray-500 dark:text-gray-400 font-mono">
  {doc.docNumber || '-'}
</td>
```

empty state의 `colSpan`을 4에서 5로 변경.
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - DocumentEditorPage.tsx에 `useSubmitDocument` import 존재
    - DocumentEditorPage.tsx에 `handleSubmitClick` 함수 존재
    - DocumentEditorPage.tsx에 `handleSubmitConfirm` 함수 존재
    - DocumentEditorPage.tsx에 `showSubmitConfirm` state 존재
    - DocumentEditorPage.tsx 제출 버튼에 `bg-blue-600` 클래스 존재 (D-03)
    - DocumentEditorPage.tsx 저장 버튼에 `border border-gray-300` 클래스 존재 (D-03, blue에서 변경)
    - DocumentEditorPage.tsx에 `opacity-50 pointer-events-none` 조건부 클래스 존재 (D-05)
    - DocumentEditorPage.tsx에 `Loader2` import 존재 (D-05 스피너)
    - DocumentEditorPage.tsx에 `ConfirmDialog` with `confirmVariant="primary"` 존재 (D-02)
    - DocumentEditorPage.tsx에 `saveNow()` 호출이 submit flow 내에 존재 (D-12)
    - DocumentEditorPage.tsx에 `uploadState.hasError` 체크 존재 (D-14)
    - DocumentEditorPage.tsx에 `uploadState.isUploading` 체크 존재 (D-13)
    - DocumentEditorPage.tsx에 409 상태 코드 처리 존재 (D-11)
    - DocumentEditorPage.tsx에 `navigate(`/documents/${savedDocId}`, { state:` 패턴 존재 (D-17)
    - DocumentDetailPage.tsx에 `submitSuccess` 문자열이 location state에서 읽히는 코드 존재
    - DocumentDetailPage.tsx에 `CheckCircle` import 존재 (성공 배너)
    - DocumentDetailPage.tsx에 `setTimeout.*5000` 패턴 존재 (D-19 자동 사라짐)
    - DocumentDetailPage.tsx에 `bg-green-50` 클래스 존재 (성공 배너 스타일)
    - DocumentDetailPage.tsx에 `doc.docNumber` 표시와 `font-mono` 클래스 존재 (D-20)
    - DocumentListTable.tsx에 `docNumber` 칼럼 존재
    - DocumentListTable.tsx에 `font-mono` 클래스 존재
    - DocumentListTable.tsx에 `colSpan={5}` 존재
    - TypeScript 컴파일 에러 없음
  </acceptance_criteria>
  <done>에디터에서 제출 버튼 클릭 -> 검증 -> 확인 다이얼로그 -> 제출 API 호출 -> 상세 페이지 리다이렉트 + 성공 메시지 전체 flow 동작. 목록에 문서번호 칼럼 표시.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 문서 제출 flow E2E 수동 검증</name>
  <files>none</files>
  <action>사용자가 직접 제출 flow를 테스트한다</action>
  <what-built>문서 제출 전체 flow: 에디터 상단 제출 버튼, 확인 다이얼로그, 제출 성공 후 문서번호 부여 + 상세 페이지 리다이렉트, 목록 문서번호 칼럼</what-built>
  <how-to-verify>
    1. 백엔드 실행: `cd backend && JAVA_HOME=$(/usr/libexec/java_home -v 17) ./gradlew bootRun`
    2. 프론트엔드 실행: `cd frontend && npm run dev`
    3. 로그인 후 새 문서 작성 (일반 업무 기안)
    4. 제목 + 본문 입력 후 에디터 상단 확인:
       - [삭제] [저장] 버튼이 회색 outline
       - [제출] 버튼이 파란색 (bg-blue-600)
    5. 제출 버튼 클릭 -> 확인 다이얼로그 표시 확인:
       - "제출 후에는 수정할 수 없습니다. 제출하시겠습니까?" 메시지
       - "제출" 확인 버튼 (파란색)
    6. "제출" 클릭 -> 제출 처리 중 로딩 확인 (스피너, form 불투명)
    7. 성공 시 문서 상세 페이지로 이동 확인:
       - 초록색 성공 메시지 "문서가 제출되었습니다 (GEN-2026-0001)" 표시
       - 5초 후 자동 사라짐 또는 X 클릭으로 닫기
       - 문서번호가 제목 옆에 표시
    8. 문서 목록으로 돌아가 문서번호 칼럼 확인:
       - 제출된 문서: GEN-2026-0001 표시
       - DRAFT 문서: '-' 표시
    9. 제출된 문서 상세 -> 에디터 모드 진입 불가 확인 (읽기 전용)
  </how-to-verify>
  <verify>수동 검증</verify>
  <done>제출 flow가 E2E로 정상 동작함을 사용자가 확인</done>
  <resume-signal>"approved" 또는 발견된 문제 기술</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> POST /api/v1/documents/{id}/submit | 프론트엔드에서 제출 API 호출 |
| React Router state | navigate state로 성공 메시지 전달 (client-only, tamper-safe) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering | DocumentEditorPage | mitigate | 이중 클릭 방지: submitMutation.isPending 상태에서 버튼 disabled + pointer-events-none (D-05) |
| T-06-07 | Tampering | DocumentEditorPage | mitigate | 프론트엔드 검증 우회 방지: 백엔드에서도 DRAFT 상태 + 필수 필드 검증 (D-10, Plan 01에서 보장) |
| T-06-08 | Information Disclosure | DocumentDetailPage | accept | 성공 메시지에 문서번호 노출 — 본인 문서이므로 정보 유출 아님 |
| T-06-09 | Spoofing | navigate state | accept | React Router state는 client-only이며, 악의적 state 주입 시 성공 메시지만 보임 (보안 영향 없음) |
</threat_model>

<verification>
1. TypeScript 컴파일: `cd frontend && npx tsc --noEmit`
2. 백엔드 테스트: `cd backend && JAVA_HOME=$(/usr/libexec/java_home -v 17) ./gradlew test --tests "com.micesign.document.*" -x compileQuerydsl`
3. 수동 E2E: 에디터 -> 제출 -> 상세 페이지 -> 목록 확인
</verification>

<success_criteria>
1. 에디터 상단에 [삭제(gray)] [저장(gray)] [제출(blue)] 버튼이 D-01, D-03 순서대로 배치됨
2. 제출 확인 다이얼로그가 D-02 사양대로 간단 경고 표시
3. 제출 중 D-05 로딩 오버레이 + 스피너 동작
4. D-12 자동 저장 후 제출, D-13 업로드 대기, D-14 실패 파일 차단 동작
5. 성공 시 D-17 리다이렉트 + D-18 성공 메시지 + D-19 자동 사라짐
6. D-20 문서번호 상세 페이지 표시 + D-21 목록 칼럼 추가
7. TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/06-document-submission-numbering/06-02-SUMMARY.md`
</output>
