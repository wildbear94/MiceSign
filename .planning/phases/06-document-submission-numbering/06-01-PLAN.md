---
phase: 06-document-submission-numbering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/resources/db/migration/V17__fix_leave_prefix.sql
  - backend/src/test/resources/db/testmigration/V17__fix_leave_prefix.sql
  - backend/src/main/java/com/micesign/service/DocumentService.java
  - backend/src/test/java/com/micesign/document/DocumentSubmitTest.java
autonomous: true
requirements:
  - DOC-03
  - DOC-04
  - DOC-07

must_haves:
  truths:
    - "DRAFT 문서를 제출하면 SUBMITTED 상태로 변경되고 문서번호가 부여된다"
    - "제출된 문서는 수정/삭제할 수 없다 (immutability)"
    - "동시 제출 시에도 문서번호 중복이 발생하지 않는다"
    - "결재선 없이도 제출이 가능하다 (Phase 7에서 필수화 예정)"
    - "LEAVE 템플릿 프리픽스가 LEV로 올바르게 적용된다"
  artifacts:
    - path: "backend/src/main/resources/db/migration/V17__fix_leave_prefix.sql"
      provides: "LEAVE prefix LVE -> LEV 수정"
      contains: "UPDATE approval_template SET prefix"
    - path: "backend/src/test/java/com/micesign/document/DocumentSubmitTest.java"
      provides: "제출 통합 테스트"
      min_lines: 80
  key_links:
    - from: "DocumentService.submitDocument"
      to: "DocSequenceRepository.findByTemplateCodeAndYearForUpdate"
      via: "PESSIMISTIC_WRITE lock"
      pattern: "generateDocNumber"
    - from: "DocumentController.submitDocument"
      to: "DocumentService.submitDocument"
      via: "POST /{id}/submit"
      pattern: "submitDocument"
---

<objective>
백엔드 제출 로직을 Phase 6 요구사항에 맞게 수정하고 통합 테스트를 작성한다.

Purpose: 기존 submitDocument는 결재선 필수를 요구하나, Phase 6에서는 결재선 없이 제출 허용(D-07). LEAVE 프리픽스 불일치(LVE->LEV)도 수정 필요. 이 플랜이 완료되면 프론트엔드가 안전하게 submit API를 호출할 수 있다.
Output: 수정된 submitDocument 메서드, Flyway migration, 통합 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-document-submission-numbering/06-CONTEXT.md
@.planning/phases/06-document-submission-numbering/06-RESEARCH.md
@.planning/phases/06-document-submission-numbering/06-VALIDATION.md

@backend/src/main/java/com/micesign/service/DocumentService.java
@backend/src/main/java/com/micesign/controller/DocumentController.java
@backend/src/main/java/com/micesign/domain/DocSequence.java
@backend/src/main/java/com/micesign/repository/DocSequenceRepository.java
@backend/src/test/java/com/micesign/document/DocumentControllerTest.java

<interfaces>
<!-- Key types and contracts the executor needs -->

From DocumentService.java (line 249-313):
```java
public DocumentDetailResponse submitDocument(Long docId, Long userId) {
    // loadAndVerifyOwnerDraft: checks DRAFT status + owner match
    // Validates title not blank
    // Current: validates approval lines (MUST BE REMOVED for Phase 6)
    // Runs formValidator.validate()
    // Calls generateDocNumber()
    // Sets status=SUBMITTED, docNumber, submittedAt
    // Current: sets currentStep from approval lines (MUST HANDLE empty case)
    // Moves attachments to permanent folder
    // Logs audit
    // Publishes events (MUST GUARD for empty approval lines)
}
```

From DocumentController.java (line 92-97):
```java
@PostMapping("/{id}/submit")
public ResponseEntity<ApiResponse<DocumentDetailResponse>> submitDocument(
        @PathVariable Long id,
        @AuthenticationPrincipal CustomUserDetails user) {
    DocumentDetailResponse detail = documentService.submitDocument(id, user.getUserId());
    return ResponseEntity.ok(ApiResponse.ok(detail));
}
```

From DocSequenceRepository.java:
```java
@Lock(LockModeType.PESSIMISTIC_WRITE)
@Query("SELECT ds FROM DocSequence ds WHERE ds.templateCode = :templateCode AND ds.year = :year")
Optional<DocSequence> findByTemplateCodeAndYearForUpdate(@Param("templateCode") String templateCode, @Param("year") int year);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flyway migration LEV 프리픽스 수정 + submitDocument 결재선 검증 제거</name>
  <files>
    backend/src/main/resources/db/migration/V17__fix_leave_prefix.sql,
    backend/src/test/resources/db/testmigration/V17__fix_leave_prefix.sql,
    backend/src/main/java/com/micesign/service/DocumentService.java
  </files>
  <read_first>
    backend/src/main/resources/db/migration/V2__seed_initial_data.sql,
    backend/src/test/resources/db/testmigration/V2__seed_initial_data.sql,
    backend/src/main/java/com/micesign/service/DocumentService.java,
    backend/src/main/java/com/micesign/repository/DocSequenceRepository.java,
    backend/src/main/java/com/micesign/domain/DocSequence.java
  </read_first>
  <action>
**1) Flyway migration V17__fix_leave_prefix.sql (main + test 동일 내용):**

per D-23, D-24 LEAVE 프리픽스를 LVE에서 LEV로 수정:

```sql
-- V17__fix_leave_prefix.sql
-- Fix LEAVE template prefix: LVE -> LEV (per Phase 6 D-23)
UPDATE approval_template SET prefix = 'LEV' WHERE code = 'LEAVE';
```

main 경로: `backend/src/main/resources/db/migration/V17__fix_leave_prefix.sql`
test 경로: `backend/src/test/resources/db/testmigration/V17__fix_leave_prefix.sql`

두 파일 내용은 동일해야 한다.

**2) DocumentService.submitDocument 수정 (line 249-313 영역):**

per D-07 결재선 없이 제출 허용. 다음 3개 블록을 수정한다:

**(a) 결재선 검증 블록 제거 (line 257-270):**
기존의 `hasApprover` 검증과 `drafterInLine` 검증을 제거하고, 결재선 조회만 유지한다. 주석으로 Phase 7 재활성화를 명시한다:

```java
// Validate approval lines
// TODO Phase 7: Re-enable approval line validation
// List<ApprovalLine> approvalLines = approvalLineRepository.findByDocumentIdOrderByStepOrderAsc(docId);
// boolean hasApprover = approvalLines.stream()
//         .anyMatch(line -> line.getLineType() == ApprovalLineType.APPROVE);
// if (!hasApprover) {
//     throw new BusinessException("APR_NO_APPROVER", "최소 1명의 승인자(승인 유형)를 추가해주세요.");
// }
// boolean drafterInLine = approvalLines.stream()
//         .anyMatch(line -> line.getApprover().getId().equals(userId));
// if (drafterInLine) {
//     throw new BusinessException("APR_SELF_NOT_ALLOWED", "기안자는 결재선에 포함될 수 없습니다.");
// }
```

**(b) currentStep 설정을 조건부로 변경 (line 285-291):**
결재선이 비어있으면 currentStep을 null로 유지한다:

```java
// Set currentStep to first non-REFERENCE step (if approval lines exist)
List<ApprovalLine> approvalLines = approvalLineRepository.findByDocumentIdOrderByStepOrderAsc(docId);
if (!approvalLines.isEmpty()) {
    int firstStep = approvalLines.stream()
            .filter(line -> line.getLineType() != ApprovalLineType.REFERENCE)
            .mapToInt(ApprovalLine::getStepOrder)
            .min()
            .orElse(1);
    document.setCurrentStep(firstStep);
}
// Phase 6: If no approval lines, currentStep stays null
```

**(c) 이벤트 발행을 조건부로 변경 (line 300-311):**
결재선이 비어있으면 ApprovalNotificationEvent를 발행하지 않는다:

```java
// Publish notification event only if approval lines exist
if (!approvalLines.isEmpty()) {
    eventPublisher.publishEvent(
            new ApprovalNotificationEvent(docId, NotificationEventType.SUBMIT.name(), userId));
}
```

BudgetIntegrationEvent는 결재선과 무관하므로 그대로 유지한다.

**3) 기존 loadAndVerifyOwnerDraft 메서드 확인:**
이 메서드가 DRAFT 상태와 owner를 검증하여 DOC-04 (immutability) 와 D-16 (본인 문서만 제출)을 이미 보장한다. 수정 불필요하나 존재를 확인한다.
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/backend && JAVA_HOME=$(/usr/libexec/java_home -v 17) ./gradlew test --tests "com.micesign.document.DocumentControllerTest" -x compileQuerydsl 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - V17__fix_leave_prefix.sql 파일이 main과 test 경로에 모두 존재하며 `UPDATE approval_template SET prefix = 'LEV' WHERE code = 'LEAVE'` 포함
    - DocumentService.java에서 `APR_NO_APPROVER` 문자열이 주석 처리됨 (활성 코드에 없음)
    - DocumentService.java에서 `APR_SELF_NOT_ALLOWED` 문자열이 주석 처리됨 (활성 코드에 없음)
    - DocumentService.java에서 `if (!approvalLines.isEmpty())` 조건으로 currentStep 설정이 감싸져 있음
    - DocumentService.java에서 ApprovalNotificationEvent 발행이 `if (!approvalLines.isEmpty())` 조건 안에 있음
    - DocumentService.java에서 `// TODO Phase 7: Re-enable approval line validation` 주석이 존재
    - 기존 DocumentControllerTest가 통과 (gradlew test 성공)
  </acceptance_criteria>
  <done>V17 migration이 적용되어 LEAVE prefix가 LEV로 수정되고, submitDocument가 결재선 없이도 정상 동작하며, 기존 테스트가 통과한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 제출 통합 테스트 작성</name>
  <files>
    backend/src/test/java/com/micesign/document/DocumentSubmitTest.java
  </files>
  <read_first>
    backend/src/test/java/com/micesign/document/DocumentControllerTest.java,
    backend/src/main/java/com/micesign/service/DocumentService.java,
    backend/src/main/java/com/micesign/controller/DocumentController.java,
    backend/src/test/resources/db/testmigration/V2__seed_initial_data.sql,
    backend/src/test/java/com/micesign/auth/TestTokenHelper.java
  </read_first>
  <behavior>
    - Test 1: 정상 제출 (DRAFT -> SUBMITTED) — POST /api/v1/documents/{id}/submit 호출 시 200 OK, 응답에 status=SUBMITTED, docNumber가 PREFIX-YYYY-NNNN 형식
    - Test 2: 문서번호 형식 검증 — GENERAL 템플릿은 GEN-{year}-0001, EXPENSE는 EXP-{year}-0001, LEAVE는 LEV-{year}-0001
    - Test 3: 비DRAFT 문서 제출 시 400 에러 — 이미 제출된 문서를 다시 제출하면 에러 (DOC-04 immutability, D-11)
    - Test 4: 타인 문서 제출 시 403 에러 — 다른 사용자가 문서 제출 시도 (D-16)
    - Test 5: 제목 없는 문서 제출 시 400 에러 — title이 blank인 문서 제출 (D-06 backend validation)
    - Test 6: 제출 후 수정 시도 시 에러 — SUBMITTED 상태 문서 update API 호출 시 에러 (DOC-04)
    - Test 7: 시퀀스 증가 검증 — 같은 템플릿 두 번째 제출 시 0002 번호 부여
  </behavior>
  <action>
기존 DocumentControllerTest 패턴을 참고하여 새 테스트 클래스 작성.

```java
package com.micesign.document;

@SpringBootTest
@AutoConfigureMockMvc
@ActiveProfiles("test")
@Transactional
class DocumentSubmitTest {

    @Autowired MockMvc mockMvc;
    @Autowired JdbcTemplate jdbcTemplate;
    @Autowired TestTokenHelper tokenHelper;

    private String accessToken;
    private Long userId;

    @BeforeEach
    void setUp() {
        // 기존 DocumentControllerTest.setUp() 패턴 참조:
        // 1. JdbcTemplate로 테스트 사용자 조회 또는 생성
        // 2. tokenHelper.generateAccessToken(userId, email, role)로 JWT 생성
        // 3. DRAFT 문서 생성 (INSERT document + document_content)
    }

    // Test 1: 정상 제출
    @Test
    void submitDraft_success() {
        // POST /api/v1/documents/{id}/submit with Authorization header
        // Assert: status 200, $.data.status == "SUBMITTED"
        // Assert: $.data.docNumber matches "GEN-{year}-0001" 패턴
        // Assert: $.data.submittedAt != null
    }

    // Test 2: LEAVE 프리픽스 LEV 확인
    @Test
    void submitLeaveDocument_prefixIsLEV() {
        // LEAVE 템플릿 문서 생성 + 제출
        // Assert: $.data.docNumber starts with "LEV-"
    }

    // Test 3: 이미 제출된 문서 재제출 시 에러
    @Test
    void submitAlreadySubmitted_returns400() {
        // 문서를 SUBMITTED 상태로 직접 설정 (UPDATE document SET status='SUBMITTED')
        // POST submit -> 400 에러
    }

    // Test 4: 타인 문서 제출 시 403
    @Test
    void submitOtherUserDocument_returns403() {
        // 다른 사용자 토큰으로 제출 시도
        // Assert: status 403
    }

    // Test 5: 제목 없는 문서 제출 시 400
    @Test
    void submitWithoutTitle_returns400() {
        // title이 빈 문서 생성 후 제출
        // Assert: status 400, error code "DOC_TITLE_REQUIRED"
    }

    // Test 6: 제출된 문서 수정 시도 시 에러
    @Test
    void updateSubmittedDocument_returns400() {
        // 문서 제출 후 PUT /api/v1/documents/{id} 호출
        // Assert: 에러 응답 (DRAFT가 아니므로 수정 불가)
    }

    // Test 7: 시퀀스 증가 검증
    @Test
    void submitTwice_sequenceIncrements() {
        // 같은 템플릿 문서 2개 생성 + 각각 제출
        // 첫 번째: GEN-{year}-0001
        // 두 번째: GEN-{year}-0002
    }
}
```

테스트 데이터 생성 시 JdbcTemplate 직접 INSERT 패턴 사용 (기존 DocumentControllerTest와 동일). `@Transactional`로 각 테스트 후 롤백. 문서 생성 시 document + document_content 모두 INSERT 필요 (formValidator가 content를 조회하므로).

GENERAL 템플릿의 formValidator는 bodyHtml이 비어있지 않은지 확인하므로, 테스트 데이터에 bodyHtml = '<p>test</p>' 설정 필수.
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/backend && JAVA_HOME=$(/usr/libexec/java_home -v 17) ./gradlew test --tests "com.micesign.document.DocumentSubmitTest" -x compileQuerydsl 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - DocumentSubmitTest.java 파일이 존재하며 7개 이상의 @Test 메서드 포함
    - `submitDraft_success` 테스트가 status SUBMITTED와 docNumber 형식 GEN-{year}-0001 검증
    - `submitLeaveDocument_prefixIsLEV` 테스트가 LEV- 프리픽스 검증
    - `submitAlreadySubmitted_returns400` 테스트가 비DRAFT 제출 차단 검증
    - `submitOtherUserDocument_returns403` 테스트가 타인 문서 제출 차단 검증
    - `updateSubmittedDocument_returns400` 테스트가 제출 후 수정 차단 검증 (DOC-04)
    - 모든 테스트 통과 (gradlew test 종료 코드 0)
  </acceptance_criteria>
  <done>7개 이상의 통합 테스트가 모두 GREEN이며, DOC-03 (정상 제출 + 번호 부여), DOC-04 (immutability), DOC-07 (시퀀스 증가 + LEV 프리픽스) 요구사항을 검증한다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> POST /api/v1/documents/{id}/submit | 인증된 사용자가 문서 제출 요청 |
| DocumentService -> DocSequenceRepository | 시퀀스 번호 생성 시 DB lock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | DocumentService.submitDocument | mitigate | loadAndVerifyOwnerDraft에서 drafter_id == userId 검증 (기존 구현, D-16) |
| T-06-02 | Tampering | DocumentService.submitDocument | mitigate | loadAndVerifyOwnerDraft에서 status == DRAFT 검증 — 제출/승인된 문서 수정 차단 (DOC-04) |
| T-06-03 | Tampering | DocSequenceRepository | mitigate | PESSIMISTIC_WRITE lock + unique constraint (V13)로 동시 제출 시 중복 번호 방지 (DOC-07) |
| T-06-04 | Denial of Service | POST /{id}/submit | accept | 50명 사내 시스템, 동시 대량 제출 가능성 낮음. Rate limiting 불필요 |
| T-06-05 | Repudiation | DocumentService.submitDocument | mitigate | auditLogService.log()로 제출 이력 기록 (기존 구현) |
</threat_model>

<verification>
cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/backend && JAVA_HOME=$(/usr/libexec/java_home -v 17) ./gradlew test --tests "com.micesign.document.*" -x compileQuerydsl
</verification>

<success_criteria>
1. V17 migration이 main/test 모두 존재하며 LEAVE prefix가 LEV로 수정됨
2. submitDocument가 결재선 없이 정상 동작 (APR_NO_APPROVER 검증 비활성)
3. 이벤트 발행이 결재선 없을 때 안전하게 skip됨
4. 7개 이상의 통합 테스트 모두 GREEN
5. 기존 DocumentControllerTest도 통과 (regression 없음)
</success_criteria>

<output>
After completion, create `.planning/phases/06-document-submission-numbering/06-01-SUMMARY.md`
</output>
