---
phase: 07-approval-workflow
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vitest.config.ts
  - frontend/src/test/setup.ts
  - frontend/src/features/approval/components/__tests__/ApprovalLineEditor.test.tsx
  - frontend/src/features/approval/components/__tests__/ApprovalActionPanel.test.tsx
  - frontend/src/features/approval/components/__tests__/ApprovalLineTimeline.test.tsx
  - frontend/src/features/document/pages/__tests__/DocumentDetailPage.approval.test.tsx
autonomous: true
requirements:
  - APR-01
  - APR-02
  - APR-03
  - APR-04
  - APR-05
  - APR-06
  - APR-07

must_haves:
  truths:
    - "Vitest and testing-library are installed and configured"
    - "vitest run exits 0 with stub tests"
    - "Test stubs exist for all approval components that Plans 02 and 03 will create"
  artifacts:
    - path: "frontend/vitest.config.ts"
      provides: "Vitest configuration for React component testing"
      contains: "defineConfig"
    - path: "frontend/src/test/setup.ts"
      provides: "Testing-library setup with jsdom"
      contains: "@testing-library/jest-dom"
    - path: "frontend/src/features/approval/components/__tests__/ApprovalLineEditor.test.tsx"
      provides: "Test stubs for approval line editor behaviors"
      contains: "describe"
    - path: "frontend/src/features/approval/components/__tests__/ApprovalActionPanel.test.tsx"
      provides: "Test stubs for approval action behaviors"
      contains: "describe"
  key_links:
    - from: "frontend/vitest.config.ts"
      to: "frontend/src/test/setup.ts"
      via: "setupFiles configuration"
      pattern: "setupFiles.*setup"
---

<objective>
Set up frontend test infrastructure (Vitest + testing-library) and create behavioral test stubs for all approval workflow components.

Purpose: Wave 0 establishes the test scaffolding that Plans 01-03 executors can run after each task. Without this, frontend plans have no behavioral automated verification — only tsc type-checking. This satisfies the Nyquist sampling requirement.
Output: Vitest configured, testing-library installed, test stubs for APR-01 through APR-07 behaviors ready to be filled in by component plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-approval-workflow/07-CONTEXT.md
@.planning/phases/07-approval-workflow/07-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest + testing-library and configure test environment</name>
  <files>
    frontend/package.json
    frontend/vitest.config.ts
    frontend/src/test/setup.ts
  </files>
  <read_first>
    frontend/package.json
    frontend/vite.config.ts
    frontend/tsconfig.json
  </read_first>
  <action>
1. Install test dependencies:
```bash
cd frontend && npm install -D vitest @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom
```

2. Create `frontend/vitest.config.ts`:
```typescript
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: ['./src/test/setup.ts'],
    include: ['src/**/*.test.{ts,tsx}'],
    css: false,
  },
});
```

3. Create `frontend/src/test/setup.ts`:
```typescript
import '@testing-library/jest-dom';
```

4. Add test script to `package.json`:
```json
"scripts": {
  "test": "vitest run",
  "test:watch": "vitest"
}
```

5. Add `"types": ["vitest/globals"]` to `tsconfig.json` compilerOptions if not present, so `describe`, `it`, `expect` are globally available.

6. Verify with: `cd frontend && npx vitest run` — should exit 0 (no tests yet, but config valid).
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>Vitest configured and runs successfully. testing-library/react and jest-dom installed. Test script added to package.json.</done>
</task>

<task type="auto">
  <name>Task 2: Create behavioral test stubs for approval components (APR-01 through APR-07)</name>
  <files>
    frontend/src/features/approval/components/__tests__/ApprovalLineEditor.test.tsx
    frontend/src/features/approval/components/__tests__/ApprovalActionPanel.test.tsx
    frontend/src/features/approval/components/__tests__/ApprovalLineTimeline.test.tsx
    frontend/src/features/document/pages/__tests__/DocumentDetailPage.approval.test.tsx
  </files>
  <action>
Create test stub files with `describe` blocks and `it.todo()` entries that document the expected behaviors. These stubs will be filled in by Plans 02 and 03 executors as they build the components.

1. **`ApprovalLineEditor.test.tsx`** — Covers APR-01, APR-02:
```typescript
import { describe, it } from 'vitest';

describe('ApprovalLineEditor', () => {
  // APR-01: User can build an approval line selecting approvers
  it.todo('renders empty state when no approvers added');
  it.todo('calls onAdd when user is selected from org picker');
  it.todo('prevents adding drafter (self) to approval line (D-05)');
  it.todo('prevents adding duplicate user to approval line (D-08)');
  it.todo('enforces maximum 10 approvers limit (D-09)');
  it.todo('allows drag-and-drop reorder of sequential approvers (D-03)');
  it.todo('separates REFERENCE items into distinct section (D-10)');
  it.todo('defaults new approver type to APPROVE (D-06)');

  // APR-02: Sequential processing types
  it.todo('assigns sequential stepOrder to APPROVE and AGREE types');
  it.todo('assigns stepOrder 0 to REFERENCE type');
});

describe('toApprovalLineRequests', () => {
  it.todo('converts ApprovalLineItem[] to ApprovalLineRequest[] with correct stepOrder');
  it.todo('assigns stepOrder 0 to REFERENCE items');
});

describe('toApprovalLineItems', () => {
  it.todo('converts ApprovalLineResponse[] back to ApprovalLineItem[]');
});
```

2. **`ApprovalActionPanel.test.tsx`** — Covers APR-03, APR-04:
```typescript
import { describe, it } from 'vitest';

describe('ApprovalActionPanel', () => {
  // APR-03: Approver can approve or reject
  it.todo('shows approve and reject buttons when canApprove is true');
  it.todo('shows "not your turn" message when canApprove is false (D-14)');
  it.todo('calls approve mutation on approve button click (D-15)');
  it.todo('shows success message after approve');

  // APR-04: Rejection with mandatory comment
  it.todo('requires comment for rejection (D-12)');
  it.todo('shows error when rejecting without comment');
  it.todo('shows confirmation dialog before rejection (D-17)');
  it.todo('calls reject mutation with comment after confirmation');
  it.todo('disables buttons during pending mutation');
});
```

3. **`ApprovalLineTimeline.test.tsx`** — Covers APR-05:
```typescript
import { describe, it } from 'vitest';

describe('ApprovalLineTimeline', () => {
  // APR-05: Approval status display
  it.todo('renders sequential approvers with step numbers');
  it.todo('shows APPROVED badge for approved steps');
  it.todo('shows REJECTED badge for rejected steps');
  it.todo('shows PENDING badge for pending steps');
  it.todo('highlights current approver step');
  it.todo('shows comment text when present');
  it.todo('renders REFERENCE items in separate section (D-10)');
  it.todo('shows acted-at timestamp for completed steps');
});
```

4. **`DocumentDetailPage.approval.test.tsx`** — Covers APR-06, APR-07:
```typescript
import { describe, it } from 'vitest';

describe('DocumentDetailPage - Approval Actions', () => {
  // APR-06: Withdrawal
  it.todo('shows withdraw button when drafter and next approver has not acted (D-18, D-19)');
  it.todo('hides withdraw button when condition not met (D-19)');
  it.todo('shows confirmation dialog on withdraw click (D-20)');
  it.todo('calls withdraw mutation on confirm');
  it.todo('stays on page after withdrawal (D-22)');

  // APR-07: Resubmission
  it.todo('shows resubmit button on REJECTED document for drafter (D-23)');
  it.todo('shows resubmit button on WITHDRAWN document for drafter (D-23)');
  it.todo('calls rewrite mutation on resubmit click');
  it.todo('navigates to new draft editor after resubmit (D-26)');
});
```

Each test file uses only `it.todo()` — no imports of components that do not exist yet. The stubs establish the behavioral contract. Plan 02 executor fills in ApprovalLineEditor tests. Plan 03 executor fills in ApprovalActionPanel, Timeline, and DetailPage tests.
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Four test stub files created covering APR-01 through APR-07 behavioral expectations. `vitest run` exits 0 with all tests showing as todo (not failing). Test stubs reference specific decision IDs (D-03, D-05, D-06, etc.) for traceability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Test infrastructure only — no production code or trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | No threats — test infrastructure only |
</threat_model>

<verification>
1. `cd frontend && npx vitest run --reporter=verbose` exits 0
2. All four test stub files exist with `describe` blocks
3. `package.json` contains vitest, @testing-library/react, @testing-library/jest-dom in devDependencies
</verification>

<success_criteria>
- Vitest configured with jsdom environment and testing-library setup
- `npx vitest run` exits 0
- Test stubs exist for ApprovalLineEditor, ApprovalActionPanel, ApprovalLineTimeline, DocumentDetailPage approval behaviors
- All APR-01 through APR-07 requirements have at least one `it.todo()` test entry
</success_criteria>

<output>
After completion, create `.planning/phases/07-approval-workflow/07-00-SUMMARY.md`
</output>
