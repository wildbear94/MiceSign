---
phase: 23-table-column-editor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/admin/components/SchemaFieldEditor/types.ts
  - frontend/src/features/admin/components/SchemaFieldEditor/constants.ts
  - frontend/src/features/admin/components/SchemaFieldEditor/TableColumnEditor.tsx
  - frontend/src/features/admin/components/SchemaFieldEditor/FieldConfigEditor.tsx
  - frontend/src/features/admin/components/SchemaFieldEditor/index.tsx
  - frontend/package.json
  - frontend/package-lock.json
autonomous: true
requirements:
  - TBL-01

must_haves:
  truths:
    - "table 타입 필드 선택 시 컬럼 편집 UI가 표시된다"
    - "컬럼 추가 버튼으로 새 컬럼을 추가할 수 있다"
    - "컬럼 삭제 버튼으로 즉시 삭제된다"
    - "드래그 앤 드롭으로 컬럼 순서를 변경할 수 있다"
    - "컬럼 행을 클릭하면 세부 설정이 펼쳐진다"
  artifacts:
    - path: "frontend/src/features/admin/components/SchemaFieldEditor/types.ts"
      provides: "TableColumn interface + TableColumnType + SchemaFieldConfig extensions"
      contains: "TableColumn"
    - path: "frontend/src/features/admin/components/SchemaFieldEditor/TableColumnEditor.tsx"
      provides: "Column list with DnD + add/delete/expand"
      min_lines: 80
    - path: "frontend/src/features/admin/components/SchemaFieldEditor/FieldConfigEditor.tsx"
      provides: "case 'table' rendering TableColumnEditor"
      contains: "case 'table'"
  key_links:
    - from: "FieldConfigEditor.tsx"
      to: "TableColumnEditor.tsx"
      via: "case 'table' renders <TableColumnEditor>"
      pattern: "<TableColumnEditor"
    - from: "TableColumnEditor.tsx"
      to: "@dnd-kit/sortable"
      via: "DndContext + SortableContext"
      pattern: "DndContext"
---

<objective>
TableColumn 타입 정의 + @dnd-kit 설치 + TableColumnEditor 핵심 컴포넌트(컬럼 추가/삭제/DnD 순서변경/접기펼치기) 구현 + FieldConfigEditor에 case 'table' 추가

Purpose: table 타입 필드의 컬럼 구조를 시각적으로 관리할 수 있는 기반 UI를 구축한다
Output: TableColumnEditor.tsx, 확장된 types.ts, FieldConfigEditor에 case 'table' 연결
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-table-column-editor/23-CONTEXT.md
@.planning/phases/23-table-column-editor/23-RESEARCH.md
@.planning/phases/23-table-column-editor/23-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From frontend/src/features/admin/components/SchemaFieldEditor/types.ts:
```typescript
export type SchemaFieldType =
  | 'text' | 'textarea' | 'number' | 'date' | 'select' | 'staticText' | 'hidden' | 'table';

export interface SchemaFieldConfig {
  placeholder?: string;
  maxLength?: number;
  min?: number;
  max?: number;
  unit?: string;
  options?: { value: string; label: string }[];
  content?: string;
  defaultValue?: string;
  width?: string;
}

export interface SchemaField {
  id: string;
  type: SchemaFieldType;
  label: string;
  required: boolean;
  config: SchemaFieldConfig;
}
```

From frontend/src/features/admin/components/SchemaFieldEditor/FieldConfigEditor.tsx:
```typescript
export function FieldConfigEditor({
  field,
  onConfigChange,
}: {
  field: SchemaField;
  onConfigChange: (config: SchemaFieldConfig) => void;
}) {
  // switch(field.type) { case 'text': ... case 'select': ... }
  // case 'table' 추가 대상 (line 19 switch문)
}
```

From frontend/src/features/admin/components/SchemaFieldEditor/constants.ts:
```typescript
export const FIELD_TYPES: SchemaFieldType[] = [
  'text', 'textarea', 'number', 'date', 'select', 'staticText', 'hidden',
]; // 'table' 미포함 — 이 Plan에서 추가 필요

export const FIELD_TYPE_META: Record<SchemaFieldType, { icon; color; bgColor }> = {
  // ... table: { icon: Table, color: 'text-teal-700 ...', bgColor: 'bg-teal-100 ...' } 이미 존재
};

export const SMALL_INPUT_CLASS = 'w-full h-9 px-3 text-sm border rounded-lg ...';
```

From frontend/src/features/document/types/dynamicForm.ts (호환성 참조):
```typescript
export interface ColumnDefinition {
  id: string;
  type: FieldType;
  label: string;
  required?: boolean;
  config?: FieldConfig;
}
// TableColumn은 이 구조와 호환되어야 함
```

From frontend/src/features/admin/components/SchemaFieldEditor/FieldCard.tsx:
```typescript
// 접기/펼치기 패턴: ChevronRight + rotate-90, expanded && <content>
// 헤더: flex items-center gap-3 px-4 py-3 cursor-pointer select-none
// 삭제 버튼: p-1 text-gray-400 hover:text-red-600 ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: @dnd-kit 설치 + TableColumn 타입 정의 + constants 확장</name>
  <files>
    frontend/package.json
    frontend/src/features/admin/components/SchemaFieldEditor/types.ts
    frontend/src/features/admin/components/SchemaFieldEditor/constants.ts
  </files>
  <read_first>
    frontend/package.json
    frontend/src/features/admin/components/SchemaFieldEditor/types.ts
    frontend/src/features/admin/components/SchemaFieldEditor/constants.ts
    frontend/src/features/document/types/dynamicForm.ts
  </read_first>
  <action>
1. @dnd-kit 패키지 설치 (per D-07, D-08):
   ```bash
   cd frontend && npm install @dnd-kit/core@6.3.1 @dnd-kit/sortable@10.0.0
   ```

2. types.ts 확장 (per D-04, D-06, D-11, D-12, D-14):
   - 파일 상단에 `TableColumnType` 타입 추가:
     ```typescript
     export type TableColumnType =
       | 'text'
       | 'textarea'
       | 'number'
       | 'date'
       | 'select'
       | 'checkbox'
       | 'staticText';
     ```
   - `TableColumn` 인터페이스 추가 (dynamicForm.ts의 ColumnDefinition과 구조적 호환):
     ```typescript
     export interface TableColumn {
       id: string;
       type: TableColumnType;
       label: string;
       required: boolean;
       config: SchemaFieldConfig;
     }
     ```
   - `SchemaFieldConfig`에 3개 속성 추가:
     ```typescript
     columns?: TableColumn[];
     minRows?: number;
     maxRows?: number;
     ```
     기존 속성(`placeholder`, `maxLength`, `min`, `max`, `unit`, `options`, `content`, `defaultValue`, `width`) 아래에 추가.

3. constants.ts 확장:
   - `FIELD_TYPES` 배열에 `'table'` 추가 (배열 마지막에):
     ```typescript
     export const FIELD_TYPES: SchemaFieldType[] = [
       'text', 'textarea', 'number', 'date', 'select', 'staticText', 'hidden', 'table',
     ];
     ```
   - `COLUMN_TYPE_META` 상수 추가 (컬럼 타입용 — 기존 FIELD_TYPE_META에서 해당 타입 재활용 + checkbox 신규):
     ```typescript
     import { CheckSquare } from 'lucide-react';
     
     export const COLUMN_TYPE_META: Record<
       TableColumnType,
       { icon: React.ElementType; color: string; bgColor: string }
     > = {
       text: FIELD_TYPE_META.text,
       textarea: FIELD_TYPE_META.textarea,
       number: FIELD_TYPE_META.number,
       date: FIELD_TYPE_META.date,
       select: FIELD_TYPE_META.select,
       checkbox: { icon: CheckSquare, color: 'text-cyan-700 dark:text-cyan-300', bgColor: 'bg-cyan-100 dark:bg-cyan-900/40' },
       staticText: FIELD_TYPE_META.staticText,
     };
     ```
   - `COLUMN_TYPES` 배열 추가:
     ```typescript
     export const COLUMN_TYPES: TableColumnType[] = [
       'text', 'textarea', 'number', 'date', 'select', 'checkbox', 'staticText',
     ];
     ```
   - 파일 상단 import에 `TableColumnType` 추가: `import type { SchemaFieldType, TableColumnType } from './types';`
   - Lucide import에 `CheckSquare` 추가.

4. index.tsx에 `TableColumn`, `TableColumnType` export 추가:
   ```typescript
   export type { SchemaField, SchemaFieldType, SchemaFieldConfig, TableColumn, TableColumnType } from './types';
   ```
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - types.ts contains `export type TableColumnType =`
    - types.ts contains `export interface TableColumn {`
    - types.ts contains `columns?: TableColumn[]`
    - types.ts contains `minRows?: number`
    - types.ts contains `maxRows?: number`
    - constants.ts contains `'table'` in FIELD_TYPES array
    - constants.ts contains `export const COLUMN_TYPE_META`
    - constants.ts contains `export const COLUMN_TYPES`
    - constants.ts contains `CheckSquare`
    - package.json contains `@dnd-kit/core`
    - package.json contains `@dnd-kit/sortable`
    - index.tsx contains `TableColumn`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>TableColumn/TableColumnType 타입이 정의되고, SchemaFieldConfig에 columns/minRows/maxRows 추가, constants에 COLUMN_TYPE_META/COLUMN_TYPES/FIELD_TYPES table 추가, @dnd-kit 설치 완료, TypeScript 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 2: TableColumnEditor 컴포넌트 생성 + FieldConfigEditor case 'table' 연결</name>
  <files>
    frontend/src/features/admin/components/SchemaFieldEditor/TableColumnEditor.tsx
    frontend/src/features/admin/components/SchemaFieldEditor/FieldConfigEditor.tsx
  </files>
  <read_first>
    frontend/src/features/admin/components/SchemaFieldEditor/types.ts
    frontend/src/features/admin/components/SchemaFieldEditor/constants.ts
    frontend/src/features/admin/components/SchemaFieldEditor/FieldCard.tsx
    frontend/src/features/admin/components/SchemaFieldEditor/TypeBadge.tsx
    frontend/src/features/admin/components/SchemaFieldEditor/FieldConfigEditor.tsx
    frontend/public/locales/ko/admin.json
    .planning/phases/23-table-column-editor/23-UI-SPEC.md
  </read_first>
  <action>
1. TableColumnEditor.tsx 신규 생성 — 컬럼 목록 관리 + DnD + 접기/펼치기 + 추가/삭제:

   Props 인터페이스:
   ```typescript
   interface TableColumnEditorProps {
     columns: TableColumn[];
     onColumnsChange: (columns: TableColumn[]) => void;
   }
   ```

   핵심 구조:
   - `useState<string | null>(null)` — expandedColumnId (한 번에 하나만 펼침)
   - DndContext + SortableContext (verticalListSortingStrategy) 래핑
   - sensors: `useSensors(useSensor(PointerSensor, { activationConstraint: { distance: 8 } }), useSensor(KeyboardSensor))`
   - handleDragEnd: `arrayMove(columns, oldIndex, newIndex)` 패턴 사용

   컬럼 추가 (per D-09 빈 상태 시작, D-10 최대 20개 제한):
   ```typescript
   const addColumn = () => {
     if (columns.length >= 20) return;
     const newCol: TableColumn = {
       id: `col_${Date.now().toString(36)}`,
       type: 'text',
       label: '',
       required: false,
       config: {},
     };
     onColumnsChange([...columns, newCol]);
     setExpandedColumnId(newCol.id); // 새 컬럼 자동 펼침
   };
   ```

   컬럼 삭제 (per D-03 확인 없이 즉시):
   ```typescript
   const deleteColumn = (columnId: string) => {
     onColumnsChange(columns.filter(c => c.id !== columnId));
     if (expandedColumnId === columnId) setExpandedColumnId(null);
   };
   ```

   컬럼 업데이트:
   ```typescript
   const updateColumn = (columnId: string, updated: TableColumn) => {
     onColumnsChange(columns.map(c => c.id === columnId ? updated : c));
   };
   ```

   레이아웃 (UI-SPEC 참조):
   - 상단: 섹션 헤더 (`text-xs font-medium text-gray-600 dark:text-gray-400`) "컬럼 목록" + [컬럼 추가] 버튼 (per UI-SPEC: `text-blue-600 dark:text-blue-400`, Plus 아이콘 w-3.5 h-3.5)
   - 빈 상태 (0 컬럼): 안내 메시지 `t('templates.columnEmpty')` — `border border-dashed border-gray-300 dark:border-gray-600 rounded-lg p-6 text-center text-sm text-gray-400 dark:text-gray-500`
   - 컬럼 20개일 때: 추가 버튼 disabled + title에 `t('templates.columnMaxError')`

   각 컬럼 행 (SortableColumnRow 인라인 또는 내부 컴포넌트):
   - `useSortable({ id: column.id })` 사용
   - style: `{ transform: CSS.Transform.toString(transform), transition }`
   - 접힌 상태 (per UI-SPEC):
     - `div` with `bg-gray-50 dark:bg-gray-700 border border-gray-200 dark:border-gray-700 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-600 transition-colors`
     - 내부: flex items-center gap-2 px-3 py-2
     - [GripVertical w-4 h-4 text-gray-400 hover:text-gray-600 cursor-grab] — `{...attributes} {...listeners}` 바인딩, aria-label="컬럼 순서 변경"
     - [ChevronRight w-4 h-4 transition-transform, expanded ? 'rotate-90' : '']
     - COLUMN_TYPE_META 기반 타입 뱃지 (TypeBadge와 동일 패턴이나 TableColumnType용):
       ```
       <span className={`inline-flex items-center gap-1 px-2 py-0.5 text-xs font-medium rounded-full ${meta.bgColor} ${meta.color}`}>
         <Icon className="w-3 h-3" /> {t(`templates.columnType${capitalize(col.type)}`)}
       </span>
       ```
       i18n 키: `templates.columnTypeText`, `templates.columnTypeNumber` 등 사용
     - 라벨 텍스트: `text-sm text-gray-900 dark:text-gray-50 truncate flex-1` — `col.label || t('templates.noLabel')`
     - [Trash2 w-4 h-4] 삭제 버튼: `text-gray-400 hover:text-red-500 dark:hover:text-red-400 hover:bg-red-50 dark:hover:bg-red-900/20 rounded p-1 transition-colors`
       onClick stopPropagation + deleteColumn(col.id)

   - 펼친 상태 (per D-02, UI-SPEC):
     - 행 배경: `bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg`
     - 헤더 아래 구분선: `border-t border-gray-100 dark:border-gray-700`
     - 세부 설정 영역: `px-4 pb-3 pt-2 space-y-3`
     - 기본 필드 (라벨 + ID + 타입 + 필수):
       ```
       grid grid-cols-2 gap-3:
         - 컬럼 라벨 (text input, SMALL_INPUT_CLASS)
         - 컬럼 ID (text input, SMALL_INPUT_CLASS, readonly 스타일은 아닌데 수동 편집 가능)
         - 컬럼 타입 (select dropdown, SMALL_INPUT_CLASS, COLUMN_TYPES 순회):
           <select value={col.type} onChange={...}>
             {COLUMN_TYPES.map(t => <option key={t} value={t}>{t(`templates.columnType${capitalize(t)}`)}</option>)}
           </select>
         - 필수 여부 (checkbox): 
           <label className="flex items-center gap-2 cursor-pointer">
             <input type="checkbox" className="w-4 h-4 rounded border-gray-300 text-blue-600 focus:ring-blue-600" />
             <span className="text-sm text-gray-700 dark:text-gray-300">{t('templates.fieldRequired')}</span>
           </label>
       ```
     - 타입별 세부 설정: Plan 02의 ColumnConfigPanel에서 구현. 이 Task에서는 placeholder 주석만: `{/* ColumnConfigPanel will be added in Plan 02 */}`

   CSS.Transform import:
   ```typescript
   import { CSS } from '@dnd-kit/utilities';
   ```
   만약 @dnd-kit/utilities가 @dnd-kit/sortable에 포함되어 있지 않으면:
   ```bash
   cd frontend && npm install @dnd-kit/utilities
   ```

2. FieldConfigEditor.tsx에 case 'table' 추가 (per D-01):
   - import 추가: `import { TableColumnEditor } from './TableColumnEditor';`
   - switch문의 `case 'hidden':` 위에 `case 'table':` 추가:
     ```typescript
     case 'table':
       return (
         <TableColumnEditor
           columns={config.columns || []}
           onColumnsChange={(columns) => updateConfig({ columns })}
         />
       );
     ```
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - TableColumnEditor.tsx exists and contains `DndContext`
    - TableColumnEditor.tsx contains `SortableContext`
    - TableColumnEditor.tsx contains `useSortable`
    - TableColumnEditor.tsx contains `GripVertical`
    - TableColumnEditor.tsx contains `ChevronRight`
    - TableColumnEditor.tsx contains `arrayMove`
    - TableColumnEditor.tsx contains `columns.length >= 20`
    - TableColumnEditor.tsx contains `Date.now().toString(36)`
    - FieldConfigEditor.tsx contains `case 'table':`
    - FieldConfigEditor.tsx contains `<TableColumnEditor`
    - FieldConfigEditor.tsx contains `onColumnsChange={(columns) => updateConfig({ columns })`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>TableColumnEditor가 DnD 순서변경, 추가(max 20), 즉시삭제, 접기/펼치기를 지원하고, FieldConfigEditor의 case 'table'에서 렌더링되며, TypeScript 컴파일 성공</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI input | 관리자가 입력하는 컬럼 라벨/ID/설정값 — 서버 전송 전 프론트엔드 밸리데이션 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | DoS | TableColumnEditor | mitigate | max 20 컬럼 제한 (D-10), addColumn에서 `columns.length >= 20` 체크 |
| T-23-02 | Tampering | Column data | accept | 프론트엔드 전용 — 서버 측 저장 시 백엔드 밸리데이션 존재. 이 Phase는 UI만 담당 |
</threat_model>

<verification>
1. `cd frontend && npx tsc --noEmit` — TypeScript 컴파일 에러 없음
2. `cd frontend && npm run build` — 프로덕션 빌드 성공
3. 브라우저에서 양식 편집 모달 열기 → table 타입 필드 추가 → 컬럼 편집 UI 표시 확인
</verification>

<success_criteria>
- table 타입 필드 선택 시 FieldConfigEditor에 컬럼 편집 UI 표시
- 컬럼 추가/삭제/DnD 순서변경 동작
- 컬럼 행 클릭 시 세부 설정 영역 접기/펼치기
- TypeScript 컴파일 성공
</success_criteria>

<output>
After completion, create `.planning/phases/23-table-column-editor/23-01-SUMMARY.md`
</output>
