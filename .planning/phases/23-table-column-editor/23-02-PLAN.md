---
phase: 23-table-column-editor
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - frontend/src/features/admin/components/SchemaFieldEditor/ColumnConfigPanel.tsx
  - frontend/src/features/admin/components/SchemaFieldEditor/TableColumnEditor.tsx
  - frontend/src/features/admin/components/SchemaFieldEditor/FieldConfigEditor.tsx
  - frontend/src/features/admin/components/TemplateFormModal.tsx
  - frontend/src/features/admin/components/FormPreview/PreviewFieldRenderer.tsx
  - frontend/public/locales/ko/admin.json
  - frontend/public/locales/en/admin.json
autonomous: false
requirements:
  - TBL-01
  - TBL-02

must_haves:
  truths:
    - "관리자가 각 컬럼의 타입(text/number/date/select/textarea/checkbox/staticText)을 설정할 수 있다"
    - "관리자가 각 컬럼의 라벨과 필수여부를 설정할 수 있다"
    - "text 컬럼에서 placeholder/maxLength, number 컬럼에서 min/max/unit, select 컬럼에서 options를 설정할 수 있다"
    - "minRows/maxRows 행 설정이 가능하다"
    - "컬럼 변경사항이 미리보기 패널의 테이블에 실시간 반영된다"
    - "미리보기 테이블에 컬럼 헤더 + 샘플 행 2개 + 비활성 행 추가 버튼이 표시된다"
    - "저장 시 table 타입 필드에 최소 1개 컬럼이 없으면 에러가 표시되고 저장이 차단된다"
  artifacts:
    - path: "frontend/src/features/admin/components/SchemaFieldEditor/ColumnConfigPanel.tsx"
      provides: "Type-specific column config (reuses FieldConfigEditor patterns)"
      min_lines: 50
    - path: "frontend/src/features/admin/components/FormPreview/PreviewFieldRenderer.tsx"
      provides: "Real table preview with column headers + sample rows"
      contains: "<table"
    - path: "frontend/public/locales/ko/admin.json"
      provides: "i18n keys for column editor"
      contains: "columnEmpty"
    - path: "frontend/src/features/admin/components/TemplateFormModal.tsx"
      provides: "Save-time validation guard for table columns minimum"
      contains: "columnMinError"
  key_links:
    - from: "TableColumnEditor.tsx"
      to: "ColumnConfigPanel.tsx"
      via: "expanded column renders <ColumnConfigPanel>"
      pattern: "<ColumnConfigPanel"
    - from: "PreviewFieldRenderer.tsx"
      to: "field.config.columns"
      via: "table case reads columns array for headers"
      pattern: "config\\.columns"
    - from: "TemplateFormModal.tsx"
      to: "schemaFields"
      via: "onSubmit validates table fields have >= 1 column before save"
      pattern: "columns.*length"
---

<objective>
ColumnConfigPanel(타입별 컬럼 세부 설정) 구현 + TableColumnEditor에 행 설정(minRows/maxRows) 추가 + PreviewFieldRenderer 테이블 미리보기 교체 + 저장 시 최소 1개 컬럼 밸리데이션 가드 + i18n 키 추가

Purpose: 컬럼별 상세 설정과 미리보기 연동을 완성하고, 저장 경로에 컬럼 최소 요건 가드를 추가하여 Phase 23 전체 기능을 완료한다
Output: ColumnConfigPanel.tsx, 확장된 PreviewFieldRenderer, TemplateFormModal 저장 가드, i18n 키 추가
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-table-column-editor/23-CONTEXT.md
@.planning/phases/23-table-column-editor/23-RESEARCH.md
@.planning/phases/23-table-column-editor/23-UI-SPEC.md
@.planning/phases/23-table-column-editor/23-01-SUMMARY.md

<interfaces>
<!-- Types created by Plan 01 -->

From frontend/src/features/admin/components/SchemaFieldEditor/types.ts (after Plan 01):
```typescript
export type TableColumnType =
  | 'text' | 'textarea' | 'number' | 'date' | 'select' | 'checkbox' | 'staticText';

export interface TableColumn {
  id: string;
  type: TableColumnType;
  label: string;
  required: boolean;
  config: SchemaFieldConfig;
}

export interface SchemaFieldConfig {
  placeholder?: string;
  maxLength?: number;
  min?: number;
  max?: number;
  unit?: string;
  options?: { value: string; label: string }[];
  content?: string;
  defaultValue?: string;
  width?: string;
  columns?: TableColumn[];
  minRows?: number;
  maxRows?: number;
}
```

From frontend/src/features/admin/components/SchemaFieldEditor/constants.ts (after Plan 01):
```typescript
export const COLUMN_TYPE_META: Record<TableColumnType, { icon; color; bgColor }>;
export const COLUMN_TYPES: TableColumnType[];
export const SMALL_INPUT_CLASS: string;
```

From frontend/src/features/admin/components/SchemaFieldEditor/FieldConfigEditor.tsx:
```typescript
// select 타입의 옵션 편집 패턴 (lines 119-174):
// - options.map((opt, idx) => ...)로 각 항목 렌더링
// - input value/label, Trash2 삭제, Plus 추가 버튼
// - SMALL_INPUT_CLASS 사용
// - updateConfig({ options: newOpts }) 패턴
// ColumnConfigPanel에서 동일 패턴 재활용

// number 타입 설정 패턴 (lines 54-110):
// - grid grid-cols-2 gap-3
// - min, max, unit, placeholder 필드
```

From frontend/src/features/admin/components/FormPreview/PreviewFieldRenderer.tsx:
```typescript
// 현재 table case (lines 76-83): placeholder div만 있음
// 이것을 실제 <table> 렌더링으로 교체

const DISABLED_INPUT_CLASS =
  'w-full h-10 px-3 border border-gray-300 dark:border-gray-600 rounded-lg bg-gray-50 dark:bg-gray-700 text-gray-400 text-sm';
```

From frontend/src/features/admin/components/TemplateFormModal.tsx:
```typescript
// onSubmit handler (line 125):
// const onSubmit = async (data: TemplateFormData) => {
//   const schemaDefinition = JSON.stringify({ version: 1, fields: schemaFields, ... });
//   // ...save logic...
// };
// schemaFields는 컴포넌트 state — table 타입 필드의 columns 밸리데이션 추가 대상
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ColumnConfigPanel 생성 + TableColumnEditor에 행 설정 추가 + 저장 시 컬럼 최소 1개 밸리데이션 가드 + i18n 키 추가</name>
  <files>
    frontend/src/features/admin/components/SchemaFieldEditor/ColumnConfigPanel.tsx
    frontend/src/features/admin/components/SchemaFieldEditor/TableColumnEditor.tsx
    frontend/src/features/admin/components/SchemaFieldEditor/FieldConfigEditor.tsx
    frontend/src/features/admin/components/TemplateFormModal.tsx
    frontend/public/locales/ko/admin.json
    frontend/public/locales/en/admin.json
  </files>
  <read_first>
    frontend/src/features/admin/components/SchemaFieldEditor/FieldConfigEditor.tsx
    frontend/src/features/admin/components/SchemaFieldEditor/TableColumnEditor.tsx
    frontend/src/features/admin/components/SchemaFieldEditor/types.ts
    frontend/src/features/admin/components/SchemaFieldEditor/constants.ts
    frontend/src/features/admin/components/TemplateFormModal.tsx
    frontend/public/locales/ko/admin.json
    frontend/public/locales/en/admin.json
    .planning/phases/23-table-column-editor/23-UI-SPEC.md
  </read_first>
  <action>
1. ColumnConfigPanel.tsx 신규 생성 (per D-05 기존 설정 재활용):

   Props:
   ```typescript
   interface ColumnConfigPanelProps {
     column: TableColumn;
     onUpdate: (column: TableColumn) => void;
   }
   ```

   내부 헬퍼:
   ```typescript
   const updateConfig = (partial: Partial<SchemaFieldConfig>) => {
     onUpdate({ ...column, config: { ...column.config, ...partial } });
   };
   ```

   switch(column.type)로 타입별 세부 설정 렌더링 — FieldConfigEditor의 각 case 로직을 동일 패턴으로 복제:

   - `case 'text':` / `case 'textarea':` — placeholder (text input, SMALL_INPUT_CLASS) + maxLength (number input, SMALL_INPUT_CLASS). grid grid-cols-2 gap-3 레이아웃.
   - `case 'number':` — min (number input) + max (number input) + unit (text input) + placeholder (text input). grid grid-cols-2 gap-3.
   - `case 'date':` — `<p className="text-sm text-gray-500 dark:text-gray-400 italic">{t('templates.noConfig')}</p>`
   - `case 'select':` — options 편집. FieldConfigEditor의 select case와 동일 패턴:
     - options.map((opt, idx) => flex gap-2 items-center: value input + label input + Trash2 삭제 버튼)
     - Plus 추가 버튼: `text-blue-600 dark:text-blue-400`
     - options 업데이트: `updateConfig({ options: newOpts })`
   - `case 'checkbox':` — 간소화된 설정 (별도 config 없음): `<p className="text-sm text-gray-500 dark:text-gray-400 italic">{t('templates.noConfig')}</p>`
   - `case 'staticText':` — content textarea (rows=2). FieldConfigEditor staticText case와 동일.
   - `default:` — return null

   라벨 스타일: `block text-xs font-medium text-gray-600 dark:text-gray-400 mb-1` (기존 패턴 동일)

2. TableColumnEditor.tsx 수정:

   a) 펼침 영역에 ColumnConfigPanel 연결 — Plan 01에서 placeholder 주석이 있던 위치를:
      ```typescript
      import { ColumnConfigPanel } from './ColumnConfigPanel';
      // ...
      {expanded && (
        <div className="px-4 pb-3 pt-2 space-y-3 border-t border-gray-100 dark:border-gray-700">
          {/* 기본 필드: 라벨, ID, 타입, 필수 — Plan 01에서 이미 구현 */}
          {/* 타입별 세부 설정 */}
          <ColumnConfigPanel column={col} onUpdate={(updated) => updateColumn(col.id, updated)} />
        </div>
      )}
      ```

   b) 컬럼 목록 아래에 행 설정 섹션 추가 (per D-13):
      - Props에 `minRows`, `maxRows`, `onMinRowsChange`, `onMaxRowsChange` 추가:
        ```typescript
        interface TableColumnEditorProps {
          columns: TableColumn[];
          onColumnsChange: (columns: TableColumn[]) => void;
          minRows?: number;
          maxRows?: number;
          onMinRowsChange: (val: number | undefined) => void;
          onMaxRowsChange: (val: number | undefined) => void;
        }
        ```
      - 컬럼 목록 DndContext 아래에 행 설정 UI:
        ```tsx
        <div className="mt-4 pt-4 border-t border-gray-200 dark:border-gray-700">
          <label className="block text-xs font-medium text-gray-600 dark:text-gray-400 mb-2">
            {t('templates.rowSettings')}
          </label>
          <div className="grid grid-cols-2 gap-3">
            <div>
              <label className="block text-xs font-medium text-gray-600 dark:text-gray-400 mb-1">
                {t('templates.minRows')}
              </label>
              <input
                type="number"
                value={minRows ?? ''}
                onChange={(e) => onMinRowsChange(e.target.value ? parseInt(e.target.value, 10) : undefined)}
                className={SMALL_INPUT_CLASS}
                min={0}
              />
            </div>
            <div>
              <label className="block text-xs font-medium text-gray-600 dark:text-gray-400 mb-1">
                {t('templates.maxRows')}
              </label>
              <input
                type="number"
                value={maxRows ?? ''}
                onChange={(e) => onMaxRowsChange(e.target.value ? parseInt(e.target.value, 10) : undefined)}
                className={SMALL_INPUT_CLASS}
                min={0}
              />
            </div>
          </div>
        </div>
        ```

   c) FieldConfigEditor.tsx의 case 'table' 업데이트 — minRows/maxRows props 전달:
      ```typescript
      case 'table':
        return (
          <TableColumnEditor
            columns={config.columns || []}
            onColumnsChange={(columns) => updateConfig({ columns })}
            minRows={config.minRows}
            maxRows={config.maxRows}
            onMinRowsChange={(minRows) => updateConfig({ minRows })}
            onMaxRowsChange={(maxRows) => updateConfig({ maxRows })}
          />
        );
      ```

3. TemplateFormModal.tsx에 저장 시 table 컬럼 최소 1개 밸리데이션 가드 추가 (per D-10):

   `onSubmit` 함수 (line 125 부근) 내부, `schemaDefinition` 생성 직전에 밸리데이션 가드 삽입:
   ```typescript
   const onSubmit = async (data: TemplateFormData) => {
     // D-10: table 타입 필드의 최소 1개 컬럼 밸리데이션
     const tableFieldsWithNoColumns = schemaFields.filter(
       (f) => f.type === 'table' && (!f.config.columns || f.config.columns.length === 0)
     );
     if (tableFieldsWithNoColumns.length > 0) {
       toast.error(t('templates.columnMinError'));
       return;
     }

     const schemaDefinition = JSON.stringify({
       version: 1,
       fields: schemaFields,
       // ...existing code...
   ```

   이 가드는:
   - schemaFields 배열에서 type === 'table'인 필드를 필터링
   - 해당 필드의 config.columns가 없거나 빈 배열인 경우를 탐지
   - toast.error로 `t('templates.columnMinError')` 메시지 표시 ("테이블에 최소 1개의 컬럼이 필요합니다")
   - early return으로 저장을 차단

   추가 import 불필요 — toast와 t는 이미 TemplateFormModal에서 사용 중.

4. i18n 키 추가 — ko/admin.json의 `templates` 객체에 다음 키 추가 (기존 `tableFieldPlaceholder` 키 아래에 추가, `tableFieldPlaceholder`는 유지):
   ```json
   "columnList": "컬럼 목록",
   "addColumn": "컬럼 추가",
   "removeColumn": "컬럼 삭제",
   "columnLabel": "컬럼 라벨",
   "columnId": "컬럼 ID",
   "columnType": "컬럼 타입",
   "columnEmpty": "컬럼을 추가하여 테이블 구조를 설계하세요",
   "rowSettings": "행 설정",
   "minRows": "최소 행 수",
   "maxRows": "최대 행 수",
   "addRow": "행 추가",
   "columnMinError": "테이블에 최소 1개의 컬럼이 필요합니다",
   "columnMaxError": "컬럼은 최대 20개까지 추가할 수 있습니다",
   "columnTypeText": "텍스트",
   "columnTypeNumber": "숫자",
   "columnTypeDate": "날짜",
   "columnTypeSelect": "선택",
   "columnTypeTextarea": "장문 텍스트",
   "columnTypeCheckbox": "체크박스",
   "columnTypeStaticText": "고정 텍스트"
   ```

5. en/admin.json의 `templates` 객체에 동일 키 영문 추가:
   ```json
   "columnList": "Column List",
   "addColumn": "Add Column",
   "removeColumn": "Remove Column",
   "columnLabel": "Column Label",
   "columnId": "Column ID",
   "columnType": "Column Type",
   "columnEmpty": "Add columns to design the table structure",
   "rowSettings": "Row Settings",
   "minRows": "Min Rows",
   "maxRows": "Max Rows",
   "addRow": "Add Row",
   "columnMinError": "Table requires at least 1 column",
   "columnMaxError": "Maximum 20 columns allowed",
   "columnTypeText": "Text",
   "columnTypeNumber": "Number",
   "columnTypeDate": "Date",
   "columnTypeSelect": "Select",
   "columnTypeTextarea": "Textarea",
   "columnTypeCheckbox": "Checkbox",
   "columnTypeStaticText": "Static Text"
   ```
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - ColumnConfigPanel.tsx exists and contains `switch (column.type)`
    - ColumnConfigPanel.tsx contains `case 'text':` and `case 'number':` and `case 'select':` and `case 'checkbox':` and `case 'staticText':`
    - ColumnConfigPanel.tsx contains `updateConfig`
    - TableColumnEditor.tsx contains `<ColumnConfigPanel`
    - TableColumnEditor.tsx contains `onMinRowsChange` and `onMaxRowsChange` in props
    - TableColumnEditor.tsx contains `t('templates.rowSettings')`
    - FieldConfigEditor.tsx case 'table' contains `minRows={config.minRows}`
    - TemplateFormModal.tsx contains `columnMinError` in onSubmit guard
    - TemplateFormModal.tsx contains `f.type === 'table'` filter logic
    - TemplateFormModal.tsx contains `columns.length === 0` check
    - ko/admin.json contains `"columnEmpty":`
    - ko/admin.json contains `"columnTypeCheckbox":`
    - ko/admin.json contains `"rowSettings":`
    - ko/admin.json contains `"columnMinError":`
    - en/admin.json contains `"columnEmpty":`
    - en/admin.json contains `"columnMinError":`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ColumnConfigPanel이 7개 컬럼 타입별 세부 설정을 지원하고, TableColumnEditor에 행 설정 UI 추가, TemplateFormModal에 저장 시 table 컬럼 최소 1개 밸리데이션 가드 동작 (per D-10), i18n 키 완비, TypeScript 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 2: PreviewFieldRenderer 테이블 미리보기를 실제 테이블로 교체</name>
  <files>
    frontend/src/features/admin/components/FormPreview/PreviewFieldRenderer.tsx
  </files>
  <read_first>
    frontend/src/features/admin/components/FormPreview/PreviewFieldRenderer.tsx
    frontend/src/features/admin/components/SchemaFieldEditor/types.ts
    frontend/public/locales/ko/admin.json
    .planning/phases/23-table-column-editor/23-UI-SPEC.md
  </read_first>
  <action>
PreviewFieldRenderer.tsx의 case 'table' (현재 lines 76-83)을 실제 테이블 미리보기로 교체 (per D-15, D-16):

기존 코드:
```typescript
case 'table':
  return (
    <div className="border border-gray-300 dark:border-gray-600 rounded-lg overflow-hidden">
      <div className="bg-gray-50 dark:bg-gray-700 px-3 py-2 text-xs text-gray-500">
        {t('templates.tableFieldPlaceholder')}
      </div>
    </div>
  );
```

교체 코드:
```typescript
case 'table': {
  const columns = field.config.columns || [];
  if (columns.length === 0) {
    return (
      <div className="border border-dashed border-gray-300 dark:border-gray-600 rounded-lg overflow-hidden">
        <div className="bg-gray-50 dark:bg-gray-700 px-3 py-3 text-xs text-gray-400 dark:text-gray-500 text-center">
          {t('templates.columnEmpty')}
        </div>
      </div>
    );
  }
  return (
    <div className="border border-gray-300 dark:border-gray-600 rounded-lg overflow-hidden overflow-x-auto">
      <table className="w-full text-sm">
        <thead>
          <tr className="bg-gray-50 dark:bg-gray-700">
            {columns.map((col) => (
              <th key={col.id} className="px-3 py-2 text-left text-xs font-medium text-gray-600 dark:text-gray-400 whitespace-nowrap">
                {col.label || t('templates.noLabel')}
                {col.required && <span className="text-red-500 ml-1">*</span>}
              </th>
            ))}
          </tr>
        </thead>
        <tbody>
          {[0, 1].map((rowIdx) => (
            <tr key={rowIdx} className="border-t border-gray-200 dark:border-gray-700">
              {columns.map((col) => (
                <td key={col.id} className="px-3 py-2">
                  {col.type === 'checkbox' ? (
                    <input type="checkbox" disabled className="w-4 h-4 rounded border-gray-300" />
                  ) : col.type === 'select' ? (
                    <select disabled className="w-full h-8 px-2 text-xs border border-gray-300 dark:border-gray-600 rounded bg-gray-50 dark:bg-gray-700 text-gray-400">
                      <option>{t('templates.selectPlaceholder')}</option>
                    </select>
                  ) : col.type === 'date' ? (
                    <input type="date" disabled className="w-full h-8 px-2 text-xs border border-gray-300 dark:border-gray-600 rounded bg-gray-50 dark:bg-gray-700 text-gray-400" />
                  ) : col.type === 'textarea' ? (
                    <textarea disabled rows={1} className="w-full px-2 py-1 text-xs border border-gray-300 dark:border-gray-600 rounded bg-gray-50 dark:bg-gray-700 text-gray-400 resize-none" />
                  ) : col.type === 'staticText' ? (
                    <span className="text-xs text-gray-400">{col.config?.content || '-'}</span>
                  ) : (
                    <input type={col.type === 'number' ? 'number' : 'text'} disabled placeholder={col.config?.placeholder || ''} className="w-full h-8 px-2 text-xs border border-gray-300 dark:border-gray-600 rounded bg-gray-50 dark:bg-gray-700 text-gray-400" />
                  )}
                </td>
              ))}
            </tr>
          ))}
        </tbody>
      </table>
      <button
        disabled
        className="w-full py-2 text-sm text-gray-400 border-t border-dashed border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-800 cursor-not-allowed"
      >
        + {t('templates.addRow')}
      </button>
    </div>
  );
}
```

주의사항:
- `field.config.columns`의 타입이 `TableColumn[]`이 아닌 `SchemaFieldConfig['columns']`로 추론됨 — types.ts에서 `columns?: TableColumn[]`로 정의되어 있으므로 자동 호환
- import 추가 불필요 — SchemaField 타입에 이미 config.columns 포함
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - PreviewFieldRenderer.tsx contains `<table className="w-full text-sm">`
    - PreviewFieldRenderer.tsx contains `field.config.columns`
    - PreviewFieldRenderer.tsx contains `[0, 1].map`
    - PreviewFieldRenderer.tsx contains `+ {t('templates.addRow')}`
    - PreviewFieldRenderer.tsx contains `col.type === 'checkbox'`
    - PreviewFieldRenderer.tsx contains `t('templates.columnEmpty')`
    - PreviewFieldRenderer.tsx does NOT contain `tableFieldPlaceholder`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>PreviewFieldRenderer가 table 필드의 컬럼 헤더 + 타입별 disabled input 샘플 행 2개 + 비활성 [+ 행 추가] 버튼을 표시하며, 컬럼 0개일 때 빈 상태 안내 메시지 표시</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 테이블 컬럼 편집기 전체 기능 확인</name>
  <files>none</files>
  <action>사용자가 브라우저에서 전체 기능을 수동 검증한다. 자동화 작업 없음 -- checkpoint task.</action>
  <what-built>table 타입 필드의 컬럼 편집 UI (추가/삭제/DnD 순서변경/접기펼치기/타입별 설정) + 미리보기 테이블 실시간 반영 + 저장 시 컬럼 최소 1개 밸리데이션</what-built>
  <how-to-verify>
    1. 브라우저에서 관리자 로그인 -> 양식 관리 -> 양식 편집 모달 열기
    2. "필드 추가" -> "표" 타입 선택 -> table 필드가 추가되는지 확인
    3. table 필드를 펼치면 "컬럼 목록" 섹션이 표시되는지 확인
    4. "컬럼 추가" 클릭 -> 새 컬럼이 추가되고 자동으로 펼쳐지는지 확인
    5. 컬럼 라벨 입력 -> 우측 미리보기 패널에서 테이블 헤더에 실시간 반영되는지 확인
    6. 컬럼 타입을 "숫자"로 변경 -> 세부 설정에 최소값/최대값/단위가 표시되는지 확인
    7. 컬럼 타입을 "선택"으로 변경 -> 옵션 추가/삭제가 가능한지 확인
    8. 3개 이상 컬럼 추가 후 드래그 앤 드롭으로 순서 변경 -> 미리보기에 반영되는지 확인
    9. 컬럼 삭제 버튼(휴지통) 클릭 -> 확인 없이 즉시 삭제되는지 확인
    10. 행 설정에서 최소/최대 행 수 입력 가능한지 확인
    11. 미리보기 테이블에 컬럼 헤더 + 샘플 행 2개 + "[+ 행 추가]" 비활성 버튼이 표시되는지 확인
    12. (D-10 검증) 컬럼을 모두 삭제한 상태에서 저장 버튼 클릭 -> "테이블에 최소 1개의 컬럼이 필요합니다" 토스트 에러가 표시되고 저장이 차단되는지 확인
    13. 컬럼 1개 이상 추가 후 저장 -> 정상적으로 저장되는지 확인
  </how-to-verify>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>사용자가 "approved" 응답하면 Phase 23 전체 기능 검증 완료</done>
  <resume-signal>"approved" 입력 또는 발견된 문제 설명</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI input | 컬럼 설정값, options, minRows/maxRows -- 프론트엔드 밸리데이션 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Tampering | ColumnConfigPanel options | accept | 프론트엔드 전용 UI -- 백엔드 저장 시 서버 측 밸리데이션 담당 |
| T-23-04 | Information Disclosure | i18n JSON | accept | 공개 정적 파일, 민감 정보 없음 |
| T-23-05 | Denial of Service | TemplateFormModal save | mitigate | table 필드 저장 시 columns.length === 0 가드 + max 20개 컬럼 제한 (D-10) |
</threat_model>

<verification>
1. `cd frontend && npx tsc --noEmit` -- TypeScript 컴파일 에러 없음
2. `cd frontend && npm run build` -- 프로덕션 빌드 성공
3. 브라우저에서 table 필드 -> 컬럼 추가 -> 타입별 설정 확인 -> 미리보기 테이블 반영 확인
4. 컬럼 0개 상태에서 저장 시도 -> 에러 토스트 표시 + 저장 차단 확인
</verification>

<success_criteria>
- 7개 컬럼 타입 각각의 세부 설정이 동작 (text/textarea: placeholder+maxLength, number: min+max+unit, date: 설정없음, select: options, checkbox: 설정없음, staticText: content)
- minRows/maxRows 행 설정 입력 가능
- 미리보기 테이블이 컬럼 구성을 실시간 반영
- 저장 시 table 타입 필드에 컬럼 0개이면 에러 토스트 + 저장 차단 (per D-10)
- 모든 UI 텍스트가 i18n 키를 통해 표시
- TypeScript 컴파일 + 프로덕션 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/23-table-column-editor/23-02-SUMMARY.md`
</output>
