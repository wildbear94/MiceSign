---
phase: 31-dashboard
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - backend/src/main/java/com/micesign/service/DashboardService.java
  - backend/src/test/java/com/micesign/dashboard/DashboardServiceIntegrationTest.java
  - backend/src/test/java/com/micesign/document/DocumentSearchPermissionMatrixTest.java
autonomous: true
requirements: [DASH-01, DASH-02]
tags: [dashboard, backend, service, test, rbac, hierarchy]

must_haves:
  truths:
    - "DashboardService.getDashboardSummary(userId, role, departmentId) 3-arg 시그니처로 role 별 분기 동작한다"
    - "USER 스코프: drafterIds=[userId], approverIds=[userId], 기존과 동일한 본인 카운트 반환"
    - "ADMIN 스코프: drafterIds/approverIds = 부서 계층 재귀 (findDescendantIds → findIdsByDepartmentIdIn) 로 집계"
    - "SUPER_ADMIN 스코프: drafterIds/approverIds=null (sentinel), countByStatus/countAllPending 호출"
    - "응답에 pendingCount/draftCount/submittedCount/completedCount(=APPROVED)/rejectedCount 5 필드가 role 에 맞게 채워진다"
    - "recentPending/recentDocuments 는 role 불문 본인 스코프 유지 (userId 기준)"
    - "DashboardController 가 3-arg service 를 호출하도록 업데이트되어 user.getUserId/role/departmentId 전달"
    - "DashboardServiceIntegrationTest 가 USER/ADMIN/SUPER_ADMIN × 4카운트 매트릭스 + 부서 계층 fixture 로 green"
    - "DocumentSearchPermissionMatrixTest 에 부서 계층 재귀 3 케이스가 추가되어 green (HQ > Engineering > Platform + Sales 구조)"
  artifacts:
    - path: "backend/src/main/java/com/micesign/service/DashboardService.java"
      provides: "role-based summary aggregation + 부서 계층 재귀 스코프"
      contains: "findDescendantIds"
    - path: "backend/src/test/java/com/micesign/dashboard/DashboardServiceIntegrationTest.java"
      provides: "role × 4카운트 × 부서 계층 matrix integration test"
      contains: "descendant_scope\\|superAdmin_sees_all\\|user_sees_only_self"
    - path: "backend/src/main/java/com/micesign/controller/DashboardController.java"
      provides: "3-arg DashboardService 호출"
      contains: "dashboardService.getDashboardSummary"
  key_links:
    - from: "DashboardService.getDashboardSummary"
      to: "DepartmentRepository.findDescendantIds"
      via: "switch(role) ADMIN 분기"
      pattern: "departmentRepository\\.findDescendantIds"
    - from: "DashboardService.getDashboardSummary ADMIN"
      to: "UserRepository.findIdsByDepartmentIdIn"
      via: "descendantDeptIds → userIds"
      pattern: "findIdsByDepartmentIdIn"
    - from: "DashboardController"
      to: "DashboardService.getDashboardSummary(userId, role, departmentId)"
      via: "@AuthenticationPrincipal CustomUserDetails"
      pattern: "getDashboardSummary\\(.*getUserId\\(\\).*getRole.*getDepartmentId"
---

<objective>
Phase 31 의 핵심 BE 로직 — `DashboardService.getDashboardSummary` 를 role-based 3-arg 시그니처로 전면 리팩터하여 USER/ADMIN/SUPER_ADMIN 분기 및 MariaDB 부서 계층 재귀 스코프를 구현한다. `DashboardController` 를 새 시그니처에 맞게 업데이트하고, `DashboardServiceIntegrationTest` 를 신규 작성하여 role × 4카운트 × 부서 계층 matrix 를 full-stack (SpringBootTest + MockMvc + JdbcTemplate fixture) 로 검증한다. 추가로 Plan 01 의 D-A9 Option 1 upgrade 가 대시보드 숫자와 SoT 일치를 보장하는지 `DocumentSearchPermissionMatrixTest` 에 부서 계층 3 케이스를 확장한다.

Purpose: DASH-01 (4 카드 권한 스코프) + DASH-02 (최근 5건 본인 스코프 유지) 의 BE contract 확정. FE (Plan 03~) 가 의존할 수 있는 stable API 를 만든다. FE 통합 시 버그가 BE 기원인지 즉시 판별 가능한 테스트 인프라 제공.

Output: DashboardService full refactor + DashboardController 시그니처 업데이트 + 신규 integration test 5+ 케이스 + Phase 30 test matrix 확장 3+ 케이스.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-dashboard/31-CONTEXT.md
@.planning/phases/31-dashboard/31-RESEARCH.md
@.planning/phases/31-dashboard/31-PATTERNS.md
@.planning/phases/31-dashboard/31-VALIDATION.md
@.planning/phases/31-dashboard/31-01-SUMMARY.md

@backend/src/main/java/com/micesign/service/DashboardService.java
@backend/src/main/java/com/micesign/controller/DashboardController.java
@backend/src/test/java/com/micesign/document/DocumentSearchPermissionMatrixTest.java
@backend/src/test/java/com/micesign/admin/TestTokenHelper.java

<interfaces>
<!-- Plan 01 이 제공한 repository 메서드 시그니처 (executor 가 탐색 불필요) -->

## DepartmentRepository (Plan 01 추가)
```java
List<Long> findDescendantIds(@Param("deptId") Long deptId);  // self + 모든 하위 부서 id (WITH RECURSIVE CTE)
```

## DocumentRepository (Plan 01 추가)
```java
long countByDrafterIdInAndStatus(List<Long> drafterIds, DocumentStatus status);
long countByStatus(DocumentStatus status);
// 기존 유지: long countByDrafterIdAndStatus(Long drafterId, DocumentStatus status);
```

## ApprovalLineRepository (Plan 01 추가)
```java
long countPendingByApproverIdIn(@Param("userIds") List<Long> userIds);
long countAllPending();
// 기존 유지: long countPendingByApproverId(Long userId);
```

## UserRepository (Plan 01 추가)
```java
@Query("SELECT u.id FROM User u WHERE u.departmentId IN :deptIds")
List<Long> findIdsByDepartmentIdIn(@Param("deptIds") List<Long> deptIds);
```

## DashboardSummaryResponse (Plan 01 수정) — 7 필드
```java
public record DashboardSummaryResponse(
    long pendingCount, long draftCount, long submittedCount,
    long completedCount,   // APPROVED only
    long rejectedCount,    // ★ 신규
    List<PendingApprovalResponse> recentPending,
    List<DocumentResponse> recentDocuments
) {}
```

## CustomUserDetails (기존, 변경 없음)
- `getUserId(): Long`
- `getUser(): User` → `.getRole(): UserRole`, `.getDepartmentId(): Long`
(Plan 02 executor 는 `CustomUserDetails` 실제 시그니처를 확인 후 DashboardController 업데이트 시 role/departmentId 획득 방법 확정)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DashboardServiceIntegrationTest 신규 — 부서 계층 fixture + role × 4카운트 matrix (RED)</name>
  <read_first>
    - backend/src/test/java/com/micesign/document/DocumentSearchPermissionMatrixTest.java (analog Phase 30 test, L1-260)
    - backend/src/test/java/com/micesign/admin/TestTokenHelper.java (tokenForRole helper)
    - backend/src/test/java/com/micesign/document/ApprovalServiceAuditTest.java (Phase 29 analog — @BeforeEach cleanup 패턴)
    - .planning/phases/31-dashboard/31-PATTERNS.md §9, §SP-4, §SP-5 (exact copy blocks for setup/cleanup/assertion)
    - .planning/phases/31-dashboard/31-RESEARCH.md §"Pattern 5" (BE integration test structure)
    - .planning/phases/31-dashboard/31-VALIDATION.md Wave 0 requirements
  </read_first>
  <behavior>
    RED: 테스트 파일을 먼저 작성하여 실패 확인. GREEN 은 Task 2 에서 service 리팩터 후 달성.

    ## 테스트 케이스 (RED expectation)
    1. **`user_sees_only_self_drafted`**: USER role (A_ID, dept=PLATFORM) 이 `/api/v1/dashboard/summary` 호출 시 본인 4문서 (DRAFT 1, SUBMITTED 1, APPROVED 1, REJECTED 1) 기준 카운트 반환
       - pendingCount = 0 (A 는 approver 아님)
       - draftCount = 1, submittedCount = 1, completedCount = 1 (APPROVED), rejectedCount = 1
    2. **`admin_sees_descendant_scope`**: ADMIN role (adminOfEngineering, dept=ENGINEERING) 이 HQ > Engineering > Platform 계층에서 ENGINEERING 과 PLATFORM 의 모든 사용자 문서 합산 카운트
       - descendantDeptIds = [ENGINEERING, PLATFORM] → drafterIds = [A, adminOfEngineering (본인), ...]
       - Sales (다른 브랜치) 의 문서는 제외
       - submittedCount, completedCount, rejectedCount 는 Engineering+Platform 내 drafter 의 문서만 합산
    3. **`superAdmin_sees_all`**: SUPER_ADMIN 이 전사 카운트 (countByStatus 경유). Sales 브랜치 포함 모든 문서.
    4. **`admin_pending_scope_covers_department_hierarchy`**: ADMIN pending — approval_line.approver_id ∈ (Engineering+Platform users) 인 PENDING approval_line 카운트
    5. **`draftCount_remains_in_json_response`**: JSON 응답에 `draftCount` 필드가 존재함 assert (D-A3: FE 미노출이지만 백엔드 contract 유지)
    6. **`rejectedCount_field_present`**: JSON 응답에 `rejectedCount` 필드 존재 assert
    7. **`deep_hierarchy_recursion`**: 10-level deep 부서 트리에서 findDescendantIds 가 정상 종료 + ROOT 기준 10 개 id 반환 (통합 내부에서 `@Autowired DepartmentRepository` 직접 호출)
  </behavior>
  <action>
    파일 신규 생성: `backend/src/test/java/com/micesign/dashboard/DashboardServiceIntegrationTest.java`

    다음 구조로 작성 (31-PATTERNS.md §9 excerpt + SP-5 cleanup 패턴 그대로):

    ```java
    package com.micesign.dashboard;

    import com.fasterxml.jackson.databind.JsonNode;
    import com.fasterxml.jackson.databind.ObjectMapper;
    import com.micesign.admin.TestTokenHelper;
    import com.micesign.domain.enums.ApprovalLineStatus;
    import com.micesign.domain.enums.ApprovalLineType;
    import com.micesign.domain.enums.DocumentStatus;
    import com.micesign.domain.enums.UserRole;
    import com.micesign.repository.DepartmentRepository;
    import org.junit.jupiter.api.AfterEach;
    import org.junit.jupiter.api.BeforeEach;
    import org.junit.jupiter.api.DisplayName;
    import org.junit.jupiter.api.Test;
    import org.springframework.beans.factory.annotation.Autowired;
    import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
    import org.springframework.boot.test.context.SpringBootTest;
    import org.springframework.http.MediaType;
    import org.springframework.jdbc.core.JdbcTemplate;
    import org.springframework.test.context.ActiveProfiles;
    import org.springframework.test.web.servlet.MockMvc;
    import org.springframework.test.web.servlet.MvcResult;

    import java.time.LocalDateTime;
    import java.util.List;

    import static org.assertj.core.api.Assertions.assertThat;
    import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
    import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

    @SpringBootTest
    @AutoConfigureMockMvc
    @ActiveProfiles("test")
    @DisplayName("Phase 31 — DashboardService 권한 × 4카드 × 부서 계층 (DASH-01, DASH-02)")
    class DashboardServiceIntegrationTest {

        @Autowired MockMvc mockMvc;
        @Autowired TestTokenHelper tokenHelper;
        @Autowired JdbcTemplate jdbcTemplate;
        @Autowired ObjectMapper objectMapper;
        @Autowired DepartmentRepository departmentRepository;

        // 부서 계층 (31-PATTERNS.md §9 기준 — 700번대 id 로 기존 seed 와 격리)
        //   HQ (id=700)
        //    ├─ Engineering (id=701, parent=700)
        //    │    └─ Platform (id=702, parent=701)
        //    └─ Sales (id=703, parent=700)
        static final long HQ_ID = 700L;
        static final long ENG_ID = 701L;
        static final long PLAT_ID = 702L;
        static final long SALES_ID = 703L;

        // 사용자 (700번대)
        static final long USER_PLATFORM = 700L;     // USER in Platform
        static final long USER_SALES = 701L;        // USER in Sales
        static final long ADMIN_ENG = 710L;         // ADMIN in Engineering (Engineering 과 Platform 계층 스코프)
        static final long ADMIN_HQ = 711L;          // ADMIN in HQ (전체 자식 부서 계층 — 테스트 검증용)
        // SUPER_ADMIN 은 기존 V2 seed (id=1) 사용 — tokenHelper.superAdminToken()

        // 문서 id — @BeforeEach 에서 채움
        long platSubmittedId, platApprovedId, platRejectedId, platDraftId;
        long salesSubmittedId, salesApprovedId;

        @BeforeEach
        void setUp() {
            // FK 역순 cleanup (SP-5)
            jdbcTemplate.update("DELETE FROM notification_log");
            jdbcTemplate.update("DELETE FROM approval_line");
            jdbcTemplate.update("DELETE FROM document_attachment");
            jdbcTemplate.update("DELETE FROM document_content");
            jdbcTemplate.update("DELETE FROM document");
            jdbcTemplate.update("DELETE FROM doc_sequence");
            jdbcTemplate.update("DELETE FROM \"user\" WHERE id IN (?, ?, ?, ?)",
                    USER_PLATFORM, USER_SALES, ADMIN_ENG, ADMIN_HQ);
            jdbcTemplate.update("DELETE FROM department WHERE id IN (?, ?, ?)",
                    PLAT_ID, ENG_ID, SALES_ID);  // 하위부터
            jdbcTemplate.update("DELETE FROM department WHERE id = ?", HQ_ID);  // 루트 마지막

            // 부서 계층 seed (루트 → 하위 순)
            jdbcTemplate.update("INSERT INTO department (id, name, parent_id, sort_order, is_active) VALUES (?, 'HQ', NULL, 100, TRUE)", HQ_ID);
            jdbcTemplate.update("INSERT INTO department (id, name, parent_id, sort_order, is_active) VALUES (?, 'Engineering', ?, 101, TRUE)", ENG_ID, HQ_ID);
            jdbcTemplate.update("INSERT INTO department (id, name, parent_id, sort_order, is_active) VALUES (?, 'Platform', ?, 102, TRUE)", PLAT_ID, ENG_ID);
            jdbcTemplate.update("INSERT INTO department (id, name, parent_id, sort_order, is_active) VALUES (?, 'Sales', ?, 103, TRUE)", SALES_ID, HQ_ID);

            // 사용자 seed
            String pwHash = "$2a$10$07mcjXBfvelJFwjs8DnoJOnEqprFy.dnQL1NdnRvlqEWwwmX62SOW";
            String userSql = "INSERT INTO \"user\" (id, employee_no, name, email, password, department_id, "
                    + "position_id, role, status, failed_login_count, must_change_password) "
                    + "VALUES (?, ?, ?, ?, ?, ?, 3, ?, 'ACTIVE', 0, FALSE)";
            jdbcTemplate.update(userSql, USER_PLATFORM, "U_PLAT_31", "PlatUser", "platuser-31@test.com", pwHash, PLAT_ID, "USER");
            jdbcTemplate.update(userSql, USER_SALES,    "U_SALES_31", "SalesUser", "salesuser-31@test.com", pwHash, SALES_ID, "USER");
            jdbcTemplate.update(userSql, ADMIN_ENG,     "A_ENG_31", "EngAdmin", "engadmin-31@test.com", pwHash, ENG_ID, "ADMIN");
            jdbcTemplate.update(userSql, ADMIN_HQ,      "A_HQ_31", "HQAdmin",  "hqadmin-31@test.com",  pwHash, HQ_ID, "ADMIN");

            // 문서 seed (Platform user 4문서)
            platDraftId     = insertDocument(USER_PLATFORM, DocumentStatus.DRAFT,     null,                              "PLAT DRAFT");
            platSubmittedId = insertDocument(USER_PLATFORM, DocumentStatus.SUBMITTED, LocalDateTime.now().minusDays(3), "PLAT SUBMITTED");
            platApprovedId  = insertDocument(USER_PLATFORM, DocumentStatus.APPROVED,  LocalDateTime.now().minusDays(5), "PLAT APPROVED");
            platRejectedId  = insertDocument(USER_PLATFORM, DocumentStatus.REJECTED,  LocalDateTime.now().minusDays(2), "PLAT REJECTED");

            // Sales user 2문서
            salesSubmittedId = insertDocument(USER_SALES, DocumentStatus.SUBMITTED, LocalDateTime.now().minusDays(4), "SALES SUBMITTED");
            salesApprovedId  = insertDocument(USER_SALES, DocumentStatus.APPROVED,  LocalDateTime.now().minusDays(6), "SALES APPROVED");

            // approval_line — platSubmitted 에 ADMIN_ENG 가 APPROVE pending
            insertApprovalLine(platSubmittedId, ADMIN_ENG, ApprovalLineType.APPROVE, 1, ApprovalLineStatus.PENDING);
            // salesSubmitted 에 USER_SALES 는 본인 (기안자라 approver 아님), 여기선 ADMIN_HQ 가 APPROVE pending
            insertApprovalLine(salesSubmittedId, ADMIN_HQ, ApprovalLineType.APPROVE, 1, ApprovalLineStatus.PENDING);
        }

        @AfterEach
        void tearDown() {
            jdbcTemplate.update("DELETE FROM notification_log");
            jdbcTemplate.update("DELETE FROM approval_line");
            jdbcTemplate.update("DELETE FROM document_content");
            jdbcTemplate.update("DELETE FROM document");
            jdbcTemplate.update("DELETE FROM \"user\" WHERE id IN (?, ?, ?, ?)",
                    USER_PLATFORM, USER_SALES, ADMIN_ENG, ADMIN_HQ);
            jdbcTemplate.update("DELETE FROM department WHERE id IN (?, ?, ?)",
                    PLAT_ID, ENG_ID, SALES_ID);
            jdbcTemplate.update("DELETE FROM department WHERE id = ?", HQ_ID);
        }

        private long insertDocument(Long drafterId, DocumentStatus status, LocalDateTime submittedAt, String title) {
            jdbcTemplate.update(
                    "INSERT INTO document (template_code, drafter_id, title, status, submitted_at, created_at, current_step) "
                            + "VALUES (?, ?, ?, ?, ?, ?, 1)",
                    "GENERAL", drafterId, title, status.name(), submittedAt, LocalDateTime.now());
            Long id = jdbcTemplate.queryForObject(
                    "SELECT id FROM document WHERE drafter_id = ? AND status = ? ORDER BY id DESC LIMIT 1",
                    Long.class, drafterId, status.name());
            return id != null ? id : 0L;
        }

        private void insertApprovalLine(long docId, long approverId, ApprovalLineType type, int step, ApprovalLineStatus status) {
            jdbcTemplate.update(
                    "INSERT INTO approval_line (document_id, step_order, line_type, approver_id, status) VALUES (?, ?, ?, ?, ?)",
                    docId, step, type.name(), approverId, status.name());
        }

        private JsonNode callSummary(String token) throws Exception {
            MvcResult result = mockMvc.perform(get("/api/v1/dashboard/summary")
                    .header("Authorization", "Bearer " + token)
                    .accept(MediaType.APPLICATION_JSON))
                .andExpect(status().isOk())
                .andReturn();
            JsonNode root = objectMapper.readTree(result.getResponse().getContentAsByteArray());
            return root.path("data");
        }

        @Test
        @DisplayName("USER (Platform) — 본인 스코프만 카운트, pending 0, submitted 1, completed 1, rejected 1, draft 1")
        void user_sees_only_self_drafted() throws Exception {
            String token = tokenHelper.tokenForRole(USER_PLATFORM, "platuser-31@test.com", "PlatUser", UserRole.USER, PLAT_ID);
            JsonNode data = callSummary(token);
            assertThat(data.path("pendingCount").asLong()).isEqualTo(0L);
            assertThat(data.path("draftCount").asLong()).isEqualTo(1L);
            assertThat(data.path("submittedCount").asLong()).isEqualTo(1L);
            assertThat(data.path("completedCount").asLong()).isEqualTo(1L);  // APPROVED only
            assertThat(data.path("rejectedCount").asLong()).isEqualTo(1L);
        }

        @Test
        @DisplayName("ADMIN (Engineering) — 계층 재귀 [Engineering, Platform] 스코프. Sales 배제.")
        void admin_sees_descendant_scope() throws Exception {
            String token = tokenHelper.tokenForRole(ADMIN_ENG, "engadmin-31@test.com", "EngAdmin", UserRole.ADMIN, ENG_ID);
            JsonNode data = callSummary(token);
            // descendantDeptIds = [Engineering, Platform] → drafterIds = [ADMIN_ENG, USER_PLATFORM]
            // Platform user 의 4 상태 문서 중 DRAFT=1, SUBMITTED=1, APPROVED=1, REJECTED=1
            // ADMIN_ENG 본인은 문서 없음
            // Sales 문서는 제외
            assertThat(data.path("submittedCount").asLong()).isEqualTo(1L);
            assertThat(data.path("completedCount").asLong()).isEqualTo(1L);
            assertThat(data.path("rejectedCount").asLong()).isEqualTo(1L);
            assertThat(data.path("draftCount").asLong()).isEqualTo(1L);
            // pendingCount: approver.id IN [ADMIN_ENG, USER_PLATFORM], PENDING approval_line = platSubmitted 의 ADMIN_ENG 1건
            assertThat(data.path("pendingCount").asLong()).isEqualTo(1L);
        }

        @Test
        @DisplayName("SUPER_ADMIN — 전사 카운트 (Sales 포함)")
        void superAdmin_sees_all() throws Exception {
            String token = tokenHelper.superAdminToken();
            JsonNode data = callSummary(token);
            // 전체: submitted = platSubmitted + salesSubmitted = 2
            //       approved = platApproved + salesApproved = 2
            //       rejected = platRejected = 1
            assertThat(data.path("submittedCount").asLong()).isEqualTo(2L);
            assertThat(data.path("completedCount").asLong()).isEqualTo(2L);
            assertThat(data.path("rejectedCount").asLong()).isEqualTo(1L);
            // pendingCount: 전사 PENDING = platSubmitted(ADMIN_ENG) + salesSubmitted(ADMIN_HQ) = 2
            assertThat(data.path("pendingCount").asLong()).isEqualTo(2L);
        }

        @Test
        @DisplayName("ADMIN (HQ) — 최상위 계층, 전 부서 계층 재귀 — Sales 포함")
        void admin_hq_covers_full_hierarchy() throws Exception {
            String token = tokenHelper.tokenForRole(ADMIN_HQ, "hqadmin-31@test.com", "HQAdmin", UserRole.ADMIN, HQ_ID);
            JsonNode data = callSummary(token);
            // descendantDeptIds = [HQ, Engineering, Platform, Sales] → drafterIds = [USER_PLATFORM, USER_SALES, ADMIN_ENG, ADMIN_HQ]
            assertThat(data.path("submittedCount").asLong()).isEqualTo(2L);  // plat + sales
            assertThat(data.path("completedCount").asLong()).isEqualTo(2L);
            assertThat(data.path("rejectedCount").asLong()).isEqualTo(1L);
        }

        @Test
        @DisplayName("JSON 응답에 draftCount / rejectedCount 필드 존재 (API contract)")
        void draftCount_and_rejectedCount_present_in_json() throws Exception {
            String token = tokenHelper.tokenForRole(USER_PLATFORM, "platuser-31@test.com", "PlatUser", UserRole.USER, PLAT_ID);
            JsonNode data = callSummary(token);
            assertThat(data.has("draftCount")).as("D-A3: draftCount FE 미노출이지만 BE 유지").isTrue();
            assertThat(data.has("rejectedCount")).as("D-A2: rejectedCount 신규 필드").isTrue();
        }

        @Test
        @DisplayName("DepartmentRepository.findDescendantIds — 부서 계층 재귀 SoT 검증")
        void findDescendantIds_recursive_cte_returns_self_plus_all_descendants() {
            // HQ (root)
            List<Long> hqTree = departmentRepository.findDescendantIds(HQ_ID);
            assertThat(hqTree).containsExactlyInAnyOrder(HQ_ID, ENG_ID, PLAT_ID, SALES_ID);

            // Engineering (middle) — self + Platform
            List<Long> engTree = departmentRepository.findDescendantIds(ENG_ID);
            assertThat(engTree).containsExactlyInAnyOrder(ENG_ID, PLAT_ID);

            // Platform (leaf) — self only
            List<Long> platTree = departmentRepository.findDescendantIds(PLAT_ID);
            assertThat(platTree).containsExactly(PLAT_ID);

            // Sales (leaf, sibling branch) — self only
            List<Long> salesTree = departmentRepository.findDescendantIds(SALES_ID);
            assertThat(salesTree).containsExactly(SALES_ID);
        }
    }
    ```

    **첫 실행 시 RED 예상**: `./gradlew test --tests "com.micesign.dashboard.DashboardServiceIntegrationTest"` → DashboardService 가 아직 3-arg 시그니처 아니고 rejectedCount 분리 안 된 상태이므로 다수 테스트 실패. pendingCount/submittedCount 등은 현재 getSummary(userId) 가 본인 스코프로만 집계하므로 ADMIN/SUPER_ADMIN 케이스에서 숫자 불일치.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; ./gradlew test --tests "com.micesign.dashboard.DashboardServiceIntegrationTest" --console=plain 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - 파일 존재: `test -f backend/src/test/java/com/micesign/dashboard/DashboardServiceIntegrationTest.java`
    - `grep -c "@Test" backend/src/test/java/com/micesign/dashboard/DashboardServiceIntegrationTest.java` 결과 ≥ 6
    - `grep -c "admin_sees_descendant_scope" backend/src/test/java/com/micesign/dashboard/DashboardServiceIntegrationTest.java` = 1
    - `grep -c "findDescendantIds_recursive_cte" backend/src/test/java/com/micesign/dashboard/DashboardServiceIntegrationTest.java` = 1
    - `./gradlew test --tests "com.micesign.dashboard.DashboardServiceIntegrationTest"` 실행 → FAILED (RED — Task 2 전이므로 정상) — 적어도 `admin_sees_descendant_scope` 또는 `superAdmin_sees_all` 이 실패해야 함
    - TEST 가 RED 로 실패했더라도 컴파일은 성공 (`gradlew compileTestJava` exit 0)
  </acceptance_criteria>
  <done>
    테스트 파일 생성 완료. RED 상태 (의도적). Task 2 이후 GREEN 전환.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DashboardService refactor — role-based 3-arg 시그니처 + DashboardController 업데이트 (GREEN)</name>
  <read_first>
    - backend/src/main/java/com/micesign/service/DashboardService.java (수정 대상, 95 라인)
    - backend/src/main/java/com/micesign/controller/DashboardController.java (수정 대상, 29 라인)
    - backend/src/main/java/com/micesign/security/CustomUserDetails.java (role/departmentId 획득 방법 확인)
    - .planning/phases/31-dashboard/31-PATTERNS.md §2 (exact service refactor excerpt)
    - .planning/phases/31-dashboard/31-RESEARCH.md §"Pattern 2" (IN count aggregation)
    - .planning/phases/31-dashboard/31-CONTEXT.md D-A4, D-A5, D-A6 (locked)
    - backend/src/test/java/com/micesign/dashboard/DashboardServiceIntegrationTest.java (Task 1 RED — Task 2 가 GREEN 목표)
  </read_first>
  <behavior>
    GREEN: Task 1 의 7 테스트 모두 통과. 기존 Phase 30 test 도 green.

    ## 서비스 로직 (31-PATTERNS.md §2 excerpt)
    - USER: drafterIds=[userId], approverIds=[userId]
    - ADMIN: descendantDeptIds = findDescendantIds(departmentId); userIds = findIdsByDepartmentIdIn(descendantDeptIds) (없으면 fallback [userId]); drafterIds=approverIds=userIds
    - SUPER_ADMIN: drafterIds=null (sentinel), approverIds=null
    - private helper countDrafterStatus(drafterIds, status): null → countByStatus(status); empty → 0; else → countByDrafterIdInAndStatus
    - private helper countPending(approverIds): null → countAllPending(); empty → 0; else → countPendingByApproverIdIn
    - recentPending/recentDocuments 는 항상 userId 기준 (본인 스코프 유지, RESEARCH A6)
  </behavior>
  <action>
    **단계 A — DashboardService 전체 재작성** (`backend/src/main/java/com/micesign/service/DashboardService.java`):

    다음 내용으로 파일 전체 교체:

    ```java
    package com.micesign.service;

    import com.micesign.domain.ApprovalLine;
    import com.micesign.domain.ApprovalTemplate;
    import com.micesign.domain.Document;
    import com.micesign.domain.enums.DocumentStatus;
    import com.micesign.domain.enums.UserRole;
    import com.micesign.dto.dashboard.DashboardSummaryResponse;
    import com.micesign.dto.document.DocumentResponse;
    import com.micesign.dto.document.PendingApprovalResponse;
    import com.micesign.mapper.DocumentMapper;
    import com.micesign.repository.ApprovalLineRepository;
    import com.micesign.repository.ApprovalTemplateRepository;
    import com.micesign.repository.DepartmentRepository;
    import com.micesign.repository.DocumentRepository;
    import com.micesign.repository.UserRepository;
    import org.springframework.data.domain.Page;
    import org.springframework.data.domain.PageRequest;
    import org.springframework.stereotype.Service;
    import org.springframework.transaction.annotation.Transactional;

    import java.util.Collections;
    import java.util.List;
    import java.util.Map;
    import java.util.stream.Collectors;

    @Service
    @Transactional(readOnly = true)
    public class DashboardService {

        private final ApprovalLineRepository approvalLineRepository;
        private final DocumentRepository documentRepository;
        private final ApprovalTemplateRepository approvalTemplateRepository;
        private final DocumentMapper documentMapper;
        private final DepartmentRepository departmentRepository;
        private final UserRepository userRepository;

        public DashboardService(ApprovalLineRepository approvalLineRepository,
                                DocumentRepository documentRepository,
                                ApprovalTemplateRepository approvalTemplateRepository,
                                DocumentMapper documentMapper,
                                DepartmentRepository departmentRepository,
                                UserRepository userRepository) {
            this.approvalLineRepository = approvalLineRepository;
            this.documentRepository = documentRepository;
            this.approvalTemplateRepository = approvalTemplateRepository;
            this.documentMapper = documentMapper;
            this.departmentRepository = departmentRepository;
            this.userRepository = userRepository;
        }

        /**
         * Phase 31 D-A4/A5/A6 — role-based summary with 부서 계층 재귀 스코프.
         * drafterIds/approverIds == null  → SUPER_ADMIN sentinel (전사 카운트)
         * drafterIds/approverIds == empty → ADMIN 이지만 descendants 에 사용자 없음 (0 반환)
         * drafterIds/approverIds == non-empty → IN 절 카운트
         *
         * recentPending / recentDocuments 는 role 불문 본인 userId 스코프 유지 (RESEARCH A6).
         */
        public DashboardSummaryResponse getDashboardSummary(Long userId, UserRole role, Long departmentId) {
            List<Long> drafterIds;   // null == SUPER_ADMIN sentinel, empty == ADMIN no-users
            List<Long> approverIds;

            switch (role) {
                case USER -> {
                    drafterIds = List.of(userId);
                    approverIds = List.of(userId);
                }
                case ADMIN -> {
                    if (departmentId == null) {
                        // ADMIN 인데 부서 없음 — 본인 스코프로 fallback
                        drafterIds = List.of(userId);
                        approverIds = List.of(userId);
                    } else {
                        List<Long> deptIds = departmentRepository.findDescendantIds(departmentId);
                        List<Long> userIds = deptIds.isEmpty()
                                ? Collections.emptyList()
                                : userRepository.findIdsByDepartmentIdIn(deptIds);
                        drafterIds = userIds.isEmpty() ? List.of(userId) : userIds;
                        approverIds = drafterIds;
                    }
                }
                case SUPER_ADMIN -> {
                    drafterIds = null;
                    approverIds = null;
                }
                default -> {
                    drafterIds = List.of(userId);
                    approverIds = List.of(userId);
                }
            }

            // Counts (D-A2 semantics 재정의)
            long pendingCount   = countPending(approverIds);
            long draftCount     = countDrafterStatus(drafterIds, DocumentStatus.DRAFT);
            long submittedCount = countDrafterStatus(drafterIds, DocumentStatus.SUBMITTED);
            long completedCount = countDrafterStatus(drafterIds, DocumentStatus.APPROVED);  // APPROVED only
            long rejectedCount  = countDrafterStatus(drafterIds, DocumentStatus.REJECTED);

            // recentPending — 본인 스코프 (RESEARCH A6)
            Page<ApprovalLine> pendingPage = approvalLineRepository
                    .findPendingByApproverId(userId, PageRequest.of(0, 5));
            List<PendingApprovalResponse> recentPending = pendingPage.getContent().stream()
                    .map(line -> {
                        Document doc = line.getDocument();
                        return new PendingApprovalResponse(
                                line.getId(),
                                doc.getId(),
                                doc.getDocNumber(),
                                doc.getTemplateCode(),
                                doc.getTitle(),
                                doc.getDrafter().getName(),
                                doc.getDrafter().getDepartment() != null
                                        ? doc.getDrafter().getDepartment().getName() : null,
                                line.getStepOrder(),
                                line.getLineType().name(),
                                doc.getCreatedAt()
                        );
                    })
                    .toList();

            // recentDocuments — 본인 스코프 (RESEARCH A6)
            Map<String, String> templateNameMap = approvalTemplateRepository
                    .findByIsActiveTrueOrderBySortOrder().stream()
                    .collect(Collectors.toMap(ApprovalTemplate::getCode, ApprovalTemplate::getName));

            Page<Document> docPage = documentRepository.findByDrafterId(userId, PageRequest.of(0, 5));
            List<DocumentResponse> recentDocuments = docPage.getContent().stream()
                    .map(doc -> documentMapper.toResponse(doc,
                            templateNameMap.getOrDefault(doc.getTemplateCode(), doc.getTemplateCode())))
                    .toList();

            return new DashboardSummaryResponse(
                    pendingCount, draftCount, submittedCount, completedCount, rejectedCount,
                    recentPending, recentDocuments
            );
        }

        private long countPending(List<Long> approverIds) {
            if (approverIds == null) return approvalLineRepository.countAllPending();
            if (approverIds.isEmpty()) return 0L;
            return approvalLineRepository.countPendingByApproverIdIn(approverIds);
        }

        private long countDrafterStatus(List<Long> drafterIds, DocumentStatus status) {
            if (drafterIds == null) return documentRepository.countByStatus(status);
            if (drafterIds.isEmpty()) return 0L;
            return documentRepository.countByDrafterIdInAndStatus(drafterIds, status);
        }
    }
    ```

    기존 `getSummary(Long userId)` backward-compat alias 는 **제거** — Controller 에서 새 시그니처로 직접 호출.

    **단계 B — DashboardController 업데이트** (`backend/src/main/java/com/micesign/controller/DashboardController.java`):

    기존 L23-28 의 `getDashboard` 메서드를 다음으로 교체:

    ```java
        @GetMapping({"", "/summary"})
        public ResponseEntity<ApiResponse<DashboardSummaryResponse>> getDashboard(
                @AuthenticationPrincipal CustomUserDetails user) {
            DashboardSummaryResponse summary = dashboardService.getDashboardSummary(
                    user.getUserId(),
                    user.getUser().getRole(),
                    user.getUser().getDepartmentId()
            );
            return ResponseEntity.ok(ApiResponse.ok(summary));
        }
    ```

    **주의**: `CustomUserDetails.getUser()` 가 `User` 엔티티를 반환한다고 가정. 만약 `CustomUserDetails` 에 role/departmentId 를 직접 노출하는 getter 가 있다면 그것을 사용 (예: `user.getRole()` / `user.getDepartmentId()`). 실 파일 확인 후 정확한 accessor 사용.

    **단계 C — Task 1 테스트 실행 GREEN 확인**
  </action>
  <verify>
    <automated>cd backend &amp;&amp; ./gradlew test --tests "com.micesign.dashboard.DashboardServiceIntegrationTest" --tests "com.micesign.document.DocumentSearchPermissionMatrixTest" --console=plain 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getDashboardSummary(Long userId, UserRole role, Long departmentId)" backend/src/main/java/com/micesign/service/DashboardService.java` = 1
    - `grep -c "findDescendantIds" backend/src/main/java/com/micesign/service/DashboardService.java` = 1
    - `grep -c "findIdsByDepartmentIdIn" backend/src/main/java/com/micesign/service/DashboardService.java` = 1
    - `grep -c "countByStatus\|countAllPending" backend/src/main/java/com/micesign/service/DashboardService.java` ≥ 2
    - `grep -c "private final DepartmentRepository departmentRepository" backend/src/main/java/com/micesign/service/DashboardService.java` = 1
    - `grep -c "private final UserRepository userRepository" backend/src/main/java/com/micesign/service/DashboardService.java` = 1
    - `grep -c "dashboardService.getDashboardSummary" backend/src/main/java/com/micesign/controller/DashboardController.java` = 1
    - `./gradlew test --tests "com.micesign.dashboard.DashboardServiceIntegrationTest"` → BUILD SUCCESSFUL (7+ tests green)
    - `./gradlew test --tests "com.micesign.document.DocumentSearchPermissionMatrixTest"` → BUILD SUCCESSFUL (Phase 30 regression green)
  </acceptance_criteria>
  <done>
    DashboardService 3-arg 시그니처 완료. DashboardController 업데이트. Task 1 의 integration test 7건 GREEN. Phase 30 regression 유지.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DocumentSearchPermissionMatrixTest 확장 — 부서 계층 재귀 3 케이스 (D-A9 Option 1 regression)</name>
  <read_first>
    - backend/src/test/java/com/micesign/document/DocumentSearchPermissionMatrixTest.java (수정 대상, L1-260)
    - .planning/phases/31-dashboard/31-CONTEXT.md D-A9 (upgrade 결정)
    - .planning/phases/31-dashboard/31-VALIDATION.md (D-A9 regression gate)
    - backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java (Plan 01 upgraded — hierarchical branch)
  </read_first>
  <behavior>
    기존 테스트 9건 전부 green 유지. 신규 3 케이스 추가 — HQ > Engineering > Platform 부서 계층에서 ADMIN 이 descendant 부서 drafter 의 문서를 검색할 수 있는지 확인.

    **신규 케이스**:
    1. **`adminOfEngineering_sees_platformUser_document`** — ADMIN (dept=Engineering) 이 tab=all 로 검색 시 Platform 부서 사용자 (Alice-like) 의 SUBMITTED 문서를 볼 수 있다 (D-A9 Option 1 계층 재귀 성공)
    2. **`adminOfPlatform_does_not_see_engineering_sibling_documents`** — ADMIN (dept=Platform, leaf) 이 tab=all 로 검색 시 Engineering 본부의 문서는 못 본다 (계층 재귀 방향이 하위 → 상위 아님을 확인)
    3. **`tab_department_for_admin_engineering_covers_platform`** — tab=department 로 ADMIN (dept=Engineering) 검색 시 Engineering + Platform descendant 의 문서 모두 포함
  </behavior>
  <action>
    `backend/src/test/java/com/micesign/document/DocumentSearchPermissionMatrixTest.java` 에 다음 내용 추가 (파일 끝의 `}` 바로 앞에 삽입):

    **주의**: 현재 테스트는 V2 seed 의 부서 1,2,3 flat 구조를 사용 중. 계층 추가는 @BeforeEach 에서 HQ/Engineering/Platform/Sales 트리를 기존 부서와 별도로 신설하는 방식. 기존 테스트 격리를 위해 별도 부서 ID (800번대) 사용.

    @BeforeEach 의 끝 부분 (기존 seed 다음)에 다음 블록 추가:

    ```java
            // Phase 31 D-A9 Option 1 — 부서 계층 재귀 fixture (별도 800번대 id, 기존 seed 와 격리)
            jdbcTemplate.update("DELETE FROM \"user\" WHERE id IN (?, ?, ?)", 800L, 801L, 802L);
            jdbcTemplate.update("DELETE FROM department WHERE id IN (?, ?)", 801L, 802L);
            jdbcTemplate.update("DELETE FROM department WHERE id = ?", 800L);
            jdbcTemplate.update("INSERT INTO department (id, name, parent_id, sort_order, is_active) VALUES (?, 'P31_HQ', NULL, 800, TRUE)", 800L);
            jdbcTemplate.update("INSERT INTO department (id, name, parent_id, sort_order, is_active) VALUES (?, 'P31_Eng', ?, 801, TRUE)", 801L, 800L);
            jdbcTemplate.update("INSERT INTO department (id, name, parent_id, sort_order, is_active) VALUES (?, 'P31_Plat', ?, 802, TRUE)", 802L, 801L);
    ```

    @BeforeEach 끝에 사용자 seed 추가:
    ```java
            String pwHashHier = "$2a$10$07mcjXBfvelJFwjs8DnoJOnEqprFy.dnQL1NdnRvlqEWwwmX62SOW";
            String userSqlHier = "INSERT INTO \"user\" (id, employee_no, name, email, password, department_id, position_id, role, status, failed_login_count, must_change_password) VALUES (?, ?, ?, ?, ?, ?, 3, ?, 'ACTIVE', 0, FALSE)";
            jdbcTemplate.update(userSqlHier, 800L, "U_P31_PLAT", "P31Plat", "p31plat@test.com", pwHashHier, 802L, "USER");
            jdbcTemplate.update(userSqlHier, 801L, "A_P31_ENG", "P31Eng", "p31eng@test.com", pwHashHier, 801L, "ADMIN");
            jdbcTemplate.update(userSqlHier, 802L, "A_P31_PLAT", "P31PlatAdmin", "p31platadmin@test.com", pwHashHier, 802L, "ADMIN");
            // Platform user 가 기안한 SUBMITTED 문서
            jdbcTemplate.update(
                "INSERT INTO document (template_code, drafter_id, title, status, submitted_at, created_at, current_step) VALUES (?, ?, ?, ?, ?, ?, 1)",
                "GENERAL", 800L, "Phase31 Platform SUBMITTED", DocumentStatus.SUBMITTED.name(),
                LocalDateTime.now().minusDays(1), LocalDateTime.now());
    ```

    기존 @BeforeEach 의 cleanup 블록 앞부분에도 위 800번대 id cleanup 미러 추가. (테스트 메서드 시작 전 깔끔한 상태 보장.)

    새 테스트 메서드 3개 추가 (파일 끝 `}` 앞):

    ```java
        // ========== Phase 31 D-A9 Option 1 — 부서 계층 재귀 ==========

        @Test
        @DisplayName("Phase 31 D-A9: ADMIN (Engineering) tab=all 검색 시 Platform 하위 부서 사용자 문서 포함 (계층 재귀)")
        void adminOfEngineering_sees_platformUser_document_via_hierarchy() throws Exception {
            String token = tokenHelper.tokenForRole(801L, "p31eng@test.com", "P31Eng", UserRole.ADMIN, 801L);
            MvcResult result = mockMvc.perform(get("/api/v1/documents/search")
                    .param("tab", "all")
                    .param("size", "50")
                    .header("Authorization", "Bearer " + token)
                    .accept(MediaType.APPLICATION_JSON))
                .andExpect(status().isOk())
                .andReturn();
            JsonNode root = objectMapper.readTree(result.getResponse().getContentAsByteArray());
            JsonNode content = root.path("data").path("content");
            boolean hasPlatformDoc = false;
            for (JsonNode row : content) {
                if ("Phase31 Platform SUBMITTED".equals(row.path("title").asText())) {
                    hasPlatformDoc = true;
                    break;
                }
            }
            assertThat(hasPlatformDoc).as("Engineering ADMIN 은 Platform (하위부서) 사용자 문서 조회 가능 — D-A9 Option 1").isTrue();
        }

        @Test
        @DisplayName("Phase 31 D-A9: ADMIN (Platform, leaf) tab=all 은 Engineering 상위 부서 문서를 못 본다 (재귀 방향=하위)")
        void adminOfPlatform_does_not_see_engineering_sibling_documents() throws Exception {
            // Engineering 부서에 adminEng 이 기안한 문서를 추가
            jdbcTemplate.update(
                "INSERT INTO document (template_code, drafter_id, title, status, submitted_at, created_at, current_step) VALUES (?, ?, ?, ?, ?, ?, 1)",
                "GENERAL", 801L, "Phase31 Eng SUBMITTED", DocumentStatus.SUBMITTED.name(),
                LocalDateTime.now().minusDays(1), LocalDateTime.now());

            String token = tokenHelper.tokenForRole(802L, "p31platadmin@test.com", "P31PlatAdmin", UserRole.ADMIN, 802L);
            MvcResult result = mockMvc.perform(get("/api/v1/documents/search")
                    .param("tab", "all")
                    .param("size", "50")
                    .header("Authorization", "Bearer " + token)
                    .accept(MediaType.APPLICATION_JSON))
                .andExpect(status().isOk())
                .andReturn();
            JsonNode root = objectMapper.readTree(result.getResponse().getContentAsByteArray());
            JsonNode content = root.path("data").path("content");
            boolean hasEngDoc = false;
            for (JsonNode row : content) {
                if ("Phase31 Eng SUBMITTED".equals(row.path("title").asText())) {
                    hasEngDoc = true;
                    break;
                }
            }
            assertThat(hasEngDoc).as("Platform ADMIN 은 Engineering (상위) 문서 접근 불가 — 재귀는 하위로만").isFalse();
        }

        @Test
        @DisplayName("Phase 31 D-A9: ADMIN (Engineering) tab=department 는 Engineering + Platform descendant 문서 포함")
        void tab_department_for_admin_engineering_covers_platform() throws Exception {
            String token = tokenHelper.tokenForRole(801L, "p31eng@test.com", "P31Eng", UserRole.ADMIN, 801L);
            MvcResult result = mockMvc.perform(get("/api/v1/documents/search")
                    .param("tab", "department")
                    .param("size", "50")
                    .header("Authorization", "Bearer " + token)
                    .accept(MediaType.APPLICATION_JSON))
                .andExpect(status().isOk())
                .andReturn();
            JsonNode root = objectMapper.readTree(result.getResponse().getContentAsByteArray());
            JsonNode content = root.path("data").path("content");
            boolean hasPlatformDoc = false;
            for (JsonNode row : content) {
                if ("Phase31 Platform SUBMITTED".equals(row.path("title").asText())) {
                    hasPlatformDoc = true;
                    break;
                }
            }
            assertThat(hasPlatformDoc).as("tab=department 가 descendantDeptIds 로 Platform 포함").isTrue();
        }
    ```

    추가로 @BeforeEach 의 맨 위 cleanup 블록에 800번대 id 삭제 추가:
    ```java
            // Phase 31 hierarchy fixture cleanup (방어적)
            jdbcTemplate.update("DELETE FROM document WHERE drafter_id IN (?, ?)", 800L, 801L);
            jdbcTemplate.update("DELETE FROM \"user\" WHERE id IN (?, ?, ?)", 800L, 801L, 802L);
            jdbcTemplate.update("DELETE FROM department WHERE id IN (?, ?)", 801L, 802L);
            jdbcTemplate.update("DELETE FROM department WHERE id = ?", 800L);
    ```
  </action>
  <verify>
    <automated>cd backend &amp;&amp; ./gradlew test --tests "com.micesign.document.DocumentSearchPermissionMatrixTest" --console=plain 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "adminOfEngineering_sees_platformUser_document" backend/src/test/java/com/micesign/document/DocumentSearchPermissionMatrixTest.java` = 1
    - `grep -c "adminOfPlatform_does_not_see_engineering" backend/src/test/java/com/micesign/document/DocumentSearchPermissionMatrixTest.java` = 1
    - `grep -c "tab_department_for_admin_engineering" backend/src/test/java/com/micesign/document/DocumentSearchPermissionMatrixTest.java` = 1
    - `grep -c "P31_HQ\|P31_Eng\|P31_Plat" backend/src/test/java/com/micesign/document/DocumentSearchPermissionMatrixTest.java` ≥ 3
    - `./gradlew test --tests "com.micesign.document.DocumentSearchPermissionMatrixTest"` → BUILD SUCCESSFUL (기존 9 + 신규 3 = 12+ tests green)
  </acceptance_criteria>
  <done>
    Phase 30 matrix test 에 D-A9 regression 3 케이스 추가. 모든 12+ 케이스 green. Plan 01 의 D-A9 Option 1 upgrade 가 실 데이터로 검증됨.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JWT → CustomUserDetails → DashboardController | 인증된 사용자의 role/departmentId 가 service 로 전달 — 클라이언트가 임의로 role/dept 를 조작 불가 |
| DashboardService switch(role) | role 별 카운트 경로 분기 — role 이 USER 이면 본인 스코프만, ADMIN 이면 descendantDeptIds 로만 확장 |
| findPendingByApproverId(userId, 본인) | recentPending 쿼리는 항상 JWT 의 userId 사용 (role 불문) — role elevation 으로 인한 리스트 유출 불가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-T1 (from planning) | Elevation of Privilege | DashboardService switch(role) | mitigate | `UserRole role` 파라미터는 `CustomUserDetails.getUser().getRole()` 에서만 읽힘 — JWT 에서 파싱된 권한, 클라이언트 조작 불가. Integration test `user_sees_only_self_drafted` / `admin_sees_descendant_scope` / `superAdmin_sees_all` 3종 matrix 가 role-based branching 을 증명 |
| T-31-T2 | Information Disclosure | ADMIN 계층 재귀 경계 | mitigate | ADMIN branch: `findDescendantIds(departmentId)` 는 부서의 자손 방향만 탐색 (UNION ALL 에서 `d.parent_id = t.id` — 자식 방향). 상위 형제 부서로 누출 불가. `adminOfPlatform_does_not_see_engineering_sibling_documents` test 가 증명 |
| T-31-T3 | Information Disclosure | TanStack Query cache pollution | transfer | 본 BE plan 범위 밖 — queryKey 구조는 Plan 03 `['dashboard','summary']` 에서 role-independent 이나 FE 는 로그인 사용자 토큰 기반으로만 쿼리하므로 캐시에는 해당 사용자의 결과만 저장. 로그아웃 시 `queryClient.clear()` 는 authStore.clearAuth 에 추가되어야 함 (Plan 03/04 에서 확인하거나 별도 follow-up) |
| T-31-T4 | Race Condition / Staleness | invalidate 후 stale count 노출 | mitigate | 본 BE plan 은 read-only aggregation 으로 concurrency 문제 없음. `@Transactional(readOnly = true)` 유지. FE 의 invalidate race 는 Plan 05 의 invalidate spy test 로 커버 |
| T-31-05 | Denial of Service | recentPending/recentDocuments N+1 | accept | `findPendingByApproverId` 는 기존 Phase 29 에서 `JOIN FETCH approver.department` 추가됨. `findByDrafterId` 도 기존 쿼리. 5건 페이지 제한으로 N+1 영향 경미. Lazy init 문제는 DocumentMapper 가 본인 스코프에서 이미 처리 중 |
</threat_model>

<verification>

**Compilation:**
- `cd backend && ./gradlew compileJava compileTestJava` — exit 0

**Tests:**
- `cd backend && ./gradlew test --tests "com.micesign.dashboard.DashboardServiceIntegrationTest"` — BUILD SUCCESSFUL (7+ tests)
- `cd backend && ./gradlew test --tests "com.micesign.document.DocumentSearchPermissionMatrixTest"` — BUILD SUCCESSFUL (9 기존 + 3 신규)

**Artifact grep:**
```bash
grep -c "getDashboardSummary(Long userId, UserRole role, Long departmentId)" backend/src/main/java/com/micesign/service/DashboardService.java  # = 1
grep -c "findDescendantIds"   backend/src/main/java/com/micesign/service/DashboardService.java  # = 1
grep -c "countAllPending\|countByStatus\|countByDrafterIdInAndStatus\|countPendingByApproverIdIn" backend/src/main/java/com/micesign/service/DashboardService.java  # ≥ 4
grep -c "@Test" backend/src/test/java/com/micesign/dashboard/DashboardServiceIntegrationTest.java  # ≥ 6
grep -c "adminOfEngineering_sees_platformUser_document\|adminOfPlatform_does_not_see\|tab_department_for_admin_engineering" backend/src/test/java/com/micesign/document/DocumentSearchPermissionMatrixTest.java  # = 3
```

</verification>

<success_criteria>
- DashboardService 3-arg role-based 시그니처 완료
- DashboardController 새 시그니처로 호출
- DashboardServiceIntegrationTest 신규 6+ 케이스 green (USER/ADMIN(Eng)/ADMIN(HQ)/SUPER_ADMIN 4 role + JSON contract + CTE SoT)
- DocumentSearchPermissionMatrixTest 확장 3 신규 케이스 green
- Phase 30 기존 matrix 9 케이스 regression green
- 4-count 정확성 matrix 증명 완료
</success_criteria>

<output>
완료 후 `.planning/phases/31-dashboard/31-02-SUMMARY.md` 에:
- DashboardService role-based aggregation 완료 (USER/ADMIN/SUPER_ADMIN)
- DashboardController 3-arg 호출 적용
- integration test 6+ 케이스 + Phase 30 확장 3 케이스
- 4-count × 3-role × 부서 계층 matrix SoT 확립
- Plan 03 (FE types + hook) 가 DashboardSummaryResponse 7 필드에 의존 가능
</output>
