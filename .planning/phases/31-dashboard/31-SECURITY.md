---
phase: 31
slug: dashboard
status: verified
threats_open: 0
asvs_level: 1
created: 2026-04-25
---

# Phase 31 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail. Auto-generated by `/gsd-secure-phase 31` (gsd-security-auditor SECURED verdict, 6/6 closed).

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| JWT → CustomUserDetails → DashboardController | 인증된 사용자의 role/departmentId 가 service 로 전달. `UserRole.valueOf(user.getRole())` 만 JWT 클레임에서 읽음 | role (USER/ADMIN/SUPER_ADMIN), departmentId (Long), userId (Long) |
| DashboardService switch(role) | role 별 카운트 경로 분기 — USER 본인, ADMIN descendantDeptIds, SUPER_ADMIN 전사 | 4 카운트 + recentPending(5) + recentDocuments(5) |
| DepartmentRepository.findDescendantIds (native CTE) | `:deptId` named parameter 바인딩, 자손 방향 (parent_id = t.id) 만 탐색. 상위 형제 부서 누출 불가 | descendantDeptIds: List<Long> |
| DocumentRepositoryCustomImpl ADMIN predicate | `descendantDeptIds.in(...)` IN-list 바인딩. QueryDSL/JPA typed parameter — string injection 표면 없음 | drafterId, departmentId, descendantDeptIds |
| BE JSON response → TS DashboardSummary | axios + JWT 헤더로 호출 → BE 가 권한 기반 필터된 응답만 전송. JSON.parse 후 as cast | DashboardSummary (7 필드) |
| FE mutation onSuccess → TanStack Query | `queryClient.invalidateQueries` 는 client-side cache 작업; 다음 refetch 는 axios interceptor 의 JWT 로 BE 재호출 → 권한 우회 불가 | queryKey prefix `['dashboard']` |
| useAuthStore.user.role → statusPath helper | Zustand store 의 role 은 로그인 시 BE 검증 JWT payload 로 설정. URL 변조해도 BE 가 실 JWT 의 role 로 응답 필터 | URL string only |

---

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation | Status |
|-----------|----------|-----------|-------------|------------|--------|
| T-31-T1 | Elevation of Privilege | DashboardService switch(role) + DocumentService searchDocuments role 분기 + DashboardPage statusPath | mitigate | (BE) `DashboardController:30` `UserRole.valueOf(user.getRole())` 가 JWT-backed CustomUserDetails 에서 파싱 — 클라이언트 조작 불가. (BE) `DashboardService.getDashboardSummary` switch(role) + default fail-safe to self-scope. (Tests) `DashboardServiceIntegrationTest` 7/7 (`user_sees_only_self_drafted`, `admin_sees_descendant_scope`, `superAdmin_sees_all`, `admin_hq_covers_full_hierarchy`). (FE) `DashboardPage.statusPath` URL-only — BE 가 re-filter | closed |
| T-31-T2 | Information Disclosure (hierarchy boundary) | findDescendantIds CTE + DocumentRepositoryCustomImpl ADMIN predicate | mitigate | (BE) `DepartmentRepository.findDescendantIds` `WITH RECURSIVE dept_tree(id) AS (...)` + `:deptId` named param. 자손 방향 (parent_id = t.id) 만 탐색 — sibling/upward 누출 불가. (BE) `DocumentRepositoryCustomImpl` ADMIN 3 predicate `descendantDeptIds.in(...)`. (Tests) `DocumentSearchPermissionMatrixTest` 12/12 (`userF_otherDepartmentAdmin_notInApprovalLine`, `unrelatedUser_seesNothing`, `adminOfPlatform_does_not_see_engineering_sibling_documents`) | closed |
| T-31-T3 | Information Disclosure (TanStack Query cache pollution) | useDashboardSummary queryKey + placeholderData + 4 mutation invalidate | mitigate | (FE) `useDashboard.ts` queryKey `['dashboard','summary']` role-independent 이지만 axios interceptor 가 현 JWT 사용 → BE 재필터. `placeholderData: (prev) => prev` 는 페이지 세션 내에서만 유효 (in-memory). (Tests) `useApprovals.invalidation.test` 2/2 + `useDocuments.invalidation.test` 3/3 (incl. scope-boundary `useCreateDocument 는 dashboard invalidate 하지 않음`) | closed |
| T-31-T4 | DoS / Race / Staleness | CTE cost + invalidate race + read-only aggregation | mitigate | (BE) `DashboardService` `@Transactional(readOnly = true)` — concurrency 표면 없음. CTE cost <5ms (Plan 01/06 분석), `cte_max_recursion_depth=1000` default vs 부서트리 depth ≤5. (FE) TanStack Query v5 prefix-match invalidate 독립 직렬화 — race 시 마지막 refetch 가 SoT 일치 | closed |
| T-31-T5 | Repudiation / Input(XSS) / DoS(N+1) / Auth-bypass | i18n XSS surface + IN-list parameter binding + axios JWT interceptor | mitigate | (FE) `ErrorState.tsx` i18n `t('error')/t('errorDesc')/t('retry')` 정적 한국어 + JSX auto-escape. (BE) `DocumentRepositoryCustomImpl` QueryDSL `in(List<Long>)` typed binding — string injection 불가. (BE) `findPendingByApproverId` JOIN FETCH (Phase 29) + page size 5 — N+1 영향 경미 | closed |
| T-31-T6 | a11y (WCAG AA, focus management) | 4 카드 + ErrorState + focus-visible | mitigate | (FE) `CountCard.tsx` `isError` prop, `role="alert"`, `aria-live="polite"`, `aria-label`, `focus-visible:ring-2`. (FE) `ErrorState.tsx` `aria-busy={retrying}`, `Loader2` spinner, `refetchQueries({queryKey:['dashboard']})`. (Tests) `DashboardPage.test` 3/3 + `ErrorState.test` 3/3. Dark-mode `dark:text-{color}-400` per UI-SPEC §9 AA | closed |

*Status: open · closed*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| AR-31-T3-b | T-31-T3 (cache pollution sub-risk) | placeholderData leak across login switch — Zustand `clearAuth` + full-page refresh 가 in-memory cache 를 드롭. 동일 탭 내 role 전환 흐름 부재 (logout → 새 login 은 항상 routing 변경 동반). 위험도 낮음. follow-up 으로 31-HUMAN-UAT.md "회귀 cache pollution" 항목에서 logout → 재로그인 시 카운트 정합성 수동 확인 (이미 Test 14 PASS) | gsd-security-auditor + user verify-work pass | 2026-04-25 |
| AR-31-T4-CTE | T-31-T4 (DoS sub-risk) | findDescendantIds CTE 재호출 비용 — 호출당 1회, MariaDB cte_max_recursion_depth=1000 vs 부서트리 depth ≤5, < 5ms. v1.3+ 규모 증가 시 `@Cacheable` 재고 (현재 invalidation 복잡도 비용 > 캐싱 이득). | Plan 01/06 PLAN.md disposition | 2026-04-25 |
| AR-31-T5-N+1 | T-31-T5 (DoS sub-risk) | recentPending/recentDocuments N+1 — Phase 29 의 JOIN FETCH approver.department + page size 5 제한으로 영향 경미. | Plan 02 PLAN.md disposition | 2026-04-25 |
| AR-31-T5-Auth | T-31-T5 (Auth-bypass sub-risk) | refetchQueries 가 axios interceptor 경유 → 기존 JWT Authorization header 자동 첨부. 무인증 호출 표면 없음. | Plan 05 PLAN.md disposition | 2026-04-25 |
| AR-31-T6-Focus | T-31-T6 (focus management sub-risk) | invalidate 후 React rerender 시 focus-visible ring 상태 브라우저 기본 보존. 본 Phase 에서 별도 focus 조작 없음. | Plan 05 PLAN.md disposition | 2026-04-25 |

*Accepted risks do not resurface in future audit runs.*

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-04-25 | 6 | 6 | 0 | gsd-security-auditor (sonnet, ASVS L1, block_on=high) |

### Audit Run Notes (2026-04-25)

- **Inputs:** 6 PLAN.md threat models (consolidated to 6 unique threat IDs), 6 SUMMARY.md (no additional Threat Flags), 16 source/test files cross-checked for evidence.
- **Method:** Auditor verified each `mitigate` claim's evidence in actual code/tests (file:line citations). Cross-verification:
  - DashboardServiceIntegrationTest 7/7 GREEN (role × scope matrix)
  - DocumentSearchPermissionMatrixTest 12/12 GREEN (9 Phase 30 regression + 3 D-A9 hierarchy)
  - useApprovals.invalidation.test 2/2 + useDocuments.invalidation.test 3/3 (incl. scope-boundary)
  - DashboardPage.test 3/3 + ErrorState.test 3/3 (a11y + i18n)
- **Outcome:** SECURED — 0 high-severity gaps, no implementation gaps for claimed mitigations.
- **Gates:** ASVS L1 baseline met for in-scope threats. block_on=high not triggered (no high-severity open).

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-04-25
