---
phase: 31-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/micesign/dto/dashboard/DashboardSummaryResponse.java
  - backend/src/main/java/com/micesign/repository/DepartmentRepository.java
  - backend/src/main/java/com/micesign/repository/DocumentRepository.java
  - backend/src/main/java/com/micesign/repository/ApprovalLineRepository.java
  - backend/src/main/java/com/micesign/repository/UserRepository.java
  - backend/src/main/java/com/micesign/repository/DocumentRepositoryCustom.java
  - backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java
  - backend/src/main/java/com/micesign/service/DocumentService.java
autonomous: true
requirements: [DASH-01]
tags: [dashboard, backend, repository, rbac, hierarchy]

must_haves:
  truths:
    - "DashboardSummaryResponse 레코드에 rejectedCount long 필드가 존재하며 completedCount 주석이 'APPROVED only' 로 갱신되어 있다"
    - "DepartmentRepository.findDescendantIds(Long) 가 WITH RECURSIVE CTE 로 루트 부서 포함 모든 후손 ID 를 반환한다"
    - "DocumentRepository 에 countByDrafterIdInAndStatus(List<Long>, DocumentStatus) 와 countByStatus(DocumentStatus) 메서드가 존재한다"
    - "ApprovalLineRepository 에 countPendingByApproverIdIn(List<Long>) 과 countAllPending() 메서드가 존재한다"
    - "UserRepository.findIdsByDepartmentIdIn(List<Long>) 이 UserStatus 필터 없이 scalar id 를 반환한다"
    - "DocumentRepositoryCustom.searchDocuments 시그니처에 List<Long> descendantDeptIds 파라미터가 추가되고 구현체가 tab=department 및 ADMIN predicate 분기에서 in(descendantDeptIds) 로 사용한다 (D-A9 Option 1)"
    - "DocumentService.searchDocuments 가 ADMIN 또는 tab∈(department,all) 시 departmentRepository.findDescendantIds(departmentId) 결과를 리포지터리에 전달한다"
  artifacts:
    - path: "backend/src/main/java/com/micesign/dto/dashboard/DashboardSummaryResponse.java"
      provides: "rejectedCount 신규 필드 + completedCount semantics 재정의"
      contains: "long rejectedCount"
    - path: "backend/src/main/java/com/micesign/repository/DepartmentRepository.java"
      provides: "findDescendantIds recursive CTE"
      contains: "findDescendantIds"
    - path: "backend/src/main/java/com/micesign/repository/DocumentRepository.java"
      provides: "countByDrafterIdInAndStatus + countByStatus"
      contains: "countByDrafterIdInAndStatus"
    - path: "backend/src/main/java/com/micesign/repository/ApprovalLineRepository.java"
      provides: "countPendingByApproverIdIn + countAllPending"
      contains: "countPendingByApproverIdIn"
    - path: "backend/src/main/java/com/micesign/repository/UserRepository.java"
      provides: "findIdsByDepartmentIdIn scalar projection"
      contains: "findIdsByDepartmentIdIn"
    - path: "backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java"
      provides: "descendantDeptIds 기반 ADMIN predicate (D-A9 Option 1)"
      contains: "descendantDeptIds"
  key_links:
    - from: "DashboardService (Plan 02)"
      to: "DepartmentRepository.findDescendantIds"
      via: "생성자 주입 + switch ADMIN 분기"
      pattern: "departmentRepository\\.findDescendantIds"
    - from: "DocumentService.searchDocuments"
      to: "DepartmentRepository.findDescendantIds"
      via: "ADMIN/tab in (department,all) 조건부 호출"
      pattern: "findDescendantIds\\(departmentId\\)"
---

<objective>
Phase 31 대시보드 고도화의 백엔드 기반층을 retrofit 한다: DashboardSummaryResponse DTO 에 `rejectedCount` 신규 필드 추가 + `completedCount` semantics 재정의 (APPROVED only), MariaDB WITH RECURSIVE CTE 기반 `DepartmentRepository.findDescendantIds(Long)` 신설, ADMIN 부서 계층 집계를 위한 count 헬퍼 5건 (DocumentRepository × 2, ApprovalLineRepository × 2, UserRepository × 1), 그리고 **D-A9 Option 1**: Phase 30 의 `DocumentRepositoryCustomImpl` ADMIN predicate 를 단일 부서에서 계층 재귀로 upgrade 하여 대시보드 숫자와 검색 결과의 SoT 정합성 확보.

Purpose: Plan 02 (DashboardService role 분기 + 통합 테스트) 가 올바로 동작하려면 본 기반층이 먼저 제공되어야 함. D-A9 을 본 plan 에 포함시키는 이유는 Phase 30 predicate 와 Phase 31 의 계층 재귀 스코프가 같은 SoT (`findDescendantIds`) 를 공유하도록 보장하여 "대시보드 숫자 ≠ 검색 결과" 버그를 구조적으로 차단하기 위함.

Output: DTO 필드 추가 + 리포지터리 메서드 6건 + `DocumentRepositoryCustom(Impl)` 시그니처 확장 + `DocumentService.searchDocuments` 호출부 수정. 컴파일 통과 + 기존 Phase 30 테스트 전부 green (계층 재귀 regression 없음).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-dashboard/31-CONTEXT.md
@.planning/phases/31-dashboard/31-RESEARCH.md
@.planning/phases/31-dashboard/31-PATTERNS.md
@.planning/phases/31-dashboard/31-VALIDATION.md
@.planning/phases/30-where/30-CONTEXT.md

@backend/src/main/java/com/micesign/dto/dashboard/DashboardSummaryResponse.java
@backend/src/main/java/com/micesign/repository/DepartmentRepository.java
@backend/src/main/java/com/micesign/repository/DocumentRepository.java
@backend/src/main/java/com/micesign/repository/ApprovalLineRepository.java
@backend/src/main/java/com/micesign/repository/UserRepository.java
@backend/src/main/java/com/micesign/repository/DocumentRepositoryCustom.java
@backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java

<interfaces>
<!-- Executor 가 작업을 시작하기 전에 알아야 할 계약. 코드베이스 탐색 불필요. -->

## DashboardSummaryResponse 현재 시그니처 (L7-14)
```java
public record DashboardSummaryResponse(
    long pendingCount,
    long draftCount,
    long submittedCount,
    long completedCount,      // 현재 주석: "user's APPROVED + REJECTED documents" → 재정의 필요
    List<PendingApprovalResponse> recentPending,
    List<DocumentResponse> recentDocuments
) {}
```

## 기존 DocumentRepository 카운트 메서드 (L28)
```java
long countByDrafterIdAndStatus(Long drafterId, DocumentStatus status);
```

## 기존 ApprovalLineRepository 카운트 메서드 (L43-49)
```java
@Query("SELECT COUNT(al) FROM ApprovalLine al JOIN al.document d " +
       "WHERE al.approver.id = :userId " +
       "AND al.status = com.micesign.domain.enums.ApprovalLineStatus.PENDING " +
       "AND al.lineType IN (com.micesign.domain.enums.ApprovalLineType.APPROVE, com.micesign.domain.enums.ApprovalLineType.AGREE) " +
       "AND d.status = com.micesign.domain.enums.DocumentStatus.SUBMITTED " +
       "AND d.currentStep = al.stepOrder")
long countPendingByApproverId(@Param("userId") Long userId);
```

## DocumentRepositoryCustomImpl 현재 ADMIN predicate (L73-82) — 단일 부서
```java
if ("ADMIN".equals(role) && departmentId != null) {
    QUser deptUser = new QUser("deptUser");
    BooleanExpression sameDepartment = doc.drafterId.in(
            JPAExpressions.select(deptUser.id)
                    .from(deptUser)
                    .where(deptUser.departmentId.eq(departmentId))
    );
    permissionBranch = permissionBranch.or(sameDepartment);
}
```

## DocumentRepositoryCustomImpl tab=department (L47)
```java
case "department" -> where.and(drafter.departmentId.eq(departmentId));
```

## DocumentRepositoryCustom 현재 시그니처 (L10)
```java
Page<DocumentResponse> searchDocuments(DocumentSearchCondition condition, Long userId, String role, Long departmentId, Pageable pageable);
```

## DocumentService.searchDocuments 현재 호출부 (L450-454)
```java
@Transactional(readOnly = true)
public Page<DocumentResponse> searchDocuments(DocumentSearchCondition condition, Long userId,
                                               UserRole role, Long departmentId, Pageable pageable) {
    return documentRepository.searchDocuments(condition, userId, role.name(), departmentId, pageable);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DashboardSummaryResponse DTO — rejectedCount 필드 추가 + completedCount 주석 재정의</name>
  <read_first>
    - backend/src/main/java/com/micesign/dto/dashboard/DashboardSummaryResponse.java (수정 대상, 14 라인)
    - .planning/phases/31-dashboard/31-PATTERNS.md §1 (exact change excerpt)
    - .planning/phases/31-dashboard/31-CONTEXT.md D-A2 (locked semantics)
  </read_first>
  <behavior>
    - Record 컴파일 성공 (javac 6 필드 → 7 필드)
    - `rejectedCount` 가 `completedCount` 바로 뒤에 위치
    - `completedCount` 주석 "= APPROVED only (Phase 31 D-A2 semantics)" 로 명확화
  </behavior>
  <action>
    파일 `backend/src/main/java/com/micesign/dto/dashboard/DashboardSummaryResponse.java` 전체를 다음으로 교체 (D-A2 locked):
    ```java
    package com.micesign.dto.dashboard;

    import com.micesign.dto.document.DocumentResponse;
    import com.micesign.dto.document.PendingApprovalResponse;
    import java.util.List;

    public record DashboardSummaryResponse(
        long pendingCount,       // approval lines awaiting this user (or scope, Phase 31 D-A4/A5)
        long draftCount,         // user's DRAFT documents (FE 노출 안 함, 타입 유지 — Phase 31 D-A3)
        long submittedCount,     // user's SUBMITTED documents
        long completedCount,     // = APPROVED only (Phase 31 D-A2 semantics 재정의 — 기존 APPROVED+REJECTED 합산 폐기)
        long rejectedCount,      // ★ 신규 (Phase 31 D-A2) — REJECTED only
        List<PendingApprovalResponse> recentPending,     // latest 5 (본인 스코프 유지)
        List<DocumentResponse> recentDocuments            // latest 5 (본인 스코프 유지)
    ) {}
    ```

    중요: 필드 순서 고정 — `rejectedCount` 를 `completedCount` 바로 뒤, `recentPending` 앞에 위치. 순서 변경 시 `DashboardService` 생성자 호출 (Plan 02) 과 테스트 assertion 이 깨짐.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; ./gradlew compileJava --console=plain 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "long rejectedCount" backend/src/main/java/com/micesign/dto/dashboard/DashboardSummaryResponse.java` 결과 = 1
    - `grep -c "APPROVED only" backend/src/main/java/com/micesign/dto/dashboard/DashboardSummaryResponse.java` 결과 ≥ 1
    - `grep -n "rejectedCount" backend/src/main/java/com/micesign/dto/dashboard/DashboardSummaryResponse.java` 의 라인번호가 `completedCount` 라인번호 + 1
    - `./gradlew compileJava` exit 0
  </acceptance_criteria>
  <done>
    Record 7 필드 (기존 6 + rejectedCount). completedCount 주석 재정의 완료. compileJava 통과. (DashboardService 는 아직 미수정 상태이므로 생성자 호출 에러가 나면 Task 6 에서 해결되나, 본 Task 에서는 DTO 만 수정 — DashboardService 는 Plan 02 에서 리팩터)
    Note: DashboardService.java L83-86 에서 기존 생성자 6-arg 호출이 컴파일 에러로 나타날 것 — 본 Task 의 verify 는 "DashboardSummaryResponse 파일 자체" 가 컴파일 가능한지만 확인. 전체 compileJava 는 Task 2-5 완료 후 Task 6 (Plan 02 와 겹침 회피)에서 통과.
    **임시 조치:** DashboardService 의 기존 return 문을 임시로 `rejectedCount` 를 0L 로 전달하도록 **본 Task 에서 같이 수정** — `DashboardService.java` L83-86 를:
    ```java
    return new DashboardSummaryResponse(
            pendingCount, draftCount, submittedCount, completedCount, 0L,
            recentPending, recentDocuments
    );
    ```
    로 **1줄만 변경** (rejectedCount=0L 자리표시자). Plan 02 에서 role 분기와 함께 실 로직으로 교체.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DepartmentRepository — findDescendantIds native WITH RECURSIVE CTE 추가</name>
  <read_first>
    - backend/src/main/java/com/micesign/repository/DepartmentRepository.java (수정 대상, 25 라인)
    - backend/src/main/java/com/micesign/repository/DocumentRepository.java L30-37 (같은 프로젝트의 `@Query` native 주석 컨벤션 analog)
    - .planning/phases/31-dashboard/31-PATTERNS.md §3 (exact CTE query excerpt + SP-2 주석 관습)
    - .planning/phases/31-dashboard/31-RESEARCH.md §"Alternatives Considered" (native CTE 권장 이유)
    - .planning/phases/31-dashboard/31-CONTEXT.md D-A6 (locked — MariaDB WITH RECURSIVE)
  </read_first>
  <behavior>
    - `findDescendantIds(1L)` 호출 시 부서 id=1 + 모든 children/grandchildren id 반환 (앵커 포함)
    - 리프 노드 (자식 없음) 호출 시 해당 id 1개만 반환 (List<Long> size=1)
    - 존재하지 않는 id 호출 시 빈 리스트 반환
  </behavior>
  <action>
    `backend/src/main/java/com/micesign/repository/DepartmentRepository.java` 의 interface body 맨 끝 (L24 `countActiveUsersByDepartment` 다음) 에 다음 메서드를 추가:

    ```java
        /**
         * Phase 31 D-A6 / D-A9 Option 1 — 부서 계층 descendant ID 수집 (MariaDB WITH RECURSIVE CTE).
         * 주어진 deptId 를 앵커로 하여 self + 모든 하위 부서 id 반환 (is_active 불문).
         * 소비자: DashboardService (Phase 31 ADMIN 스코프), DocumentRepositoryCustomImpl (Phase 30 predicate 재귀 upgrade).
         *
         * MariaDB 10.11+ WITH RECURSIVE 지원. JPQL WITH 대신 nativeQuery 사용 —
         * Spring Data JPA HQL parser 의 CTE 지원 버전 의존성 회피.
         * 일반 MiceSign 부서 트리 깊이 ≤ 5, MariaDB cte_max_recursion_depth default 1000 여유.
         */
        @Query(value = """
            WITH RECURSIVE dept_tree AS (
              SELECT id FROM department WHERE id = :deptId
              UNION ALL
              SELECT d.id
              FROM department d
              INNER JOIN dept_tree t ON d.parent_id = t.id
            )
            SELECT id FROM dept_tree
            """, nativeQuery = true)
        List<Long> findDescendantIds(@Param("deptId") Long deptId);
    ```

    `import org.springframework.data.repository.query.Param;` 추가 필수 (현재 파일에 Param import 없음).

    기존 메서드 모두 유지. 삭제 없음.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; ./gradlew compileJava --console=plain 2>&amp;1 | tail -5 &amp;&amp; grep -c "findDescendantIds" backend/src/main/java/com/micesign/repository/DepartmentRepository.java</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "findDescendantIds" backend/src/main/java/com/micesign/repository/DepartmentRepository.java` 결과 = 1
    - `grep -c "WITH RECURSIVE" backend/src/main/java/com/micesign/repository/DepartmentRepository.java` 결과 = 1
    - `grep -c "nativeQuery = true" backend/src/main/java/com/micesign/repository/DepartmentRepository.java` 결과 = 1
    - `grep -c "import org.springframework.data.repository.query.Param" backend/src/main/java/com/micesign/repository/DepartmentRepository.java` 결과 = 1
    - `./gradlew compileJava` exit 0
  </acceptance_criteria>
  <done>
    DepartmentRepository 에 findDescendantIds(Long) native CTE 메서드 추가됨. 컴파일 통과. 실 동작 테스트는 Plan 02 의 `DashboardServiceIntegrationTest` 에서 계층 fixture 로 커버.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: DocumentRepository — countByDrafterIdInAndStatus + countByStatus 추가</name>
  <read_first>
    - backend/src/main/java/com/micesign/repository/DocumentRepository.java (수정 대상, 40 라인)
    - .planning/phases/31-dashboard/31-PATTERNS.md §4 (exact method signature)
    - .planning/phases/31-dashboard/31-CONTEXT.md D-A4 (ADMIN/SUPER_ADMIN 스코프)
  </read_first>
  <behavior>
    - `countByDrafterIdInAndStatus([1L,2L,3L], SUBMITTED)` → 3명 중 누군가가 기안한 SUBMITTED 문서 수 반환
    - `countByDrafterIdInAndStatus([], SUBMITTED)` → 0 반환 (Spring Data 자동 IN 빈 리스트 처리, 또는 호출측에서 방어)
    - `countByStatus(SUBMITTED)` → 전체 SUBMITTED 문서 수 반환
  </behavior>
  <action>
    `backend/src/main/java/com/micesign/repository/DocumentRepository.java` 의 interface body 안, 기존 `long countByDrafterIdAndStatus(Long drafterId, DocumentStatus status);` (L28) 바로 다음 줄에 다음 2개 메서드를 추가:

    ```java
        // Phase 31 D-A4 ADMIN 스코프 — 부서 계층으로 수집한 drafter id 집합의 상태별 문서 카운트
        long countByDrafterIdInAndStatus(List<Long> drafterIds, DocumentStatus status);

        // Phase 31 D-A4 SUPER_ADMIN 스코프 — 전사 drafter 필터 zero, 상태별 문서 카운트
        long countByStatus(DocumentStatus status);
    ```

    Method-name convention — Spring Data 가 `WHERE drafter_id IN (:ids) AND status = :status` 와 `WHERE status = :status` SQL 을 자동 생성. 별도 `@Query` 불필요.

    기존 메서드 전부 유지. 삭제 없음.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; ./gradlew compileJava --console=plain 2>&amp;1 | tail -5 &amp;&amp; grep -c "countByDrafterIdInAndStatus\|^    long countByStatus" backend/src/main/java/com/micesign/repository/DocumentRepository.java</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "countByDrafterIdInAndStatus" backend/src/main/java/com/micesign/repository/DocumentRepository.java` 결과 = 1
    - `grep -cE "long countByStatus\(DocumentStatus" backend/src/main/java/com/micesign/repository/DocumentRepository.java` 결과 = 1
    - `./gradlew compileJava` exit 0
  </acceptance_criteria>
  <done>
    DocumentRepository 에 신규 count 메서드 2건 추가. 컴파일 통과.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: ApprovalLineRepository — countPendingByApproverIdIn + countAllPending 추가</name>
  <read_first>
    - backend/src/main/java/com/micesign/repository/ApprovalLineRepository.java (수정 대상, L43-49 기존 쿼리)
    - .planning/phases/31-dashboard/31-PATTERNS.md §5 (exact JPQL excerpt — 같은 WHERE 절, approver.id 만 IN 으로 확장)
    - .planning/phases/31-dashboard/31-CONTEXT.md D-A5 (ADMIN pending 부서 확장)
  </read_first>
  <behavior>
    - `countPendingByApproverIdIn([1L,2L,3L])` → 3명 중 누군가가 승인 대기인 PENDING approval_line 수
    - `countAllPending()` → 전사 PENDING + SUBMITTED + currentStep matching approval_line 수
    - 기존 `countPendingByApproverId(Long)` 는 유지 (Plan 02 의 본인 스코프 pending 분기에서 여전히 사용될 가능성 있음 — 실제로는 Plan 02 에서 `countPendingByApproverIdIn(List.of(userId))` 로 통일 호출할 것이나 backward-compat 목적 유지)
  </behavior>
  <action>
    `backend/src/main/java/com/micesign/repository/ApprovalLineRepository.java` 의 interface body, 기존 `countPendingByApproverId` (L43-49) 바로 다음에 다음 2개 메서드를 추가:

    ```java
        // Phase 31 D-A5 — ADMIN 부서 계층 확장 pending 카운트. 본 쿼리의 WHERE 절은 기존
        // countPendingByApproverId 와 완전 동일, approver.id = :userId → approver.id IN :userIds 만 변경.
        // (pending 의미 변경 시 두 쿼리 동시 수정 필수)
        @Query("SELECT COUNT(al) FROM ApprovalLine al JOIN al.document d " +
               "WHERE al.approver.id IN :userIds " +
               "AND al.status = com.micesign.domain.enums.ApprovalLineStatus.PENDING " +
               "AND al.lineType IN (com.micesign.domain.enums.ApprovalLineType.APPROVE, com.micesign.domain.enums.ApprovalLineType.AGREE) " +
               "AND d.status = com.micesign.domain.enums.DocumentStatus.SUBMITTED " +
               "AND d.currentStep = al.stepOrder")
        long countPendingByApproverIdIn(@Param("userIds") List<Long> userIds);

        // Phase 31 D-A4 SUPER_ADMIN — 전사 PENDING approval_line 카운트 (approver 필터 zero).
        // 상기와 동일한 WHERE 절에서 approver 분기만 제거.
        @Query("SELECT COUNT(al) FROM ApprovalLine al JOIN al.document d " +
               "WHERE al.status = com.micesign.domain.enums.ApprovalLineStatus.PENDING " +
               "AND al.lineType IN (com.micesign.domain.enums.ApprovalLineType.APPROVE, com.micesign.domain.enums.ApprovalLineType.AGREE) " +
               "AND d.status = com.micesign.domain.enums.DocumentStatus.SUBMITTED " +
               "AND d.currentStep = al.stepOrder")
        long countAllPending();
    ```

    기존 `countPendingByApproverId` 유지 (backward compat). 삭제 없음.

    중요: WHERE 절 4가지 조건 (status=PENDING, lineType∈(APPROVE,AGREE), document.status=SUBMITTED, document.currentStep=al.stepOrder) 완전 동일 유지. SKIPPED/ASSIGNED/APPROVED/REJECTED status 는 포함 금지 (31-PATTERNS.md §5 Pitfall 경고).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; ./gradlew compileJava --console=plain 2>&amp;1 | tail -5 &amp;&amp; grep -c "countPendingByApproverIdIn\|countAllPending" backend/src/main/java/com/micesign/repository/ApprovalLineRepository.java</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "countPendingByApproverIdIn" backend/src/main/java/com/micesign/repository/ApprovalLineRepository.java` 결과 = 1
    - `grep -c "countAllPending" backend/src/main/java/com/micesign/repository/ApprovalLineRepository.java` 결과 = 1
    - `grep -c "al.approver.id IN :userIds" backend/src/main/java/com/micesign/repository/ApprovalLineRepository.java` 결과 = 1
    - `./gradlew compileJava` exit 0
  </acceptance_criteria>
  <done>
    ApprovalLineRepository 에 신규 2개 JPQL 쿼리 메서드 추가. 컴파일 통과.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: UserRepository — findIdsByDepartmentIdIn scalar projection 추가</name>
  <read_first>
    - backend/src/main/java/com/micesign/repository/UserRepository.java (수정 대상, 33 라인)
    - .planning/phases/31-dashboard/31-PATTERNS.md §6 (exact JPQL excerpt + D-A7 UserStatus 필터 zero 명시)
    - .planning/phases/31-dashboard/31-CONTEXT.md D-A7 (ACTIVE/INACTIVE/RETIRED 모두 포함 정책)
  </read_first>
  <behavior>
    - `findIdsByDepartmentIdIn([1L,2L,3L])` → 부서 1,2,3 에 속한 모든 사용자 id (status 불문) 반환
    - `findIdsByDepartmentIdIn([])` → 빈 List (Spring Data IN 빈 리스트 방어는 호출측에서)
    - 반환형 `List<Long>` — 엔티티 미로딩 scalar projection
  </behavior>
  <action>
    `backend/src/main/java/com/micesign/repository/UserRepository.java` 의 interface body 맨 끝 (L32 `findByRoleAndStatus` 다음) 에 다음 메서드 추가:

    ```java
        // Phase 31 D-A4 — 부서 계층 descendant 에서 user id scalar 수집.
        // D-A7 locked: UserStatus 필터 없음 (ACTIVE/INACTIVE/RETIRED 모두 포함 — 과거 퇴직자 기안 문서도 ADMIN 스코프 카운트 반영)
        // NOTIF-04 의 "RETIRED 수신자 skip" 은 발송 대상 필터이고 본 메서드와 정책 다름.
        @Query("SELECT u.id FROM User u WHERE u.departmentId IN :deptIds")
        List<Long> findIdsByDepartmentIdIn(@Param("deptIds") List<Long> deptIds);
    ```

    필요 import:
    ```java
    import org.springframework.data.jpa.repository.Query;
    import org.springframework.data.repository.query.Param;
    ```
    (현재 파일에 두 import 없음 — 추가 필수)

    기존 메서드 전부 유지.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; ./gradlew compileJava --console=plain 2>&amp;1 | tail -5 &amp;&amp; grep -c "findIdsByDepartmentIdIn" backend/src/main/java/com/micesign/repository/UserRepository.java</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "findIdsByDepartmentIdIn" backend/src/main/java/com/micesign/repository/UserRepository.java` 결과 = 1
    - `grep -c "SELECT u.id FROM User u WHERE u.departmentId IN :deptIds" backend/src/main/java/com/micesign/repository/UserRepository.java` 결과 = 1
    - `grep -c "import org.springframework.data.jpa.repository.Query" backend/src/main/java/com/micesign/repository/UserRepository.java` 결과 = 1
    - `./gradlew compileJava` exit 0
  </acceptance_criteria>
  <done>
    UserRepository 에 scalar id projection 메서드 1건 추가. import 2건 추가. 컴파일 통과.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: DocumentRepositoryCustom(Impl) + DocumentService — D-A9 Option 1 계층 재귀 predicate upgrade</name>
  <read_first>
    - backend/src/main/java/com/micesign/repository/DocumentRepositoryCustom.java (수정 대상, 11 라인)
    - backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java (수정 대상, L30-83 searchDocuments + permission predicate)
    - backend/src/main/java/com/micesign/service/DocumentService.java L450-454 (호출부)
    - .planning/phases/31-dashboard/31-PATTERNS.md §7, §8 (exact upgrade excerpt for L73-82 + L47)
    - .planning/phases/31-dashboard/31-CONTEXT.md D-A9 Option 1 (권장)
    - .planning/phases/30-where/30-CONTEXT.md D-A2 (Phase 30 원래 predicate 정의 — 본 plan 에서 upgrade)
    - backend/src/test/java/com/micesign/document/DocumentSearchPermissionMatrixTest.java (Phase 30 기존 테스트 — regression 확인)
  </read_first>
  <behavior>
    - `searchDocuments` 가 `descendantDeptIds: List<Long>` 파라미터를 받음
    - `tab=department`: `drafter.departmentId.in(descendantDeptIds)` 로 계층 확장
    - ADMIN predicate: `deptUser.departmentId.in(descendantDeptIds)` 로 계층 확장
    - descendantDeptIds 가 null/empty 이면 기존 의미 보존 (ADMIN branch 스킵, department branch 빈 결과)
    - Phase 30 기존 테스트 (DocumentSearchPermissionMatrixTest) 전부 green — E (같은 부서 ADMIN) 테스트에서 descendantDeptIds = [1] 이 전달되면 기존 단일 부서 결과와 동일
  </behavior>
  <action>
    **단계 A — interface 확장** (`backend/src/main/java/com/micesign/repository/DocumentRepositoryCustom.java`):

    파일 전체를 다음으로 교체:
    ```java
    package com.micesign.repository;

    import com.micesign.dto.document.DocumentResponse;
    import com.micesign.dto.document.DocumentSearchCondition;
    import org.springframework.data.domain.Page;
    import org.springframework.data.domain.Pageable;

    import java.util.List;

    public interface DocumentRepositoryCustom {

        /**
         * Phase 31 D-A9 Option 1 — ADMIN predicate 계층 재귀 확장.
         * descendantDeptIds: role=ADMIN 또는 tab∈(department,all) 일 때 소비자가 미리 계산해
         * 전달. null/empty 일 경우 해당 branch 는 스킵 (기존 USER/SUPER_ADMIN 의미 보존).
         */
        Page<DocumentResponse> searchDocuments(
                DocumentSearchCondition condition,
                Long userId,
                String role,
                Long departmentId,
                List<Long> descendantDeptIds,
                Pageable pageable);
    }
    ```

    **단계 B — impl 수정** (`backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java`):

    1. 시그니처 L30-33 을 다음으로 교체:
    ```java
        @Override
        public Page<DocumentResponse> searchDocuments(
                DocumentSearchCondition condition, Long userId, String role,
                Long departmentId, List<Long> descendantDeptIds, Pageable pageable) {
    ```

    2. tab=department 분기 L47 을 다음으로 교체:
    ```java
                case "department" -> {
                    if (descendantDeptIds != null && !descendantDeptIds.isEmpty()) {
                        where.and(drafter.departmentId.in(descendantDeptIds));
                    } else {
                        where.and(drafter.departmentId.eq(departmentId));  // fallback
                    }
                }
    ```

    3. tab=all ADMIN 분기 L50-53 을 다음으로 교체:
    ```java
                case "all" -> {
                    if ("ADMIN".equals(role)) {
                        if (descendantDeptIds != null && !descendantDeptIds.isEmpty()) {
                            where.and(drafter.departmentId.in(descendantDeptIds));
                        } else {
                            where.and(drafter.departmentId.eq(departmentId));
                        }
                    }
                    // SUPER_ADMIN sees all — no additional predicate
                }
    ```

    4. ADMIN permission branch L73-82 를 다음으로 교체:
    ```java
                if ("ADMIN".equals(role) && departmentId != null) {
                    QUser deptUser = new QUser("deptUser"); // 서브쿼리 별칭 — main drafter join 과 충돌 방지
                    BooleanExpression deptFilter;
                    if (descendantDeptIds != null && !descendantDeptIds.isEmpty()) {
                        // Phase 31 D-A9 Option 1 — 계층 재귀 확장
                        deptFilter = deptUser.departmentId.in(descendantDeptIds);
                    } else {
                        // fallback: 단일 부서 (descendantDeptIds 미전달 시 Phase 30 기존 동작)
                        deptFilter = deptUser.departmentId.eq(departmentId);
                    }
                    BooleanExpression sameDepartment = doc.drafterId.in(
                            JPAExpressions.select(deptUser.id)
                                    .from(deptUser)
                                    .where(deptFilter)
                    );
                    permissionBranch = permissionBranch.or(sameDepartment);
                }
    ```

    **단계 C — DocumentService 호출부 수정** (`backend/src/main/java/com/micesign/service/DocumentService.java` L450-454):

    기존:
    ```java
    @Transactional(readOnly = true)
    public Page<DocumentResponse> searchDocuments(DocumentSearchCondition condition, Long userId,
                                                   UserRole role, Long departmentId, Pageable pageable) {
        return documentRepository.searchDocuments(condition, userId, role.name(), departmentId, pageable);
    }
    ```

    변경 후:
    ```java
    @Transactional(readOnly = true)
    public Page<DocumentResponse> searchDocuments(DocumentSearchCondition condition, Long userId,
                                                   UserRole role, Long departmentId, Pageable pageable) {
        // Phase 31 D-A9 Option 1 — ADMIN 또는 tab∈(department,all) 일 때 부서 계층 descendant 수집.
        // tab=my 또는 SUPER_ADMIN 은 descendantDeptIds 불필요 → empty list.
        List<Long> descendantDeptIds;
        String tab = condition.tab() != null ? condition.tab().toLowerCase() : "my";
        boolean needsHierarchy = ("department".equals(tab))
                || ("all".equals(tab) && role == UserRole.ADMIN)
                || (role == UserRole.ADMIN && departmentId != null);
        if (needsHierarchy && departmentId != null) {
            descendantDeptIds = departmentRepository.findDescendantIds(departmentId);
        } else {
            descendantDeptIds = java.util.Collections.emptyList();
        }
        return documentRepository.searchDocuments(condition, userId, role.name(), departmentId, descendantDeptIds, pageable);
    }
    ```

    필요: `DocumentService` 에 `DepartmentRepository` DI 추가. 기존 생성자에 `DepartmentRepository departmentRepository` 파라미터 + `this.departmentRepository = departmentRepository;` 추가. 필드 선언 `private final DepartmentRepository departmentRepository;` 추가. import `com.micesign.repository.DepartmentRepository;` 추가. import `java.util.Collections;` 와 `java.util.List;` 가 기존 있는지 확인, 없으면 추가.

    **단계 D — Phase 30 테스트 regression 실행** — 기존 `DocumentSearchPermissionMatrixTest` 가 green 유지되는지 확인. userE 케이스에서 descendantDeptIds = [DEPT_1] 이 전달되어 기존 단일 부서 결과와 동일해야 함 (dept_1 의 하위 부서 없음).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; ./gradlew compileJava --console=plain 2>&amp;1 | tail -10 &amp;&amp; ./gradlew test --tests "com.micesign.document.DocumentSearchPermissionMatrixTest" --console=plain 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "descendantDeptIds" backend/src/main/java/com/micesign/repository/DocumentRepositoryCustom.java` 결과 = 1
    - `grep -c "descendantDeptIds" backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java` ≥ 3
    - `grep -c "departmentRepository.findDescendantIds" backend/src/main/java/com/micesign/service/DocumentService.java` 결과 = 1
    - `grep -c "private final DepartmentRepository departmentRepository" backend/src/main/java/com/micesign/service/DocumentService.java` 결과 = 1
    - `./gradlew compileJava` exit 0
    - `./gradlew test --tests "com.micesign.document.DocumentSearchPermissionMatrixTest"` → BUILD SUCCESSFUL (Phase 30 regression green)
  </acceptance_criteria>
  <done>
    DocumentRepositoryCustom(Impl) 시그니처 확장 + D-A9 Option 1 계층 재귀 predicate 완료. DocumentService 호출부에서 `findDescendantIds` 결과 전달. Phase 30 기존 integration test 7건 (userA_self, userB_approveApprover, userC_agreeApprover, userD_referenceApprover, userE_sameDepartmentAdmin, userF_otherDepartmentAdmin_notInApprovalLine, userG_superAdmin, unrelatedUser_seesNothing, countDistinct_withMultipleApprovalLines_returnsOneDoc) 전부 green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client HTTP → DashboardController | 인증된 사용자 JWT 토큰이 CustomUserDetails (userId/role/departmentId) 로 파싱되어 service 로 진입 |
| DashboardService → JPA/QueryDSL | 내부 호출, 권한 분기 책임은 service |
| QueryDSL → DB | bound parameter 로 SQL injection 방지 (QueryDSL 의 BooleanExpression) |
| DepartmentRepository.findDescendantIds (native CTE) | `:deptId` 는 named parameter — SQL injection 안전. 사용자 입력이 직접 접근하는 endpoint 아님 (controller → service → repo 경유) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Information Disclosure | DocumentRepositoryCustomImpl ADMIN predicate (D-A9 upgrade) | mitigate | descendantDeptIds 가 호출측(DocumentService) 에서 `findDescendantIds(departmentId)` 로만 도출되고, `departmentId` 는 CustomUserDetails (JWT) 에서 읽음 — 클라이언트 조작 불가. Phase 30 `DocumentSearchPermissionMatrixTest` 의 userF (다른 부서 ADMIN) 케이스가 green 유지되어 "타 부서 DRAFT 노출 zero" 불변식이 regression 없이 보존됨을 증명 |
| T-31-02 | Tampering / Elevation | findDescendantIds CTE | mitigate | `:deptId` named parameter 로 SQL injection 차단. CTE 재귀 깊이 MariaDB default `cte_max_recursion_depth=1000` — MiceSign 부서 트리 depth ≤ 5 에서 overflow 없음. 악의적 cycle (parent_id 순환) 은 UNION ALL 대신 UNION 을 쓰지 않아도 MariaDB 가 재귀 깊이 제한으로 자동 차단, 추가로 데이터 레이어 무결성 (parent_id FK + app-level 순환 방지) 가 existing |
| T-31-03 | Information Disclosure | DashboardSummaryResponse.draftCount (FE 미노출, 백엔드 유지) | accept | BE 는 여전히 DRAFT 카운트 계산 및 JSON 필드 노출 — 단 본인 스코프(USER) 에서만 의미 있음. ADMIN 스코프에서도 DRAFT 가 응답 JSON 에 포함되지만 본인이 기안했거나 descendantDeptIds 에 포함된 drafter 의 DRAFT 만 카운트되므로 "타 부서 DRAFT 노출" 아님. FE 에서 숨김 (D-A3). 위험도 낮음 |
| T-31-04 | Denial of Service | findDescendantIds 쿼리 성능 | accept | 부서 50개 규모 트리에서 CTE 1회 쿼리 비용 < 5ms 예상. 요청당 1회만 실행 (DashboardService 또는 DocumentService.searchDocuments 에서). v1.3+ 규모 증가 시 `@Cacheable` 재고. 현재 규모에서 캐싱 도입은 invalidation 복잡도 비용 > 이득 |
| T-31-05 | Repudiation | 본 plan 에 해당 없음 | — | audit_log 쓰기 경로 아님, read-only 집계 및 스키마 변경 |
</threat_model>

<verification>

**Compilation:**
- `cd backend && ./gradlew compileJava` — exit 0

**Regression (Phase 30 unchanged invariants):**
- `cd backend && ./gradlew test --tests "com.micesign.document.DocumentSearchPermissionMatrixTest"` — BUILD SUCCESSFUL

**Artifact presence (grep verifiable):**
```bash
grep -c "long rejectedCount" backend/src/main/java/com/micesign/dto/dashboard/DashboardSummaryResponse.java     # = 1
grep -c "findDescendantIds"  backend/src/main/java/com/micesign/repository/DepartmentRepository.java            # = 1
grep -c "WITH RECURSIVE"     backend/src/main/java/com/micesign/repository/DepartmentRepository.java            # = 1
grep -c "countByDrafterIdInAndStatus" backend/src/main/java/com/micesign/repository/DocumentRepository.java     # = 1
grep -cE "long countByStatus\(DocumentStatus" backend/src/main/java/com/micesign/repository/DocumentRepository.java  # = 1
grep -c "countPendingByApproverIdIn" backend/src/main/java/com/micesign/repository/ApprovalLineRepository.java  # = 1
grep -c "countAllPending"    backend/src/main/java/com/micesign/repository/ApprovalLineRepository.java          # = 1
grep -c "findIdsByDepartmentIdIn" backend/src/main/java/com/micesign/repository/UserRepository.java             # = 1
grep -c "descendantDeptIds"  backend/src/main/java/com/micesign/repository/DocumentRepositoryCustom.java        # = 1
grep -c "descendantDeptIds"  backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java    # ≥ 3
grep -c "departmentRepository.findDescendantIds" backend/src/main/java/com/micesign/service/DocumentService.java # = 1
```

</verification>

<success_criteria>
- DashboardSummaryResponse 7 필드 (rejectedCount 추가, 필드 순서 고정)
- DepartmentRepository.findDescendantIds — native WITH RECURSIVE CTE, List<Long> 반환
- DocumentRepository — countByDrafterIdInAndStatus, countByStatus 신규
- ApprovalLineRepository — countPendingByApproverIdIn, countAllPending 신규
- UserRepository — findIdsByDepartmentIdIn scalar projection 신규
- DocumentRepositoryCustom(Impl) — descendantDeptIds 파라미터 추가, tab=department/all ADMIN 분기에서 in(descendantDeptIds) 로 계층 재귀 확장 (D-A9 Option 1)
- DocumentService.searchDocuments — DepartmentRepository DI 추가, 호출측에서 descendantDeptIds 도출 및 전달
- Phase 30 `DocumentSearchPermissionMatrixTest` regression green (9 테스트 전부)
- DashboardService 임시로 rejectedCount=0L 자리표시자 전달 (Plan 02 에서 실 로직 교체)
</success_criteria>

<output>
완료 후 `.planning/phases/31-dashboard/31-01-SUMMARY.md` 에 다음 내용 기록:
- rejectedCount 필드 추가 + completedCount semantics 재정의 (D-A2)
- findDescendantIds native CTE (D-A6)
- 6개 리포지터리 신규 메서드
- D-A9 Option 1 완료 (Phase 30 predicate upgrade) — Phase 30 regression green
- Plan 02 (DashboardService role 분기) 에 필요한 모든 repo 자산 준비 완료
</output>
