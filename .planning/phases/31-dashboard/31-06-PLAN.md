---
phase: 31-dashboard
plan: 06
type: execute
wave: 1
depends_on: [01]
files_modified:
  - backend/src/main/java/com/micesign/repository/DocumentRepositoryCustom.java
  - backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java
  - backend/src/main/java/com/micesign/service/DocumentService.java
autonomous: true
requirements: [DASH-01]
tags: [dashboard, backend, repository, hierarchy, phase30-upgrade]

must_haves:
  truths:
    - "DocumentRepositoryCustom.searchDocuments 시그니처에 List<Long> descendantDeptIds 파라미터가 추가되었다"
    - "DocumentRepositoryCustomImpl 의 tab=department / tab=all(ADMIN) / ADMIN permission branch 3곳 모두 descendantDeptIds 기반 in-predicate 를 사용한다 (D-A9 Option 1)"
    - "descendantDeptIds 가 null 또는 empty 이면 기존 단일 부서 의미로 fallback (Phase 30 backward-compat)"
    - "DocumentService.searchDocuments 가 ADMIN 또는 tab∈(department,all) 인 호출에서 departmentRepository.findDescendantIds(departmentId) 결과를 리포지터리에 전달한다"
    - "Phase 30 DocumentSearchPermissionMatrixTest 9 케이스 전부 green (E: 같은 부서 ADMIN → descendantDeptIds=[DEPT_1] 이 전달되어 기존 단일 부서 결과와 동일)"
  artifacts:
    - path: "backend/src/main/java/com/micesign/repository/DocumentRepositoryCustom.java"
      provides: "searchDocuments 시그니처 확장"
      contains: "descendantDeptIds"
    - path: "backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java"
      provides: "descendantDeptIds 기반 ADMIN/department/all predicate (D-A9 Option 1)"
      contains: "descendantDeptIds"
    - path: "backend/src/main/java/com/micesign/service/DocumentService.java"
      provides: "DepartmentRepository DI + findDescendantIds 호출 및 전달"
      contains: "departmentRepository.findDescendantIds"
  key_links:
    - from: "DocumentService.searchDocuments"
      to: "DepartmentRepository.findDescendantIds (Plan 01)"
      via: "ADMIN/tab in (department,all) 조건부 호출"
      pattern: "findDescendantIds\\(departmentId\\)"
    - from: "DocumentRepositoryCustomImpl"
      to: "descendantDeptIds List<Long>"
      via: "permissionBranch OR tab=department/all predicate"
      pattern: "deptUser\\.departmentId\\.in\\(descendantDeptIds\\)"
---

<objective>
D-A9 Option 1 — Phase 30 의 `DocumentRepositoryCustomImpl` ADMIN predicate 를 단일 부서 매칭에서 **부서 계층 재귀** (MariaDB WITH RECURSIVE CTE 결과인 `descendantDeptIds`) 로 upgrade 한다. 대시보드 숫자 (Plan 02 DashboardService) 와 `/documents/search` 결과 (Phase 30) 의 SoT 를 `findDescendantIds` 로 통일하여 "대시보드 카운트 ≠ 검색 결과" 버그를 구조적으로 차단.

Purpose: Plan 02 (DashboardService ADMIN 스코프 집계) 가 사용자에게 노출하는 "부서 계층 재귀 카운트" 와 사용자가 카드 클릭 시 이동하는 `/documents?tab=search&status=...` 의 검색 결과가 **같은 drafter 집합** 을 대상으로 하도록 보장 (D-A8 navigation 기반). Option 1 (권장) — Option 2 (Phase 31 downgrade) 는 CONTEXT D-A9 에서 권장 아님.

Output: 3 파일 수정 (interface + impl + service caller). Phase 30 `DocumentSearchPermissionMatrixTest` 9 케이스 전부 green 유지 (backward-compat: descendantDeptIds null/empty → 단일 부서 fallback 으로 기존 동작 보존). Plan 02 Task 3 에서 hierarchical 3 케이스 신규 추가 (D-A9 신 동작 검증).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-dashboard/31-CONTEXT.md
@.planning/phases/31-dashboard/31-RESEARCH.md
@.planning/phases/31-dashboard/31-PATTERNS.md
@.planning/phases/31-dashboard/31-VALIDATION.md
@.planning/phases/31-dashboard/31-01-SUMMARY.md
@.planning/phases/30-where/30-CONTEXT.md

@backend/src/main/java/com/micesign/repository/DocumentRepositoryCustom.java
@backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java
@backend/src/main/java/com/micesign/service/DocumentService.java
@backend/src/test/java/com/micesign/document/DocumentSearchPermissionMatrixTest.java

<interfaces>
<!-- Plan 01 이 제공한 의존성 (executor 탐색 불필요) -->

## DepartmentRepository.findDescendantIds (Plan 01 추가)
```java
List<Long> findDescendantIds(@Param("deptId") Long deptId);  // self + 모든 하위 부서 id (WITH RECURSIVE CTE)
```

## DocumentRepositoryCustom 현재 시그니처 (L10)
```java
Page<DocumentResponse> searchDocuments(DocumentSearchCondition condition, Long userId, String role, Long departmentId, Pageable pageable);
```

## DocumentRepositoryCustomImpl 수정 대상 3곳

### (L47) tab=department 분기 — 단일 부서
```java
case "department" -> where.and(drafter.departmentId.eq(departmentId));
```

### (L50-53 근방) tab=all ADMIN 분기 — 단일 부서
```java
case "all" -> {
    if ("ADMIN".equals(role)) {
        where.and(drafter.departmentId.eq(departmentId));
    }
}
```

### (L73-82) ADMIN permission branch — 단일 부서
```java
if ("ADMIN".equals(role) && departmentId != null) {
    QUser deptUser = new QUser("deptUser");
    BooleanExpression sameDepartment = doc.drafterId.in(
            JPAExpressions.select(deptUser.id)
                    .from(deptUser)
                    .where(deptUser.departmentId.eq(departmentId))
    );
    permissionBranch = permissionBranch.or(sameDepartment);
}
```

## DocumentService.searchDocuments 현재 호출부 (L450-454)
```java
@Transactional(readOnly = true)
public Page<DocumentResponse> searchDocuments(DocumentSearchCondition condition, Long userId,
                                               UserRole role, Long departmentId, Pageable pageable) {
    return documentRepository.searchDocuments(condition, userId, role.name(), departmentId, pageable);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DocumentRepositoryCustom interface — searchDocuments 시그니처 확장 (descendantDeptIds 파라미터 추가)</name>
  <read_first>
    - backend/src/main/java/com/micesign/repository/DocumentRepositoryCustom.java (수정 대상, 11 라인)
    - .planning/phases/31-dashboard/31-PATTERNS.md §8 (exact signature upgrade)
    - .planning/phases/31-dashboard/31-CONTEXT.md D-A9 Option 1 (권장)
  </read_first>
  <behavior>
    - 파일 전체가 다음 시그니처로 교체됨
    - descendantDeptIds 는 `List<Long>` — null/empty 허용 (fallback 의미는 impl 에서 정의)
    - Javadoc 에 D-A9 의도 명시
  </behavior>
  <action>
    파일 `backend/src/main/java/com/micesign/repository/DocumentRepositoryCustom.java` 전체를 다음으로 교체:
    ```java
    package com.micesign.repository;

    import com.micesign.dto.document.DocumentResponse;
    import com.micesign.dto.document.DocumentSearchCondition;
    import org.springframework.data.domain.Page;
    import org.springframework.data.domain.Pageable;

    import java.util.List;

    public interface DocumentRepositoryCustom {

        /**
         * Phase 31 D-A9 Option 1 — ADMIN predicate 계층 재귀 확장.
         * descendantDeptIds: role=ADMIN 또는 tab∈(department,all) 일 때 소비자가 미리 계산해
         * 전달. null/empty 일 경우 해당 branch 는 기존 단일 부서 (departmentId.eq) 로 fallback
         * (Phase 30 backward-compat — USER/SUPER_ADMIN 의미 보존).
         */
        Page<DocumentResponse> searchDocuments(
                DocumentSearchCondition condition,
                Long userId,
                String role,
                Long departmentId,
                List<Long> descendantDeptIds,
                Pageable pageable);
    }
    ```

    기존 시그니처는 **제거** (impl 및 caller 가 함께 수정되므로 orphan 없음). 프로젝트 전체 컴파일은 Task 2 + Task 3 완료 후 통과 예정.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; grep -c "descendantDeptIds" backend/src/main/java/com/micesign/repository/DocumentRepositoryCustom.java</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "descendantDeptIds" backend/src/main/java/com/micesign/repository/DocumentRepositoryCustom.java` 결과 = 1
    - `grep -c "List<Long> descendantDeptIds" backend/src/main/java/com/micesign/repository/DocumentRepositoryCustom.java` 결과 = 1
    - `grep -c "Phase 31 D-A9 Option 1" backend/src/main/java/com/micesign/repository/DocumentRepositoryCustom.java` 결과 = 1
    - (프로젝트 전체 compileJava 는 Task 2 + 3 완료 후 verify — 본 Task 만으로는 impl 미수정 상태로 컴파일 에러 예상. 이는 의도적 wave 0 → 1 연쇄)
  </acceptance_criteria>
  <done>
    Interface 시그니처 확장 완료. Impl (Task 2) 및 caller (Task 3) 완료 후 전체 컴파일 통과.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DocumentRepositoryCustomImpl — 3곳 predicate 에서 descendantDeptIds 기반 in-절로 upgrade</name>
  <read_first>
    - backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java (수정 대상, L30-83)
    - .planning/phases/31-dashboard/31-PATTERNS.md §7 (exact upgrade excerpt)
    - .planning/phases/31-dashboard/31-CONTEXT.md D-A9 Option 1
    - .planning/phases/30-where/30-CONTEXT.md D-A2 (Phase 30 원래 predicate)
  </read_first>
  <behavior>
    - searchDocuments 메서드 시그니처가 descendantDeptIds 파라미터 추가
    - tab=department 분기에서 descendantDeptIds 가 non-empty 이면 `drafter.departmentId.in(descendantDeptIds)` 사용; 아니면 기존 `eq(departmentId)` fallback
    - tab=all ADMIN 분기도 동일 패턴
    - ADMIN permission branch (L73-82) 에서 `deptUser.departmentId.in(descendantDeptIds)` 사용; descendantDeptIds null/empty 시 `eq(departmentId)` fallback
    - SUPER_ADMIN 은 permissionBranch / tab=all 모두 추가 predicate 없음 (기존 유지)
    - 기존 USER/SUPER_ADMIN 케이스 동작 불변 (descendantDeptIds 미전달 또는 미사용)
  </behavior>
  <action>
    `backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java` 수정:

    **단계 A — 메서드 시그니처 (L30-33):**
    ```java
        @Override
        public Page<DocumentResponse> searchDocuments(
                DocumentSearchCondition condition, Long userId, String role,
                Long departmentId, List<Long> descendantDeptIds, Pageable pageable) {
    ```
    필요 import: `import java.util.List;` 가 기존 있는지 확인, 없으면 추가.

    **단계 B — tab=department 분기 (L47) 교체:**
    ```java
                case "department" -> {
                    if (descendantDeptIds != null && !descendantDeptIds.isEmpty()) {
                        where.and(drafter.departmentId.in(descendantDeptIds));
                    } else {
                        where.and(drafter.departmentId.eq(departmentId));  // Phase 30 backward-compat
                    }
                }
    ```

    **단계 C — tab=all ADMIN 분기 (L50-53 근방) 교체:**
    ```java
                case "all" -> {
                    if ("ADMIN".equals(role)) {
                        if (descendantDeptIds != null && !descendantDeptIds.isEmpty()) {
                            where.and(drafter.departmentId.in(descendantDeptIds));
                        } else {
                            where.and(drafter.departmentId.eq(departmentId));
                        }
                    }
                    // SUPER_ADMIN sees all — no additional predicate
                }
    ```

    **단계 D — ADMIN permission branch (L73-82) 교체:**
    ```java
                if ("ADMIN".equals(role) && departmentId != null) {
                    QUser deptUser = new QUser("deptUser"); // 서브쿼리 별칭 — main drafter join 과 충돌 방지
                    BooleanExpression deptFilter;
                    if (descendantDeptIds != null && !descendantDeptIds.isEmpty()) {
                        // Phase 31 D-A9 Option 1 — 계층 재귀 확장
                        deptFilter = deptUser.departmentId.in(descendantDeptIds);
                    } else {
                        // fallback: 단일 부서 (Phase 30 backward-compat)
                        deptFilter = deptUser.departmentId.eq(departmentId);
                    }
                    BooleanExpression sameDepartment = doc.drafterId.in(
                            JPAExpressions.select(deptUser.id)
                                    .from(deptUser)
                                    .where(deptFilter)
                    );
                    permissionBranch = permissionBranch.or(sameDepartment);
                }
    ```

    다른 기존 로직 (USER 분기, SUPER_ADMIN 분기, tab=my, date filter, status filter 등) 은 **수정 금지** — 본 Task 는 부서 predicate 3곳만 upgrade.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; grep -c "descendantDeptIds" backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "descendantDeptIds" backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java` 결과 ≥ 4 (시그니처 1 + 분기 3)
    - `grep -c "drafter.departmentId.in(descendantDeptIds)" backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java` 결과 = 2 (tab=department + tab=all)
    - `grep -c "deptUser.departmentId.in(descendantDeptIds)" backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java` 결과 = 1 (ADMIN permission branch)
    - `grep -c "drafter.departmentId.eq(departmentId)" backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java` 결과 ≥ 2 (backward-compat fallback 2곳)
    - (프로젝트 전체 compileJava 는 Task 3 완료 후 verify)
  </acceptance_criteria>
  <done>
    Impl 3곳 predicate 가 descendantDeptIds 기반 in-절로 upgrade. descendantDeptIds null/empty 시 기존 단일 부서 fallback (Phase 30 backward-compat). USER/SUPER_ADMIN 동작 불변.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: DocumentService.searchDocuments — DepartmentRepository DI + findDescendantIds 호출 및 전달</name>
  <read_first>
    - backend/src/main/java/com/micesign/service/DocumentService.java L450-454 (호출부)
    - backend/src/main/java/com/micesign/service/DocumentService.java (생성자 / 필드 선언부 파악 — DI 추가 위치)
    - .planning/phases/31-dashboard/31-PATTERNS.md §7 단계 C (exact caller excerpt)
    - backend/src/test/java/com/micesign/document/DocumentSearchPermissionMatrixTest.java (Phase 30 기존 테스트 — regression 확인)
  </read_first>
  <behavior>
    - DocumentService 필드에 `private final DepartmentRepository departmentRepository;` 추가
    - 생성자 파라미터에 `DepartmentRepository departmentRepository` 추가 + `this.departmentRepository = departmentRepository;` assignment
    - searchDocuments 메서드 본문에서 tab 판단 + role=ADMIN 또는 tab∈(department,all) 에 해당하는 호출에 대해 `departmentRepository.findDescendantIds(departmentId)` 결과를 리포지터리에 전달. 아니면 `Collections.emptyList()` 전달
    - Phase 30 기존 테스트 regression green — userE (같은 부서 ADMIN) 에서 dept_1 이 leaf 라면 descendantDeptIds = [DEPT_1] 단일 결과 → impl 의 in(descendantDeptIds) 가 eq(departmentId) 와 의미 동일
  </behavior>
  <action>
    **단계 A — DocumentService 필드/생성자/import 수정:**

    필드 선언부에 추가:
    ```java
        private final DepartmentRepository departmentRepository;
    ```

    생성자에 `DepartmentRepository departmentRepository` 파라미터 추가 + body 에 `this.departmentRepository = departmentRepository;` 추가.

    필요 import 확인 후 추가:
    ```java
    import com.micesign.repository.DepartmentRepository;
    import java.util.Collections;
    import java.util.List;
    ```
    (기존 import 에 있는지 확인 후 중복 없이 추가.)

    **단계 B — searchDocuments 메서드 교체 (L450-454):**

    기존:
    ```java
    @Transactional(readOnly = true)
    public Page<DocumentResponse> searchDocuments(DocumentSearchCondition condition, Long userId,
                                                   UserRole role, Long departmentId, Pageable pageable) {
        return documentRepository.searchDocuments(condition, userId, role.name(), departmentId, pageable);
    }
    ```

    변경 후:
    ```java
    @Transactional(readOnly = true)
    public Page<DocumentResponse> searchDocuments(DocumentSearchCondition condition, Long userId,
                                                   UserRole role, Long departmentId, Pageable pageable) {
        // Phase 31 D-A9 Option 1 — ADMIN 또는 tab∈(department,all) 일 때 부서 계층 descendant 수집.
        // tab=my 또는 SUPER_ADMIN 은 descendantDeptIds 불필요 → empty list (impl 의 fallback 로 동작 동일).
        String tab = condition.tab() != null ? condition.tab().toLowerCase() : "my";
        boolean needsHierarchy = ("department".equals(tab))
                || ("all".equals(tab) && role == UserRole.ADMIN)
                || (role == UserRole.ADMIN && departmentId != null);
        List<Long> descendantDeptIds = (needsHierarchy && departmentId != null)
                ? departmentRepository.findDescendantIds(departmentId)
                : Collections.emptyList();
        return documentRepository.searchDocuments(condition, userId, role.name(), departmentId, descendantDeptIds, pageable);
    }
    ```

    주의: `condition.tab()` 의 정확한 accessor 이름은 `DocumentSearchCondition` 이 record 이면 `tab()`, class 이면 `getTab()` — 실 파일 확인 후 정확 accessor 사용.

    **단계 C — Phase 30 regression 테스트 실행:**

    `./gradlew test --tests "com.micesign.document.DocumentSearchPermissionMatrixTest"` — 9 케이스 전부 green 확인. userE (같은 부서 ADMIN) 케이스가 특히 중요: dept_1 이 leaf 이면 descendantDeptIds = [DEPT_1], impl 의 in([DEPT_1]) 이 기존 eq(DEPT_1) 과 동등.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; ./gradlew compileJava --console=plain 2>&amp;1 | tail -10 &amp;&amp; ./gradlew test --tests "com.micesign.document.DocumentSearchPermissionMatrixTest" --console=plain 2>&amp;1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "private final DepartmentRepository departmentRepository" backend/src/main/java/com/micesign/service/DocumentService.java` 결과 = 1
    - `grep -c "departmentRepository.findDescendantIds" backend/src/main/java/com/micesign/service/DocumentService.java` 결과 = 1
    - `grep -c "descendantDeptIds" backend/src/main/java/com/micesign/service/DocumentService.java` 결과 ≥ 2
    - `grep -c "import com.micesign.repository.DepartmentRepository" backend/src/main/java/com/micesign/service/DocumentService.java` 결과 = 1
    - `./gradlew compileJava` exit 0 (프로젝트 전체 컴파일)
    - `./gradlew test --tests "com.micesign.document.DocumentSearchPermissionMatrixTest"` → BUILD SUCCESSFUL (Phase 30 9 케이스 regression green)
  </acceptance_criteria>
  <done>
    DocumentService 에 DepartmentRepository DI + findDescendantIds 호출 추가. Phase 30 regression 9 테스트 green. Plan 02 Task 3 에서 hierarchical 3 케이스 확장하여 D-A9 신 동작 검증.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DocumentService.searchDocuments → DocumentRepositoryCustomImpl | 내부 호출. descendantDeptIds 는 호출측이 `findDescendantIds(departmentId)` 로만 도출 — 클라이언트 조작 불가 |
| QueryDSL Impl → DB | bound parameter 로 SQL injection 방지. descendantDeptIds 는 `List<Long>` parameter binding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-T1 | Elevation of Privilege | DocumentService.searchDocuments role 분기 | transfer — canonical register in Plan 02 | role 판단은 `JWT CustomUserDetails.getRole()` SoT; DocumentService 는 해당 값만 수신. Plan 02 integration test matrix 가 증명 |
| T-31-T2 | Information Disclosure | descendantDeptIds 누출 / 타 부서 ADMIN 접근 | mitigate | descendantDeptIds 는 호출측(DocumentService) 에서 `findDescendantIds(departmentId)` 로만 도출되고, `departmentId` 는 CustomUserDetails (JWT) 에서 읽힘 — 클라이언트 조작 불가. Phase 30 `DocumentSearchPermissionMatrixTest` userF (다른 부서 ADMIN) 케이스가 green 유지되어 "타 부서 DRAFT 노출 zero" 불변식이 regression 없이 보존됨을 증명 (Plan 02 의 hierarchical 확장 3 케이스는 descendant 범위 안의 정합성까지 증명) |
| T-31-T3 | Information Disclosure | placeholderData / cache | transfer — canonical register in Plan 03/04 | 본 Plan 은 BE 만; FE cache pollution 은 Plan 03/04 에서 다룸 |
| T-31-T4 | Denial of Service | findDescendantIds 재호출 비용 | accept | searchDocuments 호출당 1회, CTE 비용 < 5ms (Plan 01 T-31-T4 와 동일 분석). 캐싱 도입 추후 재고 |
| T-31-T5 | Tampering / Elevation | descendantDeptIds list parameter binding | mitigate | Spring Data + QueryDSL `in(List<Long>)` 은 자동 parameter binding; SQL injection 불가. descendantDeptIds 자체는 Long 리스트 — string injection 표면 없음 |
</threat_model>

<verification>

**Compilation:**
- `cd backend && ./gradlew compileJava` — exit 0

**Regression (Phase 30):**
- `cd backend && ./gradlew test --tests "com.micesign.document.DocumentSearchPermissionMatrixTest"` — BUILD SUCCESSFUL (기존 9 케이스)

**Artifact grep:**
```bash
grep -c "descendantDeptIds" backend/src/main/java/com/micesign/repository/DocumentRepositoryCustom.java        # = 1
grep -c "descendantDeptIds" backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java    # ≥ 4
grep -c "departmentRepository.findDescendantIds" backend/src/main/java/com/micesign/service/DocumentService.java  # = 1
grep -c "private final DepartmentRepository departmentRepository" backend/src/main/java/com/micesign/service/DocumentService.java  # = 1
```

</verification>

<success_criteria>
- DocumentRepositoryCustom 시그니처에 descendantDeptIds 파라미터 추가
- DocumentRepositoryCustomImpl 3곳 predicate (tab=department, tab=all ADMIN, permissionBranch ADMIN) 가 descendantDeptIds 기반 in-절로 upgrade
- descendantDeptIds null/empty 시 단일 부서 fallback (Phase 30 backward-compat)
- DocumentService.searchDocuments 에 DepartmentRepository DI + findDescendantIds 호출 + 결과 전달
- Phase 30 DocumentSearchPermissionMatrixTest 9 케이스 regression green
- 프로젝트 전체 compileJava 통과
</success_criteria>

<output>
완료 후 `.planning/phases/31-dashboard/31-06-SUMMARY.md` 에:
- D-A9 Option 1 완료 (Phase 30 predicate hierarchical upgrade)
- DocumentRepositoryCustom(Impl) descendantDeptIds 파라미터 확장 (3곳)
- DocumentService DI + caller 완료
- Phase 30 regression green
- Plan 02 가 동일 `findDescendantIds` SoT 로 ADMIN 대시보드 카운트 계산 — "대시보드 숫자 ≠ 검색 결과" 버그 구조적 차단
</output>
</content>
