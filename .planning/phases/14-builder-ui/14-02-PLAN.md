---
phase: 14-builder-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/admin/components/TemplateListTable.tsx
  - frontend/src/features/admin/components/TemplateCreateModal.tsx
  - frontend/src/features/admin/pages/TemplateListPage.tsx
autonomous: true
requirements:
  - BLDR-06
must_haves:
  truths:
    - "Admin sees a table of all templates with name, code, field count, version, active status, and modified date"
    - "Admin can click 'New Template' to open a create modal with name/prefix/description fields"
    - "After creating a template, admin is navigated to the builder page"
    - "Admin can deactivate a template via confirm dialog"
  artifacts:
    - path: "frontend/src/features/admin/components/TemplateListTable.tsx"
      provides: "Template management table component"
    - path: "frontend/src/features/admin/components/TemplateCreateModal.tsx"
      provides: "Template creation modal"
    - path: "frontend/src/features/admin/pages/TemplateListPage.tsx"
      provides: "Template management list page"
  key_links:
    - from: "TemplateListPage.tsx"
      to: "useAdminTemplates"
      via: "hook import"
      pattern: "useAdminTemplates|useDeactivateTemplate"
    - from: "TemplateCreateModal.tsx"
      to: "useCreateTemplate"
      via: "mutation hook"
      pattern: "useCreateTemplate"
---

<objective>
Template management list page with table, create modal, and deactivate functionality.

Purpose: Admins need a management page to browse, create, and deactivate templates (BLDR-06/D-17/D-18/D-20). This page is the entry point to the builder.
Output: Fully functional template list page at /admin/templates with create and deactivate actions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-builder-ui/14-CONTEXT.md
@.planning/phases/14-builder-ui/14-UI-SPEC.md
@.planning/phases/14-builder-ui/14-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (must read after plan 01 completes, but can develop in parallel using these contracts) -->

From frontend/src/features/admin/types/builder.ts (created in Plan 01):
```typescript
export interface AdminTemplateResponse {
  id: number; name: string; prefix: string; description?: string; category?: string;
  icon?: string; isActive: boolean; isBuiltIn: boolean; schemaVersion: number;
  fieldCount: number; createdAt: string; updatedAt: string;
}
export interface CreateTemplateRequest {
  name: string; prefix: string; description?: string; category?: string; icon?: string;
  schemaDefinition?: SchemaDefinition;
}
```

From frontend/src/features/admin/hooks/useAdminTemplates.ts (created in Plan 01):
```typescript
export function useAdminTemplates(): { templates, isLoading, error, refetch }
export function useCreateTemplate(): UseMutationResult
export function useDeactivateTemplate(): UseMutationResult
```

From frontend/src/features/admin/components/ConfirmDialog.tsx (existing):
```typescript
// Reusable confirm dialog with danger variant
```

From frontend/src/components/Pagination.tsx (existing, if applicable):
```typescript
// Reusable pagination component
```

Existing admin page patterns:
- UserListPage.tsx: table + search + pagination pattern
- PositionPage.tsx: table + modal pattern
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TemplateCreateModal + TemplateListTable components</name>
  <files>
    frontend/src/features/admin/components/TemplateCreateModal.tsx
    frontend/src/features/admin/components/TemplateListTable.tsx
  </files>
  <read_first>
    frontend/src/features/admin/types/builder.ts
    frontend/src/features/admin/hooks/useAdminTemplates.ts
    frontend/src/features/admin/components/ConfirmDialog.tsx
    frontend/src/features/admin/pages/PositionPage.tsx
    frontend/src/features/admin/pages/UserListPage.tsx
    frontend/src/features/admin/components/PositionTable.tsx
  </read_first>
  <action>
1. Create `frontend/src/features/admin/components/TemplateCreateModal.tsx`:
   - Props: `{ isOpen: boolean; onClose: () => void; onCreated: (id: number) => void; }`
   - Use react-hook-form with Zod schema: `{ name: z.string().min(1).max(100), prefix: z.string().min(1).max(10).regex(/^[A-Z]+$/), description: z.string().max(500).optional() }`
   - Use `useCreateTemplate()` mutation hook
   - Modal overlay: fixed inset-0 bg-black/50 z-50, centered card bg-white rounded-xl shadow-xl max-w-md w-full p-6
   - Title: "새 양식 템플릿" (from t('templates.createModalTitle'))
   - Fields: name input (required), prefix input (required, uppercase only, placeholder: "GEN"), description textarea (optional)
   - Buttons: "취소" (gray, calls onClose) + "추가" (blue-600 primary, submits form)
   - On submit success: call `onCreated(response.data.data.id)`, close modal
   - On submit error: show error message below form
   - Use `useTranslation('admin')` for all labels

2. Create `frontend/src/features/admin/components/TemplateListTable.tsx`:
   - Props: `{ templates: AdminTemplateResponse[]; onEdit: (id: number) => void; onDeactivate: (id: number) => void; }`
   - Table with columns per D-17:
     - 양식 이름 (name) — text-sm font-semibold text-gray-900
     - 접두사 (prefix) — text-sm text-gray-500 font-mono
     - 필드 수 (fieldCount) — text-sm text-gray-500, center aligned
     - 버전 (schemaVersion) — text-sm text-gray-500, `v{version}`
     - 상태 (isActive) — badge: active = `bg-green-100 text-green-800 text-xs px-2 py-0.5 rounded`, inactive = `bg-gray-100 text-gray-500`
     - 최종 수정 (updatedAt) — text-sm text-gray-500, formatted with date-fns `format(parseISO(date), 'yyyy-MM-dd HH:mm')`
     - 작업 — "수정" button (navigates to builder) + "비활성화" button (calls onDeactivate)
   - Row click on name column navigates to builder (onEdit)
   - Built-in templates (isBuiltIn=true): show "비활성화" button disabled with tooltip "기본 양식은 비활성화할 수 없습니다"
   - Empty state when templates array is empty: centered message with "등록된 양식이 없습니다" heading + "새 템플릿 버튼을 클릭하여..." description
   - Use `useTranslation('admin')` for column headers
   - Style: match existing admin tables (UserListPage table styling — border border-gray-200 dark:border-gray-700, divide-y)
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - TemplateCreateModal.tsx contains `useCreateTemplate`
    - TemplateCreateModal.tsx contains `z.string().min(1)` for name validation
    - TemplateCreateModal.tsx contains `onCreated`
    - TemplateListTable.tsx contains `AdminTemplateResponse` type import
    - TemplateListTable.tsx contains columns for name, prefix, fieldCount, schemaVersion, isActive, updatedAt
    - TemplateListTable.tsx contains `isBuiltIn` check
    - TemplateListTable.tsx contains empty state message
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>TemplateCreateModal validates and creates templates, TemplateListTable renders template data with status badges and action buttons</done>
</task>

<task type="auto">
  <name>Task 2: TemplateListPage with create, deactivate, and navigation</name>
  <files>
    frontend/src/features/admin/pages/TemplateListPage.tsx
  </files>
  <read_first>
    frontend/src/features/admin/components/TemplateListTable.tsx
    frontend/src/features/admin/components/TemplateCreateModal.tsx
    frontend/src/features/admin/components/ConfirmDialog.tsx
    frontend/src/features/admin/hooks/useAdminTemplates.ts
    frontend/src/features/admin/pages/UserListPage.tsx
  </read_first>
  <action>
1. Replace placeholder `frontend/src/features/admin/pages/TemplateListPage.tsx`:
   - Import useAdminTemplates, useDeactivateTemplate from hooks
   - Import TemplateListTable, TemplateCreateModal, ConfirmDialog
   - Import useNavigate from react-router, useTranslation from react-i18next
   - State: `isCreateOpen` (boolean), `deactivateTarget` (AdminTemplateResponse | null)
   - Fetch templates with `useAdminTemplates()`
   - Page layout following existing admin page pattern (UserListPage):
     - Header: `<h1 className="text-2xl font-semibold leading-tight">{t('templates.title')}</h1>`
     - Top-right: "새 템플릿" button (`bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg text-sm font-semibold flex items-center gap-2` with Plus icon)
     - Below header: TemplateListTable with templates data
   - Create flow (per D-18):
     - Click "새 템플릿" -> set isCreateOpen=true
     - TemplateCreateModal onCreated -> navigate(`/admin/templates/${id}/builder`)
   - Deactivate flow (per D-20):
     - TemplateListTable onDeactivate -> set deactivateTarget
     - ConfirmDialog with danger variant:
       - title: t('templates.confirmDeactivate')
       - message: t('templates.confirmDeactivateDesc')
       - confirmLabel: t('templates.deactivate')
       - On confirm: call deactivateMutation.mutateAsync(target.id), close dialog
   - Edit flow: TemplateListTable onEdit -> navigate(`/admin/templates/${id}/builder`)
   - Loading state: show spinner or skeleton
   - Error state: show error message with retry button
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - TemplateListPage.tsx contains `useAdminTemplates()`
    - TemplateListPage.tsx contains `useDeactivateTemplate()`
    - TemplateListPage.tsx contains `TemplateCreateModal`
    - TemplateListPage.tsx contains `ConfirmDialog`
    - TemplateListPage.tsx contains `navigate.*admin/templates.*builder`
    - TemplateListPage.tsx contains `text-2xl font-semibold`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>TemplateListPage shows template table with create button opening modal, deactivate with confirmation, and navigation to builder</done>
</task>

</tasks>

<threat_model>
| Threat | Severity | Mitigation | ASVS |
|--------|----------|------------|------|
| Non-admin accessing template management | High | AdminRoute guard wraps /admin/* routes, backend validates ADMIN/SUPER_ADMIN role on all /admin/templates endpoints | V4.2 |
| XSS via template name/prefix display | Medium | React's JSX escaping handles all user-provided text, no dangerouslySetInnerHTML used | V5.3 |
| CSRF on template create/deactivate | Medium | JWT Bearer token in Authorization header (not cookie-based auth for API calls), SameSite=Strict on refresh cookie | V4.2 |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` passes
- TemplateListPage renders at /admin/templates
- Create modal validates name/prefix and calls API
- Deactivate shows confirmation dialog before calling API
</verification>

<success_criteria>
- Admin can see template list table at /admin/templates
- Admin can open create modal, fill in name/prefix, and create a template
- After creation, admin is navigated to the builder page
- Admin can deactivate a template with confirmation dialog
- Empty state is shown when no templates exist
</success_criteria>

<output>
After completion, create `.planning/phases/14-builder-ui/14-02-SUMMARY.md`
</output>
