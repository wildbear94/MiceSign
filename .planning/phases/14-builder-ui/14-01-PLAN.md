---
phase: 14-builder-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/admin/types/builder.ts
  - frontend/src/features/admin/api/adminTemplateApi.ts
  - frontend/src/features/admin/hooks/useAdminTemplates.ts
  - frontend/src/features/admin/components/builder/useBuilderReducer.ts
  - frontend/src/features/admin/components/AdminSidebar.tsx
  - frontend/src/App.tsx
  - frontend/public/locales/ko/admin.json
  - frontend/public/locales/en/admin.json
  - frontend/src/features/document/types/dynamicForm.ts
  - frontend/src/features/document/components/templates/DynamicForm.tsx
  - frontend/src/features/document/components/templates/DynamicReadOnly.tsx
  - frontend/src/features/document/components/templates/dynamic/DynamicFieldRenderer.tsx
  - backend/src/main/java/com/micesign/dto/template/FieldConfig.java
autonomous: true
requirements:
  - BLDR-01
  - BLDR-02
  - BLDR-03
  - BLDR-04
  - BLDR-06
must_haves:
  truths:
    - "Builder types exist for all builder state, actions, and template API responses"
    - "Admin template API client covers GET list, GET detail, POST create, PUT update, DELETE deactivate, GET option-sets"
    - "Builder reducer manages fields, selectedFieldId, isDirty, templateSettings with all required actions"
    - "AdminSidebar shows '양식 관리' nav item linking to /admin/templates"
    - "Routes /admin/templates and /admin/templates/:id/builder are registered in App.tsx"
    - "DynamicForm and DynamicReadOnly render half-width fields side by side"
  artifacts:
    - path: "frontend/src/features/admin/types/builder.ts"
      provides: "Builder type definitions"
    - path: "frontend/src/features/admin/api/adminTemplateApi.ts"
      provides: "Admin template CRUD API client"
    - path: "frontend/src/features/admin/components/builder/useBuilderReducer.ts"
      provides: "Builder state management reducer"
    - path: "frontend/src/features/admin/hooks/useAdminTemplates.ts"
      provides: "TanStack Query hooks for template CRUD"
  key_links:
    - from: "adminTemplateApi.ts"
      to: "/api/v1/admin/templates"
      via: "axios GET/POST/PUT/DELETE"
      pattern: "apiClient\\.(get|post|put|delete).*admin/templates"
    - from: "useAdminTemplates.ts"
      to: "adminTemplateApi.ts"
      via: "TanStack Query wrapper"
      pattern: "useQuery|useMutation"
---

<objective>
Builder UI infrastructure: types, API client, state reducer, routing, sidebar, i18n keys, and DynamicForm width support.

Purpose: All downstream builder plans (02-04) depend on these types, API client, hooks, and state management. This plan also adds the width field support (D-15/D-16) to existing components and backend DTO.
Output: Complete type system, API integration layer, builder state reducer, routing wired up, sidebar updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-builder-ui/14-CONTEXT.md
@.planning/phases/14-builder-ui/14-RESEARCH.md
@.planning/phases/14-builder-ui/14-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From frontend/src/features/document/types/dynamicForm.ts:
```typescript
export type FieldType = 'text' | 'textarea' | 'number' | 'date' | 'select' | 'table' | 'staticText' | 'hidden';
export interface FieldConfig {
  placeholder?: string; maxLength?: number; min?: number; max?: number; unit?: string;
  optionSetId?: number; options?: OptionItem[]; minRows?: number; maxRows?: number;
  columns?: FieldDefinition[]; content?: string; defaultValue?: string;
}
export interface FieldDefinition { id: string; type: FieldType; label: string; required: boolean; config?: FieldConfig; }
export interface SchemaDefinition { version: number; fields: FieldDefinition[]; conditionalRules: unknown[]; calculationRules: unknown[]; }
```

From frontend/src/features/document/api/templateApi.ts:
```typescript
export const templateApi = {
  getActiveTemplates: () => apiClient.get<ApiResponse<TemplateResponse[]>>('/templates'),
  getTemplateSchema: (code: string) => apiClient.get<ApiResponse<SchemaDefinition>>(`/templates/${code}/schema`),
};
```

From frontend/src/features/admin/components/AdminSidebar.tsx:
```typescript
const navItems = [
  { to: '/admin/departments', icon: Building2, labelKey: 'sidebar.departments' },
  { to: '/admin/positions', icon: Award, labelKey: 'sidebar.positions' },
  { to: '/admin/users', icon: Users, labelKey: 'sidebar.users' },
  { to: '/admin/audit-logs', icon: ScrollText, labelKey: 'sidebar.auditLogs' },
  { to: '/admin/notifications', icon: Mail, labelKey: 'sidebar.notifications' },
] as const;
```

Backend FieldConfig.java (needs width field added):
```java
@JsonInclude(JsonInclude.Include.NON_NULL)
public record FieldConfig(
    String placeholder, Integer maxLength, Long min, Long max, String unit,
    Long optionSetId, List<OptionItemResponse> options, Integer minRows, Integer maxRows,
    List<FieldDefinition> columns, String content, String defaultValue
) {}
```

Backend Admin Template API endpoints (already implemented):
- GET  /api/v1/admin/templates — list all (active + inactive)
- GET  /api/v1/admin/templates/{id} — detail with SchemaDefinition
- POST /api/v1/admin/templates — create
- PUT  /api/v1/admin/templates/{id} — update
- DELETE /api/v1/admin/templates/{id} — deactivate
- GET  /api/v1/admin/option-sets — option set list
- GET  /api/v1/admin/option-sets/{id} — option set detail
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Builder types, Admin template API client, TanStack Query hooks</name>
  <files>
    frontend/src/features/admin/types/builder.ts
    frontend/src/features/admin/api/adminTemplateApi.ts
    frontend/src/features/admin/hooks/useAdminTemplates.ts
  </files>
  <read_first>
    frontend/src/features/document/types/dynamicForm.ts
    frontend/src/features/document/api/templateApi.ts
    frontend/src/features/document/types/document.ts
    frontend/src/types/api.ts
    frontend/src/api/client.ts
    frontend/src/features/admin/hooks/useAdminTemplates.ts
    frontend/src/features/notification/hooks/useNotificationLogs.ts
  </read_first>
  <action>
1. Create `frontend/src/features/admin/types/builder.ts`:
   - Import `FieldType`, `FieldDefinition`, `FieldConfig`, `SchemaDefinition` from `../../document/types/dynamicForm`
   - Export `AdminTemplateResponse` interface: `{ id: number; name: string; prefix: string; description?: string; category?: string; icon?: string; isActive: boolean; isBuiltIn: boolean; schemaVersion: number; fieldCount: number; createdAt: string; updatedAt: string; }`
   - Export `AdminTemplateDetailResponse` extending above with `schemaDefinition?: SchemaDefinition`
   - Export `CreateTemplateRequest`: `{ name: string; prefix: string; description?: string; category?: string; icon?: string; schemaDefinition?: SchemaDefinition; }`
   - Export `UpdateTemplateRequest`: `{ name?: string; description?: string; category?: string; icon?: string; schemaDefinition?: SchemaDefinition; }`
   - Export `TemplateSettings` interface: `{ name: string; prefix: string; description: string; category: string; icon: string; }`
   - Export `BuilderState` interface: `{ fields: FieldDefinition[]; selectedFieldId: string | null; templateSettings: TemplateSettings; isDirty: boolean; schemaVersion: number; }`
   - Export `BuilderAction` discriminated union type with these actions:
     - `INIT_SCHEMA` (schema: SchemaDefinition, settings: TemplateSettings)
     - `ADD_FIELD` (fieldType: FieldType, insertIndex: number)
     - `REMOVE_FIELD` (fieldId: string)
     - `DUPLICATE_FIELD` (fieldId: string)
     - `REORDER_FIELD` (fromIndex: number, toIndex: number)
     - `SELECT_FIELD` (fieldId: string | null)
     - `UPDATE_FIELD` (fieldId: string, changes: Partial<FieldDefinition>)
     - `UPDATE_FIELD_CONFIG` (fieldId: string, config: Partial<FieldConfig>)
     - `UPDATE_TEMPLATE_SETTINGS` (changes: Partial<TemplateSettings>)
     - `IMPORT_SCHEMA` (schema: SchemaDefinition)
     - `MARK_SAVED`
   - Export `OptionSetResponse`: `{ id: number; name: string; description?: string; items: { id: number; value: string; label: string; sortOrder: number; }[]; }`
   - Export `PALETTE_ITEMS` constant array: 8 items mapping `{ type: FieldType; labelKey: string; icon: string }` for text, textarea, number, date, select, table, staticText, hidden
   - Export `FIELD_TYPE_DEFAULTS` record mapping each FieldType to its default FieldDefinition (label defaults like '텍스트 필드', '숫자 필드', etc.; required defaults to false; config defaults per type)

2. Create `frontend/src/features/admin/api/adminTemplateApi.ts`:
   - Import apiClient from `../../../api/client`
   - Import types from `../types/builder` and `../../../types/api`
   - Export `adminTemplateApi` object with methods:
     - `getTemplates()` -> GET `/admin/templates` -> `ApiResponse<AdminTemplateResponse[]>`
     - `getTemplate(id: number)` -> GET `/admin/templates/${id}` -> `ApiResponse<AdminTemplateDetailResponse>`
     - `createTemplate(data: CreateTemplateRequest)` -> POST `/admin/templates` -> `ApiResponse<AdminTemplateDetailResponse>`
     - `updateTemplate(id: number, data: UpdateTemplateRequest)` -> PUT `/admin/templates/${id}` -> `ApiResponse<AdminTemplateDetailResponse>`
     - `deactivateTemplate(id: number)` -> DELETE `/admin/templates/${id}` -> `ApiResponse<void>`
     - `getOptionSets()` -> GET `/admin/option-sets` -> `ApiResponse<OptionSetResponse[]>`
     - `getOptionSet(id: number)` -> GET `/admin/option-sets/${id}` -> `ApiResponse<OptionSetResponse>`

3. Create `frontend/src/features/admin/hooks/useAdminTemplates.ts`:
   - Import from `@tanstack/react-query`, `adminTemplateApi`, types
   - Export `useAdminTemplates()`: useQuery with key `['admin', 'templates']`, calls `adminTemplateApi.getTemplates()`, returns `{ templates, isLoading, error, refetch }`
   - Export `useAdminTemplate(id: number)`: useQuery with key `['admin', 'templates', id]`, calls `adminTemplateApi.getTemplate(id)`, enabled when `!!id`
   - Export `useCreateTemplate()`: useMutation calling `adminTemplateApi.createTemplate()`, onSuccess invalidates `['admin', 'templates']`
   - Export `useUpdateTemplate()`: useMutation calling `adminTemplateApi.updateTemplate()`, onSuccess invalidates both `['admin', 'templates']` and `['admin', 'templates', id]`
   - Export `useDeactivateTemplate()`: useMutation calling `adminTemplateApi.deactivateTemplate()`, onSuccess invalidates `['admin', 'templates']`
   - Export `useOptionSets()`: useQuery with key `['admin', 'option-sets']`, calls `adminTemplateApi.getOptionSets()`
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - builder.ts contains `export interface AdminTemplateResponse`
    - builder.ts contains `export interface BuilderState`
    - builder.ts contains `export type BuilderAction`
    - builder.ts contains `export const PALETTE_ITEMS`
    - builder.ts contains `export const FIELD_TYPE_DEFAULTS`
    - adminTemplateApi.ts contains `apiClient.get.*admin/templates`
    - adminTemplateApi.ts contains `apiClient.post.*admin/templates`
    - adminTemplateApi.ts contains `apiClient.delete.*admin/templates`
    - adminTemplateApi.ts contains `getOptionSets`
    - useAdminTemplates.ts contains `useQuery` and `useMutation`
    - useAdminTemplates.ts contains `useCreateTemplate`
    - useAdminTemplates.ts contains `useDeactivateTemplate`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All builder types, API client, and TanStack Query hooks exist and compile without errors</done>
</task>

<task type="auto">
  <name>Task 2: Builder state reducer, nanoid install, routing, sidebar, i18n</name>
  <files>
    frontend/src/features/admin/components/builder/useBuilderReducer.ts
    frontend/src/features/admin/components/AdminSidebar.tsx
    frontend/src/App.tsx
    frontend/public/locales/ko/admin.json
    frontend/public/locales/en/admin.json
  </files>
  <read_first>
    frontend/src/features/admin/types/builder.ts
    frontend/src/features/admin/components/AdminSidebar.tsx
    frontend/src/App.tsx
    frontend/public/locales/ko/admin.json
    frontend/public/locales/en/admin.json
    frontend/src/features/document/types/dynamicForm.ts
  </read_first>
  <action>
1. Install nanoid: `cd frontend && npm install nanoid`

2. Create `frontend/src/features/admin/components/builder/useBuilderReducer.ts`:
   - Import `useReducer` from react, `nanoid` from nanoid
   - Import `BuilderState`, `BuilderAction`, `FIELD_TYPE_DEFAULTS` from `../../types/builder`
   - Import `FieldDefinition` from `../../../document/types/dynamicForm`
   - Define `initialState: BuilderState` = `{ fields: [], selectedFieldId: null, templateSettings: { name: '', prefix: '', description: '', category: '', icon: '' }, isDirty: false, schemaVersion: 1 }`
   - Implement `builderReducer(state: BuilderState, action: BuilderAction): BuilderState`:
     - `INIT_SCHEMA`: set fields from schema.fields, schemaVersion from schema.version, templateSettings from action.settings, isDirty=false, selectedFieldId=null
     - `ADD_FIELD`: create new FieldDefinition using `FIELD_TYPE_DEFAULTS[action.fieldType]` with `id: nanoid()`, splice into fields at action.insertIndex, isDirty=true
     - `REMOVE_FIELD`: filter out field by id, if selectedFieldId was removed set to null, isDirty=true
     - `DUPLICATE_FIELD`: find field by id, deep clone with `id: nanoid()` and label + ' (복사본)', insert after original, select the new field, isDirty=true
     - `REORDER_FIELD`: array reorder from fromIndex to toIndex (splice remove + insert), isDirty=true
     - `SELECT_FIELD`: set selectedFieldId
     - `UPDATE_FIELD`: map over fields, merge changes into matching field, isDirty=true
     - `UPDATE_FIELD_CONFIG`: map over fields, merge config into matching field's config, isDirty=true
     - `UPDATE_TEMPLATE_SETTINGS`: merge changes into templateSettings, isDirty=true
     - `IMPORT_SCHEMA`: replace fields with schema.fields, schemaVersion from schema.version, isDirty=true, selectedFieldId=null
     - `MARK_SAVED`: isDirty=false
   - Export `useBuilderReducer()` custom hook that returns `useReducer(builderReducer, initialState)`

3. Update `frontend/src/features/admin/components/AdminSidebar.tsx`:
   - Add `LayoutTemplate` to lucide-react imports
   - Add new entry at end of navItems array: `{ to: '/admin/templates', icon: LayoutTemplate, labelKey: 'sidebar.templates' }`

4. Update `frontend/src/App.tsx`:
   - Add lazy imports for TemplateListPage and TemplateBuilderPage (use React.lazy or direct import):
     ```
     import TemplateListPage from './features/admin/pages/TemplateListPage';
     import TemplateBuilderPage from './features/admin/pages/TemplateBuilderPage';
     ```
   - Inside the `<Route path="/admin" element={<AdminLayout />}>` block, add:
     ```
     <Route path="templates" element={<TemplateListPage />} />
     <Route path="templates/:id/builder" element={<TemplateBuilderPage />} />
     ```
   - NOTE: The page components don't exist yet. Create placeholder files:
     - `frontend/src/features/admin/pages/TemplateListPage.tsx`: export default function returning `<div>Template List</div>`
     - `frontend/src/features/admin/pages/TemplateBuilderPage.tsx`: export default function returning `<div>Template Builder</div>`

5. Update i18n files:
   - `frontend/public/locales/ko/admin.json`: Add to existing "sidebar" section: `"templates": "양식 관리"`. Add new "templates" section:
     ```json
     "templates": {
       "title": "양식 관리",
       "newTemplate": "새 템플릿",
       "createModalTitle": "새 양식 템플릿",
       "name": "양식 이름",
       "prefix": "문서번호 접두사",
       "description": "설명",
       "category": "카테고리",
       "fieldCount": "필드 수",
       "version": "버전",
       "active": "활성",
       "inactive": "비활성",
       "lastModified": "최종 수정",
       "emptyList": "등록된 양식이 없습니다",
       "emptyListDesc": "'새 템플릿' 버튼을 클릭하여 첫 번째 양식을 만들어 보세요",
       "confirmDeactivate": "양식 비활성화",
       "confirmDeactivateDesc": "이 양식을 비활성화하시겠습니까? 기존 문서는 유지되지만 새 문서 작성 시 이 양식을 선택할 수 없습니다.",
       "deactivate": "비활성화",
       "saved": "템플릿이 저장되었습니다",
       "saveError": "저장에 실패했습니다. 다시 시도해 주세요",
       "save": "저장",
       "backToList": "목록으로",
       "editMode": "편집",
       "previewMode": "프리뷰",
       "unsavedWarning": "저장하지 않은 변경사항이 있습니다. 나가시겠습니까?",
       "emptyCanvas": "필드를 추가하세요",
       "emptyCanvasDesc": "좌측 팔레트에서 필드를 드래그하거나 클릭하여 추가할 수 있습니다",
       "noFieldSelected": "필드를 선택하면 속성을 편집할 수 있습니다",
       "tabBasic": "기본",
       "tabValidation": "검증",
       "tabAdvanced": "고급",
       "fieldLabel": "라벨",
       "fieldRequired": "필수 항목",
       "fieldPlaceholder": "플레이스홀더",
       "fieldDescription": "필드 설명",
       "fieldMinLength": "최소 길이",
       "fieldMaxLength": "최대 길이",
       "fieldMinValue": "최소값",
       "fieldMaxValue": "최대값",
       "fieldPattern": "정규식 패턴",
       "fieldWidth": "너비",
       "fieldWidthFull": "전체 (100%)",
       "fieldWidthHalf": "절반 (50%)",
       "fieldDefaultValue": "기본값",
       "fieldId": "필드 ID",
       "fieldStaticContent": "안내 텍스트 내용",
       "optionValue": "값",
       "optionLabel": "표시명",
       "addOption": "옵션 추가",
       "loadOptionSet": "옵션 세트 불러오기",
       "columnLabel": "칼럼명",
       "columnType": "칼럼 타입",
       "columnRequired": "필수",
       "addColumn": "칼럼 추가",
       "jsonExport": "JSON 내보내기",
       "jsonImport": "JSON 가져오기",
       "jsonImportTitle": "JSON 스키마 가져오기",
       "jsonImportError": "유효하지 않은 스키마입니다",
       "jsonImportConfirm": "적용",
       "desktopOnly": "양식 빌더는 데스크톱 브라우저에서 사용해 주세요",
       "duplicate": "복제",
       "delete": "삭제"
     }
     ```
   - `frontend/public/locales/en/admin.json`: Add matching English translations for the same keys (sidebar.templates = "Template Management", etc.)
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - useBuilderReducer.ts contains `import { nanoid }`
    - useBuilderReducer.ts contains `case 'ADD_FIELD'` and `case 'DUPLICATE_FIELD'`
    - useBuilderReducer.ts contains `export function useBuilderReducer`
    - AdminSidebar.tsx contains `LayoutTemplate` import
    - AdminSidebar.tsx contains `sidebar.templates`
    - App.tsx contains `templates` route path
    - App.tsx contains `TemplateBuilderPage`
    - admin.json (ko) contains `"templates": "양식 관리"` in sidebar section
    - admin.json (ko) contains `"emptyCanvas": "필드를 추가하세요"`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Builder reducer handles all 11 action types, AdminSidebar has templates nav, App.tsx has routes, i18n has all builder keys</done>
</task>

<task type="auto">
  <name>Task 3: Backend width field + DynamicForm/DynamicReadOnly width support (D-15/D-16)</name>
  <files>
    backend/src/main/java/com/micesign/dto/template/FieldConfig.java
    frontend/src/features/document/types/dynamicForm.ts
    frontend/src/features/document/components/templates/DynamicForm.tsx
    frontend/src/features/document/components/templates/DynamicReadOnly.tsx
    frontend/src/features/document/components/templates/dynamic/DynamicFieldRenderer.tsx
  </files>
  <read_first>
    backend/src/main/java/com/micesign/dto/template/FieldConfig.java
    frontend/src/features/document/types/dynamicForm.ts
    frontend/src/features/document/components/templates/DynamicForm.tsx
    frontend/src/features/document/components/templates/DynamicReadOnly.tsx
    frontend/src/features/document/components/templates/dynamic/DynamicFieldRenderer.tsx
  </read_first>
  <action>
1. Update `backend/src/main/java/com/micesign/dto/template/FieldConfig.java`:
   - Add `String width` parameter to the record at the end (after `defaultValue`). The @JsonInclude(NON_NULL) annotation ensures existing schemas without width are unaffected.
   - Final record parameters: `(String placeholder, Integer maxLength, Long min, Long max, String unit, Long optionSetId, List<OptionItemResponse> options, Integer minRows, Integer maxRows, List<FieldDefinition> columns, String content, String defaultValue, String width)`

2. Update `frontend/src/features/document/types/dynamicForm.ts`:
   - Add `width?: 'full' | 'half';` to FieldConfig interface (after `defaultValue`)

3. Update `frontend/src/features/document/components/templates/DynamicForm.tsx`:
   - Find the container where fields are rendered (the map over schema.fields)
   - Wrap the field iteration in a `<div className="flex flex-wrap gap-x-4">` container
   - For each field, wrap the DynamicFieldRenderer in a div with conditional className:
     ```tsx
     <div key={field.id} className={field.config?.width === 'half' ? 'w-[calc(50%-0.5rem)]' : 'w-full'}>
       <DynamicFieldRenderer ... />
     </div>
     ```
   - Ensure the existing structure is preserved (title input, form elements, save button)

4. Update `frontend/src/features/document/components/templates/DynamicReadOnly.tsx`:
   - Apply the same flex-wrap width pattern as DynamicForm:
     ```tsx
     <div className="flex flex-wrap gap-x-4">
       {schema.fields.map(field => (
         <div key={field.id} className={field.config?.width === 'half' ? 'w-[calc(50%-0.5rem)]' : 'w-full'}>
           {/* existing field rendering */}
         </div>
       ))}
     </div>
     ```
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign && cd backend && ./gradlew compileJava 2>&1 | tail -5 && cd ../frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - FieldConfig.java contains `String width`
    - dynamicForm.ts FieldConfig interface contains `width?: 'full' | 'half'`
    - DynamicForm.tsx contains `flex flex-wrap`
    - DynamicForm.tsx contains `w-[calc(50%-0.5rem)]`
    - DynamicReadOnly.tsx contains `w-[calc(50%-0.5rem)]`
    - `./gradlew compileJava` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Backend FieldConfig has width field, frontend FieldConfig has width type, DynamicForm and DynamicReadOnly render half-width fields side by side</done>
</task>

</tasks>

<threat_model>
| Threat | Severity | Mitigation | ASVS |
|--------|----------|------------|------|
| Unauthorized access to admin template API | High | Routes wrapped in AdminRoute guard (existing), API calls go through authenticated apiClient with JWT | V4.2 |
| XSS via template name/description in i18n | Low | i18n keys are static strings, user input is rendered through React's default escaping | V5.3 |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` passes
- `cd backend && ./gradlew compileJava` passes
- AdminSidebar renders "양식 관리" nav item
- Routes /admin/templates and /admin/templates/:id/builder are registered
</verification>

<success_criteria>
- All builder types, API client, hooks, and reducer compile without errors
- AdminSidebar shows template management nav item
- App.tsx has builder routes registered
- i18n has all builder-related Korean translation keys
- DynamicForm/DynamicReadOnly support width='half' rendering
- Backend FieldConfig accepts width parameter
</success_criteria>

<output>
After completion, create `.planning/phases/14-builder-ui/14-01-SUMMARY.md`
</output>
