---
phase: 26
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vitest.config.ts
  - frontend/src/features/admin/validations/templateImportSchema.ts
  - frontend/src/features/admin/validations/templateImportSchema.test.ts
  - frontend/src/features/admin/utils/templateExport.ts
  - frontend/src/features/admin/utils/templateExport.test.ts
  - frontend/src/features/admin/presets/expense.json
  - frontend/src/features/admin/presets/leave.json
  - frontend/src/features/admin/presets/trip.json
  - frontend/src/features/admin/presets/purchase.json
  - frontend/src/features/admin/presets/index.ts
  - frontend/src/features/admin/presets/presets.test.ts
  - frontend/src/features/admin/components/TemplateFormModal.tsx
autonomous: true
requirements: [CNV-01, CNV-02, CNV-03, CNV-04]

must_haves:
  truths:
    - "templateImportSchema로 유효한 JSON 파싱 성공, 잘못된 JSON은 경로별 오류 반환"
    - "buildExportPayload가 code/id/createdBy/createdAt/isActive를 제외하고 exportFormatVersion=1 포함"
    - "buildExportFilename이 {code}-YYYYMMDD.json 반환(code는 영숫자/_-로 sanitize)"
    - "4개 프리셋 JSON이 templateImportSchema.parse()를 통과"
    - "TemplateFormModal이 initialValues prop을 받아 생성 모드에서 name/description/prefix/category/icon/schemaFields/conditionalRules/calculationRules를 prefill"
    - "vitest 실행 커맨드 `npm run test`가 동작"
  artifacts:
    - path: "frontend/src/features/admin/validations/templateImportSchema.ts"
      provides: "Zod 스키마 + TemplateImportData 타입"
      exports: ["templateImportSchema", "TemplateImportData", "flattenZodErrors"]
    - path: "frontend/src/features/admin/utils/templateExport.ts"
      provides: "Export 직렬화 + 파일명 + Blob download"
      exports: ["buildExportPayload", "buildExportFilename", "downloadTemplateJson"]
    - path: "frontend/src/features/admin/presets/index.ts"
      provides: "4종 프리셋 eager glob + 런타임 검증"
      exports: ["presets", "Preset"]
    - path: "frontend/src/features/admin/components/TemplateFormModal.tsx"
      provides: "initialValues prop 지원"
      contains: "initialValues?: Partial"
    - path: "frontend/vitest.config.ts"
      provides: "Vitest 설정 (jsdom, RTL)"
      contains: "defineConfig"
  key_links:
    - from: "frontend/src/features/admin/presets/index.ts"
      to: "templateImportSchema"
      via: "런타임 parse() 호출"
      pattern: "templateImportSchema\\.parse"
    - from: "frontend/src/features/admin/utils/templateExport.ts"
      to: "TemplateDetailItem"
      via: "detail.code / detail.schemaDefinition"
      pattern: "buildExportPayload"
    - from: "frontend/src/features/admin/components/TemplateFormModal.tsx"
      to: "initialValues prop"
      via: "생성 모드 reset"
      pattern: "initialValues\\?\\."
---

<objective>
Phase 26 기반 레이어 — Vitest 인프라, templateImportSchema(Zod), templateExport 유틸, 4종 프리셋 JSON, TemplateFormModal의 initialValues prop 확장, 그리고 이 모든 것을 자동 검증하는 Vitest 단위 테스트 추가.

Purpose: 플랜 02의 UI 통합이 의존하는 순수 도메인/유틸/검증 레이어를 먼저 고정한다. D-05/D-08/D-12/D-14/D-21 로크 결정을 직접 구현.
Output: 테스트 가능한 Zod 스키마, export 순수 함수, 번들 assets, 확장된 모달 prop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-convenience-features/26-CONTEXT.md
@.planning/phases/26-convenience-features/26-RESEARCH.md
@.planning/phases/26-convenience-features/26-UI-SPEC.md
@.planning/phases/26-convenience-features/26-VALIDATION.md
@frontend/src/features/document/types/dynamicForm.ts
@frontend/src/features/admin/api/templateApi.ts
@frontend/src/features/admin/components/TemplateFormModal.tsx
@frontend/package.json

<interfaces>
<!-- RESEARCH.md §Pattern 1 및 dynamicForm.ts:1-71에서 추출 -->

SchemaDefinition (from frontend/src/features/document/types/dynamicForm.ts):
```typescript
type FieldType = 'text' | 'textarea' | 'number' | 'date' | 'select' | 'table' | 'staticText' | 'hidden';

interface SchemaDefinition {
  version: number;
  fields: FieldDefinition[];
  conditionalRules?: ConditionalRule[];
  calculationRules?: CalculationRule[];
}
```

TemplateDetailItem (from frontend/src/features/admin/api/templateApi.ts):
```typescript
interface TemplateDetailItem {
  id: number;
  code: string;
  name: string;
  description: string | null;
  prefix: string;
  category: string | null;
  icon: string | null;
  schemaVersion: number;
  schemaDefinition: string | null; // JSON 문자열
  isActive: boolean;
  createdBy: ...; createdAt: string;
}
```

Target export payload (D-05):
```typescript
{
  exportFormatVersion: 1;
  schemaVersion: number;
  name: string;
  description?: string;
  prefix: string;
  category?: string;
  icon?: string;
  schemaDefinition: SchemaDefinition;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vitest 인프라 구축 + package.json 스크립트 추가 (Wave 0 per VALIDATION.md)</name>
  <files>
    frontend/package.json,
    frontend/vitest.config.ts,
    frontend/src/test/setup.ts
  </files>
  <read_first>
    frontend/package.json,
    frontend/vite.config.ts,
    .planning/phases/26-convenience-features/26-VALIDATION.md,
    .planning/phases/26-convenience-features/26-RESEARCH.md (§Validation Architecture)
  </read_first>
  <action>
    **Goal**: Vitest 4.1.4가 이미 devDeps에 있으나 실행 스크립트가 없음 → 추가 (per VALIDATION.md Wave 0 요건).

    1. `frontend/package.json`의 `scripts` 객체에 다음 두 스크립트 추가:
       - `"test": "vitest run"`
       - `"test:watch": "vitest"`
       (watch 모드는 scripts에만 존재, plan verify에는 절대 사용하지 않음)

    2. devDependencies에 다음이 없으면 추가 (`npm install -D` 사용):
       - `@testing-library/react`
       - `@testing-library/user-event`
       - `jsdom`
       (`@testing-library/jest-dom`은 이미 있음 — 확인만)

    3. `frontend/vitest.config.ts` 생성:
       ```typescript
       import { defineConfig } from 'vitest/config';
       import react from '@vitejs/plugin-react';
       import path from 'node:path';

       export default defineConfig({
         plugins: [react()],
         test: {
           environment: 'jsdom',
           globals: true,
           setupFiles: ['./src/test/setup.ts'],
           include: ['src/**/*.{test,spec}.{ts,tsx}'],
         },
         resolve: {
           alias: {
             '@': path.resolve(__dirname, './src'),
           },
         },
       });
       ```

    4. `frontend/src/test/setup.ts` 생성:
       ```typescript
       import '@testing-library/jest-dom/vitest';
       ```

    5. `tsconfig.json`에 `"types": ["vitest/globals", "@testing-library/jest-dom"]` 가 없으면 `compilerOptions.types`에 추가 (기존 types 배열 보존).

    **Constraints**: 기존 스크립트(`dev`, `build`, `lint`, `preview`) 절대 제거/수정 금지. `tsc -b` build가 계속 통과해야 함.
  </action>
  <verify>
    <automated>cd frontend && npm install && npm run test -- --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run test` 커맨드가 exit 0으로 종료 (테스트 0개여도 OK)
    - `frontend/vitest.config.ts` 파일 존재, `environment: 'jsdom'` 포함
    - `frontend/src/test/setup.ts` 존재, `@testing-library/jest-dom/vitest` import 포함
    - `grep -q '"test"' frontend/package.json` 통과
    - `cd frontend && npm run build` 계속 성공 (회귀 없음)
  </acceptance_criteria>
  <done>npm run test 명령이 동작하고, Vitest가 jsdom 환경에서 실행되며, 기존 build가 회귀 없이 통과</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: templateImportSchema + flattenZodErrors + 단위 테스트 (D-08, D-14, CNV-03)</name>
  <files>
    frontend/src/features/admin/validations/templateImportSchema.ts,
    frontend/src/features/admin/validations/templateImportSchema.test.ts
  </files>
  <read_first>
    frontend/src/features/document/types/dynamicForm.ts,
    .planning/phases/26-convenience-features/26-RESEARCH.md (§Pattern 1),
    .planning/phases/26-convenience-features/26-CONTEXT.md (D-05, D-08, D-09)
  </read_first>
  <behavior>
    - Test 1: 완전한 유효한 payload (D-05 포맷)가 safeParse 성공
    - Test 2: `exportFormatVersion !== 1` 거부, 경로 `exportFormatVersion`에 오류
    - Test 3: `name` 누락 거부, 경로 `name`에 오류
    - Test 4: `schemaDefinition.fields`의 field type이 enum 외 값이면 거부, 경로 `schemaDefinition.fields.0.type`
    - Test 5: `conditionalRules[].action` 이 enum(show/hide/require/unrequire) 외면 거부
    - Test 6: `calculationRules[].dependsOn`이 배열 아닐 때 거부
    - Test 7: prototype pollution 시도 — `__proto__: { polluted: true }` 포함 객체가 fieldConfig에서 거부되거나 stripped (strict 모드)
    - Test 8: `flattenZodErrors`가 모든 issue를 `{ path, message }[]`로 변환, path는 `.`으로 join
    - Test 9: 1MB 초과 시뮬레이션은 스키마 밖이므로 스킵 (ImportTemplateModal에서 검증)
  </behavior>
  <action>
    **Step 1 (RED)**: `templateImportSchema.test.ts` 먼저 작성. 위 9개 테스트 케이스를 `describe('templateImportSchema')` 블록에 작성. 각 테스트에서 `result.success === false` 시 `result.error.issues`의 첫 issue의 `path`를 assertion.

    **Step 2 (GREEN)**: `templateImportSchema.ts` 작성. RESEARCH.md §Pattern 1의 코드를 베이스로 하되 다음 보안 강화 적용 (RESEARCH §Security Domain):
    - `fieldConfigSchema`에 `.strict()` 적용 (unknown 키 거부) — `.passthrough()` 대신
    - `columnSchema.config`의 `z.any()`는 유지 (재귀 타입 완화 — 기존 코드 호환)

    정의해야 할 Zod 구조 (RESEARCH.md 참조, exhaustive):
    - `fieldTypeSchema`: enum 8종
    - `optionItemSchema`: { value: string, label: string, sortOrder?: number }
    - `columnSchema`: { id: string(min 1), type: fieldTypeSchema, label: string(min 1), required?: boolean, config?: any }
    - `fieldConfigSchema` **with .strict()**: placeholder/maxLength/min/max/unit/options/content/defaultValue/minRows/maxRows/columns 모두 optional
    - `fieldDefinitionSchema`: { id, type, label, required: boolean, config? }
    - `conditionalRuleSchema`: { targetFieldId, condition: { fieldId, operator, value: unknown }, action: enum }
    - `calculationRuleSchema`: { targetFieldId, formula: string(min 1), dependsOn: string[] }
    - `schemaDefinitionSchema`: { version: int positive, fields: [], conditionalRules?: [], calculationRules?: [] }
    - `templateImportSchema`: D-05 포맷 전체 + `exportFormatVersion: z.literal(1)`

    Export:
    - `export const templateImportSchema`
    - `export type TemplateImportData = z.infer<typeof templateImportSchema>`
    - `export function flattenZodErrors(error: ZodError): { path: string; message: string }[]` — `error.issues.map(i => ({ path: i.path.join('.') || '(root)', message: i.message }))`

    **Step 3 (REFACTOR)**: JSDoc 주석으로 `exportFormatVersion` / `schemaVersion` / `schemaDefinition.version` 세 version 구분 명시 (RESEARCH Pitfall 1).

    **Constraints**:
    - zod 4.x API 사용 (프로젝트는 `"zod": "^4.3.6"`). `z.enum([...])`, `z.object({}).strict()`, `z.literal()` 모두 4.x 호환.
    - 기존 `schemaToZod.ts` 절대 수정/재사용 금지 (D-08).
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/features/admin/validations/templateImportSchema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Vitest 9개 테스트 전부 pass
    - `templateImportSchema.ts`가 `export const templateImportSchema`, `export type TemplateImportData`, `export function flattenZodErrors` 세 개 export
    - `grep -n "\.strict()" frontend/src/features/admin/validations/templateImportSchema.ts` 가 fieldConfigSchema 라인 매치
    - `grep -n "z.literal(1)" frontend/src/features/admin/validations/templateImportSchema.ts` 매치
    - `cd frontend && npm run build` 통과 (타입 오류 없음)
  </acceptance_criteria>
  <done>templateImportSchema가 9개 테스트를 통과, flattenZodErrors 유틸 export, .strict() 적용으로 prototype pollution 차단</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: templateExport 유틸 + 단위 테스트 (D-05, D-06, D-07, CNV-02)</name>
  <files>
    frontend/src/features/admin/utils/templateExport.ts,
    frontend/src/features/admin/utils/templateExport.test.ts
  </files>
  <read_first>
    frontend/src/features/admin/api/templateApi.ts,
    .planning/phases/26-convenience-features/26-RESEARCH.md (§Pattern 3, §Pitfall 5),
    .planning/phases/26-convenience-features/26-CONTEXT.md (D-05, D-06, D-07)
  </read_first>
  <behavior>
    - Test 1: buildExportPayload가 `exportFormatVersion: 1` 포함
    - Test 2: 반환 객체에 `code`, `id`, `createdBy`, `createdAt`, `isActive` 키가 **없음** (D-05)
    - Test 3: `description`/`category`/`icon`이 빈 문자열이면 `undefined`로 normalize
    - Test 4: `schemaDefinition`이 null이면 `{ version: 1, fields: [] }`로 fallback (RESEARCH Pitfall — 하드코딩 3종 템플릿 대응)
    - Test 5: `schemaDefinition`이 JSON 문자열이면 parse됨
    - Test 6: buildExportFilename('CUSTOM_ab12cd', new Date('2026-04-13')) === 'CUSTOM_ab12cd-20260413.json'
    - Test 7: buildExportFilename('한글code!', date) === '_______-YYYYMMDD.json' 형식 (sanitize per Pitfall 5: `[^a-zA-Z0-9_-]` → `_`)
    - Test 8: buildExportFilename 월/일 zero-padding (1월 → '01')
  </behavior>
  <action>
    **Step 1 (RED)**: `templateExport.test.ts` 작성. 위 8개 테스트를 `describe('buildExportPayload')` / `describe('buildExportFilename')` 블록에 작성.

    Mock `TemplateDetailItem` 생성 헬퍼:
    ```typescript
    const makeDetail = (over: Partial<TemplateDetailItem> = {}): TemplateDetailItem => ({
      id: 42,
      code: 'CUSTOM_test123',
      name: '테스트',
      description: '설명',
      prefix: 'TST',
      category: 'general',
      icon: 'FileText',
      schemaVersion: 3,
      schemaDefinition: JSON.stringify({ version: 1, fields: [], conditionalRules: [], calculationRules: [] }),
      isActive: true,
      createdBy: { id: 1, name: 'admin' } as any,
      createdAt: '2026-01-01T00:00:00Z',
      ...over,
    });
    ```

    **Step 2 (GREEN)**: `templateExport.ts` 작성. RESEARCH §Pattern 3 코드 베이스에 Pitfall 5 sanitize 반영:

    ```typescript
    import type { TemplateDetailItem } from '../api/templateApi';

    export function buildExportPayload(detail: TemplateDetailItem) {
      return {
        exportFormatVersion: 1 as const,
        schemaVersion: detail.schemaVersion,
        name: detail.name,
        description: detail.description || undefined,
        prefix: detail.prefix,
        category: detail.category || undefined,
        icon: detail.icon || undefined,
        schemaDefinition: detail.schemaDefinition
          ? JSON.parse(detail.schemaDefinition)
          : { version: 1, fields: [] },
      };
    }

    export function buildExportFilename(code: string, date: Date = new Date()): string {
      const safe = code.replace(/[^a-zA-Z0-9_-]/g, '_');
      const yyyy = date.getFullYear();
      const mm = String(date.getMonth() + 1).padStart(2, '0');
      const dd = String(date.getDate()).padStart(2, '0');
      return `${safe}-${yyyy}${mm}${dd}.json`;
    }

    export function downloadTemplateJson(detail: TemplateDetailItem): void {
      const payload = buildExportPayload(detail);
      const blob = new Blob([JSON.stringify(payload, null, 2)], { type: 'application/json' });
      const url = URL.createObjectURL(blob);
      try {
        const a = document.createElement('a');
        a.href = url;
        a.download = buildExportFilename(detail.code);
        document.body.appendChild(a);
        a.click();
        document.body.removeChild(a);
      } finally {
        setTimeout(() => URL.revokeObjectURL(url), 0);
      }
    }
    ```

    **Constraints**: `downloadTemplateJson`은 jsdom에서 `URL.createObjectURL`이 mocking 필요할 수 있음 — 이 함수는 단위 테스트 제외 (순수 pure 함수 2개만 테스트).
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/features/admin/utils/templateExport.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Vitest 8개 테스트 전부 pass
    - `buildExportPayload`, `buildExportFilename`, `downloadTemplateJson` export
    - `grep -q "exportFormatVersion: 1 as const" frontend/src/features/admin/utils/templateExport.ts`
    - `grep -q "code.replace" frontend/src/features/admin/utils/templateExport.ts` (sanitize 존재)
    - 반환 객체에 `code`/`id`/`createdBy`/`createdAt`/`isActive` 키 **부재** 확인 테스트 pass
    - `cd frontend && npm run build` 통과
  </acceptance_criteria>
  <done>templateExport 세 함수 export, 8개 테스트 통과, 파일명 sanitize 적용</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: 4종 프리셋 JSON + eager glob registry + 검증 테스트 (D-12~D-15, CNV-04)</name>
  <files>
    frontend/src/features/admin/presets/expense.json,
    frontend/src/features/admin/presets/leave.json,
    frontend/src/features/admin/presets/trip.json,
    frontend/src/features/admin/presets/purchase.json,
    frontend/src/features/admin/presets/index.ts,
    frontend/src/features/admin/presets/presets.test.ts
  </files>
  <read_first>
    frontend/src/features/admin/validations/templateImportSchema.ts (Task 2 산출물),
    .planning/phases/26-convenience-features/26-CONTEXT.md (D-12~D-15),
    .planning/phases/26-convenience-features/26-RESEARCH.md (§Example 4),
    frontend/src/features/document/types/dynamicForm.ts (SchemaDefinition 타입 참조)
  </read_first>
  <behavior>
    - Test 1: `presets` 배열 길이 === 4
    - Test 2: 각 프리셋 key가 {expense, leave, trip, purchase} 중 하나
    - Test 3: 각 프리셋이 `templateImportSchema.parse()` 통과 (이미 index.ts에서 parse 하지만 test에서 재검증)
    - Test 4: `expense` 프리셋이 `calculationRules` 1개 이상 포함 (D-13: 계산 규칙 데모)
    - Test 5: `leave` 프리셋이 `conditionalRules` 1개 이상 포함 (D-13: 조건 규칙 데모)
    - Test 6: `purchase` 프리셋이 `type: 'table'` 필드를 1개 이상 포함 (D-13: 테이블 데모)
    - Test 7: 모든 프리셋의 `name` 이 한국어 (D-15) — 정규식 `/[가-힣]/` 매치
  </behavior>
  <action>
    **Step 1**: 4개 프리셋 JSON 파일 작성. 모두 D-05 포맷 (`templateImportSchema` 통과). 한국어 기본값 (D-15). 각 프리셋은 Phase 22~25 기능을 최소 1개 데모.

    **expense.json — 경비신청서 (계산 규칙 데모)**:
    ```json
    {
      "exportFormatVersion": 1,
      "schemaVersion": 1,
      "name": "경비신청서",
      "description": "항목/단가/수량/합계 테이블과 자동 합계 계산을 포함합니다",
      "prefix": "EXP",
      "category": "finance",
      "icon": "Receipt",
      "schemaDefinition": {
        "version": 1,
        "fields": [
          { "id": "title", "type": "text", "label": "제목", "required": true },
          { "id": "items", "type": "table", "label": "경비 항목", "required": true,
            "config": {
              "columns": [
                { "id": "name", "type": "text", "label": "항목명", "required": true },
                { "id": "unitPrice", "type": "number", "label": "단가", "required": true },
                { "id": "quantity", "type": "number", "label": "수량", "required": true },
                { "id": "amount", "type": "number", "label": "금액" }
              ],
              "minRows": 1,
              "maxRows": 20
            }
          },
          { "id": "total", "type": "number", "label": "총합계", "required": false, "config": { "unit": "원" } }
        ],
        "conditionalRules": [],
        "calculationRules": [
          { "targetFieldId": "total", "formula": "SUM(items.amount)", "dependsOn": ["items"] }
        ]
      }
    }
    ```

    **leave.json — 휴가신청서 (조건 규칙 데모)**:
    ```json
    {
      "exportFormatVersion": 1,
      "schemaVersion": 1,
      "name": "휴가신청서",
      "description": "휴가 종류 선택과 기간별 조건부 사유 입력을 포함합니다",
      "prefix": "LV",
      "category": "hr",
      "icon": "CalendarDays",
      "schemaDefinition": {
        "version": 1,
        "fields": [
          { "id": "leaveType", "type": "select", "label": "휴가 종류", "required": true,
            "config": { "options": [
              { "value": "annual", "label": "연차" },
              { "value": "half", "label": "반차" },
              { "value": "sick", "label": "병가" },
              { "value": "family", "label": "경조" }
            ] } },
          { "id": "startDate", "type": "date", "label": "시작일", "required": true },
          { "id": "endDate", "type": "date", "label": "종료일", "required": true },
          { "id": "sickReason", "type": "textarea", "label": "병가 사유", "required": false,
            "config": { "placeholder": "병가 관련 상세 사유를 입력하세요" } },
          { "id": "reason", "type": "textarea", "label": "사유", "required": true }
        ],
        "conditionalRules": [
          { "targetFieldId": "sickReason",
            "condition": { "fieldId": "leaveType", "operator": "equals", "value": "sick" },
            "action": "show" },
          { "targetFieldId": "sickReason",
            "condition": { "fieldId": "leaveType", "operator": "equals", "value": "sick" },
            "action": "require" }
        ],
        "calculationRules": []
      }
    }
    ```

    **trip.json — 출장신청서 (계산 + 테이블 데모)**:
    ```json
    {
      "exportFormatVersion": 1,
      "schemaVersion": 1,
      "name": "출장신청서",
      "description": "일정·동행자·예상 비용 계산을 포함합니다",
      "prefix": "TRP",
      "category": "general",
      "icon": "Plane",
      "schemaDefinition": {
        "version": 1,
        "fields": [
          { "id": "destination", "type": "text", "label": "출장지", "required": true },
          { "id": "startDate", "type": "date", "label": "출발일", "required": true },
          { "id": "endDate", "type": "date", "label": "복귀일", "required": true },
          { "id": "companions", "type": "textarea", "label": "동행자", "required": false },
          { "id": "costs", "type": "table", "label": "예상 비용", "required": true,
            "config": { "columns": [
              { "id": "category", "type": "text", "label": "항목", "required": true },
              { "id": "amount", "type": "number", "label": "금액", "required": true }
            ], "minRows": 1 } },
          { "id": "totalCost", "type": "number", "label": "총 예상 비용", "config": { "unit": "원" } },
          { "id": "purpose", "type": "textarea", "label": "출장 목적", "required": true }
        ],
        "conditionalRules": [],
        "calculationRules": [
          { "targetFieldId": "totalCost", "formula": "SUM(costs.amount)", "dependsOn": ["costs"] }
        ]
      }
    }
    ```

    **purchase.json — 구매신청서 (테이블 + 계산 데모)**:
    ```json
    {
      "exportFormatVersion": 1,
      "schemaVersion": 1,
      "name": "구매신청서",
      "description": "품목 테이블과 총액 자동 계산을 포함합니다",
      "prefix": "PUR",
      "category": "finance",
      "icon": "ShoppingCart",
      "schemaDefinition": {
        "version": 1,
        "fields": [
          { "id": "title", "type": "text", "label": "구매 제목", "required": true },
          { "id": "vendor", "type": "text", "label": "거래처", "required": true },
          { "id": "items", "type": "table", "label": "품목", "required": true,
            "config": { "columns": [
              { "id": "name", "type": "text", "label": "품명", "required": true },
              { "id": "spec", "type": "text", "label": "규격" },
              { "id": "quantity", "type": "number", "label": "수량", "required": true },
              { "id": "unitPrice", "type": "number", "label": "단가", "required": true },
              { "id": "amount", "type": "number", "label": "금액" }
            ], "minRows": 1 } },
          { "id": "total", "type": "number", "label": "총액", "config": { "unit": "원" } },
          { "id": "reason", "type": "textarea", "label": "구매 사유", "required": true }
        ],
        "conditionalRules": [],
        "calculationRules": [
          { "targetFieldId": "total", "formula": "SUM(items.amount)", "dependsOn": ["items"] }
        ]
      }
    }
    ```

    **Step 2**: `presets/index.ts` 작성 (RESEARCH §Example 4 참조):
    ```typescript
    import { templateImportSchema, type TemplateImportData } from '../validations/templateImportSchema';

    const modules = import.meta.glob<{ default: unknown }>('./*.json', { eager: true });

    export interface Preset {
      key: string;
      data: TemplateImportData;
    }

    export const presets: Preset[] = Object.entries(modules)
      .map(([path, mod]) => {
        const key = path.replace(/^\.\/(.*)\.json$/, '$1');
        const parsed = templateImportSchema.parse(mod.default);
        return { key, data: parsed };
      })
      .sort((a, b) => a.key.localeCompare(b.key));
    ```

    **Step 3 (TEST)**: `presets.test.ts`:
    ```typescript
    import { describe, it, expect } from 'vitest';
    import { presets } from './index';
    import { templateImportSchema } from '../validations/templateImportSchema';

    describe('presets', () => {
      it('contains exactly 4 presets', () => {
        expect(presets).toHaveLength(4);
      });
      it('has expected keys', () => {
        const keys = presets.map(p => p.key).sort();
        expect(keys).toEqual(['expense', 'leave', 'purchase', 'trip']);
      });
      it('each preset passes templateImportSchema', () => {
        for (const p of presets) {
          const result = templateImportSchema.safeParse(p.data);
          expect(result.success, `${p.key} failed: ${JSON.stringify(result.error?.issues)}`).toBe(true);
        }
      });
      it('expense preset has calculationRules', () => {
        const expense = presets.find(p => p.key === 'expense')!;
        expect(expense.data.schemaDefinition.calculationRules?.length ?? 0).toBeGreaterThanOrEqual(1);
      });
      it('leave preset has conditionalRules', () => {
        const leave = presets.find(p => p.key === 'leave')!;
        expect(leave.data.schemaDefinition.conditionalRules?.length ?? 0).toBeGreaterThanOrEqual(1);
      });
      it('purchase preset has at least one table field', () => {
        const purchase = presets.find(p => p.key === 'purchase')!;
        const hasTable = purchase.data.schemaDefinition.fields.some(f => f.type === 'table');
        expect(hasTable).toBe(true);
      });
      it('all preset names are Korean', () => {
        for (const p of presets) {
          expect(p.data.name).toMatch(/[가-힣]/);
        }
      });
    });
    ```

    **Constraints**:
    - tsconfig에 `resolveJsonModule: true` 확인. 없으면 추가.
    - 프리셋 JSON은 **반드시** `templateImportSchema`(Task 2 완료 후)를 통과해야 함. 통과 못하면 index.ts import 시 crash.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/features/admin/presets/presets.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 4개 JSON 파일 존재 (expense/leave/trip/purchase)
    - Vitest 7개 테스트 전부 pass
    - `presets` 배열 길이 4, 모두 `templateImportSchema.parse` 통과
    - `expense`/`trip`/`purchase`에 calculationRules 존재, `leave`에 conditionalRules 존재, `purchase`/`expense`/`trip`에 table 필드 존재
    - `cd frontend && npm run build` 통과 (JSON resolveJsonModule 해결됨)
  </acceptance_criteria>
  <done>4종 프리셋 번들, eager glob 레지스트리 동작, 7개 테스트 통과, Phase 22~25 기능 데모 포함</done>
</task>

<task type="auto">
  <name>Task 5: TemplateFormModal에 initialValues prop 확장 (D-21)</name>
  <files>
    frontend/src/features/admin/components/TemplateFormModal.tsx
  </files>
  <read_first>
    frontend/src/features/admin/components/TemplateFormModal.tsx (전체 481줄),
    .planning/phases/26-convenience-features/26-RESEARCH.md (§Pattern 2, §Pitfall 2),
    .planning/phases/26-convenience-features/26-CONTEXT.md (D-21, D-03, D-10)
  </read_first>
  <action>
    **Goal**: 생성 모드(`editingTemplate === null`)일 때 `initialValues` prop으로 폼/스키마 상태를 프리필할 수 있도록 확장. 편집 경로 회귀 0.

    **Step 1**: Props 인터페이스 확장 (RESEARCH §Pattern 2):
    ```typescript
    interface TemplateFormModalProps {
      open: boolean;
      onClose: () => void;
      editingTemplate: TemplateListItem | null;
      initialValues?: {
        name?: string;
        description?: string;
        prefix?: string;
        category?: string;
        icon?: string;
        schemaFields?: SchemaField[];
        conditionalRules?: ConditionalRule[];
        calculationRules?: CalculationRule[];
      } | null;
      onSuccess?: () => void;
    }
    ```

    **Step 2**: 생성 모드 초기화 useEffect 수정 (TemplateFormModal.tsx의 open/reset useEffect — 현재 대략 72-90행 범위, 실제 라인은 파일 read로 확인):
    - `if (open && !editingTemplate)` 분기에서 기존 `reset({ name: '', ... })` 대신 `initialValues`를 fallback과 함께 사용:
      ```typescript
      reset({
        name: initialValues?.name ?? '',
        description: initialValues?.description ?? '',
        prefix: initialValues?.prefix ?? '',  // D-03/D-10: 복제/Import 시 빈 값 보장
        category: initialValues?.category ?? '',
        icon: initialValues?.icon ?? '',
      });
      setSchemaFields(initialValues?.schemaFields ?? []);
      setConditionalRules(initialValues?.conditionalRules ?? []);
      setCalculationRules(initialValues?.calculationRules ?? []);
      ```
    - useEffect 의존성 배열에 `initialValues` 추가. 객체 identity 문제 방지를 위해 `JSON.stringify(initialValues)` 또는 개별 필드 분해. **권장**: 호출부에서 새 initialValues 객체를 만들 때마다 new reference를 넘기고, deps에 `initialValues` 포함.

    **Step 3**: 편집 모드 경로(`editingTemplate !== null`) 및 detailQuery useEffect는 **절대 수정 금지**. 편집 경로의 기존 로직과 schemaFields 로드 동작이 회귀 없어야 함 (RESEARCH §Pitfall 2).

    **Step 4**: Default prop: `initialValues = null` (TypeScript 기본값). Phase 26 외 호출자(현재 TemplateListPage)는 변경 없이 동작해야 함.

    **Constraints**:
    - `editingTemplate` prop의 기존 동작은 최우선. `editingTemplate !== null`이면 `initialValues`는 **무시**.
    - `onSubmit` 로직(138-172행)의 table/conditional/calculation 검증은 건드리지 말 것 — Import 시 교차 참조 검증을 이 기존 경로에서 수행 (RESEARCH §Pitfall 3).
    - 기존 i18n 키, aria 라벨, 버튼 텍스트 변경 없음.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run test -- --run src/features/admin 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "initialValues" frontend/src/features/admin/components/TemplateFormModal.tsx` 가 props 인터페이스 + useEffect 둘 다 매치
    - `cd frontend && npm run build` 통과 (TypeScript 에러 없음)
    - 편집 모드 경로(`editingTemplate !== null`) 코드 diff 최소 — props 추가 외 변경 없음을 git diff로 확인 가능
    - `prefix: initialValues?.prefix ?? ''` 라인 존재 (D-03/D-10 불변식)
    - 기존 TemplateFormModal 호출부(TemplateListPage)가 컴파일 오류 없이 통과 (optional prop이므로)
  </acceptance_criteria>
  <done>TemplateFormModal이 initialValues prop을 받아 생성 모드에서 프리필, 편집 경로 회귀 없음, build 통과</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 파일 시스템 → 브라우저 (Import) | 사용자가 선택한 JSON 파일은 신뢰할 수 없음. FileReader → JSON.parse → Zod 검증 필수 |
| 번들 → 런타임 (프리셋) | 프리셋 JSON은 개발자 작성이지만 빌드 타임에 변경 가능성 → 런타임 parse로도 검증 |
| 프론트 → 백엔드 (POST /admin/templates) | 기존 RBAC 경계. Phase 26은 새 엔드포인트 추가 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | templateImportSchema (fieldConfigSchema) | mitigate | `.strict()` 적용으로 unknown 키 거부 → `__proto__`/`constructor` 등 prototype pollution 차단 (Task 2) |
| T-26-02 | DoS | ImportTemplateModal (Plan 02) | mitigate | 파일 크기 1MB 가드 전 `file.size > 1_000_000` 체크 (Plan 02에서 수행, 여기 Plan 01은 스키마만 제공) |
| T-26-03 | Tampering | buildExportFilename | mitigate | `code.replace(/[^a-zA-Z0-9_-]/g, '_')` sanitize로 path traversal 방지 (Task 3) |
| T-26-04 | Tampering | 프리셋 JSON | mitigate | 빌드/런타임에 `templateImportSchema.parse()` 수행 — 잘못된 프리셋은 앱 크래시로 즉시 발견 (Task 4) |
| T-26-05 | Tampering | 교차 참조 무결성 (conditionalRules 필드 존재, calculationRules 순환) | mitigate | TemplateFormModal.onSubmit의 기존 검증 경로 재사용. Plan 01은 onSubmit을 수정하지 않으므로 기존 방어선 유지 (Task 5) |
| T-26-06 | Info Disclosure | Export payload | mitigate | `code`/`id`/`createdBy`/`createdAt`/`isActive` 제외 (D-05 — Task 3 Test 2로 자동 검증) |
| T-26-07 | Elevation | 새 admin 엔드포인트 | accept | 신규 엔드포인트 없음 — 기존 `@PreAuthorize` admin 가드 재사용 (D-18) |
</threat_model>

<verification>
- Vitest 전체 suite 통과: `cd frontend && npm run test`
- TypeScript 빌드 통과: `cd frontend && npm run build`
- 모든 CNV-01..CNV-04 요구사항에 대응하는 유닛 테스트 존재
- 기존 TemplateFormModal 편집 경로 회귀 없음 (수동 spot check)
</verification>

<success_criteria>
- [ ] `npm run test` 통과 (24개 이상 테스트: schema 9 + export 8 + presets 7)
- [ ] `npm run build` 통과
- [ ] templateImportSchema/templateExport/presets/index.ts 파일 존재
- [ ] TemplateFormModal에 `initialValues` optional prop 존재 (TypeScript)
- [ ] 4개 프리셋 JSON 모두 스키마 통과
- [ ] 기존 admin 템플릿 편집 흐름 회귀 없음
</success_criteria>

<output>
After completion, create `.planning/phases/26-convenience-features/26-01-SUMMARY.md`
</output>
