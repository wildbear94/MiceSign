---
phase: 26
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - frontend/src/features/admin/components/TemplateCreateChoiceModal.tsx
  - frontend/src/features/admin/components/PresetGallery.tsx
  - frontend/src/features/admin/components/ImportTemplateModal.tsx
  - frontend/src/features/admin/components/TemplateTable.tsx
  - frontend/src/features/admin/pages/TemplateListPage.tsx
  - frontend/src/locales/ko/admin.json
  - frontend/src/locales/en/admin.json
autonomous: false
requirements: [CNV-01, CNV-02, CNV-03, CNV-04]

must_haves:
  truths:
    - "관리자가 TemplateTable 각 행의 Copy 아이콘 클릭 시 TemplateFormModal이 프리필 상태로 열린다 (name에 '(복사본)' suffix, prefix 비어있음)"
    - "관리자가 Download 아이콘 클릭 시 {code}-YYYYMMDD.json 파일이 브라우저에 다운로드된다"
    - "관리자가 '양식 추가' 클릭 시 TemplateCreateChoiceModal이 3옵션(빈/프리셋/JSON)을 보여준다"
    - "프리셋 갤러리에서 카드 선택 시 TemplateFormModal이 해당 프리셋 데이터로 프리필되어 열린다 (prefix 비어있음)"
    - "Import 모달에서 유효한 JSON을 업로드하면 성공 chip이 나타나고 가져오기 버튼이 활성화된다"
    - "Import 모달에서 잘못된 JSON을 업로드하면 필드 경로별 에러 리스트가 나타난다 (red-50 컨테이너)"
    - "파일 크기 > 1MB 거부, .json 확장자 아닐 시 거부"
    - "4가지 진입점 모두 최종적으로 동일한 POST /admin/templates 저장 경로로 수렴한다"
    - "TemplateListPage의 기존 '양식 추가' 버튼과 '편집' 경로가 회귀 없이 동작한다"
  artifacts:
    - path: "frontend/src/features/admin/components/TemplateCreateChoiceModal.tsx"
      provides: "3옵션 선택 모달 (빈 양식/프리셋/JSON)"
      contains: "TemplateCreateChoiceModal"
    - path: "frontend/src/features/admin/components/PresetGallery.tsx"
      provides: "4개 프리셋 카드 2x2 그리드"
      contains: "PresetGallery"
    - path: "frontend/src/features/admin/components/ImportTemplateModal.tsx"
      provides: "파일 선택 + Zod 검증 + 에러 리스트"
      contains: "ImportTemplateModal"
    - path: "frontend/src/features/admin/components/TemplateTable.tsx"
      provides: "Copy + Download 아이콘 버튼 (Edit 앞)"
      contains: "onDuplicate"
    - path: "frontend/src/features/admin/pages/TemplateListPage.tsx"
      provides: "createFlow 상태 머신"
      contains: "createFlow"
  key_links:
    - from: "TemplateListPage"
      to: "TemplateFormModal (initialValues)"
      via: "createFlow.kind === 'form' 전이 시 prop 전달"
      pattern: "initialValues"
    - from: "TemplateTable Copy button"
      to: "TemplateListPage.handleDuplicate"
      via: "onDuplicate prop"
      pattern: "onDuplicate"
    - from: "ImportTemplateModal"
      to: "templateImportSchema"
      via: "safeParse + flattenZodErrors"
      pattern: "templateImportSchema\\.safeParse"
    - from: "PresetGallery"
      to: "presets (eager glob)"
      via: "presets 배열 iterate"
      pattern: "import.*presets"
---

<objective>
Phase 26 UI 통합 레이어 — Plan 01이 만든 유틸/스키마/프리셋/initialValues prop을 실제 사용자 인터페이스로 연결한다. TemplateCreateChoiceModal, PresetGallery, ImportTemplateModal 3개 신규 컴포넌트 + TemplateTable(Copy/Download 버튼) + TemplateListPage(createFlow 상태 머신) 수정 + i18n 키 추가. 최종적으로 수동 UAT 체크포인트.

Purpose: D-01/D-04/D-16/D-17 진입점 통일 + CNV-01~04 end-to-end 완성.
Output: 관리자가 4가지 경로(복제/Export/Import/프리셋)로 양식을 만들 수 있는 완전한 UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-convenience-features/26-CONTEXT.md
@.planning/phases/26-convenience-features/26-RESEARCH.md
@.planning/phases/26-convenience-features/26-UI-SPEC.md
@.planning/phases/26-convenience-features/26-01-PLAN.md
@frontend/src/features/admin/pages/TemplateListPage.tsx
@frontend/src/features/admin/components/TemplateTable.tsx
@frontend/src/features/admin/components/TemplateFormModal.tsx
@frontend/src/features/admin/api/templateApi.ts
@frontend/src/features/admin/validations/templateImportSchema.ts
@frontend/src/features/admin/utils/templateExport.ts
@frontend/src/features/admin/presets/index.ts
@frontend/src/locales/ko/admin.json
@frontend/src/locales/en/admin.json

<interfaces>
<!-- Plan 01 산출물 — 호출부에서 이 시그니처를 그대로 사용 -->

templateImportSchema:
```typescript
export const templateImportSchema: ZodType<...>;
export type TemplateImportData = z.infer<typeof templateImportSchema>;
export function flattenZodErrors(error: ZodError): { path: string; message: string }[];
```

templateExport:
```typescript
export function downloadTemplateJson(detail: TemplateDetailItem): void;
```

presets:
```typescript
export interface Preset { key: string; data: TemplateImportData; }
export const presets: Preset[];
```

TemplateFormModal (extended):
```typescript
interface TemplateFormModalProps {
  open: boolean;
  onClose: () => void;
  editingTemplate: TemplateListItem | null;
  initialValues?: { name?; description?; prefix?; category?; icon?; schemaFields?; conditionalRules?; calculationRules? } | null;
  onSuccess?: () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n 키 추가 + TemplateCreateChoiceModal 컴포넌트 (D-16)</name>
  <files>
    frontend/src/locales/ko/admin.json,
    frontend/src/locales/en/admin.json,
    frontend/src/features/admin/components/TemplateCreateChoiceModal.tsx
  </files>
  <read_first>
    frontend/src/locales/ko/admin.json,
    frontend/src/locales/en/admin.json,
    .planning/phases/26-convenience-features/26-UI-SPEC.md (§Copywriting Contract, §TemplateCreateChoiceModal Layout),
    frontend/src/features/admin/components/TemplateFormModal.tsx (모달 패턴 참조)
  </read_first>
  <action>
    **Step 1 — i18n 키 추가**: `ko/admin.json`과 `en/admin.json`에 UI-SPEC §Copywriting Contract 표에 정의된 모든 키를 `templates.*` 네임스페이스에 추가. 기존 키 절대 제거/수정 금지.

    **한국어 값 (ko/admin.json)** — UI-SPEC 그대로:
    ```
    templates.duplicate = "양식 복제"
    templates.exportJson = "JSON 내보내기"
    templates.createChoiceTitle = "양식 추가 방식 선택"
    templates.createBlankTitle = "빈 양식으로 시작"
    templates.createBlankDesc = "처음부터 필드를 직접 구성합니다"
    templates.createPresetTitle = "프리셋에서 시작"
    templates.createPresetDesc = "미리 만들어진 양식에서 고릅니다"
    templates.createImportTitle = "JSON 파일에서 가져오기"
    templates.createImportDesc = "내보낸 양식 파일을 불러옵니다"
    templates.presetGalleryTitle = "프리셋 선택"
    templates.importTitle = "JSON 파일 가져오기"
    templates.importDropzone = "파일을 선택하거나 여기에 놓으세요"
    templates.importDropzoneHint = ".json 파일만 허용합니다"
    templates.importValidationOk = "검증 완료 — 계속 진행하세요"
    templates.importValidationFailTitle = "JSON 검증 실패"
    templates.importValidationFailHelp = "아래 항목을 수정한 뒤 다시 시도하세요"
    templates.importErrorOverflow = "이 외 {{count}}개 오류가 더 있습니다"
    templates.importErrorNotJson = "선택한 파일이 유효한 JSON 형식이 아닙니다"
    templates.importErrorFormatVersion = "지원하지 않는 내보내기 형식 버전입니다"
    templates.importErrorFileTooLarge = "파일 크기가 1MB를 초과합니다"
    templates.importSubmit = "가져오기"
    templates.duplicateSuffix = " (복사본)"
    templates.duplicateReady = "복제본이 준비되었습니다. 접두사를 입력하고 저장하세요"
    templates.exportSuccess = "{{name}} 내보내기 완료"
    templates.exportFailure = "내보내기에 실패했습니다"
    templates.prefixRequiredHint = "새 접두사를 입력하세요"
    ```

    영어 값은 UI-SPEC 미지정 → Claude 재량. 직역 + 자연스러운 표현 사용 (예: "Duplicate template", "Export JSON", "Choose how to add template", ...).

    `common.select` / `common.cancel` 키가 이미 있는지 grep으로 확인. 없으면 ko: "선택"/"취소", en: "Select"/"Cancel" 추가.

    **Step 2 — TemplateCreateChoiceModal 컴포넌트**: UI-SPEC §TemplateCreateChoiceModal Layout 그대로 구현.

    Props (UI-SPEC 명시):
    ```typescript
    interface TemplateCreateChoiceModalProps {
      open: boolean;
      onClose: () => void;
      onSelectBlank: () => void;
      onSelectPreset: () => void;
      onSelectImport: () => void;
    }
    ```

    레이아웃:
    - 모달 컨테이너: `max-w-md rounded-xl bg-white dark:bg-gray-800` + backdrop overlay (기존 모달 패턴 재사용)
    - 헤더: `px-6 py-4 border-b border-gray-200 dark:border-gray-700` 제목 + X 버튼
    - 본문: `p-6 space-y-3` (UI-SPEC는 3개 카드를 세로 스택으로 지정)
    - 각 카드: `w-full p-4 rounded-lg border border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-800 hover:border-blue-600 hover:ring-2 hover:ring-blue-600/30 dark:hover:ring-blue-400/30 transition-all text-left flex items-center gap-4`
    - 아이콘 chip: `w-10 h-10 rounded-lg bg-gray-100 dark:bg-gray-700 flex items-center justify-center flex-shrink-0`
      - 카드 1: `FileText` (w-5 h-5)
      - 카드 2: `LayoutTemplate` (w-5 h-5)
      - 카드 3: `Upload` (w-5 h-5)
    - 타이틀/설명: `flex-1`
      - 타이틀: `text-sm font-semibold text-gray-900 dark:text-gray-50`
      - 설명: `text-xs text-gray-500 dark:text-gray-400 mt-0.5`
    - 우측 chevron: `ChevronRight w-4 h-4 text-gray-400`

    인터랙션:
    - Escape → onClose
    - Tab → 포커스 트랩 (기존 모달 패턴 재사용)
    - 카드는 `<button type="button">`, Enter/Space 자동 지원
    - 각 카드 aria-label: UI-SPEC §Keyboard & Accessibility 참조

    복사 (translation keys): `t('templates.createChoiceTitle')`, `t('templates.createBlankTitle')`, `t('templates.createBlankDesc')`, `t('templates.createPresetTitle')`, `t('templates.createPresetDesc')`, `t('templates.createImportTitle')`, `t('templates.createImportDesc')` — 모두 `admin` 네임스페이스.

    다크모드: 모든 Tailwind 클래스에 `dark:` variant (UI-SPEC §Dark Mode).

    **Constraints**:
    - D-16 고정: 옵션 3개 (빈/프리셋/JSON), 다른 옵션 추가 금지
    - 새 npm 패키지 도입 금지
    - 기존 `ConfirmDialog` 패턴과 스타일 일관성 유지
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20 && grep -q "createChoiceTitle" src/locales/ko/admin.json && echo "i18n OK"</automated>
  </verify>
  <acceptance_criteria>
    - `ko/admin.json`과 `en/admin.json`에 UI-SPEC §Copywriting의 모든 `templates.*` 키 존재
    - `grep -q "createChoiceTitle" frontend/src/locales/ko/admin.json` 통과
    - `TemplateCreateChoiceModal.tsx` 컴포넌트가 `open`/`onClose`/`onSelectBlank`/`onSelectPreset`/`onSelectImport` prop 수용
    - 3개 카드가 FileText/LayoutTemplate/Upload 아이콘 순서로 렌더
    - 다크모드 `dark:` variant 클래스 존재
    - `cd frontend && npm run build` 통과
  </acceptance_criteria>
  <done>i18n 키가 양 로케일에 추가되고, TemplateCreateChoiceModal이 UI-SPEC 사양대로 구현되어 build 통과</done>
</task>

<task type="auto">
  <name>Task 2: PresetGallery + ImportTemplateModal 컴포넌트 (D-09, D-12~D-17, CNV-03, CNV-04)</name>
  <files>
    frontend/src/features/admin/components/PresetGallery.tsx,
    frontend/src/features/admin/components/ImportTemplateModal.tsx
  </files>
  <read_first>
    frontend/src/features/admin/presets/index.ts,
    frontend/src/features/admin/validations/templateImportSchema.ts,
    .planning/phases/26-convenience-features/26-UI-SPEC.md (§PresetGallery Layout, §ImportTemplateModal Layout, §Validation Contract),
    .planning/phases/26-convenience-features/26-CONTEXT.md (D-09, D-10)
  </read_first>
  <action>
    **Component A — PresetGallery.tsx** (UI-SPEC §PresetGallery):

    Props:
    ```typescript
    interface PresetGalleryProps {
      open: boolean;
      onClose: () => void;
      onSelect: (preset: TemplateImportData) => void; // 선택 시 parent가 TemplateFormModal 열기
    }
    ```

    데이터 소스: `import { presets } from '../presets'` (Plan 01 Task 4 산출물).

    레이아웃:
    - 모달: `max-w-3xl max-h-[80vh] rounded-xl bg-white dark:bg-gray-800`
    - 헤더: `px-6 py-4 border-b` 제목 `t('templates.presetGalleryTitle')` + X
    - 본문: `p-6 bg-gray-50 dark:bg-gray-900 overflow-y-auto max-h-[70vh]`
    - 그리드: `grid grid-cols-2 gap-4`
    - 각 preset 카드: `p-4 bg-white dark:bg-gray-800 rounded-lg border border-gray-200 dark:border-gray-700 shadow-sm hover:border-blue-600 hover:shadow-md transition-all text-left`
      - 상단 아이콘 chip: `w-10 h-10 rounded-lg bg-gray-100 dark:bg-gray-700 flex items-center justify-center mb-3`
        - 아이콘 매핑 (preset.data.icon 문자열 → lucide): Receipt(expense), CalendarDays(leave), Plane(trip), ShoppingCart(purchase). 매핑 테이블을 컴포넌트 내부에 선언.
      - 이름: `text-sm font-semibold text-gray-900 dark:text-gray-50`
      - 설명: `text-xs text-gray-500 dark:text-gray-400 mt-1 line-clamp-2`
      - 하단 "선택" 버튼: `mt-3 text-xs px-3 py-1.5 bg-blue-600 hover:bg-blue-700 text-white rounded self-end`
    - 카드 전체 클릭 시 `onSelect(preset.data)` 호출 (버튼 중첩 피하기 위해 outer `<button>` + 내부 "선택" 라벨만 텍스트로).
    - 키보드: Enter/Space 자동 지원, aria-label: `${preset.data.name} 선택`
    - Escape → onClose (이전 단계 choice modal로 복귀는 parent가 관리)

    **Component B — ImportTemplateModal.tsx** (UI-SPEC §ImportTemplateModal, §Validation Contract):

    Props:
    ```typescript
    interface ImportTemplateModalProps {
      open: boolean;
      onClose: () => void;
      onValid: (parsed: TemplateImportData) => void;
    }
    ```

    상태:
    ```typescript
    const [file, setFile] = useState<File | null>(null);
    const [errors, setErrors] = useState<{ path: string; message: string }[] | null>(null);
    const [validated, setValidated] = useState<TemplateImportData | null>(null);
    ```

    파일 선택 핸들러 (D-09, Validation Contract):
    1. `.json` 확장자 아닌 경우 → `setErrors([{ path: '(root)', message: t('templates.importErrorNotJson') }])`; `setValidated(null)`
    2. `file.size > 1_000_000` → `setErrors([{ path: '(root)', message: t('templates.importErrorFileTooLarge') }])` (Threat T-26-02 방어)
    3. `await file.text()` → `JSON.parse` — catch 시 `importErrorNotJson`
    4. `templateImportSchema.safeParse(json)` — 실패 시 `setErrors(flattenZodErrors(result.error))`; `setValidated(null)`
    5. 성공 시 `setValidated(result.data)`; `setErrors(null)`

    레이아웃 (UI-SPEC §ImportTemplateModal Layout):
    - 모달: `max-w-lg`
    - 헤더: 제목 `t('templates.importTitle')` + X
    - 본문: `p-6 space-y-4`
    - Dropzone: `<label>`로 감싼 `<input type="file" accept=".json" className="sr-only">` + 시각 영역
      - 기본: `border-2 border-dashed border-gray-300 dark:border-gray-600 bg-gray-50 dark:bg-gray-900 rounded-lg p-8 text-center cursor-pointer`
      - Hover: `hover:border-blue-600 hover:bg-blue-50/40 dark:hover:bg-blue-900/10`
      - 내용: `Upload` 아이콘 w-8 h-8, `templates.importDropzone`, `templates.importDropzoneHint`
    - 선택된 파일 row: `FileJson` 아이콘 + filename + X 제거 버튼 → `file=null, errors=null, validated=null`로 리셋
    - 에러 컨테이너 (errors !== null):
      ```
      <div role="alert" className="rounded-lg bg-red-50 dark:bg-red-900/20 border border-red-200 dark:border-red-900/40 p-4">
        <div className="flex items-center gap-2 text-red-700 dark:text-red-300 font-semibold mb-2">
          <AlertCircle className="w-4 h-4" />
          {t('templates.importValidationFailTitle')}
        </div>
        <p className="text-xs text-red-600 dark:text-red-400 mb-2">{t('templates.importValidationFailHelp')}</p>
        <ul className="max-h-48 overflow-y-auto space-y-1">
          {errors.slice(0, 50).map((e, idx) => (
            <li key={`${e.path}-${idx}`} className="text-xs">
              <code className="font-mono font-medium text-red-700 dark:text-red-300">{e.path}</code>
              <span className="text-red-600 dark:text-red-400">: {e.message}</span>
            </li>
          ))}
          {errors.length > 50 && (
            <li className="text-xs text-red-500">{t('templates.importErrorOverflow', { count: errors.length - 50 })}</li>
          )}
        </ul>
      </div>
      ```
    - 성공 chip (validated !== null):
      `<div className="flex items-center gap-2 text-emerald-600 dark:text-emerald-400 bg-emerald-50 dark:bg-emerald-900/20 px-3 py-2 rounded"><CheckCircle2 className="w-4 h-4" />{t('templates.importValidationOk')}</div>`
    - 푸터: `px-6 py-4 border-t flex justify-end gap-2`
      - 취소: neutral secondary 버튼 → onClose
      - 가져오기: `bg-blue-600 hover:bg-blue-700 text-white disabled:opacity-50 disabled:cursor-not-allowed` — `disabled={validated === null}`, onClick → `onValid(validated!)`

    **Constraints**:
    - `.strict()` Zod (Plan 01 Task 2) 덕분에 unknown 키는 자동 거부됨 — 여기서는 safeParse 결과만 소비
    - 파일 내용을 DOM에 `innerHTML`로 넣지 말 것 (XSS 방지 — Threat 기존 React 이스케이프에 의존)
    - sonner toast는 **사용 안 함** (D-09: 인라인 에러 리스트 우선)
    - Escape → onClose, Tab 포커스 트랩 (기존 모달 패턴)
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `PresetGallery.tsx`가 2x2 그리드로 4개 카드 렌더 (`grid grid-cols-2 gap-4`)
    - `ImportTemplateModal.tsx`가 파일 선택 → Zod 검증 → 에러 리스트 또는 성공 chip 분기
    - `grep -q "templateImportSchema.safeParse" frontend/src/features/admin/components/ImportTemplateModal.tsx`
    - `grep -q "file.size > 1_000_000" frontend/src/features/admin/components/ImportTemplateModal.tsx` (DoS 방어)
    - `grep -q "flattenZodErrors" frontend/src/features/admin/components/ImportTemplateModal.tsx`
    - 에러 컨테이너에 `role="alert"` 존재
    - 가져오기 버튼 `disabled={validated === null}` 패턴 존재
    - `role="button"` 또는 `<button>` 사용 (PresetGallery 키보드 접근성)
    - `cd frontend && npm run build` 통과
  </acceptance_criteria>
  <done>PresetGallery와 ImportTemplateModal이 UI-SPEC 사양대로 구현, Zod 검증 파이프라인 end-to-end 동작, build 통과</done>
</task>

<task type="auto">
  <name>Task 3: TemplateTable Copy/Download 버튼 + TemplateListPage 상태 머신 (D-01, D-02, D-03, D-07, D-17)</name>
  <files>
    frontend/src/features/admin/components/TemplateTable.tsx,
    frontend/src/features/admin/pages/TemplateListPage.tsx
  </files>
  <read_first>
    frontend/src/features/admin/components/TemplateTable.tsx (전체),
    frontend/src/features/admin/pages/TemplateListPage.tsx (전체),
    frontend/src/features/admin/api/templateApi.ts (getDetail 시그니처),
    frontend/src/features/admin/utils/templateExport.ts (downloadTemplateJson),
    frontend/src/features/admin/presets/index.ts,
    .planning/phases/26-convenience-features/26-UI-SPEC.md (§TemplateTable row action cell, §State Machine, §Export Flow),
    .planning/phases/26-convenience-features/26-CONTEXT.md (D-01~D-04, D-17)
  </read_first>
  <action>
    **Part A — TemplateTable.tsx** (UI-SPEC §TemplateTable row action cell):

    Props 확장:
    ```typescript
    interface TemplateTableProps {
      templates: TemplateListItem[];
      isLoading: boolean;
      onEdit?: (template: TemplateListItem) => void;
      onToggleActive?: ...; // 기존 유지
      onDuplicate?: (template: TemplateListItem) => void; // [NEW]
      onExport?: (template: TemplateListItem) => void;    // [NEW]
    }
    ```

    행 액션 셀 수정 (기존 Edit 버튼 위치):
    - 기존 `<div>` 컨테이너 (Edit 버튼 포함)를 `<div className="flex items-center gap-1">`로 감싸기 (또는 이미 그렇다면 유지)
    - 순서 **고정**: Copy → Download → Edit (UI-SPEC: "Order is fixed: Copy → Download → Edit")
    - Copy 버튼:
      ```tsx
      <button
        type="button"
        onClick={() => onDuplicate?.(template)}
        className="text-gray-400 hover:text-blue-600 hover:bg-gray-50 dark:hover:bg-gray-700 p-1 rounded transition-colors"
        aria-label={`${template.name} ${t('templates.duplicate')}`}
        title={t('templates.duplicate')}
      >
        <Copy className="w-4 h-4" />
      </button>
      ```
    - Download 버튼: 동일 패턴, `Download` 아이콘, aria-label `${template.name} ${t('templates.exportJson')}`, `onExport` 호출
    - Edit 버튼: 기존 그대로 유지 (Pencil 아이콘, 위치만 가장 오른쪽으로 확인)

    import 추가: `Copy, Download` from `lucide-react` (기존 import 라인에 병합).

    **제약**: 기존 Edit/Toggle 버튼의 로직, onClick, 클래스 절대 변경 금지 (CLAUDE.md: 기존 기능 보존).

    **Part B — TemplateListPage.tsx** (UI-SPEC §State Machine):

    새 상태:
    ```typescript
    import type { TemplateImportData } from '../validations/templateImportSchema';

    type InitialValues = NonNullable<React.ComponentProps<typeof TemplateFormModal>['initialValues']>;

    type CreateFlow =
      | { kind: 'closed' }
      | { kind: 'choice' }
      | { kind: 'preset' }
      | { kind: 'import' }
      | { kind: 'form'; initialValues: InitialValues | null };

    const [createFlow, setCreateFlow] = useState<CreateFlow>({ kind: 'closed' });
    ```

    기존 `showCreateModal` state (`useState(false)` 등)는 **제거**하고, 기존 로직을 `createFlow` 상태 머신으로 흡수. 편집용 `editingTemplate` state는 **유지**.

    "양식 추가" 버튼 onClick: `() => setCreateFlow({ kind: 'choice' })`

    핸들러들:
    ```typescript
    const handleSelectBlank = () => setCreateFlow({ kind: 'form', initialValues: null });
    const handleSelectPreset = () => setCreateFlow({ kind: 'preset' });
    const handleSelectImport = () => setCreateFlow({ kind: 'import' });

    const handlePresetSelected = (preset: TemplateImportData) => {
      setCreateFlow({
        kind: 'form',
        initialValues: {
          name: preset.name,
          description: preset.description,
          prefix: '', // D-10: 사용자 입력 필수
          category: preset.category,
          icon: preset.icon,
          schemaFields: preset.schemaDefinition.fields,
          conditionalRules: preset.schemaDefinition.conditionalRules ?? [],
          calculationRules: preset.schemaDefinition.calculationRules ?? [],
        },
      });
    };

    const handleImportValid = (parsed: TemplateImportData) => {
      setCreateFlow({
        kind: 'form',
        initialValues: {
          name: parsed.name,
          description: parsed.description,
          prefix: '', // D-10
          category: parsed.category,
          icon: parsed.icon,
          schemaFields: parsed.schemaDefinition.fields,
          conditionalRules: parsed.schemaDefinition.conditionalRules ?? [],
          calculationRules: parsed.schemaDefinition.calculationRules ?? [],
        },
      });
    };

    const handleDuplicate = async (tpl: TemplateListItem) => {
      try {
        const response = await templateApi.getDetail(tpl.id);
        const detail = response.data.data!;
        const schema = detail.schemaDefinition ? JSON.parse(detail.schemaDefinition) : { fields: [], conditionalRules: [], calculationRules: [] };
        setCreateFlow({
          kind: 'form',
          initialValues: {
            name: `${detail.name}${t('templates.duplicateSuffix')}`,
            description: detail.description ?? '',
            prefix: '', // D-03
            category: detail.category ?? '',
            icon: detail.icon ?? '',
            schemaFields: schema.fields ?? [],
            conditionalRules: schema.conditionalRules ?? [],
            calculationRules: schema.calculationRules ?? [],
          },
        });
        toast.success(t('templates.duplicateReady'));
      } catch (err) {
        toast.error(t('templates.exportFailure')); // 재사용 메시지 또는 새 키 `templates.duplicateFailure`
      }
    };

    const handleExport = async (tpl: TemplateListItem) => {
      try {
        const response = await templateApi.getDetail(tpl.id);
        const detail = response.data.data!;
        downloadTemplateJson(detail);
        toast.success(t('templates.exportSuccess', { name: detail.name }));
      } catch (err) {
        toast.error(t('templates.exportFailure'));
      }
    };

    const handleCloseCreateFlow = () => setCreateFlow({ kind: 'closed' });
    ```

    JSX 렌더:
    ```tsx
    <TemplateTable
      templates={...}
      isLoading={...}
      onEdit={handleEdit}
      onDuplicate={handleDuplicate}
      onExport={handleExport}
      onToggleActive={...}
    />

    <TemplateCreateChoiceModal
      open={createFlow.kind === 'choice'}
      onClose={handleCloseCreateFlow}
      onSelectBlank={handleSelectBlank}
      onSelectPreset={handleSelectPreset}
      onSelectImport={handleSelectImport}
    />

    <PresetGallery
      open={createFlow.kind === 'preset'}
      onClose={() => setCreateFlow({ kind: 'choice' })} // Esc → 이전 단계 복귀 per UI-SPEC Keyboard
      onSelect={handlePresetSelected}
    />

    <ImportTemplateModal
      open={createFlow.kind === 'import'}
      onClose={() => setCreateFlow({ kind: 'choice' })}
      onValid={handleImportValid}
    />

    <TemplateFormModal
      open={createFlow.kind === 'form' || editingTemplate !== null}
      onClose={() => {
        handleCloseCreateFlow();
        setEditingTemplate(null);
      }}
      editingTemplate={editingTemplate}
      initialValues={createFlow.kind === 'form' ? createFlow.initialValues : null}
      onSuccess={() => {
        handleCloseCreateFlow();
        setEditingTemplate(null);
        // 기존 refetch 로직 유지
      }}
    />
    ```

    **Constraints**:
    - 기존 "양식 추가" 버튼, 기존 편집 핸들러, 기존 toggle 핸들러 **제거 금지**. onClick만 교체.
    - 기존 `useTemplates`/`useCreateTemplate` 훅 재사용 (새 훅 금지)
    - `templates.duplicateFailure` 키가 필요하면 Task 1에서 추가하지 않았으므로 `templates.exportFailure` 재사용 혹은 Task 1에 되돌아가 추가 (권장: Task 1에서 `templates.duplicateFailure = "복제에 실패했습니다"` 추가 — 이 task 진행 중 발견 시 i18n 파일에 직접 추가 OK)
    - Escape 동작: preset/import 모달의 onClose는 **choice 단계로 복귀**해야 함 (UI-SPEC §Keyboard: "returns to previous state")
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `TemplateTable.tsx`에 Copy, Download 버튼 존재 (`grep -q "Copy" && grep -q "Download"`)
    - 버튼 순서: Copy → Download → Edit (행 액션 셀 내)
    - 기존 Edit/Pencil 버튼 및 toggle 버튼 로직 변경 없음 (git diff로 확인)
    - `TemplateListPage.tsx`의 `createFlow` 상태 머신 5개 kind 모두 존재
    - `grep -q "createFlow" frontend/src/features/admin/pages/TemplateListPage.tsx`
    - 4개 신규 모달(TemplateCreateChoiceModal/PresetGallery/ImportTemplateModal/TemplateFormModal)이 JSX에 렌더
    - `handleDuplicate`, `handleExport`, `handlePresetSelected`, `handleImportValid` 4개 핸들러 존재
    - 복제/Import/프리셋 모두 `prefix: ''` (D-03, D-10) 라인 존재
    - `cd frontend && npm run build` 통과 (타입 에러 없음)
    - 기존 TemplateListPage 편집 경로(`editingTemplate !== null`) 회귀 없음
  </acceptance_criteria>
  <done>TemplateTable에 Copy/Download 버튼 추가되고, TemplateListPage가 createFlow 상태 머신으로 4개 진입점을 단일 TemplateFormModal로 수렴시키며, build 통과</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 수동 UAT — Phase 26 end-to-end 검증 (CNV-01~CNV-04)</name>
  <what-built>
    - TemplateTable 각 행에 Copy + Download 아이콘 버튼 (Edit 앞)
    - "양식 추가" 클릭 시 TemplateCreateChoiceModal (3옵션)
    - 프리셋 갤러리 2x2 카드 (경비/휴가/출장/구매)
    - ImportTemplateModal (파일 선택 + Zod 검증 + 에러 리스트)
    - 4개 경로 모두 TemplateFormModal로 수렴 (prefix 비어있음)
  </what-built>
  <how-to-verify>
    실행: `cd frontend && npm run dev` → 관리자 계정 로그인 → `/admin/templates` 접속

    **체크리스트 (모두 PASS해야 Phase 완료):**

    1. **CNV-01 복제 플로우**
       - [ ] 양식 목록 각 행에 Copy 아이콘이 Edit 아이콘 왼쪽에 보인다
       - [ ] Copy 아이콘 hover 시 `text-blue-600` 전환
       - [ ] 기존 양식의 Copy 클릭 → TemplateFormModal이 열리며 name에 " (복사본)" suffix 적용됨
       - [ ] prefix 필드가 **비어 있음** (반드시)
       - [ ] description/category/icon/필드/규칙이 원본과 동일하게 프리필됨
       - [ ] prefix 입력 후 저장 → 새 템플릿 생성 성공
       - [ ] toast: "복제본이 준비되었습니다..."

    2. **CNV-02 Export 플로우**
       - [ ] Download 아이콘 클릭 → `{code}-20260413.json` 파일 다운로드됨 (오늘 날짜)
       - [ ] 다운로드된 JSON 파일 열기 → `exportFormatVersion: 1` 포함
       - [ ] `code`, `id`, `createdBy`, `createdAt`, `isActive` 키가 **없음**
       - [ ] `schemaDefinition` 객체가 중첩으로 존재 (문자열이 아님)
       - [ ] toast: "{name} 내보내기 완료"

    3. **CNV-03 Import 플로우**
       - [ ] "양식 추가" 클릭 → TemplateCreateChoiceModal 3옵션 표시
       - [ ] "JSON 파일에서 가져오기" 선택 → ImportTemplateModal 열림
       - [ ] 방금 export한 JSON 파일 선택 → 초록색 "검증 완료" chip 표시
       - [ ] "가져오기" 버튼 활성화 (파랑) → 클릭 시 TemplateFormModal 열림, prefix 비어있음
       - [ ] 의도적으로 잘못된 JSON (예: `{"foo": "bar"}`) 업로드 → 빨간 에러 리스트 표시
       - [ ] 에러 리스트에 `exportFormatVersion`, `name`, `prefix`, `schemaDefinition` 경로별 에러
       - [ ] "가져오기" 버튼 비활성 (회색)
       - [ ] 1MB 초과 파일 업로드 → "파일 크기가 1MB를 초과합니다" 에러
       - [ ] `.txt` 파일 선택 시도 → "유효한 JSON 형식이 아닙니다" 에러

    4. **CNV-04 프리셋 플로우**
       - [ ] "양식 추가" → "프리셋에서 시작" 선택 → PresetGallery 열림
       - [ ] 2x2 그리드로 4개 카드 (경비신청서/휴가신청서/출장신청서/구매신청서)
       - [ ] 각 카드에 아이콘(Receipt/CalendarDays/Plane/ShoppingCart), 이름, 설명, "선택" 버튼
       - [ ] "경비신청서" 카드 선택 → TemplateFormModal 열리며 필드 프리필됨
       - [ ] 프리필된 필드: title/items(table)/total(number) 확인
       - [ ] 계산 탭에서 total = SUM(items.amount) 규칙 확인
       - [ ] prefix 입력 후 저장 → 새 양식 생성 성공
       - [ ] "휴가신청서" 프리셋도 동일하게 테스트, 조건 규칙 sickReason 확인
       - [ ] "구매신청서" 테이블 필드 items 확인

    5. **기존 기능 회귀**
       - [ ] 기존 "양식 추가" 버튼이 여전히 존재하고 동작 (선택 모달 경유)
       - [ ] 기존 편집(Edit) 경로 정상 (편집 시 initialValues 무시되고 DB detail 로드)
       - [ ] 기존 toggle 활성/비활성 버튼 정상
       - [ ] 기존 템플릿 목록 페이지네이션/필터 정상

    6. **키보드/다크모드**
       - [ ] 모든 모달에서 Escape 키 동작 (import/preset 모달은 choice 모달로 복귀)
       - [ ] Tab 포커스 트랩
       - [ ] 다크모드 토글 시 모든 신규 컴포넌트가 다크 배경/텍스트 적용

    7. **i18n**
       - [ ] 한국어 모드에서 UI-SPEC §Copywriting의 모든 한국어 텍스트 정확히 표시
       - [ ] 영어 모드(있다면) 크래시 없이 표시

    **실패 시**: 어떤 항목이 실패했는지 명시하여 이슈 보고. 복구 후 재검증.
  </how-to-verify>
  <resume-signal>"approved" 입력 시 Phase 26 완료. 이슈 발견 시 상세 기술.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 파일 → 브라우저 | ImportTemplateModal에서 사용자가 선택한 `.json` 파일은 untrusted. 1MB 가드 + Zod strict 검증 필수 |
| 브라우저 → 백엔드 (POST /admin/templates) | 기존 admin RBAC 가드. Phase 26은 새 엔드포인트 추가 없음 (D-18) |
| 번들 → 렌더러 | 프리셋 JSON은 Plan 01에서 빌드/런타임 검증됨. 여기서는 재사용만 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02 | DoS | ImportTemplateModal 파일 입력 | mitigate | `file.size > 1_000_000` 체크 전 `FileReader` 호출 방지 (Task 2) |
| T-26-08 | Tampering | Import JSON prototype pollution | mitigate | Plan 01의 `.strict()` Zod 스키마가 `__proto__`/`constructor` unknown 키 거부. Task 2는 safeParse 결과만 사용 |
| T-26-09 | XSS | Zod 에러 메시지 렌더링 | mitigate | React의 기본 텍스트 이스케이프에 의존. `dangerouslySetInnerHTML` 사용 금지. 에러 path/message는 `{e.path}`, `{e.message}` 텍스트 노드로 삽입 (Task 2) |
| T-26-10 | Info Disclosure | Export 다운로드 | mitigate | Plan 01의 `buildExportPayload`가 민감 필드 제외. Task 3은 `downloadTemplateJson` 호출만 |
| T-26-11 | Tampering | 복제 시 schemaDefinition 재파싱 | mitigate | `JSON.parse(detail.schemaDefinition)`은 신뢰된 DB 소스(자체 백엔드). 이후 저장 시 TemplateFormModal.onSubmit 기존 검증(table/conditional/calculation) 재실행 |
| T-26-12 | Elevation | 신규 admin UI | accept | 신규 API 없음 — 기존 `POST /admin/templates` RBAC 재사용 |
| T-26-13 | Repudiation | 복제/Import로 생성된 템플릿 추적 | accept | 기존 `audit_log`가 `createTemplate` 이벤트 기록. 진입점 종류는 추적 안 함 (v1 범위 외) |
</threat_model>

<verification>
- `cd frontend && npm run build` 통과 (TypeScript strict)
- Plan 01의 모든 Vitest 단위 테스트 여전히 pass
- 수동 UAT 체크리스트 Task 4의 모든 항목 PASS
- `git diff` 확인으로 편집(edit) 경로와 toggle 경로에 의도치 않은 변경 없음
</verification>

<success_criteria>
- [ ] CNV-01 복제: Copy 아이콘 → 프리필 모달 → 저장 성공 end-to-end
- [ ] CNV-02 Export: Download 아이콘 → `{code}-YYYYMMDD.json` 파일 다운로드
- [ ] CNV-03 Import: 파일 선택 → Zod 검증 → 에러 리스트 또는 프리필 모달 → 저장
- [ ] CNV-04 프리셋: 4개 카드 → 선택 → 프리필 모달 → 저장
- [ ] TemplateCreateChoiceModal, PresetGallery, ImportTemplateModal 3개 컴포넌트 존재
- [ ] createFlow 상태 머신 TemplateListPage에 통합
- [ ] i18n 키 모두 ko/en 양쪽 추가
- [ ] 기존 편집/toggle/목록 기능 회귀 없음
- [ ] 수동 UAT 체크리스트 모든 항목 PASS
</success_criteria>

<output>
After completion, create `.planning/phases/26-convenience-features/26-02-SUMMARY.md`
</output>
