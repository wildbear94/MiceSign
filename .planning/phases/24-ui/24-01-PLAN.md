---
phase: 24-ui
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/admin/components/SchemaFieldEditor/types.ts
  - frontend/src/features/admin/components/SchemaFieldEditor/constants.ts
  - frontend/src/features/admin/components/SchemaFieldEditor/conditionalRuleUtils.ts
  - frontend/src/features/admin/components/SchemaFieldEditor/ConditionalRuleEditor.tsx
  - frontend/src/features/admin/components/SchemaFieldEditor/FieldCard.tsx
  - frontend/src/features/admin/components/SchemaFieldEditor/SchemaFieldEditor.tsx
  - frontend/public/locales/ko/admin.json
  - frontend/public/locales/en/admin.json
autonomous: true
requirements:
  - CND-01
  - CND-02

must_haves:
  truths:
    - "관리자가 필드 설정 패널에서 접기/펼치기 조건 규칙 섹션을 열 수 있다"
    - "관리자가 IF [필드] [연산자] [값] THEN [액션] 드롭다운 조합으로 조건을 설정할 수 있다"
    - "소스 필드 타입에 따라 연산자 목록이 필터링된다"
    - "자기참조 및 순환참조가 드롭다운에서 차단된다"
    - "필드 삭제 시 관련 조건 규칙이 양방향으로 자동 정리되고 토스트가 표시된다"
    - "소스 필드 타입 변경 시 관련 규칙이 자동 제거되고 토스트가 표시된다"
    - "규칙이 설정된 필드카드 헤더에 Zap 아이콘 배지가 표시된다"
  artifacts:
    - path: "frontend/src/features/admin/components/SchemaFieldEditor/ConditionalRuleEditor.tsx"
      provides: "조건 규칙 편집 UI 컴포넌트"
      min_lines: 80
    - path: "frontend/src/features/admin/components/SchemaFieldEditor/conditionalRuleUtils.ts"
      provides: "규칙 정리 유틸 함수"
      exports: ["cleanupRulesForDeletedField", "cleanupRulesForTypeChange", "getAvailableSourceFields"]
    - path: "frontend/src/features/admin/components/SchemaFieldEditor/constants.ts"
      provides: "OPERATORS_BY_TYPE, ACTION_OPTIONS 상수"
      contains: "OPERATORS_BY_TYPE"
  key_links:
    - from: "SchemaFieldEditor.tsx"
      to: "conditionalRuleUtils.ts"
      via: "import cleanupRulesForDeletedField, cleanupRulesForTypeChange"
      pattern: "cleanupRulesForDeletedField"
    - from: "FieldCard.tsx"
      to: "ConditionalRuleEditor.tsx"
      via: "render in collapsible section below FieldConfigEditor"
      pattern: "ConditionalRuleEditor"
    - from: "ConditionalRuleEditor.tsx"
      to: "conditionalRuleUtils.ts"
      via: "import getAvailableSourceFields"
      pattern: "getAvailableSourceFields"
---

<objective>
조건 규칙 편집 UI 핵심 컴포넌트와 필드 삭제/변경 시 규칙 자동 정리 로직을 구현한다.

Purpose: 관리자가 필드 간 조건부 표시/숨김 관계를 직관적으로 설정할 수 있도록 규칙 편집 인터페이스를 구축한다.
Output: ConditionalRuleEditor 컴포넌트, 유틸리티 함수, 상수 정의, i18n 키, FieldCard/SchemaFieldEditor 통합
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-ui/24-CONTEXT.md
@.planning/phases/24-ui/24-RESEARCH.md
@.planning/phases/24-ui/24-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From frontend/src/features/document/types/dynamicForm.ts:
```typescript
export interface ConditionalRule {
  targetFieldId: string;
  condition: {
    fieldId: string;
    operator: string;
    value: unknown;
  };
  action: 'show' | 'hide' | 'require' | 'unrequire';
}

export interface SchemaDefinition {
  version: number;
  fields: FieldDefinition[];
  conditionalRules?: ConditionalRule[];
  calculationRules?: CalculationRule[];
}
```

From frontend/src/features/admin/components/SchemaFieldEditor/types.ts:
```typescript
export type SchemaFieldType = 'text' | 'textarea' | 'number' | 'date' | 'select' | 'staticText' | 'hidden' | 'table';

export interface SchemaField {
  id: string;
  type: SchemaFieldType;
  label: string;
  required: boolean;
  config: SchemaFieldConfig;
}

export interface SchemaFieldEditorProps {
  fields: SchemaField[];
  onChange: (fields: SchemaField[]) => void;
}
```

From frontend/src/features/admin/components/SchemaFieldEditor/constants.ts:
```typescript
export const SMALL_INPUT_CLASS = 'w-full h-9 px-3 text-sm border rounded-lg bg-white dark:bg-gray-700 text-gray-900 dark:text-gray-50 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-blue-600 focus:border-transparent transition-colors border-gray-300 dark:border-gray-600';
```

From frontend/src/features/document/utils/evaluateConditions.ts:
```typescript
export function evaluateConditions(rules: ConditionalRule[], formValues: Record<string, unknown>): ConditionResult;
// ConditionResult = { hiddenFields: Set<string>; requiredFields: Set<string> }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 타입 정의 + 상수 + 유틸리티 함수 + i18n 키</name>
  <files>
    frontend/src/features/admin/components/SchemaFieldEditor/types.ts
    frontend/src/features/admin/components/SchemaFieldEditor/constants.ts
    frontend/src/features/admin/components/SchemaFieldEditor/conditionalRuleUtils.ts
    frontend/public/locales/ko/admin.json
    frontend/public/locales/en/admin.json
  </files>
  <read_first>
    frontend/src/features/admin/components/SchemaFieldEditor/types.ts
    frontend/src/features/admin/components/SchemaFieldEditor/constants.ts
    frontend/src/features/document/types/dynamicForm.ts
    frontend/public/locales/ko/admin.json
    frontend/public/locales/en/admin.json
  </read_first>
  <action>
**1. types.ts에 UI 전용 타입 추가** (기존 타입 유지, 하단에 추가):

```typescript
export type ComparisonOperator = 'eq' | 'neq' | 'gt' | 'gte' | 'lt' | 'lte' | 'in' | 'notIn' | 'isEmpty' | 'isNotEmpty';
export type ConditionalAction = 'show' | 'hide' | 'require' | 'unrequire';

export interface OperatorOption {
  value: ComparisonOperator;
  labelKey: string; // i18n key under templates.condition.operators.*
}

export interface ActionOption {
  value: ConditionalAction;
  labelKey: string; // i18n key under templates.condition.actions.*
}
```

**2. constants.ts에 조건 규칙 상수 추가** (기존 상수 유지, 하단에 추가):

```typescript
import type { ComparisonOperator, ConditionalAction, OperatorOption, ActionOption } from './types';

// D-05: 소스 필드 허용 타입
export const CONDITION_SOURCE_TYPES: SchemaFieldType[] = ['text', 'number', 'date', 'select'];

// D-04: 조건 타겟 제외 타입
export const CONDITION_EXCLUDED_TARGET_TYPES: SchemaFieldType[] = ['staticText', 'hidden'];

// D-08: 소스 필드 타입별 허용 연산자
export const OPERATORS_BY_TYPE: Record<string, ComparisonOperator[]> = {
  text:   ['eq', 'neq', 'isEmpty', 'isNotEmpty'],
  number: ['eq', 'neq', 'gt', 'gte', 'lt', 'lte', 'in', 'notIn', 'isEmpty', 'isNotEmpty'],
  date:   ['eq', 'neq', 'gt', 'lt', 'gte', 'lte'],
  select: ['eq', 'neq', 'in', 'notIn', 'isEmpty', 'isNotEmpty'],
};

// D-09: 액션 옵션
export const ACTION_OPTIONS: ActionOption[] = [
  { value: 'show', labelKey: 'templates.condition.actions.show' },
  { value: 'hide', labelKey: 'templates.condition.actions.hide' },
  { value: 'require', labelKey: 'templates.condition.actions.require' },
  { value: 'unrequire', labelKey: 'templates.condition.actions.unrequire' },
];
```

**3. conditionalRuleUtils.ts 신규 생성:**

```typescript
import type { ConditionalRule } from '../../../../features/document/types/dynamicForm';
// 실제 import 경로는 상대경로로 적절히 조정: '../../../document/types/dynamicForm'
import type { SchemaField } from './types';
import { CONDITION_SOURCE_TYPES } from './constants';

/**
 * 필드 삭제 시 관련 규칙 정리 (D-21, D-22)
 * 양방향: targetFieldId 또는 condition.fieldId로 참조하는 모든 규칙 제거
 * @returns [정리된 규칙 배열, 제거된 규칙 수]
 */
export function cleanupRulesForDeletedField(
  deletedFieldId: string,
  rules: ConditionalRule[],
): [ConditionalRule[], number] {
  const before = rules.length;
  const cleaned = rules.filter(
    r => r.targetFieldId !== deletedFieldId && r.condition.fieldId !== deletedFieldId
  );
  return [cleaned, before - cleaned.length];
}

/**
 * 소스 필드 타입 변경 시 관련 규칙 정리 (D-25)
 * 새 타입이 소스 불가 타입이면 해당 필드를 소스로 참조하는 규칙 제거
 * 타겟 필드가 staticText/hidden으로 변경되면 해당 필드를 타겟으로 하는 규칙도 제거
 */
export function cleanupRulesForTypeChange(
  changedFieldId: string,
  newType: string,
  rules: ConditionalRule[],
): [ConditionalRule[], number] {
  const before = rules.length;
  let cleaned = [...rules];

  // 소스로 사용 불가한 타입으로 변경 시 소스 참조 규칙 제거
  if (!CONDITION_SOURCE_TYPES.includes(newType as any)) {
    cleaned = cleaned.filter(r => r.condition.fieldId !== changedFieldId);
  }

  // 타겟 불가 타입으로 변경 시 타겟 참조 규칙 제거 (D-04)
  if (['staticText', 'hidden'].includes(newType)) {
    cleaned = cleaned.filter(r => r.targetFieldId !== changedFieldId);
  }

  return [cleaned, before - cleaned.length];
}

/**
 * 순환 참조 감지용 -- 특정 타겟 필드의 사용 가능 소스 필드 목록 (D-12, D-13)
 * 1. 자기 자신 제외
 * 2. 허용 타입만 (text, number, date, select)
 * 3. targetFieldId를 소스로 사용하는 규칙이 있는 필드 제외 (순환 방지)
 */
export function getAvailableSourceFields(
  targetFieldId: string,
  allFields: SchemaField[],
  rules: ConditionalRule[],
): SchemaField[] {
  const fieldsTargetingMe = rules
    .filter(r => r.condition.fieldId === targetFieldId)
    .map(r => r.targetFieldId);

  return allFields.filter(f =>
    f.id !== targetFieldId &&
    CONDITION_SOURCE_TYPES.includes(f.type as any) &&
    !fieldsTargetingMe.includes(f.id)
  );
}
```

**4. i18n 키 추가 (ko/admin.json, en/admin.json):**

ko/admin.json의 `"templates"` 객체 내부에 `"condition"` 키 추가:
```json
"condition": {
  "sectionTitle": "조건 규칙",
  "noRule": "조건 규칙이 없습니다",
  "addRule": "조건 추가",
  "deleteRule": "조건 삭제",
  "if": "IF",
  "then": "THEN",
  "selectField": "필드 선택",
  "selectOperator": "연산자 선택",
  "selectAction": "액션 선택",
  "enterValue": "값 입력",
  "operators": {
    "eq": "값이 같음",
    "neq": "값이 다름",
    "gt": "보다 큼",
    "gte": "이상",
    "lt": "보다 작음",
    "lte": "이하",
    "in": "포함",
    "notIn": "미포함",
    "isEmpty": "비어있음",
    "isNotEmpty": "비어있지 않음"
  },
  "actions": {
    "show": "표시",
    "hide": "숨김",
    "require": "필수로 변경",
    "unrequire": "선택으로 변경"
  },
  "rulesAutoRemoved": "조건 규칙 {{count}}개가 자동 제거되었습니다",
  "validationError": "조건 규칙에 오류가 있습니다",
  "noSourceFields": "조건 소스로 사용 가능한 필드가 없습니다",
  "resetPreview": "초기화"
}
```

en/admin.json도 동일 구조로 영어 번역 추가:
```json
"condition": {
  "sectionTitle": "Conditional Rules",
  "noRule": "No conditional rules",
  "addRule": "Add Condition",
  "deleteRule": "Delete Condition",
  "if": "IF",
  "then": "THEN",
  "selectField": "Select field",
  "selectOperator": "Select operator",
  "selectAction": "Select action",
  "enterValue": "Enter value",
  "operators": {
    "eq": "equals",
    "neq": "not equals",
    "gt": "greater than",
    "gte": "greater or equal",
    "lt": "less than",
    "lte": "less or equal",
    "in": "is in",
    "notIn": "is not in",
    "isEmpty": "is empty",
    "isNotEmpty": "is not empty"
  },
  "actions": {
    "show": "Show",
    "hide": "Hide",
    "require": "Make required",
    "unrequire": "Make optional"
  },
  "rulesAutoRemoved": "{{count}} conditional rule(s) auto-removed",
  "validationError": "Conditional rules have errors",
  "noSourceFields": "No fields available as condition source",
  "resetPreview": "Reset"
}
```
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - types.ts contains `export type ComparisonOperator =`
    - types.ts contains `export interface OperatorOption`
    - types.ts contains `export interface ActionOption`
    - constants.ts contains `export const CONDITION_SOURCE_TYPES`
    - constants.ts contains `export const OPERATORS_BY_TYPE`
    - constants.ts contains `export const ACTION_OPTIONS`
    - constants.ts contains `export const CONDITION_EXCLUDED_TARGET_TYPES`
    - conditionalRuleUtils.ts exists and contains `export function cleanupRulesForDeletedField`
    - conditionalRuleUtils.ts contains `export function cleanupRulesForTypeChange`
    - conditionalRuleUtils.ts contains `export function getAvailableSourceFields`
    - ko/admin.json contains `"condition":`
    - ko/admin.json contains `"sectionTitle": "조건 규칙"`
    - ko/admin.json contains `"eq": "값이 같음"`
    - en/admin.json contains `"sectionTitle": "Conditional Rules"`
    - `npx tsc --noEmit` exits with 0
  </acceptance_criteria>
  <done>타입 정의, 상수, 유틸리티 함수, i18n 키가 모두 생성되어 TypeScript 빌드가 통과한다</done>
</task>

<task type="auto">
  <name>Task 2: ConditionalRuleEditor 컴포넌트 + FieldCard 배지/섹션 + SchemaFieldEditor 통합</name>
  <files>
    frontend/src/features/admin/components/SchemaFieldEditor/ConditionalRuleEditor.tsx
    frontend/src/features/admin/components/SchemaFieldEditor/FieldCard.tsx
    frontend/src/features/admin/components/SchemaFieldEditor/SchemaFieldEditor.tsx
  </files>
  <read_first>
    frontend/src/features/admin/components/SchemaFieldEditor/FieldCard.tsx
    frontend/src/features/admin/components/SchemaFieldEditor/SchemaFieldEditor.tsx
    frontend/src/features/admin/components/SchemaFieldEditor/FieldConfigEditor.tsx
    frontend/src/features/admin/components/SchemaFieldEditor/conditionalRuleUtils.ts
    frontend/src/features/admin/components/SchemaFieldEditor/types.ts
    frontend/src/features/admin/components/SchemaFieldEditor/constants.ts
    frontend/src/features/document/types/dynamicForm.ts
    frontend/src/features/admin/components/SchemaFieldEditor/TableColumnEditor.tsx
    frontend/public/locales/ko/admin.json
  </read_first>
  <action>
**1. ConditionalRuleEditor.tsx 신규 생성:**

접기/펼치기 섹션 내부에 조건 규칙 편집 UI를 구현한다.

Props:
```typescript
interface ConditionalRuleEditorProps {
  targetFieldId: string;
  rule: ConditionalRule | undefined; // D-02: 필드당 1개만
  allFields: SchemaField[];
  allRules: ConditionalRule[];
  onAddRule: (rule: ConditionalRule) => void;
  onUpdateRule: (rule: ConditionalRule) => void;
  onDeleteRule: () => void;
}
```

구현 요소:
- `import { useTranslation } from 'react-i18next'` + `useTranslation('admin')`
- `import { Plus, Trash2 } from 'lucide-react'`
- `import { SMALL_INPUT_CLASS, OPERATORS_BY_TYPE, ACTION_OPTIONS, CONDITION_SOURCE_TYPES } from './constants'`
- `import { getAvailableSourceFields } from './conditionalRuleUtils'`
- `import type { ConditionalRule } from '../../../document/types/dynamicForm'`

**규칙이 없을 때 (D-03):**
```jsx
<div className="text-sm text-gray-400 dark:text-gray-500">
  <p>{t('templates.condition.noRule')}</p>
  <button type="button" onClick={handleAdd}
    className="mt-2 inline-flex items-center gap-1 text-sm text-blue-600 dark:text-blue-400 hover:text-blue-800 dark:hover:text-blue-300">
    <Plus className="w-3.5 h-3.5" />
    {t('templates.condition.addRule')}
  </button>
</div>
```

handleAdd 함수: 사용 가능한 소스 필드를 `getAvailableSourceFields`로 구하고, 첫 번째 소스 필드를 기본 선택하여 새 ConditionalRule을 생성한다. 소스 필드가 없으면 `toast(t('templates.condition.noSourceFields'))` 표시하고 return.

기본 새 규칙: `{ targetFieldId, condition: { fieldId: firstSource.id, operator: OPERATORS_BY_TYPE[firstSource.type][0], value: '' }, action: 'show' }`

**규칙이 있을 때 (D-07) — 2행 스택 레이아웃:**

행 1: IF 라벨 + 소스 필드 드롭다운 (전체 너비) + 연산자/값 행 (grid-cols-2 gap-3)
행 2: THEN 라벨 + 액션 드롭다운 + 삭제 버튼 (flex justify-between)

IF/THEN 라벨: `text-xs font-medium text-gray-500 dark:text-gray-400`

**소스 필드 드롭다운:** native `<select>` with SMALL_INPUT_CLASS. 옵션은 `getAvailableSourceFields(targetFieldId, allFields, allRules)`로 필터링. onChange 시 규칙의 `condition.fieldId`를 업데이트하고, 연산자를 새 타입의 첫 번째 연산자로 리셋하며, 값을 `''`로 리셋한다.

**연산자 드롭다운:** native `<select>` with SMALL_INPUT_CLASS. 소스 필드의 type으로 `OPERATORS_BY_TYPE[sourceFieldType]`을 조회하여 옵션 생성. 각 옵션의 텍스트는 `t('templates.condition.operators.' + op)`. onChange 시 operator를 업데이트하고, isEmpty/isNotEmpty 선택 시 value를 `null`로 설정.

**값 입력 (D-10, D-11):**
- `isEmpty`/`isNotEmpty` 연산자일 때: 값 입력 필드 숨김 (렌더링 안 함)
- 소스가 `select` 타입이고 연산자가 `in`/`notIn`일 때 (D-11): 체크박스 목록. 소스 필드의 `config.options`를 순회하며 각 옵션에 대해 체크박스 렌더링. 컨테이너: `max-h-40 overflow-y-auto border border-gray-300 dark:border-gray-600 rounded-lg p-2 space-y-1`. 각 체크박스: `<label className="flex items-center gap-2 text-sm">`. value는 선택된 옵션 value들의 배열.
- 소스가 `select` 타입이고 연산자가 `eq`/`neq`일 때 (D-10): 소스 필드의 `config.options`를 native `<select>` 드롭다운으로 표시.
- 소스가 `number` 타입일 때: `<input type="number" className={SMALL_INPUT_CLASS} />`
- 소스가 `date` 타입일 때: `<input type="date" className={SMALL_INPUT_CLASS} />`
- 그 외: `<input type="text" className={SMALL_INPUT_CLASS} placeholder={t('templates.condition.enterValue')} />`

**액션 드롭다운:** native `<select>` with SMALL_INPUT_CLASS. ACTION_OPTIONS 순회하여 옵션 생성. 텍스트는 `t(action.labelKey)`.

**삭제 버튼 (D-14):** `<button onClick={onDeleteRule} className="p-1.5 text-gray-400 hover:text-red-500 dark:hover:text-red-400 hover:bg-red-50 dark:hover:bg-red-900/20 rounded transition-colors" title={t('templates.condition.deleteRule')}><Trash2 className="w-4 h-4" /></button>`. 확인 다이얼로그 없이 즉시 삭제.

**2. FieldCard.tsx 수정:**

(a) Props 확장: 기존 inline props 타입을 확장하여 추가:
```typescript
conditionalRules: ConditionalRule[];
allFields: SchemaField[];
onAddRule: (rule: ConditionalRule) => void;
onUpdateRule: (rule: ConditionalRule) => void;
onDeleteRule: (targetFieldId: string) => void;
```

(b) 헤더에 Zap 배지 추가 (D-06): `import { Zap } from 'lucide-react'`. required `*` 표시 뒤에, conditionalRules에서 `r.targetFieldId === field.id`인 규칙이 있으면 배지 표시:
```jsx
{conditionalRules.some(r => r.targetFieldId === field.id) && (
  <span className="inline-flex items-center bg-amber-100 text-amber-700 dark:bg-amber-900/40 dark:text-amber-300 rounded px-1 py-0.5">
    <Zap className="w-3.5 h-3.5" />
  </span>
)}
```

(c) expanded 영역 내, FieldConfigEditor 아래에 조건 규칙 접기/펼치기 섹션 추가 (D-01, D-04):
- `CONDITION_EXCLUDED_TARGET_TYPES`에 포함되지 않는 필드에만 표시
- `const [conditionExpanded, setConditionExpanded] = useState(false)` state 추가
- 섹션 헤더: `<div role="button" aria-expanded={conditionExpanded} onClick={() => setConditionExpanded(!conditionExpanded)} className="flex items-center gap-2 py-2 cursor-pointer select-none">`
  - `<ChevronRight className={...rotate-90...}/>` (기존 패턴 재사용)
  - `<span className="text-xs font-medium text-gray-600 dark:text-gray-400">{t('templates.condition.sectionTitle')}</span>`
- conditionExpanded일 때만 ConditionalRuleEditor를 렌더링
- ConditionalRuleEditor에 전달할 `rule`은 `conditionalRules.find(r => r.targetFieldId === field.id)`

**3. SchemaFieldEditor.tsx 수정:**

(a) Props 확장: `SchemaFieldEditorProps`에 추가 (types.ts에서):
```typescript
export interface SchemaFieldEditorProps {
  fields: SchemaField[];
  onChange: (fields: SchemaField[]) => void;
  conditionalRules: ConditionalRule[];
  onConditionalRulesChange: (rules: ConditionalRule[]) => void;
}
```

(b) `import { cleanupRulesForDeletedField, cleanupRulesForTypeChange } from './conditionalRuleUtils'`
(c) `import { toast } from 'sonner'`

(d) deleteField 함수 수정: 기존 `window.confirm` 후, fields 제거 전에:
```typescript
const deletedFieldId = fields[index].id;
// ... 기존 필드 제거 로직 ...
const [cleanedRules, removedCount] = cleanupRulesForDeletedField(deletedFieldId, conditionalRules);
if (removedCount > 0) {
  toast(t('templates.condition.rulesAutoRemoved', { count: removedCount }));
}
onConditionalRulesChange(cleanedRules);
```

(e) updateField 함수 수정: 타입 변경 감지 후 규칙 정리 (D-25):
```typescript
const updateField = (index: number, updated: SchemaField) => {
  const oldField = fields[index];
  const newFields = [...fields];
  newFields[index] = updated;
  onChange(newFields);

  // 필드 타입이 변경된 경우 규칙 정리
  if (oldField.type !== updated.type) {
    const [cleanedRules, removedCount] = cleanupRulesForTypeChange(updated.id, updated.type, conditionalRules);
    if (removedCount > 0) {
      toast(t('templates.condition.rulesAutoRemoved', { count: removedCount }));
      onConditionalRulesChange(cleanedRules);
    }
  }
};
```

(f) FieldCard에 conditionalRules 관련 props 전달:
```jsx
<FieldCard
  key={field.id}
  field={field}
  index={index}
  total={fields.length}
  expanded={expandedIndex === index}
  onToggle={...}
  onUpdate={(updated) => updateField(index, updated)}
  onMove={(dir) => moveField(index, dir)}
  onDelete={() => deleteField(index)}
  conditionalRules={conditionalRules}
  allFields={fields}
  onAddRule={(rule) => onConditionalRulesChange([...conditionalRules, rule])}
  onUpdateRule={(rule) => onConditionalRulesChange(
    conditionalRules.map(r => r.targetFieldId === rule.targetFieldId ? rule : r)
  )}
  onDeleteRule={(targetFieldId) => onConditionalRulesChange(
    conditionalRules.filter(r => r.targetFieldId !== targetFieldId)
  )}
/>
```
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - ConditionalRuleEditor.tsx exists and contains `function ConditionalRuleEditor`
    - ConditionalRuleEditor.tsx contains `getAvailableSourceFields`
    - ConditionalRuleEditor.tsx contains `OPERATORS_BY_TYPE`
    - ConditionalRuleEditor.tsx contains `ACTION_OPTIONS`
    - ConditionalRuleEditor.tsx contains `templates.condition.noRule`
    - ConditionalRuleEditor.tsx contains `max-h-40 overflow-y-auto` (D-11 checkbox list)
    - FieldCard.tsx contains `import { Zap` (or Zap in the import)
    - FieldCard.tsx contains `bg-amber-100 text-amber-700` (D-06 badge)
    - FieldCard.tsx contains `conditionExpanded` (collapsible state)
    - FieldCard.tsx contains `ConditionalRuleEditor`
    - FieldCard.tsx contains `CONDITION_EXCLUDED_TARGET_TYPES`
    - FieldCard.tsx contains `aria-expanded`
    - SchemaFieldEditor.tsx contains `conditionalRules`
    - SchemaFieldEditor.tsx contains `onConditionalRulesChange`
    - SchemaFieldEditor.tsx contains `cleanupRulesForDeletedField`
    - SchemaFieldEditor.tsx contains `cleanupRulesForTypeChange`
    - SchemaFieldEditor.tsx contains `rulesAutoRemoved`
    - `npx tsc --noEmit` exits with 0 (may have TemplateFormModal errors due to changed props — acceptable, fixed in Plan 02)
  </acceptance_criteria>
  <done>ConditionalRuleEditor 컴포넌트가 FieldCard의 접기/펼치기 섹션에 통합되고, SchemaFieldEditor에서 필드 삭제/타입 변경 시 규칙 정리가 동작한다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin UI -> schemaDefinition JSON | 관리자 입력이 JSON으로 직렬화되어 백엔드에 저장 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | conditionalRules JSON | accept | 프론트엔드 전용 기능, 백엔드는 schemaDefinition JSON을 그대로 저장 (D-26). 규칙은 프론트엔드에서만 평가되므로 서버 측 보안 영향 없음. 50인 사내 시스템으로 위험도 낮음. |
| T-24-02 | Denial of Service | 무한 루프 조건 평가 | mitigate | 순환 참조를 설정 시점에서 드롭다운 필터링으로 원천 차단 (D-13, getAvailableSourceFields). 필드당 규칙 1개 제한 (D-02)으로 규칙 수 = 필드 수 이하. |
| T-24-03 | Information Disclosure | 미리보기에서 다른 양식 데이터 노출 | accept | 미리보기는 현재 편집 중인 양식의 필드만 표시. 다른 양식 데이터에 접근하지 않음. |
</threat_model>

<verification>
1. `npx tsc --noEmit` — TypeScript 컴파일 통과
2. `npm run build` — 프로덕션 빌드 성공
3. conditionalRuleUtils.ts의 3개 export 함수가 올바른 시그니처로 존재
4. ConditionalRuleEditor.tsx가 D-07 레이아웃 (IF-THEN 2행 스택)을 구현
5. FieldCard.tsx에 Zap 배지 + 접기/펼치기 조건 섹션 존재
6. SchemaFieldEditor.tsx에서 deleteField/updateField에 규칙 정리 로직 존재
</verification>

<success_criteria>
- conditionalRuleUtils.ts에 3개 유틸 함수 export
- ConditionalRuleEditor.tsx가 빈 상태 / 규칙 편집 상태를 모두 렌더링
- FieldCard 헤더에 조건 배지, expanded 영역에 접기/펼치기 조건 섹션
- SchemaFieldEditor에서 conditionalRules prop 수신 및 정리 로직 동작
- i18n 키 (ko/en) 모두 추가
- TypeScript 빌드 통과 (TemplateFormModal props 불일치 에러는 Plan 02에서 해결)
</success_criteria>

<output>
After completion, create `.planning/phases/24-ui/24-01-SUMMARY.md`
</output>
