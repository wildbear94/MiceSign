---
phase: 19-registration-email-notifications
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - backend/src/main/java/com/micesign/service/RegistrationService.java
  - backend/src/test/java/com/micesign/registration/RegistrationEmailServiceTest.java
autonomous: true
requirements:
  - MAIL-02
  - MAIL-03
  - MAIL-04
must_haves:
  truths:
    - "등록 신청 submit 시 REGISTRATION_SUBMIT 이벤트가 발행된다"
    - "등록 승인 시 REGISTRATION_APPROVE 이벤트가 발행된다"
    - "등록 거부 시 REGISTRATION_REJECT 이벤트가 발행된다"
    - "RegistrationEmailService가 각 이벤트에 대해 올바른 수신자에게 이메일을 발송(또는 로그)한다"
    - "REGISTRATION_SUBMIT 이벤트에서 SUPER_ADMIN들에게 알림이 발송된다"
  artifacts:
    - path: "backend/src/main/java/com/micesign/service/RegistrationService.java"
      provides: "이벤트 발행이 추가된 등록 서비스"
      contains: "eventPublisher.publishEvent"
    - path: "backend/src/test/java/com/micesign/registration/RegistrationEmailServiceTest.java"
      provides: "이메일 서비스 단위 테스트"
      contains: "RegistrationEmailServiceTest"
  key_links:
    - from: "RegistrationService.submit()"
      to: "RegistrationEmailService.handleRegistrationEvent()"
      via: "ApplicationEventPublisher.publishEvent()"
      pattern: "publishEvent.*REGISTRATION_SUBMIT"
    - from: "RegistrationService.approve()"
      to: "RegistrationEmailService.handleRegistrationEvent()"
      via: "ApplicationEventPublisher.publishEvent()"
      pattern: "publishEvent.*REGISTRATION_APPROVE"
    - from: "RegistrationService.reject()"
      to: "RegistrationEmailService.handleRegistrationEvent()"
      via: "ApplicationEventPublisher.publishEvent()"
      pattern: "publishEvent.*REGISTRATION_REJECT"
---

<objective>
RegistrationService에 이벤트 발행을 추가하고 RegistrationEmailService의 단위 테스트를 작성한다.

Purpose: Plan 01에서 구축한 이메일 인프라를 RegistrationService에 연결하여 전체 등록 이메일 알림 파이프라인을 완성한다.
Output: 이벤트 발행이 연결된 RegistrationService + 단위 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-registration-email-notifications/19-CONTEXT.md
@.planning/phases/19-registration-email-notifications/19-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 artifacts -->

From backend/src/main/java/com/micesign/event/RegistrationNotificationEvent.java:
```java
package com.micesign.event;

public class RegistrationNotificationEvent {
    private final Long registrationRequestId;
    private final RegistrationEventType eventType;

    public RegistrationNotificationEvent(Long registrationRequestId, RegistrationEventType eventType) {
        this.registrationRequestId = registrationRequestId;
        this.eventType = eventType;
    }

    public Long getRegistrationRequestId() { return registrationRequestId; }
    public RegistrationEventType getEventType() { return eventType; }
}
```

From backend/src/main/java/com/micesign/event/RegistrationEventType.java:
```java
package com.micesign.event;

public enum RegistrationEventType {
    REGISTRATION_SUBMIT,
    REGISTRATION_APPROVE,
    REGISTRATION_REJECT
}
```

From backend/src/main/java/com/micesign/service/RegistrationService.java (current):
```java
@Service
@Transactional(readOnly = true)
public class RegistrationService {
    // Constructor params: registrationRequestRepository, userRepository, passwordEncoder,
    //                     registrationMapper, auditLogService, departmentRepository, positionRepository
    // Methods: submit(), approve(), reject(), expirePendingRequests(), getStatusByEmailAndToken(), getRegistrations()
}
```

From backend/src/main/java/com/micesign/service/RegistrationEmailService.java (Plan 01):
```java
@Service
public class RegistrationEmailService {
    @TransactionalEventListener(phase = TransactionPhase.AFTER_COMMIT)
    @Async
    public void handleRegistrationEvent(RegistrationNotificationEvent event) { ... }
}
```

From backend/src/main/java/com/micesign/domain/NotificationLog.java (Plan 01에서 수정됨):
```java
// recipient 관련 필드 - nullable로 변경됨
@Column(name = "recipient_id", nullable = true, insertable = false, updatable = false)
private Long recipientId;

@ManyToOne(fetch = FetchType.LAZY)
@JoinColumn(name = "recipient_id", nullable = true)
private User recipient;

// recipientEmail - 기존 필드, Plan 01에서 nullable로 변경됨
@Column(name = "recipient_email", nullable = true, length = 150)
private String recipientEmail;
// getter: getRecipientEmail(), setter: setRecipientEmail(String)

// Plan 01에서 추가된 필드
@Column(name = "registration_request_id")
private Long registrationRequestId;
// getter: getRegistrationRequestId(), setter: setRegistrationRequestId(Long)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RegistrationService에 ApplicationEventPublisher 주입 및 이벤트 발행 추가</name>
  <files>
    backend/src/main/java/com/micesign/service/RegistrationService.java
  </files>
  <read_first>
    backend/src/main/java/com/micesign/service/RegistrationService.java,
    backend/src/main/java/com/micesign/event/RegistrationNotificationEvent.java,
    backend/src/main/java/com/micesign/event/RegistrationEventType.java
  </read_first>
  <action>
RegistrationService.java에 ApplicationEventPublisher를 추가한다 (per D-09):

1. **import 추가:**
   ```java
   import com.micesign.event.RegistrationEventType;
   import com.micesign.event.RegistrationNotificationEvent;
   import org.springframework.context.ApplicationEventPublisher;
   ```

2. **필드 추가:** 기존 필드 목록 끝에:
   ```java
   private final ApplicationEventPublisher eventPublisher;
   ```

3. **생성자 파라미터 추가:** 기존 생성자의 마지막 파라미터 뒤에 `ApplicationEventPublisher eventPublisher` 추가, 생성자 본문에 `this.eventPublisher = eventPublisher;` 추가.

4. **submit() 메서드:** `return registrationMapper.toStatusResponse(entity);` 직전에 추가:
   ```java
   eventPublisher.publishEvent(
       new RegistrationNotificationEvent(entity.getId(), RegistrationEventType.REGISTRATION_SUBMIT)
   );
   ```

5. **approve() 메서드:** 메서드 마지막 줄(auditLogService.log 호출 뒤)에 추가:
   ```java
   eventPublisher.publishEvent(
       new RegistrationNotificationEvent(reg.getId(), RegistrationEventType.REGISTRATION_APPROVE)
   );
   ```

6. **reject() 메서드:** 메서드 마지막 줄(auditLogService.log 호출 뒤)에 추가:
   ```java
   eventPublisher.publishEvent(
       new RegistrationNotificationEvent(reg.getId(), RegistrationEventType.REGISTRATION_REJECT)
   );
   ```
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/backend && ./gradlew compileJava 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - RegistrationService.java contains `private final ApplicationEventPublisher eventPublisher`
    - RegistrationService.java contains `import org.springframework.context.ApplicationEventPublisher`
    - RegistrationService.java contains `import com.micesign.event.RegistrationNotificationEvent`
    - RegistrationService.java contains `import com.micesign.event.RegistrationEventType`
    - RegistrationService.java contains `publishEvent(` at least 3 times (submit, approve, reject)
    - RegistrationService.java contains `RegistrationEventType.REGISTRATION_SUBMIT`
    - RegistrationService.java contains `RegistrationEventType.REGISTRATION_APPROVE`
    - RegistrationService.java contains `RegistrationEventType.REGISTRATION_REJECT`
    - `./gradlew compileJava` exits 0
  </acceptance_criteria>
  <done>RegistrationService의 submit/approve/reject 메서드에서 각각 REGISTRATION_SUBMIT/APPROVE/REJECT 이벤트를 발행한다.</done>
</task>

<!-- Task 2: tdd="true" 속성은 이 태스크가 테스트 우선 작성(RED-GREEN) 패턴을 따름을 의미.
     type="auto"인 이유: 독립 TDD plan이 아닌 execute plan 내의 테스트 작성 태스크이므로.
     behavior 블록으로 테스트 기대값을 먼저 정의하고, action에서 구현하는 task-level TDD 패턴. -->
<task type="auto" tdd="true">
  <name>Task 2: RegistrationEmailService 단위 테스트</name>
  <files>
    backend/src/test/java/com/micesign/registration/RegistrationEmailServiceTest.java
  </files>
  <read_first>
    backend/src/main/java/com/micesign/service/RegistrationEmailService.java,
    backend/src/main/java/com/micesign/event/RegistrationNotificationEvent.java,
    backend/src/main/java/com/micesign/event/RegistrationEventType.java,
    backend/src/main/java/com/micesign/domain/RegistrationRequest.java,
    backend/src/main/java/com/micesign/domain/NotificationLog.java,
    backend/src/test/java/com/micesign/registration/RegistrationServiceTest.java
  </read_first>
  <behavior>
    - Test 1: REGISTRATION_SUBMIT 이벤트 처리 시 신청자에게 확인 이메일 로그 기록 (mailSender=null 모드)
    - Test 2: REGISTRATION_SUBMIT 이벤트 처리 시 SUPER_ADMIN들에게 알림 이메일 로그 기록
    - Test 3: REGISTRATION_APPROVE 이벤트 처리 시 신청자에게 승인 이메일 로그 기록
    - Test 4: REGISTRATION_REJECT 이벤트 처리 시 신청자에게 거부 이메일 로그 기록 (rejectionReason 포함)
    - Test 5: 존재하지 않는 registrationRequestId로 이벤트 발생 시 로그만 출력하고 예외 없음
    - Test 6: NotificationLog에 registrationRequestId가 올바르게 저장됨
    - Test 7: NotificationLog에 올바른 eventType이 저장됨 (REGISTRATION_SUBMIT 등)
    - Test 8: SUPER_ADMIN이 없으면 admin 알림은 건너뜀 (에러 없음)
  </behavior>
  <action>
Mockito 기반 단위 테스트를 작성한다. `@ExtendWith(MockitoExtension.class)` 사용.

1. **패키지:** `com.micesign.registration`

2. **Mocks:**
   - `@Mock JavaMailSender mailSender` -- null로 전달하여 로그 모드 테스트
   - `@Mock SpringTemplateEngine templateEngine`
   - `@Mock RegistrationRequestRepository registrationRequestRepository`
   - `@Mock UserRepository userRepository`
   - `@Mock NotificationLogRepository notificationLogRepository`

3. **Setup (@BeforeEach):**
   - RegistrationEmailService를 mailSender=null (로그 모드)로 생성
   - templateEngine.process() -> 항상 "<html>test</html>" 반환하도록 stub
   - baseUrl = "http://localhost:5173", fromAddress = "noreply@test.com"

4. **테스트 메서드:**

   `test_handleSubmitEvent_sendsConfirmationToApplicant()`:
   - RegistrationRequest 생성 (id=1, name="홍길동", email="hong@test.com", createdAt=now)
   - registrationRequestRepository.findById(1L) -> Optional.of(reg)
   - userRepository.findByRoleAndStatus(SUPER_ADMIN, ACTIVE) -> 빈 리스트
   - handleRegistrationEvent(new RegistrationNotificationEvent(1L, REGISTRATION_SUBMIT))
   - verify: notificationLogRepository.save() 1회 호출
   - ArgumentCaptor<NotificationLog>로 캡처, recipientEmail="hong@test.com", eventType="REGISTRATION_SUBMIT", status=SUCCESS, registrationRequestId=1L 확인

   `test_handleSubmitEvent_notifiesSuperAdmins()`:
   - RegistrationRequest + 2명의 SUPER_ADMIN User 생성
   - handleRegistrationEvent(REGISTRATION_SUBMIT)
   - verify: notificationLogRepository.save() 3회 호출 (신청자 1 + SUPER_ADMIN 2)

   `test_handleApproveEvent_sendsApproveEmail()`:
   - RegistrationRequest (status=APPROVED, processedAt=now)
   - handleRegistrationEvent(REGISTRATION_APPROVE)
   - verify: save 1회, eventType="REGISTRATION_APPROVE", subject contains "승인"

   `test_handleRejectEvent_sendsRejectEmail()`:
   - RegistrationRequest (status=REJECTED, rejectionReason="서류 미비")
   - handleRegistrationEvent(REGISTRATION_REJECT)
   - verify: save 1회, eventType="REGISTRATION_REJECT", subject contains "결과"

   `test_handleEvent_registrationNotFound_noException()`:
   - registrationRequestRepository.findById(999L) -> Optional.empty()
   - handleRegistrationEvent(new RegistrationNotificationEvent(999L, REGISTRATION_SUBMIT))
   - verify: notificationLogRepository.save() 0회

   `test_notificationLog_hasCorrectRegistrationRequestId()`:
   - REGISTRATION_SUBMIT 이벤트 처리 후 저장된 NotificationLog의 registrationRequestId가 reg.getId()와 일치

   `test_notificationLog_hasCorrectEventType()`:
   - 각 이벤트 타입별로 NotificationLog.eventType이 "REGISTRATION_SUBMIT", "REGISTRATION_APPROVE", "REGISTRATION_REJECT" 중 올바른 값

   `test_noSuperAdmins_noError()`:
   - SUPER_ADMIN 조회 결과 빈 리스트
   - REGISTRATION_SUBMIT 처리 시 신청자 확인 이메일만 발송 (1회), 에러 없음
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/backend && ./gradlew test --tests '*RegistrationEmailServiceTest*' 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - RegistrationEmailServiceTest.java exists in backend/src/test/java/com/micesign/registration/
    - RegistrationEmailServiceTest.java contains `@ExtendWith(MockitoExtension.class)`
    - RegistrationEmailServiceTest.java contains at least 5 test methods with `@Test`
    - RegistrationEmailServiceTest.java contains `ArgumentCaptor<NotificationLog>`
    - RegistrationEmailServiceTest.java verifies `notificationLogRepository.save()`
    - RegistrationEmailServiceTest.java tests REGISTRATION_SUBMIT, REGISTRATION_APPROVE, REGISTRATION_REJECT
    - `./gradlew test --tests '*RegistrationEmailServiceTest*'` exits 0 with all tests passing
    - Full suite: `./gradlew test --tests '*Registration*'` exits 0
  </acceptance_criteria>
  <done>RegistrationEmailService의 모든 이벤트 처리 경로가 테스트됨. SUBMIT(신청자+SUPER_ADMIN), APPROVE(신청자), REJECT(신청자+사유), 미존재ID, 빈 SUPER_ADMIN 목록 시나리오 커버.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RegistrationService -> EventPublisher | 서비스 메서드에서 이벤트 발행, 비동기 리스너에서 처리 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Spoofing | Event publishing | accept | 이벤트는 내부 서비스 코드에서만 발행됨. 외부 트리거 불가. Spring 내부 이벤트 버스 사용 |
| T-19-06 | Denial of Service | 대량 이벤트 발행 | transfer | 등록 API rate limiting은 Phase 21 SEC-01에서 처리. AsyncConfig의 ThreadPool이 큐 제한(100) |
</threat_model>

<verification>
- `./gradlew test --tests '*Registration*'` 전체 통과
- RegistrationService.submit/approve/reject에서 각각 이벤트 발행 확인
- RegistrationEmailServiceTest에서 모든 이벤트 타입 + 에지 케이스 커버
</verification>

<success_criteria>
- RegistrationService의 3개 메서드(submit, approve, reject)에서 이벤트 발행
- RegistrationEmailServiceTest 전체 통과 (8개 테스트)
- 기존 Registration 테스트 회귀 없음
</success_criteria>

<output>
After completion, create `.planning/phases/19-registration-email-notifications/19-02-SUMMARY.md`
</output>
