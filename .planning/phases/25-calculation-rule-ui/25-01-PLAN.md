---
phase: 25-calculation-rule-ui
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/admin/components/SchemaFieldEditor/types.ts
  - frontend/src/features/admin/components/SchemaFieldEditor/constants.ts
  - frontend/src/features/admin/components/SchemaFieldEditor/calculationRuleUtils.ts
  - frontend/public/locales/ko/admin.json
  - frontend/public/locales/en/admin.json
autonomous: true
requirements:
  - CAL-01
  - CAL-02

must_haves:
  truths:
    - "편집 UI helper 타입(PresetType, PresetConfig, CalcSource)이 SchemaFieldEditor/types.ts 에 존재한다"
    - "constants.ts 에 PRESET_OPTIONS 4종(sum-col, sum-mul, field-sum, ratio)과 CALC_RESERVED_WORDS 가 정의된다"
    - "calculationRuleUtils.ts 가 extractDependencies/buildFormulaFromPreset/parseFormulaToPreset/validateFormula/getAvailableCalcSources/cleanupCalcRulesForDeletedField/cleanupCalcRulesForTypeChange/renderFormulaFriendly 를 export 한다"
    - "extractDependencies 정규식은 executeCalculations.ts:230 과 동일한 패턴을 사용한다 (런타임 불변)"
    - "i18n 키 admin.templates.calculation.* 네임스페이스가 ko/en 양쪽에 추가된다"
    - "npx tsc --noEmit 이 통과한다"
  artifacts:
    - path: "frontend/src/features/admin/components/SchemaFieldEditor/calculationRuleUtils.ts"
      provides: "계산 규칙 파서/프리셋 변환/검증/cascade 유틸"
      exports: ["extractDependencies", "buildFormulaFromPreset", "parseFormulaToPreset", "validateFormula", "getAvailableCalcSources", "cleanupCalcRulesForDeletedField", "cleanupCalcRulesForTypeChange", "renderFormulaFriendly"]
      min_lines: 120
    - path: "frontend/src/features/admin/components/SchemaFieldEditor/constants.ts"
      provides: "PRESET_OPTIONS 상수"
      contains: "PRESET_OPTIONS"
    - path: "frontend/src/features/admin/components/SchemaFieldEditor/types.ts"
      provides: "PresetType, PresetConfig, CalcSource"
      contains: "PresetType"
  key_links:
    - from: "calculationRuleUtils.ts::extractDependencies"
      to: "executeCalculations.ts:230 정규식"
      via: "복제된 /[a-zA-Z_]\\w*(?:\\.[a-zA-Z_]\\w*)*/g 패턴"
      pattern: "FIELD_REF_PATTERN"

threat_refs:
  - T-25-01
---

<objective>
Phase 25 의 모든 downstream plan(25-02, 25-03)이 의존할 기반 타입/상수/유틸과 i18n 키를 확립한다. 계산 규칙의 파서, 프리셋 ↔ 공식 양방향 변환, 공식 검증, cascade 정리, 한국어 친화적 렌더 등 **순수 함수 레이어**를 완성한다.

Purpose: Interface-First 원칙으로 컴포넌트(25-02)와 미리보기 통합(25-03)이 타입 불일치 없이 병렬 안전하게 진행될 수 있도록 계약 레이어를 고정한다. 런타임 엔진(executeCalculations/detectCircularDeps)은 건드리지 않고 **정규식만 복제**하여 admin UI 전용 유틸로 캡슐화한다.
Output: 확장된 types.ts/constants.ts + 신규 calculationRuleUtils.ts + ko/en i18n 키 + baseline build green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-calculation-rule-ui/25-CONTEXT.md
@.planning/phases/25-calculation-rule-ui/25-RESEARCH.md
@frontend/src/features/document/types/dynamicForm.ts
@frontend/src/features/document/utils/executeCalculations.ts
@frontend/src/features/document/utils/detectCircularDeps.ts
@frontend/src/features/admin/components/SchemaFieldEditor/types.ts
@frontend/src/features/admin/components/SchemaFieldEditor/constants.ts
@frontend/src/features/admin/components/SchemaFieldEditor/conditionalRuleUtils.ts

<interfaces>
<!-- Downstream 이 사용할 계약 -->

From frontend/src/features/document/types/dynamicForm.ts:
```typescript
export interface CalculationRule {
  targetFieldId: string;
  formula: string;
  dependsOn: string[];
}

export interface SchemaDefinition {
  version: number;
  fields: FieldDefinition[];
  conditionalRules?: ConditionalRule[];
  calculationRules?: CalculationRule[];
}
```

From frontend/src/features/document/utils/executeCalculations.ts (재사용만, 수정 금지):
```typescript
// 참조: line 230 근처의 식별자 정규식
// /[a-zA-Z_]\w*(?:\.[a-zA-Z_]\w*)*/g
export function executeCalculations(
  rules: CalculationRule[],
  formValues: Record<string, unknown>
): Record<string, number>;
```

From frontend/src/features/document/utils/detectCircularDeps.ts (재사용만):
```typescript
export function detectCircularDeps(rules: CalculationRule[]): string[][];
// self-loop 도 감지 (path.indexOf 로직, line 42-50)
```

From frontend/src/features/admin/components/SchemaFieldEditor/types.ts (기존):
```typescript
export type SchemaFieldType = 'text' | 'textarea' | 'number' | 'date' | 'select' | 'staticText' | 'hidden' | 'table';
export interface SchemaField { id: string; type: SchemaFieldType; label: string; required: boolean; config: SchemaFieldConfig; }
export interface SchemaFieldConfig {
  // ... columns?: TableColumn[] 포함
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: types.ts / constants.ts 확장 + i18n 키 추가</name>
  <files>
    frontend/src/features/admin/components/SchemaFieldEditor/types.ts
    frontend/src/features/admin/components/SchemaFieldEditor/constants.ts
    frontend/public/locales/ko/admin.json
    frontend/public/locales/en/admin.json
  </files>
  <read_first>
    frontend/src/features/admin/components/SchemaFieldEditor/types.ts
    frontend/src/features/admin/components/SchemaFieldEditor/constants.ts
    frontend/public/locales/ko/admin.json
    frontend/public/locales/en/admin.json
    frontend/src/features/document/types/dynamicForm.ts
  </read_first>
  <action>
**1. types.ts 확장** (기존 내용 유지, 하단에 추가):

```typescript
// ─── Phase 25: 계산 규칙 UI 전용 helper 타입 (D-13) ───
// CalculationRule 본체는 document/types/dynamicForm.ts 의 것을 재사용한다.

export type PresetType = 'sum-col' | 'sum-mul' | 'field-sum' | 'ratio' | 'custom';

export interface PresetConfig {
  type: PresetType;
  /**
   * 프리셋별 파라미터 (버킷):
   * - sum-col:   { tableId: string; columnId: string }
   * - sum-mul:   { tableId: string; columnA: string; columnB: string }
   * - field-sum: { fieldIds: string[] }
   * - ratio:     { numerator: string; denominator: string } // 내부적으로 * 100 고정
   * - custom:    { raw: string }
   */
  params: Record<string, unknown>;
}

/**
 * 계산 규칙의 소스 후보 (D-20).
 * columnId 가 있으면 table 필드 내부 number 컬럼, 없으면 루트 number 필드.
 */
export interface CalcSource {
  fieldId: string;
  columnId?: string;
  label: string; // UI 표시용
}

export type CalcValidationError =
  | 'emptyFormula'
  | 'syntaxError'
  | 'unknownField'
  | 'unknownColumn'
  | 'invalidOperator'
  | 'selfReference'
  | 'circularDependency';
```

**2. constants.ts 확장** (기존 유지, 하단에 추가):

```typescript
import type { PresetType } from './types';

// D-19: 타겟은 루트 number 만
export const CALC_TARGET_TYPES: SchemaFieldType[] = ['number'];

// D-20: 소스는 루트 number + table 내부 number 컬럼
export const CALC_SOURCE_ROOT_TYPES: SchemaFieldType[] = ['number'];

// D-23: 정규식 추출 시 예약어로 제거
export const CALC_RESERVED_WORDS = new Set<string>(['SUM']);

// D-15/D-16: 프리셋 4종 메타
export interface PresetOption {
  type: PresetType;
  labelKey: string; // i18n: templates.calculation.presets.*
  /** 사용자 표시용 포맷 예시 */
  exampleKey: string;
}

export const PRESET_OPTIONS: PresetOption[] = [
  { type: 'sum-col',   labelKey: 'templates.calculation.presets.sumCol',   exampleKey: 'templates.calculation.presets.sumColExample' },
  { type: 'sum-mul',   labelKey: 'templates.calculation.presets.sumMul',   exampleKey: 'templates.calculation.presets.sumMulExample' },
  { type: 'field-sum', labelKey: 'templates.calculation.presets.fieldSum', exampleKey: 'templates.calculation.presets.fieldSumExample' },
  { type: 'ratio',     labelKey: 'templates.calculation.presets.ratio',    exampleKey: 'templates.calculation.presets.ratioExample' },
];
```

주의: `SchemaFieldType` import 가 이미 있는지 확인, 없으면 `import type { SchemaFieldType } from './types';` 추가. (Phase 24 Plan 01 에서 이미 import 되어 있을 가능성 높음.)

**3. i18n 키 추가 — ko/admin.json**

기존 `"templates"` 객체 내부(`"condition"` 옆)에 `"calculation"` 키 추가:

```json
"calculation": {
  "sectionTitle": "계산 규칙",
  "noRule": "계산 규칙이 없습니다",
  "addRule": "계산 추가",
  "deleteRule": "계산 삭제",
  "presetMode": "프리셋",
  "advancedMode": "고급 모드",
  "advancedPlaceholder": "예: SUM(items.price * items.qty) * 1.1",
  "noSourceFields": "계산 소스로 사용 가능한 숫자 필드가 없습니다",
  "rulesAutoRemoved": "계산 규칙 {{count}}개가 자동 제거되었습니다",
  "validationError": "계산 규칙에 오류가 있습니다",
  "circularSaveBlocked": "계산 규칙에 순환 의존성이 있습니다",
  "targetBadgePrefix": "=",
  "presets": {
    "sumCol": "컬럼 합계",
    "sumColExample": "SUM(items.price)",
    "sumMul": "컬럼 곱 합계",
    "sumMulExample": "SUM(items.price × items.qty)",
    "fieldSum": "필드 합",
    "fieldSumExample": "A + B + C",
    "ratio": "비율",
    "ratioExample": "A ÷ B × 100",
    "selectTable": "표 선택",
    "selectColumn": "컬럼 선택",
    "selectColumnA": "컬럼 A",
    "selectColumnB": "컬럼 B",
    "selectFields": "필드 선택",
    "numerator": "분자",
    "denominator": "분모"
  },
  "errors": {
    "emptyFormula": "공식이 비어 있습니다",
    "syntaxError": "공식 구문 오류 (괄호 불일치 등)",
    "unknownField": "존재하지 않는 필드 참조",
    "unknownColumn": "존재하지 않는 컬럼 또는 숫자 타입 아님",
    "invalidOperator": "잘못된 연산자",
    "selfReference": "자기 자신을 참조할 수 없습니다",
    "circularDependency": "순환 참조 감지"
  }
}
```

**4. i18n 키 추가 — en/admin.json** (동일 구조, 영어 번역)

```json
"calculation": {
  "sectionTitle": "Calculation Rules",
  "noRule": "No calculation rule",
  "addRule": "Add Calculation",
  "deleteRule": "Delete Calculation",
  "presetMode": "Preset",
  "advancedMode": "Advanced",
  "advancedPlaceholder": "e.g. SUM(items.price * items.qty) * 1.1",
  "noSourceFields": "No numeric fields available as calculation source",
  "rulesAutoRemoved": "{{count}} calculation rule(s) auto-removed",
  "validationError": "Calculation rules have errors",
  "circularSaveBlocked": "Calculation rules have circular dependency",
  "targetBadgePrefix": "=",
  "presets": {
    "sumCol": "Column Sum",
    "sumColExample": "SUM(items.price)",
    "sumMul": "Column Product Sum",
    "sumMulExample": "SUM(items.price × items.qty)",
    "fieldSum": "Field Sum",
    "fieldSumExample": "A + B + C",
    "ratio": "Ratio",
    "ratioExample": "A ÷ B × 100",
    "selectTable": "Select table",
    "selectColumn": "Select column",
    "selectColumnA": "Column A",
    "selectColumnB": "Column B",
    "selectFields": "Select fields",
    "numerator": "Numerator",
    "denominator": "Denominator"
  },
  "errors": {
    "emptyFormula": "Formula is empty",
    "syntaxError": "Formula syntax error (unbalanced parens, etc.)",
    "unknownField": "Unknown field reference",
    "unknownColumn": "Unknown column or not a numeric type",
    "invalidOperator": "Invalid operator",
    "selfReference": "Cannot reference the target field itself",
    "circularDependency": "Circular dependency detected"
  }
}
```
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - types.ts contains `export type PresetType`
    - types.ts contains `export interface PresetConfig`
    - types.ts contains `export interface CalcSource`
    - types.ts contains `export type CalcValidationError`
    - constants.ts contains `export const PRESET_OPTIONS`
    - constants.ts contains `export const CALC_RESERVED_WORDS`
    - constants.ts contains `'sum-col'` (프리셋 타입 문자열)
    - ko/admin.json contains `"calculation"` key under `"templates"`
    - ko/admin.json contains `"sectionTitle": "계산 규칙"`
    - ko/admin.json contains `"circularDependency": "순환 참조 감지"`
    - en/admin.json contains `"calculation"` key with `"sectionTitle": "Calculation Rules"`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>types/constants/i18n 확장이 완료되고 TypeScript 빌드가 통과한다.</done>
</task>

<task type="auto">
  <name>Task 2: calculationRuleUtils.ts 신설 (파서/프리셋/검증/cascade/렌더)</name>
  <files>
    frontend/src/features/admin/components/SchemaFieldEditor/calculationRuleUtils.ts
  </files>
  <read_first>
    frontend/src/features/admin/components/SchemaFieldEditor/types.ts
    frontend/src/features/admin/components/SchemaFieldEditor/constants.ts
    frontend/src/features/admin/components/SchemaFieldEditor/conditionalRuleUtils.ts
    frontend/src/features/document/types/dynamicForm.ts
    frontend/src/features/document/utils/executeCalculations.ts
  </read_first>
  <action>
**`calculationRuleUtils.ts` 신규 파일을 생성**한다. `conditionalRuleUtils.ts` 와 **1:1 대응** 구조로 작성한다.

```typescript
import type { CalculationRule } from '../../../document/types/dynamicForm';
// 실제 import 경로는 파일 위치에 따라 상대경로로 조정할 것.
import type { SchemaField, PresetConfig, PresetType, CalcSource, CalcValidationError } from './types';
import { CALC_RESERVED_WORDS } from './constants';

/**
 * D-23: 공식에서 필드/컬럼 레퍼런스 추출.
 * Source-synced with executeCalculations.ts:230 의 정규식.
 * 런타임 경로(executeCalculations.ts)를 수정하지 않기 위해 복제한다.
 *
 * Pitfall 7 대응: 자기참조도 여기서 추출되므로 validateFormula 에서
 * selfReference 로 별도 분기 가능 (detectCircularDeps 도 self-loop 감지하지만 조기 피드백).
 */
const FIELD_REF_PATTERN = /[a-zA-Z_]\w*(?:\.[a-zA-Z_]\w*)*/g;

export function extractDependencies(formula: string): string[] {
  if (!formula) return [];
  const refs = new Set<string>();
  // 정규식 state 초기화 (global flag 공유 방지)
  FIELD_REF_PATTERN.lastIndex = 0;
  let m: RegExpExecArray | null;
  while ((m = FIELD_REF_PATTERN.exec(formula)) !== null) {
    const token = m[0];
    if (CALC_RESERVED_WORDS.has(token)) continue;
    if (/^\d/.test(token)) continue; // 숫자 리터럴 방어
    refs.add(token);
  }
  return Array.from(refs);
}

/**
 * D-15, D-16: PresetConfig → formula 문자열.
 * 저장 포맷은 executeCalculations 가 이해하는 형태 (`*`, `/`).
 */
export function buildFormulaFromPreset(preset: PresetConfig): string {
  const p = preset.params as Record<string, string | string[]>;
  switch (preset.type) {
    case 'sum-col':
      return `SUM(${p.tableId}.${p.columnId})`;
    case 'sum-mul':
      return `SUM(${p.tableId}.${p.columnA} * ${p.tableId}.${p.columnB})`;
    case 'field-sum': {
      const ids = (p.fieldIds as string[]) ?? [];
      return ids.join(' + ');
    }
    case 'ratio':
      return `${p.numerator} / ${p.denominator} * 100`;
    case 'custom':
      return (p.raw as string) ?? '';
    default:
      return '';
  }
}

/**
 * D-18: 역방향 복원. 엄격 매칭, 실패 시 'custom'.
 * Pitfall 6 대응: 호출자는 이 결과를 useMemo 로 파생 상태로만 유지해야 한다.
 */
export function parseFormulaToPreset(formula: string): PresetConfig {
  const trimmed = formula.trim();

  // sum-mul: SUM(X.a * X.b)  (동일 tableId)
  const mulMatch = /^SUM\(\s*(\w+)\.(\w+)\s*\*\s*(\w+)\.(\w+)\s*\)$/.exec(trimmed);
  if (mulMatch && mulMatch[1] === mulMatch[3]) {
    return { type: 'sum-mul', params: { tableId: mulMatch[1], columnA: mulMatch[2], columnB: mulMatch[4] } };
  }

  // sum-col: SUM(X.a)
  const colMatch = /^SUM\(\s*(\w+)\.(\w+)\s*\)$/.exec(trimmed);
  if (colMatch) {
    return { type: 'sum-col', params: { tableId: colMatch[1], columnId: colMatch[2] } };
  }

  // ratio: A / B * 100 (정확히 *100 일 때만)
  const ratioMatch = /^(\w+)\s*\/\s*(\w+)\s*\*\s*100$/.exec(trimmed);
  if (ratioMatch) {
    return { type: 'ratio', params: { numerator: ratioMatch[1], denominator: ratioMatch[2] } };
  }

  // field-sum: A + B (+ C ...), 각 토큰은 단순 식별자만
  const sumMatch = /^\w+(\s*\+\s*\w+)+$/.exec(trimmed);
  if (sumMatch) {
    return { type: 'field-sum', params: { fieldIds: trimmed.split(/\s*\+\s*/) } };
  }

  return { type: 'custom', params: { raw: formula } };
}

/**
 * D-20, D-21: 타겟 필드에서 사용 가능한 계산 소스 목록.
 * - 루트 number 필드 (자기 자신 제외)
 * - table 필드 내부 number 컬럼 (`fieldId.columnId`)
 */
export function getAvailableCalcSources(
  targetFieldId: string,
  allFields: SchemaField[],
): CalcSource[] {
  const sources: CalcSource[] = [];
  for (const f of allFields) {
    if (f.id === targetFieldId) continue;
    if (f.type === 'number') {
      sources.push({ fieldId: f.id, label: f.label || f.id });
    } else if (f.type === 'table') {
      const cols = (f.config as { columns?: Array<{ id: string; label: string; type: string }> }).columns || [];
      for (const col of cols) {
        if (col.type === 'number') {
          sources.push({
            fieldId: f.id,
            columnId: col.id,
            label: `${f.label || f.id}.${col.label || col.id}`,
          });
        }
      }
    }
  }
  return sources;
}

/**
 * D-10, D-26, D-27: 공식 유효성 검증.
 * 반환: 에러 코드 (i18n key 에 매핑) 또는 null.
 */
export function validateFormula(
  formula: string,
  allFields: SchemaField[],
  targetFieldId: string,
): CalcValidationError | null {
  if (!formula.trim()) return 'emptyFormula';

  // 괄호 매칭
  let depth = 0;
  for (const ch of formula) {
    if (ch === '(') depth++;
    else if (ch === ')') depth--;
    if (depth < 0) return 'syntaxError';
  }
  if (depth !== 0) return 'syntaxError';

  const deps = extractDependencies(formula);

  // Pitfall 7: 자기참조 조기 차단
  if (deps.includes(targetFieldId)) return 'selfReference';

  for (const dep of deps) {
    const [fieldId, columnId] = dep.split('.');
    const field = allFields.find(f => f.id === fieldId);
    if (!field) return 'unknownField';

    if (columnId !== undefined) {
      if (field.type !== 'table') return 'unknownField';
      const cols = (field.config as { columns?: Array<{ id: string; type: string }> }).columns || [];
      const col = cols.find(c => c.id === columnId);
      if (!col) return 'unknownColumn';
      if (col.type !== 'number') return 'unknownColumn';
    } else {
      if (field.type !== 'number') return 'unknownField';
    }
  }
  return null;
}

/**
 * D-06, D-07: 필드 삭제 시 관련 계산 규칙 정리 (양방향).
 * 타겟 또는 dependsOn 에 삭제 필드가 포함되면 규칙 제거.
 * table 필드 참조 `items.price` 도 `items` 삭제 시 함께 정리.
 */
export function cleanupCalcRulesForDeletedField(
  deletedFieldId: string,
  rules: CalculationRule[],
): [CalculationRule[], number] {
  const before = rules.length;
  const cleaned = rules.filter(r => {
    if (r.targetFieldId === deletedFieldId) return false;
    const refsDeleted = r.dependsOn.some(
      d => d === deletedFieldId || d.startsWith(`${deletedFieldId}.`),
    );
    return !refsDeleted;
  });
  return [cleaned, before - cleaned.length];
}

/**
 * D-08: 필드 타입 변경 시 관련 계산 규칙 정리.
 * - 새 타입이 number 가 아니게 되면 해당 필드가 타겟이거나 루트 소스인 규칙 모두 제거.
 * - table 로 남아있는 경우 내부 컬럼은 영향 없음 (Pitfall 3 는 호출자에서 처리).
 */
export function cleanupCalcRulesForTypeChange(
  changedFieldId: string,
  newType: string,
  rules: CalculationRule[],
): [CalculationRule[], number] {
  const before = rules.length;
  let cleaned = [...rules];

  if (newType !== 'number') {
    // 타겟으로 사용된 규칙 제거
    cleaned = cleaned.filter(r => r.targetFieldId !== changedFieldId);
    // 루트 소스로 참조하는 규칙 제거 (table.col 형식은 table 타입 변경이 아니므로 여기서 제외)
    cleaned = cleaned.filter(r => !r.dependsOn.includes(changedFieldId));
  }

  return [cleaned, before - cleaned.length];
}

/**
 * Pitfall 3 대응 helper: table 필드의 특정 컬럼 id 가 number 에서 다른 타입으로 변경되었을 때
 * 해당 컬럼을 참조하는 규칙 제거. 호출자는 이전/이후 columns 를 비교해 changedColumnIds 를 전달.
 */
export function cleanupCalcRulesForTableColumnChange(
  tableFieldId: string,
  changedColumnIds: string[],
  rules: CalculationRule[],
): [CalculationRule[], number] {
  if (changedColumnIds.length === 0) return [rules, 0];
  const before = rules.length;
  const refs = new Set(changedColumnIds.map(cid => `${tableFieldId}.${cid}`));
  const cleaned = rules.filter(r => {
    if (r.dependsOn.some(d => refs.has(d))) return false;
    return true;
  });
  return [cleaned, before - cleaned.length];
}

/**
 * D-25: 공식 1줄 미리보기 렌더 — `*`→`×`, `/`→`÷`.
 * 저장 값은 원본을 유지하고 표시용 변환만 수행한다.
 */
export function renderFormulaFriendly(formula: string): string {
  return formula.replace(/\*/g, '×').replace(/\//g, '÷');
}
```

주의사항:
- import 상대경로는 실제 파일 위치에 따라 조정. `conditionalRuleUtils.ts` 와 동일 디렉터리이므로 `../../../document/types/dynamicForm` 이 유효할 것. 먼저 `conditionalRuleUtils.ts` 의 import 경로를 참고한다.
- `executeCalculations.ts` 는 절대 수정하지 않는다. 정규식 복제만 수행한다.
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npx tsc --noEmit 2>&1 | tail -30 && test -f src/features/admin/components/SchemaFieldEditor/calculationRuleUtils.ts</automated>
  </verify>
  <acceptance_criteria>
    - calculationRuleUtils.ts exists
    - exports `extractDependencies`
    - exports `buildFormulaFromPreset`
    - exports `parseFormulaToPreset`
    - exports `validateFormula`
    - exports `getAvailableCalcSources`
    - exports `cleanupCalcRulesForDeletedField`
    - exports `cleanupCalcRulesForTypeChange`
    - exports `cleanupCalcRulesForTableColumnChange`
    - exports `renderFormulaFriendly`
    - contains `Source-synced with executeCalculations` comment
    - contains `FIELD_REF_PATTERN = /[a-zA-Z_]\\w*`
    - `executeCalculations.ts` 파일이 이 commit 에서 수정되지 않았음 (`git diff --stat frontend/src/features/document/utils/executeCalculations.ts` 은 비어있어야 함)
    - `detectCircularDeps.ts` 도 미수정
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>calculationRuleUtils.ts 가 9개 함수를 export 하고, 런타임 파일은 미변경 상태로 TypeScript 빌드가 통과한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin UI (프론트) → schemaDefinition JSON | 관리자 입력이 JSON 으로 직렬화되어 백엔드에 저장. 본 Plan 은 유틸/타입 레이어로 아직 boundary 를 넘지 않음. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | calculationRuleUtils::extractDependencies 정규식 | mitigate | executeCalculations.ts:230 과 **문자 단위로 동일한 정규식** 사용 (`/[a-zA-Z_]\w*(?:\.[a-zA-Z_]\w*)*/g`). 주석 `Source-synced with executeCalculations.ts:230` 로 drift 가능성 명시. 향후 런타임 정규식 변경 시 본 util 도 동기화 필요. (Pitfall 참조) |
| T-25-02 | Denial of Service | extractDependencies 무한 루프 | mitigate | global regex 의 `lastIndex` 를 호출 초기에 0 으로 리셋 (JS global flag 공유 버그 방어). Pitfall 5 와 별개 항목. |
| T-25-03 | Information Disclosure | i18n 키 노출 | accept | admin 전용 번역 키, 민감 정보 없음. |
</threat_model>

<verification>
1. `npx tsc --noEmit` 통과
2. calculationRuleUtils.ts 의 9개 export 가 올바른 시그니처로 존재
3. executeCalculations.ts, detectCircularDeps.ts 가 미수정
4. ko/en i18n 키가 templates.calculation.* 네임스페이스에 추가됨
5. PRESET_OPTIONS 가 4종을 정확히 포함
</verification>

<success_criteria>
- Phase 25 의 downstream plan(25-02, 25-03)이 본 plan 의 타입/유틸만 import 하면 되도록 계약이 완성됨
- 런타임 파일(executeCalculations.ts, detectCircularDeps.ts, DynamicCustomForm.tsx)은 미변경
- TypeScript 빌드 green
</success_criteria>

<output>
After completion, create `.planning/phases/25-calculation-rule-ui/25-01-SUMMARY.md`
</output>
</content>
</invoke>