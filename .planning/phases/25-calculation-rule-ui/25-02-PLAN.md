---
phase: 25-calculation-rule-ui
plan: "02"
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - frontend/src/features/admin/components/SchemaFieldEditor/CalculationRuleEditor.tsx
  - frontend/src/features/admin/components/SchemaFieldEditor/FieldConfigEditor.tsx
  - frontend/src/features/admin/components/SchemaFieldEditor/FieldCard.tsx
  - frontend/src/features/admin/components/SchemaFieldEditor/SchemaFieldEditor.tsx
  - frontend/src/features/admin/components/SchemaFieldEditor/types.ts
  - frontend/src/features/admin/components/TemplateFormModal.tsx
autonomous: true
requirements:
  - CAL-01
  - CAL-02

must_haves:
  truths:
    - "CalculationRuleEditor.tsx 가 프리셋 모드/고급 모드 토글과 순환 경고 배너를 렌더링한다"
    - "FieldConfigEditor 또는 FieldCard 에서 field.type === 'number' 일 때 '계산 규칙' 접기/펼치기 섹션이 나타난다 (D-01, D-19)"
    - "FieldCard 헤더에 Σ 배지 + 공식 1줄 미리보기(× ÷ 친화적 렌더)가 표시된다 (D-25)"
    - "SchemaFieldEditor 가 calculationRules prop + onCalculationRulesChange 를 수신하고, updateField/deleteField cascade 를 호출한다"
    - "SchemaFieldEditor 가 detectCircularDeps(calculationRules) 결과를 useMemo 로 계산해 FieldCard 에 cycles 로 전달한다"
    - "TemplateFormModal 이 calculationRules useState 를 추가하고 line 157 하드코딩 `calculationRules: []` 를 제거한다 (Pitfall 1)"
    - "TemplateFormModal 이 편집 모드에서 schema.calculationRules 를 로드한다 (Pitfall 1)"
    - "TemplateFormModal 저장 시 validateFormula + detectCircularDeps 검증 + 실패 시 토스트 + 저장 차단 (D-11, D-24)"
    - "필드 삭제/타입 변경/table 컬럼 타입 변경 시 관련 계산 규칙이 자동 제거되고 토스트가 표시된다 (D-06, D-08, Pitfall 3)"
  artifacts:
    - path: "frontend/src/features/admin/components/SchemaFieldEditor/CalculationRuleEditor.tsx"
      provides: "계산 규칙 편집 UI 컴포넌트 (프리셋 + 고급 + 순환 배너)"
      min_lines: 150
    - path: "frontend/src/features/admin/components/TemplateFormModal.tsx"
      provides: "calculationRules 상태 관리 + 저장 검증 + 하드코딩 제거"
      contains: "calculationRules"
  key_links:
    - from: "SchemaFieldEditor.tsx"
      to: "calculationRuleUtils.ts"
      via: "import cleanupCalcRulesForDeletedField, cleanupCalcRulesForTypeChange, cleanupCalcRulesForTableColumnChange"
      pattern: "cleanupCalcRulesForDeletedField"
    - from: "CalculationRuleEditor.tsx"
      to: "calculationRuleUtils.ts"
      via: "import buildFormulaFromPreset, parseFormulaToPreset, validateFormula, extractDependencies, getAvailableCalcSources"
      pattern: "buildFormulaFromPreset"
    - from: "TemplateFormModal.tsx"
      to: "detectCircularDeps + validateFormula"
      via: "onSubmit 검증 + 저장 버튼 disabled 파생"
      pattern: "detectCircularDeps"
    - from: "FieldCard.tsx"
      to: "CalculationRuleEditor.tsx"
      via: "render inside collapsible section when field.type === 'number'"
      pattern: "CalculationRuleEditor"

threat_refs:
  - T-25-04
  - T-25-05
  - T-25-06
---

<objective>
Phase 25 의 편집 UI 레이어를 완성한다. 25-01 의 타입/유틸을 consume 하여 `CalculationRuleEditor` 컴포넌트를 신설하고, `FieldCard`/`FieldConfigEditor`/`SchemaFieldEditor`/`TemplateFormModal` 을 확장하여 관리자가 number 필드에 계산 규칙을 설정/수정/삭제/저장할 수 있도록 한다. Phase 24 의 `ConditionalRuleEditor` / `conditionalRuleUtils` 통합 패턴과 **1:1 대응 구조**로 작성한다.

Purpose: 관리자가 직관적인 프리셋 UI 또는 고급 자유입력 모드로 공식을 작성하고, 순환 참조를 즉시 경고받으며, 저장/로드/cascade 정리가 안전하게 동작하도록 한다.
Output: CalculationRuleEditor 컴포넌트 + FieldCard/SchemaFieldEditor/TemplateFormModal 통합 + Pitfall 1~3, 6, 7 방어.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-calculation-rule-ui/25-CONTEXT.md
@.planning/phases/25-calculation-rule-ui/25-RESEARCH.md
@.planning/phases/25-calculation-rule-ui/25-01-SUMMARY.md
@frontend/src/features/document/types/dynamicForm.ts
@frontend/src/features/document/utils/detectCircularDeps.ts
@frontend/src/features/admin/components/SchemaFieldEditor/ConditionalRuleEditor.tsx
@frontend/src/features/admin/components/SchemaFieldEditor/conditionalRuleUtils.ts

<interfaces>
<!-- 25-01 outputs -->
From frontend/src/features/admin/components/SchemaFieldEditor/calculationRuleUtils.ts:
```typescript
export function extractDependencies(formula: string): string[];
export function buildFormulaFromPreset(preset: PresetConfig): string;
export function parseFormulaToPreset(formula: string): PresetConfig;
export function validateFormula(formula: string, fields: SchemaField[], targetFieldId: string): CalcValidationError | null;
export function getAvailableCalcSources(targetFieldId: string, allFields: SchemaField[]): CalcSource[];
export function cleanupCalcRulesForDeletedField(id: string, rules: CalculationRule[]): [CalculationRule[], number];
export function cleanupCalcRulesForTypeChange(id: string, newType: string, rules: CalculationRule[]): [CalculationRule[], number];
export function cleanupCalcRulesForTableColumnChange(tableId: string, changedCols: string[], rules: CalculationRule[]): [CalculationRule[], number];
export function renderFormulaFriendly(formula: string): string;
```

From frontend/src/features/admin/components/SchemaFieldEditor/types.ts:
```typescript
export type PresetType = 'sum-col' | 'sum-mul' | 'field-sum' | 'ratio' | 'custom';
export interface PresetConfig { type: PresetType; params: Record<string, unknown>; }
export interface CalcSource { fieldId: string; columnId?: string; label: string; }
```

From frontend/src/features/document/types/dynamicForm.ts:
```typescript
export interface CalculationRule { targetFieldId: string; formula: string; dependsOn: string[]; }
```

From frontend/src/features/document/utils/detectCircularDeps.ts:
```typescript
export function detectCircularDeps(rules: CalculationRule[]): string[][];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CalculationRuleEditor 컴포넌트 신설 + SchemaFieldEditorProps 확장</name>
  <files>
    frontend/src/features/admin/components/SchemaFieldEditor/CalculationRuleEditor.tsx
    frontend/src/features/admin/components/SchemaFieldEditor/types.ts
  </files>
  <read_first>
    frontend/src/features/admin/components/SchemaFieldEditor/ConditionalRuleEditor.tsx
    frontend/src/features/admin/components/SchemaFieldEditor/calculationRuleUtils.ts
    frontend/src/features/admin/components/SchemaFieldEditor/constants.ts
    frontend/src/features/admin/components/SchemaFieldEditor/types.ts
    frontend/src/features/document/types/dynamicForm.ts
  </read_first>
  <action>
**1. SchemaFieldEditorProps 확장 (types.ts)**

기존 `SchemaFieldEditorProps` 에 다음 2개 필드 추가 (Phase 24 에서 이미 conditionalRules 확장되었을 것):

```typescript
export interface SchemaFieldEditorProps {
  fields: SchemaField[];
  onChange: (fields: SchemaField[]) => void;
  conditionalRules: ConditionalRule[];
  onConditionalRulesChange: (rules: ConditionalRule[]) => void;
  // Phase 25 추가
  calculationRules: CalculationRule[];
  onCalculationRulesChange: (rules: CalculationRule[]) => void;
}
```

`CalculationRule` import 추가: `import type { CalculationRule } from '../../../document/types/dynamicForm';` (ConditionalRule 과 같은 경로).

**2. CalculationRuleEditor.tsx 신규 생성**

`ConditionalRuleEditor.tsx` 를 base 로 복제하되, 드롭다운 조합 대신 **프리셋 모드 + 고급 모드 토글** 구조로 작성한다.

```typescript
import { useState, useMemo, useEffect } from 'react';
import { useTranslation } from 'react-i18next';
import { Plus, Trash2, Sigma } from 'lucide-react';
import { toast } from 'sonner';
import type { CalculationRule } from '../../../document/types/dynamicForm';
import type { SchemaField, PresetConfig, PresetType, CalcValidationError } from './types';
import { SMALL_INPUT_CLASS, PRESET_OPTIONS } from './constants';
import {
  extractDependencies,
  buildFormulaFromPreset,
  parseFormulaToPreset,
  validateFormula,
  getAvailableCalcSources,
  renderFormulaFriendly,
} from './calculationRuleUtils';

interface CalculationRuleEditorProps {
  targetFieldId: string;
  rule: CalculationRule | undefined;
  allFields: SchemaField[];
  allRules: CalculationRule[];
  /** D-24: 상위에서 detectCircularDeps 로 계산된 사이클 목록 */
  cycles: string[][];
  onAddRule: (rule: CalculationRule) => void;
  onUpdateRule: (rule: CalculationRule) => void;
  onDeleteRule: () => void;
}

export function CalculationRuleEditor({
  targetFieldId, rule, allFields, allRules, cycles,
  onAddRule, onUpdateRule, onDeleteRule,
}: CalculationRuleEditorProps) {
  const { t } = useTranslation('admin');
  const [isAdvanced, setIsAdvanced] = useState(false);
  const [rawInput, setRawInput] = useState(rule?.formula ?? '');
  const [blurError, setBlurError] = useState<CalcValidationError | null>(null);

  // rule.formula 외부 변경 시 rawInput 동기화
  useEffect(() => {
    setRawInput(rule?.formula ?? '');
    setBlurError(null);
  }, [rule?.formula]);

  // Pitfall 6: PresetConfig 는 파생 상태 (useMemo)
  const presetConfig = useMemo(
    () => (rule ? parseFormulaToPreset(rule.formula) : null),
    [rule],
  );

  // D-24: 이 타겟 필드에 관련된 사이클만 필터
  const myCycles = useMemo(
    () => cycles.filter(c => c.includes(targetFieldId)),
    [cycles, targetFieldId],
  );

  const sources = useMemo(
    () => getAvailableCalcSources(targetFieldId, allFields),
    [targetFieldId, allFields],
  );

  const handleAdd = () => {
    if (sources.length === 0) {
      toast(t('templates.calculation.noSourceFields'));
      return;
    }
    // 기본값: 첫 table 컬럼이 있으면 sum-col, 없으면 field-sum 으로 첫 소스 1개
    const firstTable = sources.find(s => s.columnId);
    const defaultFormula = firstTable
      ? `SUM(${firstTable.fieldId}.${firstTable.columnId})`
      : sources[0].fieldId;
    onAddRule({
      targetFieldId,
      formula: defaultFormula,
      dependsOn: extractDependencies(defaultFormula),
    });
  };

  if (!rule) {
    return (
      <div className="text-sm text-gray-400 dark:text-gray-500">
        <p>{t('templates.calculation.noRule')}</p>
        <button
          type="button"
          onClick={handleAdd}
          className="mt-2 inline-flex items-center gap-1 text-sm text-blue-600 dark:text-blue-400 hover:text-blue-800 dark:hover:text-blue-300"
        >
          <Plus className="w-3.5 h-3.5" />
          {t('templates.calculation.addRule')}
        </button>
      </div>
    );
  }

  const updateFormula = (newFormula: string) => {
    onUpdateRule({
      ...rule,
      formula: newFormula,
      dependsOn: extractDependencies(newFormula),
    });
  };

  // D-26: blur 시 파싱 검증
  const handleAdvancedBlur = () => {
    const err = validateFormula(rawInput, allFields, targetFieldId);
    setBlurError(err);
    if (!err) {
      updateFormula(rawInput);
    }
  };

  // 프리셋 파라미터 변경 시 즉시 공식 재빌드
  const updateFromPreset = (next: PresetConfig) => {
    updateFormula(buildFormulaFromPreset(next));
  };

  return (
    <div className="space-y-3">
      {/* 모드 토글 (Claude's Discretion: 텍스트 링크 스타일) */}
      <div className="flex items-center gap-3 text-xs">
        <button
          type="button"
          onClick={() => setIsAdvanced(false)}
          className={!isAdvanced ? 'font-semibold text-blue-600 dark:text-blue-400' : 'text-gray-500'}
        >
          {t('templates.calculation.presetMode')}
        </button>
        <span className="text-gray-300">|</span>
        <button
          type="button"
          onClick={() => setIsAdvanced(true)}
          className={isAdvanced ? 'font-semibold text-blue-600 dark:text-blue-400' : 'text-gray-500'}
        >
          {t('templates.calculation.advancedMode')}
        </button>
        <button
          type="button"
          onClick={onDeleteRule}
          title={t('templates.calculation.deleteRule')}
          className="ml-auto p-1 text-gray-400 hover:text-red-500 dark:hover:text-red-400 hover:bg-red-50 dark:hover:bg-red-900/20 rounded"
        >
          <Trash2 className="w-4 h-4" />
        </button>
      </div>

      {/* 프리셋 영역 */}
      {!isAdvanced && presetConfig && (
        <PresetSelector
          config={presetConfig}
          allFields={allFields}
          sources={sources}
          onConfigChange={updateFromPreset}
        />
      )}

      {/* 고급 모드 영역 */}
      {isAdvanced && (
        <div>
          <input
            type="text"
            value={rawInput}
            onChange={(e) => setRawInput(e.target.value)}
            onBlur={handleAdvancedBlur}
            className={`${SMALL_INPUT_CLASS} font-mono ${blurError ? 'border-red-500' : ''}`}
            placeholder={t('templates.calculation.advancedPlaceholder')}
          />
          {blurError && (
            <p className="mt-1 text-xs text-red-600 dark:text-red-400">
              {t(`templates.calculation.errors.${blurError}`)}
            </p>
          )}
        </div>
      )}

      {/* 1줄 공식 미리보기 (D-25) */}
      <div className="flex items-center gap-1.5 text-xs font-mono text-gray-600 dark:text-gray-400 bg-gray-50 dark:bg-gray-800 px-2 py-1 rounded">
        <Sigma className="w-3.5 h-3.5 shrink-0" />
        <span className="truncate">= {renderFormulaFriendly(rule.formula)}</span>
      </div>

      {/* D-24: 순환 경고 인라인 배너 */}
      {myCycles.length > 0 && (
        <div className="bg-red-50 dark:bg-red-900/20 border border-red-300 dark:border-red-800 rounded px-3 py-2 text-xs text-red-700 dark:text-red-300">
          ⚠ {t('templates.calculation.errors.circularDependency')}: {myCycles[0].join(' → ')}
        </div>
      )}
    </div>
  );
}

// ─── 프리셋별 파라미터 드롭다운 sub-component ───
interface PresetSelectorProps {
  config: PresetConfig;
  allFields: SchemaField[];
  sources: ReturnType<typeof getAvailableCalcSources>;
  onConfigChange: (next: PresetConfig) => void;
}

function PresetSelector({ config, allFields, sources, onConfigChange }: PresetSelectorProps) {
  const { t } = useTranslation('admin');

  // 프리셋 탭 선택 시 해당 타입에 맞는 기본 파라미터로 초기화
  const selectPreset = (type: PresetType) => {
    const firstTable = sources.find(s => s.columnId);
    const rootNums = sources.filter(s => !s.columnId);
    switch (type) {
      case 'sum-col':
        if (firstTable) {
          onConfigChange({ type, params: { tableId: firstTable.fieldId, columnId: firstTable.columnId } });
        }
        break;
      case 'sum-mul': {
        if (firstTable) {
          const tableCols = sources.filter(s => s.fieldId === firstTable.fieldId && s.columnId);
          const a = tableCols[0]?.columnId ?? '';
          const b = tableCols[1]?.columnId ?? tableCols[0]?.columnId ?? '';
          onConfigChange({ type, params: { tableId: firstTable.fieldId, columnA: a, columnB: b } });
        }
        break;
      }
      case 'field-sum':
        if (rootNums.length > 0) {
          onConfigChange({ type, params: { fieldIds: [rootNums[0].fieldId] } });
        }
        break;
      case 'ratio':
        if (rootNums.length >= 2) {
          onConfigChange({ type, params: { numerator: rootNums[0].fieldId, denominator: rootNums[1].fieldId } });
        } else if (rootNums.length === 1) {
          onConfigChange({ type, params: { numerator: rootNums[0].fieldId, denominator: rootNums[0].fieldId } });
        }
        break;
    }
  };

  // 탭 헤더
  return (
    <div className="space-y-2">
      <div className="flex flex-wrap gap-1">
        {PRESET_OPTIONS.map(opt => (
          <button
            key={opt.type}
            type="button"
            onClick={() => selectPreset(opt.type)}
            className={`px-2 py-1 text-xs rounded border transition-colors ${
              config.type === opt.type
                ? 'bg-blue-50 dark:bg-blue-900/30 border-blue-400 text-blue-700 dark:text-blue-300'
                : 'border-gray-300 dark:border-gray-600 text-gray-600 dark:text-gray-400 hover:bg-gray-50 dark:hover:bg-gray-700'
            }`}
          >
            {t(opt.labelKey)}
          </button>
        ))}
        {config.type === 'custom' && (
          <span className="px-2 py-1 text-xs rounded border border-amber-300 bg-amber-50 dark:bg-amber-900/20 text-amber-700 dark:text-amber-300">
            custom
          </span>
        )}
      </div>

      {/* 파라미터 입력 UI는 config.type 별 분기 */}
      <PresetParams config={config} sources={sources} allFields={allFields} onConfigChange={onConfigChange} />
    </div>
  );
}

function PresetParams({
  config, sources, allFields, onConfigChange,
}: {
  config: PresetConfig;
  sources: ReturnType<typeof getAvailableCalcSources>;
  allFields: SchemaField[];
  onConfigChange: (next: PresetConfig) => void;
}) {
  const { t } = useTranslation('admin');
  const tableSources = sources.filter(s => s.columnId);
  const rootNums = sources.filter(s => !s.columnId);
  const tableFieldIds = Array.from(new Set(tableSources.map(s => s.fieldId)));

  const update = (params: Record<string, unknown>) => {
    onConfigChange({ type: config.type, params: { ...config.params, ...params } });
  };

  if (config.type === 'sum-col') {
    const currentTable = (config.params.tableId as string) ?? '';
    const columnsInTable = tableSources.filter(s => s.fieldId === currentTable);
    return (
      <div className="grid grid-cols-2 gap-2">
        <select className={SMALL_INPUT_CLASS} value={currentTable}
          onChange={(e) => update({ tableId: e.target.value, columnId: '' })}>
          <option value="">{t('templates.calculation.presets.selectTable')}</option>
          {tableFieldIds.map(fid => {
            const field = allFields.find(f => f.id === fid);
            return <option key={fid} value={fid}>{field?.label ?? fid}</option>;
          })}
        </select>
        <select className={SMALL_INPUT_CLASS} value={(config.params.columnId as string) ?? ''}
          onChange={(e) => update({ columnId: e.target.value })}>
          <option value="">{t('templates.calculation.presets.selectColumn')}</option>
          {columnsInTable.map(s => (
            <option key={s.columnId} value={s.columnId}>{s.label}</option>
          ))}
        </select>
      </div>
    );
  }

  if (config.type === 'sum-mul') {
    const currentTable = (config.params.tableId as string) ?? '';
    const columnsInTable = tableSources.filter(s => s.fieldId === currentTable);
    return (
      <div className="grid grid-cols-3 gap-2">
        <select className={SMALL_INPUT_CLASS} value={currentTable}
          onChange={(e) => update({ tableId: e.target.value, columnA: '', columnB: '' })}>
          <option value="">{t('templates.calculation.presets.selectTable')}</option>
          {tableFieldIds.map(fid => {
            const field = allFields.find(f => f.id === fid);
            return <option key={fid} value={fid}>{field?.label ?? fid}</option>;
          })}
        </select>
        <select className={SMALL_INPUT_CLASS} value={(config.params.columnA as string) ?? ''}
          onChange={(e) => update({ columnA: e.target.value })}>
          <option value="">{t('templates.calculation.presets.selectColumnA')}</option>
          {columnsInTable.map(s => <option key={s.columnId} value={s.columnId}>{s.label}</option>)}
        </select>
        <select className={SMALL_INPUT_CLASS} value={(config.params.columnB as string) ?? ''}
          onChange={(e) => update({ columnB: e.target.value })}>
          <option value="">{t('templates.calculation.presets.selectColumnB')}</option>
          {columnsInTable.map(s => <option key={s.columnId} value={s.columnId}>{s.label}</option>)}
        </select>
      </div>
    );
  }

  if (config.type === 'field-sum') {
    const selected = (config.params.fieldIds as string[]) ?? [];
    return (
      <div className="max-h-40 overflow-y-auto border border-gray-300 dark:border-gray-600 rounded-lg p-2 space-y-1">
        {rootNums.length === 0 && (
          <p className="text-xs text-gray-400">{t('templates.calculation.noSourceFields')}</p>
        )}
        {rootNums.map(s => (
          <label key={s.fieldId} className="flex items-center gap-2 text-sm">
            <input
              type="checkbox"
              checked={selected.includes(s.fieldId)}
              onChange={(e) => {
                const next = e.target.checked
                  ? [...selected, s.fieldId]
                  : selected.filter(id => id !== s.fieldId);
                update({ fieldIds: next });
              }}
            />
            {s.label}
          </label>
        ))}
      </div>
    );
  }

  if (config.type === 'ratio') {
    return (
      <div className="grid grid-cols-2 gap-2">
        <div>
          <label className="block text-xs text-gray-500 mb-1">{t('templates.calculation.presets.numerator')}</label>
          <select className={SMALL_INPUT_CLASS} value={(config.params.numerator as string) ?? ''}
            onChange={(e) => update({ numerator: e.target.value })}>
            <option value="">—</option>
            {rootNums.map(s => <option key={s.fieldId} value={s.fieldId}>{s.label}</option>)}
          </select>
        </div>
        <div>
          <label className="block text-xs text-gray-500 mb-1">{t('templates.calculation.presets.denominator')}</label>
          <select className={SMALL_INPUT_CLASS} value={(config.params.denominator as string) ?? ''}
            onChange={(e) => update({ denominator: e.target.value })}>
            <option value="">—</option>
            {rootNums.map(s => <option key={s.fieldId} value={s.fieldId}>{s.label}</option>)}
          </select>
        </div>
      </div>
    );
  }

  // custom: 프리셋 탭에선 안내만 표시 (편집은 고급 모드에서)
  return (
    <p className="text-xs text-amber-600 dark:text-amber-400">
      {t('templates.calculation.advancedMode')} →
    </p>
  );
}
```

**노트:**
- Pitfall 6 방어: `presetConfig` 는 `useMemo` 파생값, 로컬 state 로 중복 보유하지 않음.
- Pitfall 7 방어: `validateFormula` 가 selfReference 를 즉시 감지.
- `detectCircularDeps` 는 호출하지 않음 — 상위(SchemaFieldEditor)에서 계산하여 cycles prop 으로 주입.
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npx tsc --noEmit 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - CalculationRuleEditor.tsx exists
    - contains `function CalculationRuleEditor`
    - contains `isAdvanced` state
    - contains `parseFormulaToPreset`
    - contains `buildFormulaFromPreset`
    - contains `extractDependencies`
    - contains `validateFormula`
    - contains `Sigma` (lucide import)
    - contains `templates.calculation.errors.circularDependency`
    - contains `myCycles` filter
    - contains `PresetSelector` sub-component
    - contains branch for `sum-col`, `sum-mul`, `field-sum`, `ratio`
    - types.ts contains `calculationRules:` in `SchemaFieldEditorProps`
    - types.ts contains `onCalculationRulesChange`
    - `npx tsc --noEmit` 결과: SchemaFieldEditor 내부 호출부 불일치 에러만 허용 (Task 2에서 해결), CalculationRuleEditor 자체 에러 없음
  </acceptance_criteria>
  <done>CalculationRuleEditor 가 완성되고, SchemaFieldEditorProps 에 calculationRules 필드가 추가된다.</done>
</task>

<task type="auto">
  <name>Task 2: FieldCard + FieldConfigEditor + SchemaFieldEditor 통합 (cascade 포함)</name>
  <files>
    frontend/src/features/admin/components/SchemaFieldEditor/FieldCard.tsx
    frontend/src/features/admin/components/SchemaFieldEditor/FieldConfigEditor.tsx
    frontend/src/features/admin/components/SchemaFieldEditor/SchemaFieldEditor.tsx
  </files>
  <read_first>
    frontend/src/features/admin/components/SchemaFieldEditor/FieldCard.tsx
    frontend/src/features/admin/components/SchemaFieldEditor/FieldConfigEditor.tsx
    frontend/src/features/admin/components/SchemaFieldEditor/SchemaFieldEditor.tsx
    frontend/src/features/admin/components/SchemaFieldEditor/CalculationRuleEditor.tsx
    frontend/src/features/admin/components/SchemaFieldEditor/calculationRuleUtils.ts
    frontend/src/features/document/utils/detectCircularDeps.ts
  </read_first>
  <action>
**1. FieldCard.tsx 확장**

(a) Props 에 추가:
```typescript
calculationRules: CalculationRule[];
cycles: string[][];
onAddCalcRule: (rule: CalculationRule) => void;
onUpdateCalcRule: (rule: CalculationRule) => void;
onDeleteCalcRule: (targetFieldId: string) => void;
```

(b) imports:
```typescript
import { Sigma } from 'lucide-react'; // 기존 Zap 과 함께
import type { CalculationRule } from '../../../document/types/dynamicForm';
import { CalculationRuleEditor } from './CalculationRuleEditor';
import { renderFormulaFriendly } from './calculationRuleUtils';
```

(c) 헤더 Σ 배지 + 1줄 미리보기 (D-25):
Phase 24 Zap 배지 근처 (FieldCard.tsx:80-84 영역)에 추가:
```jsx
{(() => {
  const myRule = calculationRules.find(r => r.targetFieldId === field.id);
  if (!myRule) return null;
  const hasCycle = cycles.some(c => c.includes(field.id));
  return (
    <span className={`inline-flex items-center gap-1 rounded px-1.5 py-0.5 text-xs font-mono max-w-[200px] ${
      hasCycle
        ? 'bg-red-100 text-red-700 dark:bg-red-900/40 dark:text-red-300'
        : 'bg-purple-100 text-purple-700 dark:bg-purple-900/40 dark:text-purple-300'
    }`} title={renderFormulaFriendly(myRule.formula)}>
      <Sigma className="w-3.5 h-3.5 shrink-0" />
      <span className="truncate">= {renderFormulaFriendly(myRule.formula)}</span>
    </span>
  );
})()}
```

(d) D-01 + D-19: expanded 영역 내부, ConditionalRuleEditor 섹션 바로 아래에 **계산 규칙 접기/펼치기 섹션** 추가. `field.type === 'number'` 일 때만 렌더:

```jsx
{field.type === 'number' && (
  <>
    <div
      role="button"
      aria-expanded={calcExpanded}
      onClick={() => setCalcExpanded(!calcExpanded)}
      className="flex items-center gap-2 py-2 cursor-pointer select-none"
    >
      <ChevronRight className={`w-3.5 h-3.5 transition-transform ${calcExpanded ? 'rotate-90' : ''}`} />
      <span className="text-xs font-medium text-gray-600 dark:text-gray-400">
        {t('templates.calculation.sectionTitle')}
      </span>
    </div>
    {calcExpanded && (
      <CalculationRuleEditor
        targetFieldId={field.id}
        rule={calculationRules.find(r => r.targetFieldId === field.id)}
        allFields={allFields}
        allRules={calculationRules}
        cycles={cycles}
        onAddRule={onAddCalcRule}
        onUpdateRule={onUpdateCalcRule}
        onDeleteRule={() => onDeleteCalcRule(field.id)}
      />
    )}
  </>
)}
```

`const [calcExpanded, setCalcExpanded] = useState(false);` state 추가.

**2. FieldConfigEditor.tsx 확인**

D-01 에 따라 "FieldConfigEditor 내부" 배치가 이상적이나, Phase 24 에서 ConditionalRuleEditor 가 FieldCard 에 직접 배치되었다면 **Phase 25 도 동일 위치**(FieldCard 의 expanded 영역)로 일관성 유지한다. FieldConfigEditor.tsx 는 **수정 불필요**할 수 있다.

`read_first` 로 먼저 Phase 24 배치 확인. 만약 Phase 24 가 FieldConfigEditor 내부에 배치했다면 동일 지점에 병렬 섹션으로 추가. 아니면 FieldCard 배치 유지하고 FieldConfigEditor 는 미변경.

**3. SchemaFieldEditor.tsx 확장**

(a) imports:
```typescript
import type { CalculationRule } from '../../../document/types/dynamicForm';
import { detectCircularDeps } from '../../../document/utils/detectCircularDeps';
import {
  cleanupCalcRulesForDeletedField,
  cleanupCalcRulesForTypeChange,
  cleanupCalcRulesForTableColumnChange,
} from './calculationRuleUtils';
```

(b) 함수 시그니처/destructure 확장:
```typescript
export function SchemaFieldEditor({
  fields, onChange,
  conditionalRules, onConditionalRulesChange,
  calculationRules, onCalculationRulesChange,
}: SchemaFieldEditorProps) { ... }
```

(c) cycles 메모:
```typescript
const cycles = useMemo(
  () => detectCircularDeps(calculationRules),
  [calculationRules],
);
```

(d) `deleteField` 확장 — 조건 규칙 정리 바로 아래에 calc 버전 추가:
```typescript
const [cleanedCalc, removedCalcCount] = cleanupCalcRulesForDeletedField(deletedFieldId, calculationRules);
if (removedCalcCount > 0) {
  toast(t('templates.calculation.rulesAutoRemoved', { count: removedCalcCount }));
  onCalculationRulesChange(cleanedCalc);
}
```

(e) `updateField` 확장:
```typescript
if (oldField.type !== updated.type) {
  // 기존 conditional cleanup...
  const [cleanedCalc, removedCalcCount] = cleanupCalcRulesForTypeChange(updated.id, updated.type, calculationRules);
  if (removedCalcCount > 0) {
    toast(t('templates.calculation.rulesAutoRemoved', { count: removedCalcCount }));
    onCalculationRulesChange(cleanedCalc);
  }
}

// Pitfall 3: table 내부 컬럼 타입 변경 감지
if (oldField.type === 'table' && updated.type === 'table') {
  const oldCols = (oldField.config as { columns?: Array<{ id: string; type: string }> }).columns || [];
  const newCols = (updated.config as { columns?: Array<{ id: string; type: string }> }).columns || [];
  const changedColIds: string[] = [];
  for (const oldCol of oldCols) {
    const newCol = newCols.find(c => c.id === oldCol.id);
    // 컬럼 삭제 or number → 다른 타입
    if (!newCol || (oldCol.type === 'number' && newCol.type !== 'number')) {
      changedColIds.push(oldCol.id);
    }
  }
  if (changedColIds.length > 0) {
    const [cleanedCalc, removedCalcCount] = cleanupCalcRulesForTableColumnChange(updated.id, changedColIds, calculationRules);
    if (removedCalcCount > 0) {
      toast(t('templates.calculation.rulesAutoRemoved', { count: removedCalcCount }));
      onCalculationRulesChange(cleanedCalc);
    }
  }
}
```

(f) FieldCard 에 props 전달:
```jsx
<FieldCard
  // ...기존 props...
  calculationRules={calculationRules}
  cycles={cycles}
  onAddCalcRule={(rule) => onCalculationRulesChange([...calculationRules, rule])}
  onUpdateCalcRule={(rule) => onCalculationRulesChange(
    calculationRules.map(r => r.targetFieldId === rule.targetFieldId ? rule : r)
  )}
  onDeleteCalcRule={(targetFieldId) => onCalculationRulesChange(
    calculationRules.filter(r => r.targetFieldId !== targetFieldId)
  )}
/>
```
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npx tsc --noEmit 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - FieldCard.tsx contains `Sigma` import
    - FieldCard.tsx contains `calcExpanded` state
    - FieldCard.tsx contains `CalculationRuleEditor`
    - FieldCard.tsx contains `field.type === 'number'` condition around CalculationRuleEditor
    - FieldCard.tsx contains `renderFormulaFriendly`
    - SchemaFieldEditor.tsx contains `detectCircularDeps`
    - SchemaFieldEditor.tsx contains `const cycles = useMemo`
    - SchemaFieldEditor.tsx contains `cleanupCalcRulesForDeletedField`
    - SchemaFieldEditor.tsx contains `cleanupCalcRulesForTypeChange`
    - SchemaFieldEditor.tsx contains `cleanupCalcRulesForTableColumnChange`
    - SchemaFieldEditor.tsx contains `templates.calculation.rulesAutoRemoved`
    - SchemaFieldEditor.tsx contains `onCalculationRulesChange`
    - `npx tsc --noEmit` 결과: TemplateFormModal props 불일치 에러만 남아있어야 함 (Task 3에서 해결)
  </acceptance_criteria>
  <done>FieldCard 에 Σ 배지와 계산 규칙 섹션이 통합되고, SchemaFieldEditor 가 calculationRules prop cascade + 순환 감지 useMemo 를 수행한다.</done>
</task>

<task type="auto">
  <name>Task 3: TemplateFormModal 상태 통합 + 하드코딩 제거 + 저장 검증</name>
  <files>
    frontend/src/features/admin/components/TemplateFormModal.tsx
  </files>
  <read_first>
    frontend/src/features/admin/components/TemplateFormModal.tsx
    frontend/src/features/admin/components/SchemaFieldEditor/SchemaFieldEditor.tsx
    frontend/src/features/admin/components/SchemaFieldEditor/calculationRuleUtils.ts
    frontend/src/features/document/utils/detectCircularDeps.ts
  </read_first>
  <action>
**TemplateFormModal.tsx 수정:**

(a) imports:
```typescript
import type { CalculationRule } from '../../document/types/dynamicForm';
import { detectCircularDeps } from '../../document/utils/detectCircularDeps';
import { validateFormula } from './SchemaFieldEditor/calculationRuleUtils';
```
(상대경로는 실제 파일 위치에 따라 조정)

(b) state 추가 (conditionalRules state 옆):
```typescript
const [calculationRules, setCalculationRules] = useState<CalculationRule[]>([]);
```

(c) 모달 열기 초기화: `open && !editingTemplate` 분기에서 `setConditionalRules([])` 옆에 `setCalculationRules([])` 추가.

(d) **Pitfall 1 수정 — 편집 모드 로드**: `detailQuery.data?.schemaDefinition` 파싱 useEffect 내, `setConditionalRules(schema.conditionalRules || [])` 옆에 추가:
```typescript
setCalculationRules(schema.calculationRules || []);
```
catch 블록에도 `setCalculationRules([])` 추가.

(e) **Pitfall 1 수정 — 저장 시 하드코딩 제거**: line 157 (연구 문서 근거) 의 `calculationRules: [],` 를 찾아 다음으로 변경:
```typescript
const schemaDefinition = JSON.stringify({
  version: 1,
  fields: schemaFields,
  conditionalRules: conditionalRules,
  calculationRules: calculationRules, // Pitfall 1: 하드코딩 [] 제거
});
```

(f) **저장 검증** (onSubmit 함수, 기존 invalidRules 검증 바로 아래):
```typescript
// Phase 25: 계산 규칙 검증 (D-10, D-11, D-24, D-26)
const calcCycles = detectCircularDeps(calculationRules);
if (calcCycles.length > 0) {
  toast.error(t('templates.calculation.circularSaveBlocked'));
  return;
}
for (const rule of calculationRules) {
  const err = validateFormula(rule.formula, schemaFields, rule.targetFieldId);
  if (err) {
    toast.error(t('templates.calculation.validationError'));
    return;
  }
}
```

(g) **저장 버튼 disabled** (D-24, D-11, D-31):
모달 하단 저장 버튼에 disabled 파생 추가. 기존에 invalidRules 기반 disabled 가 있으면 그 옆에 OR 조건 추가:
```typescript
const calcCycles = useMemo(() => detectCircularDeps(calculationRules), [calculationRules]);
const hasCalcErrors = useMemo(
  () => calcCycles.length > 0
    || calculationRules.some(r => validateFormula(r.formula, schemaFields, r.targetFieldId) !== null),
  [calcCycles, calculationRules, schemaFields],
);
// ...
<button type="submit" disabled={submitDisabled || hasCalcErrors} title={hasCalcErrors ? t('templates.calculation.circularSaveBlocked') : undefined}>
```
onSubmit 내부 `const calcCycles = detectCircularDeps(...)` 는 이 memo 를 재사용하거나 별도 계산. 중복 호출 OK (O(V+E)).

(h) SchemaFieldEditor 에 props 전달:
```jsx
<SchemaFieldEditor
  fields={schemaFields}
  onChange={setSchemaFields}
  conditionalRules={conditionalRules}
  onConditionalRulesChange={setConditionalRules}
  calculationRules={calculationRules}
  onCalculationRulesChange={setCalculationRules}
/>
```

(i) FormPreview / FullscreenPreviewPortal 에도 `calculationRules={calculationRules}` 전달 (25-03 에서 consume 할 준비). 이 plan 에서는 prop drilling 만 해두고, FormPreview 측의 수신은 25-03 에서 처리한다. FormPreview props 타입이 아직 없으면 optional `?` 로 선언되도록 조정하거나 25-03 에서 완성.

**주의:** FormPreview props 타입 확장은 **25-03 의 Task 1 범위**이지만, 본 plan 에서 TemplateFormModal 이 FormPreview 에 `calculationRules` 를 넘기려면 FormPreview 의 props 타입이 이를 받을 수 있어야 한다. 둘 중 선택:
- (A) 본 plan 에서는 TemplateFormModal 에서만 prop drilling 준비하되, FormPreview 호출부는 25-03 에서 추가 (본 plan 에선 FormPreview 호출부 건드리지 않음)
- (B) FormPreview props 타입에 `calculationRules?: CalculationRule[]` 만 optional 로 추가해두고 실제 사용은 25-03

**권장: (A)** — FormPreview 파일 수정은 25-03 으로 위임. 본 plan 은 TemplateFormModal 내부 state + 저장 검증 + SchemaFieldEditor 연결까지만.
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npx tsc --noEmit 2>&1 | tail -30 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - TemplateFormModal.tsx contains `const [calculationRules, setCalculationRules] = useState`
    - TemplateFormModal.tsx contains `setCalculationRules(schema.calculationRules || [])`
    - TemplateFormModal.tsx contains `calculationRules: calculationRules` (NOT `calculationRules: []`)
    - TemplateFormModal.tsx does NOT contain `calculationRules: [],` in JSON.stringify
    - TemplateFormModal.tsx contains `detectCircularDeps`
    - TemplateFormModal.tsx contains `validateFormula`
    - TemplateFormModal.tsx contains `templates.calculation.circularSaveBlocked` OR `templates.calculation.validationError`
    - TemplateFormModal.tsx contains `onCalculationRulesChange={setCalculationRules}`
    - `npx tsc --noEmit` exits 0
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>TemplateFormModal 이 calculationRules 를 저장/로드/검증하고, line 157 하드코딩이 제거된다. 빌드 성공.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin UI → schemaDefinition JSON | 저장 시 calculationRules 가 실제로 payload 에 포함됨 (Pitfall 1 수정 후) |
| CalculationRuleEditor 고급 모드 input → formula | 자유입력 텍스트가 저장 경로에 진입. blur 시 validateFormula + 저장 시 재검증으로 2단계 방어 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | Tampering | calculationRules 하드코딩 손실 (Pitfall 1) | mitigate | TemplateFormModal.tsx line 157 의 `calculationRules: []` 를 `calculationRules: calculationRules` 로 변경. Phase 24 의 `conditionalRules: []` 수정과 동일 패턴. acceptance_criteria 에 명시적 부정 검사 포함 (`does NOT contain calculationRules: [],`). |
| T-25-05 | Tampering | 고아 규칙 (Pitfall 3) — table 컬럼 타입 변경 누락 | mitigate | SchemaFieldEditor::updateField 에서 `oldField.type === 'table' && updated.type === 'table'` 분기로 컬럼 diff → `cleanupCalcRulesForTableColumnChange` 호출. 저장 시 validateFormula 가 2차 방어망. |
| T-25-06 | DoS | 자기참조 순환 (Pitfall 7) | mitigate | validateFormula 가 `extractDependencies(formula).includes(targetFieldId)` 체크로 selfReference 조기 감지 + detectCircularDeps 가 self-loop 도 감지하여 배너/저장 차단. |
| T-25-07 | Tampering | 프리셋 ↔ 고급 모드 유령 상태 (Pitfall 6) | mitigate | PresetConfig 를 로컬 state 가 아닌 `useMemo(() => parseFormulaToPreset(rule.formula), [rule.formula])` 파생 상태로만 유지. rule.formula 가 single source of truth. |
| T-25-08 | Tampering | executeCalculations/detectCircularDeps 런타임 경로 훼손 | mitigate | 본 plan 은 두 파일을 import 만 할 뿐 수정하지 않음. acceptance_criteria 에 의해 변경 스캔 (`git diff --stat` empty 기대). |
</threat_model>

<verification>
1. `npx tsc --noEmit` 통과
2. `npm run build` 성공
3. CalculationRuleEditor 컴포넌트 4종 프리셋 분기 모두 존재
4. TemplateFormModal 의 `calculationRules: []` 하드코딩이 제거됨
5. SchemaFieldEditor 에서 detectCircularDeps 가 useMemo 로 호출됨
6. cascade 정리 4곳 (deleteField + updateField type change + updateField table column change)
</verification>

<success_criteria>
- 관리자가 number 필드의 계산 규칙 섹션을 열고 프리셋 또는 고급 모드로 공식 설정 가능
- 순환 설정 시 인라인 배너 + 저장 disabled + 토스트
- 필드 삭제/타입 변경/컬럼 타입 변경 시 관련 규칙 자동 제거 + 토스트
- 저장 후 재진입 시 규칙 복원 (Pitfall 1)
- 빌드 성공
- executeCalculations.ts / detectCircularDeps.ts 미변경
</success_criteria>

<output>
After completion, create `.planning/phases/25-calculation-rule-ui/25-02-SUMMARY.md`
</output>
</content>
</invoke>