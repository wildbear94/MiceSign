---
phase: 33
plan: 04
type: execute
wave: 3
pr_bucket: pr3-docs
depends_on: [33-01, 33-02, 33-03]
files_modified:
  - .planning/milestones/v1.2/AUDIT.md
autonomous: false
requirements: [NOTIF-01, NOTIF-02, NOTIF-03, NOTIF-04, NOTIF-05, SRCH-01, SRCH-02, SRCH-03, SRCH-04, SRCH-05, SRCH-06, NFR-01, NFR-02, NFR-03, DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, FORM-01, FORM-02]
must_haves:
  truths:
    - "비기술 stakeholder (대표/부서장) 도 §1 SC 매트릭스 + §4 출시 게이트 체크리스트는 읽고 출시 가능 여부를 판단할 수 있다 (D-A1 + specifics §1.5)"
    - "Phase 29/30/31/32 의 SC 가 자동 검증 / 매뉴얼 / Deferred 셋 중 하나로 명확히 분류됨 (D-A1 §1)"
    - "v1.2 의 모든 deferred 항목 (NFR-01 / D-A11 stale PENDING / D-B6 push 알림) 이 §2 에 사유+후속 게이트+트리거 형식으로 추적됨 (D-A1 §2)"
    - "REQUIREMENTS.md 의 v1.2 ID 21개 모두 §3 매핑 표에 등재 (D-A1 §3)"
    - "출시 게이트 체크리스트 (§4) 가 33-02 SMTP-RUNBOOK + 33-03 MONITORING 의 핵심 PASS 항목을 모두 인용 (D-A1 §4)"
    - "사용자가 5종 이벤트 manual smoke 를 SMTP-RUNBOOK §5 절차대로 수행하고 §4 출시 게이트의 해당 항목을 PASS 마크할 수 있다 (D-M3 + checkpoint)"
  artifacts:
    - path: ".planning/milestones/v1.2/AUDIT.md"
      provides: "v1.2 마일스톤 통합 audit + 출시 게이트 체크리스트 (4 섹션)"
      contains: "출시 게이트"
      min_lines: 250
  key_links:
    - from: "AUDIT.md §1 SC 매트릭스"
      to: "Phase 29/30/31/32 의 ROADMAP SC + UAT.md / HUMAN-UAT.md / VERIFICATION.md"
      via: "각 cell 의 검증 source 명시 + 자동 테스트 ID 또는 매뉴얼 파일 경로"
      pattern: "ApprovalServiceAuditTest|ApprovalNotificationIntegrationTest|DocumentListPage\\.test|HUMAN-UAT|VERIFICATION"
    - from: "AUDIT.md §2 Deferred"
      to: "MONITORING.md / SMTP-RUNBOOK.md §6 / 30-BENCH-REPORT.md"
      via: "각 deferred 항목의 보존 경로 cross-reference"
      pattern: "MONITORING\\.md|SMTP-RUNBOOK\\.md|30-BENCH-REPORT"
---

<objective>
**v1.2 마일스톤 통합 audit + 출시 게이트 체크리스트 — 비기술 stakeholder 도 출시 가능 여부 판단 가능한 단일 산출물.**

CONTEXT.md D-A1 의 4 섹션 구조 그대로 실행:
1. **§1 Phase별 SC 충족도 매트릭스** — Phase 29/30/31/32 의 ROADMAP SC 각 항목 → 자동 검증 (테스트 ID) / 매뉴얼 (UAT 파일) / Deferred 분류.
2. **§2 Deferred 항목 추적** — Plan 30-05 NFR-01 (→ MONITORING.md), Phase 29 D-A11 stale PENDING (→ SMTP-RUNBOOK §6.2), Phase 29 D-B6 push 알림 (→ Phase 1-C). 각 항목 사유+후속 게이트+트리거 형식.
3. **§3 Requirements 매핑** — REQUIREMENTS.md 의 v1.2 ID 21개 (NOTIF-01~05 + SRCH-01~06 + DASH-01~05 + FORM-01~02 + NFR-01~03) → 검증 phase + 자동 테스트 ID + 잔존 부채.
4. **§4 출시 게이트 체크리스트** — DB 마이그레이션 V1~V19 / SMTP 연결 / 5종 smoke / audit_log SQL 스팟 / BaseUrlGuard PASS / 디스크 / log rotation / 백업 / 롤백.

본 plan 의 마지막 task 는 사용자 수동 5종 smoke + audit_log SQL 스팟 (체크포인트). 사용자 PASS resume 후 §4 의 해당 항목을 PASS 로 마크.

본 plan 은 33-01/33-02/33-03 모두에 depends — Wave 3. 산출물 위치는 `.planning/milestones/v1.2/AUDIT.md` (D-A1 명시).

비기술 stakeholder 청자도 의식 — 한국어, 표 위주, 약어 풀어쓰기, 결재 도메인 용어 일관 (Phase 29 SECURITY.md / VALIDATION.md 패턴 따름).

Output: `.planning/milestones/v1.2/AUDIT.md` (한국어, 250+ lines, 4 섹션) + 사용자 PASS sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-e2e/33-CONTEXT.md
@.planning/phases/29-smtp-retrofit/29-CONTEXT.md
@.planning/phases/29-smtp-retrofit/29-VERIFICATION.md
@.planning/phases/29-smtp-retrofit/29-HUMAN-UAT.md
@.planning/phases/30-where/30-BENCH-REPORT.md
@.planning/phases/30-where/30-VALIDATION.md
@.planning/phases/31-dashboard/31-VALIDATION.md
@.planning/milestones/v1.2/SMTP-RUNBOOK.md
@.planning/milestones/v1.2/MONITORING.md
@.planning/milestones/v1.1-MILESTONE-AUDIT.md

<interfaces>
<!-- 본 plan 이 인용하는 검증 자산 (자동 테스트 IDs + 매뉴얼 UAT 파일들) -->

자동 회귀 테스트:
- Phase 29 D-D5: `backend/src/test/java/com/micesign/notification/ApprovalNotificationIntegrationTest.java` (GreenMail 5종 이벤트)
- Phase 29 D-D6: `backend/src/test/java/com/micesign/approval/ApprovalServiceAuditTest.java` (audit_log COUNT=1, NFR-03)
- Phase 30: QueryDSL 권한 predicate 28-case 매트릭스 (`DocumentRepositoryCustomImpl` 테스트)
- Plan 30-04: `frontend/src/features/document/pages/__tests__/DocumentListPage.test.tsx` (URL ↔ UI)
- Phase 31: `DashboardServiceIntegrationTest` (matrix) + `useDashboard.test`

매뉴얼 UAT 파일:
- 29-HUMAN-UAT.md (5종 이벤트 수동 smoke)
- 31-HUMAN-UAT.md (대시보드 4 카드 + invalidate)
- 32-HUMAN-UAT.md (CUSTOM 프리셋 — Phase 32 완료 시점에 생성)
- (Phase 30 은 HUMAN-UAT 파일 없음 — 30-VALIDATION.md 가 자동 회귀로 갈음)

v1.1-MILESTONE-AUDIT.md (참고 형식):
- §SC 충족도 / §Nyquist 호환성 / §FLAG / §Tech Debt 4 섹션 — 본 v1.2 AUDIT.md 가 준수해야 할 형식 표준 (단 본 AUDIT.md 는 D-A1 의 4 섹션이 우선)

DB 마이그레이션 history (V1~V19):
V1__create_schema.sql, V2__seed_initial_data.sql, V3__add_auth_columns.sql, V4__add_unique_department_name.sql,
V5__add_document_support.sql, V6__add_pending_notification_status.sql, V8__add_template_schema_support.sql,
V9__create_template_schema_version.sql, V10__create_option_tables.sql, V11__seed_dynamic_template.sql,
V12__add_budget_integration.sql, V13__add_doc_sequence_unique_constraint.sql, V14__create_registration_request.sql,
V15__add_registration_tracking_token.sql, V16__extend_notification_log_for_registration.sql,
V17__fix_leave_prefix.sql, V18__add_additional_templates.sql, V19__add_notification_dedup_unique.sql
(V7 결번 — 정상)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AUDIT.md §0 (개요) + §1 (SC 매트릭스 — Phase 29/30/31/32)</name>
  <files>.planning/milestones/v1.2/AUDIT.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 29/30/31/32 의 SC 항목들)
    - .planning/phases/29-smtp-retrofit/29-VERIFICATION.md (자동 검증 결과)
    - .planning/phases/29-smtp-retrofit/29-HUMAN-UAT.md (5종 매뉴얼)
    - .planning/phases/30-where/30-VALIDATION.md (Phase 30 검증)
    - .planning/phases/31-dashboard/31-VALIDATION.md (Phase 31 검증)
    - .planning/milestones/v1.1-MILESTONE-AUDIT.md (형식 참조)
    - .planning/phases/33-e2e/33-CONTEXT.md §D-A1
  </read_first>
  <action>
    `.planning/milestones/v1.2/AUDIT.md` 신규 작성. 본문 한국어, 식별자/SQL 영어. 비기술 stakeholder 가 §1 + §4 만 읽어도 의사결정 가능한 형식.

    파일의 §0 + §1 까지 작성 (§2-4 는 Task 2/3 에서):

    ```markdown
    # MiceSign v1.2 마일스톤 통합 Audit + 출시 게이트

    **작성:** 2026-04-28 (Phase 33 D-A1)
    **마일스톤:** v1.2 Phase 1-B — 일상 업무 대체 수준
    **포함 phase:** 29 (SMTP 알림), 30 (검색 권한 + 필터), 31 (대시보드 고도화), 32 (CUSTOM 프리셋), 33 (본 검증 + 운영 전환)

    ## 목차
    - §0. 요약 (비기술 stakeholder 용)
    - §1. Phase별 SC (Success Criteria) 충족도 매트릭스
    - §2. Deferred 항목 추적
    - §3. Requirements ID 매핑 표
    - §4. 출시 게이트 체크리스트

    ---

    ## §0. 요약

    v1.2 는 v1.0 (MVP 결재 플로우) + v1.1 (양식 빌더) 위에 **일상 업무 대체 수준의 알림·검색·대시보드·CUSTOM 프리셋** 4개 축을 올린 마일스톤.

    | 축 | Phase | 핵심 가치 | 출시 가능 여부 |
    |----|-------|-----------|--------------|
    | SMTP 이메일 알림 | 29 | 5종 결재 이벤트 자동 메일 (한글 HTML) | (§1 + §4 로 판단) |
    | 검색 권한 + 필터 | 30 | 권한 WHERE 절 보안 수정 + URL 공유 가능 검색 | (§1 + §4 로 판단) |
    | 대시보드 고도화 | 31 | 4 카운트 카드 + mutation 실시간 invalidate | (§1 + §4 로 판단) |
    | CUSTOM 프리셋 확장 | 32 | 회의록/품의서 JSON 프리셋 추가 | (§1 + §4 로 판단) |

    **출시 결정 절차:** §4 출시 게이트 체크리스트의 모든 항목 PASS 시 출시 가능. 하나라도 FAIL 또는 미확인 시 보류.

    **deferred 항목 1개:** NFR-01 (검색 95p ≤ 1초 합성 부하 실측) — Plan 30-05 deferral 결정에 따라 운영 모니터링 게이트로 이관 (§2 참조).

    ---

    ## §1. Phase별 SC 충족도 매트릭스

    각 SC 는 다음 셋 중 하나로 분류:
    - **자동** = 자동 회귀 테스트가 검증, 테스트 ID 명시
    - **매뉴얼** = HUMAN-UAT.md / VERIFICATION.md 의 수동 검증 항목, 파일 경로 명시
    - **Deferred** = 본 마일스톤 범위 외로 이관, §2 와 cross-reference

    ### Phase 29: SMTP 이메일 알림 인프라

    | SC# | 내용 (요약) | 분류 | 검증 source |
    |-----|------------|------|-------------|
    | SC1 | 상신 시 첫 비-REFERENCE 승인자에게 `[MiceSign]` HTML 메일 도착 | 자동 + 매뉴얼 | `ApprovalNotificationIntegrationTest` (GreenMail) + 29-HUMAN-UAT.md §1 + §4 출시 게이트 §G5 |
    | SC2 | 메일 본문 "문서 바로가기" 클릭 → `{app.base-url}/documents/{id}` 이동 (한글 subject UTF-8 보존) | 자동 + 매뉴얼 | `ApprovalNotificationIntegrationTest` (subject decoding) + 29-HUMAN-UAT.md §2 + §4 §G5 |
    | SC3 | SMTP 실패 시 @Retryable 3회 + notification_log status=FAILED 기록 (PENDING 고아 없음) | 자동 | `ApprovalNotificationRetryTest` (Phase 29-04) |
    | SC4 | RETIRED/INACTIVE 수신자 자동 제외 + 동일 (doc, event, recipient) 중복 SUCCESS 없음 | 자동 | `ApprovalNotificationIntegrationTest` (수신자 규칙) + V19 unique constraint |
    | SC5 | 결재 API 응답이 메일 발송 결과와 독립 (`@Async` + `AFTER_COMMIT`) + audit_log COUNT=1 per action | 자동 | `ApprovalServiceAuditTest` (NFR-03) + Phase 29 D-D6 |

    ### Phase 30: 검색 권한 WHERE 절 보안 수정 + 필터 확장

    | SC# | 내용 (요약) | 분류 | 검증 source |
    |-----|------------|------|-------------|
    | SC1 | 사용자 A 의 SUBMITTED 문서가 승인자 B 의 검색 결과에 포함, 무관한 C 의 결과에서 제외 (FSD FN-SEARCH-001) | 자동 | QueryDSL 28-case 권한 매트릭스 (Plan 30-02) |
    | SC2 | tab=department/all 에서 status != DRAFT 보장, tab=my 에서만 본인 DRAFT 노출 | 자동 | DRAFT gate 매트릭스 (Plan 30-02) |
    | SC3 | 키워드 + 상태(복수) + 양식 + 기간 + 기안자 복합 검색이 URL query 에 반영, 링크 공유로 동일 결과 재현 | 자동 | `DocumentListPage.test.tsx` (Plan 30-04) |
    | SC4 | 10K 문서 + 50 동시 사용자에서 95p ≤ 1초 (EXPLAIN 인덱스 사용 확인, countDistinct 정확) | **Deferred** | §2-A (Plan 30-05 deferral → MONITORING.md) — EXPLAIN 정적 검증은 Plan 30-05 PASS |
    | SC5 | 페이지 크기 20 offset 페이지네이션 동작, totalElements 가 접근 가능 문서 수와 일치 (JOIN 중복 inflate 없음) | 자동 | `countDistinct` 테스트 (Plan 30-02) |

    ### Phase 31: 대시보드 고도화

    | SC# | 내용 (요약) | 분류 | 검증 source |
    |-----|------------|------|-------------|
    | SC1 | 4 카운트 카드 (대기/진행 중/승인 완료/반려) + role-based 스코프 (USER vs ADMIN) | 자동 + 매뉴얼 | `DashboardServiceIntegrationTest` (matrix, Plan 31-02) + 31-HUMAN-UAT.md |
    | SC2 | "내가 처리할 결재 5건" + "내가 기안한 최근 5건" + "새 문서 작성" CTA | 자동 + 매뉴얼 | `DashboardPage.test` (Plan 31-04) + 31-HUMAN-UAT.md |
    | SC3 | skeleton 로딩 + 빈 상태 (empty) UI | 매뉴얼 | 31-HUMAN-UAT.md |
    | SC4 | mutation (승인/반려/상신/회수) 성공 시 invalidateQueries(['dashboard']) 자동 호출 → 카운트·목록 즉시 재조회 | 자동 + 매뉴얼 | invalidate spy 테스트 (Plan 31-05) + 31-HUMAN-UAT.md |

    ### Phase 32: CUSTOM 프리셋 확장

    | SC# | 내용 (요약) | 분류 | 검증 source |
    |-----|------------|------|-------------|
    | SC1 | 관리자가 "회의록" 프리셋 선택 → 사전 정의 CUSTOM 스키마 즉시 로드 | 자동 + 매뉴얼 | `presets.test.ts` (Plan 32-05) + 32-HUMAN-UAT.md (Phase 32 완료 시점) |
    | SC2 | 관리자가 "품의서" 프리셋 선택 → 사전 정의 CUSTOM 스키마 즉시 로드 | 자동 + 매뉴얼 | `presets.test.ts` (Plan 32-05) + 32-HUMAN-UAT.md |
    | SC3 | 두 프리셋 JSON 모두 v1.1 templateImportSchema Zod 통과 + DynamicCustomForm 정상 렌더 (snapshot 불변성 유지) | 자동 | `presets.test.ts` Zod 단언 + v1.1 자산 회귀 |
    ```

    Phase 32 의 SC 항목은 Phase 32 가 완료되지 않은 시점에는 "Phase 32 완료 시 PASS 검증 예정" 으로 표기 (Phase 33 가 32 에 depends 하므로 본 plan 작성 시점에는 32 완료 가정).
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign && test -f .planning/milestones/v1.2/AUDIT.md && grep -q '## §0\. 요약' .planning/milestones/v1.2/AUDIT.md && grep -q '## §1\. Phase별 SC' .planning/milestones/v1.2/AUDIT.md && grep -q '### Phase 29' .planning/milestones/v1.2/AUDIT.md && grep -q '### Phase 30' .planning/milestones/v1.2/AUDIT.md && grep -q '### Phase 31' .planning/milestones/v1.2/AUDIT.md && grep -q '### Phase 32' .planning/milestones/v1.2/AUDIT.md && grep -q 'ApprovalNotificationIntegrationTest' .planning/milestones/v1.2/AUDIT.md && grep -q 'ApprovalServiceAuditTest' .planning/milestones/v1.2/AUDIT.md && grep -q 'DocumentListPage\.test' .planning/milestones/v1.2/AUDIT.md && grep -q 'Deferred' .planning/milestones/v1.2/AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/milestones/v1.2/AUDIT.md` 존재
    - §0 요약 + §1 SC 매트릭스 작성됨
    - Phase 29/30/31/32 4개 phase 모두 SC 표 등재
    - 자동 / 매뉴얼 / Deferred 셋 분류 명시
    - 자동 테스트 ID (`ApprovalNotificationIntegrationTest` 등) cross-reference
    - SRCH SC4 (NFR-01) 가 Deferred 로 분류 + §2 cross-reference 라인 존재
  </acceptance_criteria>
  <done>
    Phase 29/30/31/32 의 모든 SC 가 한 표에서 검증 source 와 함께 추적 가능. 비기술 stakeholder 도 §1 의 표를 보고 어느 항목이 자동 검증인지 / 매뉴얼인지 / 이관되었는지 파악 가능.
  </done>
</task>

<task type="auto">
  <name>Task 2: AUDIT.md §2 (Deferred) + §3 (Requirements 매핑)</name>
  <files>.planning/milestones/v1.2/AUDIT.md</files>
  <read_first>
    - .planning/milestones/v1.2/AUDIT.md (Task 1 산출물)
    - .planning/REQUIREMENTS.md (v1.2 ID 21개 추출)
    - .planning/phases/30-where/30-BENCH-REPORT.md §Deferral Decision (NFR-01 보존 산출물)
    - .planning/phases/29-smtp-retrofit/29-CONTEXT.md (D-A11 stale PENDING / D-B6 push 알림)
    - .planning/milestones/v1.2/MONITORING.md (33-03 산출물 — NFR-01 보존 경로)
    - .planning/milestones/v1.2/SMTP-RUNBOOK.md (33-02 산출물 — D-A11 / D-B6 보존 경로)
  </read_first>
  <action>
    Task 1 의 AUDIT.md 에 §2 + §3 append.

    추가 본문:

    ```markdown
    ---

    ## §2. Deferred 항목 추적

    아래 항목은 v1.2 출시 게이트가 아닌 **운영 모니터링 게이트** 또는 **후속 마일스톤 (v1.3 / Phase 1-C / v2)** 으로 이관됨. 모든 deferred 항목은 사유 + 후속 게이트 + 트리거 + 보존 산출물을 명시.

    ### §2-A. NFR-01 (검색 95p ≤ 1초 합성 부하 실측)

    | 항목 | 내용 |
    |------|------|
    | 출처 | Phase 30 SC4, REQUIREMENTS NFR-01 + SRCH-06 |
    | 결정 | Plan 30-05 deferral (2026-04-28) — 30-BENCH-REPORT.md §Deferral Decision |
    | 사유 | 50인 사내 시스템에서 10K 문서 + 50 동시 활성 도달은 6-12개월 후 시나리오. 합성 부하 (ab) 측정값과 실 사용자 분포 차이. 정적 분석 게이트 (QueryDSL 28-case + EXPLAIN 인덱스 사용) PASS. |
    | 후속 게이트 | 운영 모니터링 — `.planning/milestones/v1.2/MONITORING.md` |
    | 트리거 (3 신호) | (a) 사용자 신고 1건 (b) slow query log 의 검색 쿼리 1초 초과 1회 (c) 동시 활성 사용자 30명 도달 |
    | 보존 산출물 | `backend/src/main/java/com/micesign/tools/SearchBenchmarkSeeder.java` (`@Profile("bench")`) + `scripts/bench-search.sh` + `30-BENCH-REPORT.md` (보강 시 append) |
    | 재개 절차 | MONITORING.md §3 — `./gradlew bootRun --args='--spring.profiles.active=bench'` → `./scripts/bench-search.sh` → 결과 평가 |
    | 종료 조건 | (1) 3 신호 6개월 무발동 → APM 검토 / (2) 신호 발동 → 재실측 + 보강 1회 완료 |

    ### §2-B. stale PENDING 자동 청소 cron

    | 항목 | 내용 |
    |------|------|
    | 출처 | Phase 29 D-A11 |
    | 결정 | 수동 운영 채택 (cron 도입은 과스펙) |
    | 사유 | 50인 + 드문 서버 재시작 가정. cron 도입 시 추가 운영 surface (실패 알림 / 모니터링) 발생. |
    | 후속 게이트 | SMTP-RUNBOOK.md §6.2 — 수동 FAILED 전환 SQL 운영 절차 |
    | 트리거 | 주간 점검 시 PENDING 행이 10분 이상 남아있는 경우 (SMTP-RUNBOOK §6.2 SQL) |
    | 보존 산출물 | SMTP-RUNBOOK.md §6.2 (수동 UPDATE SQL 그대로 복붙 가능) |
    | 종료 조건 | (1) 사용자 50인 → 200인 확장 시 cron 또는 scheduled task 도입 검토 / (2) Phase 1-C 운영 자동화 phase 에서 흡수 |

    ### §2-C. 운영자 push 알림 파이프라인 (Slack/SMS/PagerDuty)

    | 항목 | 내용 |
    |------|------|
    | 출처 | Phase 29 D-B6 — `@Retryable` 최종 실패 시 push 알림 |
    | 결정 | ERROR 로그 + notification_log.status=FAILED 기록 only (Phase 33/1-C 로 deferred) |
    | 사유 | 50인 규모 + 주간 수동 SQL 조회로 충분. push 통합 시 외부 의존성 추가 (Slack API / Twilio 등). |
    | 후속 게이트 | SMTP-RUNBOOK.md §6.3 — 주간 NOTIF FAILED 조회 SQL |
    | 트리거 | 주간 점검 시 failed_cnt > 0 발견 |
    | 보존 산출물 | SMTP-RUNBOOK.md §6.3 SQL + Phase 29 의 ERROR 로그 발생 지점 (`ApprovalEmailSender.@Recover`) |
    | 종료 조건 | Phase 1-C 운영 자동화 phase 에서 push 통합 검토 |

    ### §2-D. secret 관리 도구 (git-crypt / SOPS / Vault)

    | 항목 | 내용 |
    |------|------|
    | 출처 | Phase 33 D-C2 |
    | 결정 | 도입하지 않음 (50인 단일 환경에서 systemd EnvironmentFile + chmod 600 충분) |
    | 사유 | 외부 secret 매니저 도입 = 추가 인프라 + 운영자 학습 비용. 현재 1 서버 단일 환경에서 ROI 부정. |
    | 후속 게이트 | (없음) — Phase 1-C 또는 v2 확장 시 재검토 |
    | 트리거 | (a) multi-environment 분리 (staging/prod) 도입 / (b) compliance 요구 (ISO 27001 등) 발생 |
    | 보존 산출물 | application-prod.yml (env var only 패턴) + application-prod.yml.example (카탈로그) |

    ### §2-E. APM 도구 도입 (DataDog / NewRelic / Pinpoint)

    | 항목 | 내용 |
    |------|------|
    | 출처 | Phase 33 D-S2 의 대안 |
    | 결정 | 도입하지 않음 (slow query log + 사용자 신고로 충분) |
    | 사유 | APM 라이센스 비용 + 50인 트래픽 양에서 과스펙 |
    | 후속 게이트 | MONITORING.md §5 종료 조건 (1) — 3 신호 6개월 무발동 시 검토 |
    | 트리거 | (a) 부하 발생 빈도 증가 (b) 다중 서비스 운영 시작 |
    | 보존 산출물 | MONITORING.md (모든 절차 문서화 — APM 도입 시 자동 export) |

    ---

    ## §3. Requirements ID 매핑 표

    REQUIREMENTS.md 의 v1.2 ID 21개 모두 등재. 각 ID 의 검증 phase + 자동 테스트 ID + 잔존 부채 명시.

    ### SMTP 이메일 알림 (NOTIF, NFR-02, NFR-03)

    | ID | 요구사항 | 검증 phase | 자동 테스트 | 잔존 부채 |
    |----|----------|------------|-------------|-----------|
    | NOTIF-01 | 5종 이벤트 자동 메일 발송 | Phase 29 | ApprovalNotificationIntegrationTest | (없음) |
    | NOTIF-02 | 메일 "문서 바로가기" → app.base-url 절대 URL | Phase 29 | ApprovalNotificationIntegrationTest (subject decoding 포함) | (없음) |
    | NOTIF-03 | @Retryable 2회 재시도 + notification_log PENDING→SUCCESS/FAILED | Phase 29 | ApprovalNotificationRetryTest | (없음) |
    | NOTIF-04 | RETIRED/INACTIVE 수신자 자동 skip | Phase 29 | ApprovalNotificationIntegrationTest (수신자 규칙) | (없음) |
    | NOTIF-05 | `[MiceSign]` prefix + 한글 UTF-8 본문 | Phase 29 | ApprovalNotificationIntegrationTest (subject UTF-8) | (없음) |
    | NFR-02 | @Async + AFTER_COMMIT (트랜잭션 비블로킹) | Phase 29 | ApprovalServiceAuditTest (트랜잭션 분리) | (없음) |
    | NFR-03 | audit_log 중복 INSERT 방지 (COUNT=1 per action) | Phase 29 | ApprovalServiceAuditTest (D-D6) | §4 §G6 출시 전 SQL 스팟 |

    ### 검색/필터링 (SRCH, NFR-01)

    | ID | 요구사항 | 검증 phase | 자동 테스트 | 잔존 부채 |
    |----|----------|------------|-------------|-----------|
    | SRCH-01 | 권한 WHERE 절 보안 수정 (FSD FN-SEARCH-001) | Phase 30 | QueryDSL 28-case 매트릭스 (Plan 30-02) | (없음) |
    | SRCH-02 | 키워드 LIKE 검색 (제목/문서번호) | Phase 30 | DocumentRepositoryCustomImpl 테스트 | (없음) |
    | SRCH-03 | 상태(복수) + 양식 + 기간 필터 | Phase 30 | DocumentListPage.test + repo 테스트 | (없음) |
    | SRCH-04 | 기안자(드롭다운) 필터 | Phase 30 | /users/search 엔드포인트 테스트 (Plan 30-03) | (없음) |
    | SRCH-05 | offset 페이지네이션 + URL query 동기화 | Phase 30 | DocumentListPage.test (Plan 30-04) | (없음) |
    | SRCH-06 | 95p ≤ 1초 (10K seed + 50 동시) | Phase 30 → **Deferred** | EXPLAIN 정적 분석 PASS | §2-A (MONITORING.md) |
    | NFR-01 | 동일 (NFR 관점, SRCH-06 와 중복) | Phase 30 → **Deferred** | (동일) | §2-A |

    ### 대시보드 고도화 (DASH)

    | ID | 요구사항 | 검증 phase | 자동 테스트 | 잔존 부채 |
    |----|----------|------------|-------------|-----------|
    | DASH-01 (v1.2) | 4 카운트 카드 + role-based | Phase 31 | DashboardServiceIntegrationTest matrix | (없음) |
    | DASH-02 (v1.2) | 처리할 결재 5 + 기안 5 + CTA | Phase 31 | DashboardPage.test (Plan 31-04) | (없음) |
    | DASH-03 (v1.2) | "새 문서 작성" CTA → 양식 선택 | Phase 31 | DashboardPage.test 라우팅 단언 | (없음) |
    | DASH-04 | skeleton + empty UI | Phase 31 | smoke test + 31-HUMAN-UAT | (없음) |
    | DASH-05 | mutation 후 invalidateQueries(['dashboard']) | Phase 31 | invalidate spy 테스트 (Plan 31-05) | (없음) |

    ### 양식 확장 (FORM)

    | ID | 요구사항 | 검증 phase | 자동 테스트 | 잔존 부채 |
    |----|----------|------------|-------------|-----------|
    | FORM-01 | "회의록" 프리셋 즉시 로드 | Phase 32 | presets.test.ts (Plan 32-05) | (없음 — Phase 32 완료 가정) |
    | FORM-02 | "품의서" 프리셋 즉시 로드 | Phase 32 | presets.test.ts | (없음) |

    ### 매핑 커버리지

    - v1.2 requirements: 21 total
    - 자동 회귀 만으로 PASS: 18
    - Deferred (운영 모니터링 게이트): 2 (SRCH-06 + NFR-01 — 동일 요구사항 중복 명시)
    - 자동 + 출시 전 SQL 스팟 보강: 1 (NFR-03 → §4 §G6)
    - 미매핑 (gap): 0
    ```
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign && grep -q '## §2\. Deferred' .planning/milestones/v1.2/AUDIT.md && grep -q '### §2-A\. NFR-01' .planning/milestones/v1.2/AUDIT.md && grep -q '### §2-B\. stale PENDING' .planning/milestones/v1.2/AUDIT.md && grep -q '### §2-C\. 운영자 push' .planning/milestones/v1.2/AUDIT.md && grep -q '## §3\. Requirements ID' .planning/milestones/v1.2/AUDIT.md && grep -q 'NOTIF-01' .planning/milestones/v1.2/AUDIT.md && grep -q 'SRCH-01' .planning/milestones/v1.2/AUDIT.md && grep -q 'DASH-05' .planning/milestones/v1.2/AUDIT.md && grep -q 'FORM-01' .planning/milestones/v1.2/AUDIT.md && grep -q 'FORM-02' .planning/milestones/v1.2/AUDIT.md && grep -q 'NFR-02' .planning/milestones/v1.2/AUDIT.md && grep -q 'NFR-03' .planning/milestones/v1.2/AUDIT.md && grep -q 'MONITORING\.md' .planning/milestones/v1.2/AUDIT.md && grep -q 'SMTP-RUNBOOK\.md' .planning/milestones/v1.2/AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - §2 5개 deferred 항목 (A-E) 표 형식
    - 각 deferred 항목: 출처 / 결정 / 사유 / 후속 게이트 / 트리거 / 보존 산출물 / 종료 조건
    - §3 v1.2 21개 ID 모두 등재 (NOTIF×5 + NFR×3 + SRCH×6 + DASH×5 + FORM×2)
    - 각 ID: 검증 phase + 자동 테스트 ID + 잔존 부채
    - MONITORING.md / SMTP-RUNBOOK.md 정확한 cross-reference
    - 매핑 커버리지 요약 (자동 18 / Deferred 2 / 자동+SQL 1 / 미매핑 0)
  </acceptance_criteria>
  <done>
    deferred 항목들이 모두 사유+후속 게이트 형식으로 추적되어 누락 위험 zero. v1.2 의 모든 요구사항 ID 가 한 표에서 검증 경로 추적 가능.
  </done>
</task>

<task type="auto">
  <name>Task 3: AUDIT.md §4 (출시 게이트 체크리스트 — 사용자 수동 sign-off 전 작성)</name>
  <files>.planning/milestones/v1.2/AUDIT.md</files>
  <read_first>
    - .planning/milestones/v1.2/AUDIT.md (Task 2 산출물)
    - .planning/milestones/v1.2/SMTP-RUNBOOK.md (§3-5 가 게이트 항목의 source)
    - .planning/milestones/v1.2/MONITORING.md (§1 활성화가 게이트 항목)
  </read_first>
  <action>
    Task 2 의 AUDIT.md 에 §4 append.

    추가 본문:

    ```markdown
    ---

    ## §4. 출시 게이트 체크리스트

    아래 모든 항목 PASS 시 v1.2 운영 출시 가능. 하나라도 FAIL 또는 미확인 시 보류 + 원인 해소 후 재실행.

    각 항목 좌측 체크박스는 출시 담당자가 수동으로 채움. 본 plan 의 Task 4 (수동 5종 smoke 체크포인트) 가 §G5/§G6 를 PASS 마크.

    ### G1. DB 마이그레이션 적용 검증
    - [ ] **G1.1** Flyway 가 V1~V19 마이그레이션을 모두 success 로 기록함
      - 검증: `mysql -uroot micesign -e "SELECT version, success FROM flyway_schema_history ORDER BY installed_rank;"`
      - 기대: 18 rows (V1-V6, V8-V19 — V7 결번), 모두 `success=1`
    - [ ] **G1.2** charset / collation = utf8mb4 / utf8mb4_unicode_ci
      - 검증: `mysql -uroot micesign -e "SELECT default_character_set_name, default_collation_name FROM information_schema.schemata WHERE schema_name = 'micesign';"`

    ### G2. 환경변수 + 자격증명 위생 (Phase 33 D-C1)
    - [ ] **G2.1** `application-prod.yml` 의 username/password 가 모두 빈 default (`${DB_USER:}` / `${DB_PASS:}`)
      - 검증: `grep -E '\$\{DB_(USER|PASS):' backend/src/main/resources/application-prod.yml | grep -E ':\}$'` (2 줄)
    - [ ] **G2.2** `/etc/micesign/.env.production` 권한 = 600, owner = micesign:micesign
      - 검증 (운영 서버): `stat -c '%a %U:%G' /etc/micesign/.env.production` → `600 micesign:micesign`
    - [ ] **G2.3** systemd unit 의 `EnvironmentFile=/etc/micesign/.env.production` 설정 + daemon-reload 완료
      - 검증: `sudo systemctl show micesign.service -p EnvironmentFiles | grep .env.production`

    ### G3. SMTP 운영 연결 (Phase 33 D-M1)
    - [ ] **G3.1** `MAIL_HOST` / `MAIL_PORT` 가 `.env.production` 에 채워짐
    - [ ] **G3.2** 사내 IT 가 운영 서버 IP 를 SMTP allowlist 에 추가
    - [ ] **G3.3** 운영 서버에서 `telnet $MAIL_HOST $MAIL_PORT` 핸드셰이크 PASS (SMTP banner 출력)

    ### G4. BaseUrlGuard startup 검증 (Phase 29 D-D2)
    - [ ] **G4.1** `APP_BASE_URL` 이 사내 실 도메인 (localhost / 127.0.0.1 미포함)
    - [ ] **G4.2** `sudo systemctl restart micesign` 후 `journalctl -u micesign | grep BaseUrlGuard` 가 PASS 로그 + IllegalStateException 부재

    ### G5. 5종 이벤트 수동 smoke (Phase 33 D-M3 — 본 plan Task 4 에서 수동 sign-off)
    - [ ] **G5.1** 시나리오 1 (상신) — `[MiceSign]` 한글 subject + 본문 정상 + 문서 바로가기 동작
    - [ ] **G5.2** 시나리오 2 (중간 승인) — 동일 4 항목
    - [ ] **G5.3** 시나리오 3 (최종 승인) — 동일 4 항목 + "최종 승인" 명시
    - [ ] **G5.4** 시나리오 4 (반려) — 동일 4 항목 + 사유 표시
    - [ ] **G5.5** 시나리오 5 (회수) — 동일 4 항목

    상세 절차: SMTP-RUNBOOK.md §5

    ### G6. audit_log SQL 스팟 검증 (Phase 33 NFR-03 출시 전 보강)
    - [ ] **G6.1** §G5 5 시나리오 각 문서별 audit_log COUNT=1 per action_type 확인
      - SMTP-RUNBOOK.md §5.3 SQL 실행
      - 기대: 모든 action_type 의 cnt = 1 (SMTP listener 의 중복 INSERT 부재 — D-D6 자동 회귀의 manual confirmation)
      - cnt ≥ 2 발견 시 출시 보류 + Phase 29 D-D6 회귀 조사
    - [ ] **G6.2** §G5 5 시나리오 각 문서별 notification_log status=SUCCESS, cnt=1 확인 (NOTIF-04 dedup unique constraint 회귀 검증)
      - SMTP-RUNBOOK.md §5.4 SQL 실행

    ### G7. NFR-01 운영 모니터링 게이트 활성화 (Phase 33 D-S2)
    - [ ] **G7.1** MariaDB slow_query_log = ON + long_query_time = 1.0 + my.cnf 영구화
      - 검증: `mysql -uroot -e "SHOW VARIABLES WHERE variable_name IN ('slow_query_log', 'long_query_time', 'slow_query_log_file');"`
    - [ ] **G7.2** logrotate 설정 (`/etc/logrotate.d/mariadb-slow`) 적용
    - [ ] **G7.3** MONITORING.md §4 정기 점검 사이클이 운영자 캘린더에 등록됨 (주간 월요일)

    ### G8. 디스크 + 백업 + 롤백 절차
    - [ ] **G8.1** 디스크 사용량 < 70% (`df -h /var/lib/mysql /var/log /opt/micesign`)
    - [ ] **G8.2** MariaDB 일일 dump 백업 cron 등록 (또는 사내 IT 백업 정책 적용)
    - [ ] **G8.3** Google Drive Service Account credentials 파일 권한 600 + backup 1부 별도 보관
    - [ ] **G8.4** 롤백 절차 작성 — 직전 .jar 보존 + 롤백 시 systemd unit 의 ExecStart 라인 swap 절차 (별도 파일 또는 본 §8 항목에 인라인)

    ### G9. 산출물 cross-reference 무결성
    - [ ] **G9.1** SMTP-RUNBOOK.md §6.4 가 MONITORING.md 를 cross-reference (33-02 verify 단계에서 자동 확인)
    - [ ] **G9.2** application-prod.yml.example 이 SMTP-RUNBOOK §2.1 에 명시 (33-01 산출물이 33-02 절차에 통합됨)

    ---

    ## §5. 출시 결정 기록 (출시 시점에 채움)

    | 항목 | 값 |
    |------|-----|
    | 출시 일시 | YYYY-MM-DD HH:MM (KST) |
    | 출시 담당자 | (이름) |
    | §4 모든 게이트 PASS | YES / NO (NO 시 사유) |
    | 운영 모니터링 게이트 활성화 (§G7) | YES / NO |
    | 다음 점검 예정일 | YYYY-MM-DD (출시일 + 7일) |

    ---

    *문서 끝. 본 audit 는 v1.2 의 단일 출시 결정 source — §1-3 검증 + §4 게이트 PASS + §5 결정 기록 완료 시 출시.*
    ```
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign && grep -q '## §4\. 출시 게이트 체크리스트' .planning/milestones/v1.2/AUDIT.md && grep -q '### G1\. DB 마이그레이션' .planning/milestones/v1.2/AUDIT.md && grep -q '### G2\. 환경변수' .planning/milestones/v1.2/AUDIT.md && grep -q '### G3\. SMTP' .planning/milestones/v1.2/AUDIT.md && grep -q '### G4\. BaseUrlGuard' .planning/milestones/v1.2/AUDIT.md && grep -q '### G5\. 5종 이벤트' .planning/milestones/v1.2/AUDIT.md && grep -q '### G6\. audit_log' .planning/milestones/v1.2/AUDIT.md && grep -q '### G7\. NFR-01' .planning/milestones/v1.2/AUDIT.md && grep -q '### G8\. 디스크' .planning/milestones/v1.2/AUDIT.md && grep -q 'flyway_schema_history' .planning/milestones/v1.2/AUDIT.md && grep -q 'telnet \$MAIL_HOST' .planning/milestones/v1.2/AUDIT.md && grep -q '## §5\. 출시 결정 기록' .planning/milestones/v1.2/AUDIT.md && [ "$(wc -l < .planning/milestones/v1.2/AUDIT.md)" -ge 250 ]</automated>
  </verify>
  <acceptance_criteria>
    - §4 9개 게이트 그룹 (G1-G9) 모두 작성
    - 각 게이트 항목에 자동 검증 명령 또는 수동 절차 명시
    - SMTP-RUNBOOK §5 / MONITORING §1 / Phase 29 D-D2 / Phase 33 D-C1 cross-reference
    - §5 출시 결정 기록 표 (출시 시점 채움)
    - 전체 250+ lines
  </acceptance_criteria>
  <done>
    출시 담당자가 §4 만 보고 위에서 아래로 모든 항목을 점검 + 체크하면 v1.2 출시 가능 여부를 결정할 수 있다. PASS/FAIL 명확.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 사용자 수동 5종 이벤트 smoke + audit_log SQL 스팟 (G5/G6 sign-off)</name>
  <what-built>
    AUDIT.md §4 의 G5 (5종 이벤트 수동 smoke) + G6 (audit_log COUNT=1 SQL 스팟) 항목.

    Task 1-3 가 작성한 §1 SC 매트릭스 / §2 Deferred / §3 Requirements 매핑 / §4 게이트 9개 모두 자동 검증 PASS 완료. 본 체크포인트는 G5 + G6 의 수동 검증 결과를 사용자가 직접 sign-off.
  </what-built>
  <how-to-verify>
    **사전 조건**: 33-02 SMTP-RUNBOOK.md §1-4 절차 완료 (사내 IT 협업 + .env.production 작성 + systemd EnvironmentFile + restart + BaseUrlGuard PASS) — 이는 출시 직전 운영 환경에서 수행. 개발 환경에서 본 체크포인트 수행 시 MailHog 사용.

    1. **MailHog 또는 운영 후보 SMTP 환경 준비**
       - 개발 환경: `docker run -d -p 1025:1025 -p 8025:8025 mailhog/mailhog` + `.env.production` 의 `MAIL_HOST=localhost MAIL_PORT=1025` 임시 설정 → application 재시작
       - 운영 환경 (출시 직전): SMTP-RUNBOOK §3-4 까지 완료된 상태에서 본 절차 수행

    2. **테스트 결재 문서 준비**
       - GENERAL 양식으로 1건 작성 (시나리오 1-3 용)
       - 별도 1건 작성 (시나리오 4 — 반려용)
       - 별도 1건 작성 (시나리오 5 — 회수용)
       - 각 문서: 기안자 본인 + 승인자 2명 (자기 자신 메일 사용 권장 — 수신 확인 용이)

    3. **SMTP-RUNBOOK.md §5.2 의 5 시나리오 표를 따라 순차 수행**
       - 각 시나리오 4 항목 ([MiceSign] prefix / 한글 subject / 본문 / 바로가기 버튼) 모두 PASS 인지 시각 확인
       - MailHog UI (http://localhost:8025) 또는 사내 메일 클라이언트로 수신 메일 검증
       - 각 시나리오마다 AUDIT.md §4 §G5.N 체크박스를 PASS 시 마크

    4. **audit_log + notification_log SQL 스팟 (SMTP-RUNBOOK §5.3 / §5.4)**
       \`\`\`sql
       -- 시나리오 1-5 각 문서별로 실행
       SELECT action_type, COUNT(*) AS cnt
       FROM audit_log
       WHERE document_id = ?
       GROUP BY action_type
       ORDER BY action_type;

       SELECT event_type, recipient_id, status, COUNT(*) AS cnt
       FROM notification_log
       WHERE document_id = ?
       GROUP BY event_type, recipient_id, status
       ORDER BY event_type, recipient_id;
       \`\`\`
       - 모든 action_type / event_type cnt = 1 인지 확인
       - cnt ≥ 2 발견 시 즉시 BLOCKING — Phase 29 D-D6 회귀 조사 필요
       - PASS 시 §G6.1 + §G6.2 체크박스 마크

    5. **AUDIT.md 저장 + 본인이 PASS sign-off 응답**
  </how-to-verify>
  <resume-signal>
    "approved" 또는 "G5 G6 모두 PASS — 출시 게이트 통과" 응답 시 다음 task (또는 33-05) 진행.

    issue 발견 시: cnt ≥ 2 발견 / 메일 미수신 / 한글 깨짐 등 — 구체적으로 보고. 본 체크포인트는 BLOCKING — issue 해결 (Phase 29 회귀 조사 또는 운영 환경 재구성) 후 재수행.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 시각 검증 (G5) → AUDIT 기록 | 신뢰 기반 sign-off. 자동 회귀 (Phase 29 GreenMail) 가 백업 |
| audit_log SQL 결과 → 출시 결정 (G6) | cnt = 1 fail 시 즉시 출시 보류. 검증 가능한 수치 기반 게이트 |
| stakeholder 가 §1 + §4 만 읽고 출시 결정 | §0 요약이 비기술 stakeholder 도 이해 가능한 표 + §4 PASS/FAIL 이진 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-14 | Repudiation | G5 sign-off 후 issue 발견 시 책임 추적 곤란 | mitigate | §5 출시 결정 기록 표에 출시 담당자 + 일시 명시. 본 plan Task 4 의 사용자 응답이 git commit 으로 기록됨. |
| T-33-15 | Tampering | G6 audit_log SQL 결과 조작 (수동 입력) | accept | sudo 권한 있는 운영자 수동 검증 — 50인 사내 시스템에서 internal threat 가정 X. cnt=1 자체가 검증 가능. |
| T-33-16 | Information Disclosure | AUDIT.md 가 git public mirror 시 사내 도메인 / 시스템 구조 노출 | accept | PRD 가 사내 git server 가정. public mirror 시 별도 redaction 절차 (본 plan 범위 밖). |
| T-33-17 | Denial of Service | G5 5종 smoke 가 운영 SMTP 에 부하 발생 | accept | 5 메일 발송 = 무시 가능 부하. 사내 IT 와 §1.1 협의 시 사전 통보. |
</threat_model>

<verification>
- AUDIT.md 4 섹션 (§1-4) + §0 요약 + §5 결정 기록 모두 작성
- §1 의 4 phase × 평균 5 SC = 약 20 SC 모두 자동/매뉴얼/Deferred 분류
- §2 의 5 deferred 항목 모두 보존 경로 cross-reference
- §3 의 v1.2 21개 ID 모두 등재
- §4 의 9개 게이트 그룹 모두 자동 또는 수동 검증 절차
- 사용자 G5/G6 sign-off 완료
- 250+ lines
</verification>

<success_criteria>
- 자동 검증 3개 task PASS
- Task 4 사용자 sign-off 완료 (G5/G6 모두 PASS 응답)
- 비기술 stakeholder 가 §0 + §4 만 보고 출시 가능 여부 판단 가능
- 모든 deferred 항목이 누락 없이 §2 에 추적됨
</success_criteria>

<output>
완료 후 `.planning/phases/33-e2e/33-04-SUMMARY.md` 작성:
- AUDIT.md 5 섹션 요약
- D-A1 4 섹션 (SC / Deferred / Requirements / 게이트) → AUDIT.md §1/§2/§3/§4 mapping
- Task 4 사용자 sign-off 결과 기록
- 33-05 final verification 으로 인계 — ROADMAP.md Phase 33 → [x] 마크 + STATE 정리
</output>
