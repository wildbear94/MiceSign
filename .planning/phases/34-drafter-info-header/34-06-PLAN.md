---
phase: 34-drafter-info-header
plan: 06
type: execute
wave: 4
depends_on: [34-01, 34-02, 34-03, 34-04, 34-05]
files_modified:
  - .planning/phases/34-drafter-info-header/34-HUMAN-UAT.md
autonomous: false
requirements:
  - PHASE-34-D-D5
  - PHASE-34-D-D6
  - PHASE-34-D-E3
  - PHASE-34-D-E4
tags:
  - verification
  - uat
  - regression

must_haves:
  truths:
    - "Full backend test suite passes (./gradlew test) — DocumentSubmitTest 3 new cases included; AuthControllerTest new case included"
    - "Full frontend test suite passes (npm test) — DrafterInfoHeader 3 cases included; no regression"
    - "tsc --noEmit + npm run build both PASS"
    - "User has manually verified DRAFT and SUBMITTED header rendering in all 7 form types (8 forms × 2 modes ≈ 14-16 manual scenarios)"
    - "Legacy fallback (현재 정보) badge verified on at least one pre-Phase-34 SUBMITTED document"
    - "Responsive layout (md+ 4-col vs sm 2x2) verified by viewport toggle"
  artifacts:
    - path: ".planning/phases/34-drafter-info-header/34-HUMAN-UAT.md"
      provides: "Manual checklist for the 14-16 visual scenarios + responsive + legacy fallback"
      contains: "DRAFT mode"
  key_links:
    - from: "Plan 34-06 UAT"
      to: "All preceding plans (34-01 through 34-05)"
      via: "single end-to-end manual gate certifying observable behavior across all integration points"
      pattern: "UAT pass"
---

<objective>
Final verification gate for Phase 34 — covers two responsibilities:

1. **Automated regression sweep:** Run full BE + FE test suites, tsc, build, and grep-level invariants to confirm no plan regressed another. Fast loop, fully autonomous.

2. **Human UAT checkpoint** (D-E3, D-D5, D-D6): User manually verifies the 14-point header rendering across DRAFT/SUBMITTED modes, the responsive layout (md+ 4-col vs sm 2x2), and the legacy "(현재 정보)" fallback. This is mandatory because UI-SPEC explicitly marks D-D5 and D-D6 as manual-only verifications (`34-VALIDATION.md §"Manual-Only Verifications"`).

Per the existing v1.2 milestone wrap-up convention (e.g., Phase 31-05's `31-HUMAN-UAT.md`), the user's checkpoint sign-off is the final phase gate.

Purpose: Catch any cross-plan regression (Plan 34-04's component change + Plan 34-05's site integration could subtly disagree at runtime) AND validate the only behaviors that automated tests cannot validate (visual layout, responsive breakpoints, real-DB legacy doc rendering).

Output: 1 new file (`34-HUMAN-UAT.md` — checklist matrix) + the user's checkpoint approval signal that closes Phase 34.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-drafter-info-header/34-CONTEXT.md
@.planning/phases/34-drafter-info-header/34-VALIDATION.md
@.planning/phases/34-drafter-info-header/34-UI-SPEC.md
@.planning/phases/34-drafter-info-header/34-01-SUMMARY.md
@.planning/phases/34-drafter-info-header/34-02-SUMMARY.md
@.planning/phases/34-drafter-info-header/34-03-SUMMARY.md
@.planning/phases/34-drafter-info-header/34-04-SUMMARY.md
@.planning/phases/34-drafter-info-header/34-05-SUMMARY.md

@.planning/phases/31-dashboard/31-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated regression — BE + FE test suites + tsc + build</name>
  <files>.planning/phases/34-drafter-info-header/34-HUMAN-UAT.md</files>
  <action>
Run the full automated suite for both stacks and capture results into the start of `34-HUMAN-UAT.md`:

1. **Backend full suite:**
   ```bash
   cd backend && ./gradlew test 2>&1 | tee /tmp/34-06-be-test.log
   ```
   - Capture summary line ("BUILD SUCCESSFUL" + total test count).
   - GATE: must be `BUILD SUCCESSFUL`. If failures, halt the plan and surface to user (do NOT proceed to UAT).

2. **Frontend full suite + tsc + build:**
   ```bash
   cd frontend && npm test 2>&1 | tee /tmp/34-06-fe-test.log
   cd frontend && npx tsc --noEmit 2>&1 | tee /tmp/34-06-fe-tsc.log
   cd frontend && npm run build 2>&1 | tee /tmp/34-06-fe-build.log
   ```
   - GATE: each command must succeed (test pass count, tsc 0 errors, build "built in" line).

3. **Grep-level invariants** (cross-plan integrity check):
   ```bash
   # No nested doc.drafter.* access remains anywhere in document/ feature
   grep -rn "doc\\.drafter\\." frontend/src/features/document/ | wc -l
   # Expected: 0
   
   # DrafterInfoHeader is imported by all 14 form files
   grep -l "DrafterInfoHeader" frontend/src/features/document/components/templates/*.tsx frontend/src/features/document/components/dynamic/*.tsx | wc -l
   # Expected: 14 (6 Edit + 6 ReadOnly + 2 Dynamic)
   
   # drafterSnapshot key written by exactly one site in BE
   grep -rn "drafterSnapshot" backend/src/main/java/ | wc -l
   # Expected: 1 (DocumentService.submitDocument single-site)
   
   # i18n keys in ko/document.json
   node -e "const j=require('./frontend/public/locales/ko/document.json'); console.log(Object.keys(j.drafterInfo||{}).length);"
   # Expected: 8
   ```

4. **Write the regression results section** to `.planning/phases/34-drafter-info-header/34-HUMAN-UAT.md` (use the same YAML frontmatter convention as `31-HUMAN-UAT.md` — fields: phase, plan, status=in-progress, created=<date>; wrap with three-dash separators on lines 1 and 5; do not nest within this PLAN body to avoid YAML parser confusion):

```markdown
[FRONTMATTER: phase / plan / status: in-progress / created: <date> — three-dash YAML wrap, format per 31-HUMAN-UAT.md]

# Phase 34 — HUMAN UAT

## Automated Regression Results

| Gate | Command | Result | Notes |
|------|---------|--------|-------|
| BE full suite | `./gradlew test` | <PASS/FAIL> | <test count> |
| FE full suite | `npm test` | <PASS/FAIL> | <test count> |
| TypeScript | `npx tsc --noEmit` | <PASS/FAIL> | 0 errors |
| Build | `npm run build` | <PASS/FAIL> | <duration> |
| Nested drafter grep | `grep -rn "doc\\.drafter\\." frontend/src/features/document/` | <count = 0> | Plan 34-01/34-05 cleaned up |
| Header import count | `grep -l "DrafterInfoHeader" ...` | <count = 14> | All 14 integration points |
| BE drafterSnapshot site count | `grep -rn "drafterSnapshot" backend/src/main/java/` | <count = 1> | DocumentService single-site |
| i18n key count | node count drafterInfo keys | <count = 8> | UI-SPEC contract |

(Continued in Task 2 — manual checklist below.)
```

5. If any gate fails, STOP. Do not advance to Task 2. Halt the plan with the failure diff. The orchestrator/user must triage before retrying.
  </action>
  <verify>
    <automated>cd backend && ./gradlew test 2>&1 | tail -5 | grep -q "BUILD SUCCESSFUL" || (echo "FAIL BE" && exit 1) ; cd frontend && npm test 2>&1 | tail -10 | grep -E "passed|✓" | head -5 ; cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS" | xargs -I {} test {} -eq 0 || (echo "FAIL tsc" && exit 1) ; cd frontend && npm run build 2>&1 | tail -5 | grep -E "built|✓" || (echo "FAIL build" && exit 1) ; grep -rn "doc\\.drafter\\." frontend/src/features/document/ | wc -l | xargs -I {} test {} -eq 0 && echo "OK invariants" || (echo "FAIL nested drafter access still present" && exit 1)</automated>
  </verify>
  <done>
- `./gradlew test` BUILD SUCCESSFUL.
- `npm test` all green.
- `npx tsc --noEmit` zero errors.
- `npm run build` succeeds.
- All 4 grep-level invariants hold.
- `34-HUMAN-UAT.md` exists with regression results filled in.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 14-point HUMAN UAT + responsive + legacy fallback verification (D-D5/D6/E3)</name>
  <action>Walk through the UAT matrix below and mark each row in 34-HUMAN-UAT.md as PASS/FAIL with notes. Reply via the resume-signal when complete. (Detailed scenarios in <how-to-verify> below.)</action>
  <what-built>
  Phase 34 ships the always-on DrafterInfoHeader across all approval forms. After Plan 34-01~05:
  
  - **FE foundation:** DocumentDetailResponse type aligned to BE flat fields; DrafterInfoHeader component + 3-case vitest spec; UserProfile interface extended with departmentName/positionName; ko/document.json gains 8 i18n keys under `drafterInfo.*`; templateRegistry props extended (drafterLive on Edit; drafterSnapshot+drafterLive+submittedAt on ReadOnly).
  - **BE:** AuthService.buildUserProfile null-safe extracts dept/position names into login + refresh responses (verified by AuthControllerTest); DocumentService.submitDocument captures immutable drafterSnapshot in form_data JSON at the DRAFT→SUBMITTED transition (D-C7 rollback enforced; verified by 3 DocumentSubmitTest cases).
  - **FE integration:** All 14 form components (6 built-in Edit + 6 built-in ReadOnly + DynamicCustomForm + DynamicCustomReadOnly) render `<DrafterInfoHeader ... />` as their first child; DocumentEditorPage threads drafterLive (existingDoc OR authStore.user); DocumentDetailPage threads drafterSnapshot/drafterLive/submittedAt to ReadOnlyComponent + removes the duplicate 기안자 meta-grid cell.
  
  Automated regression (Task 1) confirmed BE + FE suites pass and 4 grep-level invariants hold.
  </what-built>
  
  <how-to-verify>
  Walk through the matrix below. Open `34-HUMAN-UAT.md` and check off each row as you verify it.
  
  **Setup:**
  1. Start the dev server: `cd backend && ./gradlew bootRun` and `cd frontend && npm run dev`
  2. Log in to the app as a user who has both a department AND a position assigned (SUPER_ADMIN should suffice — V2 seed gives departmentId=1; verify position is non-null).
  3. Open Chrome DevTools → Toggle Device Toolbar (Ctrl+Shift+M) for responsive testing later.
  
  **Matrix A — Built-in form types × DRAFT/SUBMITTED (12 scenarios — 6 forms × 2 modes):**
  
  For each of GENERAL / EXPENSE / LEAVE / PURCHASE / BUSINESS_TRIP / OVERTIME forms:
  
  1. **DRAFT mode** — Click "새 문서 작성" → select form type → on the editor page, verify the DrafterInfoHeader appears at the TOP of the form (above the existing template label / title / body).
     - 부서: shows your department name (e.g., 시스템관리부)
     - 직위·직책: shows your position name (e.g., 시스템관리자)
     - 기안자: shows your name
     - 기안일: shows `—` (em dash placeholder — DRAFT not yet submitted)
     - NO `(현재 정보)` badge
     - **Pass criteria:** all 4 cells filled correctly; `—` for date.
  
  2. Save the draft → re-open from "내 문서" → header still shows DRAFT mode (your live info, `—` for date).
  
  3. Add an approver to the approval line → click 상신. Confirm submission succeeds.
  
  4. Open the submitted document → verify the DrafterInfoHeader now shows SUBMITTED mode:
     - Same 4 fields (부서/직위·직책/기안자) — but these are now SNAPSHOTTED, not live.
     - 기안일: shows the actual submission date in `YYYY. MM. DD.` format (e.g., `2026. 04. 29.`).
     - NO `(현재 정보)` badge.
     - **Pass criteria:** snapshot date is correct; no badge; meta-grid below the header does NOT show a separate 기안자 cell (Plan 34-05 removed it).
  
  Repeat for all 6 form types.
  
  **Matrix B — CUSTOM dynamic form × DRAFT/SUBMITTED (2 scenarios):**
  
  5. Open admin → 양식 관리 → create or pick an existing CUSTOM template (or use a Phase 32 preset like "회의록" or "품의서" if available).
  6. Click "새 문서 작성" → select that CUSTOM template → on editor page, verify the DrafterInfoHeader appears at the TOP (above the dynamic form fields).
  7. Save → submit → verify SUBMITTED mode shows snapshotted info.
  
  **Matrix C — Responsive layout (D-D5):**
  
  8. On any submitted document detail page, open DevTools and toggle viewport between md+ (≥768px, e.g., 1024×768) and sm (<768px, e.g., 375×667 iPhone SE).
  9. **Pass criteria:** md+ shows 4-column row (부서 | 직위·직책 | 기안자 | 기안일); sm shows 2-column 2-row grid (row 1 = 부서/직위·직책; row 2 = 기안자/기안일).
  10. No text overflow / truncation in either viewport for typical Korean dept/name/position values.
  
  **Matrix D — Legacy fallback (D-D4):**
  
  11. Find a pre-Phase-34 SUBMITTED document (any document submitted before today's deployment of Phase 34 — its `form_data` lacks `drafterSnapshot`).
      - If no such doc exists in dev DB, create one by manually editing the DB: `UPDATE document_content SET form_data = '{"someExistingKey":"value"}' WHERE document_id = <some_submitted_doc_id>;` to remove any drafterSnapshot key.
  12. Open the document detail page.
  13. **Pass criteria:** the header renders 4 fields using the user's CURRENT live dept/position (not snapshot) AND shows the `(현재 정보)` badge in amber color next to the 기안자 name.
  14. Hover the badge if there's a tooltip; verify wording is `(현재 정보)` exactly.
  
  **Matrix E — Dark mode (UI-SPEC mentions, D-D5):**
  
  15. Toggle OS-level dark mode (macOS: System Settings → Appearance → Dark; Windows: Settings → Personalization → Dark).
  16. Reload the document page.
  17. **Pass criteria:** header background is gray-900, borders gray-700, label text gray-400, value text gray-50, badge amber-400. No legibility issues.
  
  **Matrix F — Korean i18n display:**
  
  18. Confirm all 4 labels render in Korean: 부서, 직위·직책 (note the U+00B7 middle dot, NOT a regular period or hyphen), 기안자, 기안일.
  19. Confirm the badge text is `(현재 정보)`.
  20. Confirm the placeholder is `—` (em dash U+2014).
  
  **Total scenarios:** 14 (Matrix A) + 2 (Matrix B) + 1 responsive sweep + 1 legacy + 1 dark mode + 1 i18n = ~19 manual checks, ≈30~45 minutes of focused QA.
  
  **Mark each row in `34-HUMAN-UAT.md` PASS or FAIL with notes.**
  </how-to-verify>
  
  <resume-signal>
  Reply with one of:
  - `approved` — All UAT scenarios PASS. Phase 34 closes.
  - `approved with notes: <text>` — All scenarios PASS but with minor cosmetic notes for follow-up.
  - `gaps: <list>` — One or more scenarios FAILED. Provide the failing matrix row(s) and observed-vs-expected. Phase 34 does NOT close; orchestrator should run `/gsd-plan-phase 34 --gaps` to plan remediation.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → manual UAT browser session | Human verification — no automated trust boundary |
| developer → modified /tmp logs | Local-only test artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-06-01 | Repudiation | UAT outcome not captured | mitigate | `34-HUMAN-UAT.md` records each matrix row PASS/FAIL/notes — reproducible audit trail (Phase 31-05 precedent). |
| T-34-06-02 | Denial of Service | half-fixed phase reaches production | mitigate | Task 1 gates the entire plan on full BE+FE green. Task 2 is a blocking checkpoint — phase cannot close without explicit `approved` signal. |
| T-34-06-03 | Information Disclosure | UAT log shows real seed dept/name | accept | UAT runs against dev DB; SUMMARY.md should redact specific names if shared widely. The seed names (시스템관리자) are not sensitive. |
</threat_model>

<verification>
After both tasks:
- `34-HUMAN-UAT.md` exists with regression results + 19-row matrix table all marked PASS.
- User signal `approved` (or `approved with notes`) recorded in resume conversation.
- BE + FE full suites green; tsc + build green; 4 grep-level invariants hold.
</verification>

<success_criteria>
1. Backend `./gradlew test` BUILD SUCCESSFUL.
2. Frontend `npm test`, `npx tsc --noEmit`, `npm run build` all PASS.
3. 4 grep-level invariants hold (no `doc.drafter.*`, 14 DrafterInfoHeader imports, 1 BE drafterSnapshot site, 8 i18n keys).
4. UAT matrix (≈19 rows) all PASS — captured in `34-HUMAN-UAT.md`.
5. User checkpoint signal `approved` (or equivalent).
6. Plan committed atomically as `verify(34-06): full regression + 14-point UAT — Phase 34 closes`.
</success_criteria>

<output>
After approval, create `.planning/phases/34-drafter-info-header/34-06-SUMMARY.md` per @$HOME/.claude/get-shit-done/templates/summary.md.

Required SUMMARY content:
- Regression gate results (4 commands all green).
- UAT matrix completion (19/19 PASS or list any with notes).
- User approval signal recorded.
- Phase 34 closure note: ROADMAP.md Phase 34 entry should be marked `[x]` with completion date in this SUMMARY's wrap-up step.

Final cleanup (post-approval, pre-commit):
1. Update `.planning/ROADMAP.md`:
   - `### Phase 34: 양식 기안자 정보 헤더 자동 채움` → `Plans:` count → 6/6 complete
   - Phase 34 row in the progress table → `Complete | <date>`
2. Update `.planning/STATE.md`:
   - `stopped_at:` → `Phase 34 complete`
   - `last_activity:` → `<date> — Phase 34 wrap-up complete`
   - Add to `### Decisions` accumulator: bullets for D-A1/D-D6/D-G1 outcomes (final-form summaries).
3. Update `34-VALIDATION.md` frontmatter `nyquist_compliant: true` and `wave_0_complete: true`.

Commit message for the wrap-up: `docs(34-06): close phase 34 — UAT approved, ROADMAP/STATE/VALIDATION synced`.
</output>
