---
phase: 29
plan: 03
type: execute
wave: 2
depends_on: [29-01, 29-02]
files_modified:
  - backend/src/main/java/com/micesign/service/email/ApprovalEmailSender.java
  - backend/src/main/java/com/micesign/service/EmailService.java
autonomous: true
requirements:
  - NOTIF-01
  - NOTIF-02
  - NOTIF-03
  - NOTIF-04
  - NOTIF-05
  - NFR-02
user_setup: []

must_haves:
  truths:
    - "ApprovalEmailSender.send() 가 PENDING-first 3단계(INSERT PENDING → mailSender.send() → UPDATE SUCCESS/FAILED/RETRY) 로직으로 완성된다"
    - "mailSender == null 일 때 [EMAIL STUB] 로그 + NotificationLog.status=SUCCESS 로 마감 (D-D7 null-safe)"
    - "@Retryable 이 MailSendException 에만 재시도(3회), MailAuthentication/Parse 예외는 즉시 FAILED"
    - "@Retryable 재시도 전 기존 PENDING/RETRY 행의 retry_count 가 UPDATE 로 증분된다 (D-A9)"
    - "EmailService.sendNotification() 이 ApprovalEmailSender 를 주입받아 호출하며, 리스너 안에서 자기 send() 호출을 하지 않는다 (Pitfall 1 회피)"
    - "EmailService.determineRecipients() 가 RETIRED/INACTIVE 를 제외하고, WITHDRAW 대상에서 User.id 기준으로 중복 제거한다"
    - "EmailService 에서 audit_log 추가 INSERT 를 하지 않는다 (NFR-03)"
    - "MimeMessageHelper 가 3-arg (message, true, \"UTF-8\") 로 UTF-8 강제된다 (Pitfall 3)"
    - "제목이 [MiceSign] {actionLabel}: {docNumber} {title} 포맷으로 고정된다 (D-C1)"
  artifacts:
    - path: "backend/src/main/java/com/micesign/service/email/ApprovalEmailSender.java"
      provides: "완성된 send/findOrCreatePendingLog/buildSubject/buildContext 로직"
      contains: "mailSender.send(message)"
      min_lines: 150
    - path: "backend/src/main/java/com/micesign/service/EmailService.java"
      provides: "ApprovalEmailSender 주입 + .distinct + ACTIVE filter + stub 제거"
      contains: "approvalEmailSender.send"
  key_links:
    - from: "EmailService.sendNotification"
      to: "ApprovalEmailSender.send"
      via: "constructor injection (별도 빈 호출 — 프록시 체인 보장)"
      pattern: "approvalEmailSender\\.send\\("
    - from: "ApprovalEmailSender.send"
      to: "notificationLogRepository.save (via persistLog)"
      via: "@Lazy self-injection + @Transactional(REQUIRES_NEW)"
      pattern: "self\\.persistLog"
    - from: "ApprovalEmailSender.send"
      to: "templateEngine.process(email/approval-{slug})"
      via: "Thymeleaf Context + 5종 slug 매핑"
      pattern: "templateEngine\\.process"
    - from: "EmailService.determineRecipients"
      to: "User.status = ACTIVE 필터"
      via: "stream filter + Collectors.toMap(User::getId, ...) distinct"
      pattern: "UserStatus\\.ACTIVE"
---

<objective>
Plan 01의 ApprovalEmailSender 스켈레톤에 **실제 send 로직**을 채우고, EmailService 의 stub 메서드를 **ApprovalEmailSender 위임 + .distinct + ACTIVE 필터** 로 리팩터한다. 이 Plan 완료 시점에 5종 결재 이벤트가 실제 JavaMailSender (또는 null 시 stub log) 로 발송되는 end-to-end 경로가 완성된다.

목적: NOTIF-01..05 + NFR-02 의 런타임 동작 자체를 구현. D-A1 PENDING-first + D-A5 예외 분류 + D-A7 distinct + D-A8 email snapshot + D-B2/B3 Retry/Recover + D-C1 subject + D-C2 REFERENCE 제외 + D-C6 rejectComment + D-D7 null-safe 를 모두 물리화.

산출물:
- `ApprovalEmailSender.java` 완성 (스켈레톤 → 실 로직 채움): `send()` + `recover()` + `findOrCreatePendingLog()` + `persistLog()` + `buildSubject()` + `buildContext()` + `toTemplateSlug()` + `truncate()`
- `EmailService.java` 수정: `approvalEmailSender` 주입, `sendToRecipient()` 제거, `determineRecipients()` 에 ACTIVE 필터 + distinct 추가, stub `sendEmail()` 제거
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-smtp-retrofit/29-CONTEXT.md
@.planning/phases/29-smtp-retrofit/29-RESEARCH.md
@.planning/phases/29-smtp-retrofit/29-PATTERNS.md
@.planning/phases/29-smtp-retrofit/29-01-SUMMARY.md
@backend/src/main/java/com/micesign/service/EmailService.java
@backend/src/main/java/com/micesign/service/email/ApprovalEmailSender.java
@backend/src/main/java/com/micesign/service/RegistrationEmailService.java
@backend/src/main/java/com/micesign/domain/Document.java
@backend/src/main/java/com/micesign/domain/User.java
@backend/src/main/java/com/micesign/domain/ApprovalLine.java
@backend/src/main/java/com/micesign/domain/NotificationLog.java
@backend/src/main/java/com/micesign/repository/NotificationLogRepository.java
@backend/src/main/java/com/micesign/domain/enums/NotificationEventType.java
@backend/src/main/java/com/micesign/domain/enums/NotificationStatus.java
@backend/src/main/java/com/micesign/domain/enums/UserStatus.java
@backend/src/main/java/com/micesign/domain/enums/ApprovalLineType.java
@CLAUDE.md

<interfaces>
**Plan 29-01이 이미 확정한 contract (변경 금지):**
- `ApprovalEmailSender.send(Document doc, User recipient, NotificationEventType eventType)` — void 반환
- `ApprovalEmailSender.recover(MailException e, Document doc, User recipient, NotificationEventType eventType)` — void 반환
- `NotificationLogRepository.findByDocumentIdAndEventTypeAndRecipientId(Long, String, Long)` — Optional<NotificationLog>
- `@Retryable(retryFor={MailSendException.class}, noRetryFor={MailAuthenticationException, MailParseException}, maxAttempts=3, backoff=@Backoff(delayExpression = "#{${app.mail.retry.delay-ms:300000}}"))`

**actionLabel 한글 매핑 (D-C1):**
- SUBMIT → "결재 요청"
- APPROVE → "승인"
- FINAL_APPROVE → "최종 승인"
- REJECT → "반려"
- WITHDRAW → "회수"

**template slug 매핑 (Plan 02 산출물과 일치):**
- SUBMIT → "submit"
- APPROVE → "approve"
- FINAL_APPROVE → "final-approve"
- REJECT → "reject"
- WITHDRAW → "withdraw"

**Thymeleaf Context 변수 (Plan 02 템플릿이 기대하는 키):**
- docNumber, docTitle, drafterName, drafterDepartment, recipientName, actionLabel, eventTime, approvalUrl
- rejectComment (REJECT 이벤트 한정 — null 허용, 템플릿에서 th:if 로 방어)

**수신자 결정 규칙 (D-C2 + PATTERNS §2C + RESEARCH §Code Examples §2 L764-780):**
- SUBMIT: currentStep 의 non-REFERENCE 승인자들
- APPROVE: 다음 step (중간 승인 후 이전됨) 의 non-REFERENCE 승인자들
- FINAL_APPROVE: 기안자 1명
- REJECT: 기안자 1명
- WITHDRAW: 전원 (REFERENCE 포함)
- 공통 필터: User.status == ACTIVE, User.id 기준 distinct
</interfaces>
</context>

<tasks>

<task id="T1" type="auto" tdd="false">
  <name>Task 1: EmailService 리팩터 (ApprovalEmailSender 주입 + determineRecipients distinct + ACTIVE filter + stub 제거)</name>
  <files>
    - backend/src/main/java/com/micesign/service/EmailService.java
  </files>
  <read_first>
    - backend/src/main/java/com/micesign/service/EmailService.java (전체 — 현재 stub 구조 + getActionLabel + determineRecipients 보존 대상 확인)
    - backend/src/main/java/com/micesign/domain/enums/UserStatus.java (ACTIVE enum 값 확인)
    - backend/src/main/java/com/micesign/domain/enums/ApprovalLineType.java (REFERENCE 값 확인)
    - backend/src/main/java/com/micesign/domain/User.java (getStatus 메서드 + id 필드 타입 확인)
    - .planning/phases/29-smtp-retrofit/29-RESEARCH.md §Code Examples §2 (L735-781)
    - .planning/phases/29-smtp-retrofit/29-PATTERNS.md §2 (L150-185)
  </read_first>
  <action>
현재 `EmailService.java`는 5종 결재 이벤트 리스너이지만 발송 로직이 stub 상태. 본 태스크는:

**유지:**
- `@TransactionalEventListener(phase = TransactionPhase.AFTER_COMMIT) + @Async` 애너테이션 (D-B1 외부 구조)
- `sendNotification(ApprovalNotificationEvent event)` 메서드 시그니처
- `determineRecipients(Document, NotificationEventType)` 의 switch 분기 골격 (SUBMIT/APPROVE/FINAL_APPROVE/REJECT/WITHDRAW)
- `getActionLabel(NotificationEventType)` — 하지만 이 메서드는 ApprovalEmailSender로 이관되므로 EmailService에서는 **제거**

**추가 (생성자 주입):**
```java
private final ApprovalEmailSender approvalEmailSender;
// 기존 생성자에 파라미터 추가
```

**제거:**
- `sendToRecipient(...)` private helper 메서드 전체 (현재 send-first + save-last 로직 — D-A1 위반)
- `sendEmail(to, subject, templateName, variables)` stub method (발송 로직이 ApprovalEmailSender 로 이관됨)
- subject 조립 라인 (EmailService.sendNotification 내부의 `String subject = "[MiceSign] " + ...` — ApprovalEmailSender.buildSubject가 담당)
- `getActionLabel(NotificationEventType)` — ApprovalEmailSender.buildSubject 가 내재화

**수정된 sendNotification() 구조 (RESEARCH §Code Examples §2 L740-762 그대로):**

```java
@TransactionalEventListener(phase = TransactionPhase.AFTER_COMMIT)
@Async
public void sendNotification(ApprovalNotificationEvent event) {
    try {
        Document doc = documentRepository.findByIdWithDrafter(event.getDocumentId()).orElse(null);
        if (doc == null) {
            log.warn("Document not found for notification: docId={}", event.getDocumentId());
            return;
        }

        NotificationEventType type = NotificationEventType.valueOf(event.getEventType());
        List<User> recipients = determineRecipients(doc, type);

        if (recipients.isEmpty()) {
            log.debug("No recipients for notification: docId={}, event={}", doc.getId(), type);
            return;
        }

        for (User recipient : recipients) {
            approvalEmailSender.send(doc, recipient, type);  // 별도 빈 호출 — 프록시 체인 보장 (D-B1)
        }
    } catch (Exception e) {
        // 리스너 내부에서 예외는 삼켜서 이벤트 루프 안정화 — 실 send 실패는 @Retryable 가 notification_log 에 기록
        log.error("Failed to dispatch approval notification: docId={}, event={}, error={}",
                event.getDocumentId(), event.getEventType(), e.getMessage(), e);
    }
}
```

**수정된 determineRecipients() 구조 (RESEARCH §Code Examples §2 L764-780 + D-A7 distinct, D-C2 REFERENCE filter, NOTIF-04 ACTIVE):**

```java
private List<User> determineRecipients(Document doc, NotificationEventType type) {
    List<ApprovalLine> lines = approvalLineRepository.findByDocumentIdOrderByStepOrderAsc(doc.getId());

    Stream<User> baseStream = switch (type) {
        case SUBMIT, APPROVE -> lines.stream()
                .filter(l -> l.getStepOrder().equals(doc.getCurrentStep()))
                .filter(l -> l.getLineType() != ApprovalLineType.REFERENCE)     // D-C2
                .map(ApprovalLine::getApprover);
        case FINAL_APPROVE, REJECT -> Stream.of(doc.getDrafter());
        case WITHDRAW -> lines.stream().map(ApprovalLine::getApprover);          // 전원 포함 (REFERENCE 도)
    };

    // NOTIF-04 ACTIVE-only + D-A7 distinct by User.id (User entity 의 equals/hashCode 미구현 방어)
    return baseStream
            .filter(u -> u != null && u.getStatus() == UserStatus.ACTIVE)
            .collect(Collectors.toMap(
                    User::getId,
                    Function.identity(),
                    (a, b) -> a,                                                  // 중복 key 시 첫 번째 유지
                    LinkedHashMap::new))                                          // insertion order 보존
            .values().stream().toList();
}
```

**새 import 추가 (기존 import 외에):**
```java
import com.micesign.domain.enums.UserStatus;
import com.micesign.service.email.ApprovalEmailSender;
import java.util.LinkedHashMap;
import java.util.function.Function;
import java.util.stream.Collectors;
```

**중요 — audit_log INSERT 금지 (NFR-03, D-A10):**
- 이 파일 전체에서 `auditLogService`, `auditLogRepository`, `audit_log` 에 대한 INSERT/save/log 호출이 존재하면 **모두 제거**
- Read 시점에 `grep -n "auditLog" EmailService.java` 로 확인 후 발견되면 라인 제거
- 현재 EmailService는 audit 를 만지지 않는 것으로 확인됨 (CONTEXT canonical_refs) — 만약 있다면 이 Task에서 제거

**제거 대상 private helpers (확인 후 제거):**
- `sendToRecipient(...)` — 전체
- `sendEmail(...)` — 전체
- `getActionLabel(...)` — ApprovalEmailSender.buildSubject 가 내재화하므로 EmailService 에서 제거
- subject 문자열 조립 변수/메서드

**stub-remove 검증:**
- 파일 라인 수가 기존 대비 줄어야 함 (sendToRecipient + sendEmail + getActionLabel 제거)
- `grep -c "notificationLogRepository" EmailService.java` = 0 (PENDING/SUCCESS/FAILED 관리는 ApprovalEmailSender 로 이관)
  </action>
  <verify>
    <automated>grep -q "approvalEmailSender.send" backend/src/main/java/com/micesign/service/EmailService.java && grep -q "UserStatus.ACTIVE" backend/src/main/java/com/micesign/service/EmailService.java && grep -q "Collectors.toMap" backend/src/main/java/com/micesign/service/EmailService.java && grep -q "ApprovalLineType.REFERENCE" backend/src/main/java/com/micesign/service/EmailService.java && ! grep -q "sendToRecipient\|private.*sendEmail\|getActionLabel" backend/src/main/java/com/micesign/service/EmailService.java && ! grep -q "auditLog" backend/src/main/java/com/micesign/service/EmailService.java && cd backend && ./gradlew compileJava --quiet</automated>
  </verify>
  <done>
    - `ApprovalEmailSender approvalEmailSender` 생성자 주입 추가
    - `sendNotification()` 리스너가 `approvalEmailSender.send(doc, recipient, type)` 호출로 교체
    - `determineRecipients()` 에 `UserStatus.ACTIVE` 필터 + `Collectors.toMap(User::getId, ..., LinkedHashMap::new)` distinct 추가
    - `sendToRecipient`, `sendEmail`, `getActionLabel` 제거
    - `notificationLogRepository` 직접 참조 제거 (의존성 ApprovalEmailSender 로 이관)
    - `auditLog*` 참조 0건
    - `./gradlew compileJava` 성공
  </done>
</task>

<task id="T2" type="auto" tdd="false">
  <name>Task 2: ApprovalEmailSender 실 로직 구현 (PENDING-first + send + render + Recover)</name>
  <files>
    - backend/src/main/java/com/micesign/service/email/ApprovalEmailSender.java
  </files>
  <read_first>
    - backend/src/main/java/com/micesign/service/email/ApprovalEmailSender.java (Plan 01 스켈레톤 — 채워야 할 메서드 확인)
    - backend/src/main/java/com/micesign/service/RegistrationEmailService.java (L153-187 NotificationLog setter + MimeMessageHelper 호출 패턴)
    - backend/src/main/java/com/micesign/domain/Document.java (getId, getTitle, getDocNumber, getDrafter, getApprovalLines 메서드 시그니처 확인)
    - backend/src/main/java/com/micesign/domain/User.java (getId, getEmail, getName, getDepartment, getStatus)
    - backend/src/main/java/com/micesign/domain/ApprovalLine.java (getStatus, getComment — REJECT comment 추출용)
    - backend/src/main/java/com/micesign/domain/enums/ApprovalLineStatus.java (REJECTED 값 확인)
    - .planning/phases/29-smtp-retrofit/29-RESEARCH.md §Code Examples §1 (L522-731) — 전체 스켈레톤
    - .planning/phases/29-smtp-retrofit/29-PATTERNS.md §1C/1D (L110-147) — MimeMessageHelper + NotificationLog setter 체인
  </read_first>
  <action>
Plan 01의 스켈레톤에 **실 로직을 채움**. `UnsupportedOperationException` 두 곳(`send`, `findOrCreatePendingLog`)을 실 구현으로 교체.

**중요 — 기존 스켈레톤에서 유지:**
- 패키지 `com.micesign.service.email`
- `@Component` + Logger + `ERROR_MESSAGE_MAX = 255`
- 생성자 (JavaMailSender null-safe + @Lazy self 주입)
- `@Value("${app.base-url:http://localhost:5173}")` + `@Value("${spring.mail.username:noreply@micesign.com}")`
- `@Retryable(retryFor={MailSendException.class}, noRetryFor={MailAuthenticationException.class, MailParseException.class}, maxAttempts=3, backoff=@Backoff(delayExpression = "#{${app.mail.retry.delay-ms:300000}}"))` 애너테이션 (Plan 01 Task 3 에서 이미 delayExpression 으로 작성됨)
- `@Recover public void recover(...)` 시그니처
- `@Transactional(propagation=Propagation.REQUIRES_NEW)` on persistLog/findOrCreatePendingLog

**구현할 메서드:**

**A. `send(Document doc, User recipient, NotificationEventType eventType)` 실 로직 (D-A1 PENDING-first):**

```java
@Retryable(
        retryFor = { MailSendException.class },
        noRetryFor = { MailAuthenticationException.class, MailParseException.class },
        maxAttempts = 3,
        backoff = @Backoff(delayExpression = "#{${app.mail.retry.delay-ms:300000}}"))
public void send(Document doc, User recipient, NotificationEventType eventType) {
    // Step 1: PENDING row 획득 (idempotent — 같은 dedup key 존재하면 기존 row 재사용)
    NotificationLog notifLog = self.findOrCreatePendingLog(doc, recipient, eventType);

    // Step 2: stub mode (D-D7 null-safe) — MAIL_HOST 미설정 개발 환경
    if (mailSender == null) {
        log.info("[EMAIL STUB] To: {}, Subject: {}", recipient.getEmail(), notifLog.getSubject());
        notifLog.setStatus(NotificationStatus.SUCCESS);
        notifLog.setSentAt(LocalDateTime.now());
        self.persistLog(notifLog);
        return;
    }

    // Step 3: Thymeleaf render + MimeMessage 조립 + 실 SMTP send
    try {
        String templateName = "email/approval-" + toTemplateSlug(eventType);
        org.thymeleaf.context.Context ctx = buildContext(doc, recipient, eventType);
        String html = templateEngine.process(templateName, ctx);

        jakarta.mail.internet.MimeMessage message = mailSender.createMimeMessage();
        MimeMessageHelper helper = new MimeMessageHelper(message, true, "UTF-8");  // D-Pitfall 3
        helper.setFrom("MiceSign <" + fromAddress + ">");                          // D-C5
        helper.setTo(recipient.getEmail());
        helper.setSubject(notifLog.getSubject());                                   // [MiceSign] 포맷 (findOrCreatePendingLog에서 생성)
        helper.setText(html, true);                                                 // HTML body

        mailSender.send(message);                                                   // may throw MailSendException/...

        // 성공 UPDATE (D-A1 step 3)
        notifLog.setStatus(NotificationStatus.SUCCESS);
        notifLog.setSentAt(LocalDateTime.now());
        self.persistLog(notifLog);

    } catch (MailSendException e) {
        // Transient — @Retryable 가 재진입. 재시도 직전에 RETRY 상태로 UPDATE (D-A9).
        notifLog.setStatus(NotificationStatus.RETRY);
        notifLog.setRetryCount(notifLog.getRetryCount() + 1);
        self.persistLog(notifLog);
        throw e;  // trigger @Retryable

    } catch (MailAuthenticationException | MailParseException e) {
        // Permanent — noRetryFor 이므로 @Retryable 이 건너뜀. 즉시 FAILED (D-A5).
        notifLog.setStatus(NotificationStatus.FAILED);
        notifLog.setErrorMessage(truncate(e.getClass().getSimpleName() + ": " + e.getMessage()));
        self.persistLog(notifLog);
        log.error("Permanent mail failure (no retry): to={}, docId={}, event={}, error={}",
                recipient.getEmail(), doc.getId(), eventType, e.getMessage());
        // do NOT rethrow — recovery inline

    } catch (jakarta.mail.MessagingException e) {
        // MimeMessageHelper setters 에서 발생 가능 — 영구 실패로 간주
        notifLog.setStatus(NotificationStatus.FAILED);
        notifLog.setErrorMessage(truncate("MessagingException: " + e.getMessage()));
        self.persistLog(notifLog);
        log.error("Unexpected messaging failure: to={}, docId={}, event={}, error={}",
                recipient.getEmail(), doc.getId(), eventType, e.getMessage());
    }
}
```

**B. `recover(MailException e, Document doc, User recipient, NotificationEventType eventType)` (D-B3):**

```java
@Recover
public void recover(MailException e, Document doc, User recipient, NotificationEventType eventType) {
    // maxAttempts=3 소진 후 호출됨. send() 내부의 MailSendException catch가 이미 RETRY 로 UPDATE 해 놓았음.
    // 여기서는 최종 FAILED 로 마감.
    NotificationLog notifLog = self.findOrCreatePendingLog(doc, recipient, eventType);
    notifLog.setStatus(NotificationStatus.FAILED);
    notifLog.setErrorMessage(truncate(e.getClass().getSimpleName() + ": " + e.getMessage()));
    self.persistLog(notifLog);
    log.error("Approval email failed after 3 retries: to={}, docId={}, event={}, error={}",
            recipient.getEmail(), doc.getId(), eventType, e.getMessage());
}
```

**C. `findOrCreatePendingLog(Document doc, User recipient, NotificationEventType type)` 실 로직:**

```java
@Transactional(propagation = Propagation.REQUIRES_NEW)
public NotificationLog findOrCreatePendingLog(Document doc, User recipient, NotificationEventType type) {
    return notificationLogRepository
            .findByDocumentIdAndEventTypeAndRecipientId(doc.getId(), type.name(), recipient.getId())
            .orElseGet(() -> {
                NotificationLog fresh = new NotificationLog();
                fresh.setRecipient(recipient);
                fresh.setRecipientEmail(recipient.getEmail());      // D-A8: 발송 시점 email 스냅샷
                fresh.setDocumentId(doc.getId());
                fresh.setEventType(type.name());                    // String 저장 (Plan 01에서 확인한 필드 타입)
                fresh.setSubject(buildSubject(doc, type));
                fresh.setStatus(NotificationStatus.PENDING);
                fresh.setRetryCount(0);
                return notificationLogRepository.save(fresh);
            });
}
```

**주의:** `NotificationLog.eventType` 의 실제 필드 타입을 Plan 01 Task 2 Read 시점에 확인한 결과에 따름.
- 필드가 `String` 이면 `type.name()` 그대로 (위 코드)
- 필드가 `NotificationEventType` enum 이면 `fresh.setEventType(type)` 로 변경 + repository 시그니처도 `NotificationEventType` 사용
- Plan 01 SUMMARY에 기록된 최종 타입을 따를 것

**D. `buildSubject(Document doc, NotificationEventType type)` — D-C1 포맷:**

```java
private String buildSubject(Document doc, NotificationEventType type) {
    String actionLabel = switch (type) {
        case SUBMIT -> "결재 요청";
        case APPROVE -> "승인";
        case FINAL_APPROVE -> "최종 승인";
        case REJECT -> "반려";
        case WITHDRAW -> "회수";
    };
    String docNo = doc.getDocNumber() != null ? doc.getDocNumber() : "DRAFT";
    return "[MiceSign] " + actionLabel + ": " + docNo + " " + doc.getTitle();
}
```

**E. `buildContext(Document doc, User recipient, NotificationEventType type)` — Plan 02 템플릿이 기대하는 변수 set:**

```java
private org.thymeleaf.context.Context buildContext(Document doc, User recipient, NotificationEventType type) {
    org.thymeleaf.context.Context ctx = new org.thymeleaf.context.Context();
    ctx.setVariable("docNumber", doc.getDocNumber() != null ? doc.getDocNumber() : "DRAFT");
    ctx.setVariable("docTitle", doc.getTitle());
    ctx.setVariable("drafterName", doc.getDrafter().getName());
    ctx.setVariable("drafterDepartment",
            doc.getDrafter().getDepartment() != null ? doc.getDrafter().getDepartment().getName() : "");
    ctx.setVariable("recipientName", recipient.getName());

    // actionLabel — 템플릿에서 사용 가능하도록 개별 변수로도 노출
    String actionLabel = switch (type) {
        case SUBMIT -> "결재 요청";
        case APPROVE -> "승인";
        case FINAL_APPROVE -> "최종 승인";
        case REJECT -> "반려";
        case WITHDRAW -> "회수";
    };
    ctx.setVariable("actionLabel", actionLabel);

    ctx.setVariable("eventTime", java.time.LocalDateTime.now());
    ctx.setVariable("approvalUrl", baseUrl + "/documents/" + doc.getId());

    // REJECT 전용 — approval_line 중 REJECTED 상태의 comment (D-C6, PATTERNS.md specifics L171)
    if (type == NotificationEventType.REJECT) {
        doc.getApprovalLines().stream()
                .filter(l -> l.getStatus() == com.micesign.domain.enums.ApprovalLineStatus.REJECTED)
                .findFirst()
                .ifPresent(l -> ctx.setVariable("rejectComment", l.getComment()));
    }
    return ctx;
}
```

**F. `toTemplateSlug(NotificationEventType type)`:**

```java
private String toTemplateSlug(NotificationEventType type) {
    return switch (type) {
        case SUBMIT -> "submit";
        case APPROVE -> "approve";
        case FINAL_APPROVE -> "final-approve";
        case REJECT -> "reject";
        case WITHDRAW -> "withdraw";
    };
}
```

**G. `truncate(String s)` — 이미 Plan 01에 존재. 그대로 유지.**

**추가 import 필요:**
- `jakarta.mail.internet.MimeMessage`
- `jakarta.mail.MessagingException`
- `org.springframework.mail.javamail.MimeMessageHelper`
- `org.thymeleaf.context.Context`
- `java.time.LocalDateTime`

**검증 포인트 (executor self-check before verify):**
1. `@Retryable` 애너테이션의 `backoff = @Backoff(delayExpression = "#{${app.mail.retry.delay-ms:300000}}")` — default 300000 ms = 5분. `application.yml` 의 `app.mail.retry.delay-ms` property 로 override 가능 (테스트에서 0 주입).
2. `MimeMessageHelper` 생성자는 반드시 3-arg `(message, true, "UTF-8")` (Pitfall 3)
3. `self.persistLog(...)` / `self.findOrCreatePendingLog(...)` 호출 — `this.` 로 직접 호출하지 말 것 (프록시 우회 방지)
4. `send()` 메서드 **전체에 @Transactional 을 붙이지 말 것** (D-B4 — send 실패 시 PENDING 까지 rollback 되면 안 됨)
5. `recover()` 의 첫 파라미터는 `MailException` (MailSendException의 슈퍼타입) — Spring Retry 시그니처 매칭 (Pitfall 2)
6. REJECT 본문의 rejectComment 는 `ApprovalLineStatus.REJECTED` 상태의 라인 `.getComment()` 추출
  </action>
  <verify>
    <automated>grep -q "mailSender.send(message)" backend/src/main/java/com/micesign/service/email/ApprovalEmailSender.java && grep -q 'MimeMessageHelper(message, true, "UTF-8")' backend/src/main/java/com/micesign/service/email/ApprovalEmailSender.java && grep -q "MiceSign <" backend/src/main/java/com/micesign/service/email/ApprovalEmailSender.java && grep -q '\[EMAIL STUB\]' backend/src/main/java/com/micesign/service/email/ApprovalEmailSender.java && grep -q "결재 요청" backend/src/main/java/com/micesign/service/email/ApprovalEmailSender.java && grep -q "최종 승인" backend/src/main/java/com/micesign/service/email/ApprovalEmailSender.java && grep -q "toTemplateSlug" backend/src/main/java/com/micesign/service/email/ApprovalEmailSender.java && grep -q "self.persistLog\|self\\.persistLog" backend/src/main/java/com/micesign/service/email/ApprovalEmailSender.java && grep -q "self.findOrCreatePendingLog\|self\\.findOrCreatePendingLog" backend/src/main/java/com/micesign/service/email/ApprovalEmailSender.java && grep -q "rejectComment" backend/src/main/java/com/micesign/service/email/ApprovalEmailSender.java && ! grep -q "UnsupportedOperationException" backend/src/main/java/com/micesign/service/email/ApprovalEmailSender.java && cd backend && ./gradlew compileJava --quiet</automated>
  </verify>
  <done>
    - `send()` 메서드에 stub mode + Thymeleaf render + MimeMessageHelper 3-arg UTF-8 + mailSender.send 호출 + 3가지 catch 분기
    - `findOrCreatePendingLog()` 에 repository 조회 + orElseGet(PENDING 생성) 로직
    - `buildSubject()` = `[MiceSign] {actionLabel}: {docNo} {title}` 포맷 + 5종 한글 매핑
    - `buildContext()` = 8개 변수 설정 (docNumber/docTitle/drafterName/drafterDepartment/recipientName/actionLabel/eventTime/approvalUrl) + REJECT 시 rejectComment
    - `toTemplateSlug()` 5종 slug 매핑
    - `UnsupportedOperationException` 제거 (스켈레톤 완료)
    - `./gradlew compileJava` 성공
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 수신자 User.email (user DB) → SMTP outbound | RETIRED/INACTIVE 필터로 비활성 사용자 메일 제외 |
| ApprovalLine.comment (REJECT) → 이메일 body | rejectComment HTML escape (Thymeleaf `th:text` 기본 escape) |
| baseUrl env → 이메일 CTA href | BaseUrlGuard(@Profile prod) + Plan 05 application-prod.yml 보강 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03-01 | Information Disclosure | Email 본문 PII 유출 (문서 본문) | mitigate | buildContext 에서 doc.content 를 setVariable 하지 않음 (D-C6). 본문은 메타데이터만 포함. acceptance: `! grep -q "ctx.setVariable.*content\|doc.getContent" ApprovalEmailSender.java` |
| T-29-03-02 | Tampering | JWT token 을 URL 에 포함 | mitigate | approvalUrl = `baseUrl + "/documents/" + doc.getId()` 만 조립. 토큰 주입 없음. acceptance: `! grep -q "token=\|jwt=" ApprovalEmailSender.java` |
| T-29-03-03 | Spoofing | From 주소 스푸핑 | accept | From = `MiceSign <${spring.mail.username}>` 고정. Reply-To 미사용 (D-C5). SMTP 서버가 DKIM/SPF 로 방어 (Phase 33 런북 범위) |
| T-29-03-04 | Denial of Service | @Retryable 무한 재시도 | mitigate | maxAttempts=3 고정 + noRetryFor 영구 예외 즉시 차단 + 5분 backoff — 상한 보장 |
| T-29-03-05 | Elevation of Privilege | 리스너에서 audit_log 추가 INSERT (NFR-03 위반) | mitigate | EmailService 에서 audit 관련 호출 완전 제거. acceptance: `! grep -q "auditLog" EmailService.java` |
| T-29-03-06 | Information Disclosure | errorMessage 에 password/secret 유출 | mitigate | `truncate()` 255자 제한 + MailAuthenticationException 메시지는 Jakarta Mail 이 credential 을 escape. 일반적으로 `"Authentication failed"` 수준의 표현만 포함. 로그 레벨 ERROR 만 기록 |
</threat_model>

<verification>
Plan 03 완료 판정 기준:

1. **컴파일 + 기본 테스트:**
   - `./gradlew compileJava` + `./gradlew compileTestJava` 성공
   - `./gradlew test --tests 'com.micesign.registration.RegistrationEmailServiceTest'` 통과 (회귀 없음)

2. **Contract 일치:**
   - `ApprovalEmailSender.send(Document, User, NotificationEventType)` 시그니처 (Plan 04 retry 테스트가 이 시그니처 사용)
   - `@Recover` 첫 파라미터 = `MailException` (Spring Retry 매칭 보장)

3. **Stub mode 검증 (수동 — MAIL_HOST 미설정 기동):**
   - `grep -q "\[EMAIL STUB\]" ApprovalEmailSender.java` — stub 로그 기록 경로 존재

4. **`@Lazy` self-injection 검증:**
   - `grep -c "self\\.persistLog\\|self\\.findOrCreatePendingLog" ApprovalEmailSender.java` ≥ 2 (send 내부 + recover 내부)
   - `! grep -q "this\\.persistLog\\|this\\.findOrCreatePendingLog"` — self-invocation 우회 방지

5. **회귀 방지:**
   - `RegistrationEmailService.java` 0 lines changed (별도 빈으로 완전 독립 동작)
   - `ApprovalService.java`, `DocumentService.java`, `ApprovalNotificationEvent.java` 0 lines changed (CONTEXT canonical_refs 수정 금지)
</verification>

<success_criteria>
- [ ] ApprovalEmailSender 실 로직 완성 — send/recover/findOrCreatePendingLog/buildSubject/buildContext/toTemplateSlug 모두 구현
- [ ] UnsupportedOperationException 제거
- [ ] MimeMessageHelper 3-arg UTF-8 + `MiceSign <...>` from 포맷
- [ ] PENDING-first 3단계 순서 (INSERT → send → SUCCESS/FAILED/RETRY UPDATE)
- [ ] D-A5 예외 분류 — MailSendException 만 retry, MailAuth/MailParse 즉시 FAILED, MessagingException FAILED
- [ ] `self.persistLog` + `self.findOrCreatePendingLog` — @Lazy proxy 우회
- [ ] EmailService 에 `approvalEmailSender.send` 호출, `determineRecipients` 에 ACTIVE 필터 + Collectors.toMap distinct + REFERENCE 필터
- [ ] EmailService 에서 sendToRecipient/sendEmail/getActionLabel/audit 관련 호출 제거
- [ ] 기존 RegistrationEmailServiceTest 회귀 없음
- [ ] ApprovalService/DocumentService/ApprovalNotificationEvent 미변경 (수정 금지 경계)
</success_criteria>

<output>
작업 완료 시 다음 경로에 요약 작성:
`.planning/phases/29-smtp-retrofit/29-03-SUMMARY.md`

요약에 포함할 사항:
- ApprovalEmailSender 최종 LOC + 주요 메서드 목록
- EmailService 제거된 메서드 목록 + LOC 감소량
- NotificationLog.eventType 저장 형식 (String `type.name()` vs enum — 최종 확정값)
- Plan 04 (integration tests) + Plan 05 (audit test) 가 참조할 contract (subject regex, Context 변수 set, retry 횟수)
</output>
