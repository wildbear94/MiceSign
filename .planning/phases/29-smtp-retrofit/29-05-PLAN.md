---
phase: 29
plan: 05
type: execute
wave: 3
depends_on: [29-01, 29-03]
files_modified:
  - backend/src/main/resources/application-prod.yml
  - backend/src/test/java/com/micesign/document/ApprovalServiceAuditTest.java
autonomous: true
requirements:
  - NOTIF-02
  - NFR-03
user_setup:
  - service: prod-deployment
    why: "APP_BASE_URL env var 운영 배포 전 주입 필수 — BaseUrlGuard 가 localhost 감지 시 startup 실패"
    env_vars:
      - name: APP_BASE_URL
        source: "배포 팀 확정 — 예: https://micesign.사내도메인"
    dashboard_config:
      - task: "환경변수 설정"
        location: "systemd service Environment= 또는 CI/CD secrets"
        runbook: "Phase 33 운영 전환 런북"

must_haves:
  truths:
    - "application-prod.yml 에 app.base-url 이 ${APP_BASE_URL:https://micesign.사내도메인} 으로 명시 주입된다"
    - "BaseUrlGuard 가 prod profile 에서 localhost/127.0.0.1/빈 값 감지 시 ApplicationReadyEvent listener 에서 IllegalStateException 발생"
    - "ApprovalServiceAuditTest 가 submit/approve/reject/withdraw 각 API 호출 후 audit_log COUNT=1 을 검증한다 (NFR-03)"
    - "기존 RegistrationEmailService + RegistrationEmailServiceTest 가 회귀 없이 통과한다"
  artifacts:
    - path: "backend/src/main/resources/application-prod.yml"
      provides: "prod profile app.base-url 주입"
      contains: "base-url: ${APP_BASE_URL"
    - path: "backend/src/test/java/com/micesign/document/ApprovalServiceAuditTest.java"
      provides: "audit_log COUNT=1 per action 검증 (NFR-03 CI 게이트)"
      contains: "isEqualTo(1)"
  key_links:
    - from: "application-prod.yml app.base-url"
      to: "BaseUrlGuard.verifyBaseUrl"
      via: "@Value 주입 + ApplicationReadyEvent"
      pattern: "base-url: \\$\\{APP_BASE_URL"
    - from: "ApprovalServiceAuditTest"
      to: "audit_log table"
      via: "JdbcTemplate COUNT query"
      pattern: "SELECT COUNT.*audit_log"
    - from: "ApprovalServiceAuditTest"
      to: "기존 AuditLogGapTest 패턴"
      via: "@SpringBootTest + MockMvc + TestTokenHelper 복제"
      pattern: "tokenHelper.superAdminToken|mockMvc.perform"
---

<objective>
Phase 29의 마지막 **운영 환경 준비 + NFR-03 CI 게이트 + 회귀 보증** 작업을 수행한다. BaseUrlGuard(Plan 01)가 prod에서 동작하도록 application-prod.yml 에 base-url 을 추가하고, "리스너에서 audit_log 중복 INSERT 안 함" 을 자동 검증하는 테스트를 추가한다.

목적:
- D-D2 Pitfall 5 완결 — prod 배포 시 base-url fail-fast 가 실제로 동작할 수 있도록 설정 주입
- NFR-03 — 기존 `ApprovalService`/`DocumentService` 의 audit 기록이 Phase 29 리스너 추가 후에도 여전히 `COUNT=1 per action` 임을 CI 에서 보장
- 회귀 보증 — 기존 `RegistrationEmailService` 경로에 영향 없음 최종 확인

산출물:
- `application-prod.yml` — `app.base-url` 블록 1개 추가 (4줄)
- `ApprovalServiceAuditTest` — 4개 이벤트 × audit COUNT=1 검증 (기존 `AuditLogGapTest` 패턴 복제)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-smtp-retrofit/29-CONTEXT.md
@.planning/phases/29-smtp-retrofit/29-RESEARCH.md
@.planning/phases/29-smtp-retrofit/29-PATTERNS.md
@.planning/phases/29-smtp-retrofit/29-VALIDATION.md
@.planning/phases/29-smtp-retrofit/29-03-SUMMARY.md
@backend/src/main/resources/application-prod.yml
@backend/src/main/resources/application.yml
@backend/src/test/java/com/micesign/admin/AuditLogGapTest.java
@backend/src/main/java/com/micesign/service/ApprovalService.java
@backend/src/main/java/com/micesign/service/DocumentService.java
@backend/src/main/java/com/micesign/service/AuditLogService.java
@CLAUDE.md

<interfaces>
**Audit action 이름 (planner 가 `backend/src/main/java/com/micesign/common/AuditAction.java` 확인 후 확정값 — W8):**
`AuditAction` 은 **enum 이 아니라 `public final class` 에 정의된 `public static final String` 상수 집합** (`backend/src/main/java/com/micesign/common/AuditAction.java` 확인 결과). 테스트에서 `AuditAction.DOC_SUBMIT` 처럼 **상수 참조** 하거나 raw 문자열 `"DOC_SUBMIT"` 사용 가능. 4개 실 상수값 확정:

| Event | Constant | Literal value |
|-------|----------|---------------|
| SUBMIT | `AuditAction.DOC_SUBMIT` | `"DOC_SUBMIT"` |
| APPROVE | `AuditAction.DOC_APPROVE` | `"DOC_APPROVE"` |
| REJECT | `AuditAction.DOC_REJECT` | `"DOC_REJECT"` |
| WITHDRAW | `AuditAction.DOC_WITHDRAW` | `"DOC_WITHDRAW"` |

**AuditLogGapTest 선례 (L30-57):**
- `@SpringBootTest @AutoConfigureMockMvc @ActiveProfiles("test")`
- `@Autowired MockMvc mockMvc; ObjectMapper objectMapper; JdbcTemplate jdbcTemplate; TestTokenHelper tokenHelper;`
- `@BeforeEach` + `@AfterEach`: `jdbcTemplate.update("DELETE FROM audit_log")`
- `tokenHelper.superAdminToken()` / `userToken()` 로 JWT 획득

**실 API 엔드포인트 (planner 가 `@PostMapping` grep 후 확정 — W7):**

| Event | Method | Path | Controller | Line | 상태 |
|-------|--------|------|------------|------|-----|
| SUBMIT | POST | `/api/v1/documents/{id}/submit` | `DocumentController` | L92 | ✅ 존재 |
| WITHDRAW | POST | `/api/v1/documents/{id}/withdraw` | `DocumentController` | L100 | ✅ 존재 |
| APPROVE | (서비스 메서드) | `ApprovalService.approve(Long approvalLineId, ApprovalActionRequest request, Long userId)` | `ApprovalService.java` | L73 | ⚠ **controller endpoint 미구현** — `ApprovalController.java` L17-18 주석 `"approve/reject actions will be fully implemented in Phase 7"`. Phase 29 는 Phase 7 미완료 상태이므로 **테스트가 controller mockMvc 대신 `@Autowired ApprovalService` 직접 호출**로 검증해야 함. |
| REJECT | (서비스 메서드) | `ApprovalService.reject(Long approvalLineId, ApprovalActionRequest request, Long userId)` | `ApprovalService.java` | L134 | ⚠ 동일 — service 직접 호출 |

**이 영향 (Plan 05 Task 2 테스트 스타일 분기):**
- submit / withdraw: `mockMvc.perform(post(...))` + 인증 토큰 헤더
- approve / reject: `@Autowired ApprovalService approvalService; approvalService.approve(lineId, request, userId);` — mockMvc 경유 불가, service 단위 호출

**APP_BASE_URL 운영 default (D-D2):**
- `application-prod.yml` : `base-url: ${APP_BASE_URL:https://micesign.사내도메인}`
- 플레이스홀더 `사내도메인` 은 의도적 — 배포 팀이 실 도메인 주입해야 함
- BaseUrlGuard 가 localhost 감지 시 `IllegalStateException` 발생
</interfaces>
</context>

<tasks>

<task id="T1" type="auto" tdd="false">
  <name>Task 1: application-prod.yml 에 app.base-url 주입 (D-D2)</name>
  <files>
    - backend/src/main/resources/application-prod.yml
  </files>
  <read_first>
    - backend/src/main/resources/application-prod.yml (전체 — 현재 17줄 구조)
    - backend/src/main/resources/application.yml (L43-44 — app.base-url 기본값)
    - .planning/phases/29-smtp-retrofit/29-CONTEXT.md §D-D2
    - .planning/phases/29-smtp-retrofit/29-PATTERNS.md §8 (L355-371)
  </read_first>
  <action>
현재 `application-prod.yml` 의 `app:` 블록에 `base-url` 1줄 추가. 기존 `cookie:` 는 유지.

수정 후 전체 구조:

```yaml
spring:
  datasource:
    url: jdbc:mariadb://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:micesign}
    username: ${DB_USER:miceleech}
    password: ${DB_PASS:leech0511!}
    driver-class-name: org.mariadb.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5

app:
  # Phase 29 D-D2 — BaseUrlGuard(@Profile("prod")) 가 localhost 감지 시 startup 실패시킴 (Pitfall 5)
  # 배포 시 APP_BASE_URL env var 를 실 사내 도메인으로 주입 필수
  base-url: ${APP_BASE_URL:https://micesign.사내도메인}
  cookie:
    secure: true

logging:
  level:
    com.micesign: INFO
```

**중요:**
- Default 값 `https://micesign.사내도메인` 은 의도적 placeholder — BaseUrlGuard 가 localhost/127.0.0.1/빈 값만 체크하므로 이 placeholder 는 통과하지만, 실제 이메일 링크가 깨지므로 배포 팀이 곧바로 감지. 실 배포 시 `APP_BASE_URL=https://micesign.company.com` 등 실 도메인 주입.
- 주석 2줄로 D-D2 + Pitfall 5 배경 명시 — 배포 팀 교육 목적
- `application.yml` L43-44 의 `base-url: ${APP_BASE_URL:http://localhost:5173}` 는 변경 금지 (개발/테스트 기본값)
- prod profile 이 application.yml 의 `app.base-url` 을 override 하므로 실 운영 배포에서만 위 prod 값이 적용됨
  </action>
  <verify>
    <automated>grep -q 'base-url: ${APP_BASE_URL:https://micesign' backend/src/main/resources/application-prod.yml && grep -q 'BaseUrlGuard' backend/src/main/resources/application-prod.yml && ! grep -q 'base-url.*localhost' backend/src/main/resources/application-prod.yml && grep -q 'base-url: ${APP_BASE_URL:http://localhost:5173}' backend/src/main/resources/application.yml</automated>
  </verify>
  <done>
    - application-prod.yml app 블록에 base-url 라인 + 주석 2줄 추가
    - default 값 = `https://micesign.사내도메인` (placeholder)
    - application.yml 원본 미변경 (기존 localhost 기본값 유지)
    - `./gradlew compileJava` 영향 없음 (YAML 변경은 컴파일 무관)
  </done>
</task>

<task id="T2" type="auto" tdd="false">
  <name>Task 2: ApprovalServiceAuditTest — 4이벤트 × audit_log COUNT=1 per action (NFR-03)</name>
  <files>
    - backend/src/test/java/com/micesign/document/ApprovalServiceAuditTest.java
  </files>
  <read_first>
    - backend/src/test/java/com/micesign/admin/AuditLogGapTest.java (전체 — fixture/setUp/tearDown 복제 대상)
    - backend/src/main/java/com/micesign/service/ApprovalService.java (approve/reject 메서드 + auditLogService 호출 지점 확인)
    - backend/src/main/java/com/micesign/service/DocumentService.java (submit/withdraw 메서드 + auditLogService 호출 지점)
    - backend/src/main/java/com/micesign/service/AuditLogService.java (log 메서드 시그니처)
    - backend/src/main/java/com/micesign/domain/enums/AuditAction.java (또는 AuditAction enum — SUBMIT/APPROVE/REJECT/WITHDRAW 해당 값 확인)
    - backend/src/main/java/com/micesign/service/EmailService.java (Plan 03 완성본 — audit 호출 없음 확인)
    - .planning/phases/29-smtp-retrofit/29-PATTERNS.md §13 (L550-582)
    - .planning/phases/29-smtp-retrofit/29-VALIDATION.md (L37-38)
  </read_first>
  <action>
위치: `backend/src/test/java/com/micesign/document/ApprovalServiceAuditTest.java` (Claude's Discretion: `com.micesign.document` 패키지 — AuditLogGapTest 의 admin 과 분리, 결재 도메인 한정)

**Scope:** 4개 이벤트(SUBMIT/APPROVE/REJECT/WITHDRAW) 각각에 대해 API 호출 → `SELECT COUNT(*) FROM audit_log WHERE action=? AND target_id=?` = **정확히 1** 검증. FINAL_APPROVE 는 APPROVE 의 연속이므로 최종 승인 테스트에서도 검증.

**파일 구조:**

```java
package com.micesign.document;

import com.fasterxml.jackson.databind.ObjectMapper;
import com.micesign.common.AuditAction;
import com.micesign.domain.*;
import com.micesign.domain.enums.*;
import com.micesign.repository.*;
import com.micesign.service.ApprovalService;
import com.micesign.service.dto.ApprovalActionRequest;  // 실제 위치는 Read 후 확정
// TestTokenHelper import — AuditLogGapTest 와 동일 패키지 경로
import com.micesign.test.TestTokenHelper;  // 실제 위치는 Read 후 확정
import org.junit.jupiter.api.*;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.http.MediaType;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.test.context.ActiveProfiles;
import org.springframework.test.web.servlet.MockMvc;

import static org.assertj.core.api.Assertions.assertThat;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.*;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

/**
 * NFR-03 검증 — 결재 action 당 audit_log COUNT=1 (리스너에서 중복 INSERT 금지, Pitfall 8).
 * Phase 29 리스너 추가 이후에도 ApprovalService/DocumentService 의 동기 audit 기록이 유일한 경로임을 보장.
 * AuditLogGapTest.java 패턴 복제 + isGreaterThanOrEqualTo(1) → isEqualTo(1) 강화.
 */
@SpringBootTest
@AutoConfigureMockMvc
@ActiveProfiles("test")
class ApprovalServiceAuditTest {

    @Autowired MockMvc mockMvc;
    @Autowired ObjectMapper objectMapper;
    @Autowired JdbcTemplate jdbcTemplate;
    @Autowired TestTokenHelper tokenHelper;
    @Autowired ApprovalService approvalService;   // approve/reject — controller endpoint Phase 7 대기로 service 직접 호출

    @MockBean(name = "budgetApiClient") com.micesign.budget.BudgetApiClient budgetApiClient;

    private String drafterToken;
    private String approverToken;
    private Long docId;
    private Long approvalLineId;
    private Long drafterUserId;
    private Long approverUserId;

    @BeforeEach
    void setUp() throws Exception {
        // 1. audit_log / notification_log 전체 초기화 — COUNT 검증의 baseline 보장
        jdbcTemplate.update("DELETE FROM audit_log");
        jdbcTemplate.update("DELETE FROM notification_log WHERE document_id IS NOT NULL");

        // 2. fixture 조립: drafter + approver + DRAFT 상태 document + approval_line 1개
        // (executor 가 entity 빌더 또는 기존 fixture factory 사용)

        drafterToken = tokenHelper.userToken();      // AuditLogGapTest 에 있으면 재사용
        approverToken = tokenHelper.userToken();     // 또는 별도 approver 계정
    }

    @AfterEach
    void tearDown() {
        jdbcTemplate.update("DELETE FROM audit_log");
        jdbcTemplate.update("DELETE FROM notification_log WHERE document_id IS NOT NULL");
        // fixture cleanup
    }

    @Test
    @DisplayName("SUBMIT: 상신 1회 = audit_log row 정확히 1건 (리스너 중복 INSERT 없음)")
    void submit_createsExactlyOneAuditRow() throws Exception {
        // when — /api/v1/documents/{id}/submit (실 URL 은 controller read 후 확정)
        mockMvc.perform(post("/api/v1/documents/{id}/submit", docId)
                        .header("Authorization", "Bearer " + drafterToken)
                        .contentType(MediaType.APPLICATION_JSON))
                .andExpect(status().isOk());

        // then — audit_log 에 DOC_SUBMIT action 이 정확히 1건 (NFR-03)
        Integer count = jdbcTemplate.queryForObject(
                "SELECT COUNT(*) FROM audit_log WHERE action = ? AND target_id = ?",
                Integer.class, AuditAction.DOC_SUBMIT, docId);  // String constant = "DOC_SUBMIT" (common/AuditAction.java)
        assertThat(count)
                .as("NFR-03: 리스너 추가 후에도 SUBMIT audit 는 정확히 1건")
                .isEqualTo(1);
    }

    @Test
    @DisplayName("APPROVE: 중간 승인 1회 = audit_log row 정확히 1건 (ApprovalService 직접 호출 — controller endpoint Phase 7 대기)")
    void approve_createsExactlyOneAuditRow() throws Exception {
        // given — document 가 SUBMITTED 상태 (fixture 또는 setUp 에서 submit 호출)
        // when — ApprovalService.approve 직접 호출 (ApprovalController L17-18 주석: approve endpoint Phase 7 에서 구현 예정)
        ApprovalActionRequest request = new ApprovalActionRequest("승인합니다");
        approvalService.approve(approvalLineId, request, approverUserId);

        Integer count = jdbcTemplate.queryForObject(
                "SELECT COUNT(*) FROM audit_log WHERE action = ? AND target_id = ?",
                Integer.class, AuditAction.DOC_APPROVE, docId);  // "DOC_APPROVE"
        assertThat(count)
                .as("NFR-03: 리스너 추가 후에도 APPROVE audit 는 정확히 1건")
                .isEqualTo(1);
    }

    @Test
    @DisplayName("REJECT: 반려 1회 = audit_log row 정확히 1건 (ApprovalService 직접 호출)")
    void reject_createsExactlyOneAuditRow() throws Exception {
        ApprovalActionRequest request = new ApprovalActionRequest("예산 초과");
        approvalService.reject(approvalLineId, request, approverUserId);

        Integer count = jdbcTemplate.queryForObject(
                "SELECT COUNT(*) FROM audit_log WHERE action = ? AND target_id = ?",
                Integer.class, AuditAction.DOC_REJECT, docId);  // "DOC_REJECT"
        assertThat(count)
                .as("NFR-03: 리스너 추가 후에도 REJECT audit 는 정확히 1건")
                .isEqualTo(1);
    }

    @Test
    @DisplayName("WITHDRAW: 회수 1회 = audit_log row 정확히 1건")
    void withdraw_createsExactlyOneAuditRow() throws Exception {
        mockMvc.perform(post("/api/v1/documents/{id}/withdraw", docId)
                        .header("Authorization", "Bearer " + drafterToken)
                        .contentType(MediaType.APPLICATION_JSON))
                .andExpect(status().isOk());

        Integer count = jdbcTemplate.queryForObject(
                "SELECT COUNT(*) FROM audit_log WHERE action = ? AND target_id = ?",
                Integer.class, AuditAction.DOC_WITHDRAW, docId);  // "DOC_WITHDRAW"
        assertThat(count)
                .as("NFR-03: 리스너 추가 후에도 WITHDRAW audit 는 정확히 1건")
                .isEqualTo(1);
    }

    @Test
    @DisplayName("Cross-action: 전체 라이프사이클 (submit→approve) 후 action 별 COUNT=1")
    void fullLifecycle_eachActionCountOne() throws Exception {
        // submit 1회 — mockMvc
        mockMvc.perform(post("/api/v1/documents/{id}/submit", docId)
                .header("Authorization", "Bearer " + drafterToken))
                .andExpect(status().isOk());
        // approve 1회 — service 직접 호출 (controller endpoint Phase 7 대기)
        approvalService.approve(approvalLineId, new ApprovalActionRequest("승인합니다"), approverUserId);

        Integer submitCount = jdbcTemplate.queryForObject(
                "SELECT COUNT(*) FROM audit_log WHERE action = ? AND target_id = ?",
                Integer.class, AuditAction.DOC_SUBMIT, docId);
        Integer approveCount = jdbcTemplate.queryForObject(
                "SELECT COUNT(*) FROM audit_log WHERE action = ? AND target_id = ?",
                Integer.class, AuditAction.DOC_APPROVE, docId);
        assertThat(submitCount).isEqualTo(1);
        assertThat(approveCount).isEqualTo(1);
    }
}
```

**Executor가 완성해야 할 것 (planner 가 이미 확정한 사항은 **FIXED** 표시):**
1. **AuditAction 상수값 — FIXED** — `com.micesign.common.AuditAction` 의 `public static final String` 상수: `DOC_SUBMIT` / `DOC_APPROVE` / `DOC_REJECT` / `DOC_WITHDRAW`. 테스트에서 `AuditAction.DOC_SUBMIT` 형태로 **그대로** 참조 (해당 값 = 문자열 `"DOC_SUBMIT"`). `.name()` 호출하지 말 것 (String 상수이므로 method 없음).
2. **API endpoint — FIXED**:
   - SUBMIT → `POST /api/v1/documents/{id}/submit` (DocumentController L92) — mockMvc 경유
   - WITHDRAW → `POST /api/v1/documents/{id}/withdraw` (DocumentController L100) — mockMvc 경유
   - APPROVE / REJECT → **controller endpoint 미구현 (Phase 7 대기)** → `ApprovalService.approve(lineId, request, userId)` / `.reject(lineId, request, userId)` 로 **service 직접 호출**
3. **TestTokenHelper 위치 확인** — AuditLogGapTest 의 import 구문 그대로 사용. `com.micesign.test.TestTokenHelper` 또는 유사.
4. **ApprovalActionRequest DTO 위치 확인** — `grep -rn "class ApprovalActionRequest\|record ApprovalActionRequest" backend/src/main/java/com/micesign/` 로 실 패키지 확인 (생성자 시그니처 `(String comment)` 가정 — record 일 수 있음).
5. **fixture 세부 구성** — Document(DRAFT 상태), User(drafter+approver), ApprovalLine(approver, stepOrder=1) 객체 조립. 기존 test helper 가 있으면 재사용. approverUserId 는 approver User entity 의 id getter.
6. **BudgetApiClient 이름** — application 컨텍스트에 빈 이름이 `budgetApiClient` 가 아닐 수 있음. `@MockBean` 에 `name=` 생략하고 타입 매칭만 해도 OK.

**자격(key) 확장 (선택):**
- `target_type = 'DOCUMENT'` 조건도 WHERE 절에 추가하면 더 엄격 (다른 target 과 구분). AuditLogGapTest L69-72 참고.

**왜 isEqualTo(1) 인가 (AuditLogGapTest 의 isGreaterThanOrEqualTo(1) 과 차이):**
- NFR-03 의 스펙은 "COUNT=1 per action" — 엄격한 equality
- 리스너에서 audit_log INSERT 를 추가하면 COUNT=2 가 되어 이 테스트가 실패 → CI 게이트로 기능
- AuditLogGapTest 는 "최소 1건 있음" 의 gap-fill 목적이라 완화된 assertion 사용
  </action>
  <verify>
    <automated>test -f backend/src/test/java/com/micesign/document/ApprovalServiceAuditTest.java && grep -q "@SpringBootTest" backend/src/test/java/com/micesign/document/ApprovalServiceAuditTest.java && grep -q "MockMvc" backend/src/test/java/com/micesign/document/ApprovalServiceAuditTest.java && grep -q "isEqualTo(1)" backend/src/test/java/com/micesign/document/ApprovalServiceAuditTest.java && grep -q "submit_createsExactlyOneAuditRow\|approve_createsExactlyOneAuditRow\|reject_createsExactlyOneAuditRow\|withdraw_createsExactlyOneAuditRow" backend/src/test/java/com/micesign/document/ApprovalServiceAuditTest.java && grep -q "audit_log" backend/src/test/java/com/micesign/document/ApprovalServiceAuditTest.java && cd backend && ./gradlew compileTestJava --quiet && ./gradlew test --tests 'com.micesign.document.ApprovalServiceAuditTest'</automated>
  </verify>
  <done>
    - 파일 존재 + 최소 4개 `@Test` (submit/approve/reject/withdraw) + fullLifecycle 선택적 추가
    - `@SpringBootTest + @AutoConfigureMockMvc + @ActiveProfiles("test")`
    - `@MockBean` 으로 BudgetApiClient 차단
    - `isEqualTo(1)` — 엄격한 COUNT 검증
    - 기존 `AuditLogGapTest` 패턴 복제 (fixture, token, cleanup)
    - `./gradlew test --tests 'com.micesign.document.ApprovalServiceAuditTest'` 전 통과
  </done>
</task>

<task id="T3" type="auto" tdd="false">
  <name>Task 3: 전체 회귀 검증 — RegistrationEmailService + 전체 테스트 스위트</name>
  <files>[] (기존 파일 미수정 — 테스트 실행만)</files>
  <read_first>
    - backend/src/test/java/com/micesign/registration/RegistrationEmailServiceTest.java (회귀 baseline 확인)
    - .planning/phases/29-smtp-retrofit/29-CONTEXT.md §code_context "RegistrationEmailService는 완전 독립 동작 — 작업 중 regression 발생 시 즉시 회귀로 간주"
  </read_first>
  <action>
Phase 29 의 모든 변경이 기존 기능에 영향을 주지 않았는지 최종 게이트.

**Step A — RegistrationEmailServiceTest 단독 회귀:**
```bash
cd backend && ./gradlew test --tests 'com.micesign.registration.RegistrationEmailServiceTest'
```
기대: PASSED. 실패 시 Phase 29 작업 중 의도치 않은 regression — 즉시 원인 파악 필요.

**가능한 회귀 포인트 (확인만, 수정 금지):**
1. `NotificationLog` 엔티티에 `@UniqueConstraint` 추가 — registration 행은 `document_id IS NULL` 이므로 제약 영향 없음 (MariaDB NULL 의미론 — D-A2/A5)
2. `NotificationLogRepository` 에 `findByDocumentIdAndEventTypeAndRecipientId` 추가 — 기존 `findByStatusAndRetryCountLessThan` 은 그대로
3. `application-test.yml` 에 `spring.mail.*` 블록 추가 — Plan 04에서 SMTP host 를 `127.0.0.1:3025` 로 설정. RegistrationEmailServiceTest 가 `MockitoExtension` 기반 unit test 이면 영향 없음. 만약 `@SpringBootTest` 라서 `JavaMailSender` 빈이 실제로 생성되는 경로라면 테스트 대상 확인 필요.

**Step B — 전체 테스트 스위트:**
```bash
cd backend && ./gradlew test
```
기대: 전 테스트 green. 특히 Phase 29 신규 파일 3개 (ApprovalNotificationIntegrationTest, ApprovalEmailSenderRetryTest, ApprovalServiceAuditTest) 모두 통과. 기존 인증/문서/결재/예산 테스트 회귀 없음.

**Step C — Spring context 로드 + ddl-auto validate:**
```bash
cd backend && ./gradlew bootRun --args='--spring.profiles.active=dev' &
sleep 20 && kill $!
```
(대체) 통합 테스트 중 하나라도 Spring context 를 완전 로드하면 Hibernate validate 가 `@UniqueConstraint` vs V19 DDL 이름 일치를 startup 시 검증. `ApprovalNotificationIntegrationTest.@SpringBootTest` 가 이 역할 수행 — Step B 통과면 validate 통과 확인됨.

**Step D — Linting / 타입 확인:**
- Java 컴파일 경고 확인: `./gradlew compileJava compileTestJava --warning-mode all`
- import 정리 (IDE 자동) — 비사용 import 제거

**이 Task 에서 절대 하지 말 것:**
- 기존 테스트 수정 (회귀가 발생해도 원인을 찾고 Phase 29 신규 파일을 수정하는 것이 원칙)
- ApprovalService/DocumentService/ApprovalNotificationEvent 수정 (CONTEXT canonical_refs — 변경 금지 경계)
- RegistrationEmailService.java 수정 (별도 빈, 완전 독립)

**실패 대응:**
- RegistrationEmailServiceTest 실패 → Plan 03의 EmailService 리팩터 또는 NotificationLog 스키마 변경이 registration 흐름을 침범. Plan 03 edit 또는 테스트 fixture 검토.
- ApprovalNotificationIntegrationTest 실패 → Plan 04 의 fixture 문제일 가능성 높음. test 클래스 수정.
- ApprovalServiceAuditTest 실패 → AuditAction enum 이름 또는 API endpoint URL 이 가정과 다름. grep 재확인 후 test 수정.
- 빌드 전체 회귀 → git log 로 Phase 29 변경 파일 범위 확인 후 각 plan 의 변경사항을 다시 audit.
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests 'com.micesign.registration.RegistrationEmailServiceTest' && ./gradlew test</automated>
  </verify>
  <done>
    - RegistrationEmailServiceTest 단독 통과
    - `./gradlew test` 전체 스위트 green (기존 + Phase 29 신규 모두)
    - `./gradlew compileJava compileTestJava` 경고 없이 성공
    - ddl-auto validate 가 startup 시 @UniqueConstraint ↔ V19 DDL 이름 일치 확인 (Spring 통합 테스트 통과로 간접 확인)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 배포 config → prod app | APP_BASE_URL env 누락 시 BaseUrlGuard 가 startup 실패 |
| 리스너 레이어 → audit_log table | NFR-03 — 리스너는 audit 를 write 하지 않음, 테스트로 자동 보장 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05-01 | Information Disclosure | prod 배포 시 `https://micesign.사내도메인` placeholder 그대로 이메일 링크 전송 | mitigate | BaseUrlGuard 는 localhost 만 체크하므로 placeholder 는 통과. 그러나 수신자가 링크 클릭 시 DNS 해석 실패 → 즉시 배포 팀에게 이슈 피드백. Phase 33 런북에 "실 도메인 주입 확인" 항목 포함 (user_setup) |
| T-29-05-02 | Repudiation | 리스너에서 audit_log 중복 INSERT 허용 → 감사 증거 왜곡 | mitigate | ApprovalServiceAuditTest `isEqualTo(1)` 으로 CI 게이트. 추가 INSERT 삽입 시 즉시 실패 |
| T-29-05-03 | Tampering | 기존 RegistrationEmailService 회귀 | mitigate | RegistrationEmailServiceTest + 전체 스위트 green 게이트 |
</threat_model>

<verification>
Plan 05 + Phase 29 전체 완료 판정 기준:

1. **Prod config:**
   - `grep -q 'base-url: \\${APP_BASE_URL:https://micesign' backend/src/main/resources/application-prod.yml`

2. **NFR-03 CI 게이트:**
   - `./gradlew test --tests 'com.micesign.document.ApprovalServiceAuditTest'` 통과
   - 각 action 당 audit_log COUNT = 1 (리스너 중복 INSERT 없음)

3. **회귀 없음:**
   - `./gradlew test --tests 'com.micesign.registration.RegistrationEmailServiceTest'` 통과
   - `./gradlew test` 전체 green

4. **Phase 29 전체 Success Criteria (ROADMAP) 검증 준비:**
   - SC#1 — ApprovalNotificationIntegrationTest.submit_deliversToFirstNonReferenceApprover_koreanSubject (Plan 04 Task 2)
   - SC#2 — 같은 테스트의 approvalUrl + UTF-8 subject 검증
   - SC#3 — ApprovalEmailSenderRetryTest (Plan 04 Task 3)
   - SC#4 — ApprovalNotificationIntegrationTest.skipInactiveUsers + duplicateInsert_throwsDataIntegrityViolation (Plan 04 Task 2)
   - SC#5 — ApprovalServiceAuditTest.*_createsExactlyOneAuditRow (Plan 05 Task 2)

5. **배포 시 확인 (user_setup):**
   - `APP_BASE_URL` env var 주입 — Phase 33 운영 전환 런북에 체크리스트 포함
</verification>

<success_criteria>
- [ ] application-prod.yml app.base-url 추가 (주석 2줄 + default placeholder)
- [ ] ApprovalServiceAuditTest 4+ 테스트 (submit/approve/reject/withdraw × COUNT=1)
- [ ] RegistrationEmailServiceTest 회귀 없음
- [ ] `./gradlew test` 전체 스위트 green
- [ ] Phase 29 ROADMAP Success Criteria 5항 모두 자동 테스트로 커버
- [ ] user_setup 에 APP_BASE_URL env var 주입 요구사항 명시 (Phase 33 런북 연계)
</success_criteria>

<output>
작업 완료 시 다음 경로에 요약 작성:
`.planning/phases/29-smtp-retrofit/29-05-SUMMARY.md`

요약에 포함할 사항:
- application-prod.yml 최종 app 블록 내용
- ApprovalServiceAuditTest 테스트 메서드 목록 + 각 검증 action
- 전체 `./gradlew test` 통과 시간 + 회귀 0 확인
- Phase 29 전체 완료 상태:
  - NOTIF-01~05, NFR-02, NFR-03 각 REQ-ID 어떤 테스트에서 커버되는지 매핑
  - 배포 전 체크리스트 (APP_BASE_URL env var 필요)
- Phase 33 런북으로 이관된 항목 (운영 SMTP 공급자 결정, MailHog/Mailpit 선택, stale PENDING cleanup 스크립트)
</output>
