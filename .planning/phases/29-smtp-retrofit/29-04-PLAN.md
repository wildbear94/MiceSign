---
phase: 29
plan: 04
type: execute
wave: 3
depends_on: [29-01, 29-02, 29-03]
files_modified:
  - backend/build.gradle.kts
  - backend/src/test/resources/application-test.yml
  - backend/src/test/java/com/micesign/notification/ApprovalNotificationIntegrationTest.java
  - backend/src/test/java/com/micesign/notification/ApprovalEmailSenderRetryTest.java
autonomous: true
requirements:
  - NOTIF-01
  - NOTIF-02
  - NOTIF-03
  - NOTIF-04
  - NOTIF-05
  - NFR-02
user_setup: []

must_haves:
  truths:
    - "GreenMail 2.1.3 이 testImplementation 의존성으로 build.gradle.kts 에 추가된다"
    - "application-test.yml 에 spring.mail.host=127.0.0.1 + port=3025 + app.base-url 이 추가되어 GreenMail 매칭이 가능하다"
    - "ApprovalNotificationIntegrationTest 가 5종 이벤트 × [기본 발송 + 한글 subject 디코딩 + 수신자 규칙 + RETIRED/INACTIVE skip + UNIQUE 제약] 을 검증한다"
    - "ApprovalEmailSenderRetryTest 가 @Retryable 경로(MailSendException 3회 → @Recover → FAILED)와 noRetryFor 경로(MailAuthenticationException 즉시 FAILED)를 검증한다"
    - "테스트 실행 시 실제 5분 backoff 대기 없이 동작한다 (@TestConfiguration RetryTemplate delay=0 override)"
    - "GreenMail 통합 테스트가 MimeMessage.getSubject() 로 UTF-8 한글 subject 디코딩을 검증한다"
  artifacts:
    - path: "backend/build.gradle.kts"
      provides: "GreenMail 2.1.3 testImplementation"
      contains: "greenmail-junit5:2.1.3"
    - path: "backend/src/test/resources/application-test.yml"
      provides: "GreenMail SMTP port + app.base-url 테스트 설정"
      contains: "127.0.0.1"
    - path: "backend/src/test/java/com/micesign/notification/ApprovalNotificationIntegrationTest.java"
      provides: "GreenMail + @SpringBootTest 5이벤트 E2E 검증"
      contains: "GreenMailExtension"
    - path: "backend/src/test/java/com/micesign/notification/ApprovalEmailSenderRetryTest.java"
      provides: "@Retryable 3회 + @Recover + FAILED 검증"
      contains: "MailSendException"
  key_links:
    - from: "ApprovalNotificationIntegrationTest"
      to: "GreenMail SMTP port 3025"
      via: "@RegisterExtension static GreenMailExtension + application-test.yml spring.mail.port"
      pattern: "port: 3025"
    - from: "ApprovalEmailSenderRetryTest"
      to: "@Retryable delay=0 override"
      via: "@TestConfiguration RetryTemplate bean + SimpleRetryPolicy"
      pattern: "backoffPeriod|NoBackOffPolicy"
    - from: "Integration test"
      to: "NotificationLog.status='SUCCESS' row"
      via: "JdbcTemplate queryForObject COUNT"
      pattern: "SELECT COUNT.*notification_log"
---

<objective>
Plan 01/02/03의 산출물을 **실제 SMTP 서버(GreenMail)로 검증**하는 통합 테스트 2개와 의존성/설정을 추가한다. NOTIF-01..05 + NFR-02 의 Wave 0 testing gap 을 채우고, D-D5/D-D6 CI 게이트의 핵심 셋트를 구축한다.

목적: CONTEXT D-D3/D-D5/D-D6 의 "GreenMail(JUnit5) + @Retryable 경로 별도 테스트" 검증 요구를 물리화. RESEARCH §Validation Architecture L1039-1083 의 skeleton 을 실 테스트 클래스로 확정.

산출물:
- `build.gradle.kts` — GreenMail 2.1.3 testImplementation
- `application-test.yml` — spring.mail.* + app.base-url 블록 추가
- `ApprovalNotificationIntegrationTest` — 5이벤트 × 검증 축
- `ApprovalEmailSenderRetryTest` — @Retryable/@Recover 경로
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-smtp-retrofit/29-CONTEXT.md
@.planning/phases/29-smtp-retrofit/29-RESEARCH.md
@.planning/phases/29-smtp-retrofit/29-PATTERNS.md
@.planning/phases/29-smtp-retrofit/29-VALIDATION.md
@.planning/phases/29-smtp-retrofit/29-01-SUMMARY.md
@.planning/phases/29-smtp-retrofit/29-03-SUMMARY.md
@backend/build.gradle.kts
@backend/src/test/resources/application-test.yml
@backend/src/test/java/com/micesign/admin/AuditLogGapTest.java
@backend/src/test/java/com/micesign/registration/RegistrationEmailServiceTest.java
@CLAUDE.md

<interfaces>
**GreenMail 2.1.3 API (Maven Central 최신 — 2026-04-23 확인):**
- Coordinate: `com.icegreen:greenmail-junit5:2.1.3`
- Extension: `com.icegreen.greenmail.junit5.GreenMailExtension`
- ServerSetup: `com.icegreen.greenmail.util.ServerSetupTest.SMTP` (port 3025)
- API: `greenMail.getReceivedMessages()` → `jakarta.mail.internet.MimeMessage[]`
- Util: `com.icegreen.greenmail.util.GreenMailUtil.getBody(MimeMessage)` → String

**@Retryable 테스트 패턴:**
- `@SpringBootTest` 필수 (AOP proxy 활성화) — `MockitoExtension` 만으로는 불가능
- `@MockBean JavaMailSender` + `doThrow(new MailSendException("simulated")).when(mailSender).send(any(MimeMessage.class))`
- `@TestConfiguration` 로 `RetryTemplate` 빈 override — `SimpleRetryPolicy(3)` + `NoBackOffPolicy` 로 delay 0
- 또는 `ApprovalEmailSender` 에 별도 `RetryTemplate` 주입이 없으므로, `@Backoff(delay=300_000L)` 을 바로 우회하려면 test property 로 backoff override 또는 `@EnableRetry` 의 `proxyTargetClass` 조작 필요

**ApprovalEmailSender contract (Plan 03 완성본):**
- `void send(Document doc, User recipient, NotificationEventType eventType)`
- `void recover(MailException e, Document doc, User recipient, NotificationEventType eventType)`
- Subject regex: `^\[MiceSign\] (결재 요청|승인|최종 승인|반려|회수): \S+ .+$`

**Fixture 전략:**
- 기존 AuditLogGapTest 가 `@Autowired TestTokenHelper tokenHelper` + `jdbcTemplate.update("DELETE FROM ...")` 패턴 사용
- Plan 04도 동일 패턴 복제 + GreenMail extension 추가
</interfaces>
</context>

<tasks>

<task id="T1" type="auto" tdd="false">
  <name>Task 1: GreenMail 의존성 + application-test.yml SMTP 블록 추가</name>
  <files>
    - backend/build.gradle.kts
    - backend/src/test/resources/application-test.yml
  </files>
  <read_first>
    - backend/build.gradle.kts (전체 — 기존 testImplementation 블록 위치 확인 line 64-70)
    - backend/src/test/resources/application-test.yml (전체 — 기존 spring/management 블록 구조)
    - backend/src/main/resources/application.yml (L25-36 spring.mail.* 구조)
    - .planning/phases/29-smtp-retrofit/29-RESEARCH.md §Standard Stack (L127-153) + §Validation Architecture Config (L1086-1108)
    - .planning/phases/29-smtp-retrofit/29-PATTERNS.md §9/§10 (L375-434)
  </read_first>
  <action>
**Step A — `backend/build.gradle.kts` 수정:**

기존 testImplementation 블록 (대략 L64-70):
```kotlin
    // Test
    testImplementation("org.springframework.boot:spring-boot-starter-test")
    testImplementation("org.springframework.security:spring-security-test")

    // H2 for integration tests (with MariaDB-compatible test migrations)
    testRuntimeOnly("com.h2database:h2")
```

아래로 교체 (`greenmail-junit5:2.1.3` 1줄 추가):
```kotlin
    // Test
    testImplementation("org.springframework.boot:spring-boot-starter-test")
    testImplementation("org.springframework.security:spring-security-test")
    testImplementation("com.icegreen:greenmail-junit5:2.1.3")  // Phase 29 — SMTP 통합 테스트 (Jakarta Mail 2.x 호환)

    // H2 for integration tests (with MariaDB-compatible test migrations)
    testRuntimeOnly("com.h2database:h2")
```

**버전 고정 근거 (RESEARCH Assumption A1/A2, 2026-04-23 확인됨):**
- Maven Central 조회 결과 최신 stable = **2.1.3** (`curl -s "https://search.maven.org/solrsearch/select?q=g:com.icegreen+AND+a:greenmail-junit5"` 응답의 `latestVersion` 필드)
- 1.6.x 는 `javax.mail` 시절 — Spring Boot 3.5의 Jakarta Mail 2.x와 비호환 (D-D3 CONTEXT 명시 1.6.x는 researcher가 2.1.x로 상향 권장)
- 2.1.3 은 Java 17 + Jakarta Mail 2.x + JUnit 5.10+ 완벽 호환

**Step B — `backend/src/test/resources/application-test.yml` 수정:**

기존 파일 하단 (management 블록 다음) 에 아래 블록 추가. 기존 내용은 모두 그대로 유지.

기존 파일 구조 (현재):
```yaml
spring:
  datasource: ...
  jpa: ...
  flyway: ...

jwt: ...

management:
  endpoints: ...
  health:
    mail:
      enabled: false
```

교체 후 — **spring 블록 내부에 mail 추가** + **최하단 app 블록 추가**:

```yaml
spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MariaDB;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        globally_quoted_identifiers: true
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/testmigration

  # Phase 29 — GreenMail SMTP 통합 테스트 (port 3025 = ServerSetupTest.SMTP default)
  mail:
    host: 127.0.0.1
    port: 3025
    username: test
    password: test
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: false        # GreenMail plain SMTP
        mime:
          charset: UTF-8

jwt:
  secret: test-secret-key-must-be-at-least-32-bytes-long-for-hs256-testing
  access-token-ttl: 1800000
  refresh-token-ttl: 1209600000

management:
  endpoints:
    web:
      exposure:
        include: health
  health:
    mail:
      enabled: false

# Phase 29 — 통합 테스트에서 이메일 CTA URL 검증
app:
  base-url: http://127.0.0.1:5173
```

**중요:**
- `spring.mail.host=127.0.0.1 + port=3025` 조합으로 `JavaMailSender` 빈이 **생성됨** — `@ConditionalOnProperty` 가 host 존재 기준이므로 (D-D7) ApprovalEmailSender 의 mailSender 는 non-null 상태. stub mode 가 아닌 **실제 GreenMail SMTP 전송 경로**가 테스트됨.
- `default-encoding: UTF-8` 과 `mail.mime.charset=UTF-8` 양쪽 설정으로 한글 subject UTF-8 인코딩 보장 (Pitfall 3)
- `management.health.mail.enabled=false` 는 기존대로 유지 (actuator mail health check 로 테스트 시 실패 방지)
- `app.base-url=http://127.0.0.1:5173` 로 approvalUrl contains 검증이 가능
  </action>
  <verify>
    <automated>grep -q "com.icegreen:greenmail-junit5:2.1.3" backend/build.gradle.kts && grep -q "host: 127.0.0.1" backend/src/test/resources/application-test.yml && grep -q "port: 3025" backend/src/test/resources/application-test.yml && grep -q "app:" backend/src/test/resources/application-test.yml && grep -q "base-url: http://127.0.0.1:5173" backend/src/test/resources/application-test.yml && grep -q "default-encoding: UTF-8" backend/src/test/resources/application-test.yml && cd backend && ./gradlew dependencies --configuration testRuntimeClasspath | grep -q greenmail</automated>
  </verify>
  <done>
    - build.gradle.kts 에 `com.icegreen:greenmail-junit5:2.1.3` 1줄 추가
    - application-test.yml 에 spring.mail.* 블록 추가 (host/port/username/password/default-encoding/properties.mail.*)
    - application-test.yml 에 app.base-url 블록 추가
    - `./gradlew dependencies` 로 GreenMail 이 test classpath 에 포함됨 확인
  </done>
</task>

<task id="T2" type="auto" tdd="false">
  <name>Task 2: ApprovalNotificationIntegrationTest — GreenMail 5이벤트 × 수신자 규칙 × UTF-8</name>
  <files>
    - backend/src/test/java/com/micesign/notification/ApprovalNotificationIntegrationTest.java
  </files>
  <read_first>
    - backend/src/test/java/com/micesign/admin/AuditLogGapTest.java (전체 — fixture/setUp/tearDown 패턴 복제 대상)
    - backend/src/test/java/com/micesign/registration/RegistrationEmailServiceTest.java (Mockito 기반 NotificationLog 검증 패턴)
    - backend/src/main/java/com/micesign/event/ApprovalNotificationEvent.java (POJO 시그니처 확인)
    - backend/src/main/java/com/micesign/domain/Document.java (필드 채우기 fixture 빌드용)
    - backend/src/main/java/com/micesign/domain/User.java (getStatus, UserStatus enum 값)
    - .planning/phases/29-smtp-retrofit/29-RESEARCH.md §Validation Architecture (L1039-1083)
    - .planning/phases/29-smtp-retrofit/29-PATTERNS.md §11 (L438-502)
    - .planning/phases/29-smtp-retrofit/29-VALIDATION.md (L20-31)
  </read_first>
  <action>
위치: `backend/src/test/java/com/micesign/notification/ApprovalNotificationIntegrationTest.java` (Claude's Discretion: `com.micesign.notification` 새 패키지 — research 권장 구조 채택)

**Scope:** 5개 이벤트 × 4개 축 검증 (GreenMail 수신 + 한글 subject + approvalUrl + notification_log.status=SUCCESS) + RETIRED/INACTIVE skip + UNIQUE 제약. 최소 7개 @Test 메서드.

**파일 구조 (아래를 기반으로 작성):**

```java
package com.micesign.notification;

import com.icegreen.greenmail.junit5.GreenMailExtension;
import com.icegreen.greenmail.util.GreenMailUtil;
import com.icegreen.greenmail.util.ServerSetupTest;
import com.micesign.domain.*;
import com.micesign.domain.enums.*;
import com.micesign.event.ApprovalNotificationEvent;
import com.micesign.repository.*;
import jakarta.mail.internet.MimeMessage;
import org.awaitility.Awaitility;
import org.junit.jupiter.api.*;
import org.junit.jupiter.api.extension.RegisterExtension;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.context.ApplicationEventPublisher;
import org.springframework.dao.DataIntegrityViolationException;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.test.context.ActiveProfiles;

import java.time.LocalDateTime;
import java.util.List;
import java.util.concurrent.TimeUnit;

import static org.assertj.core.api.Assertions.*;

@SpringBootTest
@ActiveProfiles("test")
class ApprovalNotificationIntegrationTest {

    @RegisterExtension
    static GreenMailExtension greenMail = new GreenMailExtension(ServerSetupTest.SMTP)
            .withPerMethodLifecycle(true);   // 각 테스트마다 inbox + SMTP 서버 리셋

    @Autowired ApplicationEventPublisher eventPublisher;
    @Autowired JdbcTemplate jdbcTemplate;
    @Autowired DocumentRepository documentRepository;
    @Autowired UserRepository userRepository;
    @Autowired ApprovalLineRepository approvalLineRepository;
    // Budget API mock — submit/approve/reject 흐름 오염 차단
    @MockBean(name = "budgetApiClient") com.micesign.budget.BudgetApiClient budgetApiClient;

    // Fixture handles
    private Long docId;
    private Long drafterId;
    private Long approver1Id;   // ACTIVE
    private Long approver2Id;   // RETIRED — 테스트용으로 비활성 상태
    private Long referenceId;   // ACTIVE but REFERENCE type

    @BeforeEach
    void setUp() {
        // notification_log 는 테스트별 독립 상태 보장
        jdbcTemplate.update("DELETE FROM notification_log WHERE document_id IS NOT NULL");
        // fixture: Drafter + 3명의 결재자 + 1 Document + approval_line 3개 생성
        // (구체적인 객체 조립은 executor 판단 — 기존 fixture factory 또는 entity builder 활용)
        // 최소 필드: Document(title="휴가 신청서", drafter=drafterId, status=SUBMITTED, docNumber="GEN-2026-0001", currentStep=1)
        //            User(approver1: ACTIVE, email="active@test.com"), User(approver2: RETIRED, email="retired@test.com")
        //            ApprovalLine(doc, approver1, APPROVE, stepOrder=1), ApprovalLine(doc, approver2, APPROVE, stepOrder=2),
        //            ApprovalLine(doc, referenceUser, REFERENCE, stepOrder=3)
    }

    @AfterEach
    void tearDown() {
        jdbcTemplate.update("DELETE FROM notification_log WHERE document_id IS NOT NULL");
        // fixture cleanup
    }

    @Test
    @DisplayName("SUBMIT 이벤트: 첫 번째 비-REFERENCE 승인자에게 한글 subject + approvalUrl 메일 도착")
    void submit_deliversToFirstNonReferenceApprover_koreanSubject() throws Exception {
        eventPublisher.publishEvent(new ApprovalNotificationEvent(docId, "SUBMIT", drafterId));

        Awaitility.await().atMost(5, TimeUnit.SECONDS)
                .until(() -> greenMail.getReceivedMessages().length == 1);

        MimeMessage[] received = greenMail.getReceivedMessages();
        assertThat(received).hasSize(1);

        MimeMessage msg = received[0];
        // NOTIF-05: [MiceSign] prefix + 한글 디코딩
        assertThat(msg.getSubject()).startsWith("[MiceSign] 결재 요청:");
        assertThat(msg.getSubject()).contains("휴가 신청서");
        // NOTIF-05: approver1 만 수신 (active non-REFERENCE) — approver2 는 stepOrder=2 이므로 SUBMIT 대상 아님
        assertThat(msg.getAllRecipients()[0].toString()).isEqualTo("active@test.com");

        // NOTIF-02: CTA URL 검증
        String body = GreenMailUtil.getBody(msg);
        assertThat(body).contains("http://127.0.0.1:5173/documents/" + docId);
        assertThat(body).contains("문서 바로가기");

        // DB 상태 검증 — notification_log.status=SUCCESS 정확히 1건 (PENDING row 없음)
        Integer successCount = jdbcTemplate.queryForObject(
                "SELECT COUNT(*) FROM notification_log WHERE document_id=? AND event_type=? AND status='SUCCESS'",
                Integer.class, docId, "SUBMIT");
        assertThat(successCount).isEqualTo(1);
        Integer pendingCount = jdbcTemplate.queryForObject(
                "SELECT COUNT(*) FROM notification_log WHERE document_id=? AND event_type='SUBMIT' AND status='PENDING'",
                Integer.class, docId);
        assertThat(pendingCount).isZero();
    }

    @Test
    @DisplayName("APPROVE 이벤트: 중간 승인 후 다음 step 의 비-REFERENCE 승인자에게 전달")
    void approve_deliversToNextStepApprover() throws Exception {
        // currentStep=2 로 설정 — approver2(RETIRED) 가 다음 대상이지만 ACTIVE 필터로 skip
        // 실제로는 approver2 를 ACTIVE 로 두고 테스트하거나, currentStep=3 으로 referenceId 인 경우 REFERENCE 제외 검증
        // 본 테스트는 "stepOrder=1→2 전이" 에 대해 approver2=ACTIVE 로 fixture 조정 후 검증
        // ... (executor 가 fixture 세부 구성)
        eventPublisher.publishEvent(new ApprovalNotificationEvent(docId, "APPROVE", approver1Id));
        Awaitility.await().atMost(5, TimeUnit.SECONDS)
                .until(() -> greenMail.getReceivedMessages().length == 1);
        // subject check, recipient check, approvalUrl check, status=SUCCESS check
    }

    @Test
    @DisplayName("FINAL_APPROVE 이벤트: 기안자에게 최종 승인 완료 메일")
    void finalApprove_deliversToDrafter() throws Exception {
        eventPublisher.publishEvent(new ApprovalNotificationEvent(docId, "FINAL_APPROVE", approver1Id));
        Awaitility.await().atMost(5, TimeUnit.SECONDS)
                .until(() -> greenMail.getReceivedMessages().length == 1);
        MimeMessage msg = greenMail.getReceivedMessages()[0];
        assertThat(msg.getSubject()).startsWith("[MiceSign] 최종 승인:");
        // 수신자 = drafter email
    }

    @Test
    @DisplayName("REJECT 이벤트: 기안자에게 반려 메일 + rejectComment body 포함")
    void reject_deliversToDrafter_withRejectComment() throws Exception {
        // fixture: approver1 이 REJECTED 상태, comment="예산 초과"
        eventPublisher.publishEvent(new ApprovalNotificationEvent(docId, "REJECT", approver1Id));
        Awaitility.await().atMost(5, TimeUnit.SECONDS)
                .until(() -> greenMail.getReceivedMessages().length == 1);
        MimeMessage msg = greenMail.getReceivedMessages()[0];
        assertThat(msg.getSubject()).startsWith("[MiceSign] 반려:");
        String body = GreenMailUtil.getBody(msg);
        assertThat(body).contains("반려 사유");
        assertThat(body).contains("예산 초과");   // comment 가 본문에 노출 (D-C6)
    }

    @Test
    @DisplayName("WITHDRAW 이벤트: 결재선 전원(REFERENCE 포함)에게 회수 메일, distinct by User.id")
    void withdraw_deliversToAllApprovers_distinctByUserId() throws Exception {
        // fixture: approver1(ACTIVE), referenceId(ACTIVE, REFERENCE) 2명. approver2 는 RETIRED.
        // 같은 사용자가 stepOrder 1, 3 두 번 등록된 경우(의도된 중복) .distinct 로 1통만 수신
        eventPublisher.publishEvent(new ApprovalNotificationEvent(docId, "WITHDRAW", drafterId));
        Awaitility.await().atMost(5, TimeUnit.SECONDS)
                .until(() -> greenMail.getReceivedMessages().length == 2);  // approver1 + referenceUser
        // RETIRED 는 제외됨
    }

    @Test
    @DisplayName("RETIRED/INACTIVE 사용자는 수신 대상에서 제외된다 (NOTIF-04)")
    void skipInactiveUsers() throws Exception {
        // fixture: approver1 을 RETIRED 로 변경, approver2 만 ACTIVE
        // submit 이벤트 발행 — currentStep=1 인 approver1 이 RETIRED 이므로 메일 수신 0건
        // (단 stepOrder=1 이 유일한 승인자이면 empty recipients 로 로그만 남음)
        // 또는 approver1=RETIRED, approver2=ACTIVE + stepOrder 2개 동일 step 인 경우 approver2 만 1통 수신
    }

    @Test
    @DisplayName("UNIQUE(document_id, event_type, recipient_id) 제약 DB 레벨 enforce (D-A2)")
    void duplicateInsert_throwsDataIntegrityViolation() {
        // 직접 INSERT 2회 시도 — 두 번째에 DataIntegrityViolationException
        jdbcTemplate.update(
                "INSERT INTO notification_log (document_id, event_type, recipient_id, recipient_email, subject, status, retry_count, created_at) VALUES (?, 'SUBMIT', ?, 'a@test.com', 'test', 'SUCCESS', 0, ?)",
                docId, approver1Id, LocalDateTime.now());
        assertThatThrownBy(() -> jdbcTemplate.update(
                "INSERT INTO notification_log (document_id, event_type, recipient_id, recipient_email, subject, status, retry_count, created_at) VALUES (?, 'SUBMIT', ?, 'a@test.com', 'test2', 'SUCCESS', 0, ?)",
                docId, approver1Id, LocalDateTime.now()))
                .isInstanceOf(DataIntegrityViolationException.class);
    }
}
```

**Executor가 완성해야 할 것:**
1. Fixture 세부 구성 — Document/User/ApprovalLine 을 실제 Entity 빌더로 생성. 기존 test helper(있으면 재사용) 또는 plain `new Entity()` + repository.save.
2. Department 엔티티도 User.department 에 필요 — fixture 에 포함.
3. `BudgetApiClient` import 경로 확인 후 `@MockBean` 적용 — submit/approve 흐름에서 budget 호출을 방지.
4. `awaitility` 의존성 — Spring Boot starter-test 에 이미 포함 (별도 추가 불필요).
5. 각 테스트 후 fixture cleanup — `@AfterEach` 에서 FK 순서 맞춰 DELETE (audit_log → notification_log → approval_line → document → user → department).

**Test profile 관련 주의 (RESEARCH Pitfall 5):**
- `@ActiveProfiles("test")` 이므로 `BaseUrlGuard(@Profile("prod"))` 는 로드되지 않음 — 기동 실패 없음
- `app.base-url=http://127.0.0.1:5173` (application-test.yml) 이므로 approvalUrl 은 `http://127.0.0.1:5173/documents/{id}` 형태로 생성

**@Async + @TransactionalEventListener(AFTER_COMMIT) — AFTER_COMMIT fire 보장 (W9):**
- `publishEvent()` 는 이벤트 발행 직후 반환. `@TransactionalEventListener(phase = AFTER_COMMIT)` 은 **현재 트랜잭션이 commit 된 직후** 에만 fire.
- 통합 테스트에서 `@Transactional` 테스트 메서드는 기본 rollback — AFTER_COMMIT 이 fire 되지 않음 → 테스트가 실패로 감지되지 않고 "receivedMessages=0" 로 조용히 지나갈 수 있음.
- **본 Task 의 2가지 선택지 (옵션 A 권장):**

  **옵션 A (권장) — 실 controller 경유 + 실 트랜잭션 경계 확보:**
  ```java
  // submit 이벤트: DocumentController.submitDocument → DocumentService.submitDocument (트랜잭션 commit) → publishEvent → AFTER_COMMIT listener fire
  mockMvc.perform(post("/api/v1/documents/{id}/submit", docId)
          .header("Authorization", "Bearer " + drafterToken))
          .andExpect(status().isOk());

  Awaitility.await().atMost(5, TimeUnit.SECONDS)
          .until(() -> greenMail.getReceivedMessages().length == 1);
  ```
  장점: 실 커밋 경계 보장, controller→service→listener 전 경로 검증. 단점: mockMvc + auth 셋업 필요.

  **옵션 B (fallback) — TransactionTemplate.execute 로 명시적 커밋:**
  ```java
  @Autowired TransactionTemplate transactionTemplate;
  // ...
  transactionTemplate.execute(status -> {
      eventPublisher.publishEvent(new ApprovalNotificationEvent(docId, "SUBMIT", drafterId));
      return null;
  });
  // TransactionTemplate 가 블록 종료 시 commit → AFTER_COMMIT fire
  ```

- **테스트 메서드에 `@Transactional` 을 절대 붙이지 말 것** — 롤백되면 AFTER_COMMIT 이 fire 되지 않음.
- fixture cleanup 은 `@AfterEach` 에서 `jdbcTemplate.update("DELETE FROM ...")` 로 수동 수행.

**Executor 기본 전략:** submit/approve/reject/withdraw 는 **옵션 A** (mockMvc 경유 — DocumentController 또는 ApprovalService 직접 호출 시 `TransactionTemplate` 감싸기). 각 `@Test` 별로 controller endpoint 가 있는 이벤트는 옵션 A, 없는 이벤트(approve/reject — Plan 05 에서 controller endpoint Phase 7 대기 명시)는 옵션 B 로 `TransactionTemplate` 감싸서 publishEvent.
  </action>
  <verify>
    <automated>test -f backend/src/test/java/com/micesign/notification/ApprovalNotificationIntegrationTest.java && grep -q "GreenMailExtension" backend/src/test/java/com/micesign/notification/ApprovalNotificationIntegrationTest.java && grep -q "@RegisterExtension" backend/src/test/java/com/micesign/notification/ApprovalNotificationIntegrationTest.java && grep -q "ServerSetupTest.SMTP" backend/src/test/java/com/micesign/notification/ApprovalNotificationIntegrationTest.java && grep -q "\[MiceSign\] 결재 요청" backend/src/test/java/com/micesign/notification/ApprovalNotificationIntegrationTest.java && grep -q "\[MiceSign\] 반려" backend/src/test/java/com/micesign/notification/ApprovalNotificationIntegrationTest.java && grep -q "\[MiceSign\] 최종 승인" backend/src/test/java/com/micesign/notification/ApprovalNotificationIntegrationTest.java && grep -q "duplicateInsert_throwsDataIntegrityViolation\|DataIntegrityViolation" backend/src/test/java/com/micesign/notification/ApprovalNotificationIntegrationTest.java && grep -q "skipInactiveUsers\|RETIRED\|INACTIVE" backend/src/test/java/com/micesign/notification/ApprovalNotificationIntegrationTest.java && cd backend && ./gradlew compileTestJava --quiet && ./gradlew test --tests 'com.micesign.notification.ApprovalNotificationIntegrationTest'</automated>
  </verify>
  <done>
    - 파일 존재 + 최소 7개 `@Test` 메서드 (5 이벤트 + skip + UNIQUE)
    - GreenMailExtension `@RegisterExtension static` 선언
    - 5종 subject 한글 prefix (`결재 요청`, `승인`, `최종 승인`, `반려`, `회수`)
    - approvalUrl body 검증 (NOTIF-02)
    - rejectComment body 검증 (D-C6)
    - RETIRED/INACTIVE skip 검증 (NOTIF-04)
    - DataIntegrityViolationException 검증 (D-A2)
    - `./gradlew test --tests 'ApprovalNotificationIntegrationTest'` 전 테스트 통과
  </done>
</task>

<task id="T3" type="auto" tdd="false">
  <name>Task 3: ApprovalEmailSenderRetryTest — @Retryable 3회 + @Recover + noRetryFor 경로</name>
  <files>
    - backend/src/test/java/com/micesign/notification/ApprovalEmailSenderRetryTest.java
    - backend/src/test/resources/application-test.yml  # Task 1 에서 작성된 파일에 app.mail.retry.delay-ms: 0 1줄 추가
  </files>
  <read_first>
    - backend/src/test/java/com/micesign/registration/RegistrationEmailServiceTest.java (Mockito + ArgumentCaptor 패턴)
    - backend/src/main/java/com/micesign/service/email/ApprovalEmailSender.java (Plan 03 완성본 — @Retryable 시그니처)
    - backend/src/main/java/com/micesign/config/RetryConfig.java (@EnableRetry 확인)
    - .planning/phases/29-smtp-retrofit/29-RESEARCH.md §Common Pitfalls #2 (L385-408) + §Validation Architecture (L1036 retry test)
    - .planning/phases/29-smtp-retrofit/29-PATTERNS.md §12 (L506-547)
    - .planning/phases/29-smtp-retrofit/29-VALIDATION.md (L32-35)
  </read_first>
  <action>
위치: `backend/src/test/java/com/micesign/notification/ApprovalEmailSenderRetryTest.java`

**Scope:** `@Retryable` 이 `MailSendException` 에 대해 3회 재시도 후 `@Recover` 로 진입해서 FAILED 기록되는지 + `MailAuthenticationException` 은 즉시 FAILED 되는지. 5분 backoff 실제 대기 회피가 핵심.

**5분 backoff 회피 전략 (RESEARCH Open Questions #3 — RESOLVED):**
Plan 01 Task 3 에서 `ApprovalEmailSender.java` 의 `@Retryable` 애너테이션은 처음부터 `@Backoff(delayExpression = "#{${app.mail.retry.delay-ms:300000}}")` 로 작성됨 (default 300000ms). Plan 01 Task 3 는 `application.yml` 에 `app.mail.retry.delay-ms: ${APP_MAIL_RETRY_DELAY_MS:300000}` 을 default property 로 이미 추가함.

**본 Task 의 backoff 회피 스코프:** `application-test.yml` 에 `app.mail.retry.delay-ms: 0` override property 1줄을 추가하는 것만으로 테스트 프로파일에서 backoff 대기가 0ms 로 단축됨. `ApprovalEmailSender.java` 는 건드리지 않음 (ApprovalEmailSender.java 를 이 Task 의 files_modified 에서 제외).

테스트 클래스는 추가적으로 `@SpringBootTest(properties = "app.mail.retry.delay-ms=0")` 로 명시적 override 를 선언해 application-test.yml 과 별개로 테스트별 격리 보장 (context cache key 분리 용이).

**Step A — application-test.yml 에 `app.mail.retry.delay-ms: 0` override 추가 (Plan 04 Task 1 에서 작성된 파일 편집):**

Plan 04 Task 1 에서 추가한 `spring.mail.*` + `app.base-url` 블록에 이어 `app.mail.retry.delay-ms` 1줄 추가. Plan 01 Task 3 에서 application.yml 에 정의된 default 300000ms 를 test profile 에서 0ms 로 override.

`backend/src/test/resources/application-test.yml` 의 기존 app 블록:
```yaml
# Phase 29 — 통합 테스트에서 이메일 CTA URL 검증
app:
  base-url: http://127.0.0.1:5173
```

수정 후:
```yaml
# Phase 29 — 통합 테스트에서 이메일 CTA URL 검증 + @Retryable backoff 제거
app:
  base-url: http://127.0.0.1:5173
  mail:
    retry:
      delay-ms: 0   # @Backoff(delayExpression = "#{${app.mail.retry.delay-ms:300000}}") override — 테스트는 backoff 대기 없이 즉시 재시도
```

**중요:** 이 override 는 application-test.yml 파일에만 추가. `ApprovalEmailSender.java` 및 `application.yml` 은 건드리지 않음 (Plan 01 Task 3 에서 이미 delayExpression 형태로 확정).

**Step B — 테스트 파일 구조:**

```java
package com.micesign.notification;

import com.micesign.domain.*;
import com.micesign.domain.enums.*;
import com.micesign.repository.*;
import com.micesign.service.email.ApprovalEmailSender;
import jakarta.mail.internet.MimeMessage;
import org.junit.jupiter.api.*;
import org.mockito.ArgumentCaptor;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.mail.MailAuthenticationException;
import org.springframework.mail.MailSendException;
import org.springframework.mail.javamail.JavaMailSender;
import org.springframework.test.context.ActiveProfiles;

import java.time.LocalDateTime;

import static org.assertj.core.api.Assertions.*;
import static org.mockito.ArgumentMatchers.any;
import static org.mockito.Mockito.*;

/**
 * @Retryable / @Recover 경로 검증.
 * <p>5분 backoff 회피: application-test.yml 에 app.mail.retry.delay-ms=0 로 주입되도록
 * ApprovalEmailSender.@Retryable 이 @Backoff(delayExpression = "#{${app.mail.retry.delay-ms:300000}}") 사용.
 */
@SpringBootTest(properties = "app.mail.retry.delay-ms=0")
@ActiveProfiles("test")
class ApprovalEmailSenderRetryTest {

    @Autowired ApprovalEmailSender approvalEmailSender;
    @Autowired JdbcTemplate jdbcTemplate;
    @Autowired DocumentRepository documentRepository;
    @Autowired UserRepository userRepository;

    @MockBean JavaMailSender mailSender;                // GreenMail 대체 — send 를 강제 throw
    @MockBean(name = "budgetApiClient") com.micesign.budget.BudgetApiClient budgetApiClient;

    private Document doc;
    private User recipient;

    @BeforeEach
    void setUp() {
        jdbcTemplate.update("DELETE FROM notification_log WHERE document_id IS NOT NULL");
        // fixture: 문서 1개 + 수신자 1명 ACTIVE
        // (executor 가 entity 빌더로 조립)
        when(mailSender.createMimeMessage()).thenReturn(new MimeMessage((jakarta.mail.Session) null));
    }

    @AfterEach
    void tearDown() {
        jdbcTemplate.update("DELETE FROM notification_log WHERE document_id IS NOT NULL");
    }

    @Test
    @DisplayName("MailSendException 3회 후 @Recover → status=FAILED + retry_count=2")
    void mailSendException_retriesThreeTimes_thenRecoversToFailed() throws Exception {
        doThrow(new MailSendException("simulated transient failure"))
                .when(mailSender).send(any(MimeMessage.class));

        approvalEmailSender.send(doc, recipient, NotificationEventType.SUBMIT);

        // @Retryable maxAttempts=3 — 총 3번 호출
        verify(mailSender, times(3)).send(any(MimeMessage.class));

        // 최종 status=FAILED + errorMessage 기록
        String status = jdbcTemplate.queryForObject(
                "SELECT status FROM notification_log WHERE document_id=? AND event_type='SUBMIT' AND recipient_id=?",
                String.class, doc.getId(), recipient.getId());
        assertThat(status).isEqualTo("FAILED");

        String errorMessage = jdbcTemplate.queryForObject(
                "SELECT error_message FROM notification_log WHERE document_id=? AND event_type='SUBMIT' AND recipient_id=?",
                String.class, doc.getId(), recipient.getId());
        assertThat(errorMessage).contains("MailSendException");
        assertThat(errorMessage).contains("simulated transient failure");
        assertThat(errorMessage.length()).isLessThanOrEqualTo(255);  // truncate 보장

        // retry_count 증분 확인 — send() 3회 중 2번은 RETRY 상태로 UPDATE
        Integer retryCount = jdbcTemplate.queryForObject(
                "SELECT retry_count FROM notification_log WHERE document_id=? AND event_type='SUBMIT' AND recipient_id=?",
                Integer.class, doc.getId(), recipient.getId());
        assertThat(retryCount)
                .as("VALIDATION.md L34: @Retryable 3회 후 retry_count 정확히 2 (send() 내부 MailSendException catch 에서 +1 두 번)")
                .isEqualTo(2);
    }

    @Test
    @DisplayName("MailAuthenticationException 은 noRetryFor 이므로 1회 호출 후 즉시 FAILED")
    void mailAuthenticationException_failsImmediately_noRetry() throws Exception {
        doThrow(new MailAuthenticationException("bad credentials"))
                .when(mailSender).send(any(MimeMessage.class));

        approvalEmailSender.send(doc, recipient, NotificationEventType.SUBMIT);

        // 재시도 없음 — 1회 호출만
        verify(mailSender, times(1)).send(any(MimeMessage.class));

        String status = jdbcTemplate.queryForObject(
                "SELECT status FROM notification_log WHERE document_id=? AND event_type='SUBMIT'",
                String.class);
        assertThat(status).isEqualTo("FAILED");

        Integer retryCount = jdbcTemplate.queryForObject(
                "SELECT retry_count FROM notification_log WHERE document_id=? AND event_type='SUBMIT'",
                Integer.class);
        assertThat(retryCount).isZero();
    }

    @Test
    @DisplayName("ApprovalEmailSender 빈이 Spring AOP 프록시로 감싸져 있다 (@Retryable 활성 확인)")
    void approvalEmailSender_isAopProxy() {
        assertThat(org.springframework.aop.support.AopUtils.isAopProxy(approvalEmailSender))
                .as("@Retryable 활성화 — ApprovalEmailSender 는 Spring AOP 프록시여야 함 (Pitfall 1)")
                .isTrue();
    }
}
```

**Production 파일 미변경 보장:**
`ApprovalEmailSender.java` 는 이 Task 에서 수정하지 않음 (Plan 01 Task 3 에서 이미 `delayExpression` 으로 최종 형태 확정). `application.yml` 도 이 Task 에서 수정하지 않음 (Plan 01 Task 3 에서 default property 확정). 본 Task 는 **test 리소스 + 새 테스트 클래스** 만 수정.
  </action>
  <verify>
    <automated>test -f backend/src/test/java/com/micesign/notification/ApprovalEmailSenderRetryTest.java && grep -q "MailSendException" backend/src/test/java/com/micesign/notification/ApprovalEmailSenderRetryTest.java && grep -q "times(3)" backend/src/test/java/com/micesign/notification/ApprovalEmailSenderRetryTest.java && grep -q "MailAuthenticationException" backend/src/test/java/com/micesign/notification/ApprovalEmailSenderRetryTest.java && grep -q "AopUtils.isAopProxy\|isAopProxy" backend/src/test/java/com/micesign/notification/ApprovalEmailSenderRetryTest.java && grep -q "isEqualTo(2)" backend/src/test/java/com/micesign/notification/ApprovalEmailSenderRetryTest.java && grep -q "delay-ms: 0" backend/src/test/resources/application-test.yml && cd backend && ./gradlew test --tests 'com.micesign.notification.ApprovalEmailSenderRetryTest'</automated>
  </verify>
  <done>
    - 파일 존재 + 최소 3개 `@Test` (retryPath, noRetryPath, isAopProxy)
    - `application-test.yml` 에 `app.mail.retry.delay-ms: 0` override 추가 (1줄)
    - `@SpringBootTest(properties = "app.mail.retry.delay-ms=0")` — 실 backoff 대기 회피 (명시적 override)
    - `@MockBean JavaMailSender` + `doThrow(new MailSendException(...))` 패턴
    - `verify(mailSender, times(3)).send(...)` 와 `times(1)` 분기 검증
    - notification_log.status='FAILED' + error_message + `retry_count == 2` (정확히) 검증 (VALIDATION L34)
    - `ApprovalEmailSender.java` 미변경 — Plan 01 Task 3 에서 이미 delayExpression 확정됨
    - `./gradlew test --tests 'ApprovalEmailSenderRetryTest'` 통과 (5분 대기 없음)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GreenMail in-process SMTP → 테스트 JVM | 실 SMTP 경유하지 않으므로 outbound 누출 없음 (테스트 환경 안전) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04-01 | Denial of Service | CI 빌드에서 retry 테스트가 5분 대기하면 CI 타임아웃 | mitigate | `@Backoff(delayExpression)` + test property 0ms 주입. 테스트는 초 단위 완료. |
| T-29-04-02 | Information Disclosure | 테스트 fixture의 실제 이메일 주소 누출 | accept | `@test.com` 도메인만 사용. GreenMail 은 in-process 이므로 외부 전송 없음. |
| T-29-04-03 | Tampering | @MockBean JavaMailSender 로 SMTP 흐름 우회 검증 | accept | 의도된 unit 분리. Integration test(Task 2)에서 GreenMail 경유 실 흐름 병행 검증. |
</threat_model>

<verification>
Plan 04 완료 판정 기준:

1. **의존성/설정:**
   - `grep -q "greenmail-junit5:2.1.3" backend/build.gradle.kts`
   - `grep -q "port: 3025" backend/src/test/resources/application-test.yml`
   - `grep -q "app.mail.retry.delay-ms" backend/src/main/java/com/micesign/service/email/ApprovalEmailSender.java`

2. **테스트 실행:**
   - `./gradlew test --tests 'com.micesign.notification.*'` 전 케이스 통과
   - 실행 시간 60초 이내 (retry 테스트가 5분 대기 없음)

3. **회귀 방지:**
   - `./gradlew test --tests 'com.micesign.registration.*'` 기존 통과 (SMTP port 변경의 의도치 않은 영향 0)
   - `./gradlew test` 전체 스위트 green

4. **Contract 재확인:**
   - `@Retryable` 은 Plan 01 Task 3 에서 `delayExpression = "#{${app.mail.retry.delay-ms:300000}}"` 로 작성됨. Plan 04 는 `application-test.yml` 에 `app.mail.retry.delay-ms: 0` override 만 추가하여 5분 대기 회피.
   - `ApprovalEmailSender` 가 Spring AOP 프록시로 래핑됨 (`isAopProxy` 검증)
</verification>

<success_criteria>
- [ ] build.gradle.kts GreenMail 2.1.3 추가
- [ ] application-test.yml spring.mail.* + app.base-url 블록
- [ ] ApprovalNotificationIntegrationTest 7+ 테스트 (5 이벤트 + skip + UNIQUE)
- [ ] ApprovalEmailSenderRetryTest 3+ 테스트 (retry, noRetry, isAopProxy)
- [ ] application-test.yml 에 `app.mail.retry.delay-ms: 0` override 추가 (ApprovalEmailSender 는 Plan 01 에서 이미 delayExpression 확정)
- [ ] `./gradlew test --tests 'com.micesign.notification.*'` 전 통과 < 60s
- [ ] RegistrationEmailServiceTest 회귀 없음
- [ ] `./gradlew test` 전체 스위트 green
</success_criteria>

<output>
작업 완료 시 다음 경로에 요약 작성:
`.planning/phases/29-smtp-retrofit/29-04-SUMMARY.md`

요약에 포함할 사항:
- 최종 GreenMail 버전 확정값 (2.1.3)
- 각 테스트 클래스의 @Test 메서드 목록 + 검증한 REQ-ID
- application-test.yml 에 추가한 `app.mail.retry.delay-ms: 0` override 라인 (ApprovalEmailSender.java 는 미변경 — Plan 01 Task 3 확정본 유지)
- Plan 05 (audit test) 가 참조할 fixture 패턴
- 실행 시간 (< 60s 확인)
</output>
