---
phase: 12-schema-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/build.gradle.kts
  - backend/src/main/resources/db/migration/V8__add_template_schema_support.sql
  - backend/src/main/resources/db/migration/V9__create_template_schema_version.sql
  - backend/src/main/resources/db/migration/V10__create_option_tables.sql
  - backend/src/main/java/com/micesign/domain/ApprovalTemplate.java
  - backend/src/main/java/com/micesign/domain/DocumentContent.java
  - backend/src/main/java/com/micesign/domain/TemplateSchemaVersion.java
  - backend/src/main/java/com/micesign/domain/OptionSet.java
  - backend/src/main/java/com/micesign/domain/OptionItem.java
  - backend/src/main/java/com/micesign/repository/TemplateSchemaVersionRepository.java
  - backend/src/main/java/com/micesign/repository/OptionSetRepository.java
  - backend/src/main/java/com/micesign/repository/OptionItemRepository.java
  - backend/src/main/java/com/micesign/dto/template/SchemaDefinition.java
  - backend/src/main/java/com/micesign/dto/template/FieldDefinition.java
  - backend/src/main/java/com/micesign/dto/template/FieldConfig.java
  - backend/src/main/java/com/micesign/dto/template/CreateTemplateRequest.java
  - backend/src/main/java/com/micesign/dto/template/UpdateTemplateRequest.java
  - backend/src/main/java/com/micesign/dto/template/TemplateDetailResponse.java
  - backend/src/main/java/com/micesign/dto/option/OptionSetResponse.java
  - backend/src/main/java/com/micesign/dto/option/OptionItemResponse.java
  - backend/src/main/java/com/micesign/dto/option/CreateOptionSetRequest.java
  - backend/src/main/java/com/micesign/common/exception/FormValidationException.java
autonomous: true
requirements: [SCHM-01, SCHM-02, SCHM-04]

must_haves:
  truths:
    - "Flyway migrations V8, V9, V10 run successfully on both H2 (test) and MariaDB"
    - "ApprovalTemplate entity has schema_definition, schema_version, is_custom, category, icon, created_by fields"
    - "DocumentContent entity has schema_version, schema_definition_snapshot fields"
    - "JSON schema format is defined as Java records matching D-02/D-04/D-05 structure"
    - "All 8 field types are representable in the schema DTO structure"
  artifacts:
    - path: "backend/src/main/resources/db/migration/V8__add_template_schema_support.sql"
      provides: "approval_template + document_content column additions"
      contains: "ALTER TABLE approval_template"
    - path: "backend/src/main/java/com/micesign/dto/template/SchemaDefinition.java"
      provides: "JSON schema root structure record"
      contains: "record SchemaDefinition"
    - path: "backend/src/main/java/com/micesign/domain/TemplateSchemaVersion.java"
      provides: "Schema version history entity"
      contains: "class TemplateSchemaVersion"
    - path: "backend/src/main/java/com/micesign/common/exception/FormValidationException.java"
      provides: "Field-level validation error exception"
      contains: "class FormValidationException"
  key_links:
    - from: "ApprovalTemplate.java"
      to: "V8__add_template_schema_support.sql"
      via: "JPA column mapping matches DDL"
      pattern: "schema_definition"
    - from: "FieldDefinition.java"
      to: "SchemaDefinition.java"
      via: "fields list in schema root"
      pattern: "List<FieldDefinition>"
---

<objective>
DB 마이그레이션, JPA 엔티티 확장/생성, JSON 스키마 DTO 구조, Repository 정의 -- Phase 12의 데이터 레이어 기반을 구축한다.

Purpose: Phase 12-17 전체가 이 데이터 모델에 의존한다. 스키마 포맷(D-02)과 테이블 구조(D-15~D-18)를 올바르게 구현해야 후속 API와 검증 로직이 정상 동작한다.
Output: Flyway V8/V9/V10 마이그레이션, 확장된 JPA 엔티티들, 신규 Repository, JSON 스키마 DTO records, FormValidationException
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-schema-foundation/12-CONTEXT.md
@.planning/phases/12-schema-foundation/12-RESEARCH.md

@backend/src/main/java/com/micesign/domain/ApprovalTemplate.java
@backend/src/main/java/com/micesign/domain/DocumentContent.java
@backend/src/main/java/com/micesign/repository/ApprovalTemplateRepository.java
@backend/src/main/java/com/micesign/common/exception/GlobalExceptionHandler.java
@backend/src/main/java/com/micesign/common/exception/BusinessException.java
@backend/src/main/java/com/micesign/common/dto/ApiResponse.java
@backend/src/main/resources/db/migration/V1__create_schema.sql

<interfaces>
<!-- Existing patterns executor must follow -->

From ApprovalTemplate.java:
```java
@Entity @Table(name = "approval_template")
public class ApprovalTemplate {
    @Id @GeneratedValue(strategy = GenerationType.IDENTITY) private Long id;
    @Column(name = "code", nullable = false, unique = true, length = 20) private String code;
    @Column(name = "name", nullable = false, length = 100) private String name;
    @Column(name = "description", length = 500) private String description;
    @Column(name = "prefix", nullable = false, length = 10) private String prefix;
    @Column(name = "is_active", nullable = false) private boolean isActive = true;
    @Column(name = "sort_order", nullable = false) private int sortOrder = 0;
    @PrePersist / @PreUpdate lifecycle callbacks for createdAt/updatedAt
}
```

From DocumentContent.java:
```java
@Entity @Table(name = "document_content")
public class DocumentContent {
    @Id @GeneratedValue(strategy = GenerationType.IDENTITY) private Long id;
    @OneToOne(fetch = FetchType.LAZY) @JoinColumn(name = "document_id") private Document document;
    @Column(name = "body_html", columnDefinition = "LONGTEXT") private String bodyHtml;
    @Column(name = "form_data", columnDefinition = "JSON") private String formData;
}
```

From ApiResponse.java:
```java
public record ApiResponse<T>(boolean success, T data, ErrorDetail error) {
    public static <T> ApiResponse<T> ok(T data);
    public static <T> ApiResponse<T> error(String code, String message);
    public record ErrorDetail(String code, String message) {}
}
```
</interfaces>
</context>

<threat_model>
- **Schema injection:** JSON schema_definition stored as LONGTEXT, not executable. Parsed via Jackson ObjectMapper which does not execute code. Risk: LOW.
- **SQL injection via migration:** Flyway migrations are static SQL files. Risk: NONE.
- **Data corruption of existing templates:** All new columns are NULL-default except is_custom (DEFAULT FALSE). Existing 6 template rows retain original values. Risk: LOW.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Flyway V8/V9/V10 마이그레이션 + jnanoid 의존성</name>
  <files>
    backend/build.gradle.kts,
    backend/src/main/resources/db/migration/V8__add_template_schema_support.sql,
    backend/src/main/resources/db/migration/V9__create_template_schema_version.sql,
    backend/src/main/resources/db/migration/V10__create_option_tables.sql
  </files>
  <read_first>
    backend/build.gradle.kts,
    backend/src/main/resources/db/migration/V1__create_schema.sql,
    backend/src/main/resources/db/migration/V7__add_additional_templates.sql,
    backend/src/main/java/com/micesign/domain/ApprovalTemplate.java,
    backend/src/main/java/com/micesign/domain/DocumentContent.java
  </read_first>
  <action>
**1. build.gradle.kts:** dependencies 블록에 추가:
```kotlin
implementation("com.aventrix.jnanoid:jnanoid:2.0.0")
```

**2. V8__add_template_schema_support.sql** (per D-15, D-16):
```sql
-- approval_template 확장
ALTER TABLE approval_template
  ADD COLUMN schema_definition LONGTEXT NULL AFTER sort_order,
  ADD COLUMN schema_version INT NOT NULL DEFAULT 0 AFTER schema_definition,
  ADD COLUMN is_custom BOOLEAN NOT NULL DEFAULT FALSE AFTER schema_version,
  ADD COLUMN category VARCHAR(50) NULL AFTER is_custom,
  ADD COLUMN icon VARCHAR(50) NULL AFTER category,
  ADD COLUMN created_by BIGINT NULL AFTER icon;

ALTER TABLE approval_template
  ADD CONSTRAINT fk_template_created_by FOREIGN KEY (created_by) REFERENCES user(id);

-- document_content 확장
ALTER TABLE document_content
  ADD COLUMN schema_version INT NULL AFTER form_data,
  ADD COLUMN schema_definition_snapshot LONGTEXT NULL AFTER schema_version;
```

주의: JSON 타입 대신 LONGTEXT를 사용한다 (H2 테스트 DB 호환성 — Pitfall 5). MariaDB에서 JSON은 LONGTEXT alias이므로 동작 동일.

**3. V9__create_template_schema_version.sql** (per D-09, D-17):
```sql
CREATE TABLE template_schema_version (
  id BIGINT NOT NULL AUTO_INCREMENT,
  template_id BIGINT NOT NULL,
  version INT NOT NULL,
  schema_definition LONGTEXT NOT NULL,
  change_description VARCHAR(500) NULL,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (id),
  CONSTRAINT fk_schema_version_template FOREIGN KEY (template_id)
    REFERENCES approval_template(id) ON DELETE CASCADE,
  CONSTRAINT uk_template_version UNIQUE (template_id, version)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

**4. V10__create_option_tables.sql** (per D-06, D-18):
```sql
CREATE TABLE option_set (
  id BIGINT NOT NULL AUTO_INCREMENT,
  name VARCHAR(100) NOT NULL,
  description VARCHAR(500) NULL,
  is_active BOOLEAN NOT NULL DEFAULT TRUE,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (id),
  CONSTRAINT uk_option_set_name UNIQUE (name)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE option_item (
  id BIGINT NOT NULL AUTO_INCREMENT,
  option_set_id BIGINT NOT NULL,
  value VARCHAR(100) NOT NULL,
  label VARCHAR(200) NOT NULL,
  sort_order INT NOT NULL DEFAULT 0,
  is_active BOOLEAN NOT NULL DEFAULT TRUE,
  PRIMARY KEY (id),
  CONSTRAINT fk_option_item_set FOREIGN KEY (option_set_id)
    REFERENCES option_set(id) ON DELETE CASCADE,
  CONSTRAINT uk_option_item_value UNIQUE (option_set_id, value)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/backend && ./gradlew test --tests '*' -x compileQuerydsl 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - V8__add_template_schema_support.sql contains "ALTER TABLE approval_template" and "ADD COLUMN schema_definition LONGTEXT NULL"
    - V8__add_template_schema_support.sql contains "ALTER TABLE document_content" and "ADD COLUMN schema_definition_snapshot LONGTEXT NULL"
    - V9__create_template_schema_version.sql contains "CREATE TABLE template_schema_version" and "UNIQUE (template_id, version)"
    - V10__create_option_tables.sql contains "CREATE TABLE option_set" and "CREATE TABLE option_item"
    - build.gradle.kts contains "com.aventrix.jnanoid:jnanoid:2.0.0"
    - No LONGTEXT replaced with JSON (H2 compatibility)
  </acceptance_criteria>
  <done>3개 Flyway 마이그레이션이 H2 테스트 DB에서 정상 실행, jnanoid 의존성 추가 완료</done>
</task>

<task type="auto">
  <name>Task 2: JPA 엔티티 확장/생성 + Repository + DTO records + FormValidationException</name>
  <files>
    backend/src/main/java/com/micesign/domain/ApprovalTemplate.java,
    backend/src/main/java/com/micesign/domain/DocumentContent.java,
    backend/src/main/java/com/micesign/domain/TemplateSchemaVersion.java,
    backend/src/main/java/com/micesign/domain/OptionSet.java,
    backend/src/main/java/com/micesign/domain/OptionItem.java,
    backend/src/main/java/com/micesign/repository/TemplateSchemaVersionRepository.java,
    backend/src/main/java/com/micesign/repository/OptionSetRepository.java,
    backend/src/main/java/com/micesign/repository/OptionItemRepository.java,
    backend/src/main/java/com/micesign/dto/template/SchemaDefinition.java,
    backend/src/main/java/com/micesign/dto/template/FieldDefinition.java,
    backend/src/main/java/com/micesign/dto/template/FieldConfig.java,
    backend/src/main/java/com/micesign/dto/template/CreateTemplateRequest.java,
    backend/src/main/java/com/micesign/dto/template/UpdateTemplateRequest.java,
    backend/src/main/java/com/micesign/dto/template/TemplateDetailResponse.java,
    backend/src/main/java/com/micesign/dto/option/OptionSetResponse.java,
    backend/src/main/java/com/micesign/dto/option/OptionItemResponse.java,
    backend/src/main/java/com/micesign/dto/option/CreateOptionSetRequest.java,
    backend/src/main/java/com/micesign/common/exception/FormValidationException.java,
    backend/src/main/java/com/micesign/common/exception/GlobalExceptionHandler.java
  </files>
  <read_first>
    backend/src/main/java/com/micesign/domain/ApprovalTemplate.java,
    backend/src/main/java/com/micesign/domain/DocumentContent.java,
    backend/src/main/java/com/micesign/domain/Document.java,
    backend/src/main/java/com/micesign/domain/User.java,
    backend/src/main/java/com/micesign/repository/ApprovalTemplateRepository.java,
    backend/src/main/java/com/micesign/dto/template/TemplateResponse.java,
    backend/src/main/java/com/micesign/common/exception/BusinessException.java,
    backend/src/main/java/com/micesign/common/exception/GlobalExceptionHandler.java,
    backend/src/main/java/com/micesign/common/dto/ApiResponse.java
  </read_first>
  <action>
**1. ApprovalTemplate.java 확장** (per D-15):
기존 필드 유지하면서 아래 6개 필드 + getter/setter 추가:
```java
@Column(name = "schema_definition", columnDefinition = "LONGTEXT")
private String schemaDefinition;

@Column(name = "schema_version", nullable = false)
private int schemaVersion = 0;

@Column(name = "is_custom", nullable = false)
private boolean isCustom = false;

@Column(name = "category", length = 50)
private String category;

@Column(name = "icon", length = 50)
private String icon;

@ManyToOne(fetch = FetchType.LAZY)
@JoinColumn(name = "created_by")
private User createdBy;
```

**2. DocumentContent.java 확장** (per D-16):
기존 필드 유지하면서 아래 2개 필드 + getter/setter 추가:
```java
@Column(name = "schema_version")
private Integer schemaVersion;

@Column(name = "schema_definition_snapshot", columnDefinition = "LONGTEXT")
private String schemaDefinitionSnapshot;
```

**3. TemplateSchemaVersion.java** (NEW, per D-09/D-17):
```java
@Entity @Table(name = "template_schema_version",
  uniqueConstraints = @UniqueConstraint(columnNames = {"template_id", "version"}))
public class TemplateSchemaVersion {
    @Id @GeneratedValue(strategy = GenerationType.IDENTITY) private Long id;
    @ManyToOne(fetch = FetchType.LAZY) @JoinColumn(name = "template_id", nullable = false) private ApprovalTemplate template;
    @Column(name = "version", nullable = false) private int version;
    @Column(name = "schema_definition", columnDefinition = "LONGTEXT", nullable = false) private String schemaDefinition;
    @Column(name = "change_description", length = 500) private String changeDescription;
    @Column(name = "created_at", nullable = false, updatable = false) private LocalDateTime createdAt;
    @PrePersist protected void onCreate() { this.createdAt = LocalDateTime.now(); }
    // getters/setters
}
```

**4. OptionSet.java** (NEW, per D-06/D-18):
```java
@Entity @Table(name = "option_set")
public class OptionSet {
    @Id @GeneratedValue(strategy = GenerationType.IDENTITY) private Long id;
    @Column(name = "name", nullable = false, unique = true, length = 100) private String name;
    @Column(name = "description", length = 500) private String description;
    @Column(name = "is_active", nullable = false) private boolean isActive = true;
    @OneToMany(mappedBy = "optionSet", cascade = CascadeType.ALL, orphanRemoval = true)
    @OrderBy("sortOrder ASC") private List<OptionItem> items = new ArrayList<>();
    @Column(name = "created_at", nullable = false, updatable = false) private LocalDateTime createdAt;
    @Column(name = "updated_at", nullable = false) private LocalDateTime updatedAt;
    @PrePersist / @PreUpdate lifecycle callbacks
    // getters/setters
}
```

**5. OptionItem.java** (NEW, per D-07/D-18):
```java
@Entity @Table(name = "option_item",
  uniqueConstraints = @UniqueConstraint(columnNames = {"option_set_id", "value"}))
public class OptionItem {
    @Id @GeneratedValue(strategy = GenerationType.IDENTITY) private Long id;
    @ManyToOne(fetch = FetchType.LAZY) @JoinColumn(name = "option_set_id", nullable = false) private OptionSet optionSet;
    @Column(name = "value", nullable = false, length = 100) private String value;
    @Column(name = "label", nullable = false, length = 200) private String label;
    @Column(name = "sort_order", nullable = false) private int sortOrder = 0;
    @Column(name = "is_active", nullable = false) private boolean isActive = true;
    // getters/setters
}
```

**6. Repositories** (NEW):
- `TemplateSchemaVersionRepository extends JpaRepository<TemplateSchemaVersion, Long>` — `findByTemplateIdOrderByVersionDesc(Long templateId)`, `Optional<TemplateSchemaVersion> findByTemplateIdAndVersion(Long templateId, int version)`
- `OptionSetRepository extends JpaRepository<OptionSet, Long>` — `List<OptionSet> findByIsActiveTrueOrderByNameAsc()`
- `OptionItemRepository extends JpaRepository<OptionItem, Long>` — `List<OptionItem> findByOptionSetIdAndIsActiveTrueOrderBySortOrder(Long optionSetId)`

**7. JSON 스키마 DTO records** (per D-02, D-04, D-05):
```java
// SchemaDefinition.java — 루트 구조
public record SchemaDefinition(
    int version,
    List<FieldDefinition> fields,
    List<Object> conditionalRules,  // Phase 15에서 구체화, 현재 빈 배열
    List<Object> calculationRules   // Phase 15에서 구체화, 현재 빈 배열
) {}

// FieldDefinition.java — 개별 필드
public record FieldDefinition(
    String id,
    String type,       // text, textarea, number, date, select, table, staticText, hidden
    String label,
    boolean required,
    FieldConfig config
) {}

// FieldConfig.java — 타입별 설정 (유니온)
public record FieldConfig(
    // text/textarea
    String placeholder,
    Integer maxLength,
    // number
    Long min,
    Long max,
    String unit,
    // select
    Long optionSetId,
    List<OptionItemResponse> options,  // 스냅샷에서 사용 (D-08)
    // table
    Integer minRows,
    Integer maxRows,
    List<FieldDefinition> columns,    // 중첩 재사용 (D-05)
    // staticText
    String content,
    // hidden
    String defaultValue
) {}
```
Jackson의 `@JsonInclude(JsonInclude.Include.NON_NULL)` 적용하여 null 필드 제외.

**8. 템플릿 CRUD DTO records:**
```java
// CreateTemplateRequest.java
public record CreateTemplateRequest(
    @NotBlank String name,
    @NotBlank @Size(max = 10) String prefix,
    @Size(max = 500) String description,
    @Size(max = 50) String category,
    @Size(max = 50) String icon,
    @NotNull SchemaDefinition schemaDefinition
) {}

// UpdateTemplateRequest.java
public record UpdateTemplateRequest(
    String name,
    String description,
    String category,
    String icon,
    SchemaDefinition schemaDefinition
) {}

// TemplateDetailResponse.java
public record TemplateDetailResponse(
    Long id, String code, String name, String description, String prefix,
    boolean isActive, boolean isCustom, int schemaVersion,
    String category, String icon,
    SchemaDefinition schemaDefinition,
    LocalDateTime createdAt, LocalDateTime updatedAt
) {}
```

**9. 옵션 DTO records:**
```java
// OptionSetResponse.java
public record OptionSetResponse(Long id, String name, String description, boolean isActive, List<OptionItemResponse> items) {}

// OptionItemResponse.java
public record OptionItemResponse(Long id, String value, String label, int sortOrder, boolean isActive) {}

// CreateOptionSetRequest.java
public record CreateOptionSetRequest(
    @NotBlank @Size(max = 100) String name,
    @Size(max = 500) String description,
    @NotEmpty List<OptionItemRequest> items
) {
    public record OptionItemRequest(@NotBlank @Size(max = 100) String value, @NotBlank @Size(max = 200) String label) {}
}
```

**10. FormValidationException.java** (NEW, per D-14 / Pitfall 4):
```java
public class FormValidationException extends RuntimeException {
    private final Map<String, List<String>> fieldErrors;
    public FormValidationException(Map<String, List<String>> fieldErrors) {
        super("Form validation failed");
        this.fieldErrors = fieldErrors;
    }
    public Map<String, List<String>> getFieldErrors() { return fieldErrors; }
}
```

**11. GlobalExceptionHandler.java** 수정:
기존 handler 메서드 유지하면서 FormValidationException 핸들러 추가:
```java
@ExceptionHandler(FormValidationException.class)
public ResponseEntity<ApiResponse<?>> handleFormValidation(FormValidationException ex) {
    return ResponseEntity.badRequest().body(
        new ApiResponse<>(false, Map.of("fieldErrors", ex.getFieldErrors()),
            new ApiResponse.ErrorDetail("FORM_VALIDATION_ERROR", "양식 검증에 실패했습니다."))
    );
}
```
이렇게 하면 data 필드에 `{ fieldErrors: { fieldId: ["오류 메시지"] } }` 형태로 반환된다.
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/backend && ./gradlew compileJava 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - ApprovalTemplate.java contains "private String schemaDefinition" and "private boolean isCustom"
    - DocumentContent.java contains "private Integer schemaVersion" and "private String schemaDefinitionSnapshot"
    - TemplateSchemaVersion.java contains "@Entity" and "class TemplateSchemaVersion" and "private int version"
    - OptionSet.java contains "@Entity" and "class OptionSet" and "List<OptionItem> items"
    - OptionItem.java contains "@Entity" and "class OptionItem" and "private String value" and "private String label"
    - SchemaDefinition.java contains "record SchemaDefinition" and "List<FieldDefinition> fields"
    - FieldDefinition.java contains "record FieldDefinition" and "String type" and "FieldConfig config"
    - FieldConfig.java contains "record FieldConfig" and "List<FieldDefinition> columns" and "Long optionSetId"
    - CreateTemplateRequest.java contains "record CreateTemplateRequest" and "SchemaDefinition schemaDefinition"
    - FormValidationException.java contains "class FormValidationException" and "Map<String, List<String>> fieldErrors"
    - GlobalExceptionHandler.java contains "handleFormValidation" and "FORM_VALIDATION_ERROR"
    - TemplateSchemaVersionRepository.java contains "interface TemplateSchemaVersionRepository"
    - OptionSetRepository.java contains "interface OptionSetRepository"
    - `./gradlew compileJava` exits 0
  </acceptance_criteria>
  <done>모든 JPA 엔티티, Repository, DTO record, FormValidationException이 컴파일 성공. 기존 코드에 breaking change 없음.</done>
</task>

</tasks>

<verification>
- `cd backend && ./gradlew compileJava` 성공 (모든 엔티티/DTO 컴파일)
- `cd backend && ./gradlew test` 성공 (Flyway 마이그레이션 H2에서 정상 실행, 기존 테스트 깨지지 않음)
- ApprovalTemplate에 6개 신규 필드 존재
- DocumentContent에 2개 신규 필드 존재
- SchemaDefinition record가 D-02 구조와 정확히 일치 (version, fields[], conditionalRules[], calculationRules[])
- FieldDefinition record가 D-04 구조와 정확히 일치 (id, type, label, required, config)
</verification>

<success_criteria>
1. Flyway V8/V9/V10 마이그레이션이 기존 테스트 깨뜨리지 않고 실행
2. JSON 스키마 포맷이 Java record로 완전히 표현됨 (8개 필드 타입 모두 지원)
3. FormValidationException이 필드별 에러 맵 반환 가능
4. 기존 6개 하드코딩 템플릿의 코드/동작에 영향 없음
</success_criteria>

<output>
After completion, create `.planning/phases/12-schema-foundation/12-01-SUMMARY.md`
</output>
