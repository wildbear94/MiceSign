---
phase: 12-schema-foundation
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - backend/src/main/java/com/micesign/service/TemplateService.java
  - backend/src/main/java/com/micesign/service/TemplateSchemaService.java
  - backend/src/main/java/com/micesign/service/OptionSetService.java
  - backend/src/main/java/com/micesign/controller/TemplateController.java
  - backend/src/main/java/com/micesign/controller/OptionSetController.java
  - backend/src/main/java/com/micesign/mapper/TemplateMapper.java
  - backend/src/main/java/com/micesign/mapper/OptionSetMapper.java
  - backend/src/main/java/com/micesign/dto/template/TemplateResponse.java
  - backend/src/main/java/com/micesign/repository/ApprovalTemplateRepository.java
autonomous: true
requirements: [SCHM-01, SCHM-02, SCHM-04]

must_haves:
  truths:
    - "Admin can create a custom template via POST /api/v1/admin/templates with JSON schema"
    - "Editing a template's schema auto-increments version and saves history to template_schema_version"
    - "Admin can create, list, and manage shared option sets for select fields"
    - "Template code is auto-generated as CUSTOM_{nanoid} to prevent collision with hardcoded codes"
    - "Prefix uniqueness is validated against existing templates"
    - "Existing GET /api/v1/templates still returns active templates (backward compatible)"
  artifacts:
    - path: "backend/src/main/java/com/micesign/service/TemplateSchemaService.java"
      provides: "Schema version management"
      contains: "class TemplateSchemaService"
      exports: ["updateSchema", "resolveSchemaWithOptions"]
    - path: "backend/src/main/java/com/micesign/controller/TemplateController.java"
      provides: "Template CRUD endpoints"
      contains: "createTemplate"
    - path: "backend/src/main/java/com/micesign/service/OptionSetService.java"
      provides: "Option set CRUD"
      contains: "class OptionSetService"
  key_links:
    - from: "TemplateController.java"
      to: "TemplateService.java"
      via: "constructor injection"
      pattern: "templateService.create"
    - from: "TemplateService.java"
      to: "TemplateSchemaService.java"
      via: "schema version management delegation"
      pattern: "schemaService.updateSchema"
    - from: "TemplateSchemaService.java"
      to: "TemplateSchemaVersionRepository.java"
      via: "append-only version save"
      pattern: "schemaVersionRepository.save"
---

<objective>
템플릿 CRUD API, 스키마 버전 관리 서비스, 옵션 세트 CRUD API를 구현한다. Admin이 API를 통해 커스텀 템플릿을 생성/수정/비활성화/조회할 수 있다.

Purpose: SCHM-01(스키마 정의), SCHM-02(버전 관리), SCHM-04(8개 필드 타입 지원)의 API 레이어 구현. Phase 14(Builder UI)가 이 API를 호출한다.
Output: TemplateService CRUD, TemplateSchemaService, OptionSetService, REST 컨트롤러, MapStruct 매퍼
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-schema-foundation/12-CONTEXT.md
@.planning/phases/12-schema-foundation/12-RESEARCH.md
@.planning/phases/12-schema-foundation/12-01-SUMMARY.md

@backend/src/main/java/com/micesign/service/TemplateService.java
@backend/src/main/java/com/micesign/controller/TemplateController.java
@backend/src/main/java/com/micesign/mapper/TemplateMapper.java
@backend/src/main/java/com/micesign/dto/template/TemplateResponse.java
@backend/src/main/java/com/micesign/repository/ApprovalTemplateRepository.java

<interfaces>
<!-- From Plan 01 outputs -->

From SchemaDefinition.java:
```java
public record SchemaDefinition(int version, List<FieldDefinition> fields,
    List<Object> conditionalRules, List<Object> calculationRules) {}
```

From FieldDefinition.java:
```java
public record FieldDefinition(String id, String type, String label,
    boolean required, FieldConfig config) {}
```

From CreateTemplateRequest.java:
```java
public record CreateTemplateRequest(
    @NotBlank String name, @NotBlank @Size(max = 10) String prefix,
    @Size(max = 500) String description, @Size(max = 50) String category,
    @Size(max = 50) String icon, @NotNull SchemaDefinition schemaDefinition) {}
```

From UpdateTemplateRequest.java:
```java
public record UpdateTemplateRequest(String name, String description,
    String category, String icon, SchemaDefinition schemaDefinition) {}
```

From TemplateDetailResponse.java:
```java
public record TemplateDetailResponse(Long id, String code, String name, String description,
    String prefix, boolean isActive, boolean isCustom, int schemaVersion,
    String category, String icon, SchemaDefinition schemaDefinition,
    LocalDateTime createdAt, LocalDateTime updatedAt) {}
```

From CreateOptionSetRequest.java:
```java
public record CreateOptionSetRequest(
    @NotBlank @Size(max = 100) String name, @Size(max = 500) String description,
    @NotEmpty List<OptionItemRequest> items) {
    public record OptionItemRequest(@NotBlank String value, @NotBlank String label) {}
}
```

From ApprovalTemplate.java (after Plan 01):
- schemaDefinition (String, nullable), schemaVersion (int, default 0), isCustom (boolean), category, icon, createdBy (User)

From TemplateSchemaVersionRepository.java:
- findByTemplateIdOrderByVersionDesc(Long), findByTemplateIdAndVersion(Long, int)

From OptionSetRepository.java:
- findByIsActiveTrueOrderByNameAsc()
</interfaces>
</context>

<threat_model>
- **Authorization bypass:** All template CRUD endpoints use `@PreAuthorize("hasAnyRole('ADMIN', 'SUPER_ADMIN')")` per D-19. Risk: LOW with annotation.
- **Template code collision:** Code auto-generated as CUSTOM_{nanoid}, hardcoded codes (GENERAL, EXPENSE, etc.) cannot collide. Prefix checked against existing. Risk: LOW.
- **Schema version tampering:** Version auto-incremented server-side, not from client input. Risk: NONE.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: TemplateSchemaService + OptionSetService + OptionSetController + Mappers</name>
  <files>
    backend/src/main/java/com/micesign/service/TemplateSchemaService.java,
    backend/src/main/java/com/micesign/service/OptionSetService.java,
    backend/src/main/java/com/micesign/controller/OptionSetController.java,
    backend/src/main/java/com/micesign/mapper/OptionSetMapper.java
  </files>
  <read_first>
    backend/src/main/java/com/micesign/domain/ApprovalTemplate.java,
    backend/src/main/java/com/micesign/domain/TemplateSchemaVersion.java,
    backend/src/main/java/com/micesign/domain/OptionSet.java,
    backend/src/main/java/com/micesign/domain/OptionItem.java,
    backend/src/main/java/com/micesign/repository/TemplateSchemaVersionRepository.java,
    backend/src/main/java/com/micesign/repository/OptionSetRepository.java,
    backend/src/main/java/com/micesign/repository/OptionItemRepository.java,
    backend/src/main/java/com/micesign/dto/template/SchemaDefinition.java,
    backend/src/main/java/com/micesign/dto/template/FieldDefinition.java,
    backend/src/main/java/com/micesign/dto/template/FieldConfig.java,
    backend/src/main/java/com/micesign/dto/option/CreateOptionSetRequest.java,
    backend/src/main/java/com/micesign/dto/option/OptionSetResponse.java,
    backend/src/main/java/com/micesign/dto/option/OptionItemResponse.java,
    backend/src/main/java/com/micesign/mapper/TemplateMapper.java
  </read_first>
  <action>
**1. TemplateSchemaService.java** (NEW):
```java
@Service @Transactional
public class TemplateSchemaService {
    private final TemplateSchemaVersionRepository schemaVersionRepository;
    private final OptionSetRepository optionSetRepository;
    private final OptionItemRepository optionItemRepository;
    private final ObjectMapper objectMapper;
    // constructor injection

    /**
     * 스키마 변경 시 새 버전을 template_schema_version에 추가하고 template의 현재 버전을 갱신 (D-09, D-11)
     */
    public void updateSchema(ApprovalTemplate template, SchemaDefinition schema) {
        String schemaJson = objectMapper.writeValueAsString(schema);
        int newVersion = template.getSchemaVersion() + 1;

        TemplateSchemaVersion version = new TemplateSchemaVersion();
        version.setTemplate(template);
        version.setVersion(newVersion);
        version.setSchemaDefinition(schemaJson);
        schemaVersionRepository.save(version);

        template.setSchemaDefinition(schemaJson);
        template.setSchemaVersion(newVersion);
    }

    /**
     * 스키마에서 select 필드의 optionSetId를 실제 옵션 목록으로 resolve (D-08)
     * 문서 생성 시 스냅샷 저장에 사용
     */
    public String resolveSchemaWithOptions(String schemaJson) {
        JsonNode root = objectMapper.readTree(schemaJson);
        ArrayNode fields = (ArrayNode) root.get("fields");
        for (JsonNode field : fields) {
            resolveFieldOptions(field);
        }
        return objectMapper.writeValueAsString(root);
    }

    private void resolveFieldOptions(JsonNode field) {
        String type = field.get("type").asText();
        ObjectNode config = (ObjectNode) field.get("config");
        if (config == null) return;

        if ("select".equals(type) && config.has("optionSetId")) {
            Long optionSetId = config.get("optionSetId").asLong();
            List<OptionItem> items = optionItemRepository
                .findByOptionSetIdAndIsActiveTrueOrderBySortOrder(optionSetId);
            ArrayNode optionsArray = objectMapper.createArrayNode();
            for (OptionItem item : items) {
                ObjectNode opt = objectMapper.createObjectNode();
                opt.put("value", item.getValue());
                opt.put("label", item.getLabel());
                optionsArray.add(opt);
            }
            config.set("options", optionsArray);
        }

        if ("table".equals(type) && config.has("columns")) {
            for (JsonNode col : config.get("columns")) {
                resolveFieldOptions(col);
            }
        }
    }

    /**
     * 특정 버전의 스키마를 조회
     */
    @Transactional(readOnly = true)
    public List<TemplateSchemaVersion> getVersionHistory(Long templateId) {
        return schemaVersionRepository.findByTemplateIdOrderByVersionDesc(templateId);
    }
}
```
try-catch로 `JsonProcessingException`을 `BusinessException("SCHEMA_PARSE_ERROR", ...)` 으로 래핑.

**2. OptionSetService.java** (NEW):
```java
@Service @Transactional
public class OptionSetService {
    private final OptionSetRepository optionSetRepository;
    private final OptionSetMapper optionSetMapper;
    // constructor injection

    public OptionSetResponse create(CreateOptionSetRequest req) {
        if (optionSetRepository.existsByName(req.name())) {
            throw new BusinessException("OPT_NAME_DUPLICATE", "이미 존재하는 옵션 세트명입니다: " + req.name());
        }
        OptionSet optionSet = new OptionSet();
        optionSet.setName(req.name());
        optionSet.setDescription(req.description());
        int sortOrder = 0;
        for (var itemReq : req.items()) {
            OptionItem item = new OptionItem();
            item.setOptionSet(optionSet);
            item.setValue(itemReq.value());
            item.setLabel(itemReq.label());
            item.setSortOrder(sortOrder++);
            optionSet.getItems().add(item);
        }
        return optionSetMapper.toResponse(optionSetRepository.save(optionSet));
    }

    @Transactional(readOnly = true)
    public List<OptionSetResponse> getActiveOptionSets() {
        return optionSetRepository.findByIsActiveTrueOrderByNameAsc().stream()
            .map(optionSetMapper::toResponse).collect(Collectors.toList());
    }

    @Transactional(readOnly = true)
    public OptionSetResponse getById(Long id) {
        OptionSet set = optionSetRepository.findById(id)
            .orElseThrow(() -> new BusinessException("OPT_NOT_FOUND", "옵션 세트를 찾을 수 없습니다."));
        return optionSetMapper.toResponse(set);
    }
}
```
OptionSetRepository에 `boolean existsByName(String name)` 메서드 추가 필요.

**3. OptionSetController.java** (NEW):
```java
@RestController @RequestMapping("/api/v1/admin/option-sets")
@PreAuthorize("hasAnyRole('ADMIN', 'SUPER_ADMIN')")
public class OptionSetController {
    private final OptionSetService optionSetService;

    @PostMapping — create(CreateOptionSetRequest) → 201 Created
    @GetMapping — list() → 200 OK
    @GetMapping("/{id}") — getById(Long id) → 200 OK
}
```

**4. OptionSetMapper.java** (NEW, MapStruct):
```java
@Mapper(componentModel = "spring")
public interface OptionSetMapper {
    OptionSetResponse toResponse(OptionSet optionSet);
    OptionItemResponse toItemResponse(OptionItem item);
}
```

**5. OptionSetRepository 추가 메서드:**
`boolean existsByName(String name)` 추가.
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/backend && ./gradlew compileJava 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - TemplateSchemaService.java contains "class TemplateSchemaService" and "void updateSchema" and "resolveSchemaWithOptions"
    - OptionSetService.java contains "class OptionSetService" and "OptionSetResponse create"
    - OptionSetController.java contains "@RequestMapping(\"/api/v1/admin/option-sets\")" and "@PreAuthorize"
    - OptionSetMapper.java contains "@Mapper(componentModel = \"spring\")" and "OptionSetResponse toResponse"
    - OptionSetRepository.java contains "existsByName"
    - `./gradlew compileJava` exits 0
  </acceptance_criteria>
  <done>TemplateSchemaService, OptionSetService, OptionSetController, OptionSetMapper 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 2: TemplateService CRUD + TemplateController 확장 + TemplateMapper 확장 + 통합 테스트</name>
  <files>
    backend/src/main/java/com/micesign/service/TemplateService.java,
    backend/src/main/java/com/micesign/controller/TemplateController.java,
    backend/src/main/java/com/micesign/mapper/TemplateMapper.java,
    backend/src/main/java/com/micesign/dto/template/TemplateResponse.java,
    backend/src/main/java/com/micesign/repository/ApprovalTemplateRepository.java,
    backend/src/test/java/com/micesign/template/TemplateApiIntegrationTest.java,
    backend/src/test/java/com/micesign/template/OptionSetApiIntegrationTest.java
  </files>
  <read_first>
    backend/src/main/java/com/micesign/service/TemplateService.java,
    backend/src/main/java/com/micesign/controller/TemplateController.java,
    backend/src/main/java/com/micesign/mapper/TemplateMapper.java,
    backend/src/main/java/com/micesign/dto/template/TemplateResponse.java,
    backend/src/main/java/com/micesign/dto/template/TemplateDetailResponse.java,
    backend/src/main/java/com/micesign/dto/template/CreateTemplateRequest.java,
    backend/src/main/java/com/micesign/dto/template/UpdateTemplateRequest.java,
    backend/src/main/java/com/micesign/repository/ApprovalTemplateRepository.java,
    backend/src/main/java/com/micesign/service/TemplateSchemaService.java,
    backend/src/main/java/com/micesign/domain/ApprovalTemplate.java,
    backend/src/test/java/com/micesign/auth/AuthApiIntegrationTest.java
  </read_first>
  <action>
**1. TemplateResponse.java 확장:**
기존 필드 유지 + isCustom, category, icon 추가:
```java
public record TemplateResponse(
    Long id, String code, String name, String description, String prefix,
    boolean isCustom, String category, String icon
) {}
```

**2. ApprovalTemplateRepository.java 확장:**
기존 메서드 유지 + 추가:
```java
boolean existsByPrefix(String prefix);
boolean existsByPrefixAndIdNot(String prefix, Long id);
Optional<ApprovalTemplate> findByIdAndIsCustomTrue(Long id);
```

**3. TemplateMapper.java 확장:**
기존 toResponse 유지 + 추가:
```java
@Mapper(componentModel = "spring")
public interface TemplateMapper {
    TemplateResponse toResponse(ApprovalTemplate template);
    // 기존 toResponse는 MapStruct가 자동 매핑 (isCustom, category, icon 추가됨)
}
```
TemplateDetailResponse는 TemplateService에서 수동 매핑 (schemaDefinition JSON 파싱이 필요하므로).

**4. TemplateService.java 확장** (per D-01, D-19):
기존 getActiveTemplates() 유지하면서 CRUD 메서드 추가:
```java
@Service @Transactional
public class TemplateService {
    private final ApprovalTemplateRepository approvalTemplateRepository;
    private final TemplateMapper templateMapper;
    private final TemplateSchemaService schemaService;
    private final ObjectMapper objectMapper;
    // constructor injection (기존 + 신규)

    // 기존: getActiveTemplates() — 그대로 유지

    public TemplateDetailResponse createTemplate(Long userId, CreateTemplateRequest req) {
        // prefix 중복 검증 (Pitfall 2)
        if (approvalTemplateRepository.existsByPrefix(req.prefix().toUpperCase())) {
            throw new BusinessException("TPL_PREFIX_DUPLICATE", "이미 사용 중인 접두사입니다: " + req.prefix());
        }
        // code 자동 생성 (D-01): CUSTOM_ + nanoid 12자
        String code = "CUSTOM_" + NanoIdUtils.randomNanoId(NanoIdUtils.DEFAULT_NUMBER_GENERATOR,
            NanoIdUtils.DEFAULT_ALPHABET, 12);

        ApprovalTemplate template = new ApprovalTemplate();
        template.setCode(code);
        template.setName(req.name());
        template.setDescription(req.description());
        template.setPrefix(req.prefix().toUpperCase());
        template.setCustom(true);
        template.setCategory(req.category());
        template.setIcon(req.icon());
        // createdBy는 userId로 User를 조회하여 설정

        // 스키마 저장 + 버전 1 생성 (D-09, D-11)
        schemaService.updateSchema(template, req.schemaDefinition());

        approvalTemplateRepository.save(template);
        return toDetailResponse(template);
    }

    @Transactional(readOnly = true)
    public TemplateDetailResponse getTemplateDetail(Long id) {
        ApprovalTemplate template = approvalTemplateRepository.findById(id)
            .orElseThrow(() -> new BusinessException("TPL_NOT_FOUND", "양식을 찾을 수 없습니다."));
        return toDetailResponse(template);
    }

    public TemplateDetailResponse updateTemplate(Long id, UpdateTemplateRequest req) {
        ApprovalTemplate template = approvalTemplateRepository.findByIdAndIsCustomTrue(id)
            .orElseThrow(() -> new BusinessException("TPL_NOT_FOUND", "커스텀 양식을 찾을 수 없습니다."));

        if (req.name() != null) template.setName(req.name());
        if (req.description() != null) template.setDescription(req.description());
        if (req.category() != null) template.setCategory(req.category());
        if (req.icon() != null) template.setIcon(req.icon());

        // 스키마 변경 시 자동 버전 증가 (D-11)
        if (req.schemaDefinition() != null) {
            schemaService.updateSchema(template, req.schemaDefinition());
        }

        approvalTemplateRepository.save(template);
        return toDetailResponse(template);
    }

    public void deactivateTemplate(Long id) {
        ApprovalTemplate template = approvalTemplateRepository.findByIdAndIsCustomTrue(id)
            .orElseThrow(() -> new BusinessException("TPL_NOT_FOUND", "커스텀 양식을 찾을 수 없습니다."));
        template.setActive(false);
        approvalTemplateRepository.save(template);
    }

    @Transactional(readOnly = true)
    public List<TemplateResponse> getAllTemplates() {
        return approvalTemplateRepository.findAll().stream()
            .map(templateMapper::toResponse).collect(Collectors.toList());
    }

    private TemplateDetailResponse toDetailResponse(ApprovalTemplate t) {
        SchemaDefinition schema = null;
        if (t.getSchemaDefinition() != null) {
            schema = objectMapper.readValue(t.getSchemaDefinition(), SchemaDefinition.class);
        }
        return new TemplateDetailResponse(t.getId(), t.getCode(), t.getName(), t.getDescription(),
            t.getPrefix(), t.isActive(), t.isCustom(), t.getSchemaVersion(),
            t.getCategory(), t.getIcon(), schema, t.getCreatedAt(), t.getUpdatedAt());
    }
}
```
try-catch로 JsonProcessingException → BusinessException 래핑.

**5. TemplateController.java 확장** (per D-19):
기존 `GET /api/v1/templates` 유지하면서 Admin CRUD 추가:
```java
@RestController
@RequestMapping("/api/v1")
public class TemplateController {
    // 기존: GET /api/v1/templates (인증된 사용자 전체)
    @GetMapping("/templates")
    public ApiResponse<List<TemplateResponse>> getActiveTemplates() { ... }

    // 신규 Admin API
    @PostMapping("/admin/templates")
    @PreAuthorize("hasAnyRole('ADMIN', 'SUPER_ADMIN')")
    public ResponseEntity<ApiResponse<TemplateDetailResponse>> createTemplate(
        @AuthenticationPrincipal CustomUserDetails user,
        @Valid @RequestBody CreateTemplateRequest req) {
        return ResponseEntity.status(201).body(
            ApiResponse.ok(templateService.createTemplate(user.getId(), req)));
    }

    @GetMapping("/admin/templates")
    @PreAuthorize("hasAnyRole('ADMIN', 'SUPER_ADMIN')")
    public ApiResponse<List<TemplateResponse>> getAllTemplates() { ... }

    @GetMapping("/admin/templates/{id}")
    @PreAuthorize("hasAnyRole('ADMIN', 'SUPER_ADMIN')")
    public ApiResponse<TemplateDetailResponse> getTemplateDetail(@PathVariable Long id) { ... }

    @PutMapping("/admin/templates/{id}")
    @PreAuthorize("hasAnyRole('ADMIN', 'SUPER_ADMIN')")
    public ApiResponse<TemplateDetailResponse> updateTemplate(
        @PathVariable Long id, @Valid @RequestBody UpdateTemplateRequest req) { ... }

    @DeleteMapping("/admin/templates/{id}")
    @PreAuthorize("hasAnyRole('ADMIN', 'SUPER_ADMIN')")
    public ApiResponse<Void> deactivateTemplate(@PathVariable Long id) { ... }
}
```
`@RequestMapping` 을 `/api/v1`로 변경하여 `/templates`(공개)와 `/admin/templates`(관리자)를 하나의 컨트롤러에서 처리. 또는 별도 AdminTemplateController로 분리해도 됨 — 기존 패턴을 확인하고 일관되게 선택.

**6. 통합 테스트:**

**TemplateApiIntegrationTest.java:**
- `@SpringBootTest @AutoConfigureMockMvc`
- ADMIN 사용자로 인증 후 POST /api/v1/admin/templates 호출
- 요청 body: `{ "name": "테스트 양식", "prefix": "TST", "schemaDefinition": { "version": 1, "fields": [{ "id": "f_test1", "type": "text", "label": "제목", "required": true, "config": { "maxLength": 200 } }], "conditionalRules": [], "calculationRules": [] } }`
- 응답 검증: 201, code가 "CUSTOM_"로 시작, schemaVersion == 1
- PUT으로 스키마 수정 → schemaVersion == 2 검증
- GET으로 상세 조회 → schemaDefinition.fields[0].type == "text" 검증
- DELETE로 비활성화 → isActive == false 검증
- USER 권한으로 POST 시도 → 403 검증

**OptionSetApiIntegrationTest.java:**
- POST /api/v1/admin/option-sets 생성 검증
- GET /api/v1/admin/option-sets 목록 조회 검증
- 중복 이름 생성 시도 → 에러 검증
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/backend && ./gradlew test --tests '*TemplateApi*' --tests '*OptionSetApi*' 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - TemplateService.java contains "createTemplate" and "updateTemplate" and "deactivateTemplate" and "CUSTOM_"
    - TemplateController.java contains "/admin/templates" and "@PreAuthorize" and "createTemplate" and "updateTemplate"
    - TemplateResponse.java contains "boolean isCustom" and "String category"
    - ApprovalTemplateRepository.java contains "existsByPrefix" and "findByIdAndIsCustomTrue"
    - TemplateApiIntegrationTest.java contains "@SpringBootTest" and "createTemplate" and "schemaVersion"
    - OptionSetApiIntegrationTest.java contains "@SpringBootTest" and "option-sets"
    - `./gradlew test --tests '*TemplateApi*' --tests '*OptionSetApi*'` exits 0
  </acceptance_criteria>
  <done>템플릿 CRUD API + 옵션 세트 API가 통합 테스트 통과. Admin만 접근 가능. 스키마 수정 시 자동 버전 증가 확인.</done>
</task>

</tasks>

<verification>
- `cd backend && ./gradlew test --tests '*TemplateApi*' --tests '*OptionSetApi*'` 통과
- POST /api/v1/admin/templates로 커스텀 템플릿 생성 가능
- PUT 시 schemaVersion 자동 증가 + template_schema_version 테이블에 이력 저장
- GET /api/v1/templates (기존 API) 정상 동작 (backward compatible)
- USER 권한으로 Admin API 접근 시 403 반환
</verification>

<success_criteria>
1. Admin이 API로 커스텀 템플릿 CRUD 가능
2. 스키마 수정 시 version 자동 증가 + append-only 이력 저장
3. prefix 중복 방지, code 자동 생성
4. 옵션 세트 생성/목록 조회 가능
5. 기존 GET /api/v1/templates 하위 호환 유지
6. 통합 테스트 green
</success_criteria>

<output>
After completion, create `.planning/phases/12-schema-foundation/12-02-SUMMARY.md`
</output>
