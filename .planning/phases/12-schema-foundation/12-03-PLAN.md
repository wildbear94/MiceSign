---
phase: 12-schema-foundation
plan: 03
type: execute
wave: 3
depends_on: [12-02]
files_modified:
  - backend/src/main/java/com/micesign/service/validation/DynamicFormValidator.java
  - backend/src/main/java/com/micesign/service/DocumentFormValidator.java
  - backend/src/main/java/com/micesign/service/DocumentService.java
  - backend/src/test/java/com/micesign/template/DynamicFormValidationTest.java
  - backend/src/test/java/com/micesign/template/SchemaSnapshotTest.java
autonomous: true
requirements: [SCHM-03, SCHM-04]

must_haves:
  truths:
    - "Backend rejects invalid form_data for dynamic templates with field-level error messages"
    - "Existing 6 hardcoded template validations are untouched and still work"
    - "DocumentFormValidator falls back to DynamicFormValidator when templateCode is not in strategy map"
    - "Document creation for dynamic templates saves schema snapshot with resolved options in document_content"
    - "All 8 field types are validated correctly (text, textarea, number, date, select, table, staticText, hidden)"
  artifacts:
    - path: "backend/src/main/java/com/micesign/service/validation/DynamicFormValidator.java"
      provides: "Dynamic schema field-by-field validation"
      contains: "class DynamicFormValidator"
      exports: ["validate"]
    - path: "backend/src/main/java/com/micesign/service/DocumentFormValidator.java"
      provides: "Fallback routing to DynamicFormValidator"
      contains: "dynamicFormValidator"
    - path: "backend/src/test/java/com/micesign/template/DynamicFormValidationTest.java"
      provides: "Validation tests for all 8 field types"
      contains: "class DynamicFormValidationTest"
  key_links:
    - from: "DocumentFormValidator.java"
      to: "DynamicFormValidator.java"
      via: "fallback when strategy == null"
      pattern: "dynamicFormValidator.validate"
    - from: "DynamicFormValidator.java"
      to: "ApprovalTemplateRepository.java"
      via: "loads template to get schemaDefinition"
      pattern: "templateRepository.findByCode"
    - from: "DocumentService.java"
      to: "TemplateSchemaService.java"
      via: "resolves schema with options for snapshot"
      pattern: "schemaService.resolveSchemaWithOptions"
---

<objective>
DynamicFormValidator 구현, DocumentFormValidator fallback 통합, DocumentService 스키마 스냅샷 저장 로직 추가. 동적 템플릿의 form_data가 스키마에 맞게 검증되고, 문서 생성 시 스냅샷이 보존된다.

Purpose: SCHM-03(백엔드 검증)과 SCHM-04(8개 필드 타입 지원)의 핵심 로직. 이 검증이 없으면 잘못된 form_data가 저장되어 Phase 13 렌더링에서 오류 발생.
Output: DynamicFormValidator, 수정된 DocumentFormValidator, 수정된 DocumentService, 통합/단위 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-schema-foundation/12-CONTEXT.md
@.planning/phases/12-schema-foundation/12-RESEARCH.md
@.planning/phases/12-schema-foundation/12-01-SUMMARY.md
@.planning/phases/12-schema-foundation/12-02-SUMMARY.md

@backend/src/main/java/com/micesign/service/DocumentFormValidator.java
@backend/src/main/java/com/micesign/service/DocumentService.java
@backend/src/main/java/com/micesign/service/validation/FormValidationStrategy.java
@backend/src/main/java/com/micesign/common/exception/FormValidationException.java

<interfaces>
<!-- From Plan 01/02 outputs -->

From DocumentFormValidator.java (current):
```java
@Component
public class DocumentFormValidator {
    private final Map<String, FormValidationStrategy> strategies;
    public DocumentFormValidator(List<FormValidationStrategy> strategyList) { ... }
    public void validate(String templateCode, String bodyHtml, String formDataJson) {
        FormValidationStrategy strategy = strategies.get(templateCode);
        if (strategy == null) throw BusinessException("TPL_UNKNOWN", ...);
        strategy.validate(bodyHtml, formDataJson);
    }
}
```

From FormValidationStrategy.java:
```java
public interface FormValidationStrategy {
    String getTemplateCode();
    void validate(String bodyHtml, String formDataJson);
}
```

From FormValidationException.java (Plan 01):
```java
public class FormValidationException extends RuntimeException {
    private final Map<String, List<String>> fieldErrors;
    public FormValidationException(Map<String, List<String>> fieldErrors) { ... }
}
```

From TemplateSchemaService.java (Plan 02):
```java
public void updateSchema(ApprovalTemplate template, SchemaDefinition schema);
public String resolveSchemaWithOptions(String schemaJson);
```

From DocumentService.createDocument() (current):
```java
DocumentContent content = new DocumentContent();
content.setDocument(document);
content.setBodyHtml(req.bodyHtml());
content.setFormData(req.formData());
documentContentRepository.save(content);
```
```

From SchemaDefinition/FieldDefinition/FieldConfig (Plan 01):
- SchemaDefinition has fields[], conditionalRules[], calculationRules[]
- FieldDefinition has id, type, label, required, config
- FieldConfig has placeholder, maxLength, min, max, unit, optionSetId, options, minRows, maxRows, columns, content, defaultValue
</interfaces>
</context>

<threat_model>
- **Form data injection:** DynamicFormValidator validates every field value against its declared type. Unknown fields in form_data are ignored (not saved blindly). Risk: LOW.
- **Denial of service via large schema:** Schema size is limited by LONGTEXT but validated field count is bounded by what admin defined. Number/date overflow checked. Risk: LOW.
- **Bypass validation by submitting to hardcoded code:** Strategy map returns existing validator — no bypass possible. Risk: NONE.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: DynamicFormValidator + DocumentFormValidator fallback 통합</name>
  <files>
    backend/src/main/java/com/micesign/service/validation/DynamicFormValidator.java,
    backend/src/main/java/com/micesign/service/DocumentFormValidator.java
  </files>
  <read_first>
    backend/src/main/java/com/micesign/service/DocumentFormValidator.java,
    backend/src/main/java/com/micesign/service/validation/FormValidationStrategy.java,
    backend/src/main/java/com/micesign/common/exception/FormValidationException.java,
    backend/src/main/java/com/micesign/common/exception/BusinessException.java,
    backend/src/main/java/com/micesign/domain/ApprovalTemplate.java,
    backend/src/main/java/com/micesign/repository/ApprovalTemplateRepository.java,
    backend/src/main/java/com/micesign/dto/template/FieldConfig.java,
    backend/src/main/java/com/micesign/dto/template/FieldDefinition.java
  </read_first>
  <action>
**1. DynamicFormValidator.java** (NEW, per D-12, D-13, D-14):
```java
package com.micesign.service.validation;

@Component
public class DynamicFormValidator {
    private final ApprovalTemplateRepository templateRepository;
    private final ObjectMapper objectMapper;
    // constructor injection

    /**
     * 동적 템플릿의 form_data를 스키마 기반으로 필드별 검증.
     * 검증 실패 시 FormValidationException을 throw (D-14).
     */
    public void validate(String templateCode, String formDataJson) {
        ApprovalTemplate template = templateRepository.findByCode(templateCode)
            .orElseThrow(() -> new BusinessException("TPL_NOT_FOUND", "양식을 찾을 수 없습니다."));

        if (template.getSchemaDefinition() == null) {
            throw new BusinessException("TPL_NOT_DYNAMIC", "동적 양식이 아닙니다.");
        }

        try {
            JsonNode schema = objectMapper.readTree(template.getSchemaDefinition());
            JsonNode formData = (formDataJson != null && !formDataJson.isBlank())
                ? objectMapper.readTree(formDataJson) : objectMapper.createObjectNode();
            Map<String, List<String>> errors = new LinkedHashMap<>();

            JsonNode fields = schema.get("fields");
            if (fields != null && fields.isArray()) {
                for (JsonNode field : fields) {
                    String fieldId = field.get("id").asText();
                    String type = field.get("type").asText();
                    boolean required = field.has("required") && field.get("required").asBoolean();
                    JsonNode config = field.get("config");
                    JsonNode value = formData.get(fieldId);

                    validateField(fieldId, type, required, config, value, errors);
                }
            }

            if (!errors.isEmpty()) {
                throw new FormValidationException(errors);
            }
        } catch (FormValidationException e) {
            throw e;
        } catch (Exception e) {
            throw new BusinessException("SCHEMA_PARSE_ERROR", "스키마 검증 중 오류가 발생했습니다: " + e.getMessage());
        }
    }

    private void validateField(String fieldId, String type, boolean required,
                               JsonNode config, JsonNode value,
                               Map<String, List<String>> errors) {
        boolean isEmpty = (value == null || value.isNull() ||
            (value.isTextual() && value.asText().isBlank()));

        // staticText, hidden은 사용자 입력이 아니므로 검증 스킵
        if ("staticText".equals(type) || "hidden".equals(type)) return;

        // 필수값 체크
        if (required && isEmpty) {
            addError(errors, fieldId, "필수 입력 항목입니다.");
            return;
        }
        if (isEmpty) return;

        // 타입별 검증
        switch (type) {
            case "text", "textarea" -> validateText(fieldId, config, value, errors);
            case "number" -> validateNumber(fieldId, config, value, errors);
            case "date" -> validateDate(fieldId, value, errors);
            case "select" -> validateSelect(fieldId, config, value, errors);
            case "table" -> validateTable(fieldId, config, value, errors);
        }
    }

    private void validateText(String fieldId, JsonNode config, JsonNode value,
                              Map<String, List<String>> errors) {
        if (!value.isTextual()) {
            addError(errors, fieldId, "텍스트 형식이어야 합니다.");
            return;
        }
        if (config != null && config.has("maxLength")) {
            int maxLength = config.get("maxLength").asInt();
            if (value.asText().length() > maxLength) {
                addError(errors, fieldId, "최대 " + maxLength + "자까지 입력 가능합니다.");
            }
        }
    }

    private void validateNumber(String fieldId, JsonNode config, JsonNode value,
                                Map<String, List<String>> errors) {
        if (!value.isNumber()) {
            // Try parsing from string
            try {
                Long.parseLong(value.asText());
            } catch (NumberFormatException e) {
                addError(errors, fieldId, "숫자 형식이어야 합니다.");
                return;
            }
        }
        long numValue = value.asLong();
        if (config != null) {
            if (config.has("min") && numValue < config.get("min").asLong()) {
                addError(errors, fieldId, "최소값은 " + config.get("min").asLong() + "입니다.");
            }
            if (config.has("max") && numValue > config.get("max").asLong()) {
                addError(errors, fieldId, "최대값은 " + config.get("max").asLong() + "입니다.");
            }
        }
    }

    private void validateDate(String fieldId, JsonNode value,
                              Map<String, List<String>> errors) {
        if (!value.isTextual()) {
            addError(errors, fieldId, "날짜 형식이어야 합니다.");
            return;
        }
        try {
            LocalDate.parse(value.asText());  // ISO-8601: yyyy-MM-dd
        } catch (DateTimeParseException e) {
            addError(errors, fieldId, "날짜 형식이 올바르지 않습니다. (yyyy-MM-dd)");
        }
    }

    private void validateSelect(String fieldId, JsonNode config, JsonNode value,
                                Map<String, List<String>> errors) {
        if (!value.isTextual()) {
            addError(errors, fieldId, "선택 값은 텍스트여야 합니다.");
            return;
        }
        // select 값의 유효성은 옵션 목록과 비교하지 않음 — 옵션이 변경될 수 있으므로
        // 기본 타입 검증만 수행 (D-07: form_data에는 value만 저장)
    }

    private void validateTable(String fieldId, JsonNode config, JsonNode value,
                               Map<String, List<String>> errors) {
        if (!value.isArray()) {
            addError(errors, fieldId, "테이블 데이터는 배열이어야 합니다.");
            return;
        }
        ArrayNode rows = (ArrayNode) value;

        if (config != null) {
            if (config.has("minRows") && rows.size() < config.get("minRows").asInt()) {
                addError(errors, fieldId, "최소 " + config.get("minRows").asInt() + "행이 필요합니다.");
            }
            if (config.has("maxRows") && rows.size() > config.get("maxRows").asInt()) {
                addError(errors, fieldId, "최대 " + config.get("maxRows").asInt() + "행까지 입력 가능합니다.");
            }

            // columns별 셀 검증 (D-05)
            JsonNode columns = config.get("columns");
            if (columns != null && columns.isArray()) {
                for (int rowIdx = 0; rowIdx < rows.size(); rowIdx++) {
                    JsonNode row = rows.get(rowIdx);
                    for (JsonNode col : columns) {
                        String colId = col.get("id").asText();
                        String colType = col.get("type").asText();
                        boolean colRequired = col.has("required") && col.get("required").asBoolean();
                        JsonNode colConfig = col.get("config");
                        JsonNode cellValue = row != null ? row.get(colId) : null;

                        // 테이블 셀 에러키: "{fieldId}.rows[{rowIdx}].{colId}"
                        String cellKey = fieldId + ".rows[" + rowIdx + "]." + colId;
                        validateField(cellKey, colType, colRequired, colConfig, cellValue, errors);
                    }
                }
            }
        }
    }

    private void addError(Map<String, List<String>> errors, String fieldId, String message) {
        errors.computeIfAbsent(fieldId, k -> new ArrayList<>()).add(message);
    }
}
```

**2. DocumentFormValidator.java 수정** (per D-13):
기존 코드에서 strategy == null 일 때 BusinessException 대신 DynamicFormValidator로 위임:
```java
@Component
public class DocumentFormValidator {
    private final Map<String, FormValidationStrategy> strategies;
    private final DynamicFormValidator dynamicFormValidator;

    public DocumentFormValidator(List<FormValidationStrategy> strategyList,
                                 DynamicFormValidator dynamicFormValidator) {
        this.strategies = strategyList.stream()
            .collect(Collectors.toMap(
                FormValidationStrategy::getTemplateCode,
                Function.identity()
            ));
        this.dynamicFormValidator = dynamicFormValidator;
    }

    public void validate(String templateCode, String bodyHtml, String formDataJson) {
        FormValidationStrategy strategy = strategies.get(templateCode);
        if (strategy != null) {
            // 기존 6개 하드코딩 양식: 기존 검증 그대로 사용
            strategy.validate(bodyHtml, formDataJson);
        } else {
            // 동적 템플릿: DynamicFormValidator로 fallback (D-13)
            dynamicFormValidator.validate(templateCode, formDataJson);
        }
    }
}
```
기존 `throw new BusinessException("TPL_UNKNOWN", ...)` 부분을 `dynamicFormValidator.validate()` 호출로 교체. 기존 6개 양식의 검증 경로는 변경 없음.
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/backend && ./gradlew compileJava 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - DynamicFormValidator.java contains "class DynamicFormValidator" and "void validate(String templateCode, String formDataJson)"
    - DynamicFormValidator.java contains "validateText" and "validateNumber" and "validateDate" and "validateSelect" and "validateTable"
    - DynamicFormValidator.java contains "throw new FormValidationException(errors)"
    - DocumentFormValidator.java contains "private final DynamicFormValidator dynamicFormValidator"
    - DocumentFormValidator.java contains "dynamicFormValidator.validate(templateCode, formDataJson)"
    - DocumentFormValidator.java does NOT contain "throw new BusinessException(\"TPL_UNKNOWN\""
    - `./gradlew compileJava` exits 0
  </acceptance_criteria>
  <done>DynamicFormValidator가 8개 필드 타입을 검증하고, DocumentFormValidator가 fallback 패턴으로 통합됨</done>
</task>

<task type="auto">
  <name>Task 2: DocumentService 스냅샷 로직 + 통합 테스트</name>
  <files>
    backend/src/main/java/com/micesign/service/DocumentService.java,
    backend/src/test/java/com/micesign/template/DynamicFormValidationTest.java,
    backend/src/test/java/com/micesign/template/SchemaSnapshotTest.java
  </files>
  <read_first>
    backend/src/main/java/com/micesign/service/DocumentService.java,
    backend/src/main/java/com/micesign/service/TemplateSchemaService.java,
    backend/src/main/java/com/micesign/service/validation/DynamicFormValidator.java,
    backend/src/main/java/com/micesign/service/DocumentFormValidator.java,
    backend/src/main/java/com/micesign/domain/DocumentContent.java,
    backend/src/main/java/com/micesign/domain/ApprovalTemplate.java,
    backend/src/main/java/com/micesign/common/exception/FormValidationException.java,
    backend/src/test/java/com/micesign/template/TemplateApiIntegrationTest.java
  </read_first>
  <action>
**1. DocumentService.java 수정** (per D-10):
`createDocument()` 메서드에서 DocumentContent 저장 직후에 스키마 스냅샷 로직 추가.

기존 코드에서 `documentContentRepository.save(content)` 이전에 다음 로직 삽입:
```java
// 동적 템플릿인 경우 스키마 스냅샷 저장 (D-10, D-08)
if (template.getSchemaDefinition() != null) {
    content.setSchemaVersion(template.getSchemaVersion());
    String resolvedSchema = templateSchemaService.resolveSchemaWithOptions(
        template.getSchemaDefinition());
    content.setSchemaDefinitionSnapshot(resolvedSchema);
}
```

DocumentService에 `TemplateSchemaService` 의존성 주입 추가:
- constructor에 `TemplateSchemaService templateSchemaService` 파라미터 추가
- private final 필드 추가

`updateDocument()` 메서드에도 동일 로직 적용 — 초안 수정 시에도 최신 스키마 스냅샷 반영. (사용자가 초안을 열 때마다 템플릿 스키마가 변경되었을 수 있으므로)

**2. DynamicFormValidationTest.java** (통합 테스트):
```java
@SpringBootTest
@AutoConfigureMockMvc
@Transactional
class DynamicFormValidationTest {

    @Autowired MockMvc mockMvc;
    @Autowired ApprovalTemplateRepository templateRepository;
    @Autowired ObjectMapper objectMapper;
    // 테스트용 JWT 토큰 생성 헬퍼 재사용

    private String adminToken;
    private ApprovalTemplate testTemplate;

    @BeforeEach
    void setup() {
        // ADMIN 사용자로 JWT 발급
        // 테스트용 커스텀 템플릿 생성 (8개 필드 타입 모두 포함)
    }

    @Test void text필드_필수값_누락시_에러반환() { ... }
    @Test void text필드_maxLength_초과시_에러반환() { ... }
    @Test void number필드_min_미만시_에러반환() { ... }
    @Test void number필드_max_초과시_에러반환() { ... }
    @Test void number필드_문자열시_에러반환() { ... }
    @Test void date필드_잘못된형식시_에러반환() { ... }
    @Test void select필드_정상값_통과() { ... }
    @Test void table필드_minRows_미만시_에러반환() { ... }
    @Test void table필드_maxRows_초과시_에러반환() { ... }
    @Test void table필드_셀검증_실패시_에러반환() { ... }
    @Test void staticText_hidden_검증스킵() { ... }
    @Test void 모든필드_정상값_통과() { ... }
    @Test void 기존하드코딩양식_검증_정상작동() { ... }
}
```

각 테스트는 POST /api/v1/documents로 form_data를 전송하여 검증.
- 검증 실패 시 400 응답 + `data.fieldErrors` 에 `{ fieldId: ["오류 메시지"] }` 형태 확인
- 검증 성공 시 201 응답

**3. SchemaSnapshotTest.java** (통합 테스트):
```java
@SpringBootTest
@AutoConfigureMockMvc
@Transactional
class SchemaSnapshotTest {

    @Test void 동적템플릿_문서생성시_스냅샷저장() {
        // 1. 커스텀 템플릿 생성 (select 필드 + optionSetId 포함)
        // 2. 옵션 세트 생성 (value/label 쌍)
        // 3. POST /api/v1/documents로 문서 생성
        // 4. document_content 조회 → schema_definition_snapshot != null
        // 5. 스냅샷에 options 배열이 resolve되어 포함되어 있는지 확인
    }

    @Test void 하드코딩양식_문서생성시_스냅샷_NULL() {
        // 1. GENERAL 템플릿으로 문서 생성
        // 2. document_content 조회 → schema_definition_snapshot == null
        // 3. schema_version == null
    }

    @Test void 스키마수정후_기존문서_스냅샷유지() {
        // 1. 커스텀 템플릿 생성 (version 1)
        // 2. 문서 생성 (스냅샷 version 1)
        // 3. 템플릿 스키마 수정 (version 2)
        // 4. 기존 문서의 schema_definition_snapshot → version 1 유지
    }
}
```
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/backend && ./gradlew test --tests '*DynamicFormValidation*' --tests '*SchemaSnapshot*' 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - DocumentService.java contains "templateSchemaService" and "resolveSchemaWithOptions" and "setSchemaDefinitionSnapshot"
    - DocumentService.java contains "content.setSchemaVersion(template.getSchemaVersion())"
    - DynamicFormValidationTest.java contains "@SpringBootTest" and "FORM_VALIDATION_ERROR"
    - DynamicFormValidationTest.java contains test methods for text, number, date, select, table, staticText, hidden
    - SchemaSnapshotTest.java contains "@SpringBootTest" and "schema_definition_snapshot" or "schemaDefinitionSnapshot"
    - SchemaSnapshotTest.java contains "하드코딩양식" or "GENERAL"
    - `./gradlew test --tests '*DynamicFormValidation*' --tests '*SchemaSnapshot*'` exits 0
    - `./gradlew test` (full suite) exits 0 — no regressions in existing tests
  </acceptance_criteria>
  <done>DynamicFormValidator가 8개 필드 타입 검증 통과. 스냅샷 저장/유지 테스트 통과. 기존 테스트 전체 green.</done>
</task>

</tasks>

<verification>
- `cd backend && ./gradlew test` 전체 테스트 green (기존 + 신규 모두)
- 동적 템플릿 form_data 검증: required 누락, 타입 오류, 범위 초과 시 필드별 에러 메시지 반환
- 기존 6개 하드코딩 양식의 검증 로직에 변경 없음
- 문서 생성 시 동적 템플릿은 schema_definition_snapshot에 resolved 옵션 포함
- 하드코딩 양식 문서는 schema_definition_snapshot이 null
</verification>

<success_criteria>
1. DynamicFormValidator가 8개 필드 타입을 올바르게 검증
2. 검증 오류가 `{ fieldId: ["오류 메시지"] }` 맵 형태로 반환
3. DocumentFormValidator fallback 패턴이 기존 양식에 영향 없이 동작
4. 스키마 스냅샷이 select 옵션 resolved 상태로 저장
5. 전체 테스트 스위트 green
</success_criteria>

<output>
After completion, create `.planning/phases/12-schema-foundation/12-03-SUMMARY.md`
</output>
