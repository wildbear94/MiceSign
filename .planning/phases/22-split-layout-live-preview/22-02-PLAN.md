---
phase: 22-split-layout-live-preview
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - frontend/src/features/admin/components/TemplateFormModal.tsx
autonomous: false
requirements:
  - RFT-02
  - PRV-01
  - PRV-02
  - PRV-03
must_haves:
  truths:
    - "TemplateFormModal이 95vw/95vh 크기로 열리며 배경 딤 뒤로 기존 페이지가 보인다"
    - "모달 내부가 좌우 50:50 flex 분할이며 좌측에 기존 탭(info/fields), 우측에 미리보기 패널이 표시된다"
    - "schemaFields 상태가 변경되면 우측 미리보기가 즉시 리렌더링된다 (debounce 없음)"
    - "미리보기 패널 헤더에 전체화면(Maximize2)과 숨김(EyeOff) 버튼이 있다"
    - "전체화면 버튼 클릭 시 FullscreenPreviewPortal이 기존 모달 위에 포탈로 표시된다"
    - "미리보기 숨김 시 편집 영역이 100%로 확장되고, 모달 헤더에 Eye 아이콘 복원 버튼이 나타난다"
    - "전체화면 미리보기에서 ESC 누르면 전체화면만 닫히고 편집 모달은 유지된다"
  artifacts:
    - path: "frontend/src/features/admin/components/TemplateFormModal.tsx"
      provides: "near-fullscreen 분할 레이아웃 모달"
      contains: "w-[95vw] h-[95vh]"
  key_links:
    - from: "TemplateFormModal.tsx"
      to: "FormPreview"
      via: "import { FormPreview, FullscreenPreviewPortal } from './FormPreview'"
      pattern: "FormPreview.*fields=.*schemaFields"
    - from: "TemplateFormModal.tsx"
      to: "SchemaFieldEditor"
      via: "existing import, unchanged"
      pattern: "SchemaFieldEditor"
---

<objective>
TemplateFormModal을 near-fullscreen 분할 레이아웃으로 전환하고 FormPreview 컴포넌트를 통합한다.

Purpose: 관리자가 양식 편집과 실시간 미리보기를 좌우 분할 화면에서 동시에 수행할 수 있게 한다.
Output: 변환된 TemplateFormModal.tsx (near-fullscreen + 좌우 분할 + 미리보기 토글 + 전체화면)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-split-layout-live-preview/22-CONTEXT.md
@.planning/phases/22-split-layout-live-preview/22-RESEARCH.md
@.planning/phases/22-split-layout-live-preview/22-UI-SPEC.md
@.planning/phases/22-split-layout-live-preview/22-01-SUMMARY.md

<interfaces>
<!-- FormPreview exports created in Plan 01 -->
From frontend/src/features/admin/components/FormPreview/index.ts:
```typescript
export { default as FormPreview } from './FormPreview';
export { default as FullscreenPreviewPortal } from './FullscreenPreviewPortal';
```

FormPreview props:
```typescript
interface FormPreviewProps {
  fields: SchemaField[];
  templateName?: string;
}
```

FullscreenPreviewPortal props:
```typescript
interface FullscreenPreviewPortalProps {
  fields: SchemaField[];
  templateName?: string;
  onClose: () => void;
}
```

<!-- Existing TemplateFormModal structure (key points) -->
From frontend/src/features/admin/components/TemplateFormModal.tsx:
- useForm with zodResolver, fields: name, description, prefix, category, icon
- No `watch` currently — need to add `watch` to get live templateName for preview
- State: schemaFields (SchemaField[]), activeTab (TabKey)
- Existing modal class: "relative bg-white dark:bg-gray-800 rounded-xl shadow-lg border border-gray-200 dark:border-gray-700 p-6 w-full max-w-4xl mx-4 max-h-[90vh] overflow-y-auto"
- ESC handler via document.addEventListener('keydown')
- Tabs: info | fields
- Action buttons: close + submit at bottom of form
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TemplateFormModal near-fullscreen 분할 레이아웃 전환 + FormPreview 통합</name>
  <files>frontend/src/features/admin/components/TemplateFormModal.tsx</files>
  <read_first>
    frontend/src/features/admin/components/TemplateFormModal.tsx
    frontend/src/features/admin/components/FormPreview/index.ts
    frontend/src/features/admin/components/FormPreview/FormPreview.tsx
    frontend/src/features/admin/components/FormPreview/FullscreenPreviewPortal.tsx
    .planning/phases/22-split-layout-live-preview/22-UI-SPEC.md
    .planning/phases/22-split-layout-live-preview/22-CONTEXT.md
  </read_first>
  <action>
Modify `frontend/src/features/admin/components/TemplateFormModal.tsx` to implement the split layout. Keep ALL existing business logic (form submission, schema serialization, API calls, validation) exactly as-is. Only change layout and add preview integration.

**New imports to add:**
```typescript
import { X, Loader2, Eye, EyeOff, Maximize2 } from 'lucide-react';
import { FormPreview, FullscreenPreviewPortal } from './FormPreview';
```

**New state variables (add after existing state declarations):**
```typescript
const [showPreview, setShowPreview] = useState(true);
const [isFullscreen, setIsFullscreen] = useState(false);
```

**Add `watch` to useForm destructuring** to get live template name for preview:
Change the `useForm` destructuring to include `watch`:
```typescript
const {
  register,
  handleSubmit,
  reset,
  watch,
  formState: { errors },
  setError: setFormError,
} = useForm<TemplateFormData>({ ... });
```
Then add: `const watchedName = watch('name');`

**ESC handler modification** — change the existing ESC handler to check fullscreen state:
Replace the `handleKeyDown` function body:
```typescript
function handleKeyDown(e: KeyboardEvent) {
  if (e.key === 'Escape') {
    if (isFullscreen) {
      setIsFullscreen(false);
      e.stopPropagation();
    } else {
      onClose();
    }
  }
}
```
Note: The `useEffect` dependencies array must include `isFullscreen` in addition to existing `open` and `onClose`.

**Modal container class change** (per D-01, D-02) — replace the dialog div's className:
FROM: `"relative bg-white dark:bg-gray-800 rounded-xl shadow-lg border border-gray-200 dark:border-gray-700 p-6 w-full max-w-4xl mx-4 max-h-[90vh] overflow-y-auto"`
TO: `"relative bg-white dark:bg-gray-800 rounded-xl shadow-lg border border-gray-200 dark:border-gray-700 w-[95vw] h-[95vh] overflow-hidden flex flex-col"`

Remove the `p-6` from the container — padding will be on individual sections.

**Header restructure** — replace the existing close button + h2 with a proper header bar:
```tsx
{/* Header */}
<div className="flex-shrink-0 flex items-center justify-between px-6 py-4 border-b border-gray-200 dark:border-gray-700">
  <h2 className="text-lg font-semibold text-gray-900 dark:text-gray-50">
    {isEdit ? t('templates.editTemplate') : t('templates.addTemplate')}
  </h2>
  <div className="flex items-center gap-2">
    {!showPreview && (
      <button
        type="button"
        onClick={() => setShowPreview(true)}
        className="p-1.5 text-gray-500 hover:text-gray-700 dark:hover:text-gray-300 rounded"
        title={t('templates.previewShow')}
      >
        <Eye className="w-4 h-4" />
      </button>
    )}
    <button
      type="button"
      onClick={onClose}
      className="p-1.5 text-gray-400 hover:text-gray-600 dark:hover:text-gray-300 rounded"
      aria-label="닫기"
    >
      <X className="w-5 h-5" />
    </button>
  </div>
</div>
```

**Body: left/right split** — wrap the form content in a flex container:
```tsx
{/* Body: split layout */}
<div className="flex-1 flex min-h-0">
  {/* Left panel: editor */}
  <div className={`${showPreview ? 'w-1/2' : 'w-full'} overflow-y-auto p-6 flex flex-col`}>
    <form onSubmit={handleSubmit(onSubmit)} className="flex-1 flex flex-col">
      {/* Tabs - KEEP EXISTING TAB JSX UNCHANGED */}
      {/* Tab: info - KEEP EXISTING INFO TAB JSX UNCHANGED */}
      {/* Tab: fields - KEEP EXISTING FIELDS TAB JSX UNCHANGED */}

      {/* Action buttons - KEEP EXISTING BUTTON JSX UNCHANGED */}
    </form>
  </div>

  {/* Right panel: preview (per D-03, D-09, D-10) */}
  {showPreview && (
    <div className="w-1/2 border-l border-gray-200 dark:border-gray-700 flex flex-col">
      {/* Preview header */}
      <div className="flex-shrink-0 px-4 py-3 border-b border-gray-200 dark:border-gray-700 flex items-center justify-between">
        <span className="text-sm font-semibold text-gray-700 dark:text-gray-300">
          {t('templates.preview')}
        </span>
        <div className="flex items-center gap-1">
          <button
            type="button"
            onClick={() => setIsFullscreen(true)}
            className="p-1.5 text-gray-500 hover:text-gray-700 dark:hover:text-gray-300 rounded"
            title={t('templates.previewFullscreen')}
          >
            <Maximize2 className="w-4 h-4" />
          </button>
          <button
            type="button"
            onClick={() => setShowPreview(false)}
            className="p-1.5 text-gray-500 hover:text-gray-700 dark:hover:text-gray-300 rounded"
            title={t('templates.previewHide')}
          >
            <EyeOff className="w-4 h-4" />
          </button>
        </div>
      </div>
      {/* Preview body (per D-05) */}
      <div className="flex-1 overflow-y-auto p-6 bg-gray-100 dark:bg-gray-900">
        <FormPreview fields={schemaFields} templateName={watchedName} />
      </div>
    </div>
  )}
</div>
```

**Fullscreen portal** — add AFTER the modal div but INSIDE the outer fixed container, or better yet, render conditionally at the end of the component return:
```tsx
{/* Fullscreen preview portal (per D-07) */}
{isFullscreen && (
  <FullscreenPreviewPortal
    fields={schemaFields}
    templateName={watchedName}
    onClose={() => setIsFullscreen(false)}
  />
)}
```
Place this right before the closing fragment or at the very end of the return statement (outside the modal div but inside the outer `<div className="fixed inset-0 z-50 ...">` — actually since FullscreenPreviewPortal uses createPortal to document.body, it can be placed anywhere in the JSX tree).

**Key structural notes:**
- The `<form>` tag must wrap ONLY the left panel content (tabs + inputs + buttons). It must NOT wrap the right panel.
- Move the `<form>` tag from its current position (wrapping everything after tabs) to inside the left panel div.
- Existing tab JSX, info tab JSX, fields tab JSX, and action buttons JSX remain 100% unchanged in content — only their parent container changes.
- The `mb-4` on tabs should be kept. The `space-y-4` on info tab should be kept. The `pt-6` on action buttons should be kept.
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - TemplateFormModal.tsx contains "w-[95vw] h-[95vh]"
    - TemplateFormModal.tsx contains "overflow-hidden flex flex-col"
    - TemplateFormModal.tsx contains "w-1/2" (appears at least twice for left and right panels)
    - TemplateFormModal.tsx contains "showPreview ? 'w-1/2' : 'w-full'"
    - TemplateFormModal.tsx contains "import { FormPreview, FullscreenPreviewPortal } from './FormPreview'"
    - TemplateFormModal.tsx contains "FormPreview fields={schemaFields}"
    - TemplateFormModal.tsx contains "FullscreenPreviewPortal"
    - TemplateFormModal.tsx contains "setShowPreview" and "setIsFullscreen"
    - TemplateFormModal.tsx contains "Maximize2" and "EyeOff" and "Eye"
    - TemplateFormModal.tsx contains "bg-gray-100 dark:bg-gray-900" (preview background per D-05)
    - TemplateFormModal.tsx contains "watch('name')" or "watch" in useForm destructuring
    - TemplateFormModal.tsx contains the existing onSubmit function unchanged (mutateAsync, toast.success, etc.)
    - TemplateFormModal.tsx contains the existing SchemaFieldEditor import and usage unchanged
    - ESC handler contains "isFullscreen" check
    - tsc --noEmit exits with 0
  </acceptance_criteria>
  <done>TemplateFormModal이 95vw/95vh near-fullscreen으로 열리고, 좌우 50:50 분할 레이아웃에서 좌측은 기존 편집기, 우측은 FormPreview를 표시한다. 미리보기 토글/전체화면 포탈이 작동하며 기존 비즈니스 로직은 변경 없이 유지된다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 분할 레이아웃 + 미리보기 시각적 검증</name>
  <files>frontend/src/features/admin/components/TemplateFormModal.tsx</files>
  <action>Human verification of the split layout and live preview integration. No code changes in this task.</action>
  <what-built>
    TemplateFormModal near-fullscreen 분할 레이아웃 + FormPreview 실시간 미리보기 + 전체화면 포탈 + 미리보기 토글
  </what-built>
  <how-to-verify>
    1. 관리자로 로그인 후 양식 관리 페이지로 이동
    2. "양식 추가" 버튼 클릭 — 모달이 화면의 95% 크기로 열리는지 확인 (배경 딤 뒤로 기존 페이지가 살짝 보여야 함)
    3. 좌우 50:50 분할 확인 — 좌측에 기본정보/필드구성 탭, 우측에 "미리보기" 헤더 + 회색 배경 패널
    4. 좌측 "필드 구성" 탭 클릭 후 필드 2-3개 추가 (텍스트, 숫자, 선택 목록 등)
    5. 우측 미리보기에 추가한 필드들이 종이 문서 느낌 (흰색 카드 + 그림자)으로 즉시 표시되는지 확인
    6. 필드 라벨 변경 시 우측 미리보기에 즉시 반영되는지 확인
    7. 미리보기 헤더의 전체화면 버튼 (Maximize2 아이콘) 클릭 — 기존 모달 위에 90vw/90vh 포탈이 표시되는지 확인
    8. 전체화면에서 ESC 키 — 전체화면만 닫히고 편집 모달은 유지되는지 확인
    9. 전체화면에서 X 버튼 — 전체화면만 닫히는지 확인
    10. 미리보기 헤더의 EyeOff 아이콘 클릭 — 미리보기 패널이 사라지고 편집 영역이 전체 너비로 확장되는지 확인
    11. 모달 헤더의 Eye 아이콘 클릭 — 미리보기 패널이 다시 표시되는지 확인
    12. 기존 양식 수정 모드도 테스트 — 양식 목록에서 수정 버튼 클릭 후 기존 필드가 미리보기에 표시되는지 확인
    13. 닫기/저장 버튼이 좌측 패널 하단에 정상 표시되는지 확인
    14. 양식 저장이 기존과 동일하게 작동하는지 확인 (기본 정보 입력 후 필드 추가 후 저장)
  </how-to-verify>
  <verify>Human visual verification</verify>
  <done>관리자가 분할 레이아웃에서 양식 편집 + 실시간 미리보기를 사용할 수 있고, 전체화면/토글/ESC 처리가 정상 작동하며, 기존 양식 저장 기능이 깨지지 않음을 확인</done>
  <resume-signal>"approved"를 입력하거나, 발견된 문제를 설명해주세요</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. TemplateFormModal remains behind admin authentication. No new API calls or data paths added.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02 | T (Tampering) | TemplateFormModal | accept | Layout-only changes; form submission logic unchanged. Existing CSRF/auth protections apply. |
| T-22-03 | D (Denial of Service) | FormPreview re-render | accept | React's natural re-rendering on state change. SchemaField arrays are small (< 50 fields typically). No performance concern. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- 모달이 95vw/95vh 크기로 열림
- 좌우 분할 레이아웃 작동
- 미리보기 실시간 반영
- 전체화면 포탈 작동 (ESC로 전체화면만 닫힘)
- 미리보기 토글 작동 (숨김 -> 복원)
- 기존 양식 저장 기능 정상 작동
</verification>

<success_criteria>
- RFT-02: TemplateFormModal이 near-fullscreen 분할 레이아웃으로 확장됨
- PRV-01: 필드 변경 시 우측 패널에 실시간 미리보기 표시
- PRV-02: 전체화면 미리보기 포탈 작동
- PRV-03: 미리보기 패널 토글 가능
- 기존 양식 생성/수정 기능이 깨지지 않음
</success_criteria>

<output>
After completion, create `.planning/phases/22-split-layout-live-preview/22-02-SUMMARY.md`
</output>
