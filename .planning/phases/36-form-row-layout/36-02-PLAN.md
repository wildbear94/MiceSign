---
phase: 36-form-row-layout
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - frontend/src/features/admin/components/SchemaFieldEditor/RowPositionSelector.tsx
  - frontend/src/features/admin/components/SchemaFieldEditor/__tests__/RowPositionSelector.test.tsx
  - frontend/src/features/admin/components/SchemaFieldEditor/FieldCard.tsx
  - frontend/src/features/admin/components/SchemaFieldEditor/SchemaFieldEditor.tsx
autonomous: true
requirements:
  - PHASE-36-DA1
  - PHASE-36-DA2
  - PHASE-36-DC1
  - PHASE-36-DC2
  - PHASE-36-DC4
  - PHASE-36-DF2
  - PHASE-36-DF3
  - PHASE-36-DG1
must_haves:
  truths:
    - "Admin can pick rowGroup for any non-wide field via pill selector buttons"
    - "Buttons for rows already at cap=3 are visually disabled and show tooltip"
    - "Clicking a disabled button triggers a 200ms animate-pulse red flash + sonner toast"
    - "When admin changes a field type to textarea or table while rowGroup is set, rowGroup is silently cleared and a sonner toast fires"
    - "FieldCard shows colored left border (4-color cycle by rowGroup) and {N}행 pill in collapsed header for grouped non-wide fields"
    - "FieldCard shows '한 줄 차지' amber badge in collapsed header for textarea/table fields"
    - "RowPositionSelector hides entirely from FieldCard expanded panel when type is textarea/table"
  artifacts:
    - path: "frontend/src/features/admin/components/SchemaFieldEditor/RowPositionSelector.tsx"
      provides: "Pill-button row position selector"
      exports: ["default RowPositionSelector"]
    - path: "frontend/src/features/admin/components/SchemaFieldEditor/__tests__/RowPositionSelector.test.tsx"
      provides: "Vitest+RTL coverage of selector states"
      contains: "describe"
    - path: "frontend/src/features/admin/components/SchemaFieldEditor/FieldCard.tsx"
      provides: "Updated FieldCard with rowGroup pill, wide-type badge, border-l, RowPositionSelector slot"
      contains: "RowPositionSelector"
    - path: "frontend/src/features/admin/components/SchemaFieldEditor/SchemaFieldEditor.tsx"
      provides: "Updated orchestrator with rowOccupancy memo + force-single-row guard"
      contains: "rowOccupancy"
  key_links:
    - from: "FieldCard.tsx"
      to: "RowPositionSelector.tsx"
      via: "JSX child component"
      pattern: "RowPositionSelector"
    - from: "SchemaFieldEditor.tsx updateField"
      to: "WIDE_TYPES guard"
      via: "type-change branch sets rowGroup=undefined plus toast"
      pattern: "WIDE_TYPES\\.has"
    - from: "FieldCard.tsx outer wrapper"
      to: "ROW_GROUP_BORDER_CLASSES"
      via: "static lookup by index"
      pattern: "ROW_GROUP_BORDER_CLASSES"
    - from: "RowPositionSelector flash"
      to: "useState plus setTimeout(200)"
      via: "transient red ring on disabled-click"
      pattern: "ring-red-500"
---

<objective>
Build the builder-side UI for row group assignment.

Components produced by this plan:

1. NEW component RowPositionSelector — pill-button selector with "[단독] [행 1] [행 2] [행 3] [+ 새 행]" per UI-SPEC §B Builder Layout (lines 287~302). Disabled state for rows at cap=3. Hard-cap-3 violation flash (200ms animate-pulse ring-2 ring-red-500) plus sonner toast.

2. NEW vitest+RTL test for RowPositionSelector covering 8 documented scenarios (D-G1 builder unit test layer).

3. Modify FieldCard.tsx:
   - Outer wrapper: conditional border-l-4 border-l-{cycle}-400 via static lookup in ROW_GROUP_BORDER_CLASSES (Wave 1 export).
   - Collapsed header: rowGroup pill ({N}행) for non-wide grouped fields; "한 줄 차지" amber badge for textarea/table.
   - Expanded panel: insert RowPositionSelector block ONLY for non-wide types (UI-SPEC §B lines 268~286). Section divider style copies existing conditional-rule divider at L198.

4. Modify SchemaFieldEditor.tsx:
   - Add rowOccupancy useMemo derived from fields (UI-SPEC §G lines 601~610).
   - Pass rowOccupancy and ownCurrentRowGroup props down through FieldCard.
   - Extend updateField with wide-type force-single-row guard (D-C4): when oldField.type changes to wide AND updated.rowGroup is defined, silently clear rowGroup plus fire toast wideTypeAutoSingleToast.

Purpose: Empower admin to explicitly assign row groups (D-A1) with discoverable UX (D-A2 — pill selector chosen over drag-drop per UI-SPEC §"Source Citations"). All UX decisions are reversible per-field, no destructive operations.

Output:
- 4 files (2 new + 2 modified).
- All vitest specs green; tsc --noEmit clean; vite build succeeds.
- Manual UAT readiness for Wave 4 HUMAN-UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36-form-row-layout/36-CONTEXT.md
@.planning/phases/36-form-row-layout/36-UI-SPEC.md
@.planning/phases/36-form-row-layout/36-PATTERNS.md
@.planning/phases/36-form-row-layout/36-01-SUMMARY.md

@frontend/src/features/admin/components/SchemaFieldEditor/SchemaFieldEditor.tsx
@frontend/src/features/admin/components/SchemaFieldEditor/FieldCard.tsx
@frontend/src/features/admin/components/SchemaFieldEditor/constants.ts
@frontend/src/features/admin/components/SchemaFieldEditor/types.ts
@frontend/src/features/document/components/__tests__/DrafterInfoHeader.test.tsx

<interfaces>
RowPositionSelector public contract — committed by UI-SPEC §"Component API Contract A" lines 542~556:

```
RowPositionSelectorProps:
  value: number | null  (null = single-row, no group)
  onChange: (rowGroup: number | null) => void
  currentRowOccupancy: Record<number, number>  (rowGroup → count of non-wide fields in that row)
  ownCurrentRowGroup: number | null            (so its own slot doesn't disable its row)
  maxFieldsPerRow?: number  (defaults to 3)
```

WIDE_TYPES + ROW_GROUP_BORDER_CLASSES + ROW_GROUP_PILL_CLASSES are exported from constants.ts in Wave 1:

```
import { WIDE_TYPES, ROW_GROUP_BORDER_CLASSES, ROW_GROUP_PILL_CLASSES } from './constants';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RowPositionSelector component plus 8-case vitest unit test</name>
  <files>frontend/src/features/admin/components/SchemaFieldEditor/RowPositionSelector.tsx, frontend/src/features/admin/components/SchemaFieldEditor/__tests__/RowPositionSelector.test.tsx</files>
  <behavior>
- Renders horizontal pill row with [단독], [행 1..N] buttons (one per existing rowGroup), and a [+ 새 행] button.
- [단독] button selected when value is null. Always enabled. Calls onChange(null).
- [행 N] button selected when value === N. Disabled when currentRowOccupancy[N] >= 3 AND ownCurrentRowGroup !== N.
- [+ 새 행] button always enabled. Calls onChange(maxExistingRowGroup + 1) — if no row groups exist, returns 1.
- Clicking a disabled button: NO state change; trigger 200ms animate-pulse ring-2 ring-red-500 ring on that button + sonner toast(t('templates.rowLayout.rowFullToast')).
- Disabled buttons get native `disabled` attr + aria-disabled="true" + title attribute (rowFullTooltip).
- All buttons use type="button".
- 8 vitest cases:
  1. value=null → 단독 selected; 행 1 / + 새 행 enabled
  2. value=1, occupancy={1:1} → 행 1 selected
  3. value=null, occupancy={1:3} → 행 1 disabled, title=rowFullTooltip
  4. value=2, occupancy={1:3, 2:2} → 행 1 disabled (own field not in row 1), 행 2 selected
  5. clicking 단독 → onChange(null)
  6. clicking 행 2 → onChange(2)
  7. clicking + 새 행 → onChange(maxRowGroup + 1)
  8. clicking disabled 행 1 → onChange NOT called
  </behavior>
  <action>
Create frontend/src/features/admin/components/SchemaFieldEditor/RowPositionSelector.tsx with these elements (verbatim contract per UI-SPEC §B "Button base class" lines 294~296):

- Default export RowPositionSelector(props: RowPositionSelectorProps).
- Imports: useState, useMemo from react; Plus, Square from lucide-react; useTranslation from react-i18next; toast from sonner.
- Three module-scope class string constants: SELECTED_CLASS, UNSELECTED_CLASS, DISABLED_CLASS — concatenated literal strings (avoid template interpolation per PATTERNS Tailwind rule). Take these strings VERBATIM from UI-SPEC §B lines 294~296.
- FLASH_CLASS = 'animate-pulse ring-2 ring-red-500' literal string.
- visibleRowNumbers derived: build array of rowGroup numbers from currentRowOccupancy keys (sorted ascending). If `value` is set to a row not yet in occupancy (just clicked + 새 행 in last render), include it.
- maxRow = max of visibleRowNumbers (or 0 if empty).
- triggerFlashAndToast(idx): set flashIndex state, call toast(t('templates.rowLayout.rowFullToast')), setTimeout(200) clears flashIndex.
- handleSingleClick: onChange(null).
- handleRowClick(n, isDisabled, idx): if disabled call triggerFlashAndToast(idx) and return; else onChange(n).
- handleNewRowClick: onChange(maxRow + 1).
- JSX outer wrapper: <div className="flex items-center gap-2 flex-wrap">.
- 단독 button: type="button", aria-pressed={value===null}, className=SELECTED if value===null else UNSELECTED, contains <Square className="w-3 h-3"/> + i18n singleButton text.
- Per visibleRowNumbers map: button type="button", disabled={isFull && !isSelected}, aria-disabled, aria-pressed, title only when full+not-selected, className choice (SELECTED if selected, DISABLED if isFull, UNSELECTED otherwise) plus FLASH_CLASS appended when flashIndex===idx, text uses t('templates.rowLayout.rowButton', {number: n}).
- + 새 행 button: type="button", className=UNSELECTED, contains <Plus className="w-3 h-3"/> + i18n newRowButton text.

Per D-A1 admin explicit + D-A2 intuition-first + D-F3 hard cap=3 visual disable plus flash. No new dependencies.

Create frontend/src/features/admin/components/SchemaFieldEditor/__tests__/RowPositionSelector.test.tsx using DrafterInfoHeader.test.tsx identity-mock pattern:

- Mock react-i18next with identity t — but for keys with {number} interpolation, return `${key}:${number}` (so test queries can match "rowButton:1", "rowButton:2" etc.).
- Mock sonner so toast calls are captured by a vi.fn (toastMock).
- 8 it() cases listed in <behavior>. Use render + screen.getByRole('button', {name: /pattern/i}) + assertions on aria-pressed / disabled / title attributes / onChange spy.
- For test 8 (disabled click), assert onChange NOT called — note in comment that native `disabled` attr suppresses synthetic click in jsdom, so the toast branch is exercised by handler logic but not necessarily fired in this jsdom path. Comment the rationale per PATTERNS test commentary.

Per PATTERNS §"Toast notification pattern" + §"<button type='button'> + lucide icon + Tailwind state classes" + §"Test fixture builder pattern".
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/features/admin/components/SchemaFieldEditor/__tests__/RowPositionSelector.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <done>
- All 8 vitest cases pass
- File exports default RowPositionSelector matching prop signature
- Buttons use type="button"
- Disabled buttons have native disabled attr + aria-disabled + title
- tsc --noEmit clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FieldCard.tsx — border-l + collapsed-header pill/badge + expanded-panel selector slot</name>
  <files>frontend/src/features/admin/components/SchemaFieldEditor/FieldCard.tsx</files>
  <behavior>
- Outer wrapper appends one of ROW_GROUP_BORDER_CLASSES[(rowGroup-1) % 4] when rowGroup is defined AND type is non-wide. Else: no border-l class added.
- Collapsed header (between TypeBadge and field name):
  - If rowGroup defined AND non-wide: render rowGroup pill span using ROW_GROUP_PILL_CLASSES[(rowGroup-1)%4], text from t('templates.rowLayout.rowGroupBadge', {number}).
  - If wide type: render "한 줄 차지" amber badge with Square icon (mirrors L96~99 amber pattern verbatim).
  - Otherwise: nothing.
- Expanded panel (between L188 Required checkbox and L191 Type-specific config):
  - If non-wide: render section divider + label "행 위치" + RowPositionSelector + helper hint paragraph.
  - If wide: do NOT render the selector section.
- New required prop: rowOccupancy: Record<number, number>.
  </behavior>
  <action>
Edit FieldCard.tsx per UI-SPEC §F (line 588~592) and PATTERNS FieldCard section:

1. Add imports at file top: `Square` from lucide-react; default `RowPositionSelector` from './RowPositionSelector'; named `WIDE_TYPES, ROW_GROUP_BORDER_CLASSES, ROW_GROUP_PILL_CLASSES` from './constants'.

2. Extend FieldCardProps interface with required prop `rowOccupancy: Record<number, number>` (passed from SchemaFieldEditor parent — see Task 3).

3. Compute these inside the component body:
   - `const isWide = WIDE_TYPES.has(field.type);`
   - `const rowBorderClass = field.rowGroup !== undefined && !isWide ? ROW_GROUP_BORDER_CLASSES[(field.rowGroup - 1) % 4] : '';`

4. Modify the outer wrapper's className (current L74) by concatenating rowBorderClass to the existing class string. Do NOT use template interpolation for the cycle class — use the static array lookup result above. This is mandatory per PATTERNS §"Tailwind dynamic class enumeration CRITICAL".

5. Insert in the collapsed header AFTER the existing TypeBadge (~L83) and BEFORE the field name (~L84):
   - Conditional rowGroup pill: when `field.rowGroup !== undefined && !isWide`, render a span with className that concatenates the base pill classes ('inline-flex items-center gap-1 px-1.5 py-0.5 text-xs rounded') with ROW_GROUP_PILL_CLASSES[(field.rowGroup - 1) % 4]. Inner text uses t('templates.rowLayout.rowGroupBadge', {number: field.rowGroup}).
   - Conditional wide-type badge: when `isWide`, render a span with className 'inline-flex items-center gap-1 px-1.5 py-0.5 text-xs rounded bg-amber-100 text-amber-700 dark:bg-amber-900/40 dark:text-amber-300', containing a `<Square className="w-3 h-3" />` icon and text from t('templates.rowLayout.wideTypeBadge'). The amber pill class string mirrors L96~99 production SoT verbatim per UI-SPEC §"Spacing" line 58 (Dim 5 production source of truth).

6. Insert RowPositionSelector block in expanded panel — AFTER the "필수 입력" required-checkbox section (around L188) and BEFORE the FieldConfigEditor (around L191~194). Section divider style: 'border-t border-gray-100 dark:border-gray-700 pt-3' (copies the existing conditional-rule divider at L198 verbatim). Wrap the entire block in `{!isWide && (...)}`.
   - Inside: a `<label className="block text-xs font-medium text-gray-600 dark:text-gray-400 mb-2">{t('templates.rowLayout.sectionLabel')}</label>`.
   - Then `<RowPositionSelector value={field.rowGroup ?? null} onChange={(rg) => onUpdate({...field, rowGroup: rg ?? undefined})} currentRowOccupancy={rowOccupancy} ownCurrentRowGroup={field.rowGroup ?? null} />`.
   - Then a `<p className="mt-2 text-xs text-gray-500 dark:text-gray-400">{t('templates.rowLayout.helperHint')}</p>`.

7. DO NOT modify any of the 12 built-in template files (D-B2 explicit no-touch — verified absent from files_modified).

Per D-A2 (pill selector visible only for non-wide types) + D-C1/D-C2 (wide types unconditionally single-row).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10 && grep -c "RowPositionSelector\|ROW_GROUP_BORDER_CLASSES\|ROW_GROUP_PILL_CLASSES\|wideTypeBadge\|rowGroupBadge" frontend/src/features/admin/components/SchemaFieldEditor/FieldCard.tsx</automated>
  </verify>
  <done>
- tsc --noEmit returns 0 errors
- FieldCard imports RowPositionSelector + WIDE_TYPES + ROW_GROUP_*_CLASSES
- rowGroup pill renders only for non-wide grouped fields
- wide-type 한 줄 차지 badge renders only for textarea/table
- RowPositionSelector block renders only for non-wide types
- Outer wrapper border-l class is selected via static array lookup (no Tailwind interpolation)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SchemaFieldEditor.tsx — rowOccupancy memo + force-single-row guard</name>
  <files>frontend/src/features/admin/components/SchemaFieldEditor/SchemaFieldEditor.tsx</files>
  <behavior>
- rowOccupancy: Record<number, number> is computed via useMemo from fields, including only non-wide fields with defined rowGroup.
- rowOccupancy is passed as a prop to every <FieldCard rowOccupancy={rowOccupancy} ... /> instance.
- updateField extends with a wide-type force-single-row guard at the TOP of the body (before existing rules-cleanup branches): when oldField.type !== updated.type AND WIDE_TYPES.has(updated.type) AND updated.rowGroup !== undefined, mutate `updated` to clear rowGroup, then fire toast(t('templates.rowLayout.wideTypeAutoSingleToast')).
- Existing conditional-rule cleanup (L63~107) and calculation-rule cleanup branches continue to work unchanged.
- Existing tests that render SchemaFieldEditor still pass after adding the prop pass-through.
  </behavior>
  <action>
Edit SchemaFieldEditor.tsx per UI-SPEC §G (lines 601~610) and §"Force-Single-Row Guard" (lines 471~481):

1. Add `import { WIDE_TYPES } from './constants';` at file top.

2. Add a useMemo for rowOccupancy near the existing `cycles` memo (around L55). The memo: build an empty Record<number, number>, iterate fields, for each f where f.rowGroup !== undefined AND !WIDE_TYPES.has(f.type), increment occ[f.rowGroup]. Return occ. Dependency array: [fields].

3. In updateField (locate at L57~108), add the force-single-row guard at the TOP of the body BEFORE the existing rules-cleanup logic and BEFORE the existing onChange call. The guard condition: `oldField.type !== updated.type && WIDE_TYPES.has(updated.type) && updated.rowGroup !== undefined`. When true: reassign `updated = { ...updated, rowGroup: undefined };` and call `toast(t('templates.rowLayout.wideTypeAutoSingleToast'))`. CRITICAL: the guard must mutate the local `updated` reference BEFORE `onChange(newFields)` is called and BEFORE the rules-cleanup branches run, so downstream cleanup sees the cleaned field. If the existing structure differs slightly, adapt placement but preserve the invariant: the rowGroup-cleared `updated` is what gets stored.

4. Locate the existing FieldCard render call (around L198) and add `rowOccupancy={rowOccupancy}` to its props.

Per D-C4: drag-drop is not used in the field-list builder (only TableColumnEditor has drag-drop for table columns). Type change via FieldConfigEditor is the only path to convert non-wide → wide; the guard handles this transition.

DO NOT call any external function or backend API — guard is purely local state mutation. Existing toast import (L4) is reused.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10 && npx vitest run src/features/admin/components/SchemaFieldEditor 2>&1 | tail -15 && grep -c "rowOccupancy\|WIDE_TYPES\|wideTypeAutoSingleToast" frontend/src/features/admin/components/SchemaFieldEditor/SchemaFieldEditor.tsx</automated>
  </verify>
  <done>
- tsc --noEmit 0 errors
- Existing SchemaFieldEditor + FieldCard vitest suites still pass (or pass with prop addition; test fixtures may need rowOccupancy={{}} added to render calls — fix as needed)
- rowOccupancy useMemo present
- updateField guard branch present BEFORE existing rules-cleanup
- WIDE_TYPES imported from ./constants
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI → schema state (in memory) | Admin's row-position pick crosses into editable schema state via React onChange |
| Admin UI → external paste/import | NOT relevant in this plan (Zod is in Wave 1; Wave 2 is in-memory only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-06 | T (Tampering) | RowPositionSelector hard-cap-3 bypass | mitigate | Disabled buttons have native `disabled` HTML attr (suppresses click) PLUS aria-disabled + handler-side `if (isDisabled) return` guard. 3-layer enforcement: visual disable + native attr + handler check. Final layer is Wave 1 Zod refinement at import boundary. |
| T-36-07 | I (Information Disclosure) | sonner toast contents | accept | Toast text is i18n key from public catalog; no PII, no secrets. |
| T-36-08 | E (Elevation of Privilege) | rowGroup field write path | accept | Builder is admin-only (existing route guard). rowGroup is purely layout metadata; no permission, no user identity, no audit-log impact. |
| T-36-09 | T (Tampering) | wide-type force-single guard | mitigate | Guard fires inside React updateField (controlled-input flow). Admin cannot persist a wide-type-with-rowGroup state because the guard fires SYNCHRONOUSLY before onChange and BEFORE Zod validation at save time would catch it as fallback. |
| T-36-10 | D (Denial of Service) | RowPositionSelector flash useState | accept | setTimeout 200ms is bounded; if component unmounts during timer, React 18 cleanup is automatic for state setters; no leak. |
</threat_model>

<verification>
- cd frontend && npx tsc --noEmit returns 0 errors.
- cd frontend && npx vitest run src/features/admin/components/SchemaFieldEditor — all suites green (existing + new RowPositionSelector + FieldCard if present).
- grep -n "RowPositionSelector" frontend/src/features/admin/components/SchemaFieldEditor/FieldCard.tsx returns at least 2 matches (import + JSX).
- grep -c "WIDE_TYPES" frontend/src/features/admin/components/SchemaFieldEditor/SchemaFieldEditor.tsx returns at least 2 (import + guard).
- vite build (cd frontend && npm run build) succeeds.
- No regressions in existing builder smoke tests.
</verification>

<success_criteria>
1. RowPositionSelector exists, exports default + RowPositionSelectorProps interface; 8 vitest cases all pass.
2. FieldCard imports RowPositionSelector and shows: (a) colored left border for non-wide grouped fields, (b) {N}행 pill in collapsed header for non-wide grouped fields, (c) "한 줄 차지" amber badge for wide-type fields, (d) RowPositionSelector block in expanded panel ONLY for non-wide types.
3. SchemaFieldEditor exposes rowOccupancy via useMemo, passes it to all FieldCard children, and updateField clears rowGroup + fires toast when type changes to wide.
4. Per UI-SPEC §"Visual Mode Matrix" — every cell in that matrix is reachable through the implementation.
5. Wave 1 constants are USED via static array lookup (no Tailwind class interpolation anywhere in the diff).
6. tsc --noEmit clean; vite build succeeds; vitest suite green.
7. Zero changes to renderer files (FormPreview / DynamicCustomForm / DynamicCustomReadOnly belong to Wave 3).
</success_criteria>

<output>
After completion, create `.planning/phases/36-form-row-layout/36-02-SUMMARY.md` documenting:
- Files modified/created (count + paths)
- Test results (n/n passing)
- Builder UX walk-through (admin opens FieldCard → toggles row position → preview updates next wave)
- Decisions reaffirmed: D-A1, D-A2, D-C1, D-C2, D-C4, D-F2, D-F3
- Hand-off notes for Wave 3 (renderer files): rowGroup data is now persisted in admin schema state; renderer must consume it via groupFieldsByRow.
</output>
