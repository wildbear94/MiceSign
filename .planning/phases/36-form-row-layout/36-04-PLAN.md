---
phase: 36-form-row-layout
plan: 04
type: execute
wave: 4
depends_on: ["36-01", "36-02", "36-03"]
files_modified:
  - frontend/public/locales/ko/admin.json
  - frontend/public/locales/ko/document.json
  - .planning/phases/36-form-row-layout/36-HUMAN-UAT.md
autonomous: false
requirements:
  - PHASE-36-DA1
  - PHASE-36-DA2
  - PHASE-36-DA3
  - PHASE-36-DB1
  - PHASE-36-DB2
  - PHASE-36-DB3
  - PHASE-36-DB4
  - PHASE-36-DC1
  - PHASE-36-DC2
  - PHASE-36-DC3
  - PHASE-36-DC4
  - PHASE-36-DD1
  - PHASE-36-DD2
  - PHASE-36-DD4
  - PHASE-36-DE1
  - PHASE-36-DE2
  - PHASE-36-DE3
  - PHASE-36-DF1
  - PHASE-36-DF3
  - PHASE-36-DG1
  - PHASE-36-DG2
  - PHASE-36-DG3
  - PHASE-36-DG4
  - PHASE-36-DG5
must_haves:
  truths:
    - "All 13 templates.rowLayout.* keys present in ko/admin.json with correct Korean copy per UI-SPEC §i18n Key Contract"
    - "ko/document.json has rowLayout.rowGroupAriaLabel mirrored key for renderer ARIA labels"
    - "All builder UI strings (selector buttons, badges, helper hint, toasts, tooltip, Zod errors) display Korean copy at runtime"
    - "Full vitest suite green; tsc --noEmit clean; vite build succeeds"
    - "HUMAN-UAT sign-off captures all 7 visual gates: builder pill selector / md+ grid / sm 1-col fallback / legacy doc zero-shift / wide-type guard / hard-cap-3 flash / FormPreview live reflection"
  artifacts:
    - path: "frontend/public/locales/ko/admin.json"
      provides: "13 new keys under templates.rowLayout"
      contains: "rowLayout"
    - path: "frontend/public/locales/ko/document.json"
      provides: "rowGroupAriaLabel mirrored key for renderer ARIA"
      contains: "rowLayout"
    - path: ".planning/phases/36-form-row-layout/36-HUMAN-UAT.md"
      provides: "Manual UAT checklist with 7 visual gates and sign-off block"
      contains: "HUMAN-UAT"
  key_links:
    - from: "ko/admin.json templates.rowLayout"
      to: "RowPositionSelector + FieldCard + Zod refinements"
      via: "i18n key consumption"
      pattern: "templates\\.rowLayout"
    - from: "ko/document.json rowLayout.rowGroupAriaLabel"
      to: "DynamicCustomForm + DynamicCustomReadOnly aria-label"
      via: "useTranslation('document')"
      pattern: "rowGroupAriaLabel"
---

<objective>
Close Phase 36 with three actions:

1. Add the 13 Korean i18n keys under `templates.rowLayout.*` to `frontend/public/locales/ko/admin.json` per UI-SPEC §"i18n Key Contract" lines 510~525. English translations are out of scope (per Phase 32-04 / Phase 34 D-E2 Korean-only precedent).

2. Add the mirrored `rowLayout.rowGroupAriaLabel` key to `frontend/public/locales/ko/document.json` so DynamicCustomForm and DynamicCustomReadOnly can read the ARIA label from their own namespace (no cross-namespace gymnastics).

3. Run a full integration verification (vitest + tsc + vite build) and produce the HUMAN-UAT checklist as `.planning/phases/36-form-row-layout/36-HUMAN-UAT.md` — a 7-gate manual verification document, then pause for the user to execute and sign off.

This plan is non-autonomous because the third task is a `checkpoint:human-verify` — Claude cannot click through the builder/renderer UI; the user must.

Output:
- 1 new i18n catalog entry (13 keys) under existing `templates` parent in ko/admin.json.
- 1 new ARIA key mirrored into ko/document.json.
- HUMAN-UAT.md created with 7 numbered gates.
- HUMAN-UAT signed off by user before plan completes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36-form-row-layout/36-CONTEXT.md
@.planning/phases/36-form-row-layout/36-UI-SPEC.md
@.planning/phases/36-form-row-layout/36-PATTERNS.md
@.planning/phases/36-form-row-layout/36-01-SUMMARY.md
@.planning/phases/36-form-row-layout/36-02-SUMMARY.md
@.planning/phases/36-form-row-layout/36-03-SUMMARY.md

@frontend/public/locales/ko/admin.json
@frontend/public/locales/ko/document.json

<interfaces>
i18n key list — exact Korean copy per UI-SPEC §"i18n Key Contract" lines 510~525:

| Key | Korean copy |
|-----|-------------|
| templates.rowLayout.sectionLabel | `행 위치` |
| templates.rowLayout.singleButton | `단독` |
| templates.rowLayout.rowButton | `행 {{number}}` |
| templates.rowLayout.newRowButton | `+ 새 행` |
| templates.rowLayout.helperHint | `같은 '행' 의 필드들이 한 줄에 가로 배치됩니다 (모바일에서는 한 줄로 표시됩니다)` |
| templates.rowLayout.rowFullTooltip | `이 행은 이미 3개 필드가 가득 찼습니다` |
| templates.rowLayout.rowFullToast | `한 줄에는 최대 3개 필드까지 배치할 수 있습니다` |
| templates.rowLayout.wideTypeAutoSingleToast | `이 필드는 한 줄을 차지하는 필드라 행 그룹에서 빠집니다` |
| templates.rowLayout.wideTypeBadge | `한 줄 차지` |
| templates.rowLayout.rowGroupBadge | `{{number}}행` |
| templates.rowLayout.rowGroupAriaLabel | `{{number}}행 그룹` |
| templates.rowLayout.zodWideTypeError | `긴 텍스트와 표 필드는 행 그룹에 속할 수 없습니다` |
| templates.rowLayout.zodCapExceededError | `한 줄에는 최대 3개 필드까지만 배치할 수 있습니다` |

Mirrored into ko/document.json: rowLayout.rowGroupAriaLabel = `{{number}}행 그룹` (same Korean copy).

ko/admin.json currently has nested structure under `templates`:
- templates.condition.* (L230~263) — analog precedent for nested sub-tree
- templates.calculation.* (L264~) — analog precedent for nested sub-tree

Add `templates.rowLayout` as a NEW peer sub-tree.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 13 i18n keys to ko/admin.json + mirrored ARIA key to ko/document.json</name>
  <files>frontend/public/locales/ko/admin.json, frontend/public/locales/ko/document.json</files>
  <action>
1. Open `frontend/public/locales/ko/admin.json`. Find the existing `"templates": { ... }` parent object (contains nested `condition` and `calculation` siblings per PATTERNS §"i18n catalog"). Add a NEW peer key `"rowLayout"` containing the 13 keys per the table in <interfaces>. Place it after `"calculation"` (alphabetical or insertion order is fine — JSON object key order is preserved by all major parsers, including the project's existing pattern).

The exact JSON insert (verbatim Korean copy from UI-SPEC §"i18n Key Contract"):

```json
"rowLayout": {
  "sectionLabel": "행 위치",
  "singleButton": "단독",
  "rowButton": "행 {{number}}",
  "newRowButton": "+ 새 행",
  "helperHint": "같은 '행' 의 필드들이 한 줄에 가로 배치됩니다 (모바일에서는 한 줄로 표시됩니다)",
  "rowFullTooltip": "이 행은 이미 3개 필드가 가득 찼습니다",
  "rowFullToast": "한 줄에는 최대 3개 필드까지 배치할 수 있습니다",
  "wideTypeAutoSingleToast": "이 필드는 한 줄을 차지하는 필드라 행 그룹에서 빠집니다",
  "wideTypeBadge": "한 줄 차지",
  "rowGroupBadge": "{{number}}행",
  "rowGroupAriaLabel": "{{number}}행 그룹",
  "zodWideTypeError": "긴 텍스트와 표 필드는 행 그룹에 속할 수 없습니다",
  "zodCapExceededError": "한 줄에는 최대 3개 필드까지만 배치할 수 있습니다"
}
```

CRITICAL: preserve existing comma placement — the previous sibling (`calculation` block) needs a trailing comma before this new `rowLayout` key, and `rowLayout` needs a trailing comma if there are more sibling keys after it (typically there are not within `templates`). Validate JSON parses with `node -e "JSON.parse(require('fs').readFileSync('frontend/public/locales/ko/admin.json'))"` after edit.

2. Open `frontend/public/locales/ko/document.json`. Add a top-level `"rowLayout"` sub-tree containing ONE key:

```json
"rowLayout": {
  "rowGroupAriaLabel": "{{number}}행 그룹"
}
```

Place it as a new top-level key (or nested wherever the document feature's existing keys live — inspect the file structure first; if there's a top-level wrapper like `"document"`, place it inside; otherwise top-level). The renderer code in Wave 3 used `t('rowLayout.rowGroupAriaLabel', ...)` with `useTranslation('document')` — match that path exactly.

CRITICAL: only ONE key (rowGroupAriaLabel) is mirrored. The other 12 keys remain ONLY in admin.json — they're consumed by builder UI which uses `useTranslation('admin')`.

3. After both edits, validate JSON parsing for both files:

```bash
cd frontend && node -e "JSON.parse(require('fs').readFileSync('public/locales/ko/admin.json'));console.log('admin.json OK');" && node -e "JSON.parse(require('fs').readFileSync('public/locales/ko/document.json'));console.log('document.json OK');"
```

4. NO en/admin.json or en/document.json modifications (Phase 32-04 / Phase 34 D-E2 Korean-only precedent).

Per D-A2 (helper hint pre-empts mobile-fallback confusion) + D-E1 (mobile 1-col rule must be visible to admin via helper hint).
  </action>
  <verify>
    <automated>cd frontend && node -e "const a=JSON.parse(require('fs').readFileSync('public/locales/ko/admin.json'));const d=JSON.parse(require('fs').readFileSync('public/locales/ko/document.json'));console.log('admin keys:',Object.keys(a.templates.rowLayout||{}).length,'doc keys:',Object.keys(d.rowLayout||d.document?.rowLayout||{}).length);" && grep -c "rowLayout" frontend/public/locales/ko/admin.json && grep -c "rowLayout" frontend/public/locales/ko/document.json</automated>
  </verify>
  <done>
- ko/admin.json parses successfully; templates.rowLayout has exactly 13 keys
- ko/document.json parses successfully; rowLayout.rowGroupAriaLabel exists
- Korean copy matches UI-SPEC table verbatim (no English mixed in, no encoding issues)
- en/* files NOT modified
  </done>
</task>

<task type="auto">
  <name>Task 2: Full integration verification (tsc + vitest + vite build)</name>
  <files>(verification only — no file modifications expected)</files>
  <action>
Run the complete frontend verification chain:

1. tsc --noEmit:
   ```bash
   cd frontend && npx tsc --noEmit 2>&1 | tail -20
   ```
   Expect: 0 errors. Any error → STOP and revise the failing wave's plan.

2. Full vitest suite:
   ```bash
   cd frontend && npx vitest run 2>&1 | tail -40
   ```
   Expect: all tests pass. New tests from this phase: groupFieldsByRow (8 cases), templateImportSchema rowGroup (6 cases), RowPositionSelector (8 cases). Existing tests untouched. If any existing test fails:
   - SchemaFieldEditor / FieldCard tests may need `rowOccupancy={{}}` or `rowGroup={undefined}` fixture additions due to new required prop on FieldCard.
   - DynamicCustomForm / DynamicCustomReadOnly tests with hardcoded DOM assertions may need updates to expect the new `<div role="group">` wrappers in multi-field-group cases. For legacy fixtures (no rowGroup), assertions should still pass — backward compat invariant.
   - Fix the test fixtures to match the new prop signatures + new DOM structure for multi-field-group cases. Document any non-trivial fixture updates in the SUMMARY.

3. vite build:
   ```bash
   cd frontend && npm run build 2>&1 | tail -30
   ```
   Expect: build succeeds. CRITICAL CHECK: Tailwind class detection works for the dynamic-class enumeration arrays (ROW_GROUP_BORDER_CLASSES, ROW_GROUP_PILL_CLASSES, GRID_COLS_CLASS). If any of `border-l-blue-400`, `border-l-emerald-400`, `border-l-violet-400`, `border-l-amber-400`, `bg-blue-100`, `bg-emerald-100`, `bg-violet-100`, `bg-amber-100`, `md:grid-cols-1`, `md:grid-cols-2`, `md:grid-cols-3` are missing from the built CSS, the literal-string-array rule was violated somewhere — find the culprit (Tailwind silently strips undetected class fragments; visual breakage will only manifest in production). Verify by:

   ```bash
   cd frontend && grep -c "md:grid-cols-2\|md:grid-cols-3\|border-l-blue-400\|border-l-emerald-400" dist/assets/*.css 2>/dev/null
   ```

   Each token should appear at least once in the built CSS.

4. Backend smoke (no backend changes expected): no action needed — Phase 36 is frontend-only per CONTEXT scope-out.

If any step fails: HALT, document the failure in this plan's SUMMARY, revise the failing wave's plan via /gsd-plan-phase 36 in revision mode.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -5 && npx vitest run 2>&1 | tail -10 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
- tsc --noEmit: 0 errors
- vitest run: all tests pass (existing + 22 new across Wave 1/2: 8 groupFieldsByRow + 6 Zod refinement + 8 RowPositionSelector)
- vite build: succeeds
- Built CSS contains all 13 dynamic-class tokens (4 border colors + 4 pill bg + 3 md:grid-cols + 4 dark:* color variants)
  </done>
</task>

<task type="auto">
  <name>Task 3: Author HUMAN-UAT.md checklist with 7 visual gates</name>
  <files>.planning/phases/36-form-row-layout/36-HUMAN-UAT.md</files>
  <action>
Create `.planning/phases/36-form-row-layout/36-HUMAN-UAT.md` with this structure:

```markdown
# Phase 36 — HUMAN UAT Checklist

**Created:** 2026-04-30
**Decision basis:** D-G5 (manual UAT) per CONTEXT.md
**Sign-off required from:** the user (project owner)

## Setup

1. Stop any running dev servers.
2. From repo root: `cd frontend && npm run dev`. Note the Vite URL (typically http://localhost:5173).
3. Login as a SUPER_ADMIN or ADMIN user (template builder access required).
4. Navigate to the template list (관리자 → 양식 관리).

## Gate 1 — Builder pill selector renders + interacts (D-A1, D-A2, D-F2)

Steps:
1. Click "새 양식" (or open any existing CUSTOM template).
2. In the SchemaFieldEditor (left pane), click "필드 추가" → select "Text" type.
3. Expand the new FieldCard (chevron or click).
4. Locate the new "행 위치" section (between "필수 입력" checkbox and the type-specific config block).

Expected:
- [ ] "행 위치" label visible above the selector.
- [ ] Pill buttons present: `[단독] [+ 새 행]` initially. The `[행 N]` buttons appear only after clicking `[+ 새 행]`.
- [ ] `[단독]` button is selected (blue fill, white text).
- [ ] Helper hint paragraph below selector reads: `같은 '행' 의 필드들이 한 줄에 가로 배치됩니다 (모바일에서는 한 줄로 표시됩니다)`.
- [ ] All buttons render with the Pretendard font.

## Gate 2 — Hard-cap-3 visual disable + flash + toast (D-F3)

Steps:
1. Add 3 fields (e.g., text, number, date) — all set to "행 1" via the selector.
2. Add a 4th text field. In its FieldCard, expand and click "행 1".

Expected:
- [ ] `[행 1]` button is rendered in the **disabled style** (gray bg, gray text, cursor-not-allowed).
- [ ] Hovering over `[행 1]` shows tooltip `이 행은 이미 3개 필드가 가득 찼습니다`.
- [ ] Clicking `[행 1]` does NOT change the field's row position (still "단독").
- [ ] (Optional, hard to observe with disabled HTML attr in browsers) A red flash + toast `한 줄에는 최대 3개 필드까지 배치할 수 있습니다` may appear; if not, the disabled-attribute browser suppression is working as intended.

## Gate 3 — md+ grid layout in user-facing renderer (D-B1, D-E1, D-E2)

Steps:
1. Save the template (with 3 fields in row 1 from Gate 2).
2. Navigate to "기안 작성" → select your CUSTOM template.
3. Open the editor on a desktop viewport (>=768px wide).

Expected:
- [ ] The 3 fields render side-by-side in a single horizontal row.
- [ ] The grid has `gap-4` (16px) horizontal spacing between cells.
- [ ] Field labels and inputs align top-to-bottom within each cell.
- [ ] No visual break or overflow if a select option is long (cells use `min-w-0` to prevent grid blowout).

## Gate 4 — sm 1-col fallback (D-E1)

Steps:
1. With the same draft open, narrow the browser window below 768px (or use DevTools mobile emulation).

Expected:
- [ ] All 3 fields render in a single vertical column (1-col stack).
- [ ] Inter-field spacing matches the existing `space-y-4` (16px vertical).
- [ ] No horizontal scrolling, no clipped inputs.

## Gate 5 — Wide-type force-single guard (D-C1, D-C2, D-C4)

Steps:
1. In a fresh template draft, add a text field, set it to row 1.
2. In its FieldCard, change the type from "Text" to "Textarea" via the type dropdown.

Expected:
- [ ] A sonner toast appears with `이 필드는 한 줄을 차지하는 필드라 행 그룹에서 빠집니다`.
- [ ] The collapsed FieldCard header NO LONGER shows the `1행` pill.
- [ ] Instead, an amber `한 줄 차지` badge appears in the collapsed header next to the field type badge.
- [ ] The expanded FieldCard NO LONGER shows the "행 위치" selector section.
- [ ] Repeat with type "Table" — same behavior.

## Gate 6 — Backward compat / legacy SUBMITTED docs (D-D1, D-D4)

Steps:
1. Locate or submit a legacy CUSTOM document that pre-dates Phase 36 (any CUSTOM template submitted before this phase shipped).
2. Open the document detail (read-only) view.

Expected:
- [ ] Layout is **identical** to before Phase 36 — vertical stack with `space-y-4` spacing.
- [ ] No `<div role="group">` wrapper visible in DevTools inspector.
- [ ] Zero layout shift compared to a screenshot taken before Phase 36 deployed.
- [ ] Field rendering, conditional-rule visibility, and calculation values all behave as they did pre-Phase-36.

## Gate 7 — FormPreview live reflection (D-A3)

Steps:
1. Open a CUSTOM template in edit mode in the admin builder.
2. Observe the FormPreview pane (right side).
3. Add 2 text fields and assign both to "행 1" via their selectors.

Expected:
- [ ] FormPreview INSTANTLY reflects the row group — both fields appear side-by-side at md+ viewport.
- [ ] No save / refresh required.
- [ ] Changing one field's row position back to "단독" instantly returns it to its own row in the preview.
- [ ] FormPreview at narrow widths (<768px container width — possible if the modal split is narrow) shows 1-col fallback identically to the user-facing renderer.

## Sign-off

Tester: ____________________
Date: 2026-__-__
Decision (circle one): **PASS** / **FAIL** (specify which gate(s))

If FAIL, file gap-closure phase via `/gsd-plan-phase 36 --gaps`.

If PASS:
- Record sign-off in this file's footer.
- Run `/gsd-execute-phase 36` to commit the closing wave (this Plan 04).
- Phase 36 SUMMARY will be drafted; update STATE.md + ROADMAP.md to mark Phase 36 complete.
```

Per D-G5 (manual UAT) + UI-SPEC §"Visual Mode Matrix" (the 7 gates collectively cover all rows of that matrix).
  </action>
  <verify>
    <automated>test -f .planning/phases/36-form-row-layout/36-HUMAN-UAT.md && grep -c "Gate [1-7]" .planning/phases/36-form-row-layout/36-HUMAN-UAT.md && grep -c "Sign-off" .planning/phases/36-form-row-layout/36-HUMAN-UAT.md</automated>
  </verify>
  <done>
- File exists with 7 numbered gates
- Each gate has Steps + Expected checklist + decision-related subjects (D-A1~G5 references in headings)
- Sign-off block at end with decision PASS/FAIL choice
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 (checkpoint): Execute HUMAN-UAT 7 gates and sign off PASS/FAIL</name>
  <files>.planning/phases/36-form-row-layout/36-HUMAN-UAT.md</files>
  <what-built>
- 13 Korean i18n keys under `templates.rowLayout.*` in ko/admin.json
- 1 mirrored ARIA key in ko/document.json
- Full integration verification: tsc clean, vitest green, vite build OK
- HUMAN-UAT.md checklist with 7 visual gates covering D-A1, D-A2, D-A3, D-B1, D-C1, D-C2, D-C4, D-D1, D-E1, D-F3, D-G5
  </what-built>
  <action>
USER ACTION REQUIRED — Claude pauses here.

The user must execute the manual UAT script in `.planning/phases/36-form-row-layout/36-HUMAN-UAT.md` and sign off PASS or FAIL. Claude cannot click through the builder/renderer UI; this is a true human-only verification.

Procedure handed off to user:
1. Open `.planning/phases/36-form-row-layout/36-HUMAN-UAT.md`.
2. Follow the **Setup** section: start dev server, login as ADMIN.
3. Execute each of the 7 gates IN ORDER. Tick the [ ] checkboxes as you verify each expectation.
4. If any gate fails, note which gate + which expectation. Resume with `failed: gate N — <reason>`.
5. If all 7 gates pass, fill the Sign-off block (name + today's date + decision: PASS).

Estimated time: 15-20 minutes.

Tools required:
- Browser DevTools (viewport resize at Gate 4, DOM inspection at Gate 6)
- Access to a legacy CUSTOM document (any CUSTOM doc submitted before this phase)
  </action>
  <how-to-verify>
1. Open `.planning/phases/36-form-row-layout/36-HUMAN-UAT.md`.
2. Follow the Setup section to start the dev server and login as ADMIN.
3. Execute each of the 7 gates IN ORDER. Tick the [ ] checkboxes as you verify each expectation.
4. If any gate fails, note which gate + which expectation, then resume with `failed: gate N — <reason>`.
5. If all 7 gates pass, fill in the Sign-off block (your name + today's date + decision: PASS).

Estimated time: 15-20 minutes for a full pass through all 7 gates including legacy doc verification.

Tools you'll need:
- Browser DevTools (for viewport resize at Gate 4 and DOM inspection at Gate 6)
- Access to a legacy CUSTOM document (any CUSTOM doc submitted before this phase)
  </how-to-verify>
  <verify>
    <automated>grep -c "PASS\|FAIL" .planning/phases/36-form-row-layout/36-HUMAN-UAT.md</automated>
  </verify>
  <done>
- All 7 gates ticked PASS in 36-HUMAN-UAT.md
- Sign-off block filled with tester name + date + PASS decision
- If FAIL, route to /gsd-plan-phase 36 --gaps with failed gate notes
  </done>
  <resume-signal>Type "approved" once all 7 gates pass and Sign-off is filled in. If any gate failed, describe the failure and which gate; we'll route to /gsd-plan-phase 36 --gaps.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n catalog → DOM text | Korean strings render into UI; no untrusted input crosses this boundary in this plan |
| HUMAN-UAT checklist → trust | User attestation crosses into project record; trust model is the user themselves |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-16 | T (Tampering) | i18n catalog JSON | mitigate | JSON validity check (`node -e "JSON.parse(...)"`) catches malformed insertions before they reach the runtime. i18n keys are static strings; no script execution surface. |
| T-36-17 | I (Information Disclosure) | i18n strings | accept | Korean copy is generic UI labels; no PII, no secrets, no internal pathnames. Same risk profile as existing 200+ keys in admin.json. |
| T-36-18 | E (Elevation of Privilege) | HUMAN-UAT sign-off | accept | Sign-off is informational; does not grant runtime privileges. The user signs as project owner per existing GSD convention. |
| T-36-19 | T (Tampering) | Runtime Tailwind class detection bypass | mitigate | Task 2 verifies built CSS contains all 13 dynamic-class tokens via grep. If any are missing, surface immediately as a build-time guard rather than discovering visual regression in production. |
</threat_model>

<verification>
- ko/admin.json parses; templates.rowLayout has exactly 13 keys.
- ko/document.json parses; rowLayout.rowGroupAriaLabel exists at the path used by renderer.
- en/* locale files NOT modified.
- Full tsc + vitest + vite build chain succeeds.
- Built CSS contains: md:grid-cols-{1,2,3}; border-l-{blue,emerald,violet,amber}-400 + dark:* variants; bg-{blue,emerald,violet,amber}-100 + dark:* variants.
- HUMAN-UAT.md exists with 7 gates + Sign-off block.
- HUMAN-UAT signed off PASS by user before plan completes (checkpoint:human-verify resume).
</verification>

<success_criteria>
1. 13 Korean i18n keys present and parsing-valid in ko/admin.json under `templates.rowLayout`.
2. 1 mirrored ARIA key present in ko/document.json (consumed by document-namespace renderer).
3. Full integration chain green: tsc clean, vitest all pass (8 groupFieldsByRow + 6 Zod + 8 RowPositionSelector + existing suite), vite build OK, dynamic class tokens present in built CSS.
4. HUMAN-UAT.md created with 7 numbered gates that collectively cover the UI-SPEC §"Visual Mode Matrix" rows.
5. User executes all 7 HUMAN-UAT gates and signs off PASS — or files specific failures for gap-closure routing.
6. en/admin.json and en/document.json NOT modified (Phase 32-04 / Phase 34 D-E2 Korean-only precedent).
</success_criteria>

<output>
After completion (HUMAN-UAT signed off PASS), create `.planning/phases/36-form-row-layout/36-04-SUMMARY.md` documenting:
- Files modified (3 paths: 2 i18n + 1 UAT doc)
- Final verification results (tsc / vitest counts / build status / built-CSS token grep)
- HUMAN-UAT outcome (gates PASS/FAIL + sign-off date + tester name)
- Phase 36 closing notes: 14 files total impact (matches scope_summary), all D-A1~D-G5 decisions implemented or attested, no scope deviation
- ROADMAP.md should be updated to mark Phase 36 [x] complete with date
- STATE.md should record Phase 36 closing alongside the v1.2 milestone progress

If any HUMAN-UAT gate FAILED, route to `/gsd-plan-phase 36 --gaps` with the failed gate notes; do NOT mark phase complete.
</output>
