---
phase: 08-dashboard-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/micesign/service/UserManagementService.java
  - backend/src/main/java/com/micesign/service/PasswordService.java
  - backend/src/main/java/com/micesign/service/DepartmentService.java
  - backend/src/main/java/com/micesign/service/PositionService.java
  - backend/src/main/java/com/micesign/service/TemplateService.java
  - backend/src/main/java/com/micesign/controller/AdminTemplateController.java
  - backend/src/main/java/com/micesign/controller/DepartmentController.java
  - backend/src/main/java/com/micesign/controller/PositionController.java
  - backend/src/main/java/com/micesign/controller/PasswordController.java
  - backend/src/test/java/com/micesign/admin/AuditLogGapTest.java
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - AUD-01

must_haves:
  truths:
    - "Dashboard shows pending approval documents for the logged-in user"
    - "Dashboard shows recent documents with current status"
    - "Dashboard shows badge counts for pending approvals, in-progress drafts, and completed documents"
    - "Admin user/department/position/template operations produce audit log entries"
    - "Admin password reset produces an audit log entry"
  artifacts:
    - path: "backend/src/main/java/com/micesign/service/UserManagementService.java"
      provides: "Audit logging for createUser, updateUser, deactivateUser"
      contains: "auditLogService.log"
    - path: "backend/src/main/java/com/micesign/service/DepartmentService.java"
      provides: "Audit logging for department CRUD"
      contains: "auditLogService.log"
    - path: "backend/src/main/java/com/micesign/service/PositionService.java"
      provides: "Audit logging for position CRUD"
      contains: "auditLogService.log"
    - path: "backend/src/main/java/com/micesign/service/TemplateService.java"
      provides: "Audit logging for template CRUD"
      contains: "auditLogService.log"
    - path: "backend/src/main/java/com/micesign/service/PasswordService.java"
      provides: "Audit logging for admin password reset"
      contains: "auditLogService.log"
    - path: "backend/src/test/java/com/micesign/admin/AuditLogGapTest.java"
      provides: "Integration tests verifying audit log entries for admin operations"
      contains: "class AuditLogGapTest"
  key_links:
    - from: "UserManagementService.java"
      to: "AuditLogService.java"
      via: "constructor injection"
      pattern: "private final AuditLogService auditLogService"
    - from: "DepartmentService.java"
      to: "AuditLogService.java"
      via: "constructor injection"
      pattern: "private final AuditLogService auditLogService"
---

<objective>
Verify the existing dashboard satisfies DASH-01/02/03 requirements (no code changes expected per D-01/D-02/D-03), and fill all missing audit log calls in admin services to complete AUD-01 coverage.

Purpose: The dashboard is already implemented but needs formal verification against requirements. The audit log infrastructure exists but admin operations (user/dept/position/template management, password reset) lack auditLogService.log() calls.

Output: Verified dashboard, audit-complete admin services, integration tests proving audit coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dashboard-audit/08-CONTEXT.md
@.planning/phases/08-dashboard-audit/08-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend AuditLogService.java:
```java
public void log(Long userId, String action, String targetType, Long targetId, Object detail)
// Uses Propagation.REQUIRES_NEW — safe to call within existing transactions
```

From backend AuditAction.java:
```java
public static final String ADMIN_USER_EDIT = "ADMIN_USER_EDIT";
public static final String ADMIN_ORG_EDIT = "ADMIN_ORG_EDIT";
```

From backend DocumentService.java (pattern to follow):
```java
auditLogService.log(userId, AuditAction.DOC_CREATE, "DOCUMENT", document.getId(), detail);
// detail is typically Map.of("action", "create", "field", "value")
```

From frontend DashboardPage.tsx:
- CountCard: 3 cards (pending, drafts, completed) with counts from API
- PendingList: top 5 pending approval documents
- RecentDocumentsList: top 5 recent documents with status
- 60-second auto-refresh via refetchInterval
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify dashboard satisfies DASH-01/02/03</name>
  <files>
    frontend/src/features/dashboard/pages/DashboardPage.tsx
    frontend/src/features/dashboard/components/CountCard.tsx
    frontend/src/features/dashboard/components/PendingList.tsx
    frontend/src/features/dashboard/components/RecentDocumentsList.tsx
    frontend/src/features/dashboard/hooks/useDashboard.ts
    frontend/src/features/dashboard/api/dashboardApi.ts
    backend/src/main/java/com/micesign/controller/DashboardController.java
    backend/src/main/java/com/micesign/service/DashboardService.java
  </files>
  <read_first>
    frontend/src/features/dashboard/pages/DashboardPage.tsx
    frontend/src/features/dashboard/components/CountCard.tsx
    frontend/src/features/dashboard/components/PendingList.tsx
    frontend/src/features/dashboard/components/RecentDocumentsList.tsx
    frontend/src/features/dashboard/hooks/useDashboard.ts
    frontend/src/features/dashboard/api/dashboardApi.ts
    backend/src/main/java/com/micesign/controller/DashboardController.java
    backend/src/main/java/com/micesign/service/DashboardService.java
  </read_first>
  <action>
Read and verify (do NOT modify unless a clear bug is found) that:

1. **DASH-01 (Pending approvals list):** DashboardPage renders PendingList component. PendingList fetches pending approval documents for the current user. Each item shows document title, drafter name, and submission date. Items link to document detail page.

2. **DASH-02 (Recent documents):** DashboardPage renders RecentDocumentsList component. RecentDocumentsList fetches the user's recent documents. Each item shows document title, status badge, and date. Items link to document detail page.

3. **DASH-03 (Badge counts):** DashboardPage renders 3 CountCard components showing:
   - Pending approvals count (결재 대기)
   - In-progress drafts count (진행 중 초안)
   - Completed documents count (완료 문서)
   Counts come from the dashboard summary API endpoint.

4. **Auto-refresh:** useDashboard hook uses `refetchInterval: 60000` (60-second polling per D-03).

5. **List limits:** Both PendingList and RecentDocumentsList show a maximum of 5 items per D-03.

If all checks pass, report verification results in the summary. No code changes needed per D-01/D-02/D-03.

If a bug is found (e.g., wrong API path, missing status badge, broken link), fix it minimally with a comment explaining the fix.
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - DashboardPage.tsx contains `PendingList` component reference
    - DashboardPage.tsx contains `RecentDocumentsList` component reference
    - DashboardPage.tsx contains 3 `CountCard` component references
    - useDashboard.ts contains `refetchInterval` with value 60000
    - DashboardController.java has endpoints returning pending list and summary counts
    - Verification report documents each DASH requirement as satisfied or identifies specific bugs fixed
  </acceptance_criteria>
  <done>All three DASH requirements verified against existing implementation. No unnecessary changes made per D-01/D-02/D-03.</done>
</task>

<task type="auto">
  <name>Task 2: Add missing audit log calls to admin services</name>
  <files>
    backend/src/main/java/com/micesign/service/UserManagementService.java
    backend/src/main/java/com/micesign/service/PasswordService.java
    backend/src/main/java/com/micesign/service/DepartmentService.java
    backend/src/main/java/com/micesign/service/PositionService.java
    backend/src/main/java/com/micesign/service/TemplateService.java
    backend/src/main/java/com/micesign/controller/AdminTemplateController.java
    backend/src/main/java/com/micesign/controller/DepartmentController.java
    backend/src/main/java/com/micesign/controller/PositionController.java
    backend/src/main/java/com/micesign/controller/PasswordController.java
    backend/src/test/java/com/micesign/admin/AuditLogGapTest.java
  </files>
  <read_first>
    backend/src/main/java/com/micesign/service/UserManagementService.java
    backend/src/main/java/com/micesign/service/PasswordService.java
    backend/src/main/java/com/micesign/service/DepartmentService.java
    backend/src/main/java/com/micesign/service/PositionService.java
    backend/src/main/java/com/micesign/service/TemplateService.java
    backend/src/main/java/com/micesign/service/AuditLogService.java
    backend/src/main/java/com/micesign/service/DocumentService.java
    backend/src/main/java/com/micesign/controller/AdminTemplateController.java
    backend/src/main/java/com/micesign/controller/DepartmentController.java
    backend/src/main/java/com/micesign/controller/PositionController.java
    backend/src/main/java/com/micesign/controller/PasswordController.java
    backend/src/main/java/com/micesign/common/AuditAction.java
  </read_first>
  <action>
Add audit logging to all admin service mutation methods. Per D-04 and D-05, only supplement missing calls -- do not modify existing logging.

**Challenge: userId propagation.** DepartmentService, PositionService, and TemplateService mutation methods don't currently accept userId. Two options from RESEARCH: (a) add userId param to service methods, (b) log at controller level. Use option (a) -- add userId parameter to service mutation methods for consistency with the DocumentService pattern.

**UserManagementService** -- already receives userId context via method params or can derive from CustomUserDetails:
1. Inject `AuditLogService` via constructor (add field + constructor param)
2. In `createUser()`: after successful save, call `auditLogService.log(adminUserId, AuditAction.ADMIN_USER_EDIT, "USER", savedUser.getId(), Map.of("action", "create", "email", savedUser.getEmail(), "name", savedUser.getName()))`
3. In `updateUser()`: after successful update, call `auditLogService.log(adminUserId, AuditAction.ADMIN_USER_EDIT, "USER", user.getId(), Map.of("action", "update", "email", user.getEmail()))`
4. In `deactivateUser()`: after status change, call `auditLogService.log(adminUserId, AuditAction.ADMIN_USER_EDIT, "USER", user.getId(), Map.of("action", "deactivate", "email", user.getEmail()))`
   - If these methods don't take adminUserId, add a `Long actingUserId` parameter and update the corresponding controller calls to pass `user.getUserId()` from `@AuthenticationPrincipal CustomUserDetails`

**PasswordService:**
1. Inject `AuditLogService` via constructor
2. In `adminResetPassword()`: after successful reset, call `auditLogService.log(adminUserId, AuditAction.ADMIN_USER_EDIT, "USER", targetUser.getId(), Map.of("action", "adminPasswordReset", "targetEmail", targetUser.getEmail()))`
   - If adminUserId is not a param, add it and update PasswordController accordingly

**DepartmentService:**
1. Inject `AuditLogService` via constructor
2. Add `Long actingUserId` parameter to `createDepartment()`, `updateDepartment()`, `deactivateDepartment()` methods
3. In `createDepartment()`: `auditLogService.log(actingUserId, AuditAction.ADMIN_ORG_EDIT, "DEPARTMENT", dept.getId(), Map.of("action", "create", "name", dept.getName()))`
4. In `updateDepartment()`: `auditLogService.log(actingUserId, AuditAction.ADMIN_ORG_EDIT, "DEPARTMENT", dept.getId(), Map.of("action", "update", "name", dept.getName()))`
5. In `deactivateDepartment()`: `auditLogService.log(actingUserId, AuditAction.ADMIN_ORG_EDIT, "DEPARTMENT", dept.getId(), Map.of("action", "deactivate", "name", dept.getName()))`
6. Update `DepartmentController` to pass `user.getUserId()` from `@AuthenticationPrincipal CustomUserDetails` to each modified service method

**PositionService:**
1. Inject `AuditLogService` via constructor
2. Add `Long actingUserId` parameter to `createPosition()`, `updatePosition()`, `deactivatePosition()` methods
3. In `createPosition()`: `auditLogService.log(actingUserId, AuditAction.ADMIN_ORG_EDIT, "POSITION", position.getId(), Map.of("action", "create", "name", position.getName()))`
4. In `updatePosition()`: `auditLogService.log(actingUserId, AuditAction.ADMIN_ORG_EDIT, "POSITION", position.getId(), Map.of("action", "update", "name", position.getName()))`
5. In `deactivatePosition()`: `auditLogService.log(actingUserId, AuditAction.ADMIN_ORG_EDIT, "POSITION", position.getId(), Map.of("action", "deactivate", "name", position.getName()))`
6. Update `PositionController` to pass `user.getUserId()` from `@AuthenticationPrincipal CustomUserDetails` to each modified service method

**TemplateService:**
1. Inject `AuditLogService` via constructor
2. Add `Long actingUserId` parameter to `createTemplate()`, `updateTemplate()`, `deactivateTemplate()` methods
3. In `createTemplate()`: `auditLogService.log(actingUserId, AuditAction.ADMIN_ORG_EDIT, "TEMPLATE", template.getId(), Map.of("action", "create", "code", template.getCode()))`
4. In `updateTemplate()`: `auditLogService.log(actingUserId, AuditAction.ADMIN_ORG_EDIT, "TEMPLATE", template.getId(), Map.of("action", "update", "code", template.getCode()))`
5. In `deactivateTemplate()`: `auditLogService.log(actingUserId, AuditAction.ADMIN_ORG_EDIT, "TEMPLATE", template.getId(), Map.of("action", "deactivate"))`
6. Update `AdminTemplateController` to pass `user.getUserId()` to each modified service method. Note: createTemplate already receives `@AuthenticationPrincipal CustomUserDetails user`; add it to updateTemplate and deactivateTemplate methods too.

**Integration Test (`AuditLogGapTest.java`):**
Create `backend/src/test/java/com/micesign/admin/AuditLogGapTest.java`:
- Use `@SpringBootTest` + `@ActiveProfiles("test")` + `@AutoConfigureMockMvc`
- Test that creating a department via `POST /api/v1/admin/departments` produces an audit log entry with action `ADMIN_ORG_EDIT` and targetType `DEPARTMENT`
- Test that updating a user via `PUT /api/v1/admin/users/{id}` produces an audit log entry with action `ADMIN_USER_EDIT`
- Use `JdbcTemplate` to query `audit_log` table directly after API calls
- Authenticate test requests using the existing `TestTokenHelper` pattern (generate JWT for SUPER_ADMIN user)
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/backend && ./gradlew test --tests "com.micesign.admin.AuditLogGapTest" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - UserManagementService.java contains `private final AuditLogService auditLogService`
    - UserManagementService.java contains `auditLogService.log` (at least 3 occurrences for create/update/deactivate)
    - PasswordService.java contains `auditLogService.log`
    - DepartmentService.java contains `private final AuditLogService auditLogService`
    - DepartmentService.java contains `auditLogService.log` (at least 3 occurrences)
    - PositionService.java contains `private final AuditLogService auditLogService`
    - PositionService.java contains `auditLogService.log` (at least 3 occurrences)
    - TemplateService.java contains `private final AuditLogService auditLogService`
    - TemplateService.java contains `auditLogService.log` (at least 3 occurrences)
    - AuditLogGapTest.java exists and contains `@SpringBootTest`
    - `./gradlew test --tests "com.micesign.admin.AuditLogGapTest"` exits with code 0
    - `./gradlew compileJava` exits with code 0 (no compilation errors from signature changes)
  </acceptance_criteria>
  <done>All 13 missing audit log calls added per RESEARCH gap analysis. Integration tests pass proving admin operations produce audit entries. AUD-01 fully satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin API endpoints | Only ADMIN/SUPER_ADMIN roles can trigger admin operations; audit logs capture who did what |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | R (Repudiation) | Admin services | mitigate | Add auditLogService.log() to all admin mutation methods with actingUserId, action detail, and target entity ID -- this is the primary deliverable of this plan |
| T-08-02 | T (Tampering) | Audit log entries | accept | AuditLogService uses REQUIRES_NEW propagation so audit entries persist even if the parent transaction rolls back. Audit log table is append-only (no UPDATE/DELETE operations in codebase) |
| T-08-03 | I (Info Disclosure) | Dashboard API | accept | Dashboard endpoints already filtered by authenticated user -- users only see their own pending/recent docs. No change needed. |
</threat_model>

<verification>
1. `cd backend && ./gradlew compileJava` -- all service changes compile cleanly
2. `cd backend && ./gradlew test` -- full test suite passes including new AuditLogGapTest
3. `grep -r "auditLogService.log" backend/src/main/java/com/micesign/service/` shows entries in UserManagementService, PasswordService, DepartmentService, PositionService, TemplateService
4. Dashboard verification report confirms DASH-01/02/03 satisfaction
</verification>

<success_criteria>
- Dashboard verified against DASH-01/02/03 without modifications (per D-01/D-02/D-03)
- All 13 missing audit log calls added to admin services (per D-04/D-05)
- Integration tests prove audit entries are created for admin operations
- Full backend test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard-audit/08-01-SUMMARY.md`
</output>
