---
phase: 01-project-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tsconfig.node.json
  - frontend/tailwind.config.js
  - frontend/postcss.config.js
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/App.css
  - frontend/src/index.css
  - frontend/src/api/client.ts
  - frontend/src/types/api.ts
autonomous: true
requirements:
  - ORG-05

must_haves:
  truths:
    - "React frontend loads in browser at http://localhost:5173"
    - "Vite dev server proxies /api requests to localhost:8080"
    - "TailwindCSS is configured with Pretendard font family"
    - "Axios client is configured with /api/v1 baseURL and response interceptor skeleton"
    - "Frontend builds without errors via npm run build"
  artifacts:
    - path: "frontend/package.json"
      provides: "All frontend dependencies pinned"
      contains: "react"
    - path: "frontend/vite.config.ts"
      provides: "Vite config with API proxy"
      contains: "/api"
    - path: "frontend/tailwind.config.js"
      provides: "Tailwind config with Pretendard font"
      contains: "Pretendard"
    - path: "frontend/src/api/client.ts"
      provides: "Axios instance with baseURL and interceptors"
      contains: "api/v1"
  key_links:
    - from: "frontend/vite.config.ts"
      to: "backend localhost:8080"
      via: "proxy config for /api"
      pattern: "target.*localhost:8080"
    - from: "frontend/src/api/client.ts"
      to: "backend API"
      via: "axios baseURL /api/v1"
      pattern: "baseURL.*api/v1"
---

<objective>
Scaffold the React 18 + TypeScript frontend project with Vite, TailwindCSS (with Pretendard font), Axios HTTP client, Zustand, TanStack Query, and React Router. Configure the Vite dev proxy for seamless backend communication.

Purpose: Establishes the frontend foundation with all dependencies wired and the design token system (per UI-SPEC) configured. The Axios client and API type definitions provide the HTTP communication layer for all subsequent phases.
Output: A buildable React application with Tailwind styling, Pretendard Korean font, Axios client pointing at /api/v1, and Vite proxy forwarding to the backend.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation/01-CONTEXT.md
@.planning/phases/01-project-foundation/01-RESEARCH.md
@.planning/phases/01-project-foundation/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React 18 + TypeScript project with all dependencies</name>
  <files>
    frontend/package.json,
    frontend/vite.config.ts,
    frontend/tsconfig.json,
    frontend/tsconfig.app.json,
    frontend/tsconfig.node.json,
    frontend/index.html,
    frontend/src/main.tsx,
    frontend/src/App.tsx,
    frontend/src/vite-env.d.ts
  </files>
  <read_first>
    .planning/phases/01-project-foundation/01-RESEARCH.md,
    .planning/phases/01-project-foundation/01-CONTEXT.md,
    .planning/phases/01-project-foundation/01-UI-SPEC.md
  </read_first>
  <action>
    1. Scaffold using Vite template, then pin React 18 (Pitfall 7 from RESEARCH.md):
       ```bash
       cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign
       npm create vite@latest frontend -- --template react-ts
       ```

    2. IMMEDIATELY after scaffolding, edit `frontend/package.json` to pin React 18 versions:
       - Change `"react"` to `"^18.3.1"`
       - Change `"react-dom"` to `"^18.3.1"`
       - Change `"@types/react"` to `"^18.3.18"`
       - Change `"@types/react-dom"` to `"^18.3.5"`
       - Remove any React 19 entries

    3. Install all required dependencies per D-05 and RESEARCH.md:
       ```bash
       cd frontend
       npm install
       npm install zustand @tanstack/react-query axios react-router zod react-hook-form
       npm install -D tailwindcss@3.4 postcss autoprefixer
       ```

    4. Initialize Tailwind:
       ```bash
       npx tailwindcss init -p
       ```

    5. Configure `frontend/vite.config.ts` per D-12 (Vite proxy for /api):
       ```typescript
       import { defineConfig } from 'vite';
       import react from '@vitejs/plugin-react';

       export default defineConfig({
         plugins: [react()],
         server: {
           port: 5173,
           proxy: {
             '/api': {
               target: 'http://localhost:8080',
               changeOrigin: true,
             },
           },
         },
       });
       ```

    6. Add Pretendard font CDN link to `frontend/index.html` (per UI-SPEC):
       Inside the `<head>` tag, add:
       ```html
       <link rel="stylesheet" as="style" crossorigin
         href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable.min.css" />
       ```
       Also set `<html lang="ko">` since MiceSign is a Korean-language system.

    7. Update `frontend/src/App.tsx` to a minimal placeholder:
       ```tsx
       function App() {
         return (
           <div className="min-h-screen bg-gray-50 flex items-center justify-center">
             <div className="text-center">
               <h1 className="text-xl font-semibold text-gray-900">MiceSign</h1>
               <p className="text-sm text-gray-600 mt-2">전자 결재 시스템</p>
             </div>
           </div>
         );
       }

       export default App;
       ```

    8. Delete any auto-generated CSS files (App.css content) and replace `frontend/src/index.css` with Tailwind directives:
       ```css
       @tailwind base;
       @tailwind components;
       @tailwind utilities;
       ```

    9. Ensure `frontend/src/main.tsx` imports index.css:
       ```tsx
       import React from 'react';
       import ReactDOM from 'react-dom/client';
       import App from './App';
       import './index.css';

       ReactDOM.createRoot(document.getElementById('root')!).render(
         <React.StrictMode>
           <App />
         </React.StrictMode>,
       );
       ```
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json contains `"react": "^18.3.1"`
    - frontend/package.json contains `"react-dom": "^18.3.1"`
    - frontend/package.json does NOT contain react version 19
    - frontend/package.json contains `zustand` in dependencies
    - frontend/package.json contains `@tanstack/react-query` in dependencies
    - frontend/package.json contains `axios` in dependencies
    - frontend/package.json contains `react-router` in dependencies
    - frontend/package.json contains `react-hook-form` in dependencies
    - frontend/package.json contains `zod` in dependencies
    - frontend/package.json contains `tailwindcss` in devDependencies
    - frontend/vite.config.ts contains `target: 'http://localhost:8080'`
    - frontend/vite.config.ts contains `'/api'`
    - frontend/index.html contains `pretendardvariable.min.css`
    - frontend/index.html contains `lang="ko"`
    - frontend/src/index.css contains `@tailwind base`
    - frontend/src/App.tsx contains `MiceSign`
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Vite project scaffolded with React 18 pinned (not 19). All dependencies installed: Zustand, TanStack Query, Axios, React Router, React Hook Form, Zod, TailwindCSS 3.4. Vite proxy configured for /api to localhost:8080. Pretendard font loaded via CDN. Tailwind CSS directives in index.css. Minimal App.tsx placeholder renders.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind with Pretendard font, create Axios client, API types, and frontend directory structure</name>
  <files>
    frontend/tailwind.config.js,
    frontend/src/api/client.ts,
    frontend/src/types/api.ts,
    frontend/src/stores/.gitkeep,
    frontend/src/hooks/.gitkeep,
    frontend/src/lib/.gitkeep,
    frontend/src/components/ui/.gitkeep,
    frontend/src/features/auth/.gitkeep,
    frontend/src/features/document/.gitkeep
  </files>
  <read_first>
    .planning/phases/01-project-foundation/01-UI-SPEC.md,
    .planning/phases/01-project-foundation/01-CONTEXT.md,
    .planning/phases/01-project-foundation/01-RESEARCH.md,
    frontend/tailwind.config.js
  </read_first>
  <action>
    1. Update `frontend/tailwind.config.js` to match the UI-SPEC contract exactly:
       ```javascript
       /** @type {import('tailwindcss').Config} */
       export default {
         content: [
           "./index.html",
           "./src/**/*.{js,ts,jsx,tsx}",
         ],
         theme: {
           extend: {
             fontFamily: {
               sans: [
                 'Pretendard Variable',
                 'Pretendard',
                 '-apple-system',
                 'BlinkMacSystemFont',
                 'system-ui',
                 'Helvetica Neue',
                 'Apple SD Gothic Neo',
                 'sans-serif',
               ],
             },
           },
         },
         plugins: [],
       }
       ```

    2. Create `frontend/src/api/client.ts` (Axios instance per D-13, D-14):
       ```typescript
       import axios from 'axios';

       const apiClient = axios.create({
         baseURL: '/api/v1',
         headers: {
           'Content-Type': 'application/json',
         },
       });

       // Response interceptor - envelope unwrapping
       apiClient.interceptors.response.use(
         (response) => response,
         (error) => {
           // JWT refresh interceptor will be added in Phase 2
           // For now, just pass through errors
           return Promise.reject(error);
         }
       );

       export default apiClient;
       ```

    3. Create `frontend/src/types/api.ts` (TypeScript types matching D-13, D-15, D-16):
       ```typescript
       // API Response Envelope (matches backend ApiResponse record)
       export interface ApiResponse<T> {
         success: boolean;
         data: T | null;
         error: ErrorDetail | null;
       }

       export interface ErrorDetail {
         code: string;
         message: string;
       }

       // Pagination (matches D-16: page=0, size=20, Spring Data Pageable)
       export interface PageResponse<T> {
         content: T[];
         totalElements: number;
         totalPages: number;
         size: number;
         number: number; // current page (0-indexed)
         first: boolean;
         last: boolean;
       }

       export interface PageRequest {
         page?: number;  // default 0
         size?: number;  // default 20
         sort?: string;
       }
       ```

    4. Create the frontend directory structure per D-05 (feature-folder structure):
       ```
       frontend/src/
       ├── api/           # Axios instance, interceptors (client.ts)
       ├── components/
       │   └── ui/        # Shared UI components (empty for now)
       ├── features/
       │   ├── auth/      # Login page (Phase 2)
       │   └── document/  # Document pages (Phase 4+)
       ├── hooks/         # Custom hooks
       ├── stores/        # Zustand stores
       ├── types/         # TypeScript types (api.ts)
       └── lib/           # Utilities
       ```

       Create .gitkeep files in empty directories to preserve the structure in git.
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - frontend/tailwind.config.js contains `'Pretendard Variable'`
    - frontend/tailwind.config.js contains `'Apple SD Gothic Neo'`
    - frontend/tailwind.config.js contains `"./src/**/*.{js,ts,jsx,tsx}"`
    - frontend/src/api/client.ts contains `baseURL: '/api/v1'`
    - frontend/src/api/client.ts contains `axios.create`
    - frontend/src/api/client.ts contains `interceptors.response.use`
    - frontend/src/types/api.ts contains `interface ApiResponse<T>`
    - frontend/src/types/api.ts contains `interface PageResponse<T>`
    - frontend/src/types/api.ts contains `interface ErrorDetail`
    - Directory frontend/src/components/ui/ exists
    - Directory frontend/src/features/auth/ exists
    - Directory frontend/src/features/document/ exists
    - Directory frontend/src/stores/ exists
    - Directory frontend/src/hooks/ exists
    - Directory frontend/src/lib/ exists
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Tailwind configured with Pretendard Variable font family matching UI-SPEC exactly. Axios client at /api/v1 with response interceptor skeleton ready for Phase 2 JWT refresh. TypeScript types for API envelope (ApiResponse), error detail (ErrorDetail), and pagination (PageResponse, PageRequest) matching backend contracts. Feature-folder directory structure created per D-05.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` exits with code 0
2. `grep "Pretendard Variable" frontend/tailwind.config.js` returns a match
3. `grep "baseURL.*api/v1" frontend/src/api/client.ts` returns a match
4. `grep "react.*18" frontend/package.json` returns a match
5. All directories in files_modified exist on disk
</verification>

<success_criteria>
- Frontend builds without errors (npm run build produces dist/ output)
- React 18.3.x pinned (not React 19)
- TailwindCSS 3.4 configured with Pretendard font stack from UI-SPEC
- Vite dev proxy routes /api to localhost:8080
- Axios client configured with /api/v1 baseURL
- TypeScript types for API envelope, pagination match backend contracts
- Feature-folder structure (src/features/auth/, src/features/document/) established
- All required dependencies present: Zustand, TanStack Query, Axios, React Router, React Hook Form, Zod
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-02-SUMMARY.md`
</output>
