---
phase: 13-dynamic-form-rendering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/micesign/controller/TemplateController.java
  - backend/src/main/java/com/micesign/service/TemplateService.java
  - backend/src/main/java/com/micesign/dto/document/DocumentDetailResponse.java
  - backend/src/main/java/com/micesign/mapper/DocumentMapper.java
  - backend/src/test/java/com/micesign/template/TemplateSchemaControllerTest.java
  - frontend/package.json
  - frontend/src/features/document/types/dynamicForm.ts
  - frontend/src/features/document/types/document.ts
  - frontend/src/features/document/utils/schemaToZod.ts
  - frontend/src/features/document/api/templateApi.ts
autonomous: true
requirements: [RNDR-01, RNDR-02, RNDR-03]

must_haves:
  truths:
    - "일반 사용자가 templateCode로 템플릿 스키마를 조회할 수 있다"
    - "문서 상세 조회 시 schemaDefinitionSnapshot이 응답에 포함된다"
    - "JSON 스키마의 fields 배열을 Zod v4 스키마로 런타임 변환할 수 있다"
    - "프론트엔드에 SchemaDefinition/FieldDefinition/FieldConfig TypeScript 타입이 정의되어 있다"
  artifacts:
    - path: "backend/src/main/java/com/micesign/controller/TemplateController.java"
      provides: "GET /api/v1/templates/{code}/schema public endpoint"
      contains: "getTemplateSchema"
    - path: "backend/src/main/java/com/micesign/dto/document/DocumentDetailResponse.java"
      provides: "schemaDefinitionSnapshot field in document detail"
      contains: "schemaDefinitionSnapshot"
    - path: "frontend/src/features/document/types/dynamicForm.ts"
      provides: "TypeScript interfaces matching backend SchemaDefinition/FieldDefinition/FieldConfig"
      exports: ["SchemaDefinition", "FieldDefinition", "FieldType", "FieldConfig", "OptionItem"]
    - path: "frontend/src/features/document/utils/schemaToZod.ts"
      provides: "JSON schema to Zod v4 runtime conversion"
      exports: ["schemaToZod", "buildDefaultRow"]
  key_links:
    - from: "frontend/src/features/document/utils/schemaToZod.ts"
      to: "frontend/src/features/document/types/dynamicForm.ts"
      via: "import FieldDefinition type"
      pattern: "import.*FieldDefinition.*from.*dynamicForm"
    - from: "frontend/src/features/document/api/templateApi.ts"
      to: "GET /api/v1/templates/{code}/schema"
      via: "axios GET call"
      pattern: "templates.*schema"
---

<objective>
Backend API 확장 + 프론트엔드 기반 인프라 구축 (타입, schemaToZod, npm 의존성, API 클라이언트)

Purpose: DynamicForm/DynamicReadOnly 컴포넌트가 사용할 백엔드 API와 프론트엔드 유틸리티를 먼저 구축하여, 후속 Plan 02/03에서 UI 컴포넌트 구현에만 집중할 수 있도록 함
Output: 공개 스키마 조회 API, DocumentDetail에 스냅샷 포함, TypeScript 타입, schemaToZod 유틸리티, @headlessui/react + react-day-picker 설치
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-dynamic-form-rendering/13-CONTEXT.md
@.planning/phases/13-dynamic-form-rendering/13-RESEARCH.md
@.planning/phases/13-dynamic-form-rendering/13-UI-SPEC.md

<interfaces>
<!-- Backend DTOs the frontend types must mirror -->

From backend SchemaDefinition.java:
```java
public record SchemaDefinition(
    int version,
    List<FieldDefinition> fields,
    List<Object> conditionalRules,
    List<Object> calculationRules
) {}
```

From backend FieldDefinition.java:
```java
public record FieldDefinition(
    String id,
    String type,       // text, textarea, number, date, select, table, staticText, hidden
    String label,
    boolean required,
    FieldConfig config
) {}
```

From backend FieldConfig.java:
```java
public record FieldConfig(
    String placeholder, Integer maxLength,           // text/textarea
    Long min, Long max, String unit,                 // number
    Long optionSetId, List<OptionItemResponse> options, // select
    Integer minRows, Integer maxRows, List<FieldDefinition> columns, // table
    String content,                                  // staticText
    String defaultValue                              // hidden
) {}
```

From backend OptionItemResponse.java:
```java
public record OptionItemResponse(Long id, String value, String label, int sortOrder) {}
```

From templateRegistry.ts:
```typescript
export interface TemplateEditProps {
  documentId: number | null;
  initialData?: { title: string; bodyHtml?: string; formData?: string };
  onSave: (data: { title: string; bodyHtml?: string; formData?: string }) => Promise<void>;
  readOnly?: boolean;
}
export interface TemplateReadOnlyProps {
  title: string;
  bodyHtml?: string | null;
  formData?: string | null;
}
```

From document.ts:
```typescript
export interface DocumentDetailResponse extends DocumentResponse {
  drafter: DrafterInfo;
  bodyHtml: string | null;
  formData: string | null;
  approvalLines: ApprovalLineResponse[];
  sourceDocId: number | null;
  currentStep: number | null;
  submittedAt: string | null;
  completedAt: string | null;
}
```

From templateApi.ts:
```typescript
export const templateApi = {
  getActiveTemplates: () =>
    apiClient.get<ApiResponse<TemplateResponse[]>>('/templates'),
};
```

From TemplateResponse (backend):
```java
public record TemplateResponse(Long id, String code, String name, String description, String prefix, boolean isCustom, String category, String icon) {}
```
</interfaces>
</context>

<threat_model>
- **Template schema injection**: Public endpoint `/templates/{code}/schema` returns schema data. Risk: malicious schema with XSS payloads in field labels/content. Mitigation: React auto-escapes JSX output; staticText content rendered as text, not HTML. No `dangerouslySetInnerHTML` on dynamic fields.
- **Schema snapshot tampering**: schemaDefinitionSnapshot is stored as LONGTEXT in DB. Risk: if frontend parses without validation, malformed JSON could cause crashes. Mitigation: JSON.parse in try/catch with fallback rendering.
- **Unauthorized template access**: Public schema endpoint must only return active templates. Mitigation: Service layer checks `isActive` before returning.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Backend API 확장 — 공개 스키마 조회 + DocumentDetailResponse 스냅샷</name>
  <files>
    backend/src/main/java/com/micesign/controller/TemplateController.java
    backend/src/main/java/com/micesign/service/TemplateService.java
    backend/src/main/java/com/micesign/dto/document/DocumentDetailResponse.java
    backend/src/main/java/com/micesign/mapper/DocumentMapper.java
    backend/src/test/java/com/micesign/template/TemplateSchemaControllerTest.java
  </files>
  <read_first>
    backend/src/main/java/com/micesign/controller/TemplateController.java
    backend/src/main/java/com/micesign/service/TemplateService.java
    backend/src/main/java/com/micesign/service/TemplateSchemaService.java
    backend/src/main/java/com/micesign/dto/document/DocumentDetailResponse.java
    backend/src/main/java/com/micesign/mapper/DocumentMapper.java
    backend/src/main/java/com/micesign/dto/template/SchemaDefinition.java
    backend/src/main/java/com/micesign/repository/ApprovalTemplateRepository.java
    backend/src/main/java/com/micesign/domain/DocumentContent.java
  </read_first>
  <action>
**1. TemplateService — 공개 스키마 조회 메서드 추가:**

`getTemplateSchemaByCode(String code)` 메서드를 TemplateService에 추가:
- `approvalTemplateRepository.findByCode(code)` 로 템플릿 조회
- 없으면 `TEMPLATE_NOT_FOUND` ResourceNotFoundException throw
- `!template.isActive()` 이면 `TEMPLATE_NOT_FOUND` throw (비활성 템플릿 접근 차단)
- `template.getSchemaDefinition()` 이 null이면 `TEMPLATE_NOT_FOUND` throw (하드코딩 양식은 스키마 없음)
- `objectMapper.readValue(template.getSchemaDefinition(), SchemaDefinition.class)` 로 역직렬화
- `templateSchemaService.resolveSchemaWithOptions(schemaDefinition)` 호출하여 옵션 세트 resolve
- resolved SchemaDefinition 반환

반환 타입: `SchemaDefinition`

**2. TemplateController — 공개 엔드포인트 추가:**

TemplateController에 새 엔드포인트 추가 (기존 `/templates` 아래, admin이 아닌 공개):
```java
@GetMapping("/templates/{code}/schema")
public ApiResponse<SchemaDefinition> getTemplateSchema(@PathVariable String code) {
    return ApiResponse.ok(templateService.getTemplateSchemaByCode(code));
}
```

**3. DocumentDetailResponse — schemaDefinitionSnapshot 필드 추가:**

기존 record에 `String schemaDefinitionSnapshot` 필드를 `updatedAt` 뒤에 추가:
```java
public record DocumentDetailResponse(
    Long id, String docNumber, String templateCode, String templateName,
    String title, String status, DrafterInfo drafter, String bodyHtml, String formData,
    List<ApprovalLineResponse> approvalLines, Long sourceDocId, Integer currentStep,
    LocalDateTime submittedAt, LocalDateTime completedAt,
    LocalDateTime createdAt, LocalDateTime updatedAt,
    String schemaDefinitionSnapshot  // NEW
) { ... }
```

**4. DocumentMapper — 스냅샷 매핑 추가:**

`toDetailResponse` 메서드에 매핑 추가:
```java
@Mapping(target = "schemaDefinitionSnapshot", source = "content.schemaDefinitionSnapshot")
```

**5. Integration Test — TemplateSchemaControllerTest 작성:**

새 테스트 파일 `backend/src/test/java/com/micesign/template/TemplateSchemaControllerTest.java`:
- 기존 TemplateApiIntegrationTest 패턴 참고 (admin 로그인, 커스텀 템플릿 생성 후 테스트)
- 테스트 1: `GET /api/v1/templates/{code}/schema` — 커스텀 템플릿의 스키마 조회 성공 (200, fields 배열 포함)
- 테스트 2: 존재하지 않는 code로 조회 시 404
- 테스트 3: 하드코딩 템플릿(GENERAL) 코드로 조회 시 404 (schemaDefinition null)
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/backend && ./gradlew test --tests "com.micesign.template.TemplateSchemaControllerTest" -x compileQuerydsl 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - TemplateController.java contains `@GetMapping("/templates/{code}/schema")`
    - TemplateController.java contains `getTemplateSchema(@PathVariable String code)`
    - TemplateService.java contains `getTemplateSchemaByCode(String code)`
    - DocumentDetailResponse.java contains `String schemaDefinitionSnapshot`
    - DocumentMapper.java contains `@Mapping(target = "schemaDefinitionSnapshot", source = "content.schemaDefinitionSnapshot")`
    - TemplateSchemaControllerTest.java exists with at least 2 test methods
    - `./gradlew test --tests "com.micesign.template.TemplateSchemaControllerTest"` exits 0
  </acceptance_criteria>
  <done>공개 스키마 조회 API가 동작하고, 문서 상세 응답에 schemaDefinitionSnapshot이 포함됨</done>
</task>

<task type="auto">
  <name>Task 2: 프론트엔드 인프라 — npm 의존성, TypeScript 타입, schemaToZod, API 클라이언트</name>
  <files>
    frontend/package.json
    frontend/src/features/document/types/dynamicForm.ts
    frontend/src/features/document/types/document.ts
    frontend/src/features/document/utils/schemaToZod.ts
    frontend/src/features/document/api/templateApi.ts
  </files>
  <read_first>
    frontend/package.json
    frontend/src/features/document/types/document.ts
    frontend/src/features/document/api/templateApi.ts
    frontend/src/features/document/components/templates/ExpenseForm.tsx
    frontend/src/features/document/components/templates/schemas/generalSchema.ts
    frontend/src/features/document/components/templates/schemas/expenseSchema.ts
  </read_first>
  <action>
**1. npm 의존성 설치:**
```bash
cd frontend && npm install @headlessui/react@^2.2 react-day-picker@^9.14
```
per D-04 (Headless UI Combobox), D-05 (react-day-picker)

**2. TypeScript 타입 정의 — `types/dynamicForm.ts` (새 파일):**
```typescript
// Backend SchemaDefinition/FieldDefinition/FieldConfig에 대응하는 프론트엔드 타입

export type FieldType = 'text' | 'textarea' | 'number' | 'date' | 'select' | 'table' | 'staticText' | 'hidden';

export interface OptionItem {
  id?: number;
  value: string;
  label: string;
  sortOrder: number;
}

export interface FieldConfig {
  // text/textarea
  placeholder?: string;
  maxLength?: number;
  // number
  min?: number;
  max?: number;
  unit?: string;
  // select
  optionSetId?: number;
  options?: OptionItem[];
  // table
  minRows?: number;
  maxRows?: number;
  columns?: FieldDefinition[];
  // staticText
  content?: string;
  // hidden
  defaultValue?: string;
}

export interface FieldDefinition {
  id: string;
  type: FieldType;
  label: string;
  required: boolean;
  config?: FieldConfig;
}

export interface SchemaDefinition {
  version: number;
  fields: FieldDefinition[];
  conditionalRules: unknown[];
  calculationRules: unknown[];
}
```

**3. DocumentDetailResponse 타입 확장 — `types/document.ts`:**

기존 `DocumentDetailResponse` 인터페이스에 추가:
```typescript
schemaDefinitionSnapshot?: string | null;
```

기존 `TemplateResponse` 인터페이스에 추가 (Phase 12에서 백엔드에 이미 추가되었으나 프론트엔드 미반영):
```typescript
isCustom?: boolean;
category?: string | null;
icon?: string | null;
```

**4. schemaToZod 유틸리티 — `utils/schemaToZod.ts` (새 파일):**

Zod v4 API 사용 (`{ error: 'msg' }` 문법, v3의 `{ message: 'msg' }`가 아님):

```typescript
import { z } from 'zod';
import type { FieldDefinition, FieldConfig } from '../types/dynamicForm';

export function schemaToZod(fields: FieldDefinition[]): z.ZodObject<Record<string, z.ZodType>> {
  const shape: Record<string, z.ZodType> = {};

  for (const field of fields) {
    if (field.type === 'staticText') continue; // display only
    if (field.type === 'hidden') {
      // hidden fields: always optional string, no user validation
      shape[field.id] = z.string().optional();
      continue;
    }

    shape[field.id] = buildFieldZod(field);
  }

  return z.object(shape);
}

function buildFieldZod(field: FieldDefinition): z.ZodType {
  const config = field.config;
  const label = field.label;

  switch (field.type) {
    case 'text':
    case 'textarea':
    case 'date':
    case 'select':
      return buildStringZod(field.required, label, config);

    case 'number':
      return buildNumberZod(field.required, label, config);

    case 'table':
      return buildTableZod(field.required, label, config);

    default:
      return z.any();
  }
}

function buildStringZod(required: boolean, label: string, config?: FieldConfig): z.ZodType {
  let schema = z.string();

  if (config?.maxLength) {
    schema = schema.max(config.maxLength, {
      error: `${label}은(는) ${config.maxLength}자 이하여야 합니다`,
    });
  }

  if (required) {
    schema = schema.min(1, {
      error: `${label}을(를) 입력해주세요`,
    });
  }

  return required ? schema : schema.optional();
}

function buildNumberZod(required: boolean, label: string, config?: FieldConfig): z.ZodType {
  let schema = z.coerce.number({ error: `${label}은(는) 숫자여야 합니다` });

  if (config?.min != null) {
    schema = schema.min(config.min, {
      error: `${label}은(는) ${config.min} 이상이어야 합니다`,
    });
  }
  if (config?.max != null) {
    schema = schema.max(config.max, {
      error: `${label}은(는) ${config.max} 이하여야 합니다`,
    });
  }

  return required ? schema : schema.optional();
}

function buildTableZod(required: boolean, label: string, config?: FieldConfig): z.ZodType {
  if (!config?.columns) return z.any();

  const rowShape: Record<string, z.ZodType> = {};
  for (const col of config.columns) {
    rowShape[col.id] = buildFieldZod(col);
  }

  let arraySchema = z.array(z.object(rowShape));

  const minRows = config.minRows ?? (required ? 1 : 0);
  const maxRows = config.maxRows ?? 100;

  if (minRows > 0) {
    arraySchema = arraySchema.min(minRows, {
      error: `최소 ${minRows}개의 행이 필요합니다`,
    });
  }
  if (maxRows < 100) {
    arraySchema = arraySchema.max(maxRows, {
      error: `최대 ${maxRows}개의 행까지 추가할 수 있습니다`,
    });
  }

  return required ? arraySchema : arraySchema.optional();
}

/**
 * table 필드의 columns에서 빈 행 기본값 객체 생성
 * useFieldArray의 append에 사용
 */
export function buildDefaultRow(columns: FieldDefinition[]): Record<string, unknown> {
  const row: Record<string, unknown> = {};
  for (const col of columns) {
    switch (col.type) {
      case 'number':
        row[col.id] = 0;
        break;
      case 'text':
      case 'textarea':
      case 'date':
      case 'select':
      default:
        row[col.id] = '';
        break;
    }
  }
  return row;
}
```

**5. templateApi 확장 — `api/templateApi.ts`:**

기존 `templateApi` 객체에 추가:
```typescript
getTemplateSchema: (code: string) =>
  apiClient.get<ApiResponse<SchemaDefinition>>(`/templates/${code}/schema`),
```

SchemaDefinition import를 `../types/dynamicForm`에서 추가.
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/node_modules/@headlessui/react/package.json` exists
    - `frontend/node_modules/react-day-picker/package.json` exists
    - `frontend/src/features/document/types/dynamicForm.ts` contains `export type FieldType =`
    - `frontend/src/features/document/types/dynamicForm.ts` contains `export interface SchemaDefinition`
    - `frontend/src/features/document/types/dynamicForm.ts` contains `export interface FieldDefinition`
    - `frontend/src/features/document/types/dynamicForm.ts` contains `export interface FieldConfig`
    - `frontend/src/features/document/types/document.ts` contains `schemaDefinitionSnapshot`
    - `frontend/src/features/document/utils/schemaToZod.ts` contains `export function schemaToZod`
    - `frontend/src/features/document/utils/schemaToZod.ts` contains `export function buildDefaultRow`
    - `frontend/src/features/document/utils/schemaToZod.ts` contains `{ error:` (Zod v4 syntax, NOT `{ message:`)
    - `frontend/src/features/document/api/templateApi.ts` contains `getTemplateSchema`
    - `frontend/src/features/document/api/templateApi.ts` contains `/templates/${code}/schema`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>npm 의존성 설치 완료, TypeScript 타입 정의 완료, schemaToZod 유틸리티 구현 완료, API 클라이언트 확장 완료. 후속 Plan에서 UI 컴포넌트 구현 가능.</done>
</task>

</tasks>

<verification>
- Backend: `cd backend && ./gradlew test --tests "com.micesign.template.*"` 모든 테스트 통과
- Frontend: `cd frontend && npx tsc --noEmit` 타입 체크 통과
- schemaToZod가 Zod v4 문법(`{ error: }`)을 사용하는지 확인
- @headlessui/react, react-day-picker가 node_modules에 설치되어 있는지 확인
</verification>

<success_criteria>
1. `GET /api/v1/templates/{code}/schema` 가 커스텀 템플릿의 SchemaDefinition을 반환
2. 문서 상세 조회 API 응답에 `schemaDefinitionSnapshot` 필드가 포함됨
3. `schemaToZod(fields)` 가 FieldDefinition 배열을 Zod v4 스키마로 변환 (text, number, date, select, table, hidden 지원)
4. `buildDefaultRow(columns)` 가 테이블 행 기본값 객체를 생성
5. @headlessui/react, react-day-picker npm 의존성이 설치됨
</success_criteria>

<output>
After completion, create `.planning/phases/13-dynamic-form-rendering/13-01-SUMMARY.md`
</output>
