---
phase: 32-custom
plan: 04
type: execute
wave: 2
gap_closure: false
depends_on:
  - 32-01
  - 32-02
files_modified:
  - frontend/public/locales/ko/admin.json
autonomous: true
requirements:
  - FORM-01
  - FORM-02
user_setup: []

must_haves:
  truths:
    - "ko/admin.json 의 templates 섹션에 4 신규 키 (presetMeetingName/Desc, presetProposalName/Desc) 가 추가된다"
    - "JSON 이 유효 형식이다 (trailing comma / brace 깨짐 0건)"
    - "기존 preset 4 키 (presetExpenseName/Desc, presetLeaveName/Desc, presetTripName/Desc, presetPurchaseName/Desc) preserve"
    - "i18n 키 이름이 PresetGallery.tsx 의 I18N_MAP entry 와 정확 일치 (templates.preset{Pascal}Name/Desc)"
    - "en/admin.json 무수정 (D-E2 — 한국어 only 정책)"
  artifacts:
    - path: "frontend/public/locales/ko/admin.json"
      provides: "회의록/품의서 i18n 라벨 — react-i18next 가 PresetGallery 카드에 표시"
      contains: "presetMeetingName"
  key_links:
    - from: "frontend/public/locales/ko/admin.json (templates.preset*)"
      to: "frontend/src/features/admin/components/PresetGallery.tsx (I18N_MAP)"
      via: "useTranslation('admin').t('templates.presetMeetingName')"
      pattern: "presetMeetingName\\|presetProposalName"
---

<objective>
ko/admin.json 의 templates 섹션 끝부분 (presetPurchaseDesc 다음) 에 회의록/품의서 한국어 라벨 4 개 키를 추가하여, Plan 03 의 PresetGallery I18N_MAP 이 lookup 시 정상 한국어 카드 표시 (raw key fallback 미발생) 를 보장한다.

Purpose: FORM-01/FORM-02 의 i18n 표시 게이트 — i18n 미존재 시 카드에 "templates.presetMeetingName" raw 텍스트가 표시되는 회귀 차단. CLAUDE.md "한국어 documentation" 정책 + D-E2 한국어 only 준수.
Output: ko/admin.json 의 templates 섹션 4 키 추가 (단일 파일 단일 위치 수정).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-custom/32-CONTEXT.md
@.planning/phases/32-custom/32-RESEARCH.md
@.planning/phases/32-custom/32-PATTERNS.md
@.planning/phases/32-custom/32-VALIDATION.md
@./CLAUDE.md

# Direct edit target
@frontend/public/locales/ko/admin.json

<interfaces>
<!-- ko/admin.json 의 정확한 수정 위치 (VERIFIED via grep 2026-04-25) -->

L135-143 (현재 templates 섹션의 preset 4 키 — L143 의 presetPurchaseDesc 줄 trailing comma 추가 + 다음 줄에 4 키 삽입):
```json
"presetGalleryTitle": "프리셋 선택",
"presetExpenseName": "경비신청서",
"presetExpenseDesc": "항목/단가/수량/합계 테이블과 자동 합계 계산을 포함합니다",
"presetLeaveName": "휴가신청서",
"presetLeaveDesc": "휴가 종류 선택과 기간별 조건부 사유 입력을 포함합니다",
"presetTripName": "출장신청서",
"presetTripDesc": "일정·동행자·예상 비용 계산을 포함합니다",
"presetPurchaseName": "구매신청서",
"presetPurchaseDesc": "품목 테이블과 총액 자동 계산을 포함합니다",
```

**문구 일관성 패턴 (PATTERNS.md S4):**
- Name: `OO신청서` 또는 단일명사 (회의록/품의서) — 자연 한국어 명칭
- Desc: `~를/을 포함합니다` / `~합니다` 능동 종결, 12-30 자, `·` 중점으로 키워드 나열

**키 일치 의무 (Plan 03 의 I18N_MAP 과 동기):**
- `presetMeetingName` → I18N_MAP['meeting'].nameKey 의 'templates.presetMeetingName' 와 정확 일치
- `presetMeetingDesc` → I18N_MAP['meeting'].descKey
- `presetProposalName` → I18N_MAP['proposal'].nameKey
- `presetProposalDesc` → I18N_MAP['proposal'].descKey
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ko/admin.json templates 섹션에 4 신규 키 추가 — JSON 유효성 보존</name>
  <files>frontend/public/locales/ko/admin.json</files>
  <read_first>
    - frontend/public/locales/ko/admin.json (현재 L135-145 구조 확인 — trailing comma 처리 + templates 섹션 닫힘 위치 파악)
    - frontend/src/features/admin/components/PresetGallery.tsx (Plan 03 결과 — I18N_MAP 의 4 신규 키 이름과 정확 일치 의무)
  </read_first>
  <action>
    ko/admin.json 의 L143 (`"presetPurchaseDesc": "품목 테이블과 총액 자동 계산을 포함합니다",` 줄) 다음에 정확히 4 줄 삽입. trailing comma / 닫는 brace 위치 보존 의무.

    **수정 후 결과 (L135~146 영역):**
    ```json
    "presetGalleryTitle": "프리셋 선택",
    "presetExpenseName": "경비신청서",
    "presetExpenseDesc": "항목/단가/수량/합계 테이블과 자동 합계 계산을 포함합니다",
    "presetLeaveName": "휴가신청서",
    "presetLeaveDesc": "휴가 종류 선택과 기간별 조건부 사유 입력을 포함합니다",
    "presetTripName": "출장신청서",
    "presetTripDesc": "일정·동행자·예상 비용 계산을 포함합니다",
    "presetPurchaseName": "구매신청서",
    "presetPurchaseDesc": "품목 테이블과 총액 자동 계산을 포함합니다",
    "presetMeetingName": "회의록",
    "presetMeetingDesc": "회의 일시·참석자·안건·결정사항을 구조화하여 기록합니다",
    "presetProposalName": "품의서",
    "presetProposalDesc": "품의 배경·제안 내용·예상 효과를 정리합니다",
    ```

    **결정 근거 (D-E1):**
    - `presetMeetingName: "회의록"` — 단일명사 (PATTERNS S4)
    - `presetMeetingDesc: "회의 일시·참석자·안건·결정사항을 구조화하여 기록합니다"` — 회의록 4 필드 keyword 나열, 24자
    - `presetProposalName: "품의서"` — 단일명사
    - `presetProposalDesc: "품의 배경·제안 내용·예상 효과를 정리합니다"` — 품의서 3 textarea 필드 keyword 나열, 22자

    **금지 사항:**
    - **en/admin.json 무수정** per D-E2 — 한국어 only 정책 (CLAUDE.md "한국어 documentation"). 영어 i18n 추가 = 정책 위배
    - 기존 키 (`presetExpenseName/Desc`, `presetLeaveName/Desc`, `presetTripName/Desc`, `presetPurchaseName/Desc`, `presetGalleryTitle`) 손상 금지
    - JSON 유효성 깨짐 금지 (trailing comma 처리, 마지막 키도 콤마 포함 — 다음에 다른 키가 이어진다 가정)
    - 신규 키 이름 임의 변경 금지 — Plan 03 의 I18N_MAP 과 정확 일치 의무

    **확인 의무:**
    수정 후 `node -e "JSON.parse(require('fs').readFileSync('frontend/public/locales/ko/admin.json','utf8'))"` 실행하여 JSON 파싱 무에러 확인. 만약 SyntaxError 발생 시 trailing comma 또는 brace 위치 점검.
  </action>
  <verify>
    <automated>node -e "const d=JSON.parse(require('fs').readFileSync('frontend/public/locales/ko/admin.json','utf8')); const t=d.templates; console.log('OK', t.presetMeetingName, '|', t.presetMeetingDesc, '|', t.presetProposalName, '|', t.presetProposalDesc)" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - JSON 파싱 무에러: `node -e "JSON.parse(require('fs').readFileSync('frontend/public/locales/ko/admin.json','utf8'))"` 종료코드 0
    - 4 신규 키 존재 + 정확한 한국어 값:
      - `node -e "console.log(JSON.parse(require('fs').readFileSync('frontend/public/locales/ko/admin.json','utf8')).templates.presetMeetingName)"` 출력 = `회의록`
      - `node -e "console.log(JSON.parse(require('fs').readFileSync('frontend/public/locales/ko/admin.json','utf8')).templates.presetMeetingDesc)"` 출력 = `회의 일시·참석자·안건·결정사항을 구조화하여 기록합니다`
      - `node -e "console.log(JSON.parse(require('fs').readFileSync('frontend/public/locales/ko/admin.json','utf8')).templates.presetProposalName)"` 출력 = `품의서`
      - `node -e "console.log(JSON.parse(require('fs').readFileSync('frontend/public/locales/ko/admin.json','utf8')).templates.presetProposalDesc)"` 출력 = `품의 배경·제안 내용·예상 효과를 정리합니다`
    - 기존 4 키 preserve:
      - `node -e "const t=JSON.parse(require('fs').readFileSync('frontend/public/locales/ko/admin.json','utf8')).templates; console.log([t.presetExpenseName, t.presetLeaveName, t.presetTripName, t.presetPurchaseName].join('|'))"` 출력 = `경비신청서|휴가신청서|출장신청서|구매신청서`
    - en/admin.json 무수정 확인: `git diff --quiet frontend/public/locales/en/admin.json` 종료코드 0 (변경 없음)
  </acceptance_criteria>
  <done>ko/admin.json 의 templates 섹션에 4 신규 키 추가 + JSON 유효 + 기존 4 키 + presetGalleryTitle preserve. en/admin.json 무수정 확인.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend build → react-i18next runtime | i18n JSON 손상 시 빌드는 통과하나 런타임에서 raw key 표시 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | N/A | ko/admin.json | n/a | i18n JSON 은 templateImportSchema 검증 대상 외. 키 mismatch 시 react-i18next 가 fallback string 표시 (보안 게이트 무관, UX 게이트) |
| Regression — 기존 i18n 키 손상 | Tampering | ko/admin.json (templates 섹션 외) | mitigate | JSON 파싱 후 기존 4 preset 키 + presetGalleryTitle 단언 (acceptance_criteria) + Plan 06 manual UAT 의 PresetGallery 카드 시각 확인 |

</threat_model>

<verification>
- JSON 파싱 무에러 (verify automated)
- 4 신규 키 + 정확한 한국어 값 grep
- 기존 4 키 preserve grep
- en/admin.json git diff 무변경
- (Plan 06 통합) PresetGallery 카드에 한국어 명칭 표시 (raw key 미노출)
</verification>

<success_criteria>
1. ko/admin.json 의 templates 섹션에 4 신규 키 추가 (presetMeetingName/Desc, presetProposalName/Desc)
2. JSON 유효 — node parse 무에러
3. 기존 4 preset 키 + presetGalleryTitle preserve
4. en/admin.json 무수정 (D-E2 정책 준수)
5. 키 이름이 Plan 03 의 I18N_MAP 과 정확 일치 — react-i18next lookup 성공
</success_criteria>

<output>
After completion, create `.planning/phases/32-custom/32-04-SUMMARY.md`.
</output>
