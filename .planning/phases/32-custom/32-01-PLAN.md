---
phase: 32-custom
plan: 01
type: execute
wave: 1
gap_closure: false
depends_on: []
files_modified:
  - frontend/src/features/admin/presets/meeting.json
autonomous: true
requirements:
  - FORM-01
user_setup: []

must_haves:
  truths:
    - "meeting.json 이 frontend/src/features/admin/presets/ 에 존재한다"
    - "meeting.json 이 templateImportSchema.parse() (zod .strict()) 를 통과한다"
    - "meeting.json 의 schemaDefinition.fields 가 정확히 5 개 (title, meetingDate, attendees, agenda, decisions)"
    - "meeting.json 의 prefix='MTG', category='general', icon='Users' (D-A2/A3/A4)"
    - "meeting.json 의 attendees/agenda/decisions 모두 type='table' + minRows=1, maxRows=20 (D-A5)"
    - "meeting.json 의 conditionalRules 와 calculationRules 모두 빈 배열 (D-A6)"
  artifacts:
    - path: "frontend/src/features/admin/presets/meeting.json"
      provides: "회의록 CUSTOM 양식 프리셋 (FORM-01)"
      contains: '"name": "회의록"'
  key_links:
    - from: "frontend/src/features/admin/presets/meeting.json"
      to: "frontend/src/features/admin/presets/index.ts (Vite eager glob)"
      via: "import.meta.glob('./*.json', {eager:true}) 자동 인식"
      pattern: "templateImportSchema.parse"
    - from: "frontend/src/features/admin/presets/meeting.json"
      to: "frontend/src/features/admin/validations/templateImportSchema.ts (.strict)"
      via: "build-time Zod parse (T-26-04 mitigation 재사용 + T-32-01 mitigation)"
      pattern: ".strict()"
---

<objective>
회의록 (meeting) CUSTOM 양식 프리셋 JSON 을 작성하여 frontend/src/features/admin/presets/ 디렉터리에 추가하고, Vite eager glob loader 를 통한 build-time Zod (`templateImportSchema.strict()`) 검증을 통과시킨다.

Purpose: FORM-01 충족 — 관리자가 프리셋 갤러리에서 "회의록" 을 선택하면 회의 일시·참석자·안건·결정사항 필드가 사전 정의된 CUSTOM 스키마가 즉시 로드되도록 한다. 본 plan 은 데이터 작성 단계 — UI 통합은 Plan 03/04, 단언 추가는 Plan 05.
Output: meeting.json (단일 신규 파일).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-custom/32-CONTEXT.md
@.planning/phases/32-custom/32-RESEARCH.md
@.planning/phases/32-custom/32-PATTERNS.md
@.planning/phases/32-custom/32-VALIDATION.md
@./CLAUDE.md

# Analog references (Plan 작성자가 키 형식·envelope·table 구조 mimicking)
@frontend/src/features/admin/presets/expense.json
@frontend/src/features/admin/presets/leave.json
@frontend/src/features/admin/presets/purchase.json
@frontend/src/features/admin/presets/trip.json

# Schema gate (loaded for key-name verification)
@frontend/src/features/admin/validations/templateImportSchema.ts

# Loader (build-time Zod parse)
@frontend/src/features/admin/presets/index.ts

<interfaces>
<!-- templateImportSchema 의 핵심 contract — meeting.json 작성 시 정확히 일치해야 하는 키 이름들 -->

From frontend/src/features/admin/validations/templateImportSchema.ts (VERIFIED):

```typescript
// templateImportSchema (L117-128) — 최상위 envelope (.strict())
{
  exportFormatVersion: literal(1),    // MUST = 1
  schemaVersion: number().int().positive(),  // MUST >= 1
  name: string().min(1),
  description: string().optional(),
  prefix: string().min(1),
  category: string().optional(),       // 'finance' | 'hr' | 'general' (관례)
  icon: string().optional(),
  schemaDefinition: schemaDefinitionSchema,
}

// schemaDefinitionSchema (L108-115)
{
  version: number().int().positive(),
  fields: array(fieldDefinitionSchema),
  conditionalRules: array(conditionalRuleSchema).optional(),
  calculationRules: array(calculationRuleSchema).optional(),
}

// fieldDefinitionSchema (L76-84) — required 는 boolean **non-optional**
{
  id: string().min(1),
  type: 'text'|'textarea'|'number'|'date'|'select'|'table'|'staticText'|'hidden',
  label: string().min(1),
  required: boolean(),       // <-- 모든 field 에 명시 필수
  config: fieldConfigSchema.optional(),
}

// fieldConfigSchema (L60-74) — .strict() (T-26-01 mitigation)
{
  placeholder?: string,
  maxLength?: int>=0,
  min?: number, max?: number,
  unit?: string,
  options?: [{value, label, sortOrder?}],
  content?: string,
  defaultValue?: string,
  minRows?: int>=0,
  maxRows?: int>=0,
  columns?: array(columnSchema),  // table 전용
}

// columnSchema (L50-58) — required 는 **optional** (필드와 차이!)
{
  id: string().min(1),
  type: fieldTypeSchema,
  label: string().min(1),
  required?: boolean,           // <-- column 은 optional
  config?: columnConfigSchema,
}
```

**키 이름 오타 = 빌드 실패** (zod .strict()). camelCase 정확 일치 필수: `placeholder`, `maxLength`, `minRows`, `maxRows`, `meetingDate` 등.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: meeting.json 신규 작성 — D-A1~A6 충실 + Discretion 적용</name>
  <files>frontend/src/features/admin/presets/meeting.json</files>
  <read_first>
    - frontend/src/features/admin/presets/expense.json (envelope + title 필드 패턴)
    - frontend/src/features/admin/presets/purchase.json (table 필드 + minRows/maxRows + columns 패턴)
    - frontend/src/features/admin/presets/leave.json (config.placeholder 위치)
    - frontend/src/features/admin/validations/templateImportSchema.ts (정확한 키 이름 — placeholder/maxLength/minRows/maxRows 캡 일치 확인)
  </read_first>
  <action>
    frontend/src/features/admin/presets/meeting.json 신규 파일 작성. 다음 envelope 사용 (CONTEXT D-A1~A6 + Discretion 결정 적용):

    ```json
    {
      "exportFormatVersion": 1,
      "schemaVersion": 1,
      "name": "회의록",
      "description": "회의 일시·참석자·안건·결정사항을 구조화하여 기록합니다",
      "prefix": "MTG",
      "category": "general",
      "icon": "Users",
      "schemaDefinition": {
        "version": 1,
        "fields": [
          { "id": "title", "type": "text", "label": "제목", "required": true },
          { "id": "meetingDate", "type": "date", "label": "회의 일시", "required": true },
          {
            "id": "attendees",
            "type": "table",
            "label": "참석자",
            "required": true,
            "config": {
              "columns": [
                { "id": "name", "type": "text", "label": "이름", "required": true },
                { "id": "affiliation", "type": "text", "label": "소속", "required": true,
                  "config": { "placeholder": "예: 개발팀, 영업팀" } },
                { "id": "role", "type": "text", "label": "역할" }
              ],
              "minRows": 1,
              "maxRows": 20
            }
          },
          {
            "id": "agenda",
            "type": "table",
            "label": "안건",
            "required": true,
            "config": {
              "columns": [
                { "id": "number", "type": "number", "label": "번호", "required": true },
                { "id": "title", "type": "text", "label": "안건명", "required": true },
                { "id": "description", "type": "textarea", "label": "설명" }
              ],
              "minRows": 1,
              "maxRows": 20
            }
          },
          {
            "id": "decisions",
            "type": "table",
            "label": "결정사항",
            "required": true,
            "config": {
              "columns": [
                { "id": "topic", "type": "text", "label": "주제", "required": true },
                { "id": "decision", "type": "text", "label": "결정", "required": true },
                { "id": "owner", "type": "text", "label": "담당자" },
                { "id": "dueDate", "type": "date", "label": "기한" }
              ],
              "minRows": 1,
              "maxRows": 20
            }
          }
        ],
        "conditionalRules": [],
        "calculationRules": []
      }
    }
    ```

    **결정 근거 노트 (코드 주석 미포함, 본 task action 에만 기록):**
    - `prefix="MTG"` per D-A2 — 사용자 양식 생성 시 `importToInitialValues` 가 `prefix:''` 강제 reset 하므로 (Phase 26 D-10) JSON 의 prefix 는 export/import fallback 용도
    - `category="general"` per D-A3 — finance/hr/general 외 신규 카테고리 도입 금지
    - `icon="Users"` per D-A4 — Plan 03 의 ICON_MAP 매핑과 동기 (lucide-react v1.7 export 검증됨)
    - `attendees.affiliation.config.placeholder = "예: 개발팀, 영업팀"` — Discretion (CONTEXT 영역, columnConfigSchema L37 placeholder optional 허용 확인됨)
    - `agenda.columns[1].id = "title"` (옵션 A 그대로) per T-32-02 권장 default — DynamicTableField 의 path namespace `agenda.0.title` 가 root field `title` 과 react-hook-form path 분리 보장 (DynamicTableField.tsx L96 `${field.id}.${rowIdx}.${col.id}` 검증). 향후 calculationRule (`agenda.title` 참조) 도입 시 옵션 B (`subject` 로 rename) 재고 — 본 phase 미사용
    - `conditionalRules: []`, `calculationRules: []` per D-A6 — record-keeping 양식, 자동 분기/계산 부재
    - `column.required` 는 optional 이므로 필수 컬럼만 `"required": true` 명시 — `role`, `description`, `owner`, `dueDate` 는 optional 컬럼이라 키 자체 생략

    **금지 사항 (Anti-Pattern):**
    - 알려지지 않은 키 추가 금지 (`placeHolder`, `place_holder`, `__proto__`, `constructor` 등) — `.strict()` 가 즉시 throw
    - field 의 `required` 키 누락 금지 — `z.boolean()` non-optional, 누락 시 ZodError
    - `category` 에 `"meeting"` 같은 신규 값 도입 금지 — 반드시 `general` (D-A3)
    - 신규 field type 도입 금지 — `text`/`textarea`/`number`/`date`/`select`/`table`/`staticText`/`hidden` 만 허용
  </action>
  <verify>
    <automated>cd frontend && npm test -- features/admin/presets/presets.test.ts --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f frontend/src/features/admin/presets/meeting.json`
    - JSON 유효성: `node -e "JSON.parse(require('fs').readFileSync('frontend/src/features/admin/presets/meeting.json','utf8'))"` 무에러 종료
    - vitest `presets > each preset passes templateImportSchema` 단언이 meeting 포함하여 통과 (Plan 05 의 length=6 단언은 별도 plan — Plan 01 단독 실행 시 length 단언은 fail 가능, 본 task 는 schema parse 단언만 검증)
    - grep 검증: `grep -c '"id": "title"' frontend/src/features/admin/presets/meeting.json` 결과 = 2 (root + agenda.columns[1])
    - grep 검증: `grep -c '"type": "table"' frontend/src/features/admin/presets/meeting.json` 결과 = 3 (attendees, agenda, decisions)
    - grep 검증: `grep -c '"prefix": "MTG"' frontend/src/features/admin/presets/meeting.json` 결과 = 1
    - grep 검증: `grep -c '"icon": "Users"' frontend/src/features/admin/presets/meeting.json` 결과 = 1
    - grep 검증: `grep -c '"category": "general"' frontend/src/features/admin/presets/meeting.json` 결과 = 1
    - grep 검증: `grep -c 'conditionalRules": \[\]' frontend/src/features/admin/presets/meeting.json` 결과 = 1
    - grep 검증: `grep -c 'calculationRules": \[\]' frontend/src/features/admin/presets/meeting.json` 결과 = 1
  </acceptance_criteria>
  <done>meeting.json 파일이 존재하고, vitest `each preset passes templateImportSchema` 단언이 meeting 포함하여 green. JSON 키 이름 오타 0건 (zod .strict() 통과 확인).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 외부 (admin import) → frontend build | 사용자가 작성한 JSON 이 build-time 에 module load 되어 Zod parse 됨. 본 plan 의 meeting.json 은 in-tree 파일로 admin import 와 동일 schema 통과 의무 |
| frontend module load → React runtime | Zod parse 통과한 preset 만 PresetGallery 에 노출 — 손상 시 build 단계에서 throw |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | meeting.json (in-tree preset JSON) | mitigate | `templateImportSchema.strict()` (Phase 26 T-26-01/T-26-04 재사용) 가 build-time 에 unknown keys (`__proto__`, `constructor`, `prototype`, 오타 등) 자동 차단. `vite build` 또는 `npm test -- presets` 가 즉시 ZodError throw. 신규 코드 zero — 인프라 자동 보호 |
| T-32-02 | Tampering | meeting.json `agenda.columns[1].id="title"` (root field `title` 과 동명) | mitigate | DynamicTableField (DynamicTableField.tsx L96) 가 `${field.id}.${rowIdx}.${col.id}` 패스 namespace 처리 — react-hook-form path 분리 보장. 옵션 A (그대로) 채택 (CONTEXT D-A5 명시 + 본 phase 미 calculationRule). Plan 06 manual UAT 에서 회의록 양식 작성 시 root title 과 agenda row title 입력값이 분리 저장되는지 시각 확인. 향후 calculationRule (`agenda.title` 참조) 도입 시 옵션 B (`subject` rename) 로 전환 — 본 phase 외 |

</threat_model>

<verification>
- `cd frontend && npm test -- features/admin/presets --run` — `each preset passes templateImportSchema` 단언이 meeting 포함하여 통과 (Plan 01 시점에서는 length=4→5 일시 mismatch 가능, Plan 05 의 length 단언 업데이트와 동시 green 확정)
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — JSON import 타입 호환 확인 (presets/index.ts 의 `templateImportSchema.parse()` 가 TemplateImportData 반환)
- grep 단언 (acceptance_criteria 참고)
</verification>

<success_criteria>
1. `frontend/src/features/admin/presets/meeting.json` 파일 존재 + JSON 유효
2. `templateImportSchema.parse()` 통과 (vitest 단언으로 검증)
3. 5 개 fields (title, meetingDate, attendees, agenda, decisions), 3 개 table fields (attendees/agenda/decisions), prefix='MTG', icon='Users', category='general'
4. conditionalRules/calculationRules 모두 빈 배열
5. T-32-01 (input validation) 자동 차단 확인 — Zod .strict() 통과
</success_criteria>

<output>
After completion, create `.planning/phases/32-custom/32-01-SUMMARY.md` per templates/summary.md.
</output>
