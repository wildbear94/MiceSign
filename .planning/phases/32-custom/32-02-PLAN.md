---
phase: 32-custom
plan: 02
type: execute
wave: 1
gap_closure: false
depends_on: []
files_modified:
  - frontend/src/features/admin/presets/proposal.json
autonomous: true
requirements:
  - FORM-02
user_setup: []

must_haves:
  truths:
    - "proposal.json 이 frontend/src/features/admin/presets/ 에 존재한다"
    - "proposal.json 이 templateImportSchema.parse() (zod .strict()) 를 통과한다"
    - "proposal.json 의 schemaDefinition.fields 가 정확히 4 개 (title, background, proposal, expectedEffect)"
    - "proposal.json 의 prefix='PRP', category='general', icon='FileSignature' (D-B2/B3/B4)"
    - "proposal.json 의 background/proposal/expectedEffect 모두 type='textarea' + config.maxLength=2000 (D-B5)"
    - "proposal.json 의 conditionalRules 와 calculationRules 모두 빈 배열 (D-B7)"
    - "첨부 필드 미포함 — schema field 가 아닌 document_attachment 테이블이 처리 (D-B6)"
  artifacts:
    - path: "frontend/src/features/admin/presets/proposal.json"
      provides: "품의서 CUSTOM 양식 프리셋 (FORM-02)"
      contains: '"name": "품의서"'
  key_links:
    - from: "frontend/src/features/admin/presets/proposal.json"
      to: "frontend/src/features/admin/presets/index.ts (Vite eager glob)"
      via: "import.meta.glob('./*.json', {eager:true}) 자동 인식"
      pattern: "templateImportSchema.parse"
    - from: "frontend/src/features/admin/presets/proposal.json"
      to: "frontend/src/features/admin/validations/templateImportSchema.ts (.strict)"
      via: "build-time Zod parse (T-32-01 mitigation)"
      pattern: ".strict()"
---

<objective>
품의서 (proposal) CUSTOM 양식 프리셋 JSON 을 작성하여 frontend/src/features/admin/presets/ 디렉터리에 추가하고, build-time Zod (`templateImportSchema.strict()`) 검증을 통과시킨다.

Purpose: FORM-02 충족 — 관리자가 프리셋 갤러리에서 "품의서" 를 선택하면 품의 배경·제안 내용·예상 효과 필드가 사전 정의된 CUSTOM 스키마가 즉시 로드되도록 한다. 본 plan 은 데이터 작성 단계 — UI 통합은 Plan 03/04, 단언 추가는 Plan 05.
Output: proposal.json (단일 신규 파일).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-custom/32-CONTEXT.md
@.planning/phases/32-custom/32-RESEARCH.md
@.planning/phases/32-custom/32-PATTERNS.md
@.planning/phases/32-custom/32-VALIDATION.md
@./CLAUDE.md

# Analog references — leave.json (textarea + config.placeholder), expense.json (envelope + title 위치)
@frontend/src/features/admin/presets/leave.json
@frontend/src/features/admin/presets/expense.json

# Schema gate (loaded for key-name verification)
@frontend/src/features/admin/validations/templateImportSchema.ts

# Loader (build-time Zod parse)
@frontend/src/features/admin/presets/index.ts

<interfaces>
<!-- templateImportSchema 의 핵심 contract — proposal.json 작성 시 정확히 일치해야 하는 키 이름들 -->

From frontend/src/features/admin/validations/templateImportSchema.ts (VERIFIED):

```typescript
// fieldConfigSchema (L60-74) — .strict() (T-26-01 mitigation)
{
  placeholder?: string,           // textarea/text 모두 사용 가능
  maxLength?: int>=0,             // textarea 제한
  // ...
}
```

textarea + config 패턴 reference (leave.json L28-34):
```json
{
  "id": "sickReason",
  "type": "textarea",
  "label": "병가 사유",
  "required": false,
  "config": { "placeholder": "병가 관련 상세 사유를 입력하세요" }
}
```

품의서는 textarea + `maxLength: 2000` 추가 (D-B5).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: proposal.json 신규 작성 — D-B1~B7 충실 + Discretion 적용 (placeholder)</name>
  <files>frontend/src/features/admin/presets/proposal.json</files>
  <read_first>
    - frontend/src/features/admin/presets/leave.json (textarea + config.placeholder 패턴)
    - frontend/src/features/admin/presets/expense.json (envelope + title 필드 패턴)
    - frontend/src/features/admin/validations/templateImportSchema.ts (config.maxLength 키 이름 정확 일치 — camelCase, fieldConfigSchema L63)
  </read_first>
  <action>
    frontend/src/features/admin/presets/proposal.json 신규 파일 작성. 다음 envelope 사용 (CONTEXT D-B1~B7 + Discretion placeholder 적용):

    ```json
    {
      "exportFormatVersion": 1,
      "schemaVersion": 1,
      "name": "품의서",
      "description": "품의 배경·제안 내용·예상 효과를 정리합니다",
      "prefix": "PRP",
      "category": "general",
      "icon": "FileSignature",
      "schemaDefinition": {
        "version": 1,
        "fields": [
          { "id": "title", "type": "text", "label": "제목", "required": true },
          {
            "id": "background",
            "type": "textarea",
            "label": "품의 배경",
            "required": true,
            "config": { "maxLength": 2000, "placeholder": "예: 현재 시스템의 한계와 개선 필요성을 기술" }
          },
          {
            "id": "proposal",
            "type": "textarea",
            "label": "제안 내용",
            "required": true,
            "config": { "maxLength": 2000, "placeholder": "구체적인 제안 사항을 기술" }
          },
          {
            "id": "expectedEffect",
            "type": "textarea",
            "label": "예상 효과",
            "required": true,
            "config": { "maxLength": 2000, "placeholder": "정성적·정량적 기대 효과를 기술" }
          }
        ],
        "conditionalRules": [],
        "calculationRules": []
      }
    }
    ```

    **결정 근거 노트:**
    - `prefix="PRP"` per D-B2 — Phase 26 D-10 의 prefix='' reset 정책으로 사용자 입력 의무 (export/import fallback 용도)
    - `category="general"` per D-B3
    - `icon="FileSignature"` per D-B4 — Plan 03 의 ICON_MAP 매핑과 동기 (lucide-react v1.7 export 검증됨)
    - **첨부 필드 미포함** per D-B6 — `document_attachment` 테이블 + Google Drive 업로드가 문서-차원 처리. 양식 schema 와 첨부 인프라 관심사 분리
    - 모든 textarea 의 `config.placeholder` 는 Discretion (CONTEXT 영역, fieldConfigSchema L62 placeholder optional 허용)
    - `config.maxLength: 2000` 모든 textarea 적용 — 품의서 정성적 의사결정 문서의 합리적 상한 (D-B5)
    - `conditionalRules: []`, `calculationRules: []` per D-B7 — 정성적 문서, 자동 분기/계산 부재

    **금지 사항 (Anti-Pattern):**
    - 알려지지 않은 키 추가 금지 (`maxLen`, `max_length`, `__proto__`, `constructor` 등) — `.strict()` 가 즉시 throw
    - 첨부 필드 (file/attachment 타입) 추가 금지 — fieldTypeSchema 에 미포함이며 D-B6 정책 위배
    - 정량 효과 expansion (table[효과항목/금액] + estimatedBenefit calculationRule) 추가 금지 — Deferred (CONTEXT)
    - field 의 `required` 키 누락 금지
  </action>
  <verify>
    <automated>cd frontend && npm test -- features/admin/presets/presets.test.ts --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f frontend/src/features/admin/presets/proposal.json`
    - JSON 유효성: `node -e "JSON.parse(require('fs').readFileSync('frontend/src/features/admin/presets/proposal.json','utf8'))"` 무에러 종료
    - vitest `presets > each preset passes templateImportSchema` 단언이 proposal 포함하여 통과
    - grep 검증: `grep -c '"type": "textarea"' frontend/src/features/admin/presets/proposal.json` 결과 = 3
    - grep 검증: `grep -c '"maxLength": 2000' frontend/src/features/admin/presets/proposal.json` 결과 = 3
    - grep 검증: `grep -c '"prefix": "PRP"' frontend/src/features/admin/presets/proposal.json` 결과 = 1
    - grep 검증: `grep -c '"icon": "FileSignature"' frontend/src/features/admin/presets/proposal.json` 결과 = 1
    - grep 검증: `grep -c '"category": "general"' frontend/src/features/admin/presets/proposal.json` 결과 = 1
    - grep 검증: `grep -c '"id": "title"\|"id": "background"\|"id": "proposal"\|"id": "expectedEffect"' frontend/src/features/admin/presets/proposal.json` 결과 = 4
    - grep 검증 첨부 부재: `grep -c '"type": "file"\|"type": "attachment"' frontend/src/features/admin/presets/proposal.json` 결과 = 0
    - grep 검증: `grep -c 'conditionalRules": \[\]' frontend/src/features/admin/presets/proposal.json` 결과 = 1
    - grep 검증: `grep -c 'calculationRules": \[\]' frontend/src/features/admin/presets/proposal.json` 결과 = 1
  </acceptance_criteria>
  <done>proposal.json 파일이 존재하고, vitest `each preset passes templateImportSchema` 단언이 proposal 포함하여 green. 4 개 fields (title + 3 textarea), 첨부 필드 0건.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 외부 (admin import) → frontend build | 사용자 import JSON 과 동일 schema 통과 의무 — proposal.json 도 동일 게이트 |
| frontend module load → React runtime | Zod parse 통과한 preset 만 PresetGallery 노출 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | proposal.json (in-tree preset JSON) | mitigate | `templateImportSchema.strict()` (Phase 26 T-26-01/T-26-04 재사용) 가 build-time 에 unknown keys 차단. `vite build` 또는 `npm test -- presets` 가 즉시 ZodError throw. 신규 코드 zero — 인프라 자동 보호. textarea field 의 `maxLength: 2000` 은 사용자 입력 길이 상한 (XSS 표면 축소 보조 효과) |
| T-32-02 | N/A | proposal.json | n/a | proposal.json 은 table 필드 부재로 column id 충돌 risk 0. T-32-02 는 회의록 (Plan 01) 한정 |

</threat_model>

<verification>
- `cd frontend && npm test -- features/admin/presets --run` — `each preset passes templateImportSchema` 단언이 proposal 포함하여 통과
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — JSON import 타입 호환 확인
- grep 단언 (acceptance_criteria 참고)
</verification>

<success_criteria>
1. `frontend/src/features/admin/presets/proposal.json` 파일 존재 + JSON 유효
2. `templateImportSchema.parse()` 통과
3. 4 개 fields (title, background, proposal, expectedEffect), 3 개 textarea fields, prefix='PRP', icon='FileSignature', category='general'
4. 모든 textarea 에 config.maxLength=2000
5. 첨부 필드 0건 (D-B6 정책 준수)
6. conditionalRules/calculationRules 모두 빈 배열
</success_criteria>

<output>
After completion, create `.planning/phases/32-custom/32-02-SUMMARY.md`.
</output>
