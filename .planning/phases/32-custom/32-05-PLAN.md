---
phase: 32-custom
plan: 05
type: execute
wave: 2
gap_closure: false
depends_on:
  - 32-01
  - 32-02
files_modified:
  - frontend/src/features/admin/presets/presets.test.ts
autonomous: true
requirements:
  - FORM-01
  - FORM-02
user_setup: []

must_haves:
  truths:
    - "presets.test.ts 의 length 단언이 6 으로 업데이트된다"
    - "keys 단언이 ['expense','leave','meeting','proposal','purchase','trip'] alphabetical 정렬로 업데이트된다"
    - "신규 단언 'meeting preset has 5 fields' 가 추가된다"
    - "신규 단언 'proposal preset has 4 fields' 가 추가된다"
    - "기존 단언 (expense calculationRule, leave conditionalRule, purchase table field, all preset names Korean, each preset passes templateImportSchema) preserve"
    - "vitest 가 6/6 (또는 기존 + 2 신규 = 9개) green"
  artifacts:
    - path: "frontend/src/features/admin/presets/presets.test.ts"
      provides: "Phase 32 의 명시적 게이트 — 새 preset 추가 시 무의식 회귀 차단"
      contains: "meeting preset has 5 fields"
  key_links:
    - from: "frontend/src/features/admin/presets/presets.test.ts"
      to: "frontend/src/features/admin/presets/index.ts (presets array)"
      via: "import { presets } from './index'"
      pattern: "presets.find.*key === 'meeting'"
    - from: "frontend/src/features/admin/presets/presets.test.ts"
      to: "frontend/src/features/admin/presets/meeting.json + proposal.json (Plan 01/02 결과)"
      via: "presets.find((p) => p.key === 'meeting').data.schemaDefinition.fields"
      pattern: "toHaveLength\\(5\\)\\|toHaveLength\\(4\\)"
---

<objective>
presets.test.ts 의 length / keys 단언을 4→6 / 신규 alphabetical sort 결과로 업데이트하고, meeting/proposal 의 fields 개수·id 단언 2 개를 추가하여, vitest 가 신규 2 preset 의 구조를 명시적 게이트로 검증한다.

Purpose: Phase 26 D-D1 결정 — preset 추가는 자동 인식이지만 테스트 단언은 의도된 명시 단계 (무의식 추가 시 fail). FORM-01/02 의 schema 형태가 변하지 않음을 회귀 보호.
Output: presets.test.ts 의 length 단언 + keys 단언 업데이트 + 2 신규 단언 추가 (단일 파일).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-custom/32-CONTEXT.md
@.planning/phases/32-custom/32-RESEARCH.md
@.planning/phases/32-custom/32-PATTERNS.md
@.planning/phases/32-custom/32-VALIDATION.md
@./CLAUDE.md

# Direct edit target
@frontend/src/features/admin/presets/presets.test.ts

# Loaded preset data (의존)
@frontend/src/features/admin/presets/index.ts

<interfaces>
<!-- 현재 presets.test.ts 상태 (VERIFIED 2026-04-25) — 정확한 수정 위치 -->

L5-13 (length / keys 단언 — 둘 다 업데이트):
```typescript
describe('presets', () => {
  it('contains exactly 4 presets', () => {
    expect(presets).toHaveLength(4);
  });

  it('has expected keys', () => {
    const keys = presets.map((p) => p.key).sort();
    expect(keys).toEqual(['expense', 'leave', 'purchase', 'trip']);
  });
```

L25-39 (preset-specific 단언 — 모두 preserve, 끝부분에 2 신규 단언 추가):
```typescript
  it('expense preset has at least one calculationRule', () => { ... });
  it('leave preset has at least one conditionalRule', () => { ... });
  it('purchase preset has at least one table field', () => { ... });
```

L41-45 (cross-cutting 단언 — preserve, 자동으로 6 preset 모두 적용):
```typescript
  it('all preset names are Korean', () => {
    for (const p of presets) {
      expect(p.data.name).toMatch(/[가-힣]/);
    }
  });
});
```

**Sort 결과 검증 (presets/index.ts L30 의 localeCompare):**
- alphabetical: expense → leave → meeting → proposal → purchase → trip
- 실제 keys.sort() 결과도 이와 동일 (JS 의 string.sort() 도 lexicographic)

**presets.find 의 non-null assertion 패턴 (`!`) — 기존 단언 100% mimicking:**
```typescript
const meeting = presets.find((p) => p.key === 'meeting')!;
expect(meeting.data.schemaDefinition.fields).toHaveLength(5);
const ids = meeting.data.schemaDefinition.fields.map((f) => f.id);
expect(ids).toEqual(['title', 'meetingDate', 'attendees', 'agenda', 'decisions']);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: presets.test.ts length/keys 단언 업데이트 + 2 신규 단언 추가</name>
  <files>frontend/src/features/admin/presets/presets.test.ts</files>
  <read_first>
    - frontend/src/features/admin/presets/presets.test.ts (현재 단언 패턴 — 신규 단언이 동일 형식 mimicking)
    - frontend/src/features/admin/presets/index.ts (presets array 의 sort 순서 = localeCompare)
    - frontend/src/features/admin/presets/meeting.json (Plan 01 결과 — fields 개수/id 단언 정확 일치)
    - frontend/src/features/admin/presets/proposal.json (Plan 02 결과 — fields 개수/id 단언 정확 일치)
  </read_first>
  <action>
    presets.test.ts 의 다음 3 위치를 업데이트:

    **수정 1 — L6-8 length 단언:**
    `4` → `6`, 설명 문자열도 동기 업데이트.

    수정 후:
    ```typescript
    it('contains exactly 6 presets', () => {
      expect(presets).toHaveLength(6);
    });
    ```

    **수정 2 — L10-13 keys 단언:**
    `['expense', 'leave', 'purchase', 'trip']` → `['expense', 'leave', 'meeting', 'proposal', 'purchase', 'trip']` (alphabetical sort 결과).

    수정 후:
    ```typescript
    it('has expected keys', () => {
      const keys = presets.map((p) => p.key).sort();
      expect(keys).toEqual([
        'expense',
        'leave',
        'meeting',
        'proposal',
        'purchase',
        'trip',
      ]);
    });
    ```

    **수정 3 — L39 (purchase 단언 다음) 에 2 신규 단언 추가:**
    L41 의 'all preset names are Korean' 단언 직전에 삽입. 기존 단언 형식 100% mimicking (`presets.find(...)!` non-null assertion + `toHaveLength` + `map((f) => f.id)` + `toEqual`).

    추가 내용:
    ```typescript
      it('meeting preset has 5 fields', () => {
        const meeting = presets.find((p) => p.key === 'meeting')!;
        expect(meeting.data.schemaDefinition.fields).toHaveLength(5);
        const ids = meeting.data.schemaDefinition.fields.map((f) => f.id);
        expect(ids).toEqual(['title', 'meetingDate', 'attendees', 'agenda', 'decisions']);
      });

      it('proposal preset has 4 fields', () => {
        const proposal = presets.find((p) => p.key === 'proposal')!;
        expect(proposal.data.schemaDefinition.fields).toHaveLength(4);
        const ids = proposal.data.schemaDefinition.fields.map((f) => f.id);
        expect(ids).toEqual(['title', 'background', 'proposal', 'expectedEffect']);
      });
    ```

    **변경 금지 영역 (CLAUDE.md preserve):**
    - L1-3 (import statements) 변경 금지
    - L25-39 (expense calculationRule / leave conditionalRule / purchase table field 단언) 변경 금지 — 회귀 가드
    - L15-23 (each preset passes templateImportSchema 단언) 변경 금지 — 자동으로 6 preset 모두 검증
    - L41-45 (all preset names are Korean 단언) 변경 금지 — 회의록/품의서 모두 한글로 자동 통과

    **검증 의무:**
    수정 후 `cd frontend && npm test -- features/admin/presets/presets.test.ts --run` 실행. 다음 단언이 모두 green:
    - 'contains exactly 6 presets'
    - 'has expected keys' (sort 결과 6 entry)
    - 'each preset passes templateImportSchema' (자동으로 meeting + proposal 포함)
    - 'expense preset has at least one calculationRule' (preserve)
    - 'leave preset has at least one conditionalRule' (preserve)
    - 'purchase preset has at least one table field' (preserve)
    - 'meeting preset has 5 fields' (신규)
    - 'proposal preset has 4 fields' (신규)
    - 'all preset names are Korean' (preserve, 자동 6 preset)

    총 9 단언 모두 통과.
  </action>
  <verify>
    <automated>cd frontend && npm test -- features/admin/presets/presets.test.ts --run 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - vitest 종료코드 0
    - vitest 출력에 `9 passed` 또는 동등 메시지 (9 단언 모두 green)
    - grep 검증 (length 업데이트): `grep -c "expect(presets).toHaveLength(6)" frontend/src/features/admin/presets/presets.test.ts` 결과 = 1
    - grep 검증 (length 4 미잔존): `grep -c "expect(presets).toHaveLength(4)" frontend/src/features/admin/presets/presets.test.ts` 결과 = 0
    - grep 검증 (keys 6 entry): `grep -c "'meeting'\|'proposal'" frontend/src/features/admin/presets/presets.test.ts` 결과 >= 4 (keys 단언 2건 + 신규 단언 안 find 호출 2건)
    - grep 검증 (신규 단언 1): `grep -c "meeting preset has 5 fields" frontend/src/features/admin/presets/presets.test.ts` 결과 = 1
    - grep 검증 (신규 단언 2): `grep -c "proposal preset has 4 fields" frontend/src/features/admin/presets/presets.test.ts` 결과 = 1
    - grep 검증 (id 배열 단언): `grep -c "title.*meetingDate.*attendees.*agenda.*decisions" frontend/src/features/admin/presets/presets.test.ts` 결과 = 1
    - grep 검증 (id 배열 단언): `grep -c "title.*background.*proposal.*expectedEffect" frontend/src/features/admin/presets/presets.test.ts` 결과 = 1
    - grep 검증 (기존 단언 preserve): `grep -c "expense preset has at least one calculationRule\|leave preset has at least one conditionalRule\|purchase preset has at least one table field\|all preset names are Korean\|each preset passes templateImportSchema" frontend/src/features/admin/presets/presets.test.ts` 결과 = 5
  </acceptance_criteria>
  <done>vitest 9 단언 모두 green. length/keys 업데이트 완료, meeting/proposal 신규 단언 2건 추가, 기존 5 단언 preserve.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI build → preset registry | presets.test.ts 가 빌드 시점 게이트 — meeting/proposal 의 fields 개수·id 가 변경될 경우 즉시 fail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | mitigate (간접) | presets.test.ts | mitigate | `each preset passes templateImportSchema` 단언이 자동으로 meeting/proposal 도 .strict() Zod 검증 — 키 오타 시 fail. 본 plan 의 단언 업데이트가 이 게이트를 6 preset 으로 확장 |
| Regression — 기존 단언 손상 | Tampering | presets.test.ts (L25-45) | mitigate | acceptance_criteria 의 "기존 단언 preserve" grep (5 건) — calculationRule/conditionalRule/table field/Korean names/strict parse 단언 보존 검증 |

</threat_model>

<verification>
- `cd frontend && npm test -- features/admin/presets/presets.test.ts --run` — 9 단언 모두 green
- grep 단언 (acceptance_criteria 참고)
- 회귀 의도 확인: Plan 01/02 의 meeting.json/proposal.json 의 fields 개수가 변하면 본 단언이 즉시 fail
</verification>

<success_criteria>
1. vitest `cd frontend && npm test -- features/admin/presets/presets.test.ts --run` green (9 단언)
2. length 단언 4 → 6 업데이트
3. keys 단언 6 entry alphabetical sort 결과 정확 일치
4. meeting preset has 5 fields + ids 정확 일치 단언 추가
5. proposal preset has 4 fields + ids 정확 일치 단언 추가
6. 기존 5 단언 preserve (calculationRule/conditionalRule/table field/Korean names/strict parse)
</success_criteria>

<output>
After completion, create `.planning/phases/32-custom/32-05-SUMMARY.md`.
</output>
