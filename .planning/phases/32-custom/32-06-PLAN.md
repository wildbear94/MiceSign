---
phase: 32-custom
plan: 06
type: execute
wave: 3
gap_closure: false
depends_on:
  - 32-01
  - 32-02
  - 32-03
  - 32-04
  - 32-05
files_modified:
  - .planning/phases/32-custom/32-HUMAN-UAT.md
autonomous: false
requirements:
  - FORM-01
  - FORM-02
user_setup: []

must_haves:
  truths:
    - "frontend full vitest suite green (presets.test.ts 9 단언 + 기존 모든 단언)"
    - "frontend production build (npm run build) green — Vite + Zod + tsc 모두 통과"
    - "frontend tsc --noEmit green (PresetGallery.tsx 의 lucide-react import + I18N_MAP 타입)"
    - "관리자 PresetGallery 에 6 카드 노출 (회의록 + 품의서 + 기존 4) — 한국어 라벨 정상 표시"
    - "관리자가 회의록 카드 선택 시 TemplateFormModal 이 prefill 되어 열린다 (prefix 빈 입력란, 5 fields preview)"
    - "관리자가 품의서 카드 선택 시 TemplateFormModal 이 prefill 되어 열린다 (prefix 빈 입력란, 4 fields preview)"
    - "관리자가 양식 저장 후 사용자가 기안 화면에서 회의록/품의서 양식을 선택하면 DynamicCustomForm 이 정상 렌더 (table 행 추가/삭제, textarea 입력 작동)"
    - "T-32-02 검증 — 회의록 root title 입력값과 agenda row title 입력값이 분리 저장됨 (cross-scope id 충돌 없음)"
    - "기존 4 preset 카드 (경비/휴가/출장/구매) 무손상 — 양식 생성 흐름 회귀 0건"
  artifacts:
    - path: ".planning/phases/32-custom/32-HUMAN-UAT.md"
      provides: "수동 UAT 체크리스트 — 6 카드 시각 확인 + 회의록/품의서 end-to-end 기안 흐름"
      contains: "회의록"
  key_links:
    - from: "Plan 01-05 의 모든 결과물"
      to: ".planning/phases/32-custom/32-HUMAN-UAT.md"
      via: "통합 검증 단계 — automated suite + manual UAT"
      pattern: "FORM-01\\|FORM-02"
---

<objective>
Phase 32 의 모든 자동 검증 (vitest full suite + Vite build + tsc) 을 한 번에 실행하여 통합 회귀 0건을 확인하고, `.planning/phases/32-custom/32-HUMAN-UAT.md` 체크리스트를 통해 회의록/품의서 양식의 end-to-end 사용자 흐름 (관리자 양식 추가 → 프리셋 선택 → 양식 저장 → 사용자 기안 → DynamicCustomForm 렌더) 을 시각 확인한다.

Purpose: FORM-01/FORM-02 의 phase gate — 모든 자동 단언 + 시각 확인이 동시에 통과해야 phase 종결. T-32-02 의 cross-scope id 충돌 가설을 manual UAT 로 최종 검증.
Output: 32-HUMAN-UAT.md (신규 체크리스트 파일) + 모든 자동 검증 green 확인 로그.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-custom/32-CONTEXT.md
@.planning/phases/32-custom/32-RESEARCH.md
@.planning/phases/32-custom/32-PATTERNS.md
@.planning/phases/32-custom/32-VALIDATION.md
@./CLAUDE.md

# Plan 01-05 결과 (의존)
@.planning/phases/32-custom/32-01-PLAN.md
@.planning/phases/32-custom/32-02-PLAN.md
@.planning/phases/32-custom/32-03-PLAN.md
@.planning/phases/32-custom/32-04-PLAN.md
@.planning/phases/32-custom/32-05-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 통합 자동 검증 — full vitest + build + tsc 한 번에 실행</name>
  <files>(없음 — 검증 only)</files>
  <read_first>
    - .planning/phases/32-custom/32-VALIDATION.md (Test Infrastructure 섹션 — 명령 정확 일치)
    - frontend/package.json (scripts.test, scripts.build 명령 확인)
  </read_first>
  <action>
    다음 3 명령을 순차 실행하여 모두 green 인지 확인. 각 명령 출력의 마지막 30 줄을 캡처하여 SUMMARY.md 에 인용.

    ```bash
    cd frontend && npm test --run 2>&1 | tail -40
    ```
    ↓ green 확인 후

    ```bash
    cd frontend && npm run build 2>&1 | tail -40
    ```
    ↓ green 확인 후

    ```bash
    cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | tail -20
    ```

    **green 정의:**
    - `npm test --run`: 종료코드 0, presets.test.ts 의 9 단언 포함 모든 vitest 파일 통과 (기존 v1.0/v1.1 의 다른 테스트 파일 회귀 0건)
    - `npm run build`: 종료코드 0, vite build 성공 (templateImportSchema.parse() 가 6 preset 모두 통과 + tsc 통과 + bundling 완료)
    - `npx tsc --noEmit`: 종료코드 0, PresetGallery.tsx 의 lucide-react import + I18N_MAP 타입 모두 통과

    **fail 시 의무:**
    - `npm test` fail → 어떤 단언이 fail 인지 확인. preset 관련이면 Plan 01/02/05 결과 점검. 다른 v1.0/v1.1 테스트면 Plan 03/04 의 PresetGallery/i18n 수정이 회귀 유발 — 즉시 stop, 회귀 분석
    - `npm run build` fail → ZodError 또는 tsc 에러 확인. ZodError 면 meeting.json/proposal.json 의 키 오타 (Plan 01/02 점검). tsc 에러면 PresetGallery 의 lucide-react import 문제 (Plan 03 의 가정 위배)
    - `npx tsc --noEmit` fail → PresetGallery 또는 I18N_MAP 타입 mismatch — Plan 03 점검

    **결과 기록:**
    Plan 06 의 SUMMARY.md 에 3 명령의 출력 마지막 줄 (e.g., "Test Files  X passed (X)", "✓ built in Xs", tsc 무에러) 인용.
  </action>
  <verify>
    <automated>cd frontend && npm test --run 2>&1 | tail -10 && cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign && cd frontend && npm run build 2>&1 | tail -10 && cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign && cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | tail -5 && echo "ALL THREE GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test --run` 종료코드 0
    - `npm run build` 종료코드 0
    - `npx tsc --noEmit -p tsconfig.app.json` 종료코드 0
    - vitest 출력에 presets.test.ts 의 9 단언 green 확인 (또는 동등)
    - vite build 출력에 ZodError 없음
    - tsc 출력에 lucide-react / I18N_MAP / PresetGallery 관련 에러 없음
  </acceptance_criteria>
  <done>3 자동 검증 모두 green. 출력 로그 캡처 완료.</done>
</task>

<task type="auto">
  <name>Task 2: 32-HUMAN-UAT.md 체크리스트 작성 — 6 카드 + 회의록/품의서 end-to-end 흐름</name>
  <files>.planning/phases/32-custom/32-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/31-dashboard/31-HUMAN-UAT.md (또는 v1.1 의 다른 phase HUMAN-UAT — 체크리스트 형식 mimicking)
  </read_first>
  <action>
    `.planning/phases/32-custom/32-HUMAN-UAT.md` 신규 파일 작성. 다음 체크리스트 형식 사용:

    ```markdown
    ---
    phase: 32-custom
    type: human-uat
    status: pending
    created: 2026-04-25
    requirements:
      - FORM-01
      - FORM-02
    threat_refs:
      - T-32-01
      - T-32-02
    ---

    # Phase 32 — HUMAN UAT 체크리스트

    > 자동 검증 (vitest + build + tsc) 모두 green 확인 후 본 체크리스트의 모든 항목을 직접 시각/조작 확인.

    **사전 준비:**
    - 백엔드 + 프론트엔드 dev server 기동 (`cd backend && ./gradlew bootRun` + `cd frontend && npm run dev`)
    - SUPER_ADMIN 또는 ADMIN 계정으로 로그인

    ---

    ## 섹션 1 — PresetGallery 6 카드 시각 확인 (T-32-01 자동 차단 + UI 회귀)

    1. [ ] 관리자 메뉴 → "양식 관리" → "신규 양식 추가" 버튼 클릭
    2. [ ] "프리셋 선택" 모달 (PresetGallery) 가 열린다
    3. [ ] **6 개 카드** 가 grid-cols-2 (3행 × 2열) 로 표시된다
    4. [ ] 각 카드의 제목과 설명이 한국어로 정상 표시 (raw key 텍스트 `templates.preset*` 미노출):
       - [ ] 경비신청서 — 항목/단가/수량/합계 테이블과 자동 합계 계산을 포함합니다 (Receipt 아이콘)
       - [ ] 휴가신청서 — 휴가 종류 선택과 기간별 조건부 사유 입력을 포함합니다 (CalendarDays 아이콘)
       - [ ] 출장신청서 — 일정·동행자·예상 비용 계산을 포함합니다 (Plane 아이콘)
       - [ ] 구매신청서 — 품목 테이블과 총액 자동 계산을 포함합니다 (ShoppingCart 아이콘)
       - [ ] **회의록** — 회의 일시·참석자·안건·결정사항을 구조화하여 기록합니다 (**Users** 아이콘)
       - [ ] **품의서** — 품의 배경·제안 내용·예상 효과를 정리합니다 (**FileSignature** 아이콘)
    5. [ ] 모달 닫기 (X 버튼 또는 Escape) 정상 작동

    ---

    ## 섹션 2 — 회의록 양식 생성 + 사용자 기안 (FORM-01 + T-32-02 검증)

    ### 2-A. 관리자 양식 생성
    6. [ ] PresetGallery 에서 **회의록** 카드 클릭
    7. [ ] TemplateFormModal 이 prefill 되어 열린다
       - [ ] 양식명 = "회의록" (D-A1)
       - [ ] **prefix 입력란이 빈 상태** (Phase 26 D-10 의 prefix='' reset)
       - [ ] description = "회의 일시·참석자·안건·결정사항을 구조화하여 기록합니다"
       - [ ] category = general
    8. [ ] FormPreview 패널에 5 개 필드 표시:
       - [ ] 제목 (text, required)
       - [ ] 회의 일시 (date, required)
       - [ ] 참석자 (table, 3 columns: 이름/소속/역할)
       - [ ] 안건 (table, 3 columns: 번호/안건명/설명)
       - [ ] 결정사항 (table, 4 columns: 주제/결정/담당자/기한)
    9. [ ] 사용자가 신규 prefix 입력 (예: "MTG-TEST")
    10. [ ] "저장" 버튼 클릭 → 양식 목록에 회의록(MTG-TEST) row 추가됨

    ### 2-B. 사용자 기안 흐름 + T-32-02 cross-scope id 검증
    11. [ ] 일반 사용자 (또는 같은 SUPER_ADMIN) 으로 "신규 기안" → 회의록 양식 선택
    12. [ ] DynamicCustomForm 정상 렌더 (5 필드 모두 표시)
    13. [ ] **root 제목 (title)** 에 "Q2 기획 회의록" 입력
    14. [ ] 안건 table 의 "행 추가" 버튼 클릭 → 1 행 추가됨
    15. [ ] **agenda row 의 "안건명" (column id "title")** 에 "신제품 출시 일정" 입력
    16. [ ] 회의 일시 입력, 참석자 1 행 입력, 결정사항 1 행 입력
    17. [ ] 임시 저장 → 다시 열기
    18. [ ] **검증 (T-32-02 핵심):**
       - [ ] root title 입력란에 "Q2 기획 회의록" 그대로 표시
       - [ ] agenda row 의 안건명 입력란에 "신제품 출시 일정" 그대로 표시
       - [ ] 두 값이 분리 저장됨 (cross-scope id 충돌 없음, 옵션 A 검증 완료)
    19. [ ] 결재 상신 → 정상 처리

    ---

    ## 섹션 3 — 품의서 양식 생성 + 사용자 기안 (FORM-02)

    20. [ ] PresetGallery 에서 **품의서** 카드 클릭
    21. [ ] TemplateFormModal prefill:
       - [ ] 양식명 = "품의서"
       - [ ] **prefix 입력란이 빈 상태**
       - [ ] description = "품의 배경·제안 내용·예상 효과를 정리합니다"
    22. [ ] FormPreview 패널에 4 개 필드 표시:
       - [ ] 제목 (text, required)
       - [ ] 품의 배경 (textarea, required, placeholder="예: 현재 시스템의 한계와 개선 필요성을 기술", maxLength 2000)
       - [ ] 제안 내용 (textarea, required, placeholder="구체적인 제안 사항을 기술")
       - [ ] 예상 효과 (textarea, required, placeholder="정성적·정량적 기대 효과를 기술")
    23. [ ] **첨부 필드 미포함 확인** (D-B6 — schema field 가 아닌 별도 첨부 UI)
    24. [ ] 신규 prefix 입력 (예: "PRP-TEST") + 저장
    25. [ ] 일반 사용자 기안 흐름 → DynamicCustomForm 정상 렌더
    26. [ ] 3 textarea 모두 입력 + 저장 + 결재 상신 → 정상 처리

    ---

    ## 섹션 4 — 기존 4 preset 회귀 검증 (CLAUDE.md preserve)

    27. [ ] 경비신청서 카드 선택 → calculationRule (SUM(items.amount)) 정상 작동
    28. [ ] 휴가신청서 카드 선택 → conditionalRule (sick → sickReason 표시) 정상 작동
    29. [ ] 출장신청서 카드 선택 → 양식 prefill + 저장 정상
    30. [ ] 구매신청서 카드 선택 → 품목 table + total 계산 정상

    ---

    ## 섹션 5 — Snapshot 불변성 (D-D2)

    31. [ ] 회의록/품의서 양식으로 작성된 기존 문서 (있다면) 가 schema 변경 없이 그대로 표시됨 — `approval_template.schema_definition_snapshot` 자동 보호 확인 (시각 확인 + DB 직접 점검 가능)

    ---

    ## 통과 기준

    - [ ] 모든 항목 (1-31) 체크 완료
    - [ ] T-32-02 (섹션 2-B 의 18번) 명시적 통과 — root title vs agenda.title 분리 저장
    - [ ] 기존 4 preset 회귀 0건 (섹션 4)

    ## Sign-Off

    - 검증자: [이름]
    - 검증일: [YYYY-MM-DD]
    - 결과: [pass / fail (특정 항목)]
    ```

    **결정 근거:**
    - 섹션 1 — RESEARCH.md L660-665 (FORM-01/02 의 manual / E2E 단언)
    - 섹션 2-B 의 18번 — RESEARCH.md Open Question 1 + T-32-02 mitigation 의 핵심 검증 step
    - 섹션 3 의 23번 — D-B6 (첨부 미포함) 시각 확인
    - 섹션 4 — CLAUDE.md "기존 기능/메뉴/라우트 보존" 의무
    - 섹션 5 — D-D2 (snapshot 불변성)

    **금지 사항:**
    - HUMAN-UAT 체크리스트가 자동화 가능한 항목을 포함하지 않을 것 (예: vitest 단언 결과는 Task 1 의 영역)
    - 사용자에게 자동화 가능한 명령 (npm test, build) 을 수동 실행 요청 금지 — Task 1 이 처리
  </action>
  <verify>
    <automated>test -f .planning/phases/32-custom/32-HUMAN-UAT.md && grep -c "^[0-9]\+\. \[ \]" .planning/phases/32-custom/32-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - 파일 존재: `test -f .planning/phases/32-custom/32-HUMAN-UAT.md`
    - frontmatter 에 phase/type/status/requirements/threat_refs 포함
    - grep 검증: `grep -c "FORM-01\|FORM-02" .planning/phases/32-custom/32-HUMAN-UAT.md` 결과 >= 2
    - grep 검증: `grep -c "T-32-02" .planning/phases/32-custom/32-HUMAN-UAT.md` 결과 >= 1
    - grep 검증 (체크박스 개수): `grep -c "^[0-9]\+\. \[ \]\|^   \- \[ \]" .planning/phases/32-custom/32-HUMAN-UAT.md` 결과 >= 31
    - grep 검증 (회의록/품의서 시각 확인 항목): `grep -c "회의록\|품의서" .planning/phases/32-custom/32-HUMAN-UAT.md` 결과 >= 6
    - grep 검증 (기존 preset 회귀 섹션): `grep -c "경비신청서\|휴가신청서\|출장신청서\|구매신청서" .planning/phases/32-custom/32-HUMAN-UAT.md` 결과 >= 4
  </acceptance_criteria>
  <done>32-HUMAN-UAT.md 작성 완료 — 31+ 체크박스, FORM-01/02 명시, T-32-02 검증 step (섹션 2-B 18번) 포함, 기존 preset 회귀 섹션 포함.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 32-HUMAN-UAT.md 체크리스트 모든 항목 수동 실행 + Sign-off</name>
  <what-built>Plan 01-05 + Task 1 의 자동 검증이 완료된 회의록/품의서 양식 추가. PresetGallery 에 6 카드 노출, 사용자 기안 흐름 정상 작동.</what-built>
  <how-to-verify>
    `.planning/phases/32-custom/32-HUMAN-UAT.md` 의 31+ 항목을 순서대로 실행:

    1. 백엔드 + 프론트엔드 dev server 기동
    2. SUPER_ADMIN 으로 로그인
    3. 섹션 1 (PresetGallery 6 카드 시각 확인) — 모든 항목 체크
    4. 섹션 2-A/2-B (회의록 양식 생성 + 사용자 기안 + T-32-02 검증) — 18번 (root title vs agenda.title 분리) 명시 확인
    5. 섹션 3 (품의서 양식 생성 + 사용자 기안)
    6. 섹션 4 (기존 4 preset 회귀 검증)
    7. 섹션 5 (snapshot 불변성)

    체크리스트의 모든 항목 통과 시 32-HUMAN-UAT.md 의 status frontmatter 를 `pending` → `complete` 로 변경 + Sign-Off 섹션 채움 (검증자 이름 / 검증일 / 결과=pass).

    **fail 항목 발견 시:**
    - PresetGallery 카드 누락 → Plan 01/02/03/04 점검
    - i18n raw key 표시 → Plan 04 점검
    - 회의록 root title vs agenda.title 충돌 (T-32-02 fail) → 옵션 B (agenda.columns[1].id 를 'subject' 로 rename) 적용 — Plan 01/05 후속 수정 필요. 즉시 stop, 32-HUMAN-UAT.md 에 fail 기록.
    - 기존 4 preset 회귀 → Plan 03 (PresetGallery 수정) 또는 Plan 04 (ko/admin.json) 손상 — 점검 + 수정
  </how-to-verify>
  <resume-signal>"approved" 또는 "32-HUMAN-UAT pass — 31/31 체크 완료 + Sign-off" 또는 fail 항목 명시 (예: "T-32-02 fail at section 2-B step 18 — agenda.title 충돌, 옵션 B 적용 필요")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 자동 검증 → manual UAT | automated suite 통과 후에도 시각/조작 확인 영역 존재 (DOM 렌더, 사용자 입력 path, T-32-02 cross-scope) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | mitigate (재확인) | meeting.json/proposal.json (Plan 01/02 결과) | mitigate | Task 1 의 vite build 가 templateImportSchema.parse() 를 6 preset 모두 실행 — Zod .strict() 통과 재확인 |
| T-32-02 | mitigate (manual UAT 확정) | DynamicCustomForm 의 react-hook-form path namespace | mitigate | Task 3 의 섹션 2-B step 18 — root title 입력값과 agenda row 의 안건명 입력값이 분리 저장되는지 직접 확인. fail 시 옵션 B (rename to 'subject') 적용 후 Plan 01/05 후속 수정 |
| Regression — 기존 4 preset 양식 회귀 | Tampering | PresetGallery + ko/admin.json | mitigate | Task 3 섹션 4 (4 항목 — 경비/휴가/출장/구매 카드 선택 + 양식 흐름 정상) |

</threat_model>

<verification>
- Task 1: 자동 3 명령 모두 green (vitest + build + tsc)
- Task 2: 32-HUMAN-UAT.md 작성 + 31+ 체크박스
- Task 3 (checkpoint): 31+ 항목 수동 실행 + Sign-off
</verification>

<success_criteria>
1. 자동: vitest full suite + vite build + tsc --noEmit 모두 green
2. 자동: presets.test.ts 9 단언 green (Plan 05 결과 검증)
3. 자동: vite build 가 6 preset 모두 templateImportSchema.parse() 통과 (Plan 01/02 결과 검증)
4. 수동: PresetGallery 6 카드 시각 노출 + 한국어 라벨 정상 (raw key 미노출)
5. 수동: 회의록/품의서 각각 양식 생성 → 사용자 기안 → DynamicCustomForm 정상 렌더 + 결재 상신 정상
6. 수동: T-32-02 검증 — root title vs agenda.title 분리 저장 확인 (옵션 A 안전성 확정)
7. 수동: 기존 4 preset 회귀 0건 (CLAUDE.md preserve)
8. 32-HUMAN-UAT.md status: pending → complete, Sign-off 작성
</success_criteria>

<output>
After completion, create `.planning/phases/32-custom/32-06-SUMMARY.md` containing:
- Task 1 의 3 명령 출력 마지막 줄 인용
- Task 2 의 32-HUMAN-UAT.md 항목 개수
- Task 3 의 Sign-off 결과 (pass/fail)
- T-32-02 검증 결론 (옵션 A 확정 또는 옵션 B 전환 필요성)
- Phase 32 종결 선언 (FORM-01, FORM-02 모두 satisfied)
</output>
