---
phase: 32-custom
plan: 03
type: execute
wave: 2
gap_closure: false
depends_on:
  - 32-01
  - 32-02
files_modified:
  - frontend/src/features/admin/components/PresetGallery.tsx
autonomous: true
requirements:
  - FORM-01
  - FORM-02
user_setup: []

must_haves:
  truths:
    - "PresetGallery.tsx 의 ICON_MAP 이 6 개 키 (expense/leave/trip/purchase/meeting/proposal) 매핑한다"
    - "PresetGallery.tsx 의 I18N_MAP 이 6 개 키 매핑한다 (각각 nameKey/descKey 쌍)"
    - "Users + FileSignature 가 lucide-react 에서 import 된다 (TypeScript 타입 검증 통과)"
    - "기존 4 entry (expense/leave/trip/purchase) preserve — 손상 0건"
    - "PresetGallery.tsx 의 grid-cols-2 / max-h-80vh 무수정 (D-C3)"
  artifacts:
    - path: "frontend/src/features/admin/components/PresetGallery.tsx"
      provides: "PresetGallery UI — meeting/proposal 카드 노출 통합"
      contains: "meeting: Users"
  key_links:
    - from: "frontend/src/features/admin/components/PresetGallery.tsx (ICON_MAP)"
      to: "lucide-react (Users, FileSignature exports)"
      via: "import { Users, FileSignature, ... } from 'lucide-react'"
      pattern: "import.*lucide-react"
    - from: "frontend/src/features/admin/components/PresetGallery.tsx (I18N_MAP)"
      to: "frontend/public/locales/ko/admin.json (templates.preset*)"
      via: "useTranslation('admin') + t(i18n.nameKey)"
      pattern: "templates.presetMeetingName\\|templates.presetProposalName"
---

<objective>
PresetGallery.tsx 의 lucide-react import block + ICON_MAP + I18N_MAP 에 회의록(meeting) + 품의서(proposal) 2 entry 씩 추가하여, Plan 01/02 가 작성한 신규 preset JSON 이 admin 갤러리 카드로 자동 표시되도록 통합한다.

Purpose: FORM-01/FORM-02 의 UI 노출 게이트 — preset 데이터는 Vite glob 으로 자동 인식되지만 카드 표시는 ICON/I18N 명시 필요 (자동 생성 시 design system 충돌 위험으로 의도적 명시).
Output: PresetGallery.tsx 의 3 곳 (import / ICON_MAP / I18N_MAP) 수정.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-custom/32-CONTEXT.md
@.planning/phases/32-custom/32-RESEARCH.md
@.planning/phases/32-custom/32-PATTERNS.md
@.planning/phases/32-custom/32-VALIDATION.md
@./CLAUDE.md

# Direct edit target
@frontend/src/features/admin/components/PresetGallery.tsx

<interfaces>
<!-- 현재 PresetGallery.tsx 상태 (VERIFIED 2026-04-25) — 정확한 수정 위치 -->

L3-11 (현재 import block — ShoppingCart 다음에 Users, LayoutTemplate 직전에 FileSignature 추가):
```typescript
import {
  X,
  Receipt,
  CalendarDays,
  Plane,
  ShoppingCart,
  LayoutTemplate,
  type LucideIcon,
} from 'lucide-react';
```

L28-33 (현재 ICON_MAP — 끝에 2 entry 추가):
```typescript
const ICON_MAP: Record<string, LucideIcon> = {
  expense: Receipt,
  leave: CalendarDays,
  trip: Plane,
  purchase: ShoppingCart,
};
```

L36-41 (현재 I18N_MAP — 끝에 2 entry 추가):
```typescript
const I18N_MAP: Record<string, { nameKey: string; descKey: string }> = {
  expense: { nameKey: 'templates.presetExpenseName', descKey: 'templates.presetExpenseDesc' },
  leave: { nameKey: 'templates.presetLeaveName', descKey: 'templates.presetLeaveDesc' },
  trip: { nameKey: 'templates.presetTripName', descKey: 'templates.presetTripDesc' },
  purchase: { nameKey: 'templates.presetPurchaseName', descKey: 'templates.presetPurchaseDesc' },
};
```

L91-98 (변경 금지 — fallback chain 자동 통합):
```typescript
<div className="grid grid-cols-2 gap-4">  {/* D-C3: grid 무수정 */}
  {presets.map((preset) => {
    const Icon = ICON_MAP[preset.key] ?? LayoutTemplate;
    const i18n = I18N_MAP[preset.key];
    const displayName = i18n ? t(i18n.nameKey) : preset.data.name;
    const displayDesc = i18n ? t(i18n.descKey) : preset.data.description ?? '';
    // ...
  })}
</div>
```

**lucide-react 1.7.0 export 검증 (VERIFIED 2026-04-25):**
- `Users` — declared in `frontend/node_modules/lucide-react/dist/lucide-react.d.ts` ✓
- `FileSignature` — declared, alias `FileSignatureIcon` 도 존재 ✓
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PresetGallery.tsx 에 Users/FileSignature import + ICON_MAP/I18N_MAP 4 entry 추가 — 3 위치 동시 수정</name>
  <files>frontend/src/features/admin/components/PresetGallery.tsx</files>
  <read_first>
    - frontend/src/features/admin/components/PresetGallery.tsx (현재 상태 — Edit tool 의 안전 사용 의무)
    - frontend/src/features/admin/presets/index.ts (import 경로 확인 — `../presets`)
  </read_first>
  <action>
    PresetGallery.tsx 를 다음 3 위치에서 정확히 수정. 기존 4 entry 와 컴포넌트 본문 (L43~) 은 절대 변경 금지 (CLAUDE.md "기존 기능/메뉴/라우트 보존" 의무).

    **수정 1 — import block (L3-11):**
    `ShoppingCart,` 다음 줄에 `Users,` 추가, 그 다음 줄에 `FileSignature,` 추가. `LayoutTemplate,` 와 `type LucideIcon,` 순서 preserve.

    수정 후 결과:
    ```typescript
    import {
      X,
      Receipt,
      CalendarDays,
      Plane,
      ShoppingCart,
      Users,
      FileSignature,
      LayoutTemplate,
      type LucideIcon,
    } from 'lucide-react';
    ```

    **수정 2 — ICON_MAP (L28-33):**
    `purchase: ShoppingCart,` 다음에 2 entry 추가. 기존 4 entry 순서·줄 형식 그대로.

    수정 후 결과:
    ```typescript
    const ICON_MAP: Record<string, LucideIcon> = {
      expense: Receipt,
      leave: CalendarDays,
      trip: Plane,
      purchase: ShoppingCart,
      meeting: Users,
      proposal: FileSignature,
    };
    ```

    **수정 3 — I18N_MAP (L36-41):**
    `purchase: { ... },` 다음에 2 entry 추가. 키 명명 규약 = `templates.preset{PascalCase}Name/Desc` (Plan 04 의 ko/admin.json 신규 키와 정확 일치).

    수정 후 결과:
    ```typescript
    const I18N_MAP: Record<string, { nameKey: string; descKey: string }> = {
      expense: { nameKey: 'templates.presetExpenseName', descKey: 'templates.presetExpenseDesc' },
      leave: { nameKey: 'templates.presetLeaveName', descKey: 'templates.presetLeaveDesc' },
      trip: { nameKey: 'templates.presetTripName', descKey: 'templates.presetTripDesc' },
      purchase: { nameKey: 'templates.presetPurchaseName', descKey: 'templates.presetPurchaseDesc' },
      meeting: { nameKey: 'templates.presetMeetingName', descKey: 'templates.presetMeetingDesc' },
      proposal: { nameKey: 'templates.presetProposalName', descKey: 'templates.presetProposalDesc' },
    };
    ```

    **변경 금지 영역 (CLAUDE.md preserve):**
    - L1-2 (useEffect/useRef + useTranslation import) 변경 금지
    - L12-13 (presets / TemplateImportData import) 변경 금지
    - L22-26 (PresetGalleryProps interface) 변경 금지
    - L43-129 (PresetGallery 함수 본문) 변경 금지 — fallback chain (L92-98) 이 신규 entry 자동 사용
    - L91 의 `grid-cols-2` 변경 금지 (D-C3) — 6 카드 = 3행 × 2열, modal 자체 스크롤
    - L75 의 `max-w-3xl max-h-[80vh]` 변경 금지

    **검증 의무:**
    수정 직후 `cd frontend && npx tsc --noEmit -p tsconfig.app.json` 실행하여 lucide-react 의 `Users`/`FileSignature` import 가 컴파일 통과하는지 확인. 만약 타입 에러 발생 시 즉시 stop — 본 plan 의 가정 (lucide v1.7 export 가용) 이 깨진 것이며 Plan 03 fallback (`FilePenLine` 또는 `ClipboardCheck` 사용) 협의 필요.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.app.json 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit -p tsconfig.app.json` 종료코드 0
    - grep 검증: `grep -c "^  Users,$" frontend/src/features/admin/components/PresetGallery.tsx` 결과 = 1
    - grep 검증: `grep -c "^  FileSignature,$" frontend/src/features/admin/components/PresetGallery.tsx` 결과 = 1
    - grep 검증: `grep -c "meeting: Users," frontend/src/features/admin/components/PresetGallery.tsx` 결과 = 1
    - grep 검증: `grep -c "proposal: FileSignature," frontend/src/features/admin/components/PresetGallery.tsx` 결과 = 1
    - grep 검증 (i18n keys): `grep -c "templates.presetMeetingName\|templates.presetMeetingDesc" frontend/src/features/admin/components/PresetGallery.tsx` 결과 = 2
    - grep 검증 (i18n keys): `grep -c "templates.presetProposalName\|templates.presetProposalDesc" frontend/src/features/admin/components/PresetGallery.tsx` 결과 = 2
    - grep 검증 (기존 preserve): `grep -c "expense: Receipt,\|leave: CalendarDays,\|trip: Plane,\|purchase: ShoppingCart," frontend/src/features/admin/components/PresetGallery.tsx` 결과 = 4
    - grep 검증 (grid 무수정): `grep -c 'grid grid-cols-2 gap-4' frontend/src/features/admin/components/PresetGallery.tsx` 결과 = 1
    - grep 검증 (modal 무수정): `grep -c 'max-w-3xl max-h-\[80vh\]' frontend/src/features/admin/components/PresetGallery.tsx` 결과 = 1
  </acceptance_criteria>
  <done>tsc --noEmit green, ICON_MAP/I18N_MAP 모두 6 entry, lucide-react 의 Users/FileSignature import 컴파일 통과, 기존 4 entry + grid 레이아웃 무손상.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend build → admin runtime | PresetGallery 가 ICON_MAP/I18N_MAP 으로 lookup — 키 mismatch 시 fallback (LayoutTemplate / preset.data.name) 표시 |
| react-i18next → ko/admin.json | i18n 키 미존재 시 react-i18next 가 키 자체 fallback string 표시 (Plan 04 가 키 추가로 보장) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | N/A | PresetGallery.tsx | n/a | 본 plan 은 ICON/I18N entry 추가 — JSON 검증 게이트 무관 |
| T-32-02 | N/A | PresetGallery.tsx | n/a | T-32-02 는 meeting.json 데이터 layer 한정 |
| Phase-31 카드 손상 (regression) | Tampering | PresetGallery.tsx 기존 4 entry | mitigate | acceptance_criteria 의 "기존 preserve" grep (expense/leave/trip/purchase 4 entry) + grid-cols-2 / max-w-3xl 보존 grep + Plan 06 manual UAT 의 6 카드 시각 확인 |

</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — lucide-react 의 Users/FileSignature 컴파일 통과 + I18N_MAP 의 string literal 형식 통과
- `cd frontend && npm run build 2>&1 | tail -20` — vite build 가 PresetGallery 수정으로 깨지지 않음 (경고/에러 0)
- grep 단언 (acceptance_criteria 참고)
</verification>

<success_criteria>
1. PresetGallery.tsx 의 import / ICON_MAP / I18N_MAP 3 위치에 정확히 4 entry (Users + FileSignature + meeting + proposal) 추가
2. tsc --noEmit green
3. 기존 4 preset entry + grid 레이아웃 무수정 (CLAUDE.md preserve)
4. lucide-react v1.7 의 Users/FileSignature import 가용성 확정 (Open Question 2 해소)
</success_criteria>

<output>
After completion, create `.planning/phases/32-custom/32-03-SUMMARY.md`.
</output>
