---
phase: 05-file-attachments
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - frontend/src/features/document/components/templates/GeneralForm.tsx
  - frontend/src/features/document/components/templates/ExpenseForm.tsx
  - frontend/src/features/document/components/templates/LeaveForm.tsx
  - frontend/src/features/document/pages/DocumentDetailPage.tsx
autonomous: false
requirements: [FILE-01, FILE-02, FILE-03]

must_haves:
  truths:
    - "GeneralForm shows FileAttachmentArea in editor mode replacing the placeholder div"
    - "ExpenseForm shows FileAttachmentArea below expense table replacing the placeholder div"
    - "LeaveForm shows FileAttachmentArea below reason textarea replacing the placeholder div"
    - "DocumentDetailPage shows read-only FileAttachmentArea with download buttons"
    - "No placeholder.attachments text remains in any template form"
  artifacts:
    - path: "frontend/src/features/document/components/templates/GeneralForm.tsx"
      provides: "General form with real attachment area"
      contains: "FileAttachmentArea"
    - path: "frontend/src/features/document/components/templates/ExpenseForm.tsx"
      provides: "Expense form with real attachment area"
      contains: "FileAttachmentArea"
    - path: "frontend/src/features/document/components/templates/LeaveForm.tsx"
      provides: "Leave form with real attachment area"
      contains: "FileAttachmentArea"
    - path: "frontend/src/features/document/pages/DocumentDetailPage.tsx"
      provides: "Detail page with read-only attachment area"
      contains: "FileAttachmentArea"
  key_links:
    - from: "GeneralForm.tsx"
      to: "FileAttachmentArea"
      via: "import and render"
      pattern: "import.*FileAttachmentArea"
    - from: "DocumentDetailPage.tsx"
      to: "FileAttachmentArea"
      via: "import and render with readOnly=true"
      pattern: "<FileAttachmentArea.*readOnly"
---

<objective>
Integrate the FileAttachmentArea component into all three template forms (replacing placeholders) and the document detail page (read-only mode), then verify the complete file attachment flow works end-to-end.

Purpose: Connects the attachment UI built in Plan 02 to the existing document workflow, completing the user-facing file attachment feature.
Output: All four pages show real attachment UI. Checkpoint verifies upload, display, download, delete, and validation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-file-attachments/05-CONTEXT.md
@.planning/phases/05-file-attachments/05-UI-SPEC.md
@.planning/phases/05-file-attachments/05-01-SUMMARY.md
@.planning/phases/05-file-attachments/05-02-SUMMARY.md

@frontend/src/features/document/components/templates/GeneralForm.tsx
@frontend/src/features/document/components/templates/ExpenseForm.tsx
@frontend/src/features/document/components/templates/LeaveForm.tsx
@frontend/src/features/document/pages/DocumentDetailPage.tsx
@frontend/src/features/document/pages/DocumentEditorPage.tsx
@frontend/src/features/document/components/attachment/FileAttachmentArea.tsx

<interfaces>
<!-- FileAttachmentArea props -->
```typescript
interface FileAttachmentAreaProps {
  documentId: number;
  documentStatus: DocumentStatus;
  readOnly: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace attachment placeholders in all template forms and detail page</name>
  <files>
    frontend/src/features/document/components/templates/GeneralForm.tsx,
    frontend/src/features/document/components/templates/ExpenseForm.tsx,
    frontend/src/features/document/components/templates/LeaveForm.tsx,
    frontend/src/features/document/pages/DocumentDetailPage.tsx
  </files>
  <read_first>
    frontend/src/features/document/components/templates/GeneralForm.tsx,
    frontend/src/features/document/components/templates/ExpenseForm.tsx,
    frontend/src/features/document/components/templates/LeaveForm.tsx,
    frontend/src/features/document/pages/DocumentDetailPage.tsx,
    frontend/src/features/document/pages/DocumentEditorPage.tsx,
    frontend/src/features/document/components/attachment/FileAttachmentArea.tsx
  </read_first>
  <action>
**For each of the three template forms (GeneralForm.tsx, ExpenseForm.tsx, LeaveForm.tsx):**

1. Add import at top:
```typescript
import { FileAttachmentArea } from '../attachment/FileAttachmentArea';
```

2. The forms receive `documentId` as a prop (check the component's existing props interface). If `documentId` is not in the props, check how the parent `DocumentEditorPage.tsx` passes it — the document object should be available. The component needs `documentId: number` and `documentStatus: DocumentStatus` (which is 'DRAFT' in editor mode).

3. Replace the placeholder div:
```tsx
{/* Attachment placeholder */}
<div className="border border-dashed border-gray-300 dark:border-gray-600 rounded-lg p-4 text-center text-sm text-gray-400">
  {t('placeholder.attachments')}
</div>
```
With:
```tsx
{documentId && (
  <FileAttachmentArea
    documentId={documentId}
    documentStatus="DRAFT"
    readOnly={false}
  />
)}
```

Note: `documentId` may be undefined for a brand-new document that hasn't been saved yet. In that case, show a message like "문서를 저장한 후 파일을 첨부할 수 있습니다." instead of the FileAttachmentArea. Check if the document has been saved (has an ID) before showing the attachment area.

If the form props don't include `documentId`, thread it through from DocumentEditorPage. The DocumentEditorPage already has the document object from the `useDocument` hook — add `documentId={document?.id}` to the template form props.

**For DocumentDetailPage.tsx:**

1. Add import:
```typescript
import { FileAttachmentArea } from '../components/attachment/FileAttachmentArea';
```

2. Replace the placeholder div:
```tsx
{/* Attachments placeholder */}
<div className="border border-dashed border-gray-300 dark:border-gray-600 rounded-lg p-4 text-center text-sm text-gray-400">
  {t('placeholder.attachments')}
</div>
```
With:
```tsx
<FileAttachmentArea
  documentId={document.id}
  documentStatus={document.status}
  readOnly={true}
/>
```

The document detail page is always read-only for attachments (download only, no upload/delete).
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - GeneralForm.tsx contains `import.*FileAttachmentArea`
    - GeneralForm.tsx does NOT contain `placeholder.attachments`
    - ExpenseForm.tsx contains `import.*FileAttachmentArea`
    - ExpenseForm.tsx does NOT contain `placeholder.attachments`
    - LeaveForm.tsx contains `import.*FileAttachmentArea`
    - LeaveForm.tsx does NOT contain `placeholder.attachments`
    - DocumentDetailPage.tsx contains `import.*FileAttachmentArea`
    - DocumentDetailPage.tsx does NOT contain `placeholder.attachments`
    - DocumentDetailPage.tsx contains `readOnly={true}` or `readOnly`
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>All four pages use the real FileAttachmentArea component. No attachment placeholder text remains anywhere in the codebase. Editor forms show upload/delete mode, detail page shows read-only download mode.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete file attachment flow end-to-end</name>
  <files>none</files>
  <action>Human verifies the complete file attachment feature works visually and functionally.</action>
  <what-built>Complete file attachment system: backend Google Drive integration with upload/download/delete API, frontend drag-and-drop upload with progress bars, file validation (size/count/extension limits), and read-only download view on document detail page.</what-built>
  <how-to-verify>
    1. Start backend: `cd backend && JAVA_HOME=$(/usr/libexec/java_home -v 17) ./gradlew bootRun`
    2. Start frontend: `cd frontend && npm run dev`
    3. Login and navigate to a document editor page (create new or edit draft)
    4. Verify the attachment area shows a drag-and-drop zone with "파일을 여기에 끌어다 놓거나 파일 찾기를 클릭하세요"
    5. Note: If Google Drive Service Account is not configured, upload will return a server error. This is expected — verify the UI handles the error gracefully (shows error message)
    6. Verify the usage status line shows "파일 0/10개, 0KB/200MB 사용 중"
    7. Try selecting a .exe file — should be immediately rejected with "허용되지 않는 파일 형식입니다: .exe"
    8. Navigate to a document detail page — verify read-only attachment area shows (either files with download buttons, or "첨부된 파일이 없습니다.")
    9. Run full backend test suite: `cd backend && JAVA_HOME=$(/usr/libexec/java_home -v 17) ./gradlew test` — all tests pass
  </how-to-verify>
  <verify>Human approval</verify>
  <done>User confirms attachment UI renders correctly in all template forms and detail page. Validation rejects blocked extensions. Backend tests pass.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
```bash
# Frontend compiles
cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npx tsc --noEmit

# No placeholder.attachments text remaining
grep -r "placeholder.attachments" frontend/src/features/document/ || echo "No placeholders remaining"

# Backend tests pass
cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/backend && JAVA_HOME=$(/usr/libexec/java_home -v 17) ./gradlew test 2>&1 | tail -10
```
</verification>

<success_criteria>
- All three template forms show FileAttachmentArea in editor mode
- Document detail page shows read-only FileAttachmentArea
- No placeholder text remains
- Backend tests pass
- Frontend compiles with no errors
- User confirms UI looks correct and validation works
</success_criteria>

<output>
After completion, create `.planning/phases/05-file-attachments/05-03-SUMMARY.md`
</output>
