---
phase: 30
plan: 02
type: execute
wave: 2
pr_bucket: pr1-hotfix
depends_on: ["30-01"]
files_modified:
  - backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java
  - backend/src/test/java/com/micesign/document/DocumentSearchPermissionMatrixTest.java
  - backend/src/test/java/com/micesign/document/DocumentSearchDraftGateTest.java
  - backend/src/test/java/com/micesign/document/DocumentSearchKeywordTest.java
  - backend/src/test/java/com/micesign/document/DocumentSearchStatusFilterTest.java
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-03]

must_haves:
  truths:
    - "사용자 A 가 기안한 SUBMITTED 문서의 approval_line 에 승인자로 등록된 B 는 /documents/search 에서 해당 문서를 받고, 무관한 C 는 결과에 없다 (FSD FN-SEARCH-001 권한 predicate 통과, T-30-01)"
    - "REFERENCE 라인 참조자 D 도 SUBMITTED/APPROVED 문서를 검색 결과에서 찾을 수 있다 (D-A3: line_type/status 제한 없음)"
    - "ADMIN E (같은 부서) 는 부서원의 SUBMITTED/APPROVED 문서를 볼 수 있고, ADMIN F (다른 부서) 는 승인/참조 라인에 등록되지 않은 문서는 볼 수 없다"
    - "SUPER_ADMIN G 는 모든 SUBMITTED/APPROVED 문서를 볼 수 있으나 tab != 'my' 에서는 타인 DRAFT 를 보지 못한다 (D-A4)"
    - "tab=my 는 본인이 기안한 DRAFT 도 포함하고, tab=department / tab=all 은 모든 사용자가 DRAFT 를 제외한 결과만 본다 (D-A5, T-30-02)"
    - "totalElements 값이 사용자가 실제 접근 가능한 문서 수와 정확히 일치한다 (count(distinct doc.id), JOIN inflate 없음, D-D3)"
    - "기존 keyword OR 검색 (title | docNumber | drafter.name) 과 escapeLikePattern 이 회귀 없이 유지된다 (SRCH-02, D-D8)"
    - "복수 status 파라미터가 `doc.status.in(statuses)` 로 번역되고 단일 status 파라미터도 동일하게 동작한다 (역호환, I7)"
  artifacts:
    - path: "backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java"
      provides: "FSD 4-브랜치 권한 predicate (role 기반 분기) + tab != 'my' DRAFT gate + doc.countDistinct()"
      contains: "JPAExpressions.selectOne()"
    - path: "backend/src/test/java/com/micesign/document/DocumentSearchPermissionMatrixTest.java"
      provides: "7 유형 사용자 × 4 문서 상태 = 28 케이스 권한 매트릭스 통합 테스트"
      contains: "@Test"
    - path: "backend/src/test/java/com/micesign/document/DocumentSearchDraftGateTest.java"
      provides: "tab × DRAFT 격리 통합 테스트 (T-30-02 회귀 보호)"
      contains: "tab=department"
    - path: "backend/src/test/java/com/micesign/document/DocumentSearchKeywordTest.java"
      provides: "SRCH-02 keyword OR 3-필드 회귀 + escapeLikePattern SQL injection 방어 테스트 확장"
    - path: "backend/src/test/java/com/micesign/document/DocumentSearchStatusFilterTest.java"
      provides: "복수 status + 단일 역호환 + statuses=[] 누락 처리 테스트 확장"
  key_links:
    - from: "DocumentRepositoryCustomImpl.searchDocuments"
      to: "JPAExpressions.selectOne().from(approvalLine).where(...).exists()"
      via: "권한 predicate 의 approvalParticipant 서브쿼리"
      pattern: "JPAExpressions\\.selectOne\\(\\)"
    - from: "DocumentRepositoryCustomImpl.searchDocuments"
      to: "doc.status.ne(DocumentStatus.DRAFT)"
      via: "if (!\"my\".equals(tab)) DRAFT gate"
      pattern: "doc.status.ne\\(DocumentStatus.DRAFT\\)"
    - from: "DocumentRepositoryCustomImpl.searchDocuments count query"
      to: "doc.countDistinct()"
      via: "queryFactory.select(doc.countDistinct())"
      pattern: "doc\\.countDistinct\\(\\)"
---

## Revision Log

- **Revision 1 (2026-04-23)**: Checker WARNING 1/2 대응.
  - WARNING 1: Task 2 `read_first` 에 `V2__seed_initial_data.sql` 추가 + `G_ID` 상수 주석에 "tokenFor() SUPER_ADMIN 분기는 V2 seed 고정 계정 사용, G_ID 값은 assertion 전용" 명시.
  - WARNING 2: DocumentSearchPermissionMatrixTest 에 `countDistinct_withMultipleApprovalLines_returnsOneDoc` @Test 추가 — JOIN inflate 가 countDistinct 로 방어됨을 명시적으로 assert.

<objective>
`DocumentRepositoryCustomImpl.searchDocuments` 에 FSD FN-SEARCH-001 의 4-브랜치 권한 predicate (본인 OR EXISTS approval_line OR (ADMIN AND 부서 일치) OR SUPER_ADMIN) 와 tab != 'my' DRAFT gate 를 주입하여 **운영 중 보안 사고 (T-30-01 BOLA / T-30-02 DRAFT 유출)** 를 해소한다. 28 케이스 권한 매트릭스 통합 테스트로 FSD 준수를 검증한다. count → countDistinct 로 totalElements 정확성 확보 (NFR-01 준비). 본 Plan 완료 시 **PR1 hotfix 머지 가능**.

Purpose: 현재 `searchDocuments` 는 `tab` 스코프만 적용되고 FSD 권한 predicate 가 완전히 누락되어, (a) REFERENCE 라인 접근자가 자기가 관계된 문서조차 검색 결과에서 못 찾고, (b) ADMIN 이 타 부서 DRAFT 까지 볼 수 있으며, (c) tab=department/all 에서 모든 사용자가 타인 DRAFT 에 노출된다 — **3 종 보안 사고**. 본 Plan 은 `DocumentService.getDocument L204-223` 의 Java 4-브랜치 참조 구현을 QueryDSL 로 **기계적으로** 번역한다.
Output:
- `DocumentRepositoryCustomImpl.java` 에 권한 predicate 블록 + DRAFT gate + count → countDistinct
- `DocumentSearchPermissionMatrixTest.java` 신규 (28 케이스)
- `DocumentSearchDraftGateTest.java` 신규 (tab × DRAFT 조합)
- `DocumentSearchKeywordTest.java` / `DocumentSearchStatusFilterTest.java` 확장 (Plan 01 의 스캐폴드에 권한 predicate 적용된 검증 추가)
- PR1 hotfix 배포 가능 상태 달성
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-where/30-CONTEXT.md
@.planning/phases/30-where/30-RESEARCH.md
@.planning/phases/30-where/30-PATTERNS.md
@.planning/phases/30-where/30-VALIDATION.md
@.planning/phases/30-where/30-01-SUMMARY.md
@docs/FSD_MiceSign_v1.0.md
@CLAUDE.md
@backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java
@backend/src/main/java/com/micesign/service/DocumentService.java
@backend/src/main/java/com/micesign/domain/Document.java
@backend/src/main/java/com/micesign/domain/ApprovalLine.java
@backend/src/main/java/com/micesign/domain/User.java
@backend/src/main/java/com/micesign/domain/QDocument.java
@backend/src/main/java/com/micesign/domain/QApprovalLine.java
@backend/src/main/java/com/micesign/domain/QUser.java
@backend/src/main/java/com/micesign/domain/enums/DocumentStatus.java
@backend/src/main/java/com/micesign/domain/enums/UserRole.java
@backend/src/main/java/com/micesign/domain/enums/ApprovalLineType.java
@backend/src/main/java/com/micesign/repository/ApprovalLineRepository.java
@backend/src/test/java/com/micesign/document/ApprovalWorkflowTest.java
@backend/src/test/java/com/micesign/document/DocumentSubmitTest.java
@backend/src/test/java/com/micesign/admin/TestTokenHelper.java

<interfaces>
<!-- 핵심 타입/메서드 — Plan 01 이 만든 기반을 받음 -->

Plan 01 이 이미 완성한 상태 (Plan 02 의 시작점):
- `DocumentSearchCondition` record: `(keyword, statuses, templateCode, dateFrom, dateTo, tab, drafterId)`
- `DocumentController.searchDocuments` 수동 enum 변환 + 400 VALIDATION_ERROR
- `DocumentRepositoryCustomImpl` L69-70: `if (!statuses.isEmpty()) where.and(doc.status.in(statuses));` + `if (drafterId != null) where.and(doc.drafterId.eq(drafterId));` — **Plan 02 는 이 블록 앞에 권한 predicate + DRAFT gate 삽입**

QUser 필드 (backend/src/main/java/com/micesign/domain/User.java):
- `id` (Long), `departmentId` (Long), `department` (Department), `status` (UserStatus)

QApprovalLine 필드 (backend/src/main/java/com/micesign/domain/ApprovalLine.java):
- `document` (Document), `approver` (User), `stepOrder` (int), `lineType` (ApprovalLineType), `status` (ApprovalLineStatus)

QDocument 필드:
- `id` (Long), `drafterId` (Long), `drafter` (User), `status` (DocumentStatus), `title` (String), `docNumber` (String), `templateCode` (String), `submittedAt` (LocalDateTime), `createdAt` (LocalDateTime)

DocumentService.getDocument L204-223 참조 구현 (PATTERNS.md §1D L183-201 full snippet):
```java
boolean isDrafter = document.getDrafter().getId().equals(userId);
boolean isApprovalParticipant = approvalLineRepository.existsByDocumentIdAndApproverId(docId, userId);
boolean isAdminSameDept = role == UserRole.ADMIN && departmentId != null
    && departmentId.equals(document.getDrafter().getDepartmentId());
boolean isSuperAdmin = role == UserRole.SUPER_ADMIN;
```

**번역 맵 (PATTERNS.md §1D L204-208):**
- isDrafter → `doc.drafterId.eq(userId)`
- isApprovalParticipant → `JPAExpressions.selectOne().from(approvalLine).where(approvalLine.document.id.eq(doc.id).and(approvalLine.approver.id.eq(userId))).exists()`
- isAdminSameDept → external `"ADMIN".equals(role)` + `doc.drafterId.in(JPAExpressions.select(u.id).from(u).where(u.departmentId.eq(departmentId)))`
- isSuperAdmin → external `"SUPER_ADMIN".equals(role)` → predicate **SKIP**

FSD FN-SEARCH-001 원문 (docs/FSD_MiceSign_v1.0.md L1535-1576):
```sql
(
  d.drafter_id = :currentUserId
  OR EXISTS (SELECT 1 FROM approval_line al WHERE al.document_id = d.id AND al.approver_id = :currentUserId)
  OR (:role = 'ADMIN' AND d.drafter_id IN (SELECT id FROM user WHERE department_id = :myDepartmentId))
  OR :role = 'SUPER_ADMIN'
)
AND (
  /* tab=my 외에는 */ d.status != 'DRAFT'
)
```

TestTokenHelper:
```java
public String superAdminToken();
public String tokenForRole(Long userId, String email, String name, UserRole role, Long departmentId);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DocumentRepositoryCustomImpl 에 FSD 권한 predicate + DRAFT gate + countDistinct 주입</name>
  <files>
    backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java
  </files>
  <read_first>
    - 30-CONTEXT.md D-A1~D-A7 (권한 WHERE 절 + DRAFT 정책), D-D3 (countDistinct), D-D4 (2-query 분리)
    - 30-RESEARCH.md §Pattern 1 (QueryDSL 권한 predicate, L261-308)
    - 30-RESEARCH.md §Pattern 2 (DRAFT gate, L310-322)
    - 30-RESEARCH.md §Code Examples Example 1 (완성된 searchDocuments, L644-752) ← **그대로 이식 대상**
    - 30-RESEARCH.md §Pattern 6 (countDistinct, L512-532)
    - 30-RESEARCH.md §Common Pitfalls Pitfall 1 (BooleanBuilder.and(null)), Pitfall 7 (count + orderBy), Pitfall 8 (keyword 인덱스 미스)
    - 30-PATTERNS.md §[PR1] 1. 1A~1E (자기 참조 + 번역 맵 + JPQL EXISTS 대조, L81-226)
    - 30-PATTERNS.md §Pitfall Re-check
    - docs/FSD_MiceSign_v1.0.md L1535-1576 (권한 predicate 원문 — **절대 필독**)
    - backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java (Plan 01 수정 후 현재 상태)
    - backend/src/main/java/com/micesign/service/DocumentService.java L203-223 (4-브랜치 Java 원본)
    - backend/src/main/java/com/micesign/domain/QUser.java (QUser alias 생성 문법 확인)
  </read_first>
  <behavior>
    - 수정 후 컴파일 green
    - 기존 `DocumentControllerTest`, `DocumentSubmitTest`, `ApprovalWorkflowTest` 회귀 pass (다른 테스트는 Task 2/3 에서 검증)
    - Plan 01 의 `DocumentSearchConditionTest` / `DocumentSearchInvalidEnumTest` / `DocumentSearchKeywordTest` / `DocumentSearchStatusFilterTest` 의 smoke 테스트들이 여전히 green
  </behavior>
  <action>
**1-1. `DocumentRepositoryCustomImpl.searchDocuments` 를 RESEARCH.md Example 1 (L644-752) 구조로 전체 재작성.** Plan 01 이 이미 `statuses.in` + `drafterId.eq` 블록을 삽입해둠 — 그 **앞에** 권한 predicate 와 DRAFT gate 블록을 삽입하고, count 쿼리의 `doc.count()` 를 `doc.countDistinct()` 로 교체.

전체 메서드 최종 shape (RESEARCH.md §Code Examples Example 1 인용):

```java
@Override
public Page<DocumentResponse> searchDocuments(
        DocumentSearchCondition condition, Long userId, String role,
        Long departmentId, Pageable pageable) {

    QDocument doc = QDocument.document;
    QUser drafter = QUser.user;
    QDepartment dept = QDepartment.department;
    QPosition pos = QPosition.position;
    QApprovalLine approvalLine = QApprovalLine.approvalLine;

    BooleanBuilder where = new BooleanBuilder();
    String tab = condition.tab() != null ? condition.tab().toLowerCase() : "my";

    // [1] Tab scope (기존 유지)
    switch (tab) {
        case "my" -> where.and(doc.drafterId.eq(userId));
        case "department" -> where.and(drafter.departmentId.eq(departmentId));
        case "all" -> {
            if ("ADMIN".equals(role)) {
                where.and(drafter.departmentId.eq(departmentId));
            }
            // SUPER_ADMIN: no additional predicate
        }
        default -> where.and(doc.drafterId.eq(userId));
    }

    // [2] FSD FN-SEARCH-001 권한 predicate — role 기반 분기 (D-A1/A2/A3)
    //     번역 원본: DocumentService.getDocument L204-223 의 Java 4-브랜치
    if (!"SUPER_ADMIN".equals(role)) {
        BooleanExpression ownDoc = doc.drafterId.eq(userId);
        BooleanExpression approvalParticipant = JPAExpressions
            .selectOne()
            .from(approvalLine)
            .where(approvalLine.document.id.eq(doc.id)
                   .and(approvalLine.approver.id.eq(userId)))
            .exists();
        BooleanExpression permissionBranch = ownDoc.or(approvalParticipant);

        if ("ADMIN".equals(role) && departmentId != null) {
            QUser u = new QUser("deptUser");  // alias to avoid clash with main drafter join
            BooleanExpression sameDepartment = doc.drafterId.in(
                JPAExpressions.select(u.id).from(u)
                    .where(u.departmentId.eq(departmentId))
            );
            permissionBranch = permissionBranch.or(sameDepartment);
        }
        where.and(permissionBranch);
    }
    // SUPER_ADMIN: permission predicate 전체 생략 — 전체 조회 허용 (D-A2)

    // [3] DRAFT gate (D-A4/A5/A6) — tab=my 에서만 본인 DRAFT 노출
    if (!"my".equals(tab)) {
        where.and(doc.status.ne(DocumentStatus.DRAFT));
    }

    // [4] Keyword OR (SRCH-02 회귀, D-D8) — 기존 블록 무변경
    if (condition.keyword() != null && !condition.keyword().isBlank()) {
        String escaped = escapeLikePattern(condition.keyword().trim());
        String kw = "%" + escaped + "%";
        where.and(
                doc.title.likeIgnoreCase(kw)
                        .or(doc.docNumber.likeIgnoreCase(kw))
                        .or(drafter.name.likeIgnoreCase(kw))
        );
    }

    // [5] Statuses in (D-B6) — Plan 01 이 이미 삽입했음
    if (condition.statuses() != null && !condition.statuses().isEmpty()) {
        where.and(doc.status.in(condition.statuses()));
    }

    // [6] Template + date range (기존)
    if (condition.templateCode() != null && !condition.templateCode().isBlank()) {
        where.and(doc.templateCode.eq(condition.templateCode()));
    }
    if (condition.dateFrom() != null) {
        where.and(doc.submittedAt.goe(condition.dateFrom().atStartOfDay()));
    }
    if (condition.dateTo() != null) {
        where.and(doc.submittedAt.lt(condition.dateTo().plusDays(1).atStartOfDay()));
    }

    // [7] drafterId (D-B7) — Plan 01 이 이미 삽입했음
    if (condition.drafterId() != null) {
        where.and(doc.drafterId.eq(condition.drafterId()));
    }

    // [8] Count query — countDistinct 로 교체 (D-D3)
    //     Pitfall 7: count 쿼리에 orderBy 절대 없음
    Long total = queryFactory.select(doc.countDistinct())  // ← count → countDistinct
            .from(doc)
            .join(doc.drafter, drafter)
            .join(drafter.department, dept)
            .leftJoin(drafter.position, pos)
            .where(where)
            .fetchOne();
    if (total == null) {
        total = 0L;
    }

    // [9] Content query — 기존 Projection/정렬/페이지 무변경
    List<DocumentResponse> content = queryFactory
            .select(Projections.constructor(DocumentResponse.class,
                    doc.id,
                    doc.docNumber,
                    doc.templateCode,
                    doc.templateCode,
                    doc.title,
                    doc.status.stringValue(),
                    drafter.name,
                    dept.name,
                    pos.name,
                    doc.drafterId,
                    doc.submittedAt,
                    doc.completedAt,
                    doc.createdAt
            ))
            .from(doc)
            .join(doc.drafter, drafter)
            .join(drafter.department, dept)
            .leftJoin(drafter.position, pos)
            .where(where)
            .orderBy(doc.createdAt.desc())
            .offset(pageable.getOffset())
            .limit(pageable.getPageSize())
            .fetch();

    return new PageImpl<>(content, pageable, total);
}
```

**import 추가 필요:**
```java
import com.querydsl.core.types.dsl.BooleanExpression;
```

(`JPAExpressions`, `BooleanBuilder`, `QApprovalLine`, `QUser`, `DocumentStatus` 는 이미 import 돼 있음 — 기존 L3-14 확인)

**주의 포인트 (Pitfall 재점검):**
1. **Pitfall 1** — `role` 이 null 이면 NullPointerException 이 controller 의 `UserRole.valueOf(user.getRole())` 에서 먼저 발생 → QueryDSL 진입 불가 → 안전. 추가 guard 불필요.
2. **Pitfall 7** — count 쿼리에 `orderBy` 절대 추가 금지 (기존에도 없음 — 그대로).
3. **Pitfall 8** — keyword LIKE `%..%` 는 인덱스 미스. 권한 predicate 가 먼저 row 축소해서 괜찮음 — Plan 05 벤치에서 실측 확인.
4. **QUser alias** — `QUser u = new QUser("deptUser")` 로 JOIN 시 drafter alias 와 충돌 방지 (서브쿼리 내 별도 별칭).
5. **permissionBranch immutability** — `permissionBranch = permissionBranch.or(sameDepartment)` 은 **반환값 재할당 필수** (BooleanExpression.or 은 immutable, QueryDSL API).
6. **keyword/statuses/templateCode/date/drafterId 순서**: where 체인은 AND 이므로 순서 무관하나 **가독성** 을 위해 위 순서 유지.
  </action>
  <verify>
    <automated>./gradlew compileJava && ./gradlew test --tests 'com.micesign.document.DocumentSearchConditionTest' --tests 'com.micesign.document.DocumentSearchInvalidEnumTest' --tests 'com.micesign.document.DocumentSearchKeywordTest' --tests 'com.micesign.document.DocumentSearchStatusFilterTest' --tests 'com.micesign.document.ApprovalWorkflowTest' --tests 'com.micesign.document.DocumentSubmitTest'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "JPAExpressions.selectOne()" backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java` exit 0
    - `grep -q "approvalLine.document.id.eq(doc.id)" backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java` exit 0
    - `grep -q "approvalLine.approver.id.eq(userId)" backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java` exit 0
    - `grep -q 'QUser u = new QUser("deptUser")' backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java` exit 0
    - `grep -q "SUPER_ADMIN" backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java` exit 0
    - `grep -q 'if (!"my".equals(tab))' backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java` exit 0
    - `grep -q "doc.status.ne(DocumentStatus.DRAFT)" backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java` exit 0
    - `grep -q "doc.countDistinct()" backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java` exit 0
    - `! grep -q "select(doc.count())" backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java` (확인: count 제거됨)
    - `grep -q "import com.querydsl.core.types.dsl.BooleanExpression" backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java` exit 0
    - `./gradlew compileJava` exit 0
    - `./gradlew test --tests 'com.micesign.document.ApprovalWorkflowTest' --tests 'com.micesign.document.DocumentSubmitTest'` exit 0 (회귀 보호)
  </acceptance_criteria>
  <done>
    권한 predicate + DRAFT gate + countDistinct 주입 완료. 컴파일 green. 기존 `ApprovalWorkflowTest` / `DocumentSubmitTest` 회귀 pass. Plan 01 의 4 개 DocumentSearch* smoke 테스트 여전히 green. Task 2/3 가 권한 매트릭스 / DRAFT gate 28+ 케이스를 검증.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 28 케이스 권한 매트릭스 통합 테스트 (SRCH-01 핵심 검증)</name>
  <files>
    backend/src/test/java/com/micesign/document/DocumentSearchPermissionMatrixTest.java
  </files>
  <read_first>
    - 30-CONTEXT.md D-A1~D-A7 전체 (권한 정책)
    - 30-CONTEXT.md §specifics "권한 매트릭스 테스트 표" L189
    - 30-RESEARCH.md §Validation Architecture 권한 매트릭스 fixture 구조 (L1020-1045)
    - 30-PATTERNS.md §[PR1] 5A (ApprovalWorkflowTest fixture 복제, L436-464)
    - 30-PATTERNS.md §[PR1] 5B (28-case fixture 확장, L475-497)
    - 30-VALIDATION.md Nyquist 8-axis I1/I2/I3/I4 invariants
    - backend/src/test/java/com/micesign/document/ApprovalWorkflowTest.java (fixture 스타일 원본 — **반드시 구조 복제**)
    - backend/src/test/java/com/micesign/admin/TestTokenHelper.java
    - backend/src/main/resources/db/migration/V1__create_schema.sql L96-153 (document / approval_line 스키마)
    - backend/src/main/resources/db/migration/V2__seed_initial_data.sql (**Revision 1 WARNING 1 추가**: SUPER_ADMIN seed 계정 id 확인 — tokenFor() 의 SUPER_ADMIN 분기는 이 seed 의 고정 계정 사용, G_ID static 값은 assertion 용)
  </read_first>
  <behavior>
    - 7 유형 사용자 × 4 문서 상태 = **28 케이스 매트릭스**
    - 사용자 seed:
      - A (Alice, dept=1, USER, id=300)
      - B (Bob APPROVE 승인자, dept=2, USER, id=301)
      - C (Charlie AGREE 승인자, dept=2, USER, id=302)
      - D (David REFERENCE 참조자, dept=3, USER, id=303)
      - E (Admin1 same dept as Alice, dept=1, ADMIN, id=310)
      - F (Admin2 other dept, dept=2, ADMIN, id=320)
      - G (SuperAdmin, seed id=1, SUPER_ADMIN)
    - 문서 seed (drafter=A, 4 개):
      - doc_draft: DRAFT, approval_line=[]
      - doc_submitted: SUBMITTED, approval_line=[APPROVE:B step1, AGREE:C step2, REFERENCE:D step3]
      - doc_approved: APPROVED, 동일 approval_line (완료 상태)
      - doc_rejected: REJECTED, 동일 approval_line (B 가 반려)
    - 기대 매트릭스 (tab=search → 백엔드 tab=all):
      - A self: [draft:F(search)/T(my), submitted:T, approved:T, rejected:T]
      - B APPROVE approver: [draft:F, submitted:T, approved:T, rejected:T]
      - C AGREE approver: [draft:F, submitted:T, approved:T, rejected:T]
      - D REFERENCE approver: [draft:F, submitted:T, approved:T, rejected:T]  ← **D-A3 검증 핵심**
      - E same dept ADMIN (dept=1 as Alice): [draft:F(search), submitted:T, approved:T, rejected:T]
      - F other dept ADMIN (dept=2): [draft:F, submitted:F, approved:F, rejected:F]
      - G SUPER_ADMIN: [draft:F(search), submitted:T, approved:T, rejected:T]
    - 모든 `totalElements` 값이 행 수와 정확히 일치 (I3)
    - **countDistinct 정확성 (D-D3, Revision 1 WARNING 2)**: B (APPROVE 승인자) 가 `tab=all&status=SUBMITTED` 으로 검색 시 submittedDocId 는 approval_line 3 행 (APPROVE:B + AGREE:C + REFERENCE:D) 을 갖지만 `totalElements = 1` (JOIN inflate 가 countDistinct 로 방어됨)
  </behavior>
  <action>
**2-1. `DocumentSearchPermissionMatrixTest.java` 신규 작성** — PATTERNS.md §5A 의 `ApprovalWorkflowTest.setUp` 스타일 복제. JUnit 5 `@ParameterizedTest` 또는 `@TestFactory` 대신 **단순 @Test 메서드 다수** (명확성 우선 — 28 케이스면 메서드 7 개 정도로 그룹화 가능).

전체 파일 구조:

```java
package com.micesign.document;

import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.micesign.admin.TestTokenHelper;
import com.micesign.domain.enums.ApprovalLineStatus;
import com.micesign.domain.enums.ApprovalLineType;
import com.micesign.domain.enums.DocumentStatus;
import com.micesign.domain.enums.UserRole;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.http.MediaType;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.test.context.ActiveProfiles;
import org.springframework.test.web.servlet.MockMvc;
import org.springframework.test.web.servlet.MvcResult;

import java.time.LocalDateTime;
import java.util.List;

import static org.assertj.core.api.Assertions.assertThat;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

@SpringBootTest
@AutoConfigureMockMvc
@ActiveProfiles("test")
@DisplayName("Phase 30 — FSD FN-SEARCH-001 권한 매트릭스 통합 테스트 (SRCH-01, T-30-01)")
class DocumentSearchPermissionMatrixTest {

    @Autowired MockMvc mockMvc;
    @Autowired TestTokenHelper tokenHelper;
    @Autowired JdbcTemplate jdbcTemplate;
    @Autowired ObjectMapper objectMapper;

    // User ids
    static final long A_ID = 300L;
    static final long B_ID = 301L;
    static final long C_ID = 302L;
    static final long D_ID = 303L;
    static final long E_ID = 310L;
    static final long F_ID = 320L;
    static final long G_ID = 1L; // assertion 용 id 값 — tokenFor() 내부 SUPER_ADMIN 분기는 tokenHelper.superAdminToken() 이 V2__seed_initial_data.sql 의 고정 계정을 사용하므로 G_ID 값 자체는 JWT 생성에 쓰이지 않음 (Revision 1 WARNING 1 명확화)

    // Departments: Alice/Admin1 in dept=1, Bob/Charlie/Admin2 in dept=2, David in dept=3
    static final long DEPT_1 = 1L;
    static final long DEPT_2 = 2L;
    static final long DEPT_3 = 3L;

    // Document ids
    long draftDocId;
    long submittedDocId;
    long approvedDocId;
    long rejectedDocId;

    @BeforeEach
    void setUp() {
        // FK 역순 cleanup (SP-4)
        jdbcTemplate.update("DELETE FROM notification_log");
        jdbcTemplate.update("DELETE FROM approval_line");
        jdbcTemplate.update("DELETE FROM document_attachment");
        jdbcTemplate.update("DELETE FROM document_content");
        jdbcTemplate.update("DELETE FROM document");
        jdbcTemplate.update("DELETE FROM doc_sequence");
        jdbcTemplate.update("DELETE FROM \"user\" WHERE id IN (?, ?, ?, ?, ?, ?)",
            A_ID, B_ID, C_ID, D_ID, E_ID, F_ID);

        // Ensure department rows exist (V2 seed should have dept 1/2/3; if missing, fail fast)
        Integer deptCount = jdbcTemplate.queryForObject(
            "SELECT COUNT(*) FROM department WHERE id IN (1, 2, 3)", Integer.class);
        if (deptCount == null || deptCount < 3) {
            // Insert missing dept rows defensively
            jdbcTemplate.update("INSERT IGNORE INTO department (id, name, sort_order, is_active) VALUES (1, '부서1', 1, TRUE)");
            jdbcTemplate.update("INSERT IGNORE INTO department (id, name, sort_order, is_active) VALUES (2, '부서2', 2, TRUE)");
            jdbcTemplate.update("INSERT IGNORE INTO department (id, name, sort_order, is_active) VALUES (3, '부서3', 3, TRUE)");
        }

        // Insert 6 test users (password hash copied from ApprovalWorkflowTest — BCrypt("password"))
        String pwHash = "$2a$10$07mcjXBfvelJFwjs8DnoJOnEqprFy.dnQL1NdnRvlqEWwwmX62SOW";
        String userSql = "INSERT INTO \"user\" (id, employee_no, name, email, password, department_id, " +
            "position_id, role, status, failed_login_count, must_change_password) " +
            "VALUES (?, ?, ?, ?, ?, ?, 3, ?, 'ACTIVE', 0, FALSE)";
        jdbcTemplate.update(userSql, A_ID, "U_A", "Alice", "alice@test.com", pwHash, DEPT_1, "USER");
        jdbcTemplate.update(userSql, B_ID, "U_B", "Bob", "bob@test.com", pwHash, DEPT_2, "USER");
        jdbcTemplate.update(userSql, C_ID, "U_C", "Charlie", "charlie@test.com", pwHash, DEPT_2, "USER");
        jdbcTemplate.update(userSql, D_ID, "U_D", "David", "david@test.com", pwHash, DEPT_3, "USER");
        jdbcTemplate.update(userSql, E_ID, "A_E", "Admin1", "admin1@test.com", pwHash, DEPT_1, "ADMIN");
        jdbcTemplate.update(userSql, F_ID, "A_F", "Admin2", "admin2@test.com", pwHash, DEPT_2, "ADMIN");

        // Insert 4 documents (all drafted by A)
        draftDocId = insertDocument(A_ID, DocumentStatus.DRAFT, null, "DRAFT 문서");
        submittedDocId = insertDocument(A_ID, DocumentStatus.SUBMITTED, LocalDateTime.now().minusDays(3), "SUBMITTED 문서");
        approvedDocId = insertDocument(A_ID, DocumentStatus.APPROVED, LocalDateTime.now().minusDays(5), "APPROVED 문서");
        rejectedDocId = insertDocument(A_ID, DocumentStatus.REJECTED, LocalDateTime.now().minusDays(2), "REJECTED 문서");

        // approval_line for submitted/approved/rejected (same shape: APPROVE:B, AGREE:C, REFERENCE:D)
        insertApprovalLine(submittedDocId, B_ID, ApprovalLineType.APPROVE, 1, ApprovalLineStatus.PENDING);
        insertApprovalLine(submittedDocId, C_ID, ApprovalLineType.AGREE, 2, ApprovalLineStatus.PENDING);
        insertApprovalLine(submittedDocId, D_ID, ApprovalLineType.REFERENCE, 3, ApprovalLineStatus.PENDING);
        insertApprovalLine(approvedDocId, B_ID, ApprovalLineType.APPROVE, 1, ApprovalLineStatus.APPROVED);
        insertApprovalLine(approvedDocId, C_ID, ApprovalLineType.AGREE, 2, ApprovalLineStatus.APPROVED);
        insertApprovalLine(approvedDocId, D_ID, ApprovalLineType.REFERENCE, 3, ApprovalLineStatus.APPROVED);
        insertApprovalLine(rejectedDocId, B_ID, ApprovalLineType.APPROVE, 1, ApprovalLineStatus.REJECTED);
        insertApprovalLine(rejectedDocId, C_ID, ApprovalLineType.AGREE, 2, ApprovalLineStatus.SKIPPED);
        insertApprovalLine(rejectedDocId, D_ID, ApprovalLineType.REFERENCE, 3, ApprovalLineStatus.SKIPPED);
    }

    private long insertDocument(Long drafterId, DocumentStatus status, LocalDateTime submittedAt, String title) {
        jdbcTemplate.update(
            "INSERT INTO document (template_code, drafter_id, title, status, submitted_at, created_at, current_step) " +
            "VALUES (?, ?, ?, ?, ?, ?, 1)",
            "GENERAL", drafterId, title, status.name(), submittedAt, LocalDateTime.now());
        return jdbcTemplate.queryForObject(
            "SELECT id FROM document WHERE drafter_id = ? AND status = ? ORDER BY id DESC LIMIT 1",
            Long.class, drafterId, status.name());
    }

    private void insertApprovalLine(long docId, long approverId, ApprovalLineType type, int step, ApprovalLineStatus status) {
        jdbcTemplate.update(
            "INSERT INTO approval_line (document_id, step_order, line_type, approver_id, status) VALUES (?, ?, ?, ?, ?)",
            docId, step, type.name(), approverId, status.name());
    }

    private String tokenFor(long id, String email, String name, UserRole role, Long deptId) {
        if (role == UserRole.SUPER_ADMIN) return tokenHelper.superAdminToken();
        return tokenHelper.tokenForRole(id, email, name, role, deptId);
    }

    private List<Long> searchDocIds(String token, String tab) throws Exception {
        MvcResult result = mockMvc.perform(get("/api/v1/documents/search")
                .param("tab", tab)
                .param("size", "50")
                .header("Authorization", "Bearer " + token)
                .accept(MediaType.APPLICATION_JSON))
            .andExpect(status().isOk())
            .andReturn();
        JsonNode root = objectMapper.readTree(result.getResponse().getContentAsByteArray());
        JsonNode content = root.path("data").path("content");
        List<Long> ids = new java.util.ArrayList<>();
        for (JsonNode row : content) {
            ids.add(row.path("id").asLong());
        }
        // I3 invariant: totalElements matches content size for single page
        long totalElements = root.path("data").path("totalElements").asLong();
        assertThat(ids.size()).isEqualTo((int) totalElements);
        return ids;
    }

    // ========== A — self (drafter) ==========
    @Test
    @DisplayName("A (self): tab=my 는 본인 DRAFT 포함 4 건, tab=search 는 DRAFT 제외 3 건")
    void userA_self() throws Exception {
        String token = tokenFor(A_ID, "alice@test.com", "Alice", UserRole.USER, DEPT_1);
        assertThat(searchDocIds(token, "my"))
            .containsExactlyInAnyOrder(draftDocId, submittedDocId, approvedDocId, rejectedDocId);
        assertThat(searchDocIds(token, "all"))
            .containsExactlyInAnyOrder(submittedDocId, approvedDocId, rejectedDocId)
            .doesNotContain(draftDocId);
    }

    // ========== B — APPROVE approver ==========
    @Test
    @DisplayName("B (APPROVE 승인자): tab=all 에서 SUBMITTED/APPROVED/REJECTED 모두 포함, DRAFT 미포함")
    void userB_approveApprover() throws Exception {
        String token = tokenFor(B_ID, "bob@test.com", "Bob", UserRole.USER, DEPT_2);
        List<Long> ids = searchDocIds(token, "all");
        assertThat(ids).containsExactlyInAnyOrder(submittedDocId, approvedDocId, rejectedDocId);
        assertThat(ids).doesNotContain(draftDocId);
    }

    // ========== C — AGREE approver ==========
    @Test
    @DisplayName("C (AGREE 승인자): 동일 — SUBMITTED/APPROVED/REJECTED 포함")
    void userC_agreeApprover() throws Exception {
        String token = tokenFor(C_ID, "charlie@test.com", "Charlie", UserRole.USER, DEPT_2);
        List<Long> ids = searchDocIds(token, "all");
        assertThat(ids).containsExactlyInAnyOrder(submittedDocId, approvedDocId, rejectedDocId);
    }

    // ========== D — REFERENCE approver (D-A3 검증 핵심) ==========
    @Test
    @DisplayName("D (REFERENCE 참조자): line_type/status 제한 없이 접근 가능 — D-A3")
    void userD_referenceApprover() throws Exception {
        String token = tokenFor(D_ID, "david@test.com", "David", UserRole.USER, DEPT_3);
        List<Long> ids = searchDocIds(token, "all");
        // D 는 dept 3 의 일반 USER — approval_line 에 등록되어 있어야만 접근 가능
        assertThat(ids).containsExactlyInAnyOrder(submittedDocId, approvedDocId, rejectedDocId);
    }

    // ========== E — same dept ADMIN (dept=1 as Alice) ==========
    @Test
    @DisplayName("E (같은 부서 ADMIN): Alice 부서원의 SUBMITTED/APPROVED/REJECTED 볼 수 있음, DRAFT 제외 (D-A4)")
    void userE_sameDepartmentAdmin() throws Exception {
        String token = tokenFor(E_ID, "admin1@test.com", "Admin1", UserRole.ADMIN, DEPT_1);
        List<Long> ids = searchDocIds(token, "all");
        assertThat(ids).containsExactlyInAnyOrder(submittedDocId, approvedDocId, rejectedDocId);
        assertThat(ids).doesNotContain(draftDocId);
    }

    // ========== F — other dept ADMIN (dept=2, approval 라인에도 없음) ==========
    @Test
    @DisplayName("F (다른 부서 ADMIN, 승인라인 미등록): Alice 의 어떤 문서도 볼 수 없음 — 0 건")
    void userF_otherDepartmentAdmin_notInApprovalLine() throws Exception {
        String token = tokenFor(F_ID, "admin2@test.com", "Admin2", UserRole.ADMIN, DEPT_2);
        List<Long> ids = searchDocIds(token, "all");
        assertThat(ids).isEmpty();
    }

    // ========== G — SUPER_ADMIN ==========
    @Test
    @DisplayName("G (SUPER_ADMIN): tab=all 에서 DRAFT 제외하고 모두 볼 수 있음 — D-A4 SUPER_ADMIN 에도 적용")
    void userG_superAdmin() throws Exception {
        String token = tokenFor(G_ID, "super@test.com", "SuperAdmin", UserRole.SUPER_ADMIN, null);
        List<Long> ids = searchDocIds(token, "all");
        assertThat(ids).containsExactlyInAnyOrder(submittedDocId, approvedDocId, rejectedDocId);
        assertThat(ids).doesNotContain(draftDocId);
    }

    // ========== 추가 검증: 무관한 사용자 (seed user id=2 같은 일반 USER) — 0 건 ==========
    @Test
    @DisplayName("무관 사용자 Z (seed 일반 USER, 승인라인 미등록): Alice 의 어떤 SUBMITTED 문서도 볼 수 없음")
    void unrelatedUser_seesNothing() throws Exception {
        // seed user 중 id=2 는 일반 USER (V2 seed 확인 필요 — 없으면 fixture 에서 생성)
        Long unrelatedId = 999L;
        jdbcTemplate.update(
            "INSERT INTO \"user\" (id, employee_no, name, email, password, department_id, position_id, role, status, failed_login_count, must_change_password) " +
            "VALUES (?, 'U_Z', '무관자', 'unrelated@test.com', '$2a$10$07mcjXBfvelJFwjs8DnoJOnEqprFy.dnQL1NdnRvlqEWwwmX62SOW', 2, 3, 'USER', 'ACTIVE', 0, FALSE) " +
            "ON DUPLICATE KEY UPDATE id = id", unrelatedId);
        String token = tokenHelper.tokenForRole(unrelatedId, "unrelated@test.com", "무관자", UserRole.USER, DEPT_2);
        List<Long> ids = searchDocIds(token, "all");
        assertThat(ids).isEmpty();
        jdbcTemplate.update("DELETE FROM \"user\" WHERE id = ?", unrelatedId);
    }

    // ========== countDistinct 정확성 회귀 (Revision 1 WARNING 2) ==========
    @Test
    @DisplayName("countDistinct invariant: B 가 submittedDoc (approval_line 3 행) 검색 시 totalElements=1 (JOIN inflate 0)")
    void countDistinct_withMultipleApprovalLines_returnsOneDoc() throws Exception {
        // submittedDocId 는 setUp 에서 approval_line 3 행 (APPROVE:B + AGREE:C + REFERENCE:D) 을 갖는다.
        // count(doc.id) (non-distinct) 였다면 JOIN inflate 로 totalElements=3 이 됨.
        // countDistinct() 이므로 totalElements=1 이어야 한다 (D-D3 검증).
        String token = tokenFor(B_ID, "bob@test.com", "Bob", UserRole.USER, DEPT_2);
        // statuses 로 SUBMITTED 만 필터 → B 관점에서 submittedDocId 하나만 매치
        MvcResult result = mockMvc.perform(get("/api/v1/documents/search")
                .param("tab", "all")
                .param("status", "SUBMITTED")
                .param("size", "50")
                .header("Authorization", "Bearer " + tokenFor(B_ID, "bob@test.com", "Bob", UserRole.USER, DEPT_2))
                .accept(MediaType.APPLICATION_JSON))
            .andExpect(status().isOk())
            .andReturn();
        JsonNode root = objectMapper.readTree(result.getResponse().getContentAsByteArray());
        long totalElements = root.path("data").path("totalElements").asLong();
        JsonNode content = root.path("data").path("content");
        // 핵심 assertion: approval_line 3 행이어도 totalElements=1, content.size()=1
        assertThat(totalElements).as("JOIN inflate 없이 countDistinct 결과").isEqualTo(1L);
        assertThat(content.size()).isEqualTo(1);
        assertThat(content.get(0).path("id").asLong()).isEqualTo(submittedDocId);
    }
}
```

**주의 포인트:**
- `"user"` 테이블명은 MariaDB reserved word — `jdbcTemplate.update("DELETE FROM \"user\" WHERE ...")` 스타일 (ApprovalWorkflowTest 에서 확인된 관례).
- Department id 1/2/3 은 V2 seed 에 있을 수도/없을 수도 있음 — `INSERT IGNORE` 로 defensive 삽입.
- position_id=3 은 seed 값 (일반 직급) — 실제 seed 와 다르면 ApprovalWorkflowTest 참조.
- `tab=all` 호출 시 F (ADMIN) / G (SUPER_ADMIN) / E (ADMIN) 는 Controller 403 가드 통과 — Task 3 의 Draft gate 테스트에서 `tab=department` 도 검증.
- `@DisplayName` 한글로 표기 — 테스트 출력 가독성.
- I3 invariant assertion 은 모든 `searchDocIds` 헬퍼 내부에 포함.
  </action>
  <verify>
    <automated>./gradlew test --tests 'com.micesign.document.DocumentSearchPermissionMatrixTest'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f backend/src/test/java/com/micesign/document/DocumentSearchPermissionMatrixTest.java` exit 0
    - `grep -c "@Test" backend/src/test/java/com/micesign/document/DocumentSearchPermissionMatrixTest.java` ≥ 8 (A/B/C/D/E/F/G + unrelated = 최소 8)
    - `grep -q "REFERENCE" backend/src/test/java/com/micesign/document/DocumentSearchPermissionMatrixTest.java` exit 0 (D-A3 검증)
    - `grep -q "SUPER_ADMIN" backend/src/test/java/com/micesign/document/DocumentSearchPermissionMatrixTest.java` exit 0
    - `grep -q "ApprovalLineType.REFERENCE" backend/src/test/java/com/micesign/document/DocumentSearchPermissionMatrixTest.java` exit 0
    - `grep -q "totalElements" backend/src/test/java/com/micesign/document/DocumentSearchPermissionMatrixTest.java` exit 0 (I3 invariant)
    - `grep -q "countDistinct_withMultipleApprovalLines_returnsOneDoc" backend/src/test/java/com/micesign/document/DocumentSearchPermissionMatrixTest.java` exit 0 (Revision 1 WARNING 2: countDistinct 명시적 검증)
    - `./gradlew test --tests 'com.micesign.document.DocumentSearchPermissionMatrixTest'` exit 0 — 모든 테스트 pass (기존 7+ 케이스 + countDistinct 1 = 최소 8+)
  </acceptance_criteria>
  <done>
    28 케이스 권한 매트릭스 완성, 모든 테스트 green → T-30-01 BOLA 취약점 완전 차단 증명. REFERENCE 참조자도 검색 가능 (D-A3), F 타부서 ADMIN 제외 (D-A2), SUPER_ADMIN 도 tab=all 에서 DRAFT 제외 (D-A4).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DRAFT gate 통합 테스트 + 키워드/상태 필터 테스트 확장</name>
  <files>
    backend/src/test/java/com/micesign/document/DocumentSearchDraftGateTest.java,
    backend/src/test/java/com/micesign/document/DocumentSearchKeywordTest.java,
    backend/src/test/java/com/micesign/document/DocumentSearchStatusFilterTest.java
  </files>
  <read_first>
    - 30-CONTEXT.md D-A4 (tab=my 에서만 본인 DRAFT 노출), D-A5 (status != DRAFT 강제)
    - 30-RESEARCH.md §Validation Architecture (Req → Test Map)
    - 30-PATTERNS.md §[PR1] 5A (ApprovalWorkflowTest fixture)
    - 30-PATTERNS.md §[PR1] 1B (keyword OR 3-필드)
    - 30-PATTERNS.md §Pitfall Re-check 5/8 (keyword LIKE SQL injection 방어)
    - 30-VALIDATION.md Wave 0 Requirements
    - backend/src/main/java/com/micesign/repository/DocumentRepositoryCustomImpl.java L124-126 (escapeLikePattern)
    - backend/src/test/java/com/micesign/document/ApprovalWorkflowTest.java (fixture 스타일)
    - backend/src/test/java/com/micesign/document/DocumentSearchKeywordTest.java (Plan 01 스캐폴드)
    - backend/src/test/java/com/micesign/document/DocumentSearchStatusFilterTest.java (Plan 01 스캐폴드)
  </read_first>
  <behavior>
    **DocumentSearchDraftGateTest:**
    - Test 1: tab=department 에서 Alice 본인이 호출해도 본인 DRAFT 는 결과에 없음 (D-A4 엄격 정책)
    - Test 2: tab=my 에서 Alice 호출하면 본인 DRAFT 포함
    - Test 3: tab=all + E (같은 부서 ADMIN) 에서 Alice DRAFT 제외
    - Test 4: tab=all + G SUPER_ADMIN 에서 Alice DRAFT 제외 (D-A4)

    **DocumentSearchKeywordTest 확장 (Plan 01 smoke 에 덧붙임):**
    - Test: title "경비 보고서" 포함 문서 keyword=경비 로 검색 → 매치
    - Test: docNumber "GEN-2026-0001" 포함 → keyword=GEN-2026 매치
    - Test: drafter.name "김철수" 포함 → keyword=김철 매치
    - Test: escapeLikePattern 검증 — keyword="100%" 가 SQL injection 없이 이스케이프되어 리터럴 `100%` 매치 (D-D8)

    **DocumentSearchStatusFilterTest 확장:**
    - Test: statuses=[SUBMITTED] 단일 값 역호환 (I7)
    - Test: statuses=[SUBMITTED, APPROVED] 복수 값 → status IN (...) 적용
    - Test: statuses=[] 빈 목록 → 필터 미적용 (DRAFT gate + 권한 predicate 만 적용)
  </behavior>
  <action>
**3-1. `DocumentSearchDraftGateTest.java` 신규 작성** — ApprovalWorkflowTest fixture 스타일 복제, 최소 유저 seed (Alice + Admin1 + SuperAdmin seed):

```java
package com.micesign.document;

import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.micesign.admin.TestTokenHelper;
import com.micesign.domain.enums.DocumentStatus;
import com.micesign.domain.enums.UserRole;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.test.context.ActiveProfiles;
import org.springframework.test.web.servlet.MockMvc;
import org.springframework.test.web.servlet.MvcResult;

import java.time.LocalDateTime;

import static org.assertj.core.api.Assertions.assertThat;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

@SpringBootTest
@AutoConfigureMockMvc
@ActiveProfiles("test")
@DisplayName("Phase 30 — DRAFT gate (tab != 'my' 에서 DRAFT 격리, T-30-02)")
class DocumentSearchDraftGateTest {

    @Autowired MockMvc mockMvc;
    @Autowired TestTokenHelper tokenHelper;
    @Autowired JdbcTemplate jdbcTemplate;
    @Autowired ObjectMapper objectMapper;

    static final long A_ID = 400L;   // Alice (USER, dept=1)
    static final long E_ID = 410L;   // Admin1 (ADMIN, dept=1)

    long draftDocId;
    long submittedDocId;

    @BeforeEach
    void setUp() {
        jdbcTemplate.update("DELETE FROM notification_log");
        jdbcTemplate.update("DELETE FROM approval_line");
        jdbcTemplate.update("DELETE FROM document_attachment");
        jdbcTemplate.update("DELETE FROM document_content");
        jdbcTemplate.update("DELETE FROM document");
        jdbcTemplate.update("DELETE FROM doc_sequence");
        jdbcTemplate.update("DELETE FROM \"user\" WHERE id IN (?, ?)", A_ID, E_ID);

        jdbcTemplate.update("INSERT IGNORE INTO department (id, name, sort_order, is_active) VALUES (1, '부서1', 1, TRUE)");

        String pwHash = "$2a$10$07mcjXBfvelJFwjs8DnoJOnEqprFy.dnQL1NdnRvlqEWwwmX62SOW";
        String userSql = "INSERT INTO \"user\" (id, employee_no, name, email, password, department_id, position_id, role, status, failed_login_count, must_change_password) " +
            "VALUES (?, ?, ?, ?, ?, 1, 3, ?, 'ACTIVE', 0, FALSE)";
        jdbcTemplate.update(userSql, A_ID, "U_A", "Alice", "alice-dg@test.com", pwHash, "USER");
        jdbcTemplate.update(userSql, E_ID, "A_E", "Admin1", "admin1-dg@test.com", pwHash, "ADMIN");

        jdbcTemplate.update(
            "INSERT INTO document (template_code, drafter_id, title, status, submitted_at, created_at, current_step) " +
            "VALUES ('GENERAL', ?, '드래프트', 'DRAFT', NULL, ?, 1)",
            A_ID, LocalDateTime.now());
        draftDocId = jdbcTemplate.queryForObject(
            "SELECT id FROM document WHERE drafter_id = ? AND status = 'DRAFT' ORDER BY id DESC LIMIT 1", Long.class, A_ID);

        jdbcTemplate.update(
            "INSERT INTO document (template_code, drafter_id, title, status, submitted_at, created_at, current_step) " +
            "VALUES ('GENERAL', ?, '제출본', 'SUBMITTED', ?, ?, 1)",
            A_ID, LocalDateTime.now().minusDays(1), LocalDateTime.now().minusDays(1));
        submittedDocId = jdbcTemplate.queryForObject(
            "SELECT id FROM document WHERE drafter_id = ? AND status = 'SUBMITTED' ORDER BY id DESC LIMIT 1", Long.class, A_ID);
    }

    private boolean searchContainsDraft(String token, String tab) throws Exception {
        MvcResult result = mockMvc.perform(get("/api/v1/documents/search")
                .param("tab", tab)
                .param("size", "50")
                .header("Authorization", "Bearer " + token))
            .andExpect(status().isOk())
            .andReturn();
        JsonNode content = objectMapper.readTree(result.getResponse().getContentAsByteArray())
            .path("data").path("content");
        for (JsonNode row : content) {
            if ("DRAFT".equals(row.path("status").asText())) return true;
        }
        return false;
    }

    @Test
    @DisplayName("Alice — tab=my: 본인 DRAFT 포함 (D-A6)")
    void tabMy_includesOwnDraft() throws Exception {
        String token = tokenHelper.tokenForRole(A_ID, "alice-dg@test.com", "Alice", UserRole.USER, 1L);
        assertThat(searchContainsDraft(token, "my")).isTrue();
    }

    @Test
    @DisplayName("Alice — tab=department: 본인 DRAFT 제외 (D-A4 엄격 정책)")
    void tabDepartment_excludesOwnDraft() throws Exception {
        String token = tokenHelper.tokenForRole(A_ID, "alice-dg@test.com", "Alice", UserRole.USER, 1L);
        assertThat(searchContainsDraft(token, "department")).isFalse();
    }

    @Test
    @DisplayName("Admin1 (same dept) — tab=all: Alice DRAFT 제외 (T-30-02)")
    void tabAll_adminSameDept_excludesOtherDraft() throws Exception {
        String token = tokenHelper.tokenForRole(E_ID, "admin1-dg@test.com", "Admin1", UserRole.ADMIN, 1L);
        assertThat(searchContainsDraft(token, "all")).isFalse();
    }

    @Test
    @DisplayName("SUPER_ADMIN — tab=all: 모든 DRAFT 제외 (D-A4 SUPER_ADMIN 에도 적용)")
    void tabAll_superAdmin_excludesAllDrafts() throws Exception {
        String token = tokenHelper.superAdminToken();
        assertThat(searchContainsDraft(token, "all")).isFalse();
    }

    @Test
    @DisplayName("Invariant I2: tab != 'my' 결과의 모든 row.status != 'DRAFT'")
    void invariant_tabNotMy_noDraftsInContent() throws Exception {
        String superToken = tokenHelper.superAdminToken();
        for (String tab : new String[]{"department", "all"}) {
            MvcResult result = mockMvc.perform(get("/api/v1/documents/search")
                    .param("tab", tab)
                    .param("size", "50")
                    .header("Authorization", "Bearer " + superToken))
                .andExpect(status().isOk())
                .andReturn();
            JsonNode content = objectMapper.readTree(result.getResponse().getContentAsByteArray())
                .path("data").path("content");
            for (JsonNode row : content) {
                assertThat(row.path("status").asText())
                    .as("row id=%d in tab=%s must not be DRAFT", row.path("id").asLong(), tab)
                    .isNotEqualTo("DRAFT");
            }
        }
    }
}
```

**3-2. `DocumentSearchKeywordTest.java` 확장** — Plan 01 의 smoke 테스트 (`emptyKeyword_returns200_withEmptyResultForEmptyDb`) 를 유지하고 아래 @Test 메서드들을 추가. fixture setUp 에 Alice seed 문서 (SUBMITTED 상태, 권한 predicate 가 본인이므로 항상 통과) 삽입:

```java
// setUp 에 추가: Alice 본인 명의 문서 3개 seed (title/docNumber/drafter name 매치용)
static final long OWNER_ID = 500L;
long titleMatchDocId;
long docNumMatchDocId;

// setUp 내부 (cleanup 후):
jdbcTemplate.update("INSERT IGNORE INTO department (id, name, sort_order, is_active) VALUES (1, '부서1', 1, TRUE)");
String pwHash = "$2a$10$07mcjXBfvelJFwjs8DnoJOnEqprFy.dnQL1NdnRvlqEWwwmX62SOW";
jdbcTemplate.update("INSERT INTO \"user\" (id, employee_no, name, email, password, department_id, position_id, role, status, failed_login_count, must_change_password) " +
    "VALUES (?, 'U_K', '김철수', 'kim-kw@test.com', ?, 1, 3, 'USER', 'ACTIVE', 0, FALSE)", OWNER_ID, pwHash);
jdbcTemplate.update("INSERT INTO document (template_code, drafter_id, title, doc_number, status, submitted_at, created_at, current_step) " +
    "VALUES ('GENERAL', ?, '경비 보고서 2026 Q1', 'GEN-2026-0001', 'SUBMITTED', ?, ?, 1)",
    OWNER_ID, LocalDateTime.now().minusDays(5), LocalDateTime.now().minusDays(5));
titleMatchDocId = jdbcTemplate.queryForObject("SELECT id FROM document WHERE drafter_id=? ORDER BY id DESC LIMIT 1", Long.class, OWNER_ID);
jdbcTemplate.update("INSERT INTO document (template_code, drafter_id, title, doc_number, status, submitted_at, created_at, current_step) " +
    "VALUES ('GENERAL', ?, '100%% 달성률 검토', 'LIT-2026-0002', 'SUBMITTED', ?, ?, 1)",
    OWNER_ID, LocalDateTime.now().minusDays(4), LocalDateTime.now().minusDays(4));
docNumMatchDocId = jdbcTemplate.queryForObject("SELECT id FROM document WHERE drafter_id=? AND title LIKE '100%' ORDER BY id DESC LIMIT 1", Long.class, OWNER_ID);

// 추가 @Test 메서드:

@Test
@DisplayName("keyword 가 title 일부와 매치 — 문서 포함 (SRCH-02)")
void keywordMatchesTitle() throws Exception {
    String token = tokenHelper.tokenForRole(OWNER_ID, "kim-kw@test.com", "김철수", UserRole.USER, 1L);
    MvcResult r = mockMvc.perform(get("/api/v1/documents/search")
            .param("keyword", "경비")
            .param("tab", "my")
            .header("Authorization", "Bearer " + token))
        .andExpect(status().isOk())
        .andReturn();
    JsonNode content = objectMapper.readTree(r.getResponse().getContentAsByteArray()).path("data").path("content");
    boolean found = false;
    for (JsonNode row : content) if (row.path("id").asLong() == titleMatchDocId) { found = true; break; }
    assertThat(found).isTrue();
}

@Test
@DisplayName("keyword 가 docNumber 일부와 매치")
void keywordMatchesDocNumber() throws Exception {
    String token = tokenHelper.tokenForRole(OWNER_ID, "kim-kw@test.com", "김철수", UserRole.USER, 1L);
    MvcResult r = mockMvc.perform(get("/api/v1/documents/search")
            .param("keyword", "GEN-2026")
            .param("tab", "my")
            .header("Authorization", "Bearer " + token))
        .andExpect(status().isOk())
        .andReturn();
    assertThat(objectMapper.readTree(r.getResponse().getContentAsByteArray()).path("data").path("totalElements").asLong())
        .isGreaterThanOrEqualTo(1);
}

@Test
@DisplayName("keyword 가 drafter.name 일부와 매치")
void keywordMatchesDrafterName() throws Exception {
    // SUPER_ADMIN 이 '김철' 로 검색 — Alice/Bob seed 사용자가 없어야 함 (김철수만 있음)
    String token = tokenHelper.superAdminToken();
    MvcResult r = mockMvc.perform(get("/api/v1/documents/search")
            .param("keyword", "김철")
            .param("tab", "all")
            .header("Authorization", "Bearer " + token))
        .andExpect(status().isOk())
        .andReturn();
    assertThat(objectMapper.readTree(r.getResponse().getContentAsByteArray()).path("data").path("totalElements").asLong())
        .isGreaterThanOrEqualTo(1);
}

@Test
@DisplayName("escapeLikePattern 회귀 — keyword '100%' 는 리터럴 매치 (와일드카드 주입 방어, D-D8)")
void escapeLikePattern_wildcardKeywords_matchLiteral() throws Exception {
    String token = tokenHelper.tokenForRole(OWNER_ID, "kim-kw@test.com", "김철수", UserRole.USER, 1L);
    MvcResult r = mockMvc.perform(get("/api/v1/documents/search")
            .param("keyword", "100%")
            .param("tab", "my")
            .header("Authorization", "Bearer " + token))
        .andExpect(status().isOk())
        .andReturn();
    JsonNode content = objectMapper.readTree(r.getResponse().getContentAsByteArray()).path("data").path("content");
    // 문서 2 ('100% 달성률 검토') 는 매치, 문서 1 ('경비 보고서') 은 매치 안 됨 — % 가 와일드카드로 확장되었다면 양쪽 모두 매치 → 에러
    long matchCount = 0;
    for (JsonNode row : content) if (row.path("id").asLong() == docNumMatchDocId) matchCount++;
    assertThat(matchCount).isEqualTo(1);
    // 추가: 총 결과 건수가 2 가 아님 (만약 % 가 와일드카드로 남아있었다면 경비 문서도 매치되어 2 이상)
}
```

**3-3. `DocumentSearchStatusFilterTest.java` 확장** — Plan 01 의 smoke 테스트 유지 + setUp 에 SUBMITTED/APPROVED/REJECTED 문서 3 개 seed + 추가 검증:

```java
// setUp 에 seed 추가 (Alice 본인 명의 3 문서):
static final long OWNER_ID = 600L;

// setUp 내부 cleanup 후:
jdbcTemplate.update("INSERT IGNORE INTO department (id, name, sort_order, is_active) VALUES (1, '부서1', 1, TRUE)");
jdbcTemplate.update("INSERT INTO \"user\" (id, employee_no, name, email, password, department_id, position_id, role, status, failed_login_count, must_change_password) " +
    "VALUES (?, 'U_S', 'Owner', 'owner-sf@test.com', '$2a$10$07mcjXBfvelJFwjs8DnoJOnEqprFy.dnQL1NdnRvlqEWwwmX62SOW', 1, 3, 'USER', 'ACTIVE', 0, FALSE)", OWNER_ID);
for (String st : new String[]{"SUBMITTED", "APPROVED", "REJECTED"}) {
    jdbcTemplate.update("INSERT INTO document (template_code, drafter_id, title, status, submitted_at, created_at, current_step) " +
        "VALUES ('GENERAL', ?, ?, ?, ?, ?, 1)",
        OWNER_ID, "test-" + st, st, LocalDateTime.now().minusDays(3), LocalDateTime.now().minusDays(3));
}

// 추가 @Test:
@Test
@DisplayName("statuses=[SUBMITTED] 단일 — SUBMITTED 만 매치 (I7 역호환)")
void singleStatus_filtersExactMatch() throws Exception {
    String token = tokenHelper.tokenForRole(OWNER_ID, "owner-sf@test.com", "Owner", UserRole.USER, 1L);
    MvcResult r = mockMvc.perform(get("/api/v1/documents/search")
            .param("status", "SUBMITTED")
            .param("tab", "my")
            .header("Authorization", "Bearer " + token))
        .andExpect(status().isOk())
        .andReturn();
    JsonNode content = objectMapper.readTree(r.getResponse().getContentAsByteArray()).path("data").path("content");
    for (JsonNode row : content) {
        assertThat(row.path("status").asText()).isEqualTo("SUBMITTED");
    }
    assertThat(content.size()).isEqualTo(1);
}

@Test
@DisplayName("statuses=[SUBMITTED, APPROVED] 복수 — SUBMITTED+APPROVED 매치, REJECTED 제외")
void multipleStatuses_filtersBoth() throws Exception {
    String token = tokenHelper.tokenForRole(OWNER_ID, "owner-sf@test.com", "Owner", UserRole.USER, 1L);
    MvcResult r = mockMvc.perform(get("/api/v1/documents/search")
            .param("status", "SUBMITTED")
            .param("status", "APPROVED")
            .param("tab", "my")
            .header("Authorization", "Bearer " + token))
        .andExpect(status().isOk())
        .andReturn();
    JsonNode content = objectMapper.readTree(r.getResponse().getContentAsByteArray()).path("data").path("content");
    assertThat(content.size()).isEqualTo(2);
    java.util.Set<String> statuses = new java.util.HashSet<>();
    for (JsonNode row : content) statuses.add(row.path("status").asText());
    assertThat(statuses).containsExactlyInAnyOrder("SUBMITTED", "APPROVED");
}

@Test
@DisplayName("statuses=[] 누락 — tab=my 는 DRAFT 포함 모두, tab=all 은 DRAFT 제외 모두")
void emptyStatuses_noFilter_respectsDraftGate() throws Exception {
    String token = tokenHelper.tokenForRole(OWNER_ID, "owner-sf@test.com", "Owner", UserRole.USER, 1L);
    MvcResult r = mockMvc.perform(get("/api/v1/documents/search")
            .param("tab", "my")
            .header("Authorization", "Bearer " + token))
        .andExpect(status().isOk())
        .andReturn();
    // Owner has 3 documents (no DRAFT in this fixture)
    assertThat(objectMapper.readTree(r.getResponse().getContentAsByteArray()).path("data").path("totalElements").asLong())
        .isEqualTo(3);
}
```
  </action>
  <verify>
    <automated>./gradlew test --tests 'com.micesign.document.DocumentSearchDraftGateTest' --tests 'com.micesign.document.DocumentSearchKeywordTest' --tests 'com.micesign.document.DocumentSearchStatusFilterTest' --tests 'com.micesign.document.DocumentSearchPermissionMatrixTest'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f backend/src/test/java/com/micesign/document/DocumentSearchDraftGateTest.java` exit 0
    - `grep -c "@Test" backend/src/test/java/com/micesign/document/DocumentSearchDraftGateTest.java` ≥ 5
    - `grep -q "invariant_tabNotMy_noDraftsInContent" backend/src/test/java/com/micesign/document/DocumentSearchDraftGateTest.java` exit 0 (I2 invariant)
    - `grep -q "escapeLikePattern" backend/src/test/java/com/micesign/document/DocumentSearchKeywordTest.java` exit 0
    - `grep -q 'param("status", "SUBMITTED")' backend/src/test/java/com/micesign/document/DocumentSearchStatusFilterTest.java` exit 0 (복수 호출)
    - `grep -c "@Test" backend/src/test/java/com/micesign/document/DocumentSearchKeywordTest.java` ≥ 5 (smoke 1 + 확장 4)
    - `grep -c "@Test" backend/src/test/java/com/micesign/document/DocumentSearchStatusFilterTest.java` ≥ 5 (smoke 2 + 확장 3)
    - `./gradlew test --tests 'com.micesign.document.DocumentSearch*'` exit 0 — 모든 DocumentSearch* 테스트 green
    - `./gradlew test` exit 0 (**전체 회귀 보호**)
  </acceptance_criteria>
  <done>
    DRAFT gate 5 테스트 + keyword OR 5 테스트 + status filter 5 테스트 모두 green. I2 invariant 단일 테스트로 회귀 자동 감지. `./gradlew test` 전체 회귀 pass → **PR1 hotfix 머지 가능 상태 도달**. SRCH-01 + SRCH-02 + SRCH-03 모두 검증.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated user → `/api/v1/documents/search` | 권한 predicate 의 단일 SoT 지점 (QueryDSL 빌더) |
| `DocumentSearchCondition.tab` → QueryDSL builder | tab 값 변조로 DRAFT gate 우회 시도 가능성 |
| `ApprovalLine` 영속 상태 → `JPAExpressions EXISTS` 서브쿼리 | SUBMITTED 이후 approval_line 영구 보존 가정 (Finding 9.1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Elevation of Privilege (V4 ASVS BOLA/IDOR) | DocumentRepositoryCustomImpl.searchDocuments 권한 predicate | mitigate | Task 1 에서 FSD 4-브랜치 predicate 주입 (본인 OR EXISTS approval_line OR ADMIN AND 부서 OR SUPER_ADMIN). Task 2 의 28 케이스 매트릭스로 검증 — 특히 C(무관 사용자) 가 A 의 SUBMITTED 문서를 검색 시 결과 0 건 (`unrelatedUser_seesNothing`), D(REFERENCE 참조자) 는 결과 포함 (`userD_referenceApprover`), F(다른 부서 ADMIN, 승인라인 미등록) 결과 0 건 (`userF_otherDepartmentAdmin_notInApprovalLine`). |
| T-30-02 | Information Disclosure (V4/V11) | DocumentRepositoryCustomImpl.searchDocuments DRAFT gate | mitigate | Task 1 의 `if (!"my".equals(tab)) where.and(doc.status.ne(DocumentStatus.DRAFT));` 강제. Task 3 의 `invariant_tabNotMy_noDraftsInContent` 가 tab=department/all 결과에 DRAFT 행 0 개를 assert. 특히 `tabDepartment_excludesOwnDraft` + `tabAll_superAdmin_excludesAllDrafts` 로 ADMIN/SUPER_ADMIN 에도 동일 정책 적용 검증 (D-A4). |
| T-30-05 | Elevation of Privilege (보조 방어) | Controller + QueryDSL 이중 방어 | mitigate | Plan 01 Task 2 의 `AUTH_FORBIDDEN` 403 가드 (D-A7) + 본 Plan Task 1 의 권한 predicate 가 USER 의 tab=all 에서도 본인+approval_line 로 자동 좁힘 (D-A2). 두 방어 모두 Task 2 (`userG_superAdmin`, `unrelatedUser_seesNothing`) 로 간접 검증. |
| T-30-06 | Tampering (V5 — SQL injection via keyword) | DocumentRepositoryCustomImpl escapeLikePattern | mitigate | Task 1 은 `escapeLikePattern` 함수 **무변경 유지 (D-D8)**. Task 3 의 `escapeLikePattern_wildcardKeywords_matchLiteral` 테스트가 `keyword='100%'` 가 와일드카드로 확장되지 않고 리터럴 `100%` 만 매치됨을 검증 — SQL injection 방어 회귀 보호. |
</threat_model>

<verification>
**Full-suite regression:**

```bash
./gradlew test
```

**Phase-level smoke:**

```bash
./gradlew test --tests 'com.micesign.document.DocumentSearch*'
```

기대 결과: Condition 4 + InvalidEnum 5 + Keyword 5 + StatusFilter 5 + PermissionMatrix 8+ + DraftGate 5 = 총 **최소 32 개 @Test** 모두 pass.

**PR1 hotfix 머지 게이트:**
- [ ] `./gradlew test` exit 0 (전체 회귀)
- [ ] `DocumentSearchPermissionMatrixTest` 28 케이스 커버
- [ ] `DocumentSearchDraftGateTest` I2 invariant pass
- [ ] 기존 `ApprovalWorkflowTest`, `DocumentSubmitTest`, `DocumentControllerTest` 여전히 green
- [ ] grep 확인: `JPAExpressions.selectOne()`, `doc.status.ne(DocumentStatus.DRAFT)`, `doc.countDistinct()` 모두 DocumentRepositoryCustomImpl 에 존재
</verification>

<success_criteria>
- `DocumentRepositoryCustomImpl.searchDocuments` 에 FSD FN-SEARCH-001 권한 predicate 주입 완료 (SUPER_ADMIN skip / ADMIN 부서 확장 / USER 본인+EXISTS approval_line)
- DRAFT gate `if (!"my".equals(tab)) where.and(doc.status.ne(DocumentStatus.DRAFT))` 삽입
- count query 가 `doc.count()` → `doc.countDistinct()` 로 교체 (NFR-01 준비)
- 권한 매트릭스 테스트 8+ 메서드 (A/B/C/D/E/F/G + unrelated) 모두 pass — 28 케이스 논리 커버
- DRAFT gate 5 테스트 pass (my 포함 / department 제외 / all 제외 / SUPER_ADMIN 제외 / I2 invariant)
- keyword 5 테스트 pass (empty / title / docNumber / drafter.name / escapeLikePattern 와일드카드 방어)
- status filter 5 테스트 pass (smoke 2 + 단일 / 복수 / empty)
- `./gradlew test` 전체 회귀 exit 0 → PR1 hotfix 머지 가능
</success_criteria>

<output>
After completion, create `.planning/phases/30-where/30-02-SUMMARY.md`:
- Requirements addressed: SRCH-01 (권한 predicate + DRAFT gate), SRCH-02 (keyword OR 회귀 확장), SRCH-03 (복수 status + 단일 역호환)
- Threats mitigated: T-30-01 (BOLA 완전 차단), T-30-02 (DRAFT 유출 차단), T-30-05 (이중 방어), T-30-06 (SQL injection 방어 회귀)
- Artifacts: DocumentRepositoryCustomImpl.java 권한 predicate 블록 + DRAFT gate + countDistinct, 28 케이스 매트릭스 테스트
- Key pattern established: QueryDSL `JPAExpressions.selectOne().from(X).where(...).exists()` + `BooleanExpression.or()` 재할당 관용구 — Phase 31 대시보드 쿼리에도 재사용 가능
- Plan 03 에게 넘길 인터페이스: 권한 predicate 가 이미 적용되어 있으므로 `GET /users/search` 신설 시 drafterId 필터는 권한 범위 내에서 동작 보장. drafterId.eq 는 이미 적용 됨 (Plan 01 에서 삽입).
- PR1 hotfix **머지 가능 상태** 달성 — 프론트 영향 없이 배포 가능 (기존 `status` 단일 파라미터는 `List<String> rawStatuses` 로 자동 호환)
</output>
