---
phase: 21-schemafieldeditor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/admin/components/SchemaFieldEditor/types.ts
  - frontend/src/features/admin/components/SchemaFieldEditor/constants.ts
  - frontend/src/features/admin/components/SchemaFieldEditor/utils.ts
  - frontend/src/features/admin/components/SchemaFieldEditor/TypeBadge.tsx
  - frontend/src/features/admin/components/SchemaFieldEditor/FieldConfigEditor.tsx
  - frontend/src/features/admin/components/SchemaFieldEditor/FieldCard.tsx
  - frontend/src/features/admin/components/SchemaFieldEditor/SchemaFieldEditor.tsx
  - frontend/src/features/admin/components/SchemaFieldEditor/index.tsx
  - frontend/src/features/admin/components/SchemaFieldEditor.tsx
autonomous: true
requirements:
  - RFT-01

must_haves:
  truths:
    - "SchemaFieldEditor가 7개 파일로 분리되어 있다 (types, constants, utils, TypeBadge, FieldConfigEditor, FieldCard, SchemaFieldEditor + index)"
    - "TemplateFormModal에서 기존 import 경로('./SchemaFieldEditor')가 변경 없이 동작한다"
    - "필드 추가/삭제/순서변경/설정 편집 기능이 리팩토링 전과 동일하게 작동한다"
    - "각 컴포넌트 파일이 200줄 이하이다"
  artifacts:
    - path: "frontend/src/features/admin/components/SchemaFieldEditor/types.ts"
      provides: "SchemaFieldType, SchemaFieldConfig, SchemaField 타입 정의"
      exports: ["SchemaFieldType", "SchemaFieldConfig", "SchemaField"]
    - path: "frontend/src/features/admin/components/SchemaFieldEditor/constants.ts"
      provides: "FIELD_TYPE_META, FALLBACK_TYPE_META, FIELD_TYPES, INPUT_CLASS, SMALL_INPUT_CLASS"
      exports: ["FIELD_TYPE_META", "FALLBACK_TYPE_META", "FIELD_TYPES", "INPUT_CLASS", "SMALL_INPUT_CLASS"]
    - path: "frontend/src/features/admin/components/SchemaFieldEditor/utils.ts"
      provides: "toFieldId 헬퍼 함수"
      exports: ["toFieldId"]
    - path: "frontend/src/features/admin/components/SchemaFieldEditor/TypeBadge.tsx"
      provides: "TypeBadge 컴포넌트"
      exports: ["TypeBadge"]
    - path: "frontend/src/features/admin/components/SchemaFieldEditor/FieldConfigEditor.tsx"
      provides: "FieldConfigEditor 컴포넌트 (switch문 구조 유지)"
      exports: ["FieldConfigEditor"]
    - path: "frontend/src/features/admin/components/SchemaFieldEditor/FieldCard.tsx"
      provides: "FieldCard 컴포넌트"
      exports: ["FieldCard"]
    - path: "frontend/src/features/admin/components/SchemaFieldEditor/SchemaFieldEditor.tsx"
      provides: "메인 SchemaFieldEditor 컴포넌트"
      exports: ["default:SchemaFieldEditor"]
    - path: "frontend/src/features/admin/components/SchemaFieldEditor/index.tsx"
      provides: "barrel export for backward compatibility"
      exports: ["default:SchemaFieldEditor", "SchemaField", "SchemaFieldType", "SchemaFieldConfig"]
  key_links:
    - from: "frontend/src/features/admin/components/TemplateFormModal.tsx"
      to: "SchemaFieldEditor/index.tsx"
      via: "import SchemaFieldEditor from './SchemaFieldEditor'"
      pattern: "import SchemaFieldEditor from './SchemaFieldEditor'"
    - from: "SchemaFieldEditor/FieldCard.tsx"
      to: "SchemaFieldEditor/FieldConfigEditor.tsx"
      via: "FieldConfigEditor component usage inside FieldCard"
      pattern: "<FieldConfigEditor"
    - from: "SchemaFieldEditor/FieldCard.tsx"
      to: "SchemaFieldEditor/TypeBadge.tsx"
      via: "TypeBadge component usage inside FieldCard"
      pattern: "<TypeBadge"
---

<objective>
596줄 모놀리식 SchemaFieldEditor.tsx를 7개의 독립된 파일로 분리하여 유지보수성을 확보한다. 기능 변경 없이 코드 구조만 개선하는 순수 리팩토링이다.

Purpose: Phase 22-26에서 추가될 기능(미리보기, 테이블 컬럼, 조건부 규칙, 계산 규칙)의 코드 기반을 깨끗하게 준비
Output: SchemaFieldEditor/ 폴더에 7개 파일 + index.tsx barrel export
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-schemafieldeditor/21-CONTEXT.md

@frontend/src/features/admin/components/SchemaFieldEditor.tsx
@frontend/src/features/admin/components/TemplateFormModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 폴더 생성 및 하위 모듈 추출 (types, constants, utils, TypeBadge, FieldConfigEditor, FieldCard)</name>
  <files>
    frontend/src/features/admin/components/SchemaFieldEditor/types.ts
    frontend/src/features/admin/components/SchemaFieldEditor/constants.ts
    frontend/src/features/admin/components/SchemaFieldEditor/utils.ts
    frontend/src/features/admin/components/SchemaFieldEditor/TypeBadge.tsx
    frontend/src/features/admin/components/SchemaFieldEditor/FieldConfigEditor.tsx
    frontend/src/features/admin/components/SchemaFieldEditor/FieldCard.tsx
  </files>
  <read_first>
    frontend/src/features/admin/components/SchemaFieldEditor.tsx
    frontend/src/features/admin/components/TemplateFormModal.tsx
  </read_first>
  <action>
Per D-03, D-05, D-06, D-07, D-01: `frontend/src/features/admin/components/SchemaFieldEditor/` 폴더를 생성하고 원본 파일에서 6개 모듈을 추출한다.

**types.ts** (per D-05):
원본 라인 22-55의 타입 정의를 그대로 옮긴다. 모든 타입은 named export:
```typescript
export type SchemaFieldType = 'text' | 'textarea' | 'number' | 'date' | 'select' | 'staticText' | 'hidden' | 'table';

export interface SchemaFieldConfig {
  placeholder?: string;
  maxLength?: number;
  min?: number;
  max?: number;
  unit?: string;
  options?: { value: string; label: string }[];
  content?: string;
  defaultValue?: string;
  width?: string;
}

export interface SchemaField {
  id: string;
  type: SchemaFieldType;
  label: string;
  required: boolean;
  config: SchemaFieldConfig;
}

export interface SchemaFieldEditorProps {
  fields: SchemaField[];
  onChange: (fields: SchemaField[]) => void;
}
```

**constants.ts** (per D-06):
원본 라인 59-116의 상수를 옮긴다. `SchemaFieldType`을 `./types`에서 import한다.
아이콘 import는 이 파일에서 직접 수행 (lucide-react에서 Type, AlignLeft, Hash, Calendar, List, FileText, EyeOff, Table, HelpCircle import):
```typescript
import { Type, AlignLeft, Hash, Calendar, List, FileText, EyeOff, Table, HelpCircle } from 'lucide-react';
import type { SchemaFieldType } from './types';

export const FIELD_TYPE_META: Record<SchemaFieldType, { icon: React.ElementType; color: string; bgColor: string }> = { ... };
export const FALLBACK_TYPE_META = { ... };
export const FIELD_TYPES: SchemaFieldType[] = ['text', 'textarea', 'number', 'date', 'select', 'staticText', 'hidden'];
export const INPUT_CLASS = '...';
export const SMALL_INPUT_CLASS = '...';
```
상수 값은 원본과 완전 동일하게 복사. React import 필요 시 `import type React from 'react'` 추가.

**utils.ts** (per D-07):
원본 라인 87-94의 toFieldId 함수를 그대로 옮긴다:
```typescript
export function toFieldId(label: string): string {
  return label
    .trim()
    .replace(/[^a-zA-Z0-9가-힣\s]/g, '')
    .replace(/\s+(.)/g, (_, c: string) => c.toUpperCase())
    .replace(/\s/g, '')
    .replace(/^(.)/, (_, c: string) => c.toLowerCase());
}
```

**TypeBadge.tsx** (per D-01):
원본 라인 98-110의 TypeBadge 컴포넌트를 옮긴다. import 대상: `useTranslation` from react-i18next, `SchemaFieldType` from ./types, `FIELD_TYPE_META`, `FALLBACK_TYPE_META` from ./constants. named export: `export function TypeBadge(...)`.

**FieldConfigEditor.tsx** (per D-01, D-02):
원본 라인 118-323의 FieldConfigEditor 컴포넌트를 그대로 옮긴다. switch문 구조 유지(D-02). import 대상: `useTranslation` from react-i18next, `Trash2`, `Plus` from lucide-react, `SchemaField`, `SchemaFieldConfig` from ./types, `SMALL_INPUT_CLASS` from ./constants. named export: `export function FieldConfigEditor(...)`.

**FieldCard.tsx** (per D-01):
원본 라인 327-468의 FieldCard 컴포넌트를 옮긴다. import 대상: `useState` from react, `ChevronRight`, `ChevronUp`, `ChevronDown`, `Trash2` from lucide-react, `useTranslation` from react-i18next, `SchemaField` from ./types, `INPUT_CLASS` from ./constants, `toFieldId` from ./utils, `TypeBadge` from ./TypeBadge, `FieldConfigEditor` from ./FieldConfigEditor. named export: `export function FieldCard(...)`. props 타입은 인라인으로 유지(원본과 동일).
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/features/admin/components/SchemaFieldEditor/ 디렉토리가 존재
    - types.ts에 `export type SchemaFieldType`, `export interface SchemaFieldConfig`, `export interface SchemaField`, `export interface SchemaFieldEditorProps` 포함
    - constants.ts에 `export const FIELD_TYPE_META`, `export const FALLBACK_TYPE_META`, `export const FIELD_TYPES`, `export const INPUT_CLASS`, `export const SMALL_INPUT_CLASS` 포함
    - utils.ts에 `export function toFieldId` 포함
    - TypeBadge.tsx에 `export function TypeBadge` 포함, 30줄 이하
    - FieldConfigEditor.tsx에 `export function FieldConfigEditor` 포함, `switch (field.type)` 포함, 210줄 이하
    - FieldCard.tsx에 `export function FieldCard` 포함, `<TypeBadge` 포함, `<FieldConfigEditor` 포함, 160줄 이하
    - 6개 파일 모두 200줄 이하 (`wc -l` 확인)
  </acceptance_criteria>
  <done>types.ts, constants.ts, utils.ts, TypeBadge.tsx, FieldConfigEditor.tsx, FieldCard.tsx 6개 파일이 SchemaFieldEditor/ 폴더에 생성되고 각각 200줄 이하이며 TypeScript 컴파일 에러 없음</done>
</task>

<task type="auto">
  <name>Task 2: 메인 컴포넌트 재작성 + barrel export + 원본 파일 삭제</name>
  <files>
    frontend/src/features/admin/components/SchemaFieldEditor/SchemaFieldEditor.tsx
    frontend/src/features/admin/components/SchemaFieldEditor/index.tsx
    frontend/src/features/admin/components/SchemaFieldEditor.tsx
  </files>
  <read_first>
    frontend/src/features/admin/components/SchemaFieldEditor.tsx
    frontend/src/features/admin/components/TemplateFormModal.tsx
    frontend/src/features/admin/components/SchemaFieldEditor/types.ts
    frontend/src/features/admin/components/SchemaFieldEditor/FieldCard.tsx
  </read_first>
  <action>
Per D-01, D-04:

**SchemaFieldEditor/SchemaFieldEditor.tsx** (메인 컴포넌트):
원본 라인 472-596의 메인 컴포넌트를 새 파일에 작성한다. import 대상:
- `useState`, `useRef`, `useEffect` from react
- `Plus` from lucide-react
- `useTranslation` from react-i18next
- `SchemaField`, `SchemaFieldType`, `SchemaFieldEditorProps` from ./types
- `FIELD_TYPE_META`, `FALLBACK_TYPE_META`, `FIELD_TYPES` from ./constants
- `FieldCard` from ./FieldCard

`addField`, `updateField`, `moveField`, `deleteField` 함수 및 JSX는 원본과 완전 동일하게 유지.
`export default function SchemaFieldEditor(...)` default export 유지.

**SchemaFieldEditor/index.tsx** (per D-04):
barrel export로 기존 import 경로 호환 유지:
```typescript
export { default } from './SchemaFieldEditor';
export type { SchemaField, SchemaFieldType, SchemaFieldConfig } from './types';
```

**원본 파일 삭제:**
`frontend/src/features/admin/components/SchemaFieldEditor.tsx` (원본 단일 파일)를 삭제한다.
이제 `./SchemaFieldEditor`는 디렉토리의 index.tsx를 resolve하므로 TemplateFormModal.tsx의 `import SchemaFieldEditor from './SchemaFieldEditor'` 와 `import type { SchemaField } from './SchemaFieldEditor'` 가 변경 없이 동작한다.

**TemplateFormModal.tsx:**
import 문 변경 불필요. `'./SchemaFieldEditor'` 경로가 디렉토리 index로 resolve됨. 수정하지 않는다.
  </action>
  <verify>
    <automated>cd /Volumes/USB-SSD/03-code/VibeCoding/MiceSign/frontend && npx tsc --noEmit --pretty 2>&1 | head -30 && echo "---LINE-COUNT---" && wc -l src/features/admin/components/SchemaFieldEditor/*.ts src/features/admin/components/SchemaFieldEditor/*.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SchemaFieldEditor/SchemaFieldEditor.tsx에 `export default function SchemaFieldEditor` 포함, 130줄 이하
    - SchemaFieldEditor/index.tsx에 `export { default } from './SchemaFieldEditor'` 포함, `export type { SchemaField` 포함
    - 원본 frontend/src/features/admin/components/SchemaFieldEditor.tsx 파일이 존재하지 않음 (디렉토리로 대체됨)
    - `npx tsc --noEmit` 에러 없음 (exit code 0)
    - TemplateFormModal.tsx의 import 문이 변경되지 않음: `import SchemaFieldEditor from './SchemaFieldEditor'` 및 `import type { SchemaField } from './SchemaFieldEditor'` 그대로
    - 모든 파일이 200줄 이하 (`wc -l` 확인)
  </acceptance_criteria>
  <done>SchemaFieldEditor/ 폴더의 8개 파일로 완전 분리 완료, 원본 단일 파일 삭제됨, TypeScript 컴파일 성공, TemplateFormModal import 호환 유지, 모든 파일 200줄 이하</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | 순수 리팩토링 - 새로운 trust boundary 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | N/A | N/A | accept | 순수 코드 구조 리팩토링으로 새로운 보안 위협 없음. 기존 입력 처리 로직 변경 없음. |
</threat_model>

<verification>
1. `npx tsc --noEmit` 성공 (TypeScript 컴파일 에러 없음)
2. SchemaFieldEditor/ 폴더에 8개 파일 존재 (types.ts, constants.ts, utils.ts, TypeBadge.tsx, FieldConfigEditor.tsx, FieldCard.tsx, SchemaFieldEditor.tsx, index.tsx)
3. 원본 SchemaFieldEditor.tsx 단일 파일 삭제됨
4. 모든 파일 200줄 이하 확인
5. TemplateFormModal.tsx import 문 변경 없음 확인
</verification>

<success_criteria>
- SchemaFieldEditor가 8개 파일로 분리되어 각각 200줄 이하
- TypeScript 컴파일 에러 없음
- TemplateFormModal에서 기존 import 경로 호환 유지
- 양식 생성/편집 기능이 동일하게 작동 (수동 QA, per D-10)
</success_criteria>

<output>
After completion, create `.planning/phases/21-schemafieldeditor/21-01-SUMMARY.md`
</output>
