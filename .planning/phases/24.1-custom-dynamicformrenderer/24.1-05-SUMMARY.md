---
phase: 24.1
plan: 05
status: complete
---

# Plan 24.1-05 — UAT 수동 검증 게이트 (SUMMARY)

**Role:** Phase 24.1 최종 게이트. 자동(tsc + build) + 수동(admin 회귀 / CUSTOM e2e / snapshot 정책 / 하드코딩 회귀 / D-10 payload) 검증.

## 결과

- **Task 1 — 자동 검증:** PASS
  - `npx tsc --noEmit` exit 0
  - `npm run build` exit 0 (836ms, 1,315.08 kB / gzip 376.59 kB)
  - 5개 신규 dynamic 컴포넌트 파일 존재 확인
  - Editor/Detail CUSTOM fallback grep 확인
  - D-26 빈 행 제거 로직 grep 확인
- **Task 2 — 수동 UAT:** PASS (사용자 "approved" 사인오프, 2026-04-13)
  - A. admin FormPreview 회귀 4항목 (24-V1~V4) — Phase 24 동등성 유지
  - B. CUSTOM e2e 13항목 (IR-01/02/04/05/06/07/08, D-16/19/22/26-PRE/POST)
  - C. IR-09 snapshot 정책 1항목
  - D. GENERAL 하드코딩 템플릿 회귀 방지 2항목
  - E. D-10 payload 정책 2항목 (request 미포함 / response 채워짐)

## Baseline 사전 수정 (옵션 A 인라인)

Phase 24 잔존 baseline red 2건을 본 plan 실행 직전 동일 커밋 그룹에서 해소:

- `frontend/package.json` — `vitest`, `@testing-library/jest-dom` devDependency 추가
- `frontend/src/features/document/components/templates/ExpenseForm.tsx:6` — 미사용 `parseNumericInput` import 제거

이로써 downstream phase들도 `npm run build` 기반 자동 검증을 green baseline 에서 시작할 수 있다.

## 커밋

- `2bef69b` fix(24.1-05): restore build baseline (install vitest/jest-dom, drop unused import)
- `cac9784` feat(24.1-05): add UAT skeleton with automated checks green
- (본 SUMMARY 커밋)

## Phase 24.1 Retrospective

**목표:** 기존 user-side 하드코딩 템플릿 시스템에 admin `schemaDefinition` 기반 CUSTOM 양식을 붙여, admin FormPreview 와 동등한 UX 를 user side 에서 재현.

**실행 요약 (5 plans / 4 waves / sequential):**

| Wave | Plan | 핵심 산출물 |
|------|------|-------------|
| 1 | 01 | `DocumentDetailResponse` 에 `schemaVersion` / `schemaDefinitionSnapshot` 타입 필드 + TemplateEdit/ReadOnly props `schemaSnapshot` 채널 |
| 1 | 02 | `DynamicFieldRenderer` (7개 필드 타입) + `DynamicTableField` (useFieldArray) + `adaptSchemaField` adapter |
| 2 | 03 | `DynamicCustomForm` (FormProvider + 커스텀 Resolver + evaluateConditions + executeCalculations + D-26 빈 행 제거) + `DynamicCustomReadOnly` |
| 3 | 04 | admin `PreviewFieldRenderer` → `DynamicFieldRenderer` 위임, `DocumentEditorPage` / `DocumentDetailPage` 에 CUSTOM fallback 분기 |
| 4 | 05 | tsc + build green + 수동 UAT 사인오프 + baseline 복구 |

**Locked decisions 준수:**
- **D-06** admin FormPreview 회귀 0 건 (선택 A: FormPreview.tsx 무변경, PreviewFieldRenderer 만 위임)
- **D-10** 프론트 request payload 에 schema 필드 미포함 (백엔드 자동 스냅샷)
- **D-16** hidden required 필드는 검증 제외 (커스텀 Resolver)
- **D-19** 숨김 필드 값 보존 (setValue 보류)
- **D-22** readOnly 에서 숨김 필드 렌더 제외
- **D-26** 빈 table 행 제출 시 filter 제거
- **IR-09** snapshot 기반 구 문서 안정 렌더

**주요 deviation:**
- Plan 02 Task — `PLAN.md` 가 `field.options` / `field.columns` / `config.text` 를 가정했으나 실제 dynamicForm.ts 는 `field.config?.options` / `field.config?.columns` / `config.content` 구조. 실제 타입에 맞춰 정정 (Rule 3, 의미 동일).
- Plan 03 Task — `react-router-dom` → `react-router` (v7 단일 패키지) 정정.
- Plan 05 Task 1 — phase 24 baseline red 로 인해 option A 인라인 fix 선행.

**후속 권장:**
- Phase 24 에서 중단되었던 vitest 기반 테스트 세트업 살리기 (`src/test/setup.ts` + 4개 테스트 파일) — 별도 `/gsd:quick`.
- `dist/assets/index-*.js` 번들이 1.3 MB 로 chunk-size 경고 발생. 동적 import 기반 route-level code splitting 은 향후 phase 에서 고려.

**Status:** ✅ COMPLETE
