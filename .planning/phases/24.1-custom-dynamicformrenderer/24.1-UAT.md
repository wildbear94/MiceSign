# Phase 24.1 — UAT (User Acceptance Test)

**Phase:** 24.1-custom-dynamicformrenderer
**Executed (auto):** 2026-04-13

## 1. 자동 검증

| Check | Command | Result | Notes |
|-------|---------|--------|-------|
| typecheck | `cd frontend && npx tsc --noEmit` | ✅ PASS | exit 0 |
| build | `cd frontend && npm run build` | ✅ PASS | 836ms, dist/index-psrgM0Hf.js 1,315.08 kB (gzip 376.59 kB) |
| DynamicFieldRenderer exists | ls | ✅ PASS | `frontend/src/features/document/components/dynamic/DynamicFieldRenderer.tsx` |
| DynamicTableField exists | ls | ✅ PASS | `frontend/src/features/document/components/dynamic/DynamicTableField.tsx` |
| DynamicCustomForm exists | ls | ✅ PASS | `frontend/src/features/document/components/dynamic/DynamicCustomForm.tsx` |
| DynamicCustomReadOnly exists | ls | ✅ PASS | `frontend/src/features/document/components/dynamic/DynamicCustomReadOnly.tsx` |
| adaptSchemaField exists | ls | ✅ PASS | `frontend/src/features/document/components/dynamic/adaptSchemaField.ts` |
| Editor fallback wired | grep `DYNAMIC_CUSTOM_EDIT` | ✅ PASS | `DocumentEditorPage.tsx` |
| Detail fallback wired | grep `DYNAMIC_CUSTOM_READONLY` | ✅ PASS | `DocumentDetailPage.tsx` |
| Type field added | grep `schemaDefinitionSnapshot` | ✅ PASS | `types/document.ts` |
| D-26 빈 행 제거 로직 존재 | grep `filter(` | ✅ PASS | `DynamicCustomForm.tsx` |

**Baseline pre-fix (이 plan 실행 직전 수행):**
- `npm install --save-dev vitest @testing-library/jest-dom` (Phase 24 잔존 devDependency 누락 해소)
- `ExpenseForm.tsx:6` 의 미사용 `parseNumericInput` import 제거
- 위 2건은 Phase 24에서 넘어온 baseline red 이슈로 `deferred-items.md`에 추적되어 있었으며, `/gsd-execute-phase` 세션 중 옵션 A 선택에 따라 인라인으로 처리함.

## 2. 수동 검증 체크리스트

> 이 섹션은 Task 2 (`checkpoint:human-verify`) 수행 시 브라우저에서 실제 동작을 확인하며 체크한다.
> 실행 절차는 `24.1-05-PLAN.md` Task 2 의 `<how-to-verify>` 를 따른다.

### A. admin FormPreview 회귀 (Phase 24 재검증 — D-06)

- [x] **[24-V1] conditionalRules 동작** — 조건 필드 값 변경 시 대상 필드 show/hide 즉시 반영
- [x] **[24-V2] 미리보기 인터랙션** — 모든 필드에 입력 가능, 값 보존
- [x] **[24-V3] 필드 삭제 토스트** — 필드 삭제 시 sonner 토스트 정상 표시
- [x] **[24-V4] 저장/로드 라운드트립** — 양식 저장 → 새로고침 → 필드 복원

### B. CUSTOM 양식 사용자 시나리오 (IR-01~IR-09)

- [x] **[IR-01] DynamicCustomForm 렌더** — 모든 필드 admin 미리보기와 동일 표시
- [x] **[IR-04] 실시간 conditionalRules** — 입력 시 즉시 show/hide/required 반영
- [x] **[IR-05] 실시간 calculationRules** — 결과 필드 자동 갱신, disabled
- [x] **[IR-06] 유효성 검증** — 필수 필드 누락 시 제출 차단 + 인라인 에러
- [x] **[IR-07] table 행 추가/삭제** — 값 보존 확인
- [x] **[D-16] 숨김 필드 검증 제외** — 숨겨진 required 필드가 제출 차단 유발 X
- [x] **[D-19] 숨김 값 보존** — 숨겼다가 다시 표시하면 이전 입력값 복원
- [x] **[D-26-PRE] 빈 행 시나리오 셋업** — table 3행: 1행 전 컬럼 / 2행 공백 / 3행 1컬럼만

### 제출 후 DocumentDetailPage

- [x] **[IR-02] DynamicCustomReadOnly 렌더** — 모든 값 작성 시점 그대로
- [x] **[D-22] readOnly 숨김 필드 미표시** — 작성 시 숨긴 필드는 표시 안 됨
- [x] **[IR-08] formData 라운드트립** — text/number/date/select/table 모두 복원
- [x] **[D-26-POST] 빈 table 행 자동 제거** — readOnly 화면에 2행만 표시 (완전 공백 1행 제거, 1컬럼만 채운 행 유지)
- [x] **(선택) DB 직접 확인** — `document_content.formData` JSON 에서 빈 행 제거 확인

### C. Schema snapshot 정책 (IR-09)

- [x] **[IR-09] snapshot 정상 표시** — 양식 필드 삭제/수정 후에도 기존 문서는 변경 전 상태로 표시

### D. 기존 하드코딩 템플릿 회귀 방지

- [x] `/documents/new/GENERAL` → GeneralForm 정상 렌더 (CUSTOM fallback 으로 빠지지 X)
- [x] 기존 GENERAL 문서 조회 → GeneralReadOnly 정상 렌더

### E. D-10 백엔드 자동 저장 정책 검증

- [x] 제출 시 POST/PUT request body 에 `schemaDefinition` / `schemaSnapshot` / `schemaDefinitionSnapshot` 키 **없음**
- [x] 조회 응답에 `schemaDefinitionSnapshot` **채워져 있음**

**실패 기록 포맷:** `[항목코드] 증상 / 재현 / 콘솔 에러` 를 아래에 append.

(실패 기록 없음 — 사용자 "approved" 사인오프)

## 3. 최종 사인오프

- [x] 모든 자동 검증 PASS
- [x] 모든 수동 검증 PASS (사용자 approved — 2026-04-13)
- [x] Phase 24.1 COMPLETE
