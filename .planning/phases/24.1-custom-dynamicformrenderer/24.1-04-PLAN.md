---
phase: 24.1
plan: 04
type: execute
wave: 3
depends_on: [24.1-02, 24.1-03]
files_modified:
  - frontend/src/features/admin/components/FormPreview/PreviewFieldRenderer.tsx
  - frontend/src/features/admin/components/FormPreview/FormPreview.tsx
  - frontend/src/features/document/components/templates/templateRegistry.ts
  - frontend/src/features/document/pages/DocumentEditorPage.tsx
  - frontend/src/features/document/pages/DocumentDetailPage.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-06
  - D-10
  - D-24
  - D-25
  - IR-01
  - IR-02
  - IR-07
must_haves:
  truths:
    - "admin FormPreview가 DynamicFieldRenderer를 경유해 필드를 렌더하며 기존 미리보기 UX가 회귀 없이 유지된다"
    - "DocumentEditorPage가 TEMPLATE_REGISTRY lookup 실패 시 DynamicCustomForm으로 fallback한다"
    - "DocumentDetailPage가 TEMPLATE_REGISTRY lookup 실패 또는 schemaDefinitionSnapshot 존재 시 DynamicCustomReadOnly로 fallback한다"
    - "두 페이지가 existingDoc.schemaDefinitionSnapshot을 initialData.schemaSnapshot 및 ReadOnly schemaSnapshot에 전달한다"
    - "onSave/onUpdate payload에 schema 필드가 추가되지 않는다 (D-10 — 백엔드 자동 저장)"
  artifacts:
    - path: "frontend/src/features/admin/components/FormPreview/PreviewFieldRenderer.tsx"
      provides: "DynamicFieldRenderer로 위임 — adapter 경유"
    - path: "frontend/src/features/document/pages/DocumentEditorPage.tsx"
      provides: "CUSTOM fallback 분기 + schemaSnapshot 전달"
    - path: "frontend/src/features/document/pages/DocumentDetailPage.tsx"
      provides: "CUSTOM fallback 분기 + schemaSnapshot 전달"
  key_links:
    - from: "DocumentEditorPage"
      to: "DynamicCustomForm"
      via: "TEMPLATE_REGISTRY miss fallback"
      pattern: "DynamicCustomForm"
    - from: "DocumentDetailPage"
      to: "DynamicCustomReadOnly"
      via: "schemaDefinitionSnapshot 존재 시 fallback"
      pattern: "DynamicCustomReadOnly"
    - from: "admin FormPreview"
      to: "DynamicFieldRenderer (via adapter)"
      via: "PreviewFieldRenderer re-export"
      pattern: "adaptSchemaFieldToFieldDefinition"
threat_refs:
  - T-24.1-04
---

<objective>
Wave 2에서 만든 컴포넌트를 실제 페이지와 admin FormPreview에 통합한다. admin PreviewFieldRenderer는 DynamicFieldRenderer로 위임(D-06 회귀 없음 목표), DocumentEditorPage/DocumentDetailPage는 TEMPLATE_REGISTRY lookup 실패 + schemaDefinitionSnapshot 존재 신호로 CUSTOM fallback 분기를 추가한다.

Purpose: D-01~D-03, D-06 통합 완료. 실제 CUSTOM 양식이 end-to-end로 동작.
Output: 5개 파일 수정 (admin 2 + registry 1 + pages 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/24.1-custom-dynamicformrenderer/24.1-CONTEXT.md
@.planning/phases/24.1-custom-dynamicformrenderer/24.1-RESEARCH.md
@frontend/src/features/admin/components/FormPreview/PreviewFieldRenderer.tsx
@frontend/src/features/admin/components/FormPreview/FormPreview.tsx
@frontend/src/features/document/components/templates/templateRegistry.ts
@frontend/src/features/document/pages/DocumentEditorPage.tsx
@frontend/src/features/document/pages/DocumentDetailPage.tsx
@frontend/src/features/document/components/dynamic/DynamicFieldRenderer.tsx
@frontend/src/features/document/components/dynamic/DynamicCustomForm.tsx
@frontend/src/features/document/components/dynamic/DynamicCustomReadOnly.tsx
@frontend/src/features/document/components/dynamic/adaptSchemaField.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: admin PreviewFieldRenderer를 DynamicFieldRenderer 위임으로 리팩터링</name>
  <files>
    frontend/src/features/admin/components/FormPreview/PreviewFieldRenderer.tsx
    frontend/src/features/admin/components/FormPreview/FormPreview.tsx
  </files>
  <read_first>
    - frontend/src/features/admin/components/FormPreview/PreviewFieldRenderer.tsx (전체 — 기존 prop 시그니처 확인)
    - **frontend/src/features/admin/components/FormPreview/FormPreview.tsx (전체 — PreviewFieldRenderer 호출 방식 및 prop 전달 구조 확인. 이 파일을 변경할지/말지 이진 결정의 근거.)**
    - frontend/src/features/document/components/dynamic/DynamicFieldRenderer.tsx
    - frontend/src/features/document/components/dynamic/adaptSchemaField.ts
    - .planning/phases/24.1-custom-dynamicformrenderer/24.1-RESEARCH.md § 8 (회귀 검증 방법)
  </read_first>
  <action>
    **⚠️ FormPreview.tsx 변경 이진 결정 (필수 선행 단계):**

    FormPreview.tsx를 읽은 직후 다음 중 **정확히 하나**를 선택하고 선택 결과를 task 실행 로그 상단에 명시한다:

    - **선택 A — 미변경 (선호):** FormPreview.tsx가 PreviewFieldRenderer에 `{field, value, onChange, error}` 시그니처로 호출 중이면, PreviewFieldRenderer의 export 시그니처를 유지하면서 내부만 DynamicFieldRenderer 위임 구조로 재작성한다. FormPreview.tsx는 **diff 0** (손대지 않음). `files_modified`에서 FormPreview.tsx 라인은 유지하되 실제 git diff는 비어 있어야 한다.
    - **선택 B — 변경:** PreviewFieldRenderer의 시그니처 유지가 기술적으로 불가능한 경우에만 FormPreview.tsx의 import 경로 또는 호출부를 최소 수정한다. 수정 내용은 "import 경로 변경" 또는 "prop 전달 구조 단일 수정" 수준으로 국한한다.

    선택 A가 가능하면 무조건 A. 선택 B로 갈 경우 acceptance_criteria의 grep 조건을 만족시킬 수 있는 범위 내에서만 변경.

    **전략 (선택 A 기준):** admin FormPreview를 완전히 react-hook-form으로 전환하지 않고, 기존 `formValues` state + `onChange` 패턴을 유지한 채 `PreviewFieldRenderer`가 내부에서 mini `useForm`을 만들어 `DynamicFieldRenderer`를 호출하도록 한다.

    ```tsx
    // PreviewFieldRenderer.tsx (thin wrapper)
    import type { SchemaField } from '../SchemaFieldEditor/types';
    import DynamicFieldRenderer from '../../../document/components/dynamic/DynamicFieldRenderer';
    import { adaptSchemaFieldToFieldDefinition } from '../../../document/components/dynamic/adaptSchemaField';
    import { useForm, FormProvider } from 'react-hook-form';
    import { useEffect } from 'react';

    interface PreviewFieldRendererProps {
      field: SchemaField;
      value: unknown;
      onChange: (value: unknown) => void;
      error?: string;
    }

    export default function PreviewFieldRenderer({ field, value, onChange, error }: PreviewFieldRendererProps) {
      const adapted = adaptSchemaFieldToFieldDefinition(field);
      const form = useForm({ defaultValues: { [field.id]: value } });

      useEffect(() => {
        const sub = form.watch((vals) => {
          const v = vals[field.id];
          if (v !== value) onChange(v);
        });
        return () => sub.unsubscribe();
      }, [form, field.id, value, onChange]);

      useEffect(() => {
        if (form.getValues(field.id) !== value) {
          form.setValue(field.id, value as any, { shouldDirty: false });
        }
      }, [value, field.id, form]);

      return (
        <FormProvider {...form}>
          <DynamicFieldRenderer
            field={adapted}
            mode="edit"
            register={form.register as any}
            control={form.control as any}
            error={error}
          />
        </FormProvider>
      );
    }
    ```

    **선택 A인 경우 FormPreview.tsx는 절대 수정하지 않는다** — 기존 시그니처가 호환되어야 한다.

    **table 케이스 처리:** admin FormPreview의 table 렌더는 원본에서 `[0,1]` 고정 disabled였다. DynamicFieldRenderer 위임 시 DynamicTableField가 editable 테이블을 보여주게 된다.
    - 권장: **editable 허용** (더 나은 UX). 회귀 발견 시 Plan 05 수동 체크리스트에 기록.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tee /tmp/24.1-04-1-build.log</automated>
  </verify>
  <acceptance_criteria>
    - PreviewFieldRenderer.tsx가 `DynamicFieldRenderer`를 import — `grep -q "import DynamicFieldRenderer" frontend/src/features/admin/components/FormPreview/PreviewFieldRenderer.tsx`
    - `grep -q "adaptSchemaFieldToFieldDefinition" frontend/src/features/admin/components/FormPreview/PreviewFieldRenderer.tsx`
    - **FormPreview.tsx 이진 결정 검증 — 다음 중 하나가 반드시 참:**
      - (선택 A) `git diff HEAD frontend/src/features/admin/components/FormPreview/FormPreview.tsx` 결과가 **비어 있음** (diff 0 bytes), 또는
      - (선택 B) `grep -q "PreviewFieldRenderer\|DynamicFieldRenderer" frontend/src/features/admin/components/FormPreview/FormPreview.tsx` 이고 diff는 import/호출부 최소 수정에 국한
    - `cd frontend && npm run build` exit 0
    - task 실행 로그 상단에 "선택 A: 미변경" 또는 "선택 B: <최소 변경 이유>" 명시 문구 존재
  </acceptance_criteria>
  <done>admin FormPreview가 build green 상태로 DynamicFieldRenderer를 경유해 동작. FormPreview.tsx 변경/미변경 결정이 문서화됨.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: templateRegistry + DocumentEditorPage + DocumentDetailPage fallback 분기</name>
  <files>
    frontend/src/features/document/components/templates/templateRegistry.ts
    frontend/src/features/document/pages/DocumentEditorPage.tsx
    frontend/src/features/document/pages/DocumentDetailPage.tsx
  </files>
  <read_first>
    - frontend/src/features/document/components/templates/templateRegistry.ts
    - frontend/src/features/document/pages/DocumentEditorPage.tsx (전체 — templateEntry lookup, initialData 구성, onSave 경로)
    - frontend/src/features/document/pages/DocumentDetailPage.tsx (전체 — ReadOnlyComponent lookup)
    - frontend/src/features/document/components/dynamic/DynamicCustomForm.tsx
    - frontend/src/features/document/components/dynamic/DynamicCustomReadOnly.tsx
    - .planning/phases/24.1-custom-dynamicformrenderer/24.1-RESEARCH.md § 3, § 4 (fallback 전략)
  </read_first>
  <action>
    **1) templateRegistry.ts** — 기존 registry는 건드리지 않고, fallback 헬퍼를 export:

    ```ts
    import DynamicCustomForm from '../dynamic/DynamicCustomForm';
    import DynamicCustomReadOnly from '../dynamic/DynamicCustomReadOnly';

    export const DYNAMIC_CUSTOM_EDIT = DynamicCustomForm;
    export const DYNAMIC_CUSTOM_READONLY = DynamicCustomReadOnly;

    export function isCustomTemplate(templateCode: string | undefined, hasSnapshot = false): boolean {
      if (hasSnapshot) return true;
      if (!templateCode) return false;
      return !(templateCode in TEMPLATE_REGISTRY);
    }
    ```

    **2) DocumentEditorPage.tsx** — CUSTOM fallback 분기 추가:

    ```ts
    import { TEMPLATE_REGISTRY, DYNAMIC_CUSTOM_EDIT, isCustomTemplate } from '../components/templates/templateRegistry';
    // ...
    const templateEntry = TEMPLATE_REGISTRY[resolvedTemplateCode];
    const schemaSnapshot = existingDoc?.schemaDefinitionSnapshot ?? null;
    const useCustomRenderer = !templateEntry && (
      isEditMode ? !!schemaSnapshot : isCustomTemplate(resolvedTemplateCode)
    );
    if (!templateEntry && !useCustomRenderer) {
      return <알 수 없는 양식>;
    }
    const EditComponent = templateEntry?.editComponent ?? DYNAMIC_CUSTOM_EDIT;
    <EditComponent
      documentId={documentId}
      initialData={{
        title: existingDoc?.title ?? '',
        bodyHtml: existingDoc?.bodyHtml ?? '',
        formData: existingDoc?.formData ?? undefined,
        schemaSnapshot,
      }}
      onSave={handleSave}
    />
    ```

    **중요:**
    - `handleSave`는 **그대로 사용**. payload에 schema 추가하지 않음 (D-10: 백엔드 자동 snapshot).
    - 실제 DocumentEditorPage 코드 구조에 맞춰 변수명 조정

    **3) DocumentDetailPage.tsx** — CUSTOM fallback 분기 추가:

    ```ts
    import { TEMPLATE_REGISTRY, DYNAMIC_CUSTOM_READONLY } from '../components/templates/templateRegistry';
    // ...
    const templateEntry = TEMPLATE_REGISTRY[doc.templateCode];
    const schemaSnapshot = doc.schemaDefinitionSnapshot ?? null;
    const ReadOnlyComponent = templateEntry?.readOnlyComponent ?? (schemaSnapshot ? DYNAMIC_CUSTOM_READONLY : undefined);

    {ReadOnlyComponent && (
      <ReadOnlyComponent
        title={doc.title}
        bodyHtml={doc.bodyHtml}
        formData={doc.formData}
        schemaSnapshot={schemaSnapshot}
      />
    )}
    ```

    **4) 빌드 검증:** `cd frontend && npm run build` 실행. 실패 시 원인 로그 기록.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tee /tmp/24.1-04-2-build.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "DYNAMIC_CUSTOM_EDIT" frontend/src/features/document/components/templates/templateRegistry.ts`
    - `grep -q "DYNAMIC_CUSTOM_READONLY" frontend/src/features/document/components/templates/templateRegistry.ts`
    - `grep -q "isCustomTemplate" frontend/src/features/document/components/templates/templateRegistry.ts`
    - `grep -q "DYNAMIC_CUSTOM_EDIT" frontend/src/features/document/pages/DocumentEditorPage.tsx`
    - `grep -q "schemaSnapshot" frontend/src/features/document/pages/DocumentEditorPage.tsx`
    - `grep -q "DYNAMIC_CUSTOM_READONLY" frontend/src/features/document/pages/DocumentDetailPage.tsx`
    - `grep -q "schemaDefinitionSnapshot" frontend/src/features/document/pages/DocumentDetailPage.tsx`
    - **D-10 준수:** handleSave 내부에 schemaSnapshot/schemaDefinition 전달이 없음 — `! grep -E "(schemaSnapshot|schemaDefinition).*onSave|handleSave.*schemaSnapshot" frontend/src/features/document/pages/DocumentEditorPage.tsx`
    - `cd frontend && npm run build` exit 0
    - 기존 6개 템플릿 코드(GENERAL 등)는 여전히 TEMPLATE_REGISTRY lookup으로 분기됨 (fallback 진입 안 함)
  </acceptance_criteria>
  <done>CUSTOM 템플릿 fallback이 편집/조회 두 페이지에서 모두 동작하고 build green. D-10 유지.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24.1-04 | Information Disclosure | fallback 진입 조건 (templateCode 오타 시 CUSTOM fallback 시도) | accept | templateApi.getTemplateSchema가 실패하면 DynamicCustomForm이 에러 UI 표시. 민감 정보 노출 없음. |
</threat_model>

<verification>
- `cd frontend && npm run build` 통과
- 기존 TEMPLATE_REGISTRY 하드코딩 템플릿 회귀 없음 (수동: /documents/new/GENERAL 진입 시 GeneralForm 렌더)
- FormPreview.tsx 이진 결정 기록
- D-10 준수: payload에 schema 없음
</verification>

<success_criteria>
- [ ] admin FormPreview가 DynamicFieldRenderer 경유
- [ ] FormPreview.tsx 변경/미변경 이진 결정 기록됨
- [ ] DocumentEditorPage CUSTOM fallback 동작
- [ ] DocumentDetailPage CUSTOM fallback 동작
- [ ] handleSave payload에 schema 필드 없음 (D-10)
- [ ] npm run build green
- [ ] 기존 6개 하드코딩 템플릿 영향 없음
</success_criteria>

<output>
After completion, create `.planning/phases/24.1-custom-dynamicformrenderer/24.1-04-SUMMARY.md`
</output>
