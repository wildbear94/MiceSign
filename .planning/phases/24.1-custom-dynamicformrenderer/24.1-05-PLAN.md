---
phase: 24.1
plan: 05
type: execute
wave: 4
depends_on: [24.1-04]
files_modified:
  - .planning/phases/24.1-custom-dynamicformrenderer/24.1-UAT.md
autonomous: false
requirements:
  - D-06
  - D-17
  - D-18
  - IR-01
  - IR-02
  - IR-04
  - IR-05
  - IR-07
  - IR-08
  - IR-09
must_haves:
  truths:
    - "전체 프론트엔드 빌드가 성공한다"
    - "admin FormPreview가 Phase 24 verification 4개 human 항목을 모두 통과한다"
    - "CUSTOM 양식 신규 작성→제출→목록→조회 end-to-end가 동작한다"
    - "양식 수정 후 기존 문서가 snapshot 기반으로 정상 표시된다"
    - "conditionalRules/calculationRules가 실시간으로 동작한다"
    - "table 행 추가/삭제가 동작하고 값이 저장/복원된다"
  artifacts:
    - path: ".planning/phases/24.1-custom-dynamicformrenderer/24.1-UAT.md"
      provides: "수동 검증 체크리스트와 결과 기록"
      contains: "UAT"
  key_links: []
threat_refs: []
---

<objective>
Phase 24.1 통합 검증. 타입 체크 + 빌드 green 확인 후, 관리자/사용자 양쪽 시나리오를 수동으로 검증하여 UAT 결과를 기록한다. Nyquist 검증 전략이 "수동 체크리스트 + tsc + build"이므로 이 plan이 phase 최종 게이트 역할을 한다.

Purpose: admin 회귀 방지 + CUSTOM end-to-end 동작 확인 + schemaSnapshot 라운드트립 증명.
Output: 24.1-UAT.md 체크리스트 기록.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/24.1-custom-dynamicformrenderer/24.1-CONTEXT.md
@.planning/phases/24.1-custom-dynamicformrenderer/24.1-RESEARCH.md
@.planning/phases/24.1-custom-dynamicformrenderer/24.1-VALIDATION.md
@.planning/phases/24-ui/24-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 자동 검증 - typecheck + build green + 필수 파일 존재 확인</name>
  <files>.planning/phases/24.1-custom-dynamicformrenderer/24.1-UAT.md</files>
  <read_first>
    - .planning/phases/24.1-custom-dynamicformrenderer/24.1-VALIDATION.md
    - .planning/phases/24-ui/24-VERIFICATION.md (회귀 검증 항목)
  </read_first>
  <action>
    다음 자동 검증을 순서대로 실행하고 결과를 `.planning/phases/24.1-custom-dynamicformrenderer/24.1-UAT.md`에 기록한다:

    1. `cd frontend && npx tsc --noEmit` → PASS/FAIL + 로그 저장
    2. `cd frontend && npm run build` → PASS/FAIL + dist 크기 기록
    3. 필수 신규 파일 존재 확인 (ls):
       - `frontend/src/features/document/components/dynamic/DynamicFieldRenderer.tsx`
       - `frontend/src/features/document/components/dynamic/DynamicTableField.tsx`
       - `frontend/src/features/document/components/dynamic/DynamicCustomForm.tsx`
       - `frontend/src/features/document/components/dynamic/DynamicCustomReadOnly.tsx`
       - `frontend/src/features/document/components/dynamic/adaptSchemaField.ts`
    4. 필수 grep 확인 (통합):
       - `grep -q "DYNAMIC_CUSTOM_EDIT" frontend/src/features/document/pages/DocumentEditorPage.tsx`
       - `grep -q "DYNAMIC_CUSTOM_READONLY" frontend/src/features/document/pages/DocumentDetailPage.tsx`
       - `grep -q "schemaDefinitionSnapshot" frontend/src/features/document/types/document.ts`

    `24.1-UAT.md`를 다음 구조로 생성:

    ```markdown
    # Phase 24.1 — UAT (User Acceptance Test)

    **Phase:** 24.1-custom-dynamicformrenderer
    **Executed:** YYYY-MM-DD

    ## 1. 자동 검증

    | Check | Command | Result | Notes |
    |-------|---------|--------|-------|
    | typecheck | `cd frontend && npx tsc --noEmit` | ⬜ | |
    | build | `cd frontend && npm run build` | ⬜ | |
    | DynamicFieldRenderer exists | ls | ⬜ | |
    | DynamicTableField exists | ls | ⬜ | |
    | DynamicCustomForm exists | ls | ⬜ | |
    | DynamicCustomReadOnly exists | ls | ⬜ | |
    | adaptSchemaField exists | ls | ⬜ | |
    | Editor fallback wired | grep | ⬜ | |
    | Detail fallback wired | grep | ⬜ | |
    | Type field added | grep | ⬜ | |

    ## 2. 수동 검증 체크리스트

    (Task 2에서 사용자가 체크)

    ## 3. 최종 사인오프

    - [ ] 모든 자동 검증 PASS
    - [ ] 모든 수동 검증 PASS
    - [ ] Phase 24.1 COMPLETE
    ```
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build && test -f ../.planning/phases/24.1-custom-dynamicformrenderer/24.1-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` exit 0
    - `cd frontend && npm run build` exit 0
    - `.planning/phases/24.1-custom-dynamicformrenderer/24.1-UAT.md` 파일 존재
    - UAT.md 1섹션의 모든 자동 검증이 PASS로 마킹됨
    - 5개 신규 컴포넌트 파일 모두 존재
  </acceptance_criteria>
  <done>자동 검증 green, UAT.md 스켈레톤 기록.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 수동 UAT — admin 회귀 + CUSTOM end-to-end</name>
  <what-built>
    Phase 24.1 전체 결과물:
    - DynamicFieldRenderer (7개 필드 타입)
    - DynamicTableField (행 추가/삭제)
    - DynamicCustomForm (편집 + 실시간 조건/계산)
    - DynamicCustomReadOnly (읽기 전용)
    - admin FormPreview의 DynamicFieldRenderer 통합
    - DocumentEditorPage/DocumentDetailPage의 CUSTOM fallback 분기
  </what-built>
  <how-to-verify>
    **A. admin FormPreview 회귀 (Phase 24 verification 4항목 재검증 — D-06):**

    1. 개발 서버 실행: `cd frontend && npm run dev`
    2. 관리자 로그인 → 양식 빌더 페이지 접근
    3. 기존 CUSTOM 양식(없으면 새로 하나 생성: text + number + select + table + conditionalRule + calculationRule 포함)
    4. 다음 4개 항목을 각각 확인:
       - [ ] **[24-V1] conditionalRules 동작:** 조건 필드 값 변경 시 대상 필드 show/hide 즉시 반영
       - [ ] **[24-V2] 미리보기 인터랙션:** 모든 필드에 입력 가능, 값이 보존됨
       - [ ] **[24-V3] 필드 삭제 토스트:** 필드 삭제 시 sonner 토스트 정상 표시
       - [ ] **[24-V4] 저장/로드 라운드트립:** 양식 저장 → 페이지 새로고침 → 필드 복원

    **B. CUSTOM 양식 사용자 시나리오 (IR-01 ~ IR-09):**

    5. 관리자에서 테스트 CUSTOM 양식 생성 및 저장 (예: code=`TEST_CUSTOM_01`)
    6. 일반 사용자로 로그아웃 → 로그인
    7. 양식 선택 → `TEST_CUSTOM_01` 선택 (또는 URL 직접 이동 `/documents/new/TEST_CUSTOM_01`)
    8. 확인 항목:
       - [ ] **[IR-01] DynamicCustomForm 렌더:** 모든 필드가 admin 미리보기와 동일하게 표시됨
       - [ ] **[IR-04] 실시간 conditionalRules:** 입력 시 즉시 show/hide/required 반영
       - [ ] **[IR-05] 실시간 calculationRules:** 입력 시 결과 필드 자동 갱신, 결과 필드는 disabled
       - [ ] **[IR-06] 유효성 검증:** 필수 필드 누락 시 제출 차단 + 인라인 에러
       - [ ] **[IR-07] table 행 추가/삭제:** `+ 행 추가` 버튼으로 추가, 삭제 버튼으로 제거, 값 보존
       - [ ] **[D-16] 숨김 필드 검증 제외:** 숨겨진 필수 필드가 제출 차단을 유발하지 않음
       - [ ] **[D-19] 숨김 값 보존:** 숨겼다가 다시 표시하면 이전 입력값 복원
    9. 제목 입력 → 결재선 선택 → 제출
    10. 문서 목록에서 제출한 문서 클릭 → DocumentDetailPage 진입
       - [ ] **[IR-02] DynamicCustomReadOnly 렌더:** 모든 값이 작성 시점 그대로 표시
       - [ ] **[D-22] readOnly에서도 숨김 필드 미표시:** 작성 시 숨겨진 필드는 표시 안 됨
       - [ ] **[IR-08] formData 라운드트립:** text, number, date, select, table 모든 값 정상 복원

    **C. Schema snapshot 정책 (IR-09):**

    11. 관리자로 돌아가 `TEST_CUSTOM_01` 양식의 필드 1개를 **삭제 또는 수정** 후 저장
    12. 일반 사용자로 돌아가 10단계에서 만든 문서 다시 조회
       - [ ] **[IR-09] snapshot 정상 표시:** 문서가 **양식 변경 전 상태**로 표시됨 (snapshot 유지)

    **D. 기존 하드코딩 템플릿 회귀 (Critical — 손상 방지):**

    13. `/documents/new/GENERAL` 접근
       - [ ] GeneralForm이 정상 렌더 (DynamicCustomForm으로 fallback되지 않음)
    14. 기존 GENERAL 문서 조회
       - [ ] GeneralReadOnly 정상 렌더

    **각 체크박스 결과를 `.planning/phases/24.1-custom-dynamicformrenderer/24.1-UAT.md`의 "2. 수동 검증 체크리스트" 섹션에 기록한다.**

    **실패 시:** 실패한 항목 번호와 재현 절차, 에러 메시지, 콘솔 로그를 UAT.md에 기록하고 `/gsd-plan-phase --gaps` 흐름으로 진행한다.
  </how-to-verify>
  <resume-signal>모든 항목 PASS 시 "approved" / 실패 시 항목 번호 + 증상 기술</resume-signal>
</task>

</tasks>

<threat_model>
이 plan은 검증 전용이며 코드 수정이 없으므로 신규 위협 없음. 참조: Plan 01~04의 threat model.
</threat_model>

<verification>
- 자동: tsc + build green
- 수동: Phase 24 회귀 4항목 + CUSTOM end-to-end 11항목 + snapshot 정책 1항목 + 하드코딩 회귀 2항목
</verification>

<success_criteria>
- [ ] tsc --noEmit exit 0
- [ ] npm run build exit 0
- [ ] UAT.md 체크리스트 모든 항목 PASS
- [ ] admin FormPreview 회귀 없음
- [ ] CUSTOM 양식 end-to-end 동작
- [ ] 기존 하드코딩 템플릿 회귀 없음
- [ ] schemaSnapshot 라운드트립 증명
</success_criteria>

<output>
After completion, create `.planning/phases/24.1-custom-dynamicformrenderer/24.1-05-SUMMARY.md` — 전체 phase retrospective 포함
</output>
