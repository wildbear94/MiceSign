---
phase: 24.1
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/src/features/document/types/document.ts
  - frontend/src/features/document/components/templates/templateRegistry.ts
autonomous: true
requirements:
  - D-08
  - D-09
  - D-11
  - IR-09
must_haves:
  truths:
    - "DocumentDetailResponse 타입에 schemaVersion과 schemaDefinitionSnapshot 필드가 존재한다"
    - "TemplateEditProps/TemplateReadOnlyProps가 schemaSnapshot을 전달할 수 있다"
    - "npm run build가 성공한다 (baseline green)"
  artifacts:
    - path: "frontend/src/features/document/types/document.ts"
      provides: "schemaVersion, schemaDefinitionSnapshot 필드가 추가된 DocumentDetailResponse"
      contains: "schemaDefinitionSnapshot"
    - path: "frontend/src/features/document/components/templates/templateRegistry.ts"
      provides: "schemaSnapshot을 포함한 TemplateEditProps/TemplateReadOnlyProps"
      contains: "schemaSnapshot"
  key_links:
    - from: "DocumentDetailResponse"
      to: "백엔드 DocumentDetailResponse.java"
      via: "schemaDefinitionSnapshot: string"
      pattern: "schemaDefinitionSnapshot"
threat_refs:
  - T-24.1-01
---

<objective>
Phase 24.1 기반 타입 확장 및 검증 전략 확정. 백엔드 DocumentDetailResponse에 이미 존재하는 schemaVersion / schemaDefinitionSnapshot 필드를 프론트엔드 TypeScript 타입에 추가하고, TemplateEditProps / TemplateReadOnlyProps 인터페이스에 schemaSnapshot 전달 채널을 확보한다. 후속 Wave의 모든 컴포넌트가 의존하는 계약을 먼저 고정한다.

Purpose: 인터페이스 우선(Interface-First) 원칙으로 downstream plan들이 타입 불일치 없이 병렬 진행 가능하도록 한다.
Output: 확장된 type 파일 + baseline build green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24.1-custom-dynamicformrenderer/24.1-CONTEXT.md
@.planning/phases/24.1-custom-dynamicformrenderer/24.1-RESEARCH.md
@.planning/phases/24.1-custom-dynamicformrenderer/24.1-VALIDATION.md
@frontend/src/features/document/types/document.ts
@frontend/src/features/document/components/templates/templateRegistry.ts

<interfaces>
기존 DocumentDetailResponse (frontend/src/features/document/types/document.ts:22-33) 구조 유지, 필드 2개만 추가.
백엔드 계약 (researcher 확인):
- schemaVersion?: number | null
- schemaDefinitionSnapshot?: string | null  // JSON string

기존 TemplateEditProps (templateRegistry.ts):
- documentId: number | null
- initialData?: { title: string; bodyHtml?: string; formData?: string }
- onSave: (data: { title: string; bodyHtml?: string; formData?: string }) => Promise<void>
- readOnly?: boolean

기존 TemplateReadOnlyProps:
- title: string
- bodyHtml?: string | null
- formData?: string | null
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DocumentDetailResponse에 schema snapshot 필드 추가</name>
  <files>frontend/src/features/document/types/document.ts</files>
  <read_first>
    - frontend/src/features/document/types/document.ts (전체)
    - .planning/phases/24.1-custom-dynamicformrenderer/24.1-RESEARCH.md (§10 API 응답 필드 확장 범위)
  </read_first>
  <action>
    frontend/src/features/document/types/document.ts의 `DocumentDetailResponse` 인터페이스에 다음 2개 필드를 추가한다 (기존 필드는 모두 유지):

    ```ts
    schemaVersion?: number | null;
    schemaDefinitionSnapshot?: string | null;
    ```

    위치: 기존 마지막 필드 뒤에 추가. JSDoc 주석으로 `/** 백엔드 DocumentContent.schemaVersion (CUSTOM 템플릿 전용) */`, `/** 백엔드 DocumentContent.schemaDefinitionSnapshot — JSON 직렬화된 SchemaDefinition 스냅샷 */`를 각각 필드 위에 단다.

    CreateDocumentRequest / UpdateDocumentRequest는 건드리지 않는다 (D-10 재해석: 백엔드가 자동 snapshot 저장).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tee /tmp/24.1-01-1-tsc.log && grep -q "schemaDefinitionSnapshot" src/features/document/types/document.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "schemaVersion?: number" frontend/src/features/document/types/document.ts` 출력 1건 이상
    - `grep -n "schemaDefinitionSnapshot?: string" frontend/src/features/document/types/document.ts` 출력 1건 이상
    - `cd frontend && npx tsc --noEmit` exit 0
    - DocumentResponse(목록용)는 변경되지 않음 — `git diff frontend/src/features/document/types/document.ts` 가 DocumentDetailResponse 섹션만 변경
  </acceptance_criteria>
  <done>두 필드가 DocumentDetailResponse에 존재하고 tsc --noEmit 통과.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: TemplateEditProps/TemplateReadOnlyProps에 schemaSnapshot prop 추가 + baseline build</name>
  <files>
    frontend/src/features/document/components/templates/templateRegistry.ts
  </files>
  <read_first>
    - frontend/src/features/document/components/templates/templateRegistry.ts (전체)
    - frontend/src/features/document/components/templates/GeneralForm.tsx (TemplateEditProps 사용 패턴)
    - frontend/src/features/document/components/templates/GeneralReadOnly.tsx
    - .planning/phases/24.1-custom-dynamicformrenderer/24.1-RESEARCH.md (§7, §10, Implementation Approach 타입 확장)
  </read_first>
  <action>
    frontend/src/features/document/components/templates/templateRegistry.ts 수정:

    1. `TemplateEditProps` 인터페이스의 `initialData` 타입을 다음과 같이 확장:
       ```ts
       initialData?: {
         title: string;
         bodyHtml?: string;
         formData?: string;
         schemaSnapshot?: string | null;  // CUSTOM 템플릿 전용 — DocumentDetailResponse.schemaDefinitionSnapshot 값
       };
       ```

    2. `TemplateReadOnlyProps` 인터페이스에 다음 필드 추가:
       ```ts
       schemaSnapshot?: string | null;
       ```

    3. 기존 6개 하드코딩 템플릿(GENERAL/EXPENSE/LEAVE/PURCHASE/BUSINESS_TRIP/OVERTIME) 컴포넌트는 이 필드를 사용하지 않으므로 추가 수정 불필요 (optional이므로 호환).

    4. 마지막에 전체 frontend 빌드 실행하여 baseline green 확인:
       ```
       cd frontend && npm run build
       ```
       실패 시 Phase 24 잔존 에러일 수 있으므로 로그를 /tmp/24.1-01-2-build.log에 저장 후 멈춘다.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tee /tmp/24.1-01-2-build.log && grep -q "schemaSnapshot" src/features/document/components/templates/templateRegistry.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "schemaSnapshot" frontend/src/features/document/components/templates/templateRegistry.ts` 출력 2 이상 (TemplateEditProps + TemplateReadOnlyProps)
    - `cd frontend && npm run build` exit 0 (baseline green)
    - 기존 6개 form 컴포넌트 파일은 변경되지 않음 — `git diff --stat frontend/src/features/document/components/templates/` 결과에 templateRegistry.ts 만 포함
  </acceptance_criteria>
  <done>TemplateEditProps.initialData.schemaSnapshot과 TemplateReadOnlyProps.schemaSnapshot 존재. npm run build 성공.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 백엔드 API → 프론트엔드 | DocumentDetailResponse.schemaDefinitionSnapshot은 JSON 문자열. 프론트엔드가 JSON.parse 해 SchemaDefinition으로 사용. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24.1-01 | Tampering | DocumentDetailResponse.schemaDefinitionSnapshot (JSON string) | accept | 이 plan에서는 타입 선언만 추가. JSON.parse 방어는 downstream plan(DynamicCustomForm 로딩 시 try/catch)에서 처리. 백엔드 snapshot은 서버가 생성하므로 신뢰 경계 내. |
</threat_model>

<verification>
- `npx tsc --noEmit` 통과
- `npm run build` 통과
- 두 타입 파일만 수정, 기존 컴포넌트 import 영향 없음
</verification>

<success_criteria>
- [ ] DocumentDetailResponse에 schemaVersion, schemaDefinitionSnapshot 필드 존재
- [ ] TemplateEditProps.initialData.schemaSnapshot 존재
- [ ] TemplateReadOnlyProps.schemaSnapshot 존재
- [ ] npm run build baseline green
</success_criteria>

<output>
After completion, create `.planning/phases/24.1-custom-dynamicformrenderer/24.1-01-SUMMARY.md`
</output>
